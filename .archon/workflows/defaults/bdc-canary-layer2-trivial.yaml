name: bdc-canary-layer2-trivial
description: Event-triggered build-through fixture for Cauldron Layer 2.
provider: codex
model: gpt-5.5
target_repo: bluedevilcollectibles/bdc-harness
mutates_checkout: false
nodes:
  - id: trivial-build
    prompt: |
      Create a minimal no-op verification response for the Cauldron Layer 2 fixture.
      Do not edit files. Reply with exactly: OK
    provider: codex
    model: gpt-5.5
