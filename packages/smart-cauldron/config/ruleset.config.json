{
  "_comment": "Smart Cauldron conductor ruleset. Rules are evaluated top-down; first match wins (ANY-of match on tags). defaultEntry fires when no rule matches. woClass/tags are supplied entirely by the CLI caller via --class/--tags (smart-cauldron does not parse WO frontmatter itself) -- a WO 'acquires' a tag only if the caller passes it explicitly on the command line.",
  "defaultEntry": "glm",
  "rules": [
    {
      "match": { "tags": ["billing", "money", "payment", "stripe"] },
      "entry": "claude"
    },
    {
      "match": { "tags": ["auth", "oauth", "security"] },
      "entry": "claude"
    },
    {
      "match": { "woClass": "INFRA" },
      "entry": "claude"
    },
    {
      "match": { "tags": ["feature", "net-new", "multi-surface"] },
      "entry": "codex"
    },
    {
      "match": { "woClass": "CODE", "tags": ["mechanical"] },
      "entry": "glm"
    },
    {
      "match": { "woClass": "CODE" },
      "entry": "glm"
    }
  ]
}
