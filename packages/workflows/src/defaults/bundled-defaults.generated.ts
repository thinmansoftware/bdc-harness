/**
 * AUTO-GENERATED — DO NOT EDIT.
 *
 * Regenerate with: bun run generate:bundled
 * Verify up-to-date:  bun run check:bundled
 *
 * Source of truth:
 *   .archon/commands/defaults/*.md
 *   .archon/workflows/defaults/*.{yaml,yml}
 *   harness/policies/*.md
 *
 * Contents are inlined as plain string literals (JSON-escaped) so this
 * module loads in both Bun and Node. Previous versions used
 * `import X from '...' with { type: 'text' }` which is Bun-specific.
 */

// Bundled default commands (36 total)
export const BUNDLED_COMMANDS: Record<string, string> = {
  "archon-assist": "---\ndescription: General assistance - questions, debugging, one-off tasks, exploration\nargument-hint: <any request>\n---\n\n# Assist Mode\n\n**Request**: $ARGUMENTS\n\n---\n\nYou are helping with a request that didn't match a specific workflow.\n\n## Instructions\n\n1. **Understand the request** - What is the user actually asking for?\n2. **Take action** - Use your full Claude Code capabilities to help\n3. **Be helpful** - Answer questions, debug issues, explore code, make changes\n4. **Note the gap** - If this should have been a specific workflow, mention it:\n   \"Note: Using assist mode. Consider creating a workflow for this use case.\"\n\n## Capabilities\n\nYou have full Claude Code capabilities:\n- Read and write files\n- Run commands\n- Search the codebase\n- Make code changes\n- Answer questions\n\n## Request\n\n$ARGUMENTS\n",
  "archon-auto-fix-review": "---\ndescription: Auto-fix all review findings unless clear YAGNI violations, post fix report\nargument-hint: (none - reads all review artifacts from $ARTIFACTS_DIR/review/)\n---\n\n# Auto-Fix Review Findings\n\n---\n\n## IMPORTANT: Output Behavior\n\n**Your output will be posted as a GitHub comment.** Keep working output minimal:\n- Do NOT narrate each step\n- Do NOT output verbose progress updates\n- Only output the final structured report at the end\n- Use the TodoWrite tool to track progress silently\n\n---\n\n## Your Mission\n\nRead all review artifacts produced in this workflow run and fix everything surfaced — unless a finding is a clear YAGNI violation or speculative over-engineering beyond the scope of the original fix. Validate, commit, push, write an artifact, and post a GitHub comment explaining what was fixed and why anything was skipped.\n\n**Output artifact**: `$ARTIFACTS_DIR/review/fix-report.md`\n**Git action**: Commit AND push fixes to the PR branch\n**GitHub action**: Post fix report as a comment on the PR\n\n---\n\n## Phase 1: LOAD — Get Context\n\n### 1.1 Get PR Number and Branch\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number)\nHEAD_BRANCH=$(gh pr view $PR_NUMBER --json headRefName --jq '.headRefName')\necho \"PR: $PR_NUMBER, Branch: $HEAD_BRANCH\"\n```\n\n### 1.2 Checkout PR Branch\n\n**Always re-checkout to ensure you are on the right branch.**\n\n```bash\ngit fetch origin $HEAD_BRANCH\ngit checkout $HEAD_BRANCH\ngit pull origin $HEAD_BRANCH\n```\n\nVerify:\n\n```bash\ngit branch --show-current\ngit status --porcelain\n```\n\n### 1.3 Read All Review Artifacts\n\nDiscover whatever review artifacts exist — there may be one or many depending on which review agents ran:\n\n```bash\nls $ARTIFACTS_DIR/review/\n```\n\nRead each `.md` file that looks like a findings artifact (e.g. `code-review-findings.md`, `error-handling-findings.md`, `test-coverage-findings.md`, `docs-impact-findings.md`, `consolidated-review.md`). Skip non-findings files like `scope.md` and `fix-report.md`.\n\n```bash\nfor f in $ARTIFACTS_DIR/review/*.md; do\n  echo \"=== $f ===\"; cat \"$f\"; echo\ndone\n```\n\n### 1.4 Extract Findings\n\nFrom all loaded artifacts, compile a unified list of all findings with their severity, location, and suggested fix.\n\n**PHASE_1_CHECKPOINT:**\n- [ ] PR number and branch identified\n- [ ] On correct PR branch\n- [ ] All review artifacts read\n- [ ] All findings extracted\n\n---\n\n## Phase 2: TRIAGE — Decide What to Fix\n\nFor each finding, decide: **FIX** or **SKIP**.\n\n### Fix if:\n- It is a real bug, type error, silent failure, or clear code quality issue\n- The fix is concrete and low-risk\n\n### Skip (YAGNI / out-of-scope) if the finding recommends:\n- Adding something not required to fix the original issue (new config options, new abstractions, speculative fallbacks, \"what if\" edge cases)\n- Refactoring or restructuring code that isn't broken\n- Adding validation for inputs that cannot actually be invalid in this context\n- Extracting utilities or helpers for code that currently has only one caller\n- Architectural changes that touch code well outside the PR's scope\n\nUse judgment — don't be overly restrictive. If it's a legitimate bug the reviewer found, fix it even if it's adjacent to the PR. If it's clearly speculative (\"this might be useful someday\"), skip it.\n\nFor each skipped finding, write down **the specific reason** — this goes in the report.\n\n**PHASE_2_CHECKPOINT:**\n- [ ] Every finding marked FIX or SKIP\n- [ ] Skip reasons documented\n\n---\n\n## Phase 3: IMPLEMENT — Apply Fixes\n\n### 3.1 For Each Finding Marked FIX\n\n1. Read the relevant file(s)\n2. Apply the fix following the suggested approach from the review artifact\n3. Run type-check after each fix: `bun run type-check`\n4. Note exactly what was changed\n\n### 3.2 Handle Unfixable Findings\n\nIf a fix cannot be applied (code changed since review, fix is ambiguous, fix would break other things), mark it as **BLOCKED** and document why. Do not force a broken fix.\n\n### 3.3 Add Tests for Fixed Code\n\nIf the review flagged missing test coverage for something you just fixed, add a targeted test. Run it:\n\n```bash\nbun test {file}\n```\n\n**PHASE_3_CHECKPOINT:**\n- [ ] All FIX findings attempted\n- [ ] Tests added where flagged\n- [ ] BLOCKED findings documented\n\n---\n\n## Phase 4: VALIDATE — Full Check\n\n```bash\nbun run type-check\nbun run lint\nbun test\n```\n\nAll must pass. If something fails after a fix:\n1. Review the error\n2. Adjust the fix or revert it and mark BLOCKED\n3. Re-run until clean\n\n**PHASE_4_CHECKPOINT:**\n- [ ] Type check passes\n- [ ] Lint passes\n- [ ] Tests pass\n\n---\n\n## Phase 5: COMMIT AND PUSH\n\n### 5.1 Stage and Commit\n\nOnly stage files you actually changed:\n\n```bash\ngit add {specific files}\ngit status\ngit commit -m \"fix: address review findings\n\n$(echo \"Fixed:\"; echo \"- {brief list}\")\n$(echo \"\"; echo \"Skipped (YAGNI/out-of-scope):\"; echo \"- {brief list if any}\")\"\n```\n\n### 5.2 Push\n\n```bash\ngit push origin $HEAD_BRANCH\n```\n\nIf push fails due to divergence:\n\n```bash\ngit pull --rebase origin $HEAD_BRANCH\ngit push origin $HEAD_BRANCH\n```\n\n**PHASE_5_CHECKPOINT:**\n- [ ] Changes committed\n- [ ] Pushed to PR branch\n\n---\n\n## Phase 6: GENERATE — Write Fix Report\n\nWrite to `$ARTIFACTS_DIR/review/fix-report.md`:\n\n```markdown\n# Fix Report: PR #{number}\n\n**Date**: {ISO timestamp}\n**Status**: COMPLETE | PARTIAL\n**Branch**: {HEAD_BRANCH}\n**Commit**: {commit hash}\n\n---\n\n## Summary\n\n{2-3 sentences covering what was found, what was fixed, what was skipped and why}\n\n---\n\n## Fixes Applied\n\n| Severity | Finding | Location | What Was Done |\n|----------|---------|----------|---------------|\n| CRITICAL | {title} | `file:line` | {description} |\n| HIGH     | {title} | `file:line` | {description} |\n\n---\n\n## Skipped Findings\n\n| Severity | Finding | Location | Reason Skipped |\n|----------|---------|----------|----------------|\n| HIGH     | {title} | `file:line` | YAGNI: {specific reason} |\n| MEDIUM   | {title} | `file:line` | Out of scope: {reason} |\n\n---\n\n## Tests Added\n\n| File | Test Cases |\n|------|------------|\n| `{file}.test.ts` | `{test description}` |\n\n*(none)* if no tests were added\n\n---\n\n## Blocked (Could Not Fix)\n\n| Severity | Finding | Reason |\n|----------|---------|--------|\n| {sev}    | {title} | {why it could not be applied} |\n\n*(none)* if nothing was blocked\n\n---\n\n## Validation\n\n| Check | Status |\n|-------|--------|\n| Type check | ✅ / ❌ |\n| Lint | ✅ / ❌ |\n| Tests | ✅ {n} passed / ❌ |\n```\n\n**PHASE_6_CHECKPOINT:**\n- [ ] Fix report written\n\n---\n\n## Phase 7: POST — GitHub Comment\n\nPost the fix report as a PR comment:\n\n```bash\ngh pr comment $PR_NUMBER --body \"$(cat <<'EOF'\n## ⚡ Auto-Fix Report\n\n**Status**: {COMPLETE | PARTIAL}\n**Pushed**: ✅ Changes pushed to `{HEAD_BRANCH}`\n\n---\n\n### Fixes Applied\n\n| Severity | Finding | Location |\n|----------|---------|----------|\n| 🔴 CRITICAL | {title} | `file:line` |\n| 🟠 HIGH | {title} | `file:line` |\n\n*(none)* if nothing was fixed\n\n---\n\n### Skipped\n\n| Severity | Finding | Reason |\n|----------|---------|--------|\n| 🟠 HIGH | {title} | {reason — YAGNI, out of scope, blocked} |\n\n*(none)* if nothing was skipped\n\n---\n\n### Tests Added\n\n{List or \"(none)\"}\n\n---\n\n### Validation\n\n✅ Type check | ✅ Lint | ✅ Tests ({n} passed)\n\n---\n\n*Auto-fix by Archon · fixes pushed to `{HEAD_BRANCH}`*\nEOF\n)\"\n```\n\n**PHASE_7_CHECKPOINT:**\n- [ ] GitHub comment posted\n\n---\n\n## Phase 8: OUTPUT — Final Summary\n\nOutput only this:\n\n```\n## ⚡ Auto-Fix Complete\n\n**PR**: #{number}\n**Branch**: {HEAD_BRANCH}\n**Status**: COMPLETE | PARTIAL\n\nFixed: {n}\nSkipped: {n} (YAGNI/out-of-scope)\nBlocked: {n}\n\nValidation: ✅ All checks pass\nPushed: ✅\n\nFix report: $ARTIFACTS_DIR/review/fix-report.md\n```\n\n---\n\n## Error Handling\n\n### Type check fails after a fix\n1. Review the error\n2. Adjust or revert the fix\n3. If still failing after a reasonable attempt, mark BLOCKED\n\n### Tests fail\n1. Check whether the fix caused it or it was pre-existing\n2. Fix the test if the fix is correct\n3. If unclear, mark BLOCKED — do not ship broken tests\n\n### Push fails\n1. `git pull --rebase origin $HEAD_BRANCH`\n2. Resolve conflicts if any\n3. Push again\n\n### No review artifacts found\n```\n❌ No review artifacts found in $ARTIFACTS_DIR/review/\nCannot proceed without findings.\n```\n\n---\n\n## Success Criteria\n\n- **ON_CORRECT_BRANCH**: Working on PR's head branch\n- **ALL_FINDINGS_ADDRESSED**: Every finding is either fixed, skipped (with reason), or blocked (with reason)\n- **VALIDATION_PASSED**: Type check, lint, and tests all pass\n- **COMMITTED_AND_PUSHED**: Changes committed and pushed to PR branch\n- **REPORTED**: Fix report artifact written and GitHub comment posted\n",
  "archon-code-review-agent": "---\ndescription: Review code quality, CLAUDE.md compliance, and detect bugs\nargument-hint: (none - reads from scope artifact)\n---\n\n# Code Review Agent\n\n---\n\n## Your Mission\n\nReview the PR for code quality, CLAUDE.md compliance, patterns, and bugs. Produce a structured artifact with findings, fix suggestions with multiple options, and reasoning.\n\n**Output artifact**: `$ARTIFACTS_DIR/review/code-review-findings.md`\n\n---\n\n## Phase 1: LOAD - Get Context\n\n### 1.1 Get PR Number from Registry\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number)\n```\n\n### 1.2 Read Scope\n\n```bash\ncat $ARTIFACTS_DIR/review/scope.md\n```\n\nNote:\n- Changed files list\n- CLAUDE.md rules to check\n- Focus areas\n\n**CRITICAL**: Check for \"NOT Building (Scope Limits)\" section. Items listed there are **intentionally excluded** - do NOT flag them as bugs or missing features!\n\n### 1.3 Get PR Diff\n\n```bash\ngh pr diff {number}\n```\n\n### 1.4 Read CLAUDE.md\n\n```bash\ncat CLAUDE.md\n```\n\nNote all coding standards, patterns, and rules.\n\n**PHASE_1_CHECKPOINT:**\n- [ ] PR number identified\n- [ ] Scope loaded\n- [ ] Diff available\n- [ ] CLAUDE.md rules noted\n\n---\n\n## Phase 2: ANALYZE - Review Code\n\n### 2.1 Check CLAUDE.md Compliance\n\nFor each changed file, verify:\n- Import patterns match project style\n- Naming conventions followed\n- Error handling patterns correct\n- Type annotations complete\n- Testing patterns followed\n\n### 2.2 Detect Bugs\n\nLook for:\n- Logic errors\n- Null/undefined handling issues\n- Race conditions\n- Memory leaks\n- Security vulnerabilities\n- Off-by-one errors\n- Missing error handling\n\n### 2.3 Check Code Quality\n\nEvaluate:\n- Code duplication\n- Function complexity\n- Proper abstractions\n- Clear naming\n- Appropriate comments\n\n### 2.4 Pattern Matching\n\nFor each issue found, search codebase for correct patterns:\n\n```bash\n# Find similar patterns in codebase\ngrep -r \"pattern\" src/ --include=\"*.ts\" | head -5\n```\n\n### 2.5 Check for Primitive Duplication\n\nFor each new interface, class, type alias, or utility module introduced in the diff:\n\n1. Search for similar existing abstractions:\n\n```bash\n# Replace {Name} with the new abstraction's name\ngrep -r \"interface {Name}\\|class {Name}\\|type {Name}\" packages/ --include=\"*.ts\" | head -10\n```\n\n2. Flag if the new abstraction duplicates or closely overlaps an existing one.\n3. Flag if a new utility function reimplements logic already available in a shared package.\n4. Note findings in the CLAUDE.md Compliance section with verdict: **EXTENDS** (extends existing primitive) or **DUPLICATE** (redundant with existing) or **NEW** (genuinely new, no existing primitive).\n\n**PHASE_2_CHECKPOINT:**\n- [ ] CLAUDE.md compliance checked\n- [ ] Bugs identified\n- [ ] Quality issues noted\n- [ ] Patterns found for fixes\n- [ ] Primitive duplication checked\n\n---\n\n## Phase 3: GENERATE - Create Artifact\n\nWrite to `$ARTIFACTS_DIR/review/code-review-findings.md`:\n\n```markdown\n# Code Review Findings: PR #{number}\n\n**Reviewer**: code-review-agent\n**Date**: {ISO timestamp}\n**Files Reviewed**: {count}\n\n---\n\n## Summary\n\n{2-3 sentence overview of code quality and main concerns}\n\n**Verdict**: {APPROVE | REQUEST_CHANGES | NEEDS_DISCUSSION}\n\n---\n\n## Findings\n\n### Finding 1: {Descriptive Title}\n\n**Severity**: CRITICAL | HIGH | MEDIUM | LOW\n**Category**: bug | style | performance | security | pattern-violation\n**Location**: `{file}:{line}`\n\n**Issue**:\n{Clear description of what's wrong}\n\n**Evidence**:\n```typescript\n// Current code at {file}:{line}\n{problematic code snippet}\n```\n\n**Why This Matters**:\n{Explain the impact - what could go wrong, why it violates standards}\n\n---\n\n#### Fix Suggestions\n\n| Option | Approach | Pros | Cons |\n|--------|----------|------|------|\n| A | {approach description} | {benefits} | {drawbacks} |\n| B | {alternative approach} | {benefits} | {drawbacks} |\n\n**Recommended**: Option {A/B}\n\n**Reasoning**:\n{Explain why this option is preferred, referencing:\n- Codebase patterns\n- CLAUDE.md rules\n- Best practices\n- Specific project context}\n\n**Recommended Fix**:\n```typescript\n// Suggested fix\n{corrected code}\n```\n\n**Codebase Pattern Reference**:\n```typescript\n// SOURCE: {file}:{lines}\n// This pattern shows how similar code is handled elsewhere\n{existing code from codebase}\n```\n\n---\n\n### Finding 2: {Title}\n\n{Same structure...}\n\n---\n\n## Statistics\n\n| Severity | Count | Auto-fixable |\n|----------|-------|--------------|\n| CRITICAL | {n} | {n} |\n| HIGH | {n} | {n} |\n| MEDIUM | {n} | {n} |\n| LOW | {n} | {n} |\n\n---\n\n## CLAUDE.md Compliance\n\n| Rule | Status | Notes |\n|------|--------|-------|\n| {rule from CLAUDE.md} | PASS/FAIL | {details} |\n| ... | ... | ... |\n\n---\n\n## Patterns Referenced\n\n| File | Lines | Pattern |\n|------|-------|---------|\n| `src/example.ts` | 42-50 | {what this pattern demonstrates} |\n| ... | ... | ... |\n\n---\n\n## Positive Observations\n\n{List things done well - good patterns, clean code, etc.}\n\n---\n\n## Metadata\n\n- **Agent**: code-review-agent\n- **Timestamp**: {ISO timestamp}\n- **Artifact**: `$ARTIFACTS_DIR/review/code-review-findings.md`\n```\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Artifact file created\n- [ ] All findings have severity and location\n- [ ] Fix options provided with reasoning\n- [ ] Codebase patterns referenced\n\n---\n\n## Phase 4: VALIDATE - Check Artifact\n\n### 4.1 Verify File Exists\n\n```bash\ncat $ARTIFACTS_DIR/review/code-review-findings.md | head -20\n```\n\n### 4.2 Check Structure\n\nVerify artifact contains:\n- Summary with verdict\n- At least findings section (even if empty)\n- Statistics table\n- CLAUDE.md compliance table\n\n**PHASE_4_CHECKPOINT:**\n- [ ] Artifact file exists\n- [ ] Structure is complete\n- [ ] No placeholder text remaining\n\n---\n\n## Success Criteria\n\n- **CONTEXT_LOADED**: Scope and diff read successfully\n- **ANALYSIS_COMPLETE**: All changed files reviewed\n- **ARTIFACT_CREATED**: Findings file written\n- **PATTERNS_INCLUDED**: Each finding references codebase patterns\n- **OPTIONS_PROVIDED**: Multiple fix options where applicable\n",
  "archon-comment-quality-agent": "---\ndescription: Review code comments for accuracy, completeness, and maintainability\nargument-hint: (none - reads from scope artifact)\n---\n\n# Comment Quality Agent\n\n---\n\n## Your Mission\n\nAnalyze code comments for accuracy against actual code, identify comment rot, check documentation completeness, and ensure comments aid long-term maintainability. Produce a structured artifact with findings and recommendations.\n\n**Output artifact**: `$ARTIFACTS_DIR/review/comment-quality-findings.md`\n\n---\n\n## Phase 1: LOAD - Get Context\n\n### 1.1 Get PR Number from Registry\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number)\n```\n\n### 1.2 Read Scope\n\n```bash\ncat $ARTIFACTS_DIR/review/scope.md\n```\n\n**CRITICAL**: Check for \"NOT Building (Scope Limits)\" section. Items listed there are **intentionally excluded** - do NOT flag them as missing documentation or comment issues!\n\n### 1.3 Get PR Diff\n\n```bash\ngh pr diff {number}\n```\n\nFocus on:\n- New comments added\n- Comments near modified code\n- JSDoc/docstrings added or changed\n\n**PHASE_1_CHECKPOINT:**\n- [ ] PR number identified\n- [ ] Changed files with comments identified\n- [ ] Diff available\n\n---\n\n## Phase 2: ANALYZE - Review Comments\n\n### 2.1 Check Comment Accuracy\n\nFor each comment in changed code:\n- Does the comment accurately describe what the code does?\n- Is the comment up-to-date with the implementation?\n- Are parameter descriptions correct?\n- Are return value descriptions accurate?\n- Are edge cases documented correctly?\n\n### 2.2 Identify Comment Rot\n\nLook for:\n- Comments that describe old behavior\n- TODO/FIXME that should have been addressed\n- Outdated references (old file names, removed functions)\n- Comments that contradict the code\n\n### 2.3 Check Documentation Completeness\n\nEvaluate:\n- Are complex functions properly documented?\n- Are public APIs documented?\n- Are non-obvious algorithms explained?\n- Are magic numbers/constants explained?\n- Are important decisions documented?\n\n### 2.4 Assess Maintainability\n\nConsider:\n- Will future developers understand the \"why\"?\n- Are there redundant comments (just restating code)?\n- Is the signal-to-noise ratio good?\n- Are comments in the right places?\n\n**PHASE_2_CHECKPOINT:**\n- [ ] Comment accuracy verified\n- [ ] Comment rot identified\n- [ ] Completeness gaps found\n- [ ] Maintainability assessed\n\n---\n\n## Phase 3: GENERATE - Create Artifact\n\nWrite to `$ARTIFACTS_DIR/review/comment-quality-findings.md`:\n\n```markdown\n# Comment Quality Findings: PR #{number}\n\n**Reviewer**: comment-quality-agent\n**Date**: {ISO timestamp}\n**Comments Reviewed**: {count}\n\n---\n\n## Summary\n\n{2-3 sentence overview of comment quality}\n\n**Verdict**: {APPROVE | REQUEST_CHANGES | NEEDS_DISCUSSION}\n\n---\n\n## Findings\n\n### Finding 1: {Descriptive Title}\n\n**Severity**: CRITICAL | HIGH | MEDIUM | LOW\n**Category**: inaccurate | outdated | missing | redundant | misleading\n**Location**: `{file}:{line}`\n\n**Issue**:\n{Clear description of the comment problem}\n\n**Current Comment**:\n```typescript\n// {the problematic comment}\n{code the comment describes}\n```\n\n**Actual Code Behavior**:\n{What the code actually does vs what comment says}\n\n**Impact**:\n{How this could mislead future developers}\n\n---\n\n#### Fix Suggestions\n\n| Option | Approach | Pros | Cons |\n|--------|----------|------|------|\n| A | {update comment} | {benefits} | {drawbacks} |\n| B | {remove comment} | {benefits} | {drawbacks} |\n| C | {expand comment} | {benefits} | {drawbacks} |\n\n**Recommended**: Option {X}\n\n**Reasoning**:\n{Why this option:\n- Matches documentation standards\n- Provides value without being redundant\n- Will remain accurate over time}\n\n**Recommended Fix**:\n```typescript\n/**\n * {corrected/improved comment}\n *\n * @param {type} param - {accurate description}\n * @returns {type} - {accurate description}\n */\n{code}\n```\n\n**Good Comment Pattern**:\n```typescript\n// SOURCE: {file}:{lines}\n// Example of good documentation in this codebase\n{existing well-documented code}\n```\n\n---\n\n### Finding 2: {Title}\n\n{Same structure...}\n\n---\n\n## Comment Audit\n\n| Location | Type | Accurate | Up-to-date | Useful | Verdict |\n|----------|------|----------|------------|--------|---------|\n| `file:line` | JSDoc | YES/NO | YES/NO | YES/NO | GOOD/UPDATE/REMOVE |\n| ... | ... | ... | ... | ... | ... |\n\n---\n\n## Statistics\n\n| Severity | Count | Auto-fixable |\n|----------|-------|--------------|\n| CRITICAL | {n} | {n} |\n| HIGH | {n} | {n} |\n| MEDIUM | {n} | {n} |\n| LOW | {n} | {n} |\n\n---\n\n## Documentation Gaps\n\n| Code Area | What's Missing | Priority |\n|-----------|----------------|----------|\n| `function xyz()` | Parameter docs, return type | HIGH |\n| `class Abc` | Class purpose, usage example | MEDIUM |\n| ... | ... | ... |\n\n---\n\n## Comment Rot Found\n\n| Location | Comment Says | Code Does | Age |\n|----------|--------------|-----------|-----|\n| `file:line` | \"{old description}\" | {actual behavior} | {when introduced} |\n| ... | ... | ... | ... |\n\n---\n\n## Positive Observations\n\n{Well-documented code, helpful comments, good explanations}\n\n---\n\n## Metadata\n\n- **Agent**: comment-quality-agent\n- **Timestamp**: {ISO timestamp}\n- **Artifact**: `$ARTIFACTS_DIR/review/comment-quality-findings.md`\n```\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Artifact file created\n- [ ] Comment accuracy verified\n- [ ] Comment rot documented\n- [ ] Documentation gaps listed\n\n---\n\n## Success Criteria\n\n- **COMMENTS_AUDITED**: All comments in changed code reviewed\n- **ACCURACY_CHECKED**: Comments verified against actual code\n- **ROT_IDENTIFIED**: Outdated comments found\n- **GAPS_DOCUMENTED**: Missing documentation noted\n",
  "archon-confirm-plan": "---\ndescription: Verify plan research is still valid - check patterns exist, code hasn't drifted\nargument-hint: (no arguments - reads from workflow artifacts)\n---\n\n# Confirm Plan Research\n\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Your Mission\n\nVerify that the plan's research is still valid before implementation begins.\n\nPlans can become stale:\n- Files may have been renamed or moved\n- Code patterns may have changed\n- APIs may have been updated\n\n**This step does NOT implement anything** - it only validates the plan is still accurate.\n\n---\n\n## Phase 1: LOAD - Read Context Artifact\n\n### 1.1 Load Plan Context\n\n```bash\ncat $ARTIFACTS_DIR/plan-context.md\n```\n\nIf not found, STOP with error:\n```\n❌ Plan context not found at $ARTIFACTS_DIR/plan-context.md\n\nRun archon-plan-setup first.\n```\n\n### 1.2 Extract Verification Targets\n\nFrom the context, identify:\n\n1. **Patterns to Mirror** - Files and line ranges to verify\n2. **Files to Change** - Files that will be created/updated\n3. **Validation Commands** - Commands that should work\n\n**PHASE_1_CHECKPOINT:**\n\n- [ ] Context artifact loaded\n- [ ] Patterns to verify extracted\n- [ ] Files to change identified\n\n---\n\n## Phase 2: VERIFY - Check Patterns Exist\n\n### 2.1 Verify Pattern Files\n\nFor each file in \"Patterns to Mirror\":\n\n1. Check if file exists:\n   ```bash\n   test -f {file-path} && echo \"EXISTS\" || echo \"MISSING\"\n   ```\n\n2. If exists, read the referenced lines:\n   ```bash\n   sed -n '{start},{end}p' {file-path}\n   ```\n\n3. Compare with what the plan expected (if plan included code snippets)\n\n### 2.2 Document Findings\n\nFor each pattern file:\n\n| File | Status | Notes |\n|------|--------|-------|\n| `src/adapters/telegram.ts` | ✅ EXISTS | Lines 11-23 match expected pattern |\n| `src/types/index.ts` | ✅ EXISTS | Interface still present |\n| `src/old-file.ts` | ❌ MISSING | File was renamed/deleted |\n| `src/changed.ts` | ⚠️ DRIFTED | Code structure changed significantly |\n\n### 2.3 Severity Assessment\n\n| Finding | Severity | Action |\n|---------|----------|--------|\n| File exists, code matches | ✅ OK | Proceed |\n| File exists, minor differences | ⚠️ WARNING | Note in artifact, proceed with caution |\n| File exists, major drift | 🟠 CONCERN | Flag for review, may need plan update |\n| File missing | ❌ BLOCKER | Stop, plan needs revision |\n\n**PHASE_2_CHECKPOINT:**\n\n- [ ] All pattern files checked\n- [ ] Findings documented\n- [ ] Severity assessed\n\n---\n\n## Phase 3: VERIFY - Check Target Locations\n\n### 3.1 Check Files to Create\n\nFor each file marked CREATE:\n\n1. Verify it doesn't already exist (would be unexpected):\n   ```bash\n   test -f {file-path} && echo \"ALREADY EXISTS\" || echo \"OK - will create\"\n   ```\n\n2. Verify parent directory exists or can be created:\n   ```bash\n   dirname {file-path} | xargs test -d && echo \"DIR EXISTS\" || echo \"DIR WILL BE CREATED\"\n   ```\n\n### 3.2 Check Files to Update\n\nFor each file marked UPDATE:\n\n1. Verify it exists:\n   ```bash\n   test -f {file-path} && echo \"EXISTS\" || echo \"MISSING\"\n   ```\n\n2. If the plan references specific lines/functions, verify they exist\n\n**PHASE_3_CHECKPOINT:**\n\n- [ ] CREATE targets verified (don't exist yet)\n- [ ] UPDATE targets verified (do exist)\n\n---\n\n## Phase 4: VERIFY - Check Validation Commands\n\n### 4.1 Dry Run Validation Commands\n\nTest that the validation commands work (without expecting them to pass):\n\n```bash\n# Check type-check command exists\nbun run type-check --help 2>/dev/null || echo \"type-check not available\"\n\n# Check lint command exists\nbun run lint --help 2>/dev/null || echo \"lint not available\"\n\n# Check test command exists\nbun test --help 2>/dev/null || echo \"test not available\"\n```\n\n### 4.2 Document Command Availability\n\n| Command | Status |\n|---------|--------|\n| `bun run type-check` | ✅ Available |\n| `bun run lint` | ✅ Available |\n| `bun test` | ✅ Available |\n| `bun run build` | ✅ Available |\n\n**PHASE_4_CHECKPOINT:**\n\n- [ ] Validation commands tested\n- [ ] All required commands available\n\n---\n\n## Phase 5: ARTIFACT - Write Confirmation\n\n### 5.1 Write Confirmation Artifact\n\nWrite to `$ARTIFACTS_DIR/plan-confirmation.md`:\n\n```markdown\n# Plan Confirmation\n\n**Generated**: {YYYY-MM-DD HH:MM}\n**Workflow ID**: $WORKFLOW_ID\n**Status**: {CONFIRMED | WARNINGS | BLOCKED}\n\n---\n\n## Pattern Verification\n\n| Pattern | File | Status | Notes |\n|---------|------|--------|-------|\n| Constructor pattern | `src/adapters/telegram.ts:11-23` | ✅ | Matches expected |\n| Interface definition | `src/types/index.ts:49-74` | ✅ | Present |\n| ... | ... | ... | ... |\n\n**Pattern Summary**: {X} of {Y} patterns verified\n\n---\n\n## Target Files\n\n### Files to Create\n\n| File | Status |\n|------|--------|\n| `src/new-file.ts` | ✅ Does not exist (ready to create) |\n\n### Files to Update\n\n| File | Status |\n|------|--------|\n| `src/existing.ts` | ✅ Exists |\n\n---\n\n## Validation Commands\n\n| Command | Available |\n|---------|-----------|\n| `bun run type-check` | ✅ |\n| `bun run lint` | ✅ |\n| `bun test` | ✅ |\n| `bun run build` | ✅ |\n\n---\n\n## Issues Found\n\n{If no issues:}\nNo issues found. Plan research is valid.\n\n{If issues:}\n### Warnings\n\n- **{file}**: {description of drift or concern}\n\n### Blockers\n\n- **{file}**: {description of missing file or critical issue}\n\n---\n\n## Recommendation\n\n{One of:}\n- ✅ **PROCEED**: Plan research is valid, continue to implementation\n- ⚠️ **PROCEED WITH CAUTION**: Minor drift detected, implementation may need adjustments\n- ❌ **STOP**: Critical issues found, plan needs revision\n\n---\n\n## Next Step\n\n{If PROCEED or PROCEED WITH CAUTION:}\nContinue to `archon-implement-tasks` to execute the plan.\n\n{If STOP:}\nRevise the plan to address blockers, then re-run `archon-plan-setup`.\n```\n\n**PHASE_5_CHECKPOINT:**\n\n- [ ] Confirmation artifact written\n- [ ] Status clearly indicated\n- [ ] Issues documented\n\n---\n\n## Phase 6: OUTPUT - Report to User\n\n### If Confirmed (no blockers):\n\n```markdown\n## Plan Confirmed ✅\n\n**Workflow ID**: `$WORKFLOW_ID`\n**Status**: Ready for implementation\n\n### Verification Summary\n\n| Check | Result |\n|-------|--------|\n| Pattern files | ✅ {X}/{Y} verified |\n| Target files | ✅ Ready |\n| Validation commands | ✅ Available |\n\n{If warnings:}\n### Warnings\n\n- {warning 1}\n- {warning 2}\n\nThese are minor and shouldn't block implementation.\n\n### Artifact\n\nConfirmation written to: `$ARTIFACTS_DIR/plan-confirmation.md`\n\n### Next Step\n\nProceed to `archon-implement-tasks` to execute the plan.\n```\n\n### If Blocked:\n\n```markdown\n## Plan Blocked ❌\n\n**Workflow ID**: `$WORKFLOW_ID`\n**Status**: Cannot proceed\n\n### Blockers Found\n\n1. **{file}**: {description}\n2. **{file}**: {description}\n\n### Required Action\n\nThe plan references files or patterns that no longer exist. Options:\n\n1. **Update the plan** to reflect current codebase state\n2. **Restore missing files** if they were accidentally deleted\n3. **Re-run planning** with `/archon-plan` to generate a fresh plan\n\n### Artifact\n\nDetails written to: `$ARTIFACTS_DIR/plan-confirmation.md`\n```\n\n---\n\n## Success Criteria\n\n- **PATTERNS_VERIFIED**: All pattern files exist and are reasonably similar\n- **TARGETS_VALID**: CREATE files don't exist, UPDATE files do exist\n- **COMMANDS_AVAILABLE**: Validation commands can be run\n- **ARTIFACT_WRITTEN**: Confirmation artifact created with clear status\n",
  "archon-create-plan": "---\ndescription: Create comprehensive feature implementation plan with codebase analysis and research\nargument-hint: <feature description | path/to/prd.md>\n---\n\n# Create Implementation Plan\n\n**Input**: $ARGUMENTS\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Your Mission\n\nTransform \"$ARGUMENTS\" into a battle-tested implementation plan through systematic codebase exploration, pattern extraction, and strategic research.\n\n**Core Principle**: PLAN ONLY - no code written. Create a context-rich document that enables one-pass implementation success.\n\n**Execution Order**: CODEBASE FIRST, RESEARCH SECOND. Solutions must fit existing patterns before introducing new ones.\n\n**Agent Strategy**: Use Task tool with subagent_type=\"Explore\" for codebase intelligence gathering. This ensures thorough pattern discovery before any external research.\n\n**Output**: `$ARTIFACTS_DIR/plan.md`\n\n---\n\n## Phase 0: DETECT - Input Type Resolution\n\n### 0.1 Determine Input Type\n\n| Input Pattern | Type | Action |\n|---------------|------|--------|\n| Ends with `.prd.md` | PRD file | Parse PRD, select next phase |\n| Ends with `.md` and contains \"Implementation Phases\" | PRD file | Parse PRD, select next phase |\n| File path that exists | Document | Read and extract feature description |\n| Free-form text | Description | Use directly as feature input |\n| Empty/blank | Error | STOP - require input |\n\n### 0.2 If PRD File Detected\n\n1. **Read the PRD file**\n2. **Parse the Implementation Phases table** - find rows with `Status: pending`\n3. **Check dependencies** - only select phases whose dependencies are `complete`\n4. **Select the next actionable phase:**\n   - First pending phase with all dependencies complete\n   - If multiple candidates with same dependencies, note parallelism opportunity\n\n5. **Extract phase context:**\n   ```\n   PHASE: {phase number and name}\n   GOAL: {from phase details}\n   SCOPE: {from phase details}\n   SUCCESS SIGNAL: {from phase details}\n   PRD CONTEXT: {problem statement, user, hypothesis from PRD}\n   ```\n\n6. **Report selection to user:**\n   ```\n   PRD: {prd file path}\n   Selected Phase: #{number} - {name}\n\n   {If parallel phases available:}\n   Note: Phase {X} can also run in parallel (in separate worktree).\n\n   Proceeding with Phase #{number}...\n   ```\n\n### 0.3 If Free-form Description\n\nProceed directly to Phase 1 with the input as feature description.\n\n**PHASE_0_CHECKPOINT:**\n\n- [ ] Input type determined\n- [ ] If PRD: next phase selected and dependencies verified\n- [ ] Feature description ready for Phase 1\n\n---\n\n## Phase 1: PARSE - Feature Understanding\n\n### 1.1 Discover Project Structure\n\n**CRITICAL**: Do NOT assume `src/` exists. Discover actual structure:\n\n```bash\n# List root contents\nls -la\n\n# Find main source directories\nls -la */ 2>/dev/null | head -50\n\n# Identify project type from config files\ncat package.json 2>/dev/null | head -20\ncat pyproject.toml 2>/dev/null | head -20\ncat Cargo.toml 2>/dev/null | head -20\ncat go.mod 2>/dev/null | head -20\n```\n\nCommon alternatives to `src/`:\n- `app/` (Next.js, Rails, Laravel)\n- `lib/` (Ruby gems, Elixir)\n- `packages/` (monorepos)\n- `cmd/`, `internal/`, `pkg/` (Go)\n- Root-level source files (Python, scripts)\n\n### 1.2 Read CLAUDE.md\n\n```bash\ncat CLAUDE.md\n```\n\nNote all coding standards, patterns, and rules that apply to this codebase.\n\n### 1.3 Extract from Input\n\n- Core problem being solved\n- User value and business impact\n- Feature type: NEW_CAPABILITY | ENHANCEMENT | REFACTOR | BUG_FIX\n- Complexity: LOW | MEDIUM | HIGH\n- Affected systems list\n\n### 1.4 Formulate User Story\n\n```\nAs a <user type>\nI want to <action/goal>\nSo that <benefit/value>\n```\n\n**PHASE_1_CHECKPOINT:**\n\n- [ ] Project structure discovered\n- [ ] CLAUDE.md rules noted\n- [ ] Problem statement is specific and testable\n- [ ] User story follows correct format\n- [ ] Complexity assessment has rationale\n- [ ] Affected systems identified\n\n**GATE**: If requirements are AMBIGUOUS → STOP and ASK user for clarification before proceeding.\n\n---\n\n## Phase 2: EXPLORE - Codebase Intelligence\n\n**CRITICAL: Use Task tool with subagent_type=\"Explore\" with thoroughness=\"very thorough\"**\n\n### 2.1 Launch Explore Agent\n\n```\nExplore the codebase to find patterns, conventions, and integration points\nrelevant to implementing: [feature description].\n\nDISCOVER:\n1. Similar implementations - find analogous features with file:line references\n2. Naming conventions - extract actual examples of function/class/file naming\n3. Error handling patterns - how errors are created, thrown, caught\n4. Logging patterns - logger usage, message formats\n5. Type definitions - relevant interfaces and types\n6. Test patterns - test file structure, assertion styles\n7. Integration points - where new code connects to existing\n8. Dependencies - relevant libraries already in use\n\nReturn ACTUAL code snippets from codebase, not generic examples.\n```\n\n### 2.2 Document Discoveries\n\n**Format in table:**\n\n| Category | File:Lines | Pattern Description | Code Snippet |\n|----------|------------|---------------------|--------------|\n| NAMING | `src/features/X/service.ts:10-15` | camelCase functions | `export function createThing()` |\n| ERRORS | `src/features/X/errors.ts:5-20` | Custom error classes | `class ThingNotFoundError` |\n| LOGGING | `src/core/logging/index.ts:1-10` | getLogger pattern | `const logger = getLogger(\"domain\")` |\n| TESTS | `src/features/X/tests/service.test.ts:1-30` | describe/it blocks | `describe(\"service\", () => {` |\n| TYPES | `src/features/X/models.ts:1-20` | Type inference | `type Thing = typeof things.$inferSelect` |\n\n**PHASE_2_CHECKPOINT:**\n\n- [ ] Explore agent launched and completed successfully\n- [ ] At least 3 similar implementations found with file:line refs\n- [ ] Code snippets are ACTUAL (copy-pasted from codebase, not invented)\n- [ ] Integration points mapped with specific file paths\n- [ ] Dependencies cataloged with versions from package.json\n\n---\n\n## Phase 3: RESEARCH - External Documentation\n\n**ONLY AFTER Phase 2 is complete** - solutions must fit existing codebase patterns first.\n\n### 3.1 Search for Documentation\n\nUse WebSearch tool for:\n- Official documentation for involved libraries (match versions from package.json)\n- Known gotchas, breaking changes, deprecations\n- Security considerations and best practices\n- Performance optimization patterns\n\n### 3.2 Format References\n\n```markdown\n- [Library Docs v{version}](https://url#specific-section)\n  - KEY_INSIGHT: {what we learned that affects implementation}\n  - APPLIES_TO: {which task/file this affects}\n  - GOTCHA: {potential pitfall and how to avoid}\n```\n\n**PHASE_3_CHECKPOINT:**\n\n- [ ] Documentation versions match package.json\n- [ ] URLs include specific section anchors (not just homepage)\n- [ ] Gotchas documented with mitigation strategies\n- [ ] No conflicting patterns between external docs and existing codebase\n\n---\n\n## Phase 4: DESIGN - UX Transformation\n\n### 4.1 Create ASCII Diagrams\n\n**Before State:**\n\n```\n╔═══════════════════════════════════════════════════════════════════════════════╗\n║                              BEFORE STATE                                      ║\n╠═══════════════════════════════════════════════════════════════════════════════╣\n║                                                                               ║\n║   ┌─────────────┐         ┌─────────────┐         ┌─────────────┐            ║\n║   │   Screen/   │ ──────► │   Action    │ ──────► │   Result    │            ║\n║   │  Component  │         │   Current   │         │   Current   │            ║\n║   └─────────────┘         └─────────────┘         └─────────────┘            ║\n║                                                                               ║\n║   USER_FLOW: [describe current step-by-step experience]                       ║\n║   PAIN_POINT: [what's missing, broken, or inefficient]                        ║\n║   DATA_FLOW: [how data moves through the system currently]                    ║\n║                                                                               ║\n╚═══════════════════════════════════════════════════════════════════════════════╝\n```\n\n**After State:**\n\n```\n╔═══════════════════════════════════════════════════════════════════════════════╗\n║                               AFTER STATE                                      ║\n╠═══════════════════════════════════════════════════════════════════════════════╣\n║                                                                               ║\n║   ┌─────────────┐         ┌─────────────┐         ┌─────────────┐            ║\n║   │   Screen/   │ ──────► │   Action    │ ──────► │   Result    │            ║\n║   │  Component  │         │    NEW      │         │    NEW      │            ║\n║   └─────────────┘         └─────────────┘         └─────────────┘            ║\n║                                   │                                           ║\n║                                   ▼                                           ║\n║                          ┌─────────────┐                                      ║\n║                          │ NEW_FEATURE │  ◄── [new capability added]          ║\n║                          └─────────────┘                                      ║\n║                                                                               ║\n║   USER_FLOW: [describe new step-by-step experience]                           ║\n║   VALUE_ADD: [what user gains from this change]                               ║\n║   DATA_FLOW: [how data moves through the system after]                        ║\n║                                                                               ║\n╚═══════════════════════════════════════════════════════════════════════════════╝\n```\n\n### 4.2 Document Interaction Changes\n\n| Location | Before | After | User_Action | Impact |\n|----------|--------|-------|-------------|--------|\n| `/route` | State A | State B | Click X | Can now Y |\n| `Component.tsx` | Missing feature | Has feature | Input Z | Gets result W |\n\n**PHASE_4_CHECKPOINT:**\n\n- [ ] Before state accurately reflects current system behavior\n- [ ] After state shows ALL new capabilities\n- [ ] Data flows are traceable from input to output\n- [ ] User value is explicit and measurable\n\n---\n\n## Phase 5: ARCHITECT - Strategic Design\n\n### 5.0 Primitives Inventory\n\nBefore designing the solution, audit existing building blocks:\n\n1. **What primitives already exist?** List the core abstractions in the codebase\n   related to this feature — with file:line references from the Explore agent output.\n2. **Are they complete?** Do the existing primitives cover this use case, or do they\n   have gaps that require extension?\n3. **Extend before adding** — can we extend an existing primitive rather than creating\n   a new one? Prefer `implements ExistingInterface` over `interface NewInterface`.\n4. **Minimum primitive surface** — if new primitives ARE needed, what's the smallest\n   addition that enables this feature and remains useful to future callers?\n5. **Dependency chain** — what must exist first? What does this feature unlock downstream?\n\n| Primitive | File:Lines | Complete? | Role in Feature |\n|-----------|-----------|-----------|----------------|\n| {name} | `path/to/file.ts:10-30` | Yes/Partial/No | {how it's used or extended} |\n\n### 5.1 Deep Analysis\n\nConsider (use extended thinking if needed):\n\n- **ARCHITECTURE_FIT**: How does this integrate with the existing architecture?\n- **EXECUTION_ORDER**: What must happen first → second → third?\n- **FAILURE_MODES**: Edge cases, race conditions, error scenarios?\n- **PERFORMANCE**: Will this scale? Database queries optimized?\n- **SECURITY**: Attack vectors? Data exposure risks? Auth/authz?\n- **MAINTAINABILITY**: Will future devs understand this code?\n\n### 5.2 Document Decisions\n\n```markdown\nAPPROACH_CHOSEN: [description]\nRATIONALE: [why this over alternatives - reference codebase patterns]\n\nALTERNATIVES_REJECTED:\n- [Alternative 1]: Rejected because [specific reason]\n- [Alternative 2]: Rejected because [specific reason]\n\nNOT_BUILDING (explicit scope limits):\n- [Item 1 - explicitly out of scope and why]\n- [Item 2 - explicitly out of scope and why]\n```\n\n**PHASE_5_CHECKPOINT:**\n\n- [ ] Approach aligns with existing architecture and patterns\n- [ ] Dependencies ordered correctly (types → repository → service → routes)\n- [ ] Edge cases identified with specific mitigation strategies\n- [ ] Scope boundaries are explicit and justified\n\n---\n\n## Phase 6: GENERATE - Write Plan File\n\n### 6.1 Create Artifact Directory\n\n```bash\n```\n\n### 6.2 Write Plan\n\nWrite to `$ARTIFACTS_DIR/plan.md`:\n\n```markdown\n# Feature: {Feature Name}\n\n## Summary\n\n{One paragraph: What we're building and high-level approach}\n\n## User Story\n\nAs a {user type}\nI want to {action}\nSo that {benefit}\n\n## Problem Statement\n\n{Specific problem this solves - must be testable}\n\n## Solution Statement\n\n{How we're solving it - architecture overview}\n\n## Metadata\n\n| Field | Value |\n|-------|-------|\n| Type | NEW_CAPABILITY / ENHANCEMENT / REFACTOR / BUG_FIX |\n| Complexity | LOW / MEDIUM / HIGH |\n| Systems Affected | {comma-separated list} |\n| Dependencies | {external libs/services with versions} |\n| Estimated Tasks | {count} |\n\n---\n\n## UX Design\n\n### Before State\n\n{ASCII diagram - current user experience with data flows}\n\n### After State\n\n{ASCII diagram - new user experience with data flows}\n\n### Interaction Changes\n\n| Location | Before | After | User Impact |\n|----------|--------|-------|-------------|\n| {path/component} | {old behavior} | {new behavior} | {what changes for user} |\n\n---\n\n## Mandatory Reading\n\n**CRITICAL: Implementation agent MUST read these files before starting any task:**\n\n| Priority | File | Lines | Why Read This |\n|----------|------|-------|---------------|\n| P0 | `path/to/critical.ts` | 10-50 | Pattern to MIRROR exactly |\n| P1 | `path/to/types.ts` | 1-30 | Types to IMPORT |\n| P2 | `path/to/test.ts` | all | Test pattern to FOLLOW |\n\n**External Documentation:**\n\n| Source | Section | Why Needed |\n|--------|---------|------------|\n| [Lib Docs v{version}](url#anchor) | {section name} | {specific reason} |\n\n---\n\n## Patterns to Mirror\n\n**NAMING_CONVENTION:**\n```typescript\n// SOURCE: {file:lines}\n// COPY THIS PATTERN:\n{actual code snippet from codebase}\n```\n\n**ERROR_HANDLING:**\n```typescript\n// SOURCE: {file:lines}\n// COPY THIS PATTERN:\n{actual code snippet from codebase}\n```\n\n**LOGGING_PATTERN:**\n```typescript\n// SOURCE: {file:lines}\n// COPY THIS PATTERN:\n{actual code snippet from codebase}\n```\n\n**TEST_STRUCTURE:**\n```typescript\n// SOURCE: {file:lines}\n// COPY THIS PATTERN:\n{actual code snippet from codebase}\n```\n\n---\n\n## Files to Change\n\n| File | Action | Justification |\n|------|--------|---------------|\n| `src/features/new/models.ts` | CREATE | Type definitions |\n| `src/features/new/service.ts` | CREATE | Business logic |\n| `src/existing/index.ts` | UPDATE | Add integration |\n\n---\n\n## NOT Building (Scope Limits)\n\nExplicit exclusions to prevent scope creep:\n\n- {Item 1 - explicitly out of scope and why}\n- {Item 2 - explicitly out of scope and why}\n\n---\n\n## Step-by-Step Tasks\n\nExecute in order. Each task is atomic and independently verifiable.\n\n### Task 1: {CREATE/UPDATE} `{file path}`\n\n- **ACTION**: {CREATE new file / UPDATE existing file}\n- **IMPLEMENT**: {specific what to implement}\n- **MIRROR**: `{source-file:lines}` - follow this pattern exactly\n- **IMPORTS**: `{specific imports needed}`\n- **GOTCHA**: {known issue to avoid}\n- **VALIDATE**: `{validation-command}` - must pass before next task\n\n### Task 2: {CREATE/UPDATE} `{file path}`\n\n{... repeat for each task ...}\n\n---\n\n## Testing Strategy\n\n### Unit Tests to Write\n\n| Test File | Test Cases | Validates |\n|-----------|------------|-----------|\n| `src/features/new/tests/service.test.ts` | CRUD ops, edge cases | Business logic |\n\n### Edge Cases Checklist\n\n- [ ] Empty string inputs\n- [ ] Missing required fields\n- [ ] Unauthorized access attempts\n- [ ] Not found scenarios\n- [ ] {feature-specific edge case}\n\n---\n\n## Validation Commands\n\n### Level 1: STATIC_ANALYSIS\n\n```bash\n{runner} run type-check && {runner} run lint\n```\n\n**EXPECT**: Exit 0, no errors or warnings\n\n### Level 2: UNIT_TESTS\n\n```bash\n{runner} test {path/to/feature/tests}\n```\n\n**EXPECT**: All tests pass\n\n### Level 3: FULL_SUITE\n\n```bash\n{runner} run validate\n```\n\n**EXPECT**: All tests pass, build succeeds\n\n---\n\n## Acceptance Criteria\n\n- [ ] All specified functionality implemented per user story\n- [ ] Level 1-3 validation commands pass with exit 0\n- [ ] Code mirrors existing patterns exactly (naming, structure, logging)\n- [ ] No regressions in existing tests\n- [ ] UX matches \"After State\" diagram\n\n---\n\n## Completion Checklist\n\n- [ ] All tasks completed in dependency order\n- [ ] Each task validated immediately after completion\n- [ ] All acceptance criteria met\n\n---\n\n## Risks and Mitigations\n\n| Risk | Likelihood | Impact | Mitigation |\n|------|------------|--------|------------|\n| {Risk description} | LOW/MED/HIGH | LOW/MED/HIGH | {Specific prevention/handling strategy} |\n\n---\n\n## Notes\n\n{Additional context, design decisions, trade-offs, future considerations}\n```\n\n### 6.3 If Input Was PRD\n\nAlso update the PRD file:\n1. Change the phase's Status from `pending` to `in-progress`\n2. Add the plan file path to the PRP Plan column\n\n**PHASE_6_CHECKPOINT:**\n\n- [ ] Plan file written to `$ARTIFACTS_DIR/plan.md`\n- [ ] All sections populated with actual codebase data\n- [ ] If PRD: source file updated\n\n---\n\n## Phase 7: VERIFY - Plan Quality Check\n\n### 7.1 Context Completeness\n\n- [ ] All patterns from Explore agent documented with file:line references\n- [ ] External docs versioned to match package.json\n- [ ] Integration points mapped with specific file paths\n- [ ] Gotchas captured with mitigation strategies\n- [ ] Every task has at least one executable validation command\n\n### 7.2 Implementation Readiness\n\n- [ ] Tasks ordered by dependency (can execute top-to-bottom)\n- [ ] Each task is atomic and independently testable\n- [ ] No placeholders - all content is specific and actionable\n- [ ] Pattern references include actual code snippets (copy-pasted, not invented)\n\n### 7.3 Pattern Faithfulness\n\n- [ ] Every new file mirrors existing codebase style exactly\n- [ ] No unnecessary abstractions introduced\n- [ ] Naming follows discovered conventions\n- [ ] Error/logging patterns match existing\n- [ ] Test structure matches existing tests\n\n### 7.4 No Prior Knowledge Test\n\n**Could an agent unfamiliar with this codebase implement using ONLY the plan?**\n\nIf NO → add missing context to plan.\n\n**PHASE_7_CHECKPOINT:**\n\n- [ ] All verification checks pass\n- [ ] Plan is self-contained\n\n---\n\n## Phase 8: OUTPUT - Report to User\n\n```markdown\n## Plan Created\n\n**File**: `$ARTIFACTS_DIR/plan.md`\n**Workflow ID**: `$WORKFLOW_ID`\n\n{If from PRD:}\n**Source PRD**: `{prd-file-path}`\n**Phase**: #{number} - {phase name}\n**PRD Updated**: Status set to `in-progress`, plan linked\n\n{If parallel phases available:}\n**Parallel Opportunity**: Phase {X} can run concurrently in a separate worktree.\n\n---\n\n### Summary\n\n{2-3 sentence feature overview}\n\n### Metadata\n\n| Field | Value |\n|-------|-------|\n| Complexity | {LOW/MEDIUM/HIGH} |\n| Files to CREATE | {N} |\n| Files to UPDATE | {M} |\n| Total Tasks | {K} |\n\n### Key Patterns Discovered\n\n- {Pattern 1 from Explore agent with file:line}\n- {Pattern 2 from Explore agent with file:line}\n- {Pattern 3 from Explore agent with file:line}\n\n### External Research\n\n- {Key doc 1 with version}\n- {Key doc 2 with version}\n\n### UX Transformation\n\n- **BEFORE**: {one-line current state}\n- **AFTER**: {one-line new state}\n\n### Risks\n\n- {Primary risk}: {mitigation}\n\n### Confidence Score\n\n**{1-10}/10** for one-pass implementation success\n\n{Rationale for score}\n\n---\n\n### Next Step\n\nPlan ready. Proceeding to implementation setup.\n```\n\n---\n\n## Success Criteria\n\n- **CONTEXT_COMPLETE**: All patterns, gotchas, integration points documented from actual codebase via Explore agent\n- **IMPLEMENTATION_READY**: Tasks executable top-to-bottom without questions, research, or clarification\n- **PATTERN_FAITHFUL**: Every new file mirrors existing codebase style exactly\n- **VALIDATION_DEFINED**: Every task has executable verification command\n- **UX_DOCUMENTED**: Before/After transformation is visually clear with data flows\n- **ONE_PASS_TARGET**: Confidence score 8+ indicates high likelihood of first-attempt success\n- **ARTIFACT_WRITTEN**: Plan saved to `$ARTIFACTS_DIR/plan.md`\n",
  "archon-create-pr": "---\ndescription: Create a PR from current branch with implementation context\nargument-hint: [base-branch] (default: auto-detected from config or repo)\n---\n\n# Create Pull Request\n\n**Base branch override**: $ARGUMENTS\n**Default base branch**: $BASE_BRANCH\n\n> If a base branch was provided as argument above, use it for `--base`. Otherwise use the default base branch.\n\n---\n\n## Pre-flight: Check for Existing PRs\n\nExtract the issue number from the current branch name or context (e.g., `fix/issue-580` → `580`).\n\n```bash\nBRANCH=$(git branch --show-current)\nISSUE_NUM=$(echo \"$BRANCH\" | grep -oE '[0-9]+' | tail -1)\n```\n\nIf an issue number was found, search for open PRs that already reference it:\n\n```bash\ngh pr list \\\n  --search \"Fixes #${ISSUE_NUM} OR Closes #${ISSUE_NUM}\" \\\n  --state open \\\n  --json number,url,headRefName\n```\n\n**If a matching PR is returned**: stop here, report the existing PR URL, and do **not** proceed to Phase 2 or Phase 3.\n\n```\nExisting PR found for issue #${ISSUE_NUM}: [url]\nSkipping PR creation.\n```\n\n**If no match is found** (or no issue number could be extracted): continue to Phase 1.\n\n---\n\n## Phase 1: Gather Context\n\n### 1.1 Check Git State\n\n```bash\ngit branch --show-current\ngit status --short\ngit log origin/$BASE_BRANCH..HEAD --oneline\n```\n\n### 1.2 Check for Implementation Report\n\nLook for the most recent implementation report:\n\n```bash\nls -t $ARTIFACTS_DIR/../reports/*-report.md 2>/dev/null | head -1\n```\n\nIf found, read it to extract:\n- Summary of what was implemented\n- Files changed\n- Validation results\n- Any deviations from plan\n\n### 1.3 Get Commit Summary\n\n```bash\ngit log origin/$BASE_BRANCH..HEAD --pretty=format:\"- %s\"\n```\n\n---\n\n## Phase 2: Prepare Branch\n\n### 2.1 Ensure All Changes Committed\n\nIf uncommitted changes exist:\n\n```bash\ngit status --porcelain\n```\n\n**If dirty**:\n\n1. Stage **only** the source files that are part of this change — never `git add -A`, `git add .`, or `git add -u`. List them by name:\n   ```bash\n   git add path/to/file1 path/to/file2 ...\n   git status --porcelain  # verify nothing else is staged\n   ```\n2. **Never stage** scratch / review / PR-body artifacts, even if they show up in `git status`:\n   - `.pr-body.md`, `pr-body.md`, `*.scratch.md`, `*.tmp.md`\n   - `review/`, `*-report.md` at the repo root\n   - Anything under `$ARTIFACTS_DIR`\n3. Commit: `git commit -m \"Final changes before PR\"`\n\n### 2.2 Push Branch\n\n```bash\ngit push -u origin HEAD\n```\n\n---\n\n## Phase 3: Create PR\n\n### 3.1 Check for PR Template\n\nLook for the project's PR template at `.github/pull_request_template.md`, `.github/PULL_REQUEST_TEMPLATE.md`, or `docs/PULL_REQUEST_TEMPLATE.md`. Read whichever one exists.\n\n**If template found**: Use it as the structure, fill in **every section** with details from the implementation report and commits. Don't skip sections or leave placeholders.\n\n**If no template**, use this format:\n\n```markdown\n## Summary\n\n[Brief description from implementation report or commits]\n\n## Changes\n\n[List from implementation report \"Files Changed\" section, or from commits]\n- file1.ts - description\n- file2.ts - description\n\n## Validation\n\n[From implementation report \"Validation Results\" section]\n- [x] Type check passes\n- [x] Lint passes\n- [x] Tests pass\n- [x] Build succeeds\n\n## Testing Notes\n\n[Any manual testing done or integration test results]\n\n---\n\n[If from a GitHub issue, add: Closes #XXX]\n```\n\n### 3.2 Determine PR Title\n\n**Title**: Concise, imperative mood\n- From implementation report summary, OR\n- From commit messages\n\n### 3.3 Create the PR\n\n```bash\n# Write body to file to avoid shell escaping\ncat > $ARTIFACTS_DIR/pr-body.md <<'EOF'\n[body from above]\nEOF\n\ngh pr create \\\n  --title \"[title]\" \\\n  --body-file $ARTIFACTS_DIR/pr-body.md \\\n  --base $BASE_BRANCH\n```\n\nOr if the content is simple:\n\n```bash\ngh pr create --fill --base $BASE_BRANCH\n```\n\nAfter creating the PR, capture its identifiers for downstream steps. Only write artifacts if PR creation succeeded — never persist stale data from a pre-existing PR:\n\n```bash\n# After creating the PR, capture and persist the PR number for downstream steps\n# IMPORTANT: Only write artifacts after confirmed successful PR creation\nif gh pr view --json number,url -q '.number,.url' > /dev/null 2>&1; then\n  PR_NUMBER=$(gh pr view --json number -q '.number')\n  PR_URL=$(gh pr view --json url -q '.url')\n  echo \"$PR_NUMBER\" > \"$ARTIFACTS_DIR/.pr-number\"\n  echo \"$PR_URL\" > \"$ARTIFACTS_DIR/.pr-url\"\nelse\n  echo \"WARNING: Could not confirm PR creation; skipping .pr-number/.pr-url artifacts\"\nfi\n```\n\n---\n\n## Phase 4: Output\n\nReport the result:\n\n```markdown\n## PR Created\n\n**URL**: [PR URL]\n**Branch**: [branch-name] → [base-branch]\n**Title**: [PR title]\n\n### Summary\n[Brief summary of what the PR contains]\n\n### Next Steps\n1. Request review if needed\n2. Address any CI failures\n3. Merge when approved\n```\n\n---\n\n## Error Handling\n\n### No Commits to Push\n\n```\nNo commits between origin/$BASE_BRANCH and HEAD.\nNothing to create a PR for.\n```\n\n### Branch Already Has PR\n\n```bash\ngh pr view --web\n```\n\nOpens the existing PR instead of creating a duplicate.\n\n### Push Fails\n\n1. Check if branch exists remotely: `git ls-remote --heads origin [branch]`\n2. If conflicts: `git pull --rebase origin $BASE_BRANCH` then retry push\n3. If permission issues: Check GitHub access\n",
  "archon-docs-impact-agent": "---\ndescription: Check if PR changes require documentation updates (CLAUDE.md, docs/, agents)\nargument-hint: (none - reads from scope artifact)\n---\n\n# Documentation Impact Agent\n\n---\n\n## Your Mission\n\nAnalyze if the PR changes require updates to project documentation: CLAUDE.md, docs/ folder, agent definitions, or other documentation. Produce a structured artifact with recommendations.\n\n**Output artifact**: `$ARTIFACTS_DIR/review/docs-impact-findings.md`\n\n---\n\n## Phase 1: LOAD - Get Context\n\n### 1.1 Get PR Number from Registry\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number)\n```\n\n### 1.2 Read Scope\n\n```bash\ncat $ARTIFACTS_DIR/review/scope.md\n```\n\n**CRITICAL**: Check for \"NOT Building (Scope Limits)\" section. Items listed there are **intentionally excluded** - do NOT flag them as missing documentation needs!\n\n### 1.3 Get PR Diff\n\n```bash\ngh pr diff {number}\n```\n\n### 1.4 Read Current Documentation\n\n```bash\n# Read CLAUDE.md\ncat CLAUDE.md\n\n# List docs folder\nls -la $DOCS_DIR\n\n# List agent definitions\nls -la .claude/agents/ 2>/dev/null || true\nls -la .archon/commands/ 2>/dev/null || true\n```\n\n**PHASE_1_CHECKPOINT:**\n- [ ] PR number identified\n- [ ] Changes understood\n- [ ] Current docs read\n\n---\n\n## Phase 2: ANALYZE - Check Documentation Impact\n\n### 2.1 CLAUDE.md Impact\n\nCheck if changes affect documented:\n- Commands or slash commands\n- Workflows\n- Development setup\n- Environment variables\n- Database schema\n- API endpoints\n- Testing instructions\n- Code patterns/standards\n\n### 2.2 docs/ Folder Impact\n\nCheck if changes affect:\n- Architecture documentation\n- Getting started guide\n- Configuration documentation\n- API documentation\n- Deployment instructions\n\n### 2.3 Agent/Command Definitions\n\nCheck if changes affect:\n- Agent capabilities\n- Command arguments\n- Workflow steps\n- Tool usage patterns\n\n### 2.4 README Impact\n\nCheck if changes affect:\n- Feature list\n- Installation instructions\n- Usage examples\n- Configuration options\n\n**PHASE_2_CHECKPOINT:**\n- [ ] CLAUDE.md impact assessed\n- [ ] docs/ impact assessed\n- [ ] Agent definitions checked\n- [ ] README checked\n\n---\n\n## Phase 3: GENERATE - Create Artifact\n\nWrite to `$ARTIFACTS_DIR/review/docs-impact-findings.md`:\n\n```markdown\n# Documentation Impact Findings: PR #{number}\n\n**Reviewer**: docs-impact-agent\n**Date**: {ISO timestamp}\n**Docs Checked**: CLAUDE.md, docs/, agents, README\n\n---\n\n## Summary\n\n{2-3 sentence overview of documentation impact}\n\n**Verdict**: {NO_CHANGES_NEEDED | UPDATES_REQUIRED | CRITICAL_UPDATES}\n\n---\n\n## Impact Assessment\n\n| Document | Impact | Required Update |\n|----------|--------|-----------------|\n| CLAUDE.md | NONE/LOW/HIGH | {description or \"None\"} |\n| $DOCS_DIR/architecture.md | NONE/LOW/HIGH | {description or \"None\"} |\n| $DOCS_DIR/configuration.md | NONE/LOW/HIGH | {description or \"None\"} |\n| README.md | NONE/LOW/HIGH | {description or \"None\"} |\n| .claude/agents/*.md | NONE/LOW/HIGH | {description or \"None\"} |\n| .archon/commands/*.md | NONE/LOW/HIGH | {description or \"None\"} |\n\n---\n\n## Findings\n\n### Finding 1: {Descriptive Title}\n\n**Severity**: CRITICAL | HIGH | MEDIUM | LOW\n**Category**: missing-docs | outdated-docs | incomplete-docs | misleading-docs\n**Document**: `{file path}`\n**PR Change**: `{source file}:{line}` - {what changed}\n\n**Issue**:\n{Clear description of why docs need updating}\n\n**Current Documentation**:\n```markdown\n{current text in docs}\n```\n\n**Code Change**:\n```typescript\n// What changed in the PR\n{new code that docs don't reflect}\n```\n\n**Impact if Not Updated**:\n{What happens if docs aren't updated - user confusion, wrong setup, etc.}\n\n---\n\n#### Update Suggestions\n\n| Option | Approach | Scope | Effort |\n|--------|----------|-------|--------|\n| A | {minimal update} | {what it covers} | LOW |\n| B | {comprehensive update} | {what it covers} | MED/HIGH |\n\n**Recommended**: Option {X}\n\n**Reasoning**:\n{Why this update approach:\n- Keeps docs accurate\n- Matches existing documentation style\n- Appropriate level of detail}\n\n**Suggested Documentation Update**:\n```markdown\n{what the docs should say after update}\n```\n\n**Documentation Style Reference**:\n```markdown\n# SOURCE: {doc file}\n# How similar features are documented\n{existing documentation pattern}\n```\n\n---\n\n### Finding 2: {Title}\n\n{Same structure...}\n\n---\n\n## CLAUDE.md Sections to Update\n\n| Section | Current | Needed Update |\n|---------|---------|---------------|\n| {section name} | {current text summary} | {what to add/change} |\n| ... | ... | ... |\n\n---\n\n## Statistics\n\n| Severity | Count | Documents Affected |\n|----------|-------|-------------------|\n| CRITICAL | {n} | {list} |\n| HIGH | {n} | {list} |\n| MEDIUM | {n} | {list} |\n| LOW | {n} | {list} |\n\n---\n\n## New Documentation Needed\n\n| Topic | Suggested Location | Priority |\n|-------|-------------------|----------|\n| {new feature/change} | {where to document} | HIGH/MED/LOW |\n| ... | ... | ... |\n\n---\n\n## Positive Observations\n\n{Documentation already updated in PR, good inline docs, etc.}\n\n---\n\n## Metadata\n\n- **Agent**: docs-impact-agent\n- **Timestamp**: {ISO timestamp}\n- **Artifact**: `$ARTIFACTS_DIR/review/docs-impact-findings.md`\n```\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Artifact file created\n- [ ] All docs checked\n- [ ] Update suggestions provided\n- [ ] Existing doc style referenced\n\n---\n\n## Success Criteria\n\n- **DOCS_ANALYZED**: All relevant docs checked\n- **IMPACT_ASSESSED**: Each doc rated for impact\n- **UPDATES_SPECIFIED**: Clear update suggestions\n- **STYLE_MATCHED**: Suggestions match existing doc style\n",
  "archon-error-handling-agent": "---\ndescription: Review error handling for silent failures, inadequate catch blocks, and poor fallbacks\nargument-hint: (none - reads from scope artifact)\n---\n\n# Error Handling Agent\n\n---\n\n## Your Mission\n\nHunt for silent failures, inadequate error handling, broad catch blocks, and inappropriate fallback behavior. Produce a structured artifact with findings, fix suggestions with options, and reasoning.\n\n**Output artifact**: `$ARTIFACTS_DIR/review/error-handling-findings.md`\n\n---\n\n## Phase 1: LOAD - Get Context\n\n### 1.1 Get PR Number from Registry\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number)\n```\n\n### 1.2 Read Scope\n\n```bash\ncat $ARTIFACTS_DIR/review/scope.md\n```\n\n**CRITICAL**: Check for \"NOT Building (Scope Limits)\" section. Items listed there are **intentionally excluded** - do NOT flag them as bugs or missing features!\n\n### 1.3 Get PR Diff\n\n```bash\ngh pr diff {number}\n```\n\n### 1.4 Read CLAUDE.md Error Handling Rules\n\n```bash\ncat CLAUDE.md | grep -A 20 -i \"error\"\n```\n\n**PHASE_1_CHECKPOINT:**\n- [ ] PR number identified\n- [ ] Scope loaded\n- [ ] Diff available\n\n---\n\n## Phase 2: ANALYZE - Hunt for Issues\n\n### 2.1 Find All Error Handling Code\n\nSearch for:\n- `try { ... } catch` blocks\n- `.catch(` handlers\n- `|| fallback` patterns\n- `?? defaultValue` patterns\n- `?.` optional chaining that might hide errors\n- Error event handlers\n- Conditional error state handling\n\n### 2.2 Scrutinize Each Handler\n\nFor every error handling location, evaluate:\n\n**Logging Quality:**\n- Is error logged with appropriate severity?\n- Does log include sufficient context?\n- Would this help debugging in 6 months?\n\n**User Feedback:**\n- Does user receive actionable feedback?\n- Is the error message specific and helpful?\n- Are technical details appropriately hidden/shown?\n\n**Catch Block Specificity:**\n- Does it catch only expected error types?\n- Could it accidentally suppress unrelated errors?\n- Should it be multiple catch blocks?\n\n**Fallback Behavior:**\n- Is fallback explicitly documented/intended?\n- Does fallback mask the underlying problem?\n- Is user aware they're seeing fallback behavior?\n\n### 2.3 Find Codebase Error Patterns\n\n```bash\n# Find error handling patterns in codebase\ngrep -r \"catch\" src/ --include=\"*.ts\" -A 3 | head -30\ngrep -r \"console.error\" src/ --include=\"*.ts\" -B 2 -A 2 | head -30\n```\n\n**PHASE_2_CHECKPOINT:**\n- [ ] All error handlers identified\n- [ ] Each handler evaluated\n- [ ] Codebase patterns found\n\n---\n\n## Phase 3: GENERATE - Create Artifact\n\nWrite to `$ARTIFACTS_DIR/review/error-handling-findings.md`:\n\n```markdown\n# Error Handling Findings: PR #{number}\n\n**Reviewer**: error-handling-agent\n**Date**: {ISO timestamp}\n**Error Handlers Reviewed**: {count}\n\n---\n\n## Summary\n\n{2-3 sentence overview of error handling quality}\n\n**Verdict**: {APPROVE | REQUEST_CHANGES | NEEDS_DISCUSSION}\n\n---\n\n## Findings\n\n### Finding 1: {Descriptive Title}\n\n**Severity**: CRITICAL | HIGH | MEDIUM | LOW\n**Category**: silent-failure | broad-catch | missing-logging | poor-user-feedback | unsafe-fallback\n**Location**: `{file}:{line}`\n\n**Issue**:\n{Clear description of the error handling problem}\n\n**Evidence**:\n```typescript\n// Current error handling at {file}:{line}\n{problematic code}\n```\n\n**Hidden Errors**:\nThis catch block could silently hide:\n- {Error type 1}: {scenario when it occurs}\n- {Error type 2}: {scenario when it occurs}\n- {Error type 3}: {scenario when it occurs}\n\n**User Impact**:\n{What happens to the user when this error occurs? Why is it bad?}\n\n---\n\n#### Fix Suggestions\n\n| Option | Approach | Pros | Cons |\n|--------|----------|------|------|\n| A | {e.g., Add specific error types} | {benefits} | {drawbacks} |\n| B | {e.g., Add logging + user message} | {benefits} | {drawbacks} |\n| C | {e.g., Propagate error instead} | {benefits} | {drawbacks} |\n\n**Recommended**: Option {X}\n\n**Reasoning**:\n{Explain why this option is preferred:\n- Aligns with project error handling patterns\n- Provides better debugging experience\n- Gives users actionable feedback\n- Follows CLAUDE.md rules}\n\n**Recommended Fix**:\n```typescript\n// Improved error handling\n{corrected code with proper logging, specific catches, user feedback}\n```\n\n**Codebase Pattern Reference**:\n```typescript\n// SOURCE: {file}:{lines}\n// This is how similar errors are handled elsewhere\n{existing error handling pattern from codebase}\n```\n\n---\n\n### Finding 2: {Title}\n\n{Same structure...}\n\n---\n\n## Error Handler Audit\n\n| Location | Type | Logging | User Feedback | Specificity | Verdict |\n|----------|------|---------|---------------|-------------|---------|\n| `file:line` | try-catch | GOOD/BAD | GOOD/BAD | GOOD/BAD | PASS/FAIL |\n| ... | ... | ... | ... | ... | ... |\n\n---\n\n## Statistics\n\n| Severity | Count | Auto-fixable |\n|----------|-------|--------------|\n| CRITICAL | {n} | {n} |\n| HIGH | {n} | {n} |\n| MEDIUM | {n} | {n} |\n| LOW | {n} | {n} |\n\n---\n\n## Silent Failure Risk Assessment\n\n| Risk | Likelihood | Impact | Mitigation |\n|------|------------|--------|------------|\n| {potential silent failure} | HIGH/MED/LOW | {user impact} | {fix needed} |\n| ... | ... | ... | ... |\n\n---\n\n## Patterns Referenced\n\n| File | Lines | Pattern |\n|------|-------|---------|\n| `src/example.ts` | 42-50 | {error handling pattern} |\n| ... | ... | ... |\n\n---\n\n## Positive Observations\n\n{Error handling done well, good patterns, proper logging}\n\n---\n\n## Metadata\n\n- **Agent**: error-handling-agent\n- **Timestamp**: {ISO timestamp}\n- **Artifact**: `$ARTIFACTS_DIR/review/error-handling-findings.md`\n```\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Artifact file created\n- [ ] All error handlers audited\n- [ ] Hidden errors listed for each finding\n- [ ] Fix options with reasoning provided\n\n---\n\n## Success Criteria\n\n- **ERROR_HANDLERS_FOUND**: All try/catch, .catch, fallbacks identified\n- **EACH_HANDLER_AUDITED**: Logging, feedback, specificity evaluated\n- **HIDDEN_ERRORS_LISTED**: Each finding lists what could be hidden\n- **ARTIFACT_CREATED**: Findings file written with complete structure\n",
  "archon-finalize-pr": "---\ndescription: Commit changes, create PR with template, mark ready for review\nargument-hint: (no arguments - reads from workflow artifacts)\n---\n\n# Finalize Pull Request\n\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Your Mission\n\nFinalize the implementation and create the PR:\n1. Commit all changes\n2. Push to remote\n3. Create PR using project's template (if exists)\n4. Mark PR as ready for review\n\n---\n\n## Phase 1: LOAD - Gather Context\n\n### 1.1 Load Workflow Artifacts\n\n```bash\ncat $ARTIFACTS_DIR/plan-context.md\ncat $ARTIFACTS_DIR/implementation.md\ncat $ARTIFACTS_DIR/validation.md\n```\n\nExtract:\n- Plan title and summary\n- Branch name\n- Files changed\n- Tests written\n- Validation results\n- Deviations from plan (if any)\n\n### 1.2 Check for PR Template\n\n**IMPORTANT**: Always check for the project's PR template first. Look for it at `.github/pull_request_template.md`, `.github/PULL_REQUEST_TEMPLATE.md`, or `docs/PULL_REQUEST_TEMPLATE.md`. Read whichever one exists.\n\n**If template found**: Use it as the structure, fill in **every section** with implementation details.\n**If no template**: Use the default format defined in Phase 3.\n\n### 1.3 Check for Existing PR\n\n```bash\ngh pr list --head $(git branch --show-current) --json number,url,state\n```\n\n**If PR already exists**: Will update it instead of creating new one.\n**If no PR**: Will create new one.\n\n**PHASE_1_CHECKPOINT:**\n\n- [ ] Artifacts loaded\n- [ ] Template identified (or using default)\n- [ ] Existing PR status known\n\n---\n\n## Phase 2: COMMIT - Stage and Commit Changes\n\n### 2.1 Check Git Status\n\n```bash\ngit status --porcelain\n```\n\n### 2.2 Stage Changes\n\nStage **only** the implementation files you actually edited — never `git add -A`, `git add .`, or `git add -u`. List them by name:\n\n```bash\ngit add path/to/file1 path/to/file2 ...\ngit status --porcelain  # verify nothing else is staged\n```\n\n**Never stage** scratch / review / PR-body artifacts, even if they appear in `git status`:\n\n- `.pr-body.md`, `pr-body.md`, `*.scratch.md`, `*.tmp.md`\n- `review/`, `*-report.md` at the repo root\n- Anything under `$ARTIFACTS_DIR`\n\n**Review staged files** — ensure no sensitive files (`.env`, credentials) and no scratch artifacts are included:\n\n```bash\ngit diff --cached --name-only\n```\n\n### 2.3 Create Commit\n\nCreate a descriptive commit message:\n\n```bash\ngit commit -m \"{summary of implementation}\n\n- {key change 1}\n- {key change 2}\n- {key change 3}\n\n{If from plan/issue: Implements #{number}}\n\"\n```\n\n### 2.4 Push to Remote\n\n```bash\ngit push origin HEAD\n```\n\n**PHASE_2_CHECKPOINT:**\n\n- [ ] All changes staged\n- [ ] No sensitive files included\n- [ ] Commit created\n- [ ] Pushed to remote\n\n---\n\n## Phase 3: CREATE/UPDATE - Pull Request\n\n### 3.1 Prepare PR Body\n\n**If project has PR template**, fill in each section with implementation details:\n- Replace placeholder text with actual content\n- Fill in checkboxes based on what was done\n- Keep the template's structure intact\n\n**If no template**, use this default format:\n\n```markdown\n## Summary\n\n{Brief description from plan summary}\n\n## Changes\n\n{From implementation.md \"Files Changed\" section}\n\n| File | Action | Description |\n|------|--------|-------------|\n| `src/x.ts` | CREATE | {what it does} |\n| `src/y.ts` | UPDATE | {what changed} |\n\n## Tests\n\n{From implementation.md \"Tests Written\" section}\n\n- `src/x.test.ts` - {test descriptions}\n- `src/y.test.ts` - {test descriptions}\n\n## Validation\n\n{From validation.md}\n\n- [x] Type check passes\n- [x] Lint passes\n- [x] Format passes\n- [x] All tests pass ({N} tests)\n- [x] Build succeeds\n\n## Implementation Notes\n\n{If deviations from plan:}\n### Deviations from Plan\n\n{List deviations and reasons}\n\n{If issues encountered:}\n### Issues Resolved\n\n{List issues and resolutions}\n\n---\n\n**Plan**: `{plan-source-path}`\n**Workflow ID**: `$WORKFLOW_ID`\n```\n\n### 3.2 Create or Update PR\n\n**If no PR exists**, create one:\n\n```bash\n# Write prepared body to file to avoid shell escaping\ncat > $ARTIFACTS_DIR/pr-body.md <<'EOF'\n{prepared-body}\nEOF\n\ngh pr create \\\n  --title \"{plan-title}\" \\\n  --body-file $ARTIFACTS_DIR/pr-body.md \\\n  --base $BASE_BRANCH\n```\n\n**If PR already exists**, update it:\n\n```bash\ngh pr edit {pr-number} --body-file $ARTIFACTS_DIR/pr-body.md\n```\n\n### 3.3 Ensure Ready for Review\n\nIf PR was created as draft, mark ready:\n\n```bash\ngh pr ready {pr-number} 2>/dev/null || true\n```\n\n### 3.4 Capture PR Info\n\n```bash\ngh pr view --json number,url,headRefName,baseRefName\n```\n\n### 3.5 Write PR Number Registry\n\nWrite PR number for downstream review steps:\n\n```bash\nPR_NUMBER=$(gh pr view --json number -q '.number')\nPR_URL=$(gh pr view --json url -q '.url')\necho \"$PR_NUMBER\" > $ARTIFACTS_DIR/.pr-number\necho \"$PR_URL\" > $ARTIFACTS_DIR/.pr-url\n```\n\n**PHASE_3_CHECKPOINT:**\n\n- [ ] PR created or updated\n- [ ] PR body uses template (if available)\n- [ ] PR ready for review\n- [ ] PR URL captured\n- [ ] PR number registry written\n\n---\n\n## Phase 4: ARTIFACT - Write PR Ready Status\n\n### 4.1 Write Final Artifact\n\nWrite to `$ARTIFACTS_DIR/pr-ready.md`:\n\n```markdown\n# PR Ready for Review\n\n**Generated**: {YYYY-MM-DD HH:MM}\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Pull Request\n\n| Field | Value |\n|-------|-------|\n| **Number** | #{number} |\n| **URL** | {url} |\n| **Branch** | `{head}` → `{base}` |\n| **Status** | Ready for Review |\n\n---\n\n## Commit\n\n**Hash**: {commit-sha}\n**Message**: {commit-message-first-line}\n\n---\n\n## Files in PR\n\n{From git diff --name-only origin/$BASE_BRANCH}\n\n| File | Status |\n|------|--------|\n| `src/x.ts` | Added |\n| `src/y.ts` | Modified |\n\n---\n\n## PR Description\n\n{Whether template was used or default format}\n\n- Template used: {yes/no}\n- Template path: {path if used}\n\n---\n\n## Next Step\n\nContinue to PR review workflow:\n1. `archon-pr-review-scope`\n2. `archon-sync-pr-with-main`\n3. Review agents (parallel)\n4. `archon-synthesize-review`\n5. `archon-implement-review-fixes`\n```\n\n**PHASE_4_CHECKPOINT:**\n\n- [ ] PR ready artifact written\n\n---\n\n## Phase 5: OUTPUT - Report Status\n\n```markdown\n## PR Ready for Review ✅\n\n**Workflow ID**: `$WORKFLOW_ID`\n\n### Pull Request\n\n| Field | Value |\n|-------|-------|\n| PR | #{number} |\n| URL | {url} |\n| Branch | `{branch}` → `{base}` |\n| Status | 🟢 Ready for Review |\n\n### Commit\n\n```\n{commit-sha-short} {commit-message-first-line}\n```\n\n### Files Changed\n\n- {N} files added\n- {M} files modified\n- {K} files deleted\n\n### Validation Summary\n\n| Check | Status |\n|-------|--------|\n| Type check | ✅ |\n| Lint | ✅ |\n| Tests | ✅ ({N} passed) |\n| Build | ✅ |\n\n### Artifact\n\nStatus written to: `$ARTIFACTS_DIR/pr-ready.md`\n\n### Next Step\n\nProceeding to comprehensive PR review.\n```\n\n---\n\n## Error Handling\n\n### Nothing to Commit\n\nIf no changes to commit:\n\n```markdown\nℹ️ No changes to commit\n\nAll changes were already committed. Proceeding to update PR description.\n```\n\n### Push Fails\n\n```bash\n# Try force push if branch was rebased\ngit push --force-with-lease origin HEAD\n```\n\nIf still fails:\n```\n❌ Push failed\n\nCheck:\n1. Branch protection rules\n2. Push access to repository\n3. Remote branch status: `git fetch origin && git status`\n```\n\n### PR Not Found\n\n```\n❌ PR not found: #{number}\n\nThe draft PR may have been closed or deleted. Create a new one:\n`gh pr create --title \"...\" --body \"...\"`\n```\n\n### Template Parsing\n\nIf template has complex structure that's hard to fill:\n- Use as much of the template as possible\n- Add implementation details in relevant sections\n- Note at bottom: \"Some template sections may need manual completion\"\n\n---\n\n## Success Criteria\n\n- **CHANGES_COMMITTED**: All changes in a commit\n- **PUSHED**: Branch pushed to remote\n- **PR_UPDATED**: PR description reflects implementation\n- **PR_READY**: Draft status removed\n- **ARTIFACT_WRITTEN**: PR ready artifact created\n",
  "archon-fix-issue": "---\ndescription: Implement a fix from investigation artifact - code changes, validation, and commit (no PR)\nargument-hint: <issue-number|artifact-path>\n---\n\n# Fix Issue\n\n**Input**: $ARGUMENTS\n\n---\n\n## Your Mission\n\nExecute the implementation plan from `/investigate-issue`:\n\n1. Load and validate the artifact\n2. Ensure git state is correct\n3. Discover and install dependencies in the worktree\n4. Implement the changes exactly as specified\n5. Run validation\n6. Commit changes\n7. Write implementation report\n\n**Golden Rule**: Follow the artifact. If something seems wrong, validate it first - don't silently deviate.\n\n---\n\n## Phase 1: LOAD - Get the Artifact\n\n### 1.1 Find Investigation Artifact\n\nLook for the investigation artifact from the previous step:\n\n```bash\n# Check for artifact in workflow runs directory\nls $ARTIFACTS_DIR/investigation.md\n```\n\n**If input is a specific path**, use that path directly.\n\n### 1.2 Load and Parse Artifact\n\n```bash\ncat {artifact-path}\n```\n\n**Extract from artifact:**\n- Issue number and title\n- Type (BUG/ENHANCEMENT/etc)\n- Files to modify (with line numbers)\n- Implementation steps\n- Validation commands\n- Test cases to add\n\n### 1.3 Validate Artifact Exists\n\n**If artifact not found:**\n```\n❌ Investigation artifact not found at $ARTIFACTS_DIR/investigation.md\n\nRun `/investigate-issue {number}` first to create the implementation plan.\n```\n\n**PHASE_1_CHECKPOINT:**\n- [ ] Artifact found and loaded\n- [ ] Key sections parsed (files, steps, validation)\n- [ ] Issue number extracted (if applicable)\n\n---\n\n## Phase 2: VALIDATE - Sanity Check\n\n### 2.1 Verify Plan Accuracy\n\nFor each file mentioned in the artifact:\n- Read the actual current code\n- Compare to what artifact expects\n- Check if the \"current code\" snippets match reality\n\n**If significant drift detected:**\n```\n⚠️ Code has changed since investigation:\n\nFile: src/x.ts:45\n- Artifact expected: {snippet}\n- Actual code: {different snippet}\n\nOptions:\n1. Re-run /investigate-issue to get fresh analysis\n2. Proceed carefully with manual adjustments\n```\n\n### 2.2 Confirm Approach Makes Sense\n\nAsk yourself:\n- Does the proposed fix actually address the root cause?\n- Are there obvious problems with the approach?\n- Has something changed that invalidates the plan?\n\n**If plan seems wrong:**\n- STOP\n- Explain what's wrong\n- Suggest re-investigation\n\n**PHASE_2_CHECKPOINT:**\n- [ ] Artifact matches current codebase state\n- [ ] Approach still makes sense\n- [ ] No blocking issues identified\n\n---\n\n## Phase 3: GIT-CHECK - Ensure Correct State\n\n### 3.1 Check Current Git State\n\n```bash\n# What branch are we on?\ngit branch --show-current\n\n# Are we in a worktree?\ngit rev-parse --show-toplevel\ngit worktree list\n\n# Is working directory clean?\ngit status --porcelain\n\n# Are we up to date with remote?\ngit fetch origin\ngit status\n```\n\n### 3.2 Decision Tree\n\n```text\n┌─ IN WORKTREE?\n│  └─ YES → Use current branch AS-IS. Do NOT switch branches. Do NOT create\n│           new branches. The isolation system has already set up the correct\n│           branch; any deviation operates on the wrong code.\n│           Log: \"Using worktree at {path} on branch {branch}\"\n│\n├─ ON $BASE_BRANCH? (main, master, or configured base branch)\n│  └─ Q: Working directory clean?\n│     ├─ YES → Create branch: fix/issue-{number}-{slug}\n│     │        git checkout -b fix/issue-{number}-{slug}\n│     │        (only applies outside a worktree — e.g., manual CLI usage)\n│     └─ NO  → STOP: \"Uncommitted changes on $BASE_BRANCH.\n│              Please commit or stash before proceeding.\"\n│\n├─ ON OTHER BRANCH?\n│  └─ Use it AS-IS (assume it was set up for this work).\n│     Do NOT switch to another branch (e.g., one shown by `git branch` but\n│     not currently checked out).\n│     If branch name doesn't contain issue number:\n│       Warn: \"Branch '{name}' may not be for issue #{number}\"\n│\n└─ DIRTY STATE?\n   └─ STOP: \"Uncommitted changes. Please commit or stash first.\"\n```\n\n### 3.3 Ensure Up-to-Date\n\n```bash\n# If branch tracks remote\ngit pull --rebase origin $BASE_BRANCH 2>/dev/null || git pull origin $BASE_BRANCH\n```\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Git state is clean and correct\n- [ ] On appropriate branch (created or existing)\n- [ ] Up to date with base branch\n\n---\n\n## Phase 4: DEPENDENCIES - Discover and Install\n\n### 4.1 Detect Install Command\n\nInspect the worktree for lock/config files and choose the install command:\n\n- `package.json` + `bun.lock` → `bun install`\n- `package.json` + `package-lock.json` → `npm install`\n- `package.json` + `yarn.lock` → `yarn install`\n- `package.json` + `pnpm-lock.yaml` → `pnpm install`\n- `requirements.txt` → `pip install -r requirements.txt`\n- `pyproject.toml` + `poetry.lock` → `poetry install`\n- `Cargo.toml` → `cargo build`\n- `go.mod` → `go mod download`\n\n### 4.2 Run Install\n\nRun the chosen install command from the worktree root before any validation or tests.\n\n### 4.3 Failure Handling\n\nIf install fails, STOP and report the error. Do not proceed to validation with missing dependencies.\n\n**PHASE_4_CHECKPOINT:**\n- [ ] Install command discovered\n- [ ] Dependencies installed successfully\n\n---\n\n## Phase 5: IMPLEMENT - Make Changes\n\n### 5.1 Execute Each Step\n\nFor each step in the artifact's Implementation Plan:\n\n1. **Read the target file** - understand current state\n2. **Make the change** - exactly as specified\n3. **Verify types compile** - `bun run type-check`\n\n### 5.2 Implementation Rules\n\n**DO:**\n- Follow artifact steps in order\n- Match existing code style exactly\n- Copy patterns from \"Patterns to Follow\" section\n- Add tests as specified\n\n**DON'T:**\n- Refactor unrelated code\n- Add \"improvements\" not in the plan\n- Change formatting of untouched lines\n- Deviate from the artifact without noting it\n\n### 5.3 Handle Each File Type\n\n**For UPDATE files:**\n- Read current content\n- Find the exact lines mentioned\n- Make the specified change\n- Preserve surrounding code\n\n**For CREATE files:**\n- Use patterns from artifact\n- Follow existing file structure conventions\n- Include all specified content\n\n**For test files:**\n- Add test cases as specified\n- Follow existing test patterns\n- Ensure tests actually test the fix\n\n### 5.4 Track Deviations\n\nIf you must deviate from the artifact:\n- Note what changed and why\n- Include in implementation report\n\n**PHASE_5_CHECKPOINT:**\n- [ ] All steps from artifact executed\n- [ ] Types compile after each change\n- [ ] Tests added as specified\n- [ ] Any deviations documented\n\n---\n\n## Phase 6: VERIFY - Run Validation\n\n### 6.1 Run Artifact Validation Commands\n\nExecute each command from the artifact's Validation section:\n\n```bash\nbun run type-check\nbun test {pattern-from-artifact}\nbun run lint\n```\n\n### 6.2 Check Results\n\n**All must pass before proceeding.**\n\nIf failures:\n1. Analyze what's wrong\n2. Fix the issue\n3. Re-run validation\n4. Note any fixes in implementation report\n\n### 6.3 Manual Verification (if specified)\n\nExecute any manual verification steps from the artifact.\n\n**PHASE_6_CHECKPOINT:**\n- [ ] Type check passes\n- [ ] Tests pass\n- [ ] Lint passes\n- [ ] Manual verification complete (if applicable)\n\n---\n\n## Phase 7: COMMIT - Save Changes\n\n### 7.1 Stage Changes\n\nStage **only** the files you actually edited — never `git add -A`, `git add .`, or `git add -u`. List them by name:\n\n```bash\ngit add path/to/file1 path/to/file2 ...\ngit status --porcelain  # verify nothing scratch/review/PR-body is staged\n```\n\n**Never stage**:\n\n- `.pr-body.md`, `pr-body.md`, `*.scratch.md`, `*.tmp.md`\n- `review/`, `*-report.md` at the repo root\n- Anything under `$ARTIFACTS_DIR`\n\n### 7.2 Write Commit Message\n\n**Format:**\n```\nFix: {brief description} (#{issue-number})\n\n{Problem statement from artifact - 1-2 sentences}\n\nChanges:\n- {Change 1 from artifact}\n- {Change 2 from artifact}\n- Added test for {case}\n\nFixes #{issue-number}\n```\n\n**Commit:**\n```bash\ngit commit -m \"$(cat <<'EOF'\nFix: {title} (#{number})\n\n{problem statement}\n\nChanges:\n- {change 1}\n- {change 2}\n\nFixes #{number}\nEOF\n)\"\n```\n\n**PHASE_7_CHECKPOINT:**\n- [ ] All changes committed\n- [ ] Commit message references issue\n\n---\n\n## Phase 8: WRITE - Implementation Report\n\n### 8.1 Write Implementation Artifact\n\nWrite to `$ARTIFACTS_DIR/implementation.md`:\n\n```markdown\n# Implementation Report\n\n**Issue**: #{number}\n**Generated**: {YYYY-MM-DD HH:MM}\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Tasks Completed\n\n| # | Task | File | Status |\n|---|------|------|--------|\n| 1 | {task} | `src/x.ts` | ✅ |\n| 2 | {task} | `src/x.test.ts` | ✅ |\n\n---\n\n## Files Changed\n\n| File | Action | Lines |\n|------|--------|-------|\n| `src/x.ts` | UPDATE | +{N}/-{M} |\n| `src/x.test.ts` | CREATE | +{N} |\n\n---\n\n## Deviations from Investigation\n\n{If none: \"Implementation matched the investigation exactly.\"}\n\n{If any:}\n### Deviation 1: {title}\n\n**Expected**: {from investigation}\n**Actual**: {what was done}\n**Reason**: {why}\n\n---\n\n## Validation Results\n\n| Check | Result |\n|-------|--------|\n| Type check | ✅ |\n| Tests | ✅ ({N} passed) |\n| Lint | ✅ |\n```\n\n**PHASE_8_CHECKPOINT:**\n- [ ] Implementation artifact written\n\n---\n\n## Phase 9: OUTPUT - Report to User\n\nSkip archiving - artifacts remain in place for review workflow to access.\n\n---\n\n```markdown\n## Implementation Complete\n\n**Issue**: #{number} - {title}\n**Branch**: `{branch-name}`\n\n### Changes Made\n\n| File | Change |\n|------|--------|\n| `src/x.ts` | {description} |\n| `src/x.test.ts` | Added test |\n\n### Validation\n\n| Check | Result |\n|-------|--------|\n| Type check | ✅ Pass |\n| Tests | ✅ Pass |\n| Lint | ✅ Pass |\n\n### Artifacts\n\n- 📄 Investigation: `$ARTIFACTS_DIR/investigation.md`\n- 📄 Implementation: `$ARTIFACTS_DIR/implementation.md`\n\n### Next Step\n\nProceeding to PR creation...\n```\n\n---\n\n## Handling Edge Cases\n\n### Artifact is outdated\n- Warn user about drift\n- Suggest re-running `/investigate-issue`\n- Can proceed with caution if changes are minor\n\n### Tests fail after implementation\n- Debug the failure\n- Fix the code (not the test, unless test is wrong)\n- Re-run validation\n- Note the additional fix in implementation report\n\n### Merge conflicts during rebase\n- Resolve conflicts\n- Re-run full validation\n- Note conflict resolution in implementation report\n\n### Already on a branch with changes\n- Use the existing branch\n- Warn if branch name doesn't match issue\n- Don't create a new branch\n\n### In a worktree\n- Use it as-is\n- Assume it was created for this purpose\n- Log that worktree is being used\n\n---\n\n## Success Criteria\n\n- **PLAN_EXECUTED**: All investigation steps completed\n- **VALIDATION_PASSED**: All checks green\n- **CHANGES_COMMITTED**: All changes committed to branch\n- **IMPLEMENTATION_ARTIFACT**: Written to $ARTIFACTS_DIR/\n- **READY_FOR_PR**: Workflow continues to PR creation\n",
  "archon-implement-issue": "---\ndescription: Implement a fix from investigation artifact - code changes, PR, and self-review\nargument-hint: <issue-number|artifact-path>\n---\n\n# Implement Issue\n\n**Input**: $ARGUMENTS\n\n---\n\n## Your Mission\n\nExecute the implementation plan from `/investigate-issue`:\n\n1. Load and validate the artifact\n2. Ensure git state is correct\n3. Discover and install dependencies in the worktree\n4. Implement the changes exactly as specified\n5. Run validation\n6. Create PR linked to issue\n7. Run self-review and post findings\n8. Archive the artifact\n\n**Golden Rule**: Follow the artifact. If something seems wrong, validate it first - don't silently deviate.\n\n---\n\n## Phase 1: LOAD - Get the Artifact\n\n### 1.1 Find Investigation Artifact\n\nLook for the investigation artifact from the previous step:\n\n```bash\n# Check for artifact in workflow runs directory\nls $ARTIFACTS_DIR/investigation.md\n```\n\n**If input is a specific path**, use that path directly.\n\n### 1.2 Load and Parse Artifact\n\n```bash\ncat {artifact-path}\n```\n\n**Extract from artifact:**\n- Issue number and title\n- Type (BUG/ENHANCEMENT/etc)\n- Files to modify (with line numbers)\n- Implementation steps\n- Validation commands\n- Test cases to add\n\n### 1.3 Validate Artifact Exists\n\n**If artifact not found:**\n```\n❌ Investigation artifact not found at $ARTIFACTS_DIR/investigation.md\n\nRun `/investigate-issue {number}` first to create the implementation plan.\n```\n\n**PHASE_1_CHECKPOINT:**\n- [ ] Artifact found and loaded\n- [ ] Key sections parsed (files, steps, validation)\n- [ ] Issue number extracted (if applicable)\n\n---\n\n## Phase 2: VALIDATE - Sanity Check\n\n### 2.1 Verify Plan Accuracy\n\nFor each file mentioned in the artifact:\n- Read the actual current code\n- Compare to what artifact expects\n- Check if the \"current code\" snippets match reality\n\n**If significant drift detected:**\n```\n⚠️ Code has changed since investigation:\n\nFile: src/x.ts:45\n- Artifact expected: {snippet}\n- Actual code: {different snippet}\n\nOptions:\n1. Re-run /investigate-issue to get fresh analysis\n2. Proceed carefully with manual adjustments\n```\n\n### 2.2 Confirm Approach Makes Sense\n\nAsk yourself:\n- Does the proposed fix actually address the root cause?\n- Are there obvious problems with the approach?\n- Has something changed that invalidates the plan?\n\n**If plan seems wrong:**\n- STOP\n- Explain what's wrong\n- Suggest re-investigation\n\n**PHASE_2_CHECKPOINT:**\n- [ ] Artifact matches current codebase state\n- [ ] Approach still makes sense\n- [ ] No blocking issues identified\n\n---\n\n## Phase 3: GIT-CHECK - Ensure Correct State\n\n### 3.1 Check Current Git State\n\n```bash\n# What branch are we on?\ngit branch --show-current\n\n# Are we in a worktree?\ngit rev-parse --show-toplevel\ngit worktree list\n\n# Is working directory clean?\ngit status --porcelain\n\n# Are we up to date with remote?\ngit fetch origin\ngit status\n```\n\n### 3.2 Decision Tree\n\n```text\n┌─ IN WORKTREE?\n│  └─ YES → Use current branch AS-IS. Do NOT switch branches. Do NOT create\n│           new branches. The isolation system has already set up the correct\n│           branch; any deviation operates on the wrong code.\n│           Log: \"Using worktree at {path} on branch {branch}\"\n│\n├─ ON $BASE_BRANCH? (main, master, or configured base branch)\n│  └─ Q: Working directory clean?\n│     ├─ YES → Create branch: fix/issue-{number}-{slug}\n│     │        git checkout -b fix/issue-{number}-{slug}\n│     │        (only applies outside a worktree — e.g., manual CLI usage)\n│     └─ NO  → STOP: \"Uncommitted changes on $BASE_BRANCH.\n│              Please commit or stash before proceeding.\"\n│\n├─ ON OTHER BRANCH?\n│  └─ Use it AS-IS (assume it was set up for this work).\n│     Do NOT switch to another branch (e.g., one shown by `git branch` but\n│     not currently checked out).\n│     If branch name doesn't contain issue number:\n│       Warn: \"Branch '{name}' may not be for issue #{number}\"\n│\n└─ DIRTY STATE?\n   └─ STOP: \"Uncommitted changes. Please commit or stash first.\"\n```\n\n### 3.3 Ensure Up-to-Date\n\n```bash\n# If branch tracks remote\ngit pull --rebase origin $BASE_BRANCH 2>/dev/null || git pull origin $BASE_BRANCH\n```\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Git state is clean and correct\n- [ ] On appropriate branch (created or existing)\n- [ ] Up to date with base branch\n\n---\n\n## Phase 4: DEPENDENCIES - Discover and Install\n\n### 4.1 Detect Install Command\n\nInspect the worktree for lock/config files and choose the install command:\n\n- `package.json` + `bun.lock` → `bun install`\n- `package.json` + `package-lock.json` → `npm install`\n- `package.json` + `yarn.lock` → `yarn install`\n- `package.json` + `pnpm-lock.yaml` → `pnpm install`\n- `requirements.txt` → `pip install -r requirements.txt`\n- `pyproject.toml` + `poetry.lock` → `poetry install`\n- `Cargo.toml` → `cargo build`\n- `go.mod` → `go mod download`\n\n### 4.2 Run Install\n\nRun the chosen install command from the worktree root before any validation or tests.\n\n### 4.3 Failure Handling\n\nIf install fails, STOP and report the error. Do not proceed to validation with missing dependencies.\n\n**PHASE_4_CHECKPOINT:**\n- [ ] Install command discovered\n- [ ] Dependencies installed successfully\n\n---\n\n## Phase 5: IMPLEMENT - Make Changes\n\n### 5.1 Execute Each Step\n\nFor each step in the artifact's Implementation Plan:\n\n1. **Read the target file** - understand current state\n2. **Make the change** - exactly as specified\n3. **Verify types compile** - `bun run type-check`\n\n### 5.2 Implementation Rules\n\n**DO:**\n- Follow artifact steps in order\n- Match existing code style exactly\n- Copy patterns from \"Patterns to Follow\" section\n- Add tests as specified\n\n**DON'T:**\n- Refactor unrelated code\n- Add \"improvements\" not in the plan\n- Change formatting of untouched lines\n- Deviate from the artifact without noting it\n\n### 5.3 Handle Each File Type\n\n**For UPDATE files:**\n- Read current content\n- Find the exact lines mentioned\n- Make the specified change\n- Preserve surrounding code\n\n**For CREATE files:**\n- Use patterns from artifact\n- Follow existing file structure conventions\n- Include all specified content\n\n**For test files:**\n- Add test cases as specified\n- Follow existing test patterns\n- Ensure tests actually test the fix\n\n### 5.4 Track Deviations\n\nIf you must deviate from the artifact:\n- Note what changed and why\n- Include in PR description\n\n**PHASE_5_CHECKPOINT:**\n- [ ] All steps from artifact executed\n- [ ] Types compile after each change\n- [ ] Tests added as specified\n- [ ] Any deviations documented\n\n---\n\n## Phase 6: VERIFY - Run Validation\n\n### 6.1 Run Artifact Validation Commands\n\nExecute each command from the artifact's Validation section:\n\n```bash\nbun run type-check\nbun test {pattern-from-artifact}\nbun run lint\n```\n\n### 6.2 Check Results\n\n**All must pass before proceeding.**\n\nIf failures:\n1. Analyze what's wrong\n2. Fix the issue\n3. Re-run validation\n4. Note any fixes in PR description\n\n### 6.3 Manual Verification (if specified)\n\nExecute any manual verification steps from the artifact.\n\n**PHASE_6_CHECKPOINT:**\n- [ ] Type check passes\n- [ ] Tests pass\n- [ ] Lint passes\n- [ ] Manual verification complete (if applicable)\n\n---\n\n## Phase 7: COMMIT - Save Changes\n\n### 7.1 Stage Changes\n\nStage **only** the files you actually edited — never `git add -A`, `git add .`, or `git add -u`. List them by name:\n\n```bash\ngit add path/to/file1 path/to/file2 ...\ngit status --porcelain  # verify nothing scratch/review/PR-body is staged\n```\n\n**Never stage**:\n\n- `.pr-body.md`, `pr-body.md`, `*.scratch.md`, `*.tmp.md`\n- `review/`, `*-report.md` at the repo root\n- Anything under `$ARTIFACTS_DIR`\n\n### 7.2 Write Commit Message\n\n**Format:**\n```\nFix: {brief description} (#{issue-number})\n\n{Problem statement from artifact - 1-2 sentences}\n\nChanges:\n- {Change 1 from artifact}\n- {Change 2 from artifact}\n- Added test for {case}\n\nFixes #{issue-number}\n```\n\n**Commit:**\n```bash\ngit commit -m \"$(cat <<'EOF'\nFix: {title} (#{number})\n\n{problem statement}\n\nChanges:\n- {change 1}\n- {change 2}\n\nFixes #{number}\nEOF\n)\"\n```\n\n**PHASE_7_CHECKPOINT:**\n- [ ] All changes committed\n- [ ] Commit message references issue\n\n---\n\n## Phase 8: PR - Create Pull Request\n\n**Before creating a PR**, check if one already exists for this issue or branch using `gh pr list`. If a PR already exists, skip creation and use the existing one.\n\n### 8.1 Push to Remote\n\n```bash\ngit push -u origin HEAD\n```\n\nIf branch was rebased:\n```bash\ngit push -u origin HEAD --force-with-lease\n```\n\n### 8.2 Prepare PR Body\n\nLook for the project's PR template at `.github/pull_request_template.md`, `.github/PULL_REQUEST_TEMPLATE.md`, or `docs/PULL_REQUEST_TEMPLATE.md`. Read whichever one exists.\n\n**If template found**: Use it as the structure, fill in **every section** with details from the artifact (root cause, changes, validation results, etc.). Don't skip sections or leave placeholders. Make sure to include `Fixes #{number}`.\n\n**If no template**, write a body covering: summary, root cause, changes table, validation evidence, and `Fixes #{number}`.\n\n### 8.3 Create PR\n\nWrite the prepared body to `$ARTIFACTS_DIR/pr-body.md`, then:\n\n```bash\ngh pr create --title \"Fix: {title} (#{number})\" \\\n  --body-file $ARTIFACTS_DIR/pr-body.md \\\n  --base $BASE_BRANCH\n```\n\n### 8.3 Get PR Number\n\n```bash\nPR_URL=$(gh pr view --json url -q '.url')\nPR_NUMBER=$(gh pr view --json number -q '.number')\n```\n\n**PHASE_8_CHECKPOINT:**\n- [ ] Changes pushed to remote\n- [ ] PR created\n- [ ] PR linked to issue with \"Fixes #{number}\"\n\n---\n\n## Phase 9: WRITE - Implementation Report\n\n### 9.1 Write Implementation Artifact\n\nWrite to `$ARTIFACTS_DIR/implementation.md`:\n\n```markdown\n# Implementation Report\n\n**Issue**: #{number}\n**Generated**: {YYYY-MM-DD HH:MM}\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Tasks Completed\n\n| # | Task | File | Status |\n|---|------|------|--------|\n| 1 | {task} | `src/x.ts` | ✅ |\n| 2 | {task} | `src/x.test.ts` | ✅ |\n\n---\n\n## Files Changed\n\n| File | Action | Lines |\n|------|--------|-------|\n| `src/x.ts` | UPDATE | +{N}/-{M} |\n| `src/x.test.ts` | CREATE | +{N} |\n\n---\n\n## Deviations from Investigation\n\n{If none: \"Implementation matched the investigation exactly.\"}\n\n{If any:}\n### Deviation 1: {title}\n\n**Expected**: {from investigation}\n**Actual**: {what was done}\n**Reason**: {why}\n\n---\n\n## Validation Results\n\n| Check | Result |\n|-------|--------|\n| Type check | ✅ |\n| Tests | ✅ ({N} passed) |\n| Lint | ✅ |\n\n---\n\n## PR Created\n\n- **Number**: #{pr-number}\n- **URL**: {pr-url}\n- **Branch**: {branch-name}\n```\n\n**PHASE_9_CHECKPOINT:**\n- [ ] Implementation artifact written\n\n---\n\n## Phase 10: OUTPUT - Report to User\n\nSkip archiving - artifacts remain in place for review workflow to access.\n\n---\n\n```markdown\n## Implementation Complete\n\n**Issue**: #{number} - {title}\n**Branch**: `{branch-name}`\n**PR**: #{pr-number} - {pr-url}\n\n### Changes Made\n\n| File | Change |\n|------|--------|\n| `src/x.ts` | {description} |\n| `src/x.test.ts` | Added test |\n\n### Validation\n\n| Check | Result |\n|-------|--------|\n| Type check | ✅ Pass |\n| Tests | ✅ Pass |\n| Lint | ✅ Pass |\n\n### Artifacts\n\n- 📄 Investigation: `$ARTIFACTS_DIR/investigation.md`\n- 📄 Implementation: `$ARTIFACTS_DIR/implementation.md`\n\n### Next Step\n\nProceeding to comprehensive code review...\n```\n\n---\n\n## Handling Edge Cases\n\n### Artifact is outdated\n- Warn user about drift\n- Suggest re-running `/investigate-issue`\n- Can proceed with caution if changes are minor\n\n### Tests fail after implementation\n- Debug the failure\n- Fix the code (not the test, unless test is wrong)\n- Re-run validation\n- Note the additional fix in PR\n\n### Merge conflicts during rebase\n- Resolve conflicts\n- Re-run full validation\n- Note conflict resolution in PR\n\n### PR creation fails\n- Check if PR already exists for branch\n- Check for permission issues\n- Provide manual gh command\n\n### Already on a branch with changes\n- Use the existing branch\n- Warn if branch name doesn't match issue\n- Don't create a new branch\n\n### In a worktree\n- Use it as-is\n- Assume it was created for this purpose\n- Log that worktree is being used\n\n---\n\n## Success Criteria\n\n- **PLAN_EXECUTED**: All investigation steps completed\n- **VALIDATION_PASSED**: All checks green\n- **PR_CREATED**: PR exists and linked to issue\n- **IMPLEMENTATION_ARTIFACT**: Written to runs/$WORKFLOW_ID/\n- **READY_FOR_REVIEW**: Workflow continues to comprehensive review\n",
  "archon-implement-review-fixes": "---\ndescription: Implement CRITICAL and HIGH fixes from review, add tests, report remaining issues\nargument-hint: (none - reads from consolidated review artifact)\n---\n\n# Implement Review Fixes\n\n---\n\n## IMPORTANT: Output Behavior\n\n**Your output will be posted as a GitHub comment.** Keep your working output minimal:\n- Do NOT narrate each step (\"Now I'll read the file...\", \"Let me check...\")\n- Do NOT output verbose progress updates\n- Only output the final structured report at the end\n- Use the TodoWrite tool to track progress silently\n\n---\n\n## Your Mission\n\nRead the consolidated review artifact and implement all CRITICAL and HIGH priority fixes. Add tests for fixed code if missing. Commit and push changes. Report what was fixed, what wasn't (and why), and suggest follow-up issues for remaining items.\n\n**Output artifact**: `$ARTIFACTS_DIR/review/fix-report.md`\n**Git action**: Commit AND push fixes to the PR branch\n**GitHub action**: Post fix report comment\n\n---\n\n## Phase 1: LOAD - Get Fix List\n\n### 1.1 Get PR Number from Registry\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number)\n\n# Get the PR's head branch name\nHEAD_BRANCH=$(gh pr view $PR_NUMBER --json headRefName --jq '.headRefName')\necho \"PR: $PR_NUMBER, Branch: $HEAD_BRANCH\"\n```\n\n### 1.2 Checkout the PR Branch\n\n**CRITICAL: Work on the PR's actual branch, not a new branch.**\n\n```bash\n# Fetch and checkout the PR's branch\ngit fetch origin $HEAD_BRANCH\ngit checkout $HEAD_BRANCH\ngit pull origin $HEAD_BRANCH\n```\n\n### 1.3 Read Consolidated Review\n\n```bash\ncat $ARTIFACTS_DIR/review/consolidated-review.md\n```\n\nExtract:\n- All CRITICAL issues with fixes\n- All HIGH issues with fixes\n- MEDIUM issues (for reporting)\n- LOW issues (for reporting)\n\n### 1.4 Read Individual Artifacts for Details\n\nIf consolidated doesn't have full fix code, read original artifacts:\n\n```bash\ncat $ARTIFACTS_DIR/review/code-review-findings.md\ncat $ARTIFACTS_DIR/review/error-handling-findings.md\ncat $ARTIFACTS_DIR/review/test-coverage-findings.md\ncat $ARTIFACTS_DIR/review/docs-impact-findings.md\n```\n\n### 1.5 Check Current Git State\n\n```bash\ngit status --porcelain\ngit branch --show-current\n```\n\nVerify you are on the correct PR branch (should be `$HEAD_BRANCH`).\n\n**PHASE_1_CHECKPOINT:**\n- [ ] PR number identified\n- [ ] On the correct PR branch (NOT base branch, NOT a new branch)\n- [ ] Consolidated review loaded\n- [ ] CRITICAL/HIGH issues extracted\n\n---\n\n## Phase 2: IMPLEMENT - Apply Fixes\n\n### 2.1 For Each CRITICAL Issue\n\n1. **Read the file**\n2. **Apply the recommended fix**\n3. **Verify fix compiles**: `bun run type-check`\n4. **Track**: Note what was changed\n\n### 2.2 For Each HIGH Issue\n\nSame process as CRITICAL.\n\n### 2.3 For Test Coverage Gaps\n\nIf test-coverage-agent identified missing tests for fixed code:\n\n1. **Create/update test file**\n2. **Add tests for the fix**\n3. **Verify tests pass**: `bun test {file}`\n\n### 2.4 Handle Unfixable Issues\n\nIf a fix cannot be applied:\n- **Conflict**: Code has changed since review\n- **Complex**: Requires architectural changes\n- **Unclear**: Recommendation is ambiguous\n- **Risk**: Fix might break other things\n\nDocument the reason clearly.\n\n**PHASE_2_CHECKPOINT:**\n- [ ] All CRITICAL fixes attempted\n- [ ] All HIGH fixes attempted\n- [ ] Tests added for fixes\n- [ ] Unfixable issues documented\n\n---\n\n## Phase 3: VALIDATE - Verify Fixes\n\n### 3.1 Type Check\n\n```bash\nbun run type-check\n```\n\nMust pass. If not, fix type errors.\n\n### 3.2 Lint\n\n```bash\nbun run lint\n```\n\nFix any lint errors introduced.\n\n### 3.3 Run Tests\n\n```bash\nbun test\n```\n\nAll tests must pass. If new tests fail, fix them.\n\n### 3.4 Build Check\n\n```bash\nbun run build\n```\n\nMust succeed.\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Type check passes\n- [ ] Lint passes\n- [ ] All tests pass\n- [ ] Build succeeds\n\n---\n\n## Phase 4: COMMIT AND PUSH - Save and Push Changes\n\n### 4.1 Stage Changes\n\nStage **only** the files you actually edited while applying review fixes — never `git add -A`, `git add .`, or `git add -u`. List them by name:\n\n```bash\ngit add path/to/file1 path/to/file2 ...\ngit status --porcelain  # verify nothing scratch/review/PR-body is staged\n```\n\n**Never stage**:\n\n- `.pr-body.md`, `pr-body.md`, `*.scratch.md`, `*.tmp.md`\n- `review/`, `*-report.md` at the repo root\n- Anything under `$ARTIFACTS_DIR` (review artifacts live here, not in the worktree)\n\n### 4.2 Commit\n\n```bash\ngit commit -m \"fix: Address review findings (CRITICAL/HIGH)\n\nFixes applied:\n- {brief list of fixes}\n\nTests added:\n- {list of new tests if any}\n\nSkipped (see review artifacts):\n- {brief list of unfixable if any}\n\nReview artifacts: $ARTIFACTS_DIR/review/\"\n```\n\n### 4.3 Push to PR Branch\n\n**Push the fixes to the PR branch so they appear in the PR.**\n\n```bash\ngit push origin $HEAD_BRANCH\n```\n\nIf push fails due to divergence:\n```bash\ngit pull --rebase origin $HEAD_BRANCH\ngit push origin $HEAD_BRANCH\n```\n\n**PHASE_4_CHECKPOINT:**\n- [ ] Changes committed\n- [ ] Changes pushed to PR branch\n- [ ] PR now shows the fixes\n\n---\n\n## Phase 5: GENERATE - Create Fix Report\n\nWrite to `$ARTIFACTS_DIR/review/fix-report.md`:\n\n```markdown\n# Fix Report: PR #{number}\n\n**Date**: {ISO timestamp}\n**Status**: {COMPLETE | PARTIAL}\n**Branch**: {HEAD_BRANCH}\n\n---\n\n## Summary\n\n{2-3 sentence overview of fixes applied}\n\n---\n\n## Fixes Applied\n\n### CRITICAL Fixes ({n}/{total})\n\n| Issue | Location | Status | Details |\n|-------|----------|--------|---------|\n| {title} | `file:line` | ✅ FIXED | {what was done} |\n| {title} | `file:line` | ❌ SKIPPED | {why} |\n\n---\n\n### HIGH Fixes ({n}/{total})\n\n| Issue | Location | Status | Details |\n|-------|----------|--------|---------|\n| {title} | `file:line` | ✅ FIXED | {what was done} |\n\n---\n\n## Tests Added\n\n| Test File | Test Cases | For Issue |\n|-----------|------------|-----------|\n| `src/x.test.ts` | `it('should...')` | {issue title} |\n\n---\n\n## Not Fixed (Requires Manual Action)\n\n### {Issue Title}\n\n**Severity**: {CRITICAL/HIGH}\n**Location**: `{file}:{line}`\n**Reason Not Fixed**: {reason}\n\n**Suggested Action**:\n{What the user should do}\n\n---\n\n## MEDIUM Issues (User Decision Required)\n\n| Issue | Location | Options |\n|-------|----------|---------|\n| {title} | `file:line` | Fix now / Create issue / Skip |\n\n---\n\n## LOW Issues (For Consideration)\n\n| Issue | Location | Suggestion |\n|-------|----------|------------|\n| {title} | `file:line` | {brief suggestion} |\n\n---\n\n## Suggested Follow-up Issues\n\n| Issue Title | Priority | Related Finding |\n|-------------|----------|-----------------|\n| \"{title}\" | P{1/2/3} | {which finding} |\n\n---\n\n## Validation Results\n\n| Check | Status |\n|-------|--------|\n| Type check | ✅ |\n| Lint | ✅ |\n| Tests | ✅ ({n} passed) |\n| Build | ✅ |\n\n---\n\n## Git Status\n\n- **Branch**: {HEAD_BRANCH}\n- **Commit**: {commit-hash}\n- **Pushed**: ✅ Yes\n```\n\n**PHASE_5_CHECKPOINT:**\n- [ ] Fix report created\n- [ ] All fixes documented\n\n---\n\n## Phase 6: POST - GitHub Comment\n\n### 6.1 Post Fix Report\n\n```bash\ngh pr comment {number} --body \"$(cat <<'EOF'\n# ⚡ Auto-Fix Report\n\n**Status**: {COMPLETE | PARTIAL}\n**Pushed**: ✅ Changes pushed to PR\n\n---\n\n## Fixes Applied\n\n| Severity | Fixed | Skipped |\n|----------|-------|---------|\n| 🔴 CRITICAL | {n} | {n} |\n| 🟠 HIGH | {n} | {n} |\n\n### What Was Fixed\n\n{For each fix:}\n- ✅ **{title}** (`{file}:{line}`) - {brief description}\n\n### Tests Added\n\n{If any:}\n- `{test-file}`: {n} new test cases\n\n---\n\n## ❌ Not Fixed (Manual Action Required)\n\n{If any:}\n- **{title}** (`{file}`) - {reason}\n\n---\n\n## 🟡 MEDIUM Issues (Your Decision)\n\n{If any:}\n| Issue | Options |\n|-------|---------|\n| {title} | Fix now / Create issue / Skip |\n\n---\n\n## 📋 Suggested Follow-up Issues\n\n{If any items should become issues:}\n1. **{Issue Title}** (P{1/2/3}) - {brief description}\n\n---\n\n## Validation\n\n✅ Type check | ✅ Lint | ✅ Tests | ✅ Build\n\n---\n\n*Auto-fixed by Archon comprehensive-pr-review workflow*\n*Fixes pushed to branch `{HEAD_BRANCH}`*\nEOF\n)\"\n```\n\n**PHASE_6_CHECKPOINT:**\n- [ ] GitHub comment posted\n\n---\n\n## Phase 7: OUTPUT - Final Report\n\nOutput only this summary (keep it brief):\n\n```markdown\n## ✅ Fix Implementation Complete\n\n**PR**: #{number}\n**Branch**: {HEAD_BRANCH}\n**Status**: {COMPLETE | PARTIAL}\n\n| Severity | Fixed |\n|----------|-------|\n| CRITICAL | {n}/{total} |\n| HIGH | {n}/{total} |\n\n**Validation**: ✅ All checks pass\n**Pushed**: ✅ Changes pushed to PR\n\nSee fix report: `$ARTIFACTS_DIR/review/fix-report.md`\n```\n\n---\n\n## Error Handling\n\n### Type Check Fails After Fix\n\n1. Review the error\n2. Adjust the fix\n3. Re-run type check\n4. If still failing, mark as \"Not Fixed\" with reason\n\n### Tests Fail\n\n1. Check if fix caused the failure\n2. Either: fix the implementation, or fix the test\n3. If unclear, mark as \"Not Fixed\" for manual review\n\n### Push Fails\n\n1. Pull with rebase: `git pull --rebase origin $HEAD_BRANCH`\n2. Resolve any conflicts\n3. Push again\n\n---\n\n## Success Criteria\n\n- **ON_CORRECT_BRANCH**: Working on PR's head branch, not base branch or new branch\n- **CRITICAL_ADDRESSED**: All CRITICAL issues attempted\n- **HIGH_ADDRESSED**: All HIGH issues attempted\n- **VALIDATION_PASSED**: Type check, lint, tests, build all pass\n- **COMMITTED_AND_PUSHED**: Changes committed AND pushed to PR branch\n- **REPORTED**: Fix report artifact and GitHub comment created\n",
  "archon-implement-tasks": "---\ndescription: Execute plan tasks with type-checking after each change\nargument-hint: (no arguments - reads from workflow artifacts)\n---\n\n# Implement Tasks\n\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Your Mission\n\nExecute each task from the plan, validating after every change.\n\n**Core Philosophy**:\n- Type-check after EVERY file change\n- Fix issues immediately before moving on\n- Document any deviations from the plan\n\n**This step assumes setup is complete** - branch exists, PR is created, plan is confirmed.\n\n---\n\n## Phase 1: LOAD - Read Context\n\n### 1.1 Load Plan Context\n\n```bash\ncat $ARTIFACTS_DIR/plan-context.md\n```\n\nExtract:\n- Files to change (CREATE/UPDATE list)\n- Validation commands (especially type-check)\n- Patterns to mirror\n\n### 1.2 Load Plan Confirmation\n\n```bash\ncat $ARTIFACTS_DIR/plan-confirmation.md\n```\n\nCheck:\n- Status is CONFIRMED or PROCEED WITH CAUTION\n- Note any warnings to handle during implementation\n\n### 1.3 Load Original Plan\n\nThe plan source path is in `plan-context.md`. Read the full plan for detailed task instructions:\n\n```bash\ncat {plan-source-path}\n```\n\n### 1.4 Identify Package Manager\n\n```bash\ntest -f bun.lockb && echo \"bun\" || \\\ntest -f pnpm-lock.yaml && echo \"pnpm\" || \\\ntest -f yarn.lock && echo \"yarn\" || \\\ntest -f package-lock.json && echo \"npm\" || \\\necho \"unknown\"\n```\n\nStore the runner for validation commands.\n\n**PHASE_1_CHECKPOINT:**\n\n- [ ] Plan context loaded\n- [ ] Confirmation status verified\n- [ ] Original plan loaded\n- [ ] Package manager identified\n\n---\n\n## Phase 2: EXECUTE - Implement Each Task\n\n**For each task in the plan's \"Tasks\" or \"Step-by-Step Tasks\" section:**\n\n### 2.1 Read Task Context\n\nBefore implementing each task:\n\n1. **Read the MIRROR file** referenced in the task\n2. **Understand the pattern** to follow\n3. **Note any GOTCHA warnings**\n4. **Check IMPORTS** needed\n\n### 2.2 Implement the Task\n\nMake the change as specified:\n\n- **CREATE**: Write new file following the pattern\n- **UPDATE**: Modify existing file as described\n- **Follow patterns exactly** - match style, naming, structure\n\n### 2.3 Type-Check Immediately\n\n**After EVERY file change:**\n\n```bash\n{runner} run type-check\n```\n\n**If type-check fails:**\n\n1. Read the error message carefully\n2. Fix the type issue\n3. Re-run type-check\n4. Only proceed when passing\n\n**Do NOT accumulate errors** - fix each one before moving to the next task.\n\n### 2.4 Track Progress\n\nLog each task as completed:\n\n```\nTask 1: CREATE src/features/x/models.ts ✅\nTask 2: CREATE src/features/x/service.ts ✅\nTask 3: UPDATE src/routes/index.ts ✅\n```\n\n### 2.5 Handle Deviations\n\nIf you must deviate from the plan:\n\n1. **Document WHAT** changed\n2. **Document WHY** it changed\n3. **Continue** with the deviation noted\n\nCommon reasons for deviation:\n- Pattern file has changed since plan was created\n- Missing import discovered\n- Type incompatibility requires different approach\n- Better solution discovered during implementation\n\n**PHASE_2_CHECKPOINT (per task):**\n\n- [ ] Task implemented\n- [ ] Type-check passes\n- [ ] Progress logged\n- [ ] Deviations documented (if any)\n\n---\n\n## Phase 3: TESTS - Write Required Tests\n\n### 3.1 Test Requirements\n\nEvery new function/feature needs at least one test:\n\n- **New file created** → Create corresponding test file\n- **New function added** → Add test for that function\n- **Behavior changed** → Update existing tests\n\n### 3.2 Follow Test Patterns\n\nFind existing test files to mirror:\n\n```bash\nfind . -name \"*.test.ts\" -type f | head -5\n```\n\nRead a relevant test file to understand the project's test patterns.\n\n### 3.3 Write Tests\n\nFor each new/changed file, write tests that cover:\n\n1. **Happy path** - Normal expected behavior\n2. **Edge cases** - Boundary conditions from the plan\n3. **Error cases** - What happens with bad input\n\n### 3.4 Run Tests\n\n```bash\n{runner} test\n```\n\n**If tests fail:**\n\n1. Determine: bug in implementation or bug in test?\n2. Fix the actual issue (usually implementation)\n3. Re-run tests\n4. Repeat until green\n\n**PHASE_3_CHECKPOINT:**\n\n- [ ] Tests written for new code\n- [ ] All tests pass\n\n---\n\n## Phase 4: ARTIFACT - Write Implementation Progress\n\n### 4.1 Write Progress Artifact\n\nWrite to `$ARTIFACTS_DIR/implementation.md`:\n\n```markdown\n# Implementation Progress\n\n**Generated**: {YYYY-MM-DD HH:MM}\n**Workflow ID**: $WORKFLOW_ID\n**Status**: {COMPLETE | IN_PROGRESS | BLOCKED}\n\n---\n\n## Tasks Completed\n\n| # | Task | File | Status | Notes |\n|---|------|------|--------|-------|\n| 1 | {description} | `src/x.ts` | ✅ | |\n| 2 | {description} | `src/y.ts` | ✅ | |\n| 3 | {description} | `src/z.ts` | ✅ | Minor deviation - see below |\n\n**Progress**: {X} of {Y} tasks completed\n\n---\n\n## Files Changed\n\n| File | Action | Lines |\n|------|--------|-------|\n| `src/new-file.ts` | CREATE | +{N} |\n| `src/existing.ts` | UPDATE | +{N}/-{M} |\n\n---\n\n## Tests Written\n\n| Test File | Test Cases |\n|-----------|------------|\n| `src/x.test.ts` | `should do X`, `should handle Y` |\n| `src/y.test.ts` | `creates correctly`, `validates input` |\n\n---\n\n## Deviations from Plan\n\n{If none:}\nNo deviations. Implementation matched the plan exactly.\n\n{If any:}\n### Deviation 1: {brief title}\n\n**Task**: {which task}\n**Expected**: {what plan said}\n**Actual**: {what was done}\n**Reason**: {why the change was necessary}\n\n---\n\n## Type-Check Status\n\n- [x] Passes after all changes\n\n---\n\n## Test Status\n\n- [x] All tests pass\n- Tests added: {N}\n- Tests modified: {M}\n\n---\n\n## Issues Encountered\n\n{If none:}\nNo issues encountered.\n\n{If any:}\n### Issue 1: {title}\n\n**Problem**: {description}\n**Resolution**: {how it was fixed}\n\n---\n\n## Next Step\n\nContinue to `archon-validate` for full validation suite.\n```\n\n**PHASE_4_CHECKPOINT:**\n\n- [ ] Implementation artifact written\n- [ ] All tasks documented\n- [ ] Deviations noted\n- [ ] Test status recorded\n\n---\n\n## Phase 5: OUTPUT - Report Progress\n\n```markdown\n## Implementation Complete\n\n**Workflow ID**: `$WORKFLOW_ID`\n**Status**: ✅ All tasks executed\n\n### Progress Summary\n\n| Metric | Count |\n|--------|-------|\n| Tasks completed | {X}/{Y} |\n| Files created | {N} |\n| Files updated | {M} |\n| Tests written | {K} |\n\n### Type-Check\n\n✅ Passes\n\n### Tests\n\n✅ All pass ({N} tests)\n\n{If deviations:}\n### Deviations\n\n{count} deviation(s) from plan documented in artifact.\n\n### Artifact\n\nProgress written to: `$ARTIFACTS_DIR/implementation.md`\n\n### Next Step\n\nProceed to `archon-validate` for full validation (lint, build, integration tests).\n```\n\n---\n\n## Error Handling\n\n### Type-Check Fails\n\nDo NOT proceed to next task. Fix the issue:\n\n1. Read the error carefully\n2. Identify the file and line\n3. Fix the type issue\n4. Re-run type-check\n5. Only continue when green\n\n### Test Fails\n\n1. Read the failure output\n2. Identify: implementation bug or test bug?\n3. Fix the root cause\n4. Re-run tests\n\n### Pattern File Changed\n\nIf a pattern file has changed since the plan was created:\n\n1. Read the current version\n2. Adapt the implementation to match current patterns\n3. Document as a deviation\n4. Continue\n\n### Task Unclear\n\nIf a task description is ambiguous:\n\n1. Check the plan's context sections for clarity\n2. Look at the MIRROR file for guidance\n3. Make a reasonable decision\n4. Document the interpretation as a deviation\n\n---\n\n## Success Criteria\n\n- **TASKS_COMPLETE**: All tasks from plan executed\n- **TYPES_PASS**: Type-check passes after all changes\n- **TESTS_WRITTEN**: New code has tests\n- **TESTS_PASS**: All tests green\n- **DEVIATIONS_DOCUMENTED**: Any plan deviations noted\n- **ARTIFACT_WRITTEN**: Implementation progress artifact created\n",
  "archon-implement": "---\ndescription: Execute an implementation plan with rigorous validation loops\nargument-hint: <path/to/plan.md or GitHub issue URL>\n---\n\n# Implement Plan\n\n**Plan**: $ARGUMENTS\n\n---\n\n## Your Mission\n\nExecute the plan end-to-end with rigorous self-validation. You are autonomous.\n\n**Core Philosophy**: Validation loops catch mistakes early. Run checks after every change. Fix issues immediately. The goal is a working implementation, not just code that exists.\n\n**Golden Rule**: If a validation fails, fix it before moving on. Never accumulate broken state.\n\n---\n\n## Phase 0: DETECT - Project Environment\n\n### 0.1 Identify Package Manager\n\nCheck for these files to determine the project's toolchain:\n\n| File Found | Package Manager | Runner |\n|------------|-----------------|--------|\n| `bun.lockb` | bun | `bun` / `bun run` |\n| `pnpm-lock.yaml` | pnpm | `pnpm` / `pnpm run` |\n| `yarn.lock` | yarn | `yarn` / `yarn run` |\n| `package-lock.json` | npm | `npm run` |\n| `pyproject.toml` | uv/pip | `uv run` / `python` |\n| `Cargo.toml` | cargo | `cargo` |\n| `go.mod` | go | `go` |\n\n**Store the detected runner** - use it for all subsequent commands.\n\n### 0.2 Identify Validation Scripts\n\nCheck `package.json` (or equivalent) for available scripts:\n- Type checking: `type-check`, `typecheck`, `tsc`\n- Linting: `lint`, `lint:fix`\n- Testing: `test`, `test:unit`, `test:integration`\n- Building: `build`, `compile`\n\n**Use the plan's \"Validation Commands\" section** - it should specify exact commands for this project.\n\n---\n\n## Phase 1: LOAD - Read the Plan\n\n### 1.1 Load Plan File\n\n```bash\ncat $ARGUMENTS\n```\n\nIf `$ARGUMENTS` is a GitHub issue URL or number (e.g., `#123`), fetch the issue body which contains the plan.\n\n### 1.2 Extract Key Sections\n\nLocate and understand:\n\n- **Summary** - What we're building\n- **Patterns to Mirror** - Code to copy from\n- **Files to Change** - CREATE/UPDATE list\n- **Step-by-Step Tasks** - Implementation order\n- **Validation Commands** - How to verify (USE THESE, not hardcoded commands)\n- **Acceptance Criteria** - Definition of done\n\n### 1.3 Validate Plan Exists\n\n**If plan not found:**\n\n```\nError: Plan not found at $ARGUMENTS\n\nProvide a valid plan path or GitHub issue containing the plan.\n```\n\n**PHASE_1_CHECKPOINT:**\n\n- [ ] Plan file loaded\n- [ ] Key sections identified\n- [ ] Tasks list extracted\n\n---\n\n## Phase 2: PREPARE - Git State\n\n### 2.1 Check Current State\n\n```bash\n# What branch are we on?\ngit branch --show-current\n\n# Are we in a worktree?\ngit rev-parse --show-toplevel\ngit worktree list\n\n# Is working directory clean?\ngit status --porcelain\n```\n\n### 2.2 Branch Decision\n\n```text\n┌─ IN WORKTREE?\n│  └─ YES → Use current branch AS-IS. Do NOT switch branches. Do NOT create\n│           new branches. The isolation system has already set up the correct\n│           branch; any deviation operates on the wrong code.\n│           Log: \"Using worktree at {path} on branch {branch}\"\n│\n├─ ON $BASE_BRANCH? (main, master, or configured base branch)\n│  └─ Q: Working directory clean?\n│     ├─ YES → Create branch: git checkout -b feature/{plan-slug}\n│     │        (only applies outside a worktree — e.g., manual CLI usage)\n│     └─ NO  → STOP: \"Stash or commit changes first\"\n│\n├─ ON OTHER BRANCH?\n│  └─ Use it AS-IS. Do NOT switch to another branch (e.g., one shown by\n│     `git branch` but not currently checked out).\n│     Log: \"Using existing branch {name}\"\n│\n└─ DIRTY STATE?\n   └─ STOP: \"Stash or commit changes first\"\n```\n\n### 2.3 Sync with Remote\n\n```bash\ngit fetch origin\ngit pull --rebase origin $BASE_BRANCH 2>/dev/null || true\n```\n\n**PHASE_2_CHECKPOINT:**\n\n- [ ] On correct branch (not $BASE_BRANCH with uncommitted work)\n- [ ] Working directory ready\n- [ ] Up to date with remote\n\n---\n\n## Phase 3: EXECUTE - Implement Tasks\n\n**For each task in the plan's Step-by-Step Tasks section:**\n\n### 3.1 Read Context\n\n1. Read the **MIRROR** file reference from the task\n2. Understand the pattern to follow\n3. Read any **IMPORTS** specified\n\n### 3.2 Implement\n\n1. Make the change exactly as specified\n2. Follow the pattern from MIRROR reference\n3. Handle any **GOTCHA** warnings\n\n### 3.3 Validate Immediately\n\n**After EVERY file change, run the type-check command from the plan's Validation Commands section.**\n\nCommon patterns:\n- `{runner} run type-check` (JS/TS projects)\n- `mypy .` (Python)\n- `cargo check` (Rust)\n- `go build ./...` (Go)\n\n**If types fail:**\n\n1. Read the error\n2. Fix the issue\n3. Re-run type-check\n4. Only proceed when passing\n\n### 3.4 Track Progress\n\nLog each task as you complete it:\n\n```\nTask 1: CREATE src/features/x/models.ts ✅\nTask 2: CREATE src/features/x/service.ts ✅\nTask 3: UPDATE src/routes/index.ts ✅\n```\n\n**Deviation Handling:**\nIf you must deviate from the plan:\n\n- Note WHAT changed\n- Note WHY it changed\n- Continue with the deviation documented\n\n**PHASE_3_CHECKPOINT:**\n\n- [ ] All tasks executed in order\n- [ ] Each task passed type-check\n- [ ] Deviations documented\n\n---\n\n## Phase 4: VALIDATE - Full Verification\n\n### 4.1 Static Analysis\n\n**Run the type-check and lint commands from the plan's Validation Commands section.**\n\nCommon patterns:\n- JS/TS: `{runner} run type-check && {runner} run lint`\n- Python: `ruff check . && mypy .`\n- Rust: `cargo check && cargo clippy`\n- Go: `go vet ./...`\n\n**Must pass with zero errors.**\n\nIf lint errors:\n\n1. Run the lint fix command (e.g., `{runner} run lint:fix`, `ruff check --fix .`)\n2. Re-check\n3. Manual fix remaining issues\n\n### 4.2 Unit Tests\n\n**You MUST write or update tests for new code.** This is not optional.\n\n**Test requirements:**\n\n1. Every new function/feature needs at least one test\n2. Edge cases identified in the plan need tests\n3. Update existing tests if behavior changed\n\n**Write tests**, then run the test command from the plan.\n\nCommon patterns:\n- JS/TS: `{runner} test` or `{runner} run test`\n- Python: `pytest` or `uv run pytest`\n- Rust: `cargo test`\n- Go: `go test ./...`\n\n**If tests fail:**\n\n1. Read failure output\n2. Determine: bug in implementation or bug in test?\n3. Fix the actual issue\n4. Re-run tests\n5. Repeat until green\n\n### 4.3 Build Check\n\n**Run the build command from the plan's Validation Commands section.**\n\nCommon patterns:\n- JS/TS: `{runner} run build`\n- Python: N/A (interpreted) or `uv build`\n- Rust: `cargo build --release`\n- Go: `go build ./...`\n\n**Must complete without errors.**\n\n### 4.4 Integration Testing (if applicable)\n\n**If the plan involves API/server changes, use the integration test commands from the plan.**\n\nExample pattern:\n```bash\n# Start server in background (command varies by project)\n{runner} run dev &\nSERVER_PID=$!\nsleep 3\n\n# Test endpoints (adjust URL/port per project config)\ncurl -s http://localhost:{port}/health | jq\n\n# Stop server\nkill $SERVER_PID\n```\n\n### 4.5 Edge Case Testing\n\nRun any edge case tests specified in the plan.\n\n**PHASE_4_CHECKPOINT:**\n\n- [ ] Type-check passes (command from plan)\n- [ ] Lint passes (0 errors)\n- [ ] Tests pass (all green)\n- [ ] Build succeeds\n- [ ] Integration tests pass (if applicable)\n\n---\n\n## Phase 5: REPORT - Create Implementation Report\n\n### 5.1 Create Report Directory\n\n```bash\nmkdir -p $ARTIFACTS_DIR/../reports\n```\n\n### 5.2 Generate Report\n\n**Path**: `$ARTIFACTS_DIR/../reports/{plan-name}-report.md`\n\n```markdown\n# Implementation Report\n\n**Plan**: `$ARGUMENTS`\n**Source Issue**: #{number} (if applicable)\n**Branch**: `{branch-name}`\n**Date**: {YYYY-MM-DD}\n**Status**: {COMPLETE | PARTIAL}\n\n---\n\n## Summary\n\n{Brief description of what was implemented}\n\n---\n\n## Assessment vs Reality\n\nCompare the original plan's assessment with what actually happened:\n\n| Metric     | Predicted   | Actual   | Reasoning                                                                      |\n| ---------- | ----------- | -------- | ------------------------------------------------------------------------------ |\n| Complexity | {from plan} | {actual} | {Why it matched or differed - e.g., \"discovered additional integration point\"} |\n| Confidence | {from plan} | {actual} | {e.g., \"root cause was correct\" or \"had to pivot because X\"}                   |\n\n**If implementation deviated from the plan, explain why:**\n\n- {What changed and why - based on what you discovered during implementation}\n\n---\n\n## Tasks Completed\n\n| #   | Task               | File       | Status |\n| --- | ------------------ | ---------- | ------ |\n| 1   | {task description} | `src/x.ts` | ✅     |\n| 2   | {task description} | `src/y.ts` | ✅     |\n\n---\n\n## Validation Results\n\n| Check       | Result | Details               |\n| ----------- | ------ | --------------------- |\n| Type check  | ✅     | No errors             |\n| Lint        | ✅     | 0 errors, N warnings  |\n| Unit tests  | ✅     | X passed, 0 failed    |\n| Build       | ✅     | Compiled successfully |\n| Integration | ✅/⏭️  | {result or \"N/A\"}     |\n\n---\n\n## Files Changed\n\n| File       | Action | Lines     |\n| ---------- | ------ | --------- |\n| `src/x.ts` | CREATE | +{N}      |\n| `src/y.ts` | UPDATE | +{N}/-{M} |\n\n---\n\n## Deviations from Plan\n\n{List any deviations with rationale, or \"None\"}\n\n---\n\n## Issues Encountered\n\n{List any issues and how they were resolved, or \"None\"}\n\n---\n\n## Tests Written\n\n| Test File       | Test Cases               |\n| --------------- | ------------------------ |\n| `src/x.test.ts` | {list of test functions} |\n\n---\n\n## Next Steps\n\n- [ ] Review implementation\n- [ ] Create PR (next step in workflow)\n- [ ] Merge when approved\n```\n\n### 5.3 Archive Plan\n\n```bash\nmkdir -p $ARTIFACTS_DIR/../plans/completed\ncp $ARGUMENTS $ARTIFACTS_DIR/../plans/completed/ 2>/dev/null || true\n```\n\n**PHASE_5_CHECKPOINT:**\n\n- [ ] Report created at `$ARTIFACTS_DIR/../reports/`\n- [ ] Plan copied to completed folder (if local file)\n\n---\n\n## Phase 6: OUTPUT - Report to User\n\n```markdown\n## Implementation Complete\n\n**Plan**: `$ARGUMENTS`\n**Source Issue**: #{number} (if applicable)\n**Branch**: `{branch-name}`\n**Status**: ✅ Complete\n\n### Validation Summary\n\n| Check      | Result          |\n| ---------- | --------------- |\n| Type check | ✅              |\n| Lint       | ✅              |\n| Tests      | ✅ ({N} passed) |\n| Build      | ✅              |\n\n### Files Changed\n\n- {N} files created\n- {M} files updated\n- {K} tests written\n\n### Deviations\n\n{If none: \"Implementation matched the plan.\"}\n{If any: Brief summary of what changed and why}\n\n### Artifacts\n\n- Report: `$ARTIFACTS_DIR/../reports/{name}-report.md`\n\n### Next Steps\n\n1. Review the report (especially if deviations noted)\n2. Create PR (next workflow step)\n3. Merge when approved\n```\n\n---\n\n## Handling Failures\n\n### Type Check Fails\n\n1. Read error message carefully\n2. Fix the type issue\n3. Re-run the type-check command\n4. Don't proceed until passing\n\n### Tests Fail\n\n1. Identify which test failed\n2. Determine: implementation bug or test bug?\n3. Fix the root cause (usually implementation)\n4. Re-run tests\n5. Repeat until green\n\n### Lint Fails\n\n1. Run the lint fix command for auto-fixable issues\n2. Manually fix remaining issues\n3. Re-run lint\n4. Proceed when clean\n\n### Build Fails\n\n1. Usually a type or import issue\n2. Check the error output\n3. Fix and re-run\n\n### Integration Test Fails\n\n1. Check if server started correctly\n2. Verify endpoint exists\n3. Check request format\n4. Fix implementation and retry\n\n---\n\n## Success Criteria\n\n- **TASKS_COMPLETE**: All plan tasks executed\n- **TYPES_PASS**: Type-check command exits 0\n- **LINT_PASS**: Lint command exits 0 (warnings OK)\n- **TESTS_PASS**: Test command all green\n- **BUILD_PASS**: Build command succeeds\n- **REPORT_CREATED**: Implementation report exists\n",
  "archon-investigate-issue": "---\ndescription: Investigate a GitHub issue or problem - analyze codebase, create plan, post to GitHub\nargument-hint: <issue-number|url|\"description\">\n---\n\n# Investigate Issue\n\n**Input**: $ARGUMENTS\n\n---\n\n## Your Mission\n\nInvestigate the issue/problem and produce a comprehensive implementation plan that:\n\n1. Can be executed by `/implement-issue`\n2. Is posted as a GitHub comment (if GH issue provided)\n3. Captures all context needed for one-pass implementation\n\n**Golden Rule**: The artifact you produce IS the specification. The implementing agent should be able to work from it without asking questions.\n\n---\n\n## Phase 1: PARSE - Understand Input\n\n### 1.1 Determine Input Type\n\n**Check the input format:**\n\n- Looks like a number (`123`, `#123`) → GitHub issue number\n- Starts with `http` → GitHub URL (extract issue number)\n- Anything else → Free-form description\n\n```bash\n# If GitHub issue, fetch it:\ngh issue view {number} --json title,body,labels,comments,state,url,author\n```\n\n### 1.2 Extract Context\n\n**If GitHub issue:**\n- Title: What's the reported problem?\n- Body: Details, reproduction steps, expected vs actual\n- Labels: bug? enhancement? documentation?\n- Comments: Additional context from discussion\n- State: Is it still open?\n\n**If free-form:**\n- Parse as problem description\n- Note: No GitHub posting (artifact only)\n\n### 1.3 Classify Issue Type\n\n| Type | Indicators |\n|------|------------|\n| BUG | \"broken\", \"error\", \"crash\", \"doesn't work\", stack trace |\n| ENHANCEMENT | \"add\", \"support\", \"feature\", \"would be nice\" |\n| REFACTOR | \"clean up\", \"improve\", \"simplify\", \"reorganize\" |\n| CHORE | \"update\", \"upgrade\", \"maintenance\", \"dependency\" |\n| DOCUMENTATION | \"docs\", \"readme\", \"clarify\", \"example\" |\n\n### 1.4 Assess Severity/Priority, Complexity, and Confidence\n\nEach assessment requires a **one-sentence reasoning** explaining WHY you chose that value. This reasoning must be based on concrete findings from your investigation (codebase exploration, git history, integration analysis).\n\n**For BUG issues - Severity:**\n\n| Severity | Criteria |\n|----------|----------|\n| CRITICAL | System down, data loss, security vulnerability, no workaround |\n| HIGH | Major feature broken, significant user impact, difficult workaround |\n| MEDIUM | Feature partially broken, moderate impact, workaround exists |\n| LOW | Minor issue, cosmetic, edge case, easy workaround |\n\n**For ENHANCEMENT/REFACTOR/CHORE/DOCUMENTATION - Priority:**\n\n| Priority | Criteria |\n|----------|----------|\n| HIGH | Blocking other work, frequently requested, high user value |\n| MEDIUM | Important but not urgent, moderate user value |\n| LOW | Nice to have, low urgency, minimal user impact |\n\n**Complexity** (based on codebase findings):\n\n| Complexity | Criteria |\n|------------|----------|\n| HIGH | 5+ files, multiple integration points, architectural changes, high risk |\n| MEDIUM | 2-4 files, some integration points, moderate risk |\n| LOW | 1-2 files, isolated change, low risk |\n\n**Confidence** (based on evidence quality):\n\n| Confidence | Criteria |\n|------------|----------|\n| HIGH | Clear root cause, strong evidence, well-understood code path |\n| MEDIUM | Likely root cause, some assumptions, partially understood |\n| LOW | Uncertain root cause, limited evidence, many unknowns |\n\n**PHASE_1_CHECKPOINT:**\n- [ ] Input type identified (GH issue or free-form)\n- [ ] Issue content extracted\n- [ ] Type classified\n- [ ] Severity (bug) or Priority (other) assessed with reasoning\n- [ ] Complexity assessed with reasoning (after Phase 2)\n- [ ] Confidence assessed with reasoning (after Phase 3)\n- [ ] If GH issue: confirmed it's open and not already has PR\n\n---\n\n## Phase 2: EXPLORE - Codebase Intelligence\n\n### 2.1 Search for Relevant Code\n\nUse Task tool with subagent_type=\"Explore\":\n\n```\nExplore the codebase to understand the issue:\n\nISSUE: {title/description}\n\nDISCOVER:\n1. Files directly related to this functionality\n2. How the current implementation works\n3. Integration points - what calls this, what it calls\n4. Similar patterns elsewhere to mirror\n5. Existing test patterns for this area\n6. Error handling patterns used\n\nReturn:\n- File paths with specific line numbers\n- Actual code snippets (not summaries)\n- Dependencies and data flow\n```\n\n### 2.2 Document Findings\n\n| Area | File:Lines | Notes |\n|------|-----------|-------|\n| Core logic | `src/x.ts:10-50` | Main function affected |\n| Callers | `src/y.ts:20-30` | Uses the core function |\n| Types | `src/types/x.ts:5-15` | Relevant interfaces |\n| Tests | `src/x.test.ts:1-100` | Existing test patterns |\n| Similar | `src/z.ts:40-60` | Pattern to mirror |\n\n**PHASE_2_CHECKPOINT:**\n- [ ] Explore agent completed successfully\n- [ ] Core files identified with line numbers\n- [ ] Integration points mapped\n- [ ] Similar patterns found to mirror\n- [ ] Test patterns documented\n\n---\n\n## Phase 3: ANALYZE - Form Approach\n\n### 3.0 First-Principles Analysis\n\nBefore diving into bug analysis or enhancement scoping, identify the primitive:\n\n1. **What primitive is involved?** What is the core abstraction this bug/feature touches?\n   (e.g., the condition evaluator, the approval system, the isolation provider)\n2. **Is the primitive sound?** Does the existing design handle this case, or is the\n   primitive itself incomplete or missing a case?\n3. **Root cause vs symptom** — are we fixing where the error manifests, or where it\n   originates? Trace the data flow back to the source.\n4. **What's the minimal change?** What is the smallest edit that fixes the root cause?\n   Avoid adding new abstractions when extending existing ones works.\n5. **What does this unlock?** If we add/change a primitive, what other improvements\n   become possible?\n\n| Primitive | File:Lines | Sound? | Notes |\n|-----------|-----------|--------|-------|\n| {abstraction name} | `src/x.ts:10-30` | Yes/No/Partial | {if incomplete: what's missing} |\n\n### 3.1 For BUG Issues - Root Cause Analysis\n\nApply the 5 Whys:\n\n```\nWHY 1: Why does [symptom] occur?\n→ Because [cause A]\n→ Evidence: `file.ts:123` - {code snippet}\n\nWHY 2: Why does [cause A] happen?\n→ Because [cause B]\n→ Evidence: {proof}\n\n... continue until you reach fixable code ...\n\nROOT CAUSE: [the specific code/logic to change]\nEvidence: `source.ts:456` - {the problematic code}\n```\n\n**Check git history:**\n```bash\ngit log --oneline -10 -- {affected-file}\ngit blame -L {start},{end} {affected-file}\n```\n\n### 3.2 For ENHANCEMENT/REFACTOR Issues\n\n**Identify:**\n- What needs to be added/changed?\n- Where does it integrate?\n- What are the scope boundaries?\n- What should NOT be changed?\n\n### 3.3 For All Issues\n\n**Determine:**\n- Files to CREATE (new files)\n- Files to UPDATE (existing files)\n- Files to DELETE (if any)\n- Dependencies and order of changes\n- Edge cases and risks\n- Validation strategy\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Root cause identified (for bugs) OR change rationale clear (for enhancements)\n- [ ] All affected files listed with specific changes\n- [ ] Scope boundaries defined (what NOT to change)\n- [ ] Risks and edge cases identified\n- [ ] Validation approach defined\n\n---\n\n## Phase 4: GENERATE - Create Artifact\n\n### 4.1 Artifact Path\n\n```bash\n```\n\n**Path:** `$ARTIFACTS_DIR/investigation.md`\n\nThis unified path allows review agents to find the artifact regardless of workflow type.\n\n### 4.2 Artifact Template\n\nWrite this structure to the artifact file.\n\n**Note on Severity vs Priority:**\n- Use **Severity** for BUG type (CRITICAL, HIGH, MEDIUM, LOW)\n- Use **Priority** for all other types (HIGH, MEDIUM, LOW)\n\n**Important:** Each assessment must include a one-sentence reasoning based on your investigation findings.\n\n```markdown\n# Investigation: {Title}\n\n**Issue**: #{number} ({url})\n**Type**: {BUG|ENHANCEMENT|REFACTOR|CHORE|DOCUMENTATION}\n**Investigated**: {ISO timestamp}\n\n### Assessment\n\n| Metric | Value | Reasoning |\n|--------|-------|-----------|\n| Severity | {CRITICAL\\|HIGH\\|MEDIUM\\|LOW} | {Why this severity? Based on user impact, workarounds, scope of failure} |\n| Complexity | {LOW\\|MEDIUM\\|HIGH} | {Why this complexity? Based on files affected, integration points, risk} |\n| Confidence | {HIGH\\|MEDIUM\\|LOW} | {Why this confidence? Based on evidence quality, unknowns, assumptions} |\n\n<!-- For non-BUG types, replace Severity row with Priority:\n| Priority | {HIGH\\|MEDIUM\\|LOW} | {Why this priority? Based on user value, blocking status, frequency} |\n-->\n\n---\n\n## Problem Statement\n\n{Clear 2-3 sentence description of what's wrong or what's needed}\n\n---\n\n## Analysis\n\n### Root Cause / Change Rationale\n\n{For BUG: The 5 Whys chain with evidence}\n{For ENHANCEMENT: Why this change and what it enables}\n\n### Evidence Chain\n\nWHY: {symptom}\n↓ BECAUSE: {cause 1}\n  Evidence: `file.ts:123` - `{code snippet}`\n\n↓ BECAUSE: {cause 2}\n  Evidence: `file.ts:456` - `{code snippet}`\n\n↓ ROOT CAUSE: {the fixable thing}\n  Evidence: `file.ts:789` - `{problematic code}`\n\n### Affected Files\n\n| File | Lines | Action | Description |\n|------|-------|--------|-------------|\n| `src/x.ts` | 45-60 | UPDATE | {what changes} |\n| `src/x.test.ts` | NEW | CREATE | {test to add} |\n\n### Integration Points\n\n- `src/y.ts:20` calls this function\n- `src/z.ts:30` depends on this behavior\n- {other dependencies}\n\n### Git History\n\n- **Introduced**: {commit} - {date} - \"{message}\"\n- **Last modified**: {commit} - {date}\n- **Implication**: {regression? original bug? long-standing?}\n\n---\n\n## Implementation Plan\n\n### Step 1: {First change description}\n\n**File**: `src/x.ts`\n**Lines**: 45-60\n**Action**: UPDATE\n\n**Current code:**\n```typescript\n// Line 45-50\n{actual current code}\n```\n\n**Required change:**\n```typescript\n// What it should become\n{the fix/change}\n```\n\n**Why**: {brief rationale}\n\n---\n\n### Step 2: {Second change description}\n\n{Same structure...}\n\n---\n\n### Step N: Add/Update Tests\n\n**File**: `src/x.test.ts`\n**Action**: {CREATE|UPDATE}\n\n**Test cases to add:**\n```typescript\ndescribe('{feature}', () => {\n  it('should {expected behavior}', () => {\n    // Test the fix\n  });\n\n  it('should handle {edge case}', () => {\n    // Test edge case\n  });\n});\n```\n\n---\n\n## Patterns to Follow\n\n**From codebase - mirror these exactly:**\n\n```typescript\n// SOURCE: src/similar.ts:20-30\n// Pattern for {what this demonstrates}\n{actual code snippet from codebase}\n```\n\n---\n\n## Edge Cases & Risks\n\n| Risk/Edge Case | Mitigation |\n|----------------|------------|\n| {risk 1} | {how to handle} |\n| {edge case} | {how to handle} |\n\n---\n\n## Validation\n\n### Automated Checks\n\n```bash\nbun run type-check\nbun test {relevant-pattern}\nbun run lint\n```\n\n### Manual Verification\n\n1. {Step to verify the fix/feature works}\n2. {Step to verify no regression}\n\n---\n\n## Scope Boundaries\n\n**IN SCOPE:**\n- {what we're changing}\n\n**OUT OF SCOPE (do not touch):**\n- {what to leave alone}\n- {future improvements to defer}\n\n---\n\n## Metadata\n\n- **Investigated by**: Claude\n- **Timestamp**: {ISO timestamp}\n- **Artifact**: `$ARTIFACTS_DIR/investigation.md`\n```\n\n**PHASE_4_CHECKPOINT:**\n- [ ] Artifact file created\n- [ ] All sections filled with specific content\n- [ ] Code snippets are actual (not invented)\n- [ ] Steps are actionable without clarification\n\n---\n\n## Phase 5: POST - GitHub Comment\n\n**Only if input was a GitHub issue (not free-form):**\n\nFormat the artifact for GitHub and post:\n\n```bash\ngh issue comment {number} --body \"$(cat <<'EOF'\n## 🔍 Investigation: {Title}\n\n**Type**: `{TYPE}`\n\n### Assessment\n\n| Metric | Value | Reasoning |\n|--------|-------|-----------|\n| {Severity or Priority} | `{VALUE}` | {one-sentence why} |\n| Complexity | `{COMPLEXITY}` | {one-sentence why} |\n| Confidence | `{CONFIDENCE}` | {one-sentence why} |\n\n---\n\n### Problem Statement\n\n{problem statement from artifact}\n\n---\n\n### Root Cause Analysis\n\n{evidence chain, formatted for GitHub}\n\n---\n\n### Implementation Plan\n\n| Step | File | Change |\n|------|------|--------|\n| 1 | `src/x.ts:45` | {description} |\n| 2 | `src/x.test.ts` | Add test for {case} |\n\n<details>\n<summary>📋 Detailed Implementation Steps</summary>\n\n{detailed steps from artifact}\n\n</details>\n\n---\n\n### Validation\n\n```bash\nbun run type-check && bun test {pattern} && bun run lint\n```\n\n---\n\n### Next Step\n\nTo implement: `/implement-issue {number}`\n\n---\n*Investigated by Claude • {timestamp}*\nEOF\n)\"\n```\n\n**PHASE_5_CHECKPOINT:**\n- [ ] Comment posted to GitHub (if GH issue)\n- [ ] Formatting renders correctly\n\n---\n\n## Phase 6: REPORT - Output to User\n\n```markdown\n## Investigation Complete\n\n**Issue**: #{number} - {title}\n**Type**: {BUG|ENHANCEMENT|REFACTOR|...}\n\n### Assessment\n\n| Metric | Value | Reasoning |\n|--------|-------|-----------|\n| {Severity or Priority} | {value} | {why - based on investigation} |\n| Complexity | {LOW\\|MEDIUM\\|HIGH} | {why - based on files/integration/risk} |\n| Confidence | {HIGH\\|MEDIUM\\|LOW} | {why - based on evidence/unknowns} |\n\n### Key Findings\n\n- **Root Cause**: {one-line summary}\n- **Files Affected**: {count} files\n- **Estimated Changes**: {brief scope}\n\n### Files to Modify\n\n| File | Action |\n|------|--------|\n| `src/x.ts` | UPDATE |\n| `src/x.test.ts` | CREATE |\n\n### Artifact\n\n📄 `$ARTIFACTS_DIR/investigation.md`\n\n### GitHub\n\n{✅ Posted to issue | ⏭️ Skipped (free-form input)}\n\n### Next Step\n\nRun `/implement-issue {number}` to execute the plan.\n```\n\n---\n\n## Handling Edge Cases\n\n### Issue is already closed\n- Report: \"Issue #{number} is already closed\"\n- Still create artifact if user wants analysis\n\n### Issue already has linked PR\n- Warn: \"PR #{pr} already addresses this issue\"\n- Ask if user wants to continue anyway\n\n### Can't determine root cause\n- Document what you found\n- Set confidence to LOW\n- Note uncertainty in artifact\n- Proceed with best hypothesis\n\n### Very large scope\n- Suggest breaking into smaller issues\n- Focus on core problem first\n- Note deferred items in \"Out of Scope\"\n\n---\n\n## Success Criteria\n\n- **ARTIFACT_COMPLETE**: All sections filled with specific, actionable content\n- **EVIDENCE_BASED**: Every claim has file:line reference or proof\n- **IMPLEMENTABLE**: Another agent can execute without questions\n- **GITHUB_POSTED**: Comment visible on issue (if GH issue)\n- **COMMITTED**: Artifact saved in git\n",
  "archon-issue-completion-report": "---\ndescription: Post completion report to GitHub issue with results, unaddressed items, and follow-up suggestions\nargument-hint: (none - reads from workflow artifacts)\n---\n\n# Issue Completion Report\n\n**Input**: $ARGUMENTS\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Your Mission\n\nCompile all workflow artifacts into a final report and post it to the original GitHub issue. Summarize what was done, what wasn't addressed (and why), and suggest follow-up issues if needed.\n\n**GitHub action**: Post completion report as a comment on the original issue\n**Output artifact**: `$ARTIFACTS_DIR/completion-report.md`\n\n---\n\n## Phase 1: LOAD — Gather All Artifacts\n\n### 1.1 Get Issue Number\n\nExtract issue number from `$ARGUMENTS`:\n\n```bash\n# $ARGUMENTS should be the issue number or URL\necho \"$ARGUMENTS\"\n```\n\n### 1.2 Get PR Info\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number 2>/dev/null || echo \"unknown\")\nPR_URL=$(cat $ARTIFACTS_DIR/.pr-url 2>/dev/null || echo \"unknown\")\necho \"PR: $PR_NUMBER ($PR_URL)\"\n```\n\n### 1.3 Read All Available Artifacts\n\nCheck for and read each artifact that may exist:\n\n```bash\n# Investigation/Plan\ncat $ARTIFACTS_DIR/investigation.md 2>/dev/null\ncat $ARTIFACTS_DIR/plan.md 2>/dev/null\n\n# Implementation\ncat $ARTIFACTS_DIR/implementation.md 2>/dev/null\n\n# Web research\ncat $ARTIFACTS_DIR/web-research.md 2>/dev/null\n\n# Validation\ncat $ARTIFACTS_DIR/validation.md 2>/dev/null\n\n# Review artifacts\nls $ARTIFACTS_DIR/review/ 2>/dev/null\ncat $ARTIFACTS_DIR/review/consolidated-review.md 2>/dev/null\ncat $ARTIFACTS_DIR/review/fix-report.md 2>/dev/null\n```\n\n### 1.4 Get Git Info\n\n```bash\ngit branch --show-current\ngit log --oneline -5\n```\n\n**PHASE_1_CHECKPOINT:**\n\n- [ ] Issue number identified\n- [ ] PR info loaded\n- [ ] All available artifacts read\n- [ ] Git state captured\n\n---\n\n## Phase 2: COMPILE — Build Report\n\n### 2.1 Summarize What Was Done\n\nFrom the artifacts, compile:\n\n- **Classification**: What type of issue (bug/feature/etc)\n- **Investigation/Plan**: Key findings and approach\n- **Implementation**: What was changed, files modified\n- **Validation**: Test results, lint, type-check\n- **Review**: What was reviewed, findings count\n- **Self-fix**: What review findings were fixed\n\n### 2.2 Identify Unaddressed Items\n\nFrom the fix report and consolidated review:\n\n- Findings that were SKIPPED (with reasons)\n- Findings that were BLOCKED (with reasons)\n- MEDIUM/LOW findings not auto-fixed\n- Any validation issues that persisted\n\n### 2.3 Suggest Follow-up Issues\n\nFor each unaddressed item, determine if it warrants a follow-up issue:\n\n| Item | Warrants Issue? | Why |\n|------|----------------|-----|\n| {skipped finding} | YES/NO | {reason} |\n\n**PHASE_2_CHECKPOINT:**\n\n- [ ] Summary compiled\n- [ ] Unaddressed items identified\n- [ ] Follow-up suggestions prepared\n\n---\n\n## Phase 3: GENERATE — Write Artifact\n\nWrite to `$ARTIFACTS_DIR/completion-report.md`:\n\n```markdown\n# Completion Report: Issue $ARGUMENTS\n\n**Date**: {ISO timestamp}\n**Workflow ID**: $WORKFLOW_ID\n**PR**: #{pr-number} ({pr-url})\n\n---\n\n## Summary\n\n{3-5 sentence overview of the entire workflow execution}\n\n---\n\n## Classification\n\n| Field | Value |\n|-------|-------|\n| Type | {bug/feature/enhancement/...} |\n| Complexity | {LOW/MEDIUM/HIGH} |\n| Confidence | {HIGH/MEDIUM/LOW} |\n\n---\n\n## What Was Done\n\n### Investigation/Planning\n\n{Brief summary of root cause or plan}\n\n### Implementation\n\n| File | Action | Description |\n|------|--------|-------------|\n| `{file}` | {CREATE/UPDATE} | {what changed} |\n\n### Validation\n\n| Check | Result |\n|-------|--------|\n| Type check | ✅ / ❌ |\n| Lint | ✅ / ❌ |\n| Tests | ✅ ({n} passed) / ❌ |\n\n### Review & Self-Fix\n\n- **Findings**: {n} total from review agents\n- **Fixed**: {n} (including tests, docs, simplification)\n- **Skipped**: {n}\n- **Blocked**: {n}\n\n---\n\n## Unaddressed Items\n\n{If none: \"All findings were addressed.\"}\n\n### Skipped\n\n| Finding | Severity | Reason |\n|---------|----------|--------|\n| {title} | {sev} | {reason} |\n\n### Blocked\n\n| Finding | Severity | Reason |\n|---------|----------|--------|\n| {title} | {sev} | {reason} |\n\n---\n\n## Suggested Follow-up Issues\n\n| Title | Priority | Description |\n|-------|----------|-------------|\n| \"{title}\" | {P1/P2/P3} | {brief description} |\n\n*(none)* if everything was addressed\n\n---\n\n## Artifacts\n\n| Artifact | Path |\n|----------|------|\n| Investigation/Plan | `$ARTIFACTS_DIR/{investigation or plan}.md` |\n| Web Research | `$ARTIFACTS_DIR/web-research.md` |\n| Implementation | `$ARTIFACTS_DIR/implementation.md` |\n| Consolidated Review | `$ARTIFACTS_DIR/review/consolidated-review.md` |\n| Fix Report | `$ARTIFACTS_DIR/review/fix-report.md` |\n```\n\n**PHASE_3_CHECKPOINT:**\n\n- [ ] Completion report written\n\n---\n\n## Phase 4: POST — GitHub Issue Comment\n\nPost to the original GitHub issue:\n\n```bash\nISSUE_NUMBER=$(echo \"$ARGUMENTS\" | grep -oE '[0-9]+')\n\ngh issue comment $ISSUE_NUMBER --body \"$(cat <<'EOF'\n## ✅ Issue Resolution Report\n\n**PR**: #{pr-number} ({pr-url})\n**Status**: COMPLETE\n\n---\n\n### Summary\n\n{Brief overview of what was done to resolve this issue}\n\n---\n\n### Changes Made\n\n| File | Change |\n|------|--------|\n| `{file}` | {description} |\n\n---\n\n### Validation\n\n✅ Type check | ✅ Lint | ✅ Tests ({n} passed)\n\n---\n\n### Review & Self-Fix\n\n- **{n}** review findings addressed\n- **{n}** tests added\n- **{n}** docs updated\n- **{n}** code simplifications applied\n\n---\n\n### Unaddressed Items\n\n{If none: \"All review findings were addressed in the PR.\"}\n\n{If any:}\n\n| Finding | Severity | Reason |\n|---------|----------|--------|\n| {title} | {sev} | {why not addressed} |\n\n---\n\n### Suggested Follow-up Issues\n\n{If any:}\n\n1. **{Issue Title}** ({priority}) — {brief description}\n\n{If none: \"No follow-up issues needed.\"}\n\n---\n\n*Resolved by Archon workflow `$WORKFLOW_ID`*\nEOF\n)\"\n```\n\n**PHASE_4_CHECKPOINT:**\n\n- [ ] GitHub comment posted to issue\n\n---\n\n## Phase 5: OUTPUT — Final Summary\n\n```markdown\n## Issue Resolution Complete\n\n**Issue**: $ARGUMENTS\n**PR**: #{pr-number}\n**Workflow**: $WORKFLOW_ID\n\n### Results\n\n- Implementation: ✅\n- Validation: ✅\n- Review: ✅\n- Self-fix: ✅\n\n### Unaddressed: {n} items\n### Follow-up issues suggested: {n}\n\n### Artifacts\n\n- Completion report: `$ARTIFACTS_DIR/completion-report.md`\n- GitHub comment: Posted to issue\n\n### Next Steps\n\n1. Review the PR: #{pr-number}\n2. Create suggested follow-up issues if agreed\n3. Merge when ready\n```\n\n---\n\n## Success Criteria\n\n- **ALL_ARTIFACTS_READ**: All workflow artifacts loaded and parsed\n- **REPORT_COMPILED**: Comprehensive completion report written\n- **GITHUB_POSTED**: Comment posted to original issue\n- **UNADDRESSED_DOCUMENTED**: Clear reasons for anything not fixed\n- **FOLLOWUPS_SUGGESTED**: Actionable follow-up issues recommended where appropriate\n",
  "archon-plan-setup": "---\ndescription: Setup for plan execution - read plan, ensure branch ready, write context artifact\nargument-hint: <path/to/plan.md>\n---\n\n# Plan Setup\n\n**Plan**: $ARGUMENTS\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Your Mission\n\nPrepare everything needed for plan implementation:\n1. Read and parse the plan (including scope limits)\n2. Ensure we're on the correct branch\n3. Write a comprehensive context artifact for subsequent steps\n\n**This step does NOT implement anything** - it only sets up the environment.\n**This step does NOT create a PR** - that happens in `archon-finalize-pr` after implementation.\n\n---\n\n## Phase 1: LOAD - Read the Plan\n\n### 1.1 Locate Plan File\n\n**Check in order:**\n\n1. **If `$ARGUMENTS` provided**: Use that path\n2. **If plan already in workflow artifacts**: Use `$ARTIFACTS_DIR/plan.md`\n\n```bash\n# Check if plan was created by archon-create-plan in this workflow\nif [ -f \"$ARTIFACTS_DIR/plan.md\" ]; then\n  PLAN_PATH=\"$ARTIFACTS_DIR/plan.md\"\n  echo \"Using plan from workflow: $PLAN_PATH\"\nelif [ -n \"$ARGUMENTS\" ] && [ -f \"$ARGUMENTS\" ]; then\n  PLAN_PATH=\"$ARGUMENTS\"\n  echo \"Using plan from arguments: $PLAN_PATH\"\nelse\n  echo \"ERROR: No plan found\"\n  exit 1\nfi\n```\n\n### 1.2 Load Plan File\n\nRead the plan file:\n\n```bash\ncat $PLAN_PATH\n```\n\nIf `$ARGUMENTS` is a GitHub issue URL or number (e.g., `#123`), fetch the issue body instead.\n\n### 1.3 Extract Key Information\n\nFrom the plan, identify and extract:\n\n| Field | Where to Find | Example |\n|-------|---------------|---------|\n| **Title** | First `#` heading or \"Summary\" section | \"Discord Platform Adapter\" |\n| **Summary** | \"Summary\" or \"Feature Description\" section | 1-2 sentence overview |\n| **Files to Change** | \"Files to Change\" or \"Tasks\" section | List of CREATE/UPDATE files |\n| **Validation Commands** | \"Validation Commands\" or \"Validation Strategy\" | `bun run type-check`, etc. |\n| **Acceptance Criteria** | \"Acceptance Criteria\" section | Checklist items |\n| **NOT Building (Scope Limits)** | \"NOT Building\", \"Scope Limits\", or \"Out of Scope\" section | Explicit exclusions |\n\n**CRITICAL**: The \"NOT Building\" section defines what is **intentionally excluded** from scope. This MUST be captured and passed to review agents so they don't flag intentional exclusions as bugs.\n\n### 1.4 Derive Branch Name\n\nCreate a branch name from the plan title:\n\n```\nfeature/{slug}\n```\n\nWhere `{slug}` is the title lowercased, spaces replaced with hyphens, max 50 chars.\n\nExamples:\n- \"Discord Platform Adapter\" → `feature/discord-platform-adapter`\n- \"ESLint/Prettier Integration\" → `feature/eslint-prettier-integration`\n\n**PHASE_1_CHECKPOINT:**\n\n- [ ] Plan file loaded and readable\n- [ ] Key information extracted\n- [ ] Branch name derived\n\n---\n\n## Phase 2: PREPARE - Git State\n\n### 2.1 Check Current State\n\n```bash\ngit branch --show-current\ngit status --porcelain\ngit remote get-url origin\n```\n\n### 2.2 Determine Repository Info\n\nExtract owner/repo from the remote URL for PR creation:\n\n```bash\ngh repo view --json nameWithOwner -q .nameWithOwner\n```\n\n### 2.3 Branch Decision\n\nEvaluate in order (first matching case wins):\n\n```text\n┌─ IN WORKTREE?\n│  └─ YES → Use current branch AS-IS. Do NOT switch branches. Do NOT create\n│           new branches. The isolation system has already set up the correct\n│           branch; any deviation operates on the wrong code.\n│           Log: \"Using worktree branch: {name}\"\n│\n├─ ON $BASE_BRANCH? (main, master, or configured base branch)\n│  └─ Q: Working directory clean?\n│     ├─ YES → Create and checkout: `git checkout -b {branch-name}`\n│     │        (only applies outside a worktree — e.g., manual CLI usage)\n│     └─ NO  → STOP: \"Uncommitted changes on $BASE_BRANCH. Stash or commit first.\"\n│\n└─ ON OTHER BRANCH?\n   └─ Q: Does it match the expected branch for this plan?\n      ├─ YES → Use it, log \"Using existing branch: {name}\"\n      └─ NO  → STOP: \"On branch {X}, expected {Y}. Switch branches or adjust plan.\"\n```\n\n### 2.4 Sync with Remote\n\n```bash\ngit fetch origin\ngit rebase origin/$BASE_BRANCH || git merge origin/$BASE_BRANCH\n```\n\nIf conflicts occur, STOP with error: \"Merge conflicts with $BASE_BRANCH. Resolve manually.\"\n\n### 2.5 Push Branch (if commits exist)\n\nIf there are commits on the branch:\n```bash\ngit push -u origin HEAD\n```\n\nIf no commits yet (fresh branch), skip push - it will happen after implementation.\n\n**PHASE_2_CHECKPOINT:**\n\n- [ ] On correct branch\n- [ ] No uncommitted changes\n- [ ] Up to date with base branch\n\n---\n\n## Phase 3: ARTIFACT - Write Context File\n\n### 3.1 Create Artifact Directory\n\n```bash\n```\n\n### 3.2 Write Context Artifact\n\nWrite to `$ARTIFACTS_DIR/plan-context.md`:\n\n```markdown\n# Plan Context\n\n**Generated**: {YYYY-MM-DD HH:MM}\n**Workflow ID**: $WORKFLOW_ID\n**Plan Source**: $ARGUMENTS\n\n---\n\n## Branch\n\n| Field | Value |\n|-------|-------|\n| **Branch** | {branch-name} |\n| **Base** | {base-branch} |\n\n---\n\n## Plan Summary\n\n**Title**: {extracted-title}\n\n**Overview**: {1-2 sentence summary from plan}\n\n---\n\n## Files to Change\n\n{Copy the \"Files to Change\" table from the plan, or list extracted files}\n\n| File | Action |\n|------|--------|\n| `src/example.ts` | CREATE |\n| `src/other.ts` | UPDATE |\n\n---\n\n## NOT Building (Scope Limits)\n\n**CRITICAL FOR REVIEWERS**: These items are **intentionally excluded** from scope. Do NOT flag them as bugs or missing features.\n\n{Copy from plan's \"NOT Building\", \"Scope Limits\", or \"Out of Scope\" section}\n\n- {Explicit exclusion 1 with rationale}\n- {Explicit exclusion 2 with rationale}\n\n{If no explicit exclusions in plan: \"No explicit scope limits defined in plan.\"}\n\n---\n\n## Validation Commands\n\n{Copy from plan's \"Validation Commands\" section}\n\n```bash\nbun run type-check\nbun run lint\nbun test\nbun run build\n```\n\n---\n\n## Acceptance Criteria\n\n{Copy from plan's \"Acceptance Criteria\" section}\n\n- [ ] Criterion 1\n- [ ] Criterion 2\n- [ ] ...\n\n---\n\n## Patterns to Mirror\n\n{Copy key file references from plan's \"Patterns to Mirror\" section}\n\n| Pattern | Source File | Lines |\n|---------|-------------|-------|\n| {pattern-name} | `src/example.ts` | 10-50 |\n\n---\n\n## Next Steps\n\n1. `archon-confirm-plan` - Verify patterns still exist\n2. `archon-implement-tasks` - Execute the plan\n3. `archon-validate` - Run full validation\n4. `archon-finalize-pr` - Create PR and mark ready\n```\n\n**PHASE_3_CHECKPOINT:**\n\n- [ ] Artifact directory created\n- [ ] `plan-context.md` written with all sections\n- [ ] \"NOT Building\" section captured (even if empty)\n\n---\n\n## Phase 4: OUTPUT - Report to User\n\n```markdown\n## Plan Setup Complete\n\n**Plan**: `$ARGUMENTS`\n**Workflow ID**: `$WORKFLOW_ID`\n\n### Branch\n\n| Field | Value |\n|-------|-------|\n| Branch | `{branch-name}` |\n| Base | `{base-branch}` |\n\n### Plan Summary\n\n**{plan-title}**\n\n{1-2 sentence overview}\n\n### Scope\n\n- {N} files to create\n- {M} files to update\n- {K} explicit exclusions captured\n\n### Artifact\n\nContext written to: `$ARTIFACTS_DIR/plan-context.md`\n\n### Next Step\n\nProceed to `archon-confirm-plan` to verify the plan's research is still valid.\n```\n\n---\n\n## Error Handling\n\n### Plan File Not Found\n\n```\n❌ Plan not found: $ARGUMENTS\n\nVerify the path exists and try again.\n```\n\n### Uncommitted Changes on Base Branch\n\n```\n❌ Uncommitted changes on base branch\n\nOptions:\n1. Stash changes: `git stash`\n2. Commit changes: `git add . && git commit -m \"WIP\"`\n3. Discard changes: `git checkout .`\n\nThen retry.\n```\n\n### Merge Conflicts\n\n```\n❌ Merge conflicts with $BASE_BRANCH\n\nResolve conflicts manually:\n1. `git status` to see conflicts\n2. Edit conflicting files\n3. `git add <resolved-files>`\n4. `git rebase --continue`\n\nThen retry.\n```\n\n---\n\n## Success Criteria\n\n- **PLAN_LOADED**: Plan file read and parsed\n- **SCOPE_LIMITS_CAPTURED**: \"NOT Building\" section extracted (even if empty)\n- **BRANCH_READY**: On correct branch, synced with base branch\n- **ARTIFACT_WRITTEN**: `plan-context.md` contains all required sections including scope limits\n",
  "archon-post-review-to-pr": "---\ndescription: Post code review findings as a comment on the PR\nargument-hint: (none - reads from artifacts)\n---\n\n# Post Review to PR\n\n---\n\n## Your Mission\n\nRead the code review findings artifact and post a formatted summary as a comment on the PR.\n\n---\n\n## Phase 1: LOAD - Get Context\n\n### 1.1 Get PR Number\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number)\n```\n\n**If not found:**\n```\n❌ No PR number found at $ARTIFACTS_DIR/.pr-number\nCannot post review without a PR number.\n```\n\n### 1.2 Read Review Findings\n\n```bash\ncat $ARTIFACTS_DIR/review/code-review-findings.md\n```\n\n**If not found:**\n```\n❌ No review findings found at $ARTIFACTS_DIR/review/code-review-findings.md\nRun code review first.\n```\n\n**PHASE_1_CHECKPOINT:**\n- [ ] PR number loaded\n- [ ] Review findings loaded\n\n---\n\n## Phase 2: FORMAT - Build PR Comment\n\n### 2.1 Extract Key Information\n\nFrom the review findings, extract:\n- **Verdict**: APPROVE / REQUEST_CHANGES / NEEDS_DISCUSSION\n- **Summary**: 2-3 sentence overview\n- **Findings**: All findings with severity and location\n- **Statistics**: Finding counts by severity\n\n### 2.2 Build Comment Body\n\nFormat the review as a GitHub-friendly comment:\n\n```markdown\n## 🔍 Code Review\n\n**Verdict**: {APPROVE ✅ | REQUEST_CHANGES ❌ | NEEDS_DISCUSSION 💬}\n\n{Summary from findings}\n\n---\n\n### Findings\n\n{For each finding:}\n\n#### {severity emoji} {title}\n\n**Severity**: {CRITICAL|HIGH|MEDIUM|LOW} · **Category**: {category} · **Location**: `{file}:{line}`\n\n{Issue description}\n\n<details>\n<summary>Suggested Fix</summary>\n\n```typescript\n{recommended fix code}\n```\n\n**Why**: {reasoning}\n\n</details>\n\n---\n\n{End of findings}\n\n### Summary\n\n| Severity | Count |\n|----------|-------|\n| 🔴 Critical | {n} |\n| 🟠 High | {n} |\n| 🟡 Medium | {n} |\n| 🔵 Low | {n} |\n\n{If positive observations exist:}\n\n### What's Done Well\n\n{Positive observations from review}\n\n---\n\n*Automated code review*\n```\n\n**Severity emojis:**\n- CRITICAL → 🔴\n- HIGH → 🟠\n- MEDIUM → 🟡\n- LOW → 🔵\n\n**PHASE_2_CHECKPOINT:**\n- [ ] Comment body formatted\n- [ ] All findings included\n- [ ] Statistics table present\n\n---\n\n## Phase 3: POST - Comment on PR\n\n### 3.1 Post the Comment\n\n```bash\ngh pr comment {PR_NUMBER} --body \"$(cat <<'EOF'\n{formatted comment body}\nEOF\n)\"\n```\n\n### 3.2 Verify\n\n```bash\n# Check the comment was posted\ngh pr view {PR_NUMBER} --comments --json comments --jq '.comments | length'\n```\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Comment posted to PR\n- [ ] Verified comment exists\n\n---\n\n## Phase 4: OUTPUT - Report to User\n\n```markdown\n## Review Posted to PR\n\n**PR**: #{PR_NUMBER}\n**Verdict**: {verdict}\n**Findings**: {total count} ({critical} critical, {high} high, {medium} medium, {low} low)\n\nReview comment has been posted to the pull request.\n```\n\n---\n\n## Error Handling\n\n### PR not found\n- Verify PR number is correct\n- Check if PR is still open\n- Report error to user\n\n### Comment fails to post\n- Check GitHub authentication\n- Try with shorter body if too large\n- Report error with details\n\n### No findings\n- Post a clean review comment: \"No issues found. LGTM!\"\n\n---\n\n## Success Criteria\n\n- **FINDINGS_LOADED**: Review artifact read successfully\n- **COMMENT_FORMATTED**: PR comment built with all findings\n- **COMMENT_POSTED**: Comment visible on the PR\n",
  "archon-pr-review-scope": "---\ndescription: Gather PR context, verify reviewability, and prepare artifacts directory for comprehensive review\nargument-hint: <pr-number|url>\n---\n\n# PR Review Scope\n\n**Input**: $ARGUMENTS\n\n---\n\n## Your Mission\n\nVerify the PR is in a reviewable state, gather all context needed for the parallel review agents, and prepare the artifacts directory structure.\n\n---\n\n## Phase 1: IDENTIFY - Determine PR\n\n### 1.1 Get PR Number\n\n```bash\nif [ -f \"$ARTIFACTS_DIR/.pr-number\" ]; then\n  PR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n' | tr -d ' ')\n  if ! echo \"$PR_NUMBER\" | grep -qE '^[0-9]+$'; then\n    PR_NUMBER=\"\"\n  fi\nfi\n\n# From arguments (standalone review)\nif [ -z \"$PR_NUMBER\" ] && [ -n \"$ARGUMENTS\" ]; then\n  PR_NUMBER=$(echo \"$ARGUMENTS\" | grep -oE '[0-9]+' | head -1)\nfi\n\n# From current branch\nif [ -z \"$PR_NUMBER\" ]; then\n  PR_NUMBER=$(gh pr view --json number -q '.number' 2>/dev/null)\nfi\n\nif [ -z \"$PR_NUMBER\" ]; then\n  echo \"ERROR: No PR number found\"\n  exit 1\nfi\n\n# Write to registry for downstream steps (if not already there)\necho \"$PR_NUMBER\" > $ARTIFACTS_DIR/.pr-number\n```\n\n### 1.2 Fetch PR Details\n\n```bash\ngh pr view {number} --json number,title,body,url,headRefName,baseRefName,files,additions,deletions,changedFiles,state,author,isDraft,mergeable,mergeStateStatus\n```\n\n**Extract:**\n- PR number and title\n- Branch names (head → base)\n- Changed files list\n- Addition/deletion counts\n- Draft status\n- Mergeable status\n\n**PHASE_1_CHECKPOINT:**\n- [ ] PR number identified\n- [ ] PR is open (not merged/closed)\n- [ ] Basic metadata extracted\n\n---\n\n## Phase 2: VERIFY - Pre-Review Checks\n\n**Before launching review agents, verify the PR is in a reviewable state.**\n\n### 2.1 Check for Merge Conflicts\n\n```bash\ngh pr view {number} --json mergeable,mergeStateStatus --jq '.mergeable, .mergeStateStatus'\n```\n\n| Status | Action |\n|--------|--------|\n| `MERGEABLE` | Continue |\n| `CONFLICTING` | **STOP** - Tell user to resolve conflicts first |\n| `UNKNOWN` | Warn, continue (GitHub still calculating) |\n\n**If conflicts exist:**\n```markdown\n❌ **Cannot review: PR has merge conflicts**\n\nPlease resolve conflicts before requesting a review:\n```bash\ngit fetch origin {base}\ngit rebase origin/{base}\n# Resolve conflicts\ngit push --force-with-lease\n```\n\nThen re-request the review.\n```\n**Exit workflow if conflicts detected.**\n\n### 2.2 Check CI Status\n\n```bash\ngh pr checks {number} --json name,state,conclusion --jq '.[] | \"\\(.name): \\(.state) (\\(.conclusion // \"pending\"))\"'\n```\n\n| Status | Action |\n|--------|--------|\n| All passing | Continue |\n| Some failing | Warn, continue (note in scope) |\n| All failing | Warn strongly, continue (note in scope) |\n| Pending | Note, continue |\n\n**Flag CI status for review report.**\n\n### 2.3 Check Behind Base\n\n```bash\n# Get branch names\nPR_BASE=$(gh pr view {number} --json baseRefName --jq '.baseRefName')\nPR_HEAD=$(gh pr view {number} --json headRefName --jq '.headRefName')\n\n# Fetch and count\ngit fetch origin $PR_BASE --quiet\ngit fetch origin $PR_HEAD --quiet\n\n# Commits behind base branch\nBEHIND=$(git rev-list --count origin/$PR_HEAD..origin/$PR_BASE 2>/dev/null || echo \"0\")\n```\n\n| Commits Behind | Action |\n|----------------|--------|\n| 0-5 | Continue |\n| 6-15 | Warn, suggest rebase, continue |\n| 16+ | Warn strongly, recommend rebase before review |\n\n**If significantly behind:**\n```markdown\n⚠️ **Branch is {N} commits behind {base}**\n\nConsider rebasing before review to ensure you're reviewing against current code:\n```bash\ngit fetch origin {base}\ngit rebase origin/{base}\ngit push --force-with-lease\n```\n```\n\n### 2.4 Check Draft Status\n\n```bash\ngh pr view {number} --json isDraft --jq '.isDraft'\n```\n\n| Status | Action |\n|--------|--------|\n| `false` | Continue normally |\n| `true` | Note in scope, continue (user wants early feedback) |\n\n### 2.5 Check PR Size\n\n| Metric | Warning Threshold | Action |\n|--------|-------------------|--------|\n| Changed files | 20+ | Warn about review thoroughness |\n| Lines changed | 1000+ | Warn about review thoroughness |\n\n**If very large:**\n```markdown\n⚠️ **Large PR: {files} files, +{additions} -{deletions} lines**\n\nLarge PRs are harder to review thoroughly. Consider splitting into smaller PRs for better review quality.\n```\n\n### 2.6 Compile Reviewability Summary\n\n```markdown\n## Pre-Review Status\n\n| Check | Status | Notes |\n|-------|--------|-------|\n| Merge Conflicts | ✅ None / ❌ Has conflicts | {details} |\n| CI Status | ✅ Passing / ⚠️ Failing / ⏳ Pending | {details} |\n| Behind Base | ✅ Up to date / ⚠️ {N} commits behind | {details} |\n| Draft | ✅ Ready / 📝 Draft | {details} |\n| Size | ✅ Normal / ⚠️ Large ({N} files) | {details} |\n```\n\n**PHASE_2_CHECKPOINT:**\n- [ ] No merge conflicts (or workflow stopped)\n- [ ] CI status noted\n- [ ] Behind-base status checked\n- [ ] Draft status noted\n- [ ] Size warnings issued if needed\n\n---\n\n## Phase 3: CONTEXT - Gather Review Context\n\n### 3.1 Get Full Diff\n\n```bash\ngh pr diff {number}\n```\n\nStore this for reference - parallel agents will re-fetch as needed.\n\n### 3.2 List Changed Files by Type\n\n```bash\ngh pr view {number} --json files --jq '.files[].path'\n```\n\n**Categorize files:**\n- Source code (`.ts`, `.js`, `.py`, etc.)\n- Test files (`*.test.ts`, `*.spec.ts`, `test_*.py`)\n- Documentation (`*.md`, `docs/`)\n- Configuration (`.json`, `.yaml`, `.toml`)\n- Types/interfaces\n\n### 3.3 Check for CLAUDE.md\n\n```bash\ncat CLAUDE.md 2>/dev/null | head -100\n```\n\nNote key rules that reviewers should check against.\n\n### 3.4 Identify New Abstractions\n\nScan the diff for new abstractions introduced by this PR:\n\n- New interfaces, types, or abstract classes (search diff for `interface `, `type `, `abstract class`)\n- New utility modules or helper files (new `.ts` files that aren't feature files or tests)\n- New configuration keys or schema fields\n\nFor each new abstraction found, note it in the scope manifest under \"Review Focus Areas\" so the code review agent can verify it doesn't duplicate an existing primitive.\n\n```bash\n# Quick scan for new abstractions in diff\ngh pr diff {number} | grep \"^+\" | sed 's/^+//' | grep -E \"(^interface |^export interface |^type |^abstract class |^export class )\" | head -20\n```\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Diff available\n- [ ] Files categorized by type\n- [ ] CLAUDE.md rules noted\n- [ ] New abstractions scanned\n\n---\n\n## Phase 3.5: PLAN/ISSUE CONTEXT - Check for Workflow Artifacts\n\n**CRITICAL**: If this PR was created by a workflow, there will be artifacts that contain important context for reviewers.\n\n### 3.5.1 Find Workflow Artifacts\n\nCheck for artifacts from EITHER workflow type:\n\n```bash\n# Option 1: Plan-based workflow (archon-plan-to-merge)\nls -t $ARTIFACTS_DIR/../runs/*/plan-context.md 2>/dev/null | head -1\n\n# Option 2: Issue-based workflow (archon-fix-github-issue)\nls -t $ARTIFACTS_DIR/../runs/*/investigation.md 2>/dev/null | head -1\n```\n\n### 3.5.2 Extract Scope Limits\n\n**If plan-context.md exists** (from plan workflow):\n\n```bash\n# Extract the NOT Building section\nsed -n '/## NOT Building/,/^## /p' $ARTIFACTS_DIR/../runs/*/plan-context.md | head -30\n```\n\n**If investigation.md exists** (from issue workflow):\n\n```bash\n# Extract the Scope Boundaries / OUT OF SCOPE section\nsed -n '/## Scope Boundaries/,/^## /p' $ARTIFACTS_DIR/../runs/*/investigation.md | head -30\n```\n\n**These are INTENTIONAL exclusions** - do NOT flag them as bugs or missing features!\n\n### 3.5.3 Check Implementation Report\n\n```bash\n# Look for implementation report (either workflow)\nls -t $ARTIFACTS_DIR/../runs/*/implementation.md 2>/dev/null | head -1\n```\n\n**If implementation.md exists**, note any deviations:\n\n```bash\n# Extract deviations section\nsed -n '/## Deviations/,/^## /p' $ARTIFACTS_DIR/../runs/*/implementation.md | head -20\n```\n\n**PHASE_3.5_CHECKPOINT:**\n- [ ] Workflow artifacts checked (plan-context.md OR investigation.md)\n- [ ] Scope limits extracted (NOT Building OR OUT OF SCOPE)\n- [ ] Implementation deviations noted (if available)\n\n---\n\n## Phase 4: PREPARE - Create Artifacts Directory\n\n### 4.1 Create Directory Structure\n\n```bash\nmkdir -p $ARTIFACTS_DIR/review\n```\n\n### 4.2 Clean Stale Artifacts\n\n```bash\n# Remove review directories older than 7 days\nfind $ARTIFACTS_DIR/../reviews/pr-* -maxdepth 0 -mtime +7 -exec rm -rf {} \\; 2>/dev/null || true\n```\n\n### 4.3 Create Scope Manifest\n\nWrite `$ARTIFACTS_DIR/review/scope.md`:\n\n```markdown\n# PR Review Scope: #{number}\n\n**Title**: {PR title}\n**URL**: {PR URL}\n**Branch**: {head} → {base}\n**Author**: {author}\n**Date**: {ISO timestamp}\n\n---\n\n## Pre-Review Status\n\n| Check | Status | Notes |\n|-------|--------|-------|\n| Merge Conflicts | {status} | {details} |\n| CI Status | {status} | {passing}/{total} checks |\n| Behind Base | {status} | {N} commits behind |\n| Draft | {status} | {Ready/Draft} |\n| Size | {status} | {files} files, +{add}/-{del} |\n\n---\n\n## Changed Files\n\n| File | Type | Additions | Deletions |\n|------|------|-----------|-----------|\n| `src/file.ts` | source | +10 | -5 |\n| `src/file.test.ts` | test | +20 | -0 |\n| ... | ... | ... | ... |\n\n**Total**: {changedFiles} files, +{additions} -{deletions}\n\n---\n\n## File Categories\n\n### Source Files ({count})\n- `src/...`\n\n### Test Files ({count})\n- `src/...test.ts`\n\n### Documentation ({count})\n- `$DOCS_DIR/...`\n- `README.md`\n\n### Configuration ({count})\n- `package.json`\n\n---\n\n## Review Focus Areas\n\nBased on changes, reviewers should focus on:\n\n1. **Code Quality**: {list key source files}\n2. **Error Handling**: {files with try/catch, error handling}\n3. **Test Coverage**: {new functionality needing tests}\n4. **Comments/Docs**: {files with documentation changes}\n5. **Docs Impact**: {check if CLAUDE.md or $DOCS_DIR need updates}\n6. **Primitive Alignment**: {If new abstractions found: list them} — verify no duplication of existing primitives\n\n---\n\n## CLAUDE.md Rules to Check\n\n{Extract key rules from CLAUDE.md that apply to this PR}\n\n---\n\n## Workflow Context (if from automated workflow)\n\n{If plan-context.md OR investigation.md was found:}\n\n### Scope Limits (NOT Building / OUT OF SCOPE)\n\n**CRITICAL FOR REVIEWERS**: These items are **intentionally excluded** from scope. Do NOT flag them as bugs or missing features.\n\n{From plan-context.md \"NOT Building\" section OR investigation.md \"Scope Boundaries/OUT OF SCOPE\" section}\n\n**IN SCOPE:**\n- {what we're changing}\n\n**OUT OF SCOPE (do not touch):**\n- {Explicit exclusion 1 with rationale}\n- {Explicit exclusion 2 with rationale}\n\n### Implementation Deviations\n\n{If implementation.md was found and has deviations:}\n\n{Copy the \"Deviations\" section from implementation.md}\n\n{If no workflow artifacts found:}\n\n_No workflow artifacts found - this appears to be a manual PR._\n\n---\n\n## CI Details\n\n{If CI failing, list which checks failed}\n\n---\n\n## Metadata\n\n- **Scope created**: {ISO timestamp}\n- **Artifact path**: `$ARTIFACTS_DIR/review/`\n```\n\n**PHASE_4_CHECKPOINT:**\n- [ ] Directory created\n- [ ] Stale artifacts cleaned\n- [ ] Scope manifest written with pre-review status\n\n---\n\n## Phase 5: OUTPUT - Report to User\n\n### If Blocked (Conflicts)\n\n```markdown\n## ❌ Review Blocked: Merge Conflicts\n\n**PR**: #{number} - {title}\n\nThis PR has merge conflicts that must be resolved before review.\n\n### To Resolve\n\n```bash\ngit fetch origin {base}\ngit checkout {head}\ngit rebase origin/{base}\n# Resolve conflicts in your editor\ngit add .\ngit rebase --continue\ngit push --force-with-lease\n```\n\nThen re-request the review: `@archon review this PR`\n```\n\n### If Proceeding\n\n```markdown\n## PR Review Scope Complete\n\n**PR**: #{number} - {title}\n**Files**: {count} changed (+{additions} -{deletions})\n\n### Pre-Review Status\n| Check | Status |\n|-------|--------|\n| Conflicts | ✅ None |\n| CI | {✅ Passing / ⚠️ {N} failing} |\n| Behind Base | {✅ Up to date / ⚠️ {N} behind} |\n| Draft | {✅ Ready / 📝 Draft} |\n| Size | {✅ Normal / ⚠️ Large} |\n\n### File Categories\n- Source: {count} files\n- Tests: {count} files\n- Docs: {count} files\n- Config: {count} files\n\n### Artifacts Directory\n`$ARTIFACTS_DIR/review/`\n\n### Next Step\nLaunching 5 parallel review agents...\n```\n\n---\n\n## Success Criteria\n\n- **PR_IDENTIFIED**: Valid open PR found\n- **NO_CONFLICTS**: Merge conflicts block workflow\n- **CONTEXT_GATHERED**: Diff and file list available\n- **ARTIFACTS_DIR_CREATED**: Directory structure exists\n- **SCOPE_MANIFEST_WRITTEN**: `scope.md` file created with pre-review status\n",
  "archon-ralph-generate": "---\ndescription: Autonomously generate Ralph PRD files (prd.md + prd.json) from an idea or existing PRD\nargument-hint: <feature idea | path/to/existing-prd.md>\n---\n\n# Ralph PRD Generator (Autonomous)\n\n**Input**: $ARGUMENTS\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Your Mission\n\nGenerate production-quality Ralph PRD files — `prd.md` (full context document) and `prd.json` (story tracking) — through systematic codebase exploration and analysis. No interactive questions — make informed decisions autonomously.\n\n**Core Principle**: CODEBASE FIRST. Explore the project before writing anything. Stories must reference real files, real patterns, and real types.\n\n---\n\n## Phase 0: DETECT — Determine Input Type\n\n| Input Pattern | Type | Action |\n|---------------|------|--------|\n| Path to `.md` file that exists | Existing PRD | Read it, generate prd.json stories from it |\n| `.archon/ralph/{slug}/prd.md` exists | Existing PRD in ralph dir | Generate prd.json alongside it |\n| Free-form text | Feature idea | Generate both prd.md and prd.json |\n| Empty/blank | Error | STOP — require input |\n\n### If existing PRD detected:\n\n1. Read the PRD file\n2. Extract: problem statement, goals, user context, scope limits, technical requirements\n3. Skip to Phase 3 (Technical Grounding) — the PRD already covers Phases 1-2\n\n### If feature idea:\n\n1. Store the idea description\n2. Proceed through all phases\n\n---\n\n## Phase 1: UNDERSTAND — Problem & Context\n\n**Autonomously determine:**\n\n1. **Problem**: What pain point does this solve? What happens without it?\n2. **User**: Who benefits? What's their role and daily workflow?\n3. **Goal**: What's the ideal outcome? How will success be measured?\n4. **Scope**: What's MVP? What's explicitly out of scope?\n5. **Success metrics**: What measurable signals indicate this worked?\n\nBase these on the input description and your understanding of the codebase.\n\n**PHASE_1_CHECKPOINT:**\n- [ ] Problem clearly articulated\n- [ ] Target user identified\n- [ ] Goals and success metrics defined\n- [ ] Scope boundaries set\n\n---\n\n## Phase 2: UX & DESIGN — User Journey\n\n**Autonomously determine:**\n\n1. **Trigger**: What event causes the user to need this feature?\n2. **Happy path**: Step-by-step user flow from trigger to success\n3. **States**: Empty, loading, error, success — what does each look like?\n4. **Edge cases**: What can go wrong? How should it be handled?\n5. **Interaction model**: CLI commands, API endpoints, UI components?\n\nIf the feature has a UI component, describe the visual requirements.\nIf it's backend-only, describe the API surface.\n\n**PHASE_2_CHECKPOINT:**\n- [ ] User journey mapped\n- [ ] States enumerated\n- [ ] Edge cases identified\n\n---\n\n## Phase 3: TECHNICAL GROUNDING — Codebase Exploration\n\n**This is the critical phase.** Use the Task tool with `subagent_type=\"Explore\"` to systematically explore the codebase.\n\n### 3.1 Find Similar Implementations\n\n```\nExplore the codebase for patterns relevant to: {feature description}\n\nFIND:\n1. Similar implementations to mirror (with file:line references)\n2. Existing types/interfaces to extend or use\n3. Naming conventions (functions, files, variables)\n4. Error handling patterns\n5. Test patterns (framework, structure, assertion style)\n6. Database schema patterns (if applicable)\n7. Component patterns (if UI involved)\n```\n\n### 3.2 Identify Integration Points\n\n```\nTrace data flow and entry points for: {feature description}\n\nFIND:\n1. Where new code connects to existing code\n2. Which modules/packages are affected\n3. Import patterns to follow\n4. Config/env dependencies\n```\n\n### 3.3 Read Project Rules\n\n```bash\ncat CLAUDE.md\n```\n\nExtract: coding standards, naming conventions, testing requirements, lint rules.\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Similar implementations found with file:line references\n- [ ] Types and interfaces identified\n- [ ] Integration points mapped\n- [ ] CLAUDE.md rules noted\n\n---\n\n## Phase 4: STORY BREAKDOWN — Split Into Iterations\n\n### 4.1 Identify Layers\n\nBreak the feature into implementation layers:\n\n| Layer | Examples | Typical story count |\n|-------|---------|-------------------|\n| Schema/types | DB columns, interfaces, Zod schemas | 1-2 |\n| Backend logic | Services, utilities, API endpoints | 2-4 |\n| UI components | New components, modifications | 1-3 |\n| Integration | Wiring, config, exports | 1-2 |\n| Tests | Dedicated test stories (if complex) | 0-2 |\n\n### 4.2 Sizing Rules\n\nEach story must be completable in ONE iteration (~15-30 min of AI work):\n\n**Right-sized (ONE iteration):**\n- Add a database column + migration\n- Create one utility function + tests\n- Add one UI component\n- Update one API endpoint + tests\n- Write integration tests for one feature\n\n**TOO BIG (must split):**\n- \"Build entire feature\" → split into schema, types, backend, UI\n- \"Add authentication\" → split into schema, middleware, login UI, token handling\n- \"Refactor module\" → split by file or concern\n\n### 4.3 Dependency Ordering\n\n- Stories ordered by dependency (lower priority = runs first)\n- Schema before types before backend before UI before integration\n- `dependsOn` must only reference lower-priority stories\n- Validate: no circular dependencies, no forward references\n\n### 4.4 Acceptance Criteria Rules\n\n**GOOD (verifiable):**\n- \"Add `priority` column with type `'high' | 'medium' | 'low'`\"\n- \"Function returns empty array when input is null\"\n- \"Button shows loading state while submitting\"\n- \"Type-check passes with zero errors\"\n\n**BAD (vague):**\n- \"Works correctly\"\n- \"Good UX\"\n- \"Handles edge cases\"\n\nEvery criterion must be pass/fail testable.\n\n**PHASE_4_CHECKPOINT:**\n- [ ] Stories sized for single iterations\n- [ ] Dependencies form a valid DAG (no cycles)\n- [ ] Acceptance criteria are all verifiable\n- [ ] Technical notes reference real files and patterns\n\n---\n\n## Phase 5: GENERATE — Write PRD Files\n\n### 5.1 Determine Feature Slug\n\nGenerate a kebab-case slug from the feature name:\n- \"Workflow Lifecycle Overhaul\" → `workflow-lifecycle-overhaul`\n- \"Dark Mode Toggle\" → `dark-mode-toggle`\n- Max 50 characters\n\n### 5.2 Create Directory\n\n```bash\nmkdir -p .archon/ralph/{slug}\n```\n\n### 5.3 Write prd.md\n\n**Output path**: `.archon/ralph/{slug}/prd.md`\n\nInclude ALL of the following sections:\n\n```markdown\n# {Feature Name} — Product Requirements\n\n## Overview\n\n**Problem**: {What pain this solves — from Phase 1}\n**Solution**: {What we're building}\n**Branch**: `ralph/{slug}`\n\n---\n\n## Goals & Success\n\n### Primary Goal\n{The main outcome}\n\n### Success Metrics\n| Metric | Target | How Measured |\n|--------|--------|--------------|\n| {metric} | {target} | {method} |\n\n### Non-Goals (Out of Scope)\n- {Item 1} — {why excluded}\n- {Item 2} — {why excluded}\n\n---\n\n## User & Context\n\n### Target User\n- **Who**: {description}\n- **Role**: {their context}\n- **Current Pain**: {what they struggle with}\n\n### User Journey\n1. **Trigger**: {what prompts the need}\n2. **Action**: {what they do}\n3. **Outcome**: {success state}\n\n---\n\n## UX Requirements\n\n### Interaction Model\n{How users interact — CLI commands, API endpoints, UI components}\n\n### States to Handle\n| State | Description | Behavior |\n|-------|-------------|----------|\n| Empty | {when} | {what happens} |\n| Loading | {when} | {what happens} |\n| Error | {when} | {what happens} |\n| Success | {when} | {what happens} |\n\n---\n\n## Technical Context\n\n### Patterns to Follow\n- **Similar implementation**: `{file:lines}` — {what to mirror}\n- **Component pattern**: `{file:lines}` — {pattern description}\n- **Test pattern**: `{file:lines}` — {how to test}\n\n### Types & Interfaces\n```typescript\n// Key types to use or extend\n{relevant type definitions from codebase exploration}\n```\n\n### Architecture Notes\n- {Key technical decisions}\n- {Integration points from Phase 3}\n- {Dependencies}\n\n---\n\n## Implementation Summary\n\n### Story Overview\n| ID | Title | Priority | Dependencies |\n|----|-------|----------|--------------|\n| US-001 | {title} | 1 | — |\n| US-002 | {title} | 2 | US-001 |\n\n### Dependency Graph\n```\nUS-001 (schema/types)\n    ↓\nUS-002 (backend)\n    ↓\nUS-003 (UI) → US-004 (integration)\n```\n\n---\n\n## Validation Requirements\n\nEvery story must pass:\n- [ ] Type-check: `bun run type-check`\n- [ ] Lint: `bun run lint`\n- [ ] Tests: `bun run test`\n- [ ] Format: `bun run format:check`\n\n---\n\n*Generated: {ISO timestamp}*\n```\n\n**If input was an existing PRD**: Incorporate its content into this structure. Don't lose information — merge the existing PRD's goals, context, and requirements into the appropriate sections. Add the technical context from your codebase exploration (Phase 3).\n\n### 5.4 Write prd.json\n\n**Output path**: `.archon/ralph/{slug}/prd.json`\n\n```json\n{\n  \"project\": \"{ProjectName}\",\n  \"branchName\": \"ralph/{slug}\",\n  \"prdFile\": \"prd.md\",\n  \"description\": \"{One line summary}\",\n  \"userStories\": [\n    {\n      \"id\": \"US-001\",\n      \"title\": \"{Short title}\",\n      \"description\": \"As a {user}, I want {capability} so that {benefit}\",\n      \"acceptanceCriteria\": [\n        \"{Specific verifiable criterion 1}\",\n        \"{Specific verifiable criterion 2}\",\n        \"Type-check passes\",\n        \"Tests pass\"\n      ],\n      \"technicalNotes\": \"{Files to modify, patterns to follow, types to use — from Phase 3}\",\n      \"dependsOn\": [],\n      \"priority\": 1,\n      \"passes\": false,\n      \"notes\": \"\"\n    }\n  ]\n}\n```\n\n### 5.5 Commit PRD Files\n\n```bash\ngit add .archon/ralph/{slug}/\ngit commit -m \"docs: add Ralph PRD for {feature name}\"\n```\n\n**PHASE_5_CHECKPOINT:**\n- [ ] `.archon/ralph/{slug}/prd.md` written with all sections\n- [ ] `.archon/ralph/{slug}/prd.json` written with properly sized stories\n- [ ] Stories have verifiable acceptance criteria\n- [ ] Technical notes reference real files from codebase exploration\n- [ ] Files committed\n\n---\n\n## Phase 6: OUTPUT — Report\n\n```\nPRD_DIR=.archon/ralph/{slug}\nSTORIES_TOTAL={count}\nFILES_CREATED=prd.md,prd.json\n\n## Ralph PRD Ready\n\n**Feature**: {name}\n**Directory**: `.archon/ralph/{slug}/`\n**Stories**: {count} user stories\n**Dependencies**: Valid DAG (no cycles)\n\n| # | ID | Title | Dependencies |\n|---|-----|-------|--------------|\n| 1 | US-001 | {title} | — |\n| 2 | US-002 | {title} | US-001 |\n```\n\n---\n\n## Success Criteria\n\n- **CONTEXT_COMPLETE**: prd.md has goals, user context, UX, technical patterns from real codebase exploration\n- **STORIES_SIZED**: Each story completable in one iteration\n- **DEPENDENCIES_VALID**: No circular dependencies, lower priority runs first\n- **CRITERIA_VERIFIABLE**: All acceptance criteria are pass/fail testable\n- **TECHNICAL_GROUNDED**: Technical notes reference real files, types, and patterns from the codebase\n- **FILES_WRITTEN**: Both prd.md and prd.json exist in `.archon/ralph/{slug}/`\n",
  "archon-ralph-prd": "# Ralph PRD Generator\n\n**Input**: $ARGUMENTS\n\n---\n\n## Your Role\n\nYou are creating a PRD for the Ralph autonomous loop. You generate TWO files:\n1. `prd.md` - Full context document (goals, persona, UX, success criteria)\n2. `prd.json` - Story tracking with passes/fails\n\nEach Ralph iteration receives the FULL prd.md context plus its specific story from prd.json.\n\n**Critical Rules:**\n- Each story must be completable in ONE iteration\n- Stories ordered by dependency (schema → backend → UI)\n- Acceptance criteria must be VERIFIABLE (not vague)\n\n---\n\n## Phase 1: INITIATE\n\n**If no input provided**, ask:\n\n> **What do you want to build?**\n> Describe the feature or capability in a few sentences.\n\n**If input provided**, confirm:\n\n> I understand you want to build: {restated understanding}\n> Is this correct?\n\n**GATE**: Wait for confirmation.\n\n---\n\n## Phase 2: FOUNDATION\n\nAsk these questions together:\n\n> **Foundation Questions:**\n>\n> 1. **Problem**: What pain point does this solve? What happens if we don't build it?\n>\n> 2. **User**: Who is this for? Describe their role and context.\n>\n> 3. **Goal**: What's the ideal outcome if this succeeds?\n>\n> 4. **Scope**: MVP or full implementation? What's explicitly out of scope?\n>\n> 5. **Success**: How will we measure if this worked? What metrics matter?\n\n**GATE**: Wait for answers.\n\n---\n\n## Phase 3: UX & DESIGN\n\nAsk:\n\n> **UX Questions:**\n>\n> 1. **User Journey**: What triggers the user to need this? What's the happy path?\n>\n> 2. **UI Requirements**: Any specific visual requirements? Colors, placement, components?\n>\n> 3. **Interaction Model**: How does the user interact? Clicks, keyboard, API?\n>\n> 4. **Edge Cases**: What error states need handling? Empty states?\n>\n> 5. **Accessibility**: Any a11y requirements?\n\n**GATE**: Wait for answers.\n\n---\n\n## Phase 4: TECHNICAL GROUNDING\n\n**Use Explore agent:**\n\n```\nExplore the codebase for patterns relevant to: {feature}\n\nFIND:\n1. Similar implementations to mirror (with file:line references)\n2. Existing types/interfaces to extend\n3. Component patterns to follow\n4. Test patterns used\n5. Database schema patterns\n```\n\n**Summarize:**\n\n> **Technical Context:**\n> - Similar pattern: {file:lines}\n> - Types to extend: {types}\n> - Components to use: {components}\n> - Test pattern: {pattern}\n>\n> Any additional technical constraints?\n\n**GATE**: Brief pause for input.\n\n---\n\n## Phase 5: STORY BREAKDOWN\n\nAsk:\n\n> **Story Planning:**\n>\n> 1. **Database**: Schema changes needed? New tables/columns?\n>\n> 2. **Types**: New interfaces or type extensions?\n>\n> 3. **Backend**: Server logic, API endpoints, services?\n>\n> 4. **UI Components**: New components or modifications?\n>\n> 5. **Integration**: How do pieces connect?\n\n**GATE**: Wait for answers.\n\n---\n\n## Phase 6: GENERATE FILES\n\n**Naming Convention**: Use the feature name as a kebab-case slug.\n- Feature: \"User Authentication\" → slug: `user-authentication`\n- Feature: \"Dark Mode Toggle\" → slug: `dark-mode-toggle`\n\n**First**, create the ralph directory for this feature:\n```bash\n# Replace {feature-slug} with the actual kebab-case feature name\nmkdir -p .archon/ralph/{feature-slug}\n```\n\n### File 1: prd.md\n\n**Output path**: `.archon/ralph/{feature-slug}/prd.md`\n\n```markdown\n# {Feature Name} - Product Requirements\n\n## Overview\n\n**Problem**: {What pain this solves}\n**Solution**: {What we're building}\n**Branch**: `ralph/{feature-kebab}`\n\n---\n\n## Goals & Success\n\n### Primary Goal\n{The main outcome we want}\n\n### Success Metrics\n| Metric | Target | How Measured |\n|--------|--------|--------------|\n| {metric} | {target} | {method} |\n\n### Non-Goals (Out of Scope)\n- {Item 1} - {why excluded}\n- {Item 2} - {why excluded}\n\n---\n\n## User & Context\n\n### Target User\n- **Who**: {Specific description}\n- **Role**: {Their job/context}\n- **Current Pain**: {What they struggle with today}\n\n### User Journey\n1. **Trigger**: {What prompts the need}\n2. **Action**: {What they do}\n3. **Outcome**: {What success looks like}\n\n### Jobs to Be Done\nWhen {situation}, I want to {motivation}, so I can {outcome}.\n\n---\n\n## UX Requirements\n\n### Visual Design\n- {Color/style requirements}\n- {Component preferences}\n- {Layout requirements}\n\n### Interaction Model\n- {How users interact}\n- {Keyboard shortcuts if any}\n- {Mobile considerations}\n\n### States to Handle\n| State | Description | UI Behavior |\n|-------|-------------|-------------|\n| Empty | {when} | {show what} |\n| Loading | {when} | {show what} |\n| Error | {when} | {show what} |\n| Success | {when} | {show what} |\n\n### Accessibility\n- {A11y requirements}\n\n---\n\n## Technical Context\n\n### Patterns to Follow\n- **Similar implementation**: `{file:lines}` - {what to mirror}\n- **Component pattern**: `{file:lines}` - {pattern description}\n- **Test pattern**: `{file:lines}` - {how to test}\n\n### Types & Interfaces\n```typescript\n// Extend or use these existing types:\n{relevant type definitions}\n```\n\n### Architecture Notes\n- {Key technical decisions}\n- {Integration points}\n- {Dependencies}\n\n---\n\n## Implementation Summary\n\n### Story Overview\n| ID | Title | Priority | Dependencies |\n|----|-------|----------|--------------|\n| US-001 | {title} | 1 | - |\n| US-002 | {title} | 2 | US-001 |\n{...}\n\n### Dependency Graph\n```\nUS-001 (schema)\n    ↓\nUS-002 (types)\n    ↓\nUS-003 (backend) → US-004 (UI components)\n                        ↓\n                   US-005 (integration)\n```\n\n---\n\n## Validation Requirements\n\nEvery story must pass:\n- [ ] Typecheck: `bun run type-check`\n- [ ] Lint: `bun run lint`\n- [ ] Tests: `bun test`\n\n---\n\n*Generated: {ISO timestamp}*\n```\n\n### File 2: prd.json\n\n**Output path**: `.archon/ralph/{feature-slug}/prd.json`\n\n```json\n{\n  \"project\": \"{ProjectName}\",\n  \"branchName\": \"ralph/{feature-kebab}\",\n  \"prdFile\": \"prd.md\",\n  \"description\": \"{One line summary}\",\n  \"userStories\": [\n    {\n      \"id\": \"US-001\",\n      \"title\": \"{Short title}\",\n      \"description\": \"As a {user}, I want {capability} so that {benefit}\",\n      \"acceptanceCriteria\": [\n        \"{Specific verifiable criterion}\",\n        \"Typecheck passes\"\n      ],\n      \"technicalNotes\": \"{Implementation hints from prd.md}\",\n      \"dependsOn\": [],\n      \"priority\": 1,\n      \"passes\": false,\n      \"notes\": \"\"\n    }\n  ]\n}\n```\n\n### Story Sizing Rules\n\n**Right-sized (ONE iteration):**\n- Add a database column + migration\n- Create one utility function + tests\n- Add one UI component\n- Update one API endpoint\n\n**TOO BIG (split):**\n- \"Build entire feature\" → schema, types, backend, UI\n- \"Add authentication\" → schema, middleware, login UI\n\n### Acceptance Criteria Rules\n\n**GOOD (verifiable):**\n- \"Add `priority` column with type 'high' | 'medium' | 'low'\"\n- \"Function returns empty array when input is null\"\n- \"Button shows loading state while submitting\"\n\n**BAD (vague):**\n- \"Works correctly\"\n- \"Good UX\"\n- \"Handles edge cases\"\n\n---\n\n## Phase 7: OUTPUT\n\nAfter generating both files, report:\n\n```markdown\n## Ralph PRD Created\n\n### Files Generated\n\n| File | Purpose |\n|------|---------|\n| `.archon/ralph/{feature-slug}/prd.md` | Full context - goals, UX, technical patterns |\n| `.archon/ralph/{feature-slug}/prd.json` | Story tracking - passes/fails per story |\n\n### Summary\n\n**Feature**: {name}\n**Branch**: `ralph/{feature}`\n**Stories**: {count} user stories\n**Estimated iterations**: {count}\n\n### User Stories\n\n| # | ID | Title | Dependencies |\n|---|-----|-------|--------------|\n| 1 | US-001 | {title} | - |\n| 2 | US-002 | {title} | US-001 |\n{...}\n\n### Context Passed to Each Iteration\n\nEach Ralph iteration receives:\n1. **Full PRD** (`.archon/ralph/{feature-slug}/prd.md`) - Goals, persona, UX, technical patterns\n2. **Current Story** - From `.archon/ralph/{feature-slug}/prd.json` with acceptance criteria\n3. **Previous Learnings** - From `.archon/ralph/{feature-slug}/progress.txt`\n\n### To Start\n\n```bash\n# Create feature branch\ngit checkout -b ralph/{feature-slug}\n\n# Initialize progress\necho \"# Ralph Progress Log\\nStarted: $(date)\\n---\" > .archon/ralph/{feature-slug}/progress.txt\n\n# Run Ralph - specify the feature directory\n@Archon run ralph .archon/ralph/{feature-slug}\n```\n```\n\n---\n\n## Question Flow\n\n```\nINITIATE → FOUNDATION → UX/DESIGN → TECHNICAL → BREAKDOWN → GENERATE\n    ↓           ↓            ↓           ↓           ↓          ↓\n Confirm    Problem,      Journey,   Patterns,   Stories,   prd.md +\n  idea      User,         UI reqs,   Types,      DB/API/    prd.json\n            Goals         States     Tests       UI split\n```\n\n---\n\n## Success Criteria\n\n- **CONTEXT_COMPLETE**: prd.md has goals, persona, UX, technical context\n- **STORIES_SIZED**: Each story completable in one iteration\n- **DEPENDENCIES_VALID**: Lower priority never depends on higher\n- **CRITERIA_VERIFIABLE**: All acceptance criteria are pass/fail\n- **READY_TO_RUN**: User can immediately start Ralph loop\n",
  "archon-resolve-merge-conflicts": "---\ndescription: Analyze and resolve merge conflicts in a PR\nargument-hint: <pr-number|url>\n---\n\n# Resolve Merge Conflicts\n\n**Input**: $ARGUMENTS\n\n---\n\n## Your Mission\n\nAnalyze merge conflicts in the PR, automatically resolve simple conflicts where intent is clear, present options for complex conflicts, and push the resolution.\n\n---\n\n## Phase 1: IDENTIFY - Get PR and Conflict Info\n\n### 1.1 Parse Input\n\n**Check input format:**\n- Number (`123`, `#123`) → GitHub PR number\n- URL (`https://github.com/...`) → Extract PR number\n- Empty → Check current branch for open PR\n\n```bash\ngh pr view {number} --json number,title,headRefName,baseRefName,mergeable,mergeStateStatus\n```\n\n### 1.2 Verify Conflicts Exist\n\n```bash\ngh pr view {number} --json mergeable,mergeStateStatus --jq '.mergeable, .mergeStateStatus'\n```\n\n| Status | Action |\n|--------|--------|\n| `CONFLICTING` | Continue with resolution |\n| `MERGEABLE` | Report \"No conflicts to resolve\" and exit |\n| `UNKNOWN` | Wait and retry, or proceed with caution |\n\n**If no conflicts:**\n```markdown\n## ✅ No Conflicts\n\nPR #{number} has no merge conflicts. It's ready for review/merge.\n```\n**Exit if no conflicts.**\n\n### 1.3 Setup Local Branch\n\n```bash\n# Get branch info\nPR_HEAD=$(gh pr view {number} --json headRefName --jq '.headRefName')\nPR_BASE=$(gh pr view {number} --json baseRefName --jq '.baseRefName')\n\n# Fetch latest\ngit fetch origin $PR_BASE\ngit fetch origin $PR_HEAD\n\n# Checkout the PR branch\ngit checkout $PR_HEAD\ngit pull origin $PR_HEAD\n```\n\n**PHASE_1_CHECKPOINT:**\n- [ ] PR identified with conflicts\n- [ ] Branches fetched\n- [ ] On PR branch locally\n\n---\n\n## Phase 2: ANALYZE - Understand the Conflicts\n\n### 2.1 Attempt Rebase to Surface Conflicts\n\n```bash\ngit rebase origin/$PR_BASE\n```\n\nThis will stop at the first conflict. Note the output.\n\n### 2.2 Identify Conflicting Files\n\n```bash\ngit diff --name-only --diff-filter=U\n```\n\nList all files with conflicts.\n\n### 2.3 Analyze Each Conflict\n\nFor each conflicting file:\n\n```bash\n# Show the conflict markers\ngit diff --check\ncat {file} | grep -A 10 -B 2 \"<<<<<<<\"\n```\n\n**Categorize each conflict:**\n\n| Type | Description | Auto-resolvable? |\n|------|-------------|------------------|\n| **SIMPLE_ADDITION** | One side added, other didn't change that area | ✅ Yes |\n| **SIMPLE_DELETION** | One side deleted, other didn't change | ⚠️ Maybe (check intent) |\n| **DIFFERENT_AREAS** | Both changed but different lines | ✅ Yes |\n| **SAME_LINES** | Both changed the exact same lines | ❌ No - needs decision |\n| **STRUCTURAL** | File moved/renamed + modified | ❌ No - needs decision |\n\n### 2.4 Read Both Versions\n\nFor complex conflicts, understand what each side was trying to do:\n\n```bash\n# Show base version (common ancestor)\ngit show :1:{file} 2>/dev/null || echo \"File didn't exist in base\"\n\n# Show \"ours\" version (HEAD/current branch)\ngit show :2:{file}\n\n# Show \"theirs\" version (incoming from base branch)\ngit show :3:{file}\n```\n\n**PHASE_2_CHECKPOINT:**\n- [ ] All conflicting files identified\n- [ ] Each conflict categorized\n- [ ] Both sides' intent understood\n\n---\n\n## Phase 3: RESOLVE - Fix the Conflicts\n\n### 3.1 Auto-Resolve Simple Conflicts\n\nFor conflicts where intent is clear:\n\n```bash\n# For each auto-resolvable file\n# Edit to keep both changes (if both are additive)\n# Or keep the appropriate side based on intent\n```\n\n**Auto-resolution rules:**\n1. **Both added different things**: Keep both additions\n2. **One updated, one didn't touch**: Keep the update\n3. **Import additions**: Merge both import lists\n4. **Comment changes**: Prefer the more informative version\n\n### 3.2 Present Options for Complex Conflicts\n\nFor conflicts that need human decision:\n\n```markdown\n## Conflict in `{file}`\n\n**Lines {start}-{end}**\n\n### Option A: Keep PR Changes (HEAD)\n```{language}\n{code from PR branch}\n```\n\n**What this does**: {explanation of PR's intent}\n\n### Option B: Keep Base Branch Changes\n```{language}\n{code from base branch}\n```\n\n**What this does**: {explanation of base branch's intent}\n\n### Option C: Merge Both (Recommended if compatible)\n```{language}\n{merged version if possible}\n```\n\n**Why**: {explanation of why this merge makes sense}\n\n### Option D: Custom Resolution Needed\nThe changes are incompatible. Manual review required.\n\n---\n\n**Recommendation**: Option {X}\n\n**Reasoning**: {why this option based on:\n- Code functionality\n- PR intent from title/description\n- Which change is more recent/complete\n- Impact on other code}\n```\n\n### 3.3 Apply Resolutions\n\nFor each conflict:\n\n1. **If auto-resolvable**: Apply the resolution\n2. **If needs decision**: Use recommended option (or ask user if unclear)\n\n```bash\n# After editing each file\ngit add {file}\n```\n\n### 3.4 Continue Rebase\n\n```bash\n# After resolving all conflicts in current commit\ngit rebase --continue\n```\n\nRepeat for any additional conflicting commits.\n\n**PHASE_3_CHECKPOINT:**\n- [ ] All simple conflicts auto-resolved\n- [ ] Complex conflicts resolved with documented reasoning\n- [ ] All files staged\n- [ ] Rebase completed\n\n---\n\n## Phase 4: VALIDATE - Verify Resolution\n\n### 4.1 Check No Remaining Conflicts\n\n```bash\ngit diff --check\n```\n\nShould return empty (no conflict markers remaining).\n\n### 4.2 Verify Code Compiles\n\n```bash\nbun run type-check\n```\n\nIf type errors related to resolution, fix them.\n\n### 4.3 Run Tests\n\n```bash\nbun test\n```\n\nIf tests fail due to resolution, investigate and fix.\n\n### 4.4 Lint Check\n\n```bash\nbun run lint\n```\n\nFix any lint issues.\n\n**PHASE_4_CHECKPOINT:**\n- [ ] No conflict markers remaining\n- [ ] Type check passes\n- [ ] Tests pass\n- [ ] Lint passes\n\n---\n\n## Phase 5: PUSH - Update the PR\n\n### 5.1 Force Push the Resolved Branch\n\n```bash\ngit push --force-with-lease origin $PR_HEAD\n```\n\n**Note**: `--force-with-lease` is safer than `--force` as it fails if someone else pushed.\n\n### 5.2 Verify PR is Now Mergeable\n\n```bash\ngh pr view {number} --json mergeable,mergeStateStatus\n```\n\nShould show `MERGEABLE`.\n\n**PHASE_5_CHECKPOINT:**\n- [ ] Branch pushed successfully\n- [ ] PR shows as mergeable\n\n---\n\n## Phase 6: REPORT - Document Resolution\n\n### 6.1 Create Resolution Artifact\n\nWrite to `$ARTIFACTS_DIR/../reviews/pr-{number}/conflict-resolution.md` (create dir if needed):\n\n```markdown\n# Conflict Resolution: PR #{number}\n\n**Date**: {ISO timestamp}\n**Branch**: {head} rebased onto {base}\n\n---\n\n## Summary\n\nResolved {N} conflicts in {M} files.\n\n---\n\n## Conflicts Resolved\n\n### File: `{file1}`\n\n**Conflict Type**: {SIMPLE_ADDITION | SAME_LINES | etc.}\n**Resolution**: {Auto-resolved | Option A/B/C chosen}\n\n**Before (conflict)**:\n```{language}\n<<<<<<< HEAD\n{head version}\n=======\n{base version}\n>>>>>>> {base}\n```\n\n**After (resolved)**:\n```{language}\n{final code}\n```\n\n**Reasoning**: {why this resolution}\n\n---\n\n### File: `{file2}`\n\n{Same structure...}\n\n---\n\n## Validation\n\n| Check | Status |\n|-------|--------|\n| No conflict markers | ✅ |\n| Type check | ✅ |\n| Tests | ✅ |\n| Lint | ✅ |\n\n---\n\n## Git Log\n\n```\n{git log --oneline -5}\n```\n\n---\n\n## Metadata\n\n- **Resolved by**: Archon\n- **Timestamp**: {ISO timestamp}\n```\n\n### 6.2 Post GitHub Comment\n\n```bash\ngh pr comment {number} --body \"$(cat <<'EOF'\n## ✅ Conflicts Resolved\n\n**Rebased onto**: `{base}`\n**Conflicts resolved**: {N} in {M} files\n\n### Resolution Summary\n\n| File | Conflict Type | Resolution |\n|------|---------------|------------|\n| `{file1}` | {type} | {resolution approach} |\n| `{file2}` | {type} | {resolution approach} |\n\n### Validation\n✅ Type check | ✅ Tests | ✅ Lint\n\n### Details\nSee `$ARTIFACTS_DIR/../reviews/pr-{number}/conflict-resolution.md` for full resolution details.\n\n---\n*Resolved by Archon resolve-conflicts workflow*\nEOF\n)\"\n```\n\n**PHASE_6_CHECKPOINT:**\n- [ ] Artifact created\n- [ ] GitHub comment posted\n\n---\n\n## Phase 7: OUTPUT - Final Report\n\n```markdown\n## ✅ Conflicts Resolved\n\n**PR**: #{number} - {title}\n**Branch**: `{head}` rebased onto `{base}`\n\n### Summary\n- **Files with conflicts**: {M}\n- **Conflicts resolved**: {N}\n- **Auto-resolved**: {X}\n- **Manual decisions**: {Y}\n\n### Resolution Details\n\n| File | Type | Resolution |\n|------|------|------------|\n| `{file}` | {type} | {approach} |\n\n### Validation\n| Check | Status |\n|-------|--------|\n| Type check | ✅ |\n| Tests | ✅ |\n| Lint | ✅ |\n\n### Artifacts\n- Resolution details: `$ARTIFACTS_DIR/../reviews/pr-{number}/conflict-resolution.md`\n\n### Next Steps\n1. Review the resolution if needed: `git log -p -1`\n2. PR is now ready for review\n3. Request review: `@archon review this PR`\n```\n\n---\n\n## Error Handling\n\n### Rebase Fails Mid-way\n\nIf rebase fails on a commit that can't be resolved:\n\n```bash\n# Check status\ngit status\n\n# If truly stuck, abort and report\ngit rebase --abort\n```\n\nReport the failure with details about which commit and why.\n\n### Push Fails\n\nIf `--force-with-lease` fails (someone else pushed):\n\n1. Fetch latest\n2. Re-analyze conflicts\n3. Start over\n\n### Validation Fails After Resolution\n\nIf type-check/tests fail after resolution:\n\n1. Investigate which resolution caused the issue\n2. Try alternative resolution\n3. If stuck, report and suggest manual review\n\n---\n\n## Success Criteria\n\n- **CONFLICTS_IDENTIFIED**: All conflicting files found\n- **CONFLICTS_RESOLVED**: All conflicts resolved (auto or manual)\n- **VALIDATION_PASSED**: Type check, tests, lint all pass\n- **BRANCH_PUSHED**: PR branch updated with resolution\n- **PR_MERGEABLE**: GitHub shows PR as mergeable\n- **DOCUMENTED**: Resolution artifact and GitHub comment created\n",
  "archon-self-fix-all": "---\ndescription: Aggressively fix all review findings - lean towards fixing unless clearly a new concern\nargument-hint: (none - reads all review artifacts from $ARTIFACTS_DIR/review/)\n---\n\n# Self-Fix All Review Findings\n\n---\n\n## IMPORTANT: Output Behavior\n\n**Your output will be posted as a GitHub comment.** Keep working output minimal:\n- Do NOT narrate each step\n- Do NOT output verbose progress updates\n- Only output the final structured report at the end\n\n---\n\n## Your Mission\n\nRead all review artifacts and fix EVERYTHING surfaced. Unlike conservative auto-fix, you lean aggressively towards fixing. LLMs are fast at generating code — use that advantage to add tests, fix docs, improve error handling, and address all findings.\n\n**Philosophy**: Fix it unless it's clearly a NEW unrelated concern that deserves its own issue. Adding tests for existing code? Fix it. Updating docs? Fix it. Adding missing error handling? Fix it. The bar for skipping is HIGH — only skip when the fix would introduce a genuinely new feature or concern outside the PR's scope.\n\n**Output artifact**: `$ARTIFACTS_DIR/review/fix-report.md`\n**Git action**: Commit AND push fixes to the PR branch\n**GitHub action**: Post fix report as a comment on the PR\n\n---\n\n## Phase 1: LOAD — Get Context\n\n### 1.1 Get PR Number and Branch\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number)\nHEAD_BRANCH=$(gh pr view $PR_NUMBER --json headRefName --jq '.headRefName')\necho \"PR: $PR_NUMBER, Branch: $HEAD_BRANCH\"\n```\n\n### 1.2 Checkout PR Branch\n\n```bash\ngit fetch origin $HEAD_BRANCH\ngit checkout $HEAD_BRANCH\ngit pull origin $HEAD_BRANCH\n```\n\nVerify:\n\n```bash\ngit branch --show-current\ngit status --porcelain\n```\n\n### 1.3 Read All Review Artifacts\n\n```bash\nls $ARTIFACTS_DIR/review/\n```\n\nRead each `.md` file that contains findings (e.g. `code-review-findings.md`, `error-handling-findings.md`, `test-coverage-findings.md`, `comment-quality-findings.md`, `docs-impact-findings.md`, `consolidated-review.md`). Skip `scope.md` and `fix-report.md`.\n\n```bash\nfor f in $ARTIFACTS_DIR/review/*.md; do\n  echo \"=== $f ===\"; cat \"$f\"; echo\ndone\n```\n\n### 1.4 Extract All Findings\n\nCompile a unified list of ALL findings with severity, location, and suggested fix.\n\n**PHASE_1_CHECKPOINT:**\n\n- [ ] PR number and branch identified\n- [ ] On correct PR branch\n- [ ] All review artifacts read\n- [ ] All findings extracted\n\n---\n\n## Phase 2: TRIAGE — Decide What to Fix\n\nFor each finding, decide: **FIX** or **SKIP**.\n\n### FIX (default — lean towards fixing):\n\n- Real bugs, type errors, silent failures, code quality issues\n- Missing tests for changed or existing code touched by the PR\n- Missing or outdated documentation\n- Error handling gaps\n- Comment quality issues\n- Import organization\n- Naming improvements\n- Any finding where the fix is concrete and the code is within the PR's touched area\n\n### SKIP only if:\n\n- The fix introduces a **genuinely new feature** not related to the PR\n- The fix requires **architectural changes** that affect untouched subsystems\n- The fix is about code **completely unrelated** to the PR's changes\n- The finding is factually wrong or based on a misunderstanding\n\n**Key principle**: If the review agent found it while reviewing THIS PR, it's fair game to fix. Tests, docs, simplification, error handling — all fixable. The only skip reason is \"this is a new concern that deserves its own issue.\"\n\nFor each skipped finding, write down **the specific reason**.\n\n**PHASE_2_CHECKPOINT:**\n\n- [ ] Every finding marked FIX or SKIP\n- [ ] Skip reasons documented (should be very few)\n\n---\n\n## Phase 3: IMPLEMENT — Apply Fixes\n\n### 3.1 For Each Finding Marked FIX\n\n1. Read the relevant file(s)\n2. Apply the fix following the suggested approach\n3. Run type-check after each fix: `bun run type-check`\n4. Note exactly what was changed\n\n### 3.2 Add Tests\n\nFor ANY finding about missing tests:\n\n1. Create or update the test file\n2. Write meaningful tests (not just stubs)\n3. Run them: `bun test {file}`\n\n### 3.3 Fix Documentation\n\nFor ANY finding about docs:\n\n1. Update the relevant documentation\n2. Ensure accuracy with the current code\n\n### 3.4 Handle Blocked Fixes\n\nIf a fix cannot be applied (code changed since review, fix would break other things), mark as **BLOCKED** with reason. Do not force a broken fix.\n\n**PHASE_3_CHECKPOINT:**\n\n- [ ] All FIX findings attempted\n- [ ] Tests added where flagged\n- [ ] Docs updated where flagged\n- [ ] BLOCKED findings documented\n\n---\n\n## Phase 4: VALIDATE — Full Check\n\n```bash\nbun run type-check\nbun run lint\nbun test\n```\n\nAll must pass. If something fails after a fix:\n\n1. Review the error\n2. Adjust the fix or revert it and mark BLOCKED\n3. Re-run until clean\n\n**PHASE_4_CHECKPOINT:**\n\n- [ ] Type check passes\n- [ ] Lint passes\n- [ ] Tests pass\n\n---\n\n## Phase 5: COMMIT AND PUSH\n\n### 5.1 Stage and Commit\n\nOnly stage files you actually changed:\n\n```bash\ngit add {specific files}\ngit status\ngit commit -m \"$(cat <<'EOF'\nfix: address review findings\n\nFixed:\n- {brief list of fixes}\n\nTests added:\n- {brief list if any}\n\nSkipped:\n- {brief list if any, with reasons}\nEOF\n)\"\n```\n\n### 5.2 Push\n\n```bash\ngit push origin $HEAD_BRANCH\n```\n\nIf push fails due to divergence:\n\n```bash\ngit pull --rebase origin $HEAD_BRANCH\ngit push origin $HEAD_BRANCH\n```\n\n**PHASE_5_CHECKPOINT:**\n\n- [ ] Changes committed\n- [ ] Pushed to PR branch\n\n---\n\n## Phase 6: GENERATE — Write Fix Report\n\nWrite to `$ARTIFACTS_DIR/review/fix-report.md`:\n\n```markdown\n# Fix Report: PR #{number}\n\n**Date**: {ISO timestamp}\n**Status**: COMPLETE | PARTIAL\n**Branch**: {HEAD_BRANCH}\n**Commit**: {commit hash}\n**Philosophy**: Aggressive fix — lean towards fixing everything\n\n---\n\n## Summary\n\n{2-3 sentences: what was found, what was fixed, what was skipped and why}\n\n---\n\n## Fixes Applied\n\n| Severity | Finding | Location | What Was Done |\n|----------|---------|----------|---------------|\n| CRITICAL | {title} | `file:line` | {description} |\n| HIGH     | {title} | `file:line` | {description} |\n| MEDIUM   | {title} | `file:line` | {description} |\n| LOW      | {title} | `file:line` | {description} |\n\n---\n\n## Tests Added\n\n| File | Test Cases |\n|------|------------|\n| `{file}.test.ts` | `{test description}` |\n\n*(none)* if no tests were added\n\n---\n\n## Docs Updated\n\n| File | Changes |\n|------|---------|\n| `{file}` | {what was updated} |\n\n*(none)* if no docs were updated\n\n---\n\n## Skipped Findings\n\n| Severity | Finding | Location | Reason Skipped |\n|----------|---------|----------|----------------|\n| {sev}    | {title} | `file:line` | New concern: {specific reason} |\n\n*(none)* if nothing was skipped — ideal outcome\n\n---\n\n## Blocked (Could Not Fix)\n\n| Severity | Finding | Reason |\n|----------|---------|--------|\n| {sev}    | {title} | {why it could not be applied} |\n\n*(none)* if nothing was blocked\n\n---\n\n## Suggested Follow-up Issues\n\n{For any skipped or blocked findings that warrant their own issue:}\n\n| Issue Title | Priority | Reason |\n|-------------|----------|--------|\n| \"{title}\" | {P1/P2/P3} | {why this deserves a separate issue} |\n\n*(none)* if everything was addressed\n\n---\n\n## Validation\n\n| Check | Status |\n|-------|--------|\n| Type check | ✅ / ❌ |\n| Lint | ✅ / ❌ |\n| Tests | ✅ {n} passed / ❌ |\n```\n\n**PHASE_6_CHECKPOINT:**\n\n- [ ] Fix report written\n\n---\n\n## Phase 7: POST — GitHub Comment\n\nPost the fix report as a PR comment:\n\n```bash\ngh pr comment $PR_NUMBER --body \"$(cat <<'EOF'\n## ⚡ Self-Fix Report (Aggressive)\n\n**Status**: {COMPLETE | PARTIAL}\n**Pushed**: ✅ Changes pushed to `{HEAD_BRANCH}`\n**Philosophy**: Fix everything unless clearly a new concern\n\n---\n\n### Fixes Applied ({n} total)\n\n| Severity | Count |\n|----------|-------|\n| 🔴 CRITICAL | {n} |\n| 🟠 HIGH | {n} |\n| 🟡 MEDIUM | {n} |\n| 🟢 LOW | {n} |\n\n<details>\n<summary>View all fixes</summary>\n\n{For each fix:}\n- ✅ **{title}** (`{file}:{line}`) — {brief description}\n\n</details>\n\n---\n\n### Tests Added\n\n{List or \"(none)\"}\n\n---\n\n### Skipped ({n})\n\n{If any:}\n| Finding | Reason |\n|---------|--------|\n| {title} | New concern: {reason} |\n\n*(none — all findings addressed)*\n\n---\n\n### Suggested Follow-up Issues\n\n{If any skipped/blocked items warrant issues:}\n1. **{Issue Title}** — {brief description}\n\n*(none)*\n\n---\n\n### Validation\n\n✅ Type check | ✅ Lint | ✅ Tests ({n} passed)\n\n---\n\n*Self-fix by Archon · aggressive mode · fixes pushed to `{HEAD_BRANCH}`*\nEOF\n)\"\n```\n\n**PHASE_7_CHECKPOINT:**\n\n- [ ] GitHub comment posted\n\n---\n\n## Phase 8: OUTPUT — Final Summary\n\n```\n## ⚡ Self-Fix Complete\n\n**PR**: #{number}\n**Branch**: {HEAD_BRANCH}\n**Status**: COMPLETE | PARTIAL\n\nFixed: {n} (across all severities)\nTests added: {n}\nDocs updated: {n}\nSkipped: {n} (new concerns only)\nBlocked: {n}\n\nValidation: ✅ All checks pass\nPushed: ✅\n\nFix report: $ARTIFACTS_DIR/review/fix-report.md\n```\n\n---\n\n## Success Criteria\n\n- **ON_CORRECT_BRANCH**: Working on PR's head branch\n- **ALL_FINDINGS_ADDRESSED**: Every finding is fixed, skipped (with reason), or blocked (with reason)\n- **AGGRESSIVE_FIXING**: Most findings fixed — skip rate should be very low\n- **TESTS_ADDED**: Missing test coverage addressed\n- **DOCS_UPDATED**: Documentation gaps filled\n- **VALIDATION_PASSED**: Type check, lint, and tests all pass\n- **COMMITTED_AND_PUSHED**: Changes committed and pushed to PR branch\n- **REPORTED**: Fix report artifact written and GitHub comment posted\n",
  "archon-simplify-changes": "---\ndescription: Simplify code changed in this PR — implements fixes directly, commits, and pushes\nargument-hint: (none - operates on the current branch diff against $BASE_BRANCH)\n---\n\n# Simplify Changed Code\n\n---\n\n## IMPORTANT: Output Behavior\n\n**Your output will be posted as a GitHub comment.** Keep working output minimal:\n- Do NOT narrate each step\n- Do NOT output verbose progress updates\n- Only output the final structured report at the end\n\n---\n\n## Your Mission\n\nReview ALL code changed on this branch and implement simplifications directly. You are not advisory — you edit files, validate, commit, and push.\n\n## Scope\n\n**Only code changed in this PR** — run `git diff $BASE_BRANCH...HEAD --name-only` to get the file list. Do not touch unrelated files.\n\n## What to Simplify\n\n| Opportunity | What to Look For |\n|-------------|------------------|\n| **Unnecessary complexity** | Deep nesting, convoluted logic paths |\n| **Redundant code** | Duplicated logic, unused variables/imports |\n| **Over-abstraction** | Abstractions that obscure rather than clarify |\n| **Poor naming** | Unclear variable/function names |\n| **Nested ternaries** | Multiple conditions in ternary chains — use if/else |\n| **Dense one-liners** | Compact code that sacrifices readability |\n| **Obvious comments** | Comments that describe what code clearly shows |\n| **Inconsistent patterns** | Code that doesn't follow project conventions (read CLAUDE.md) |\n\n## Rules\n\n- **Preserve exact functionality** — simplification must not change behavior\n- **Clarity over brevity** — readable beats compact\n- **No speculative refactors** — only simplify what's obviously improvable\n- **Follow project conventions** — read CLAUDE.md before making changes\n- **Small, obvious changes** — each simplification should be self-evidently correct\n\n## Process\n\n### Phase 1: ANALYZE\n\n1. Read CLAUDE.md for project conventions\n2. Get changed files: `git diff $BASE_BRANCH...HEAD --name-only`\n3. Read each changed file\n4. Identify simplification opportunities per file\n\n### Phase 2: IMPLEMENT\n\nFor each simplification:\n1. Edit the file\n2. Run `bun run type-check` — if it fails, revert that change\n3. Run `bun run lint` — if it fails, fix or revert\n\n**Track every path you edit.** You will need this list in Phase 3 to stage only the files you touched.\n\n### Phase 3: VALIDATE & COMMIT\n\n1. Run full validation: `bun run type-check && bun run lint`\n2. If simplifications were applied, stage **only** the files you edited in Phase 2 — never `git add -A`, `git add .`, or `git add -u`:\n   ```bash\n   # Stage by name, using the list you tracked in Phase 2\n   git add path/to/file1.ts path/to/file2.ts\n   # Verify nothing else snuck in\n   git status --porcelain\n   ```\n3. **Never stage** report, scratch, or PR-body artifacts, even if they show up as untracked or modified in the worktree:\n   - Anything under `$ARTIFACTS_DIR` (the artifacts directory normally lives outside the worktree, but copies/symlinks may exist)\n   - `review/`, `simplify-report.md`, `*-report.md` at the repo root\n   - `.pr-body.md`, `pr-body.md`, `*.scratch.md`, `*.tmp.md`\n   - If `git status --porcelain` shows files you don't recognize as part of your simplifications, leave them unstaged\n4. Commit and push only the staged source edits:\n   ```bash\n   git commit -m \"simplify: reduce complexity in changed files\"\n   git push\n   ```\n5. If no simplifications were applied, skip the commit entirely\n\n### Phase 4: REPORT\n\nWrite report to `$ARTIFACTS_DIR/review/simplify-report.md` and output:\n\n```markdown\n## Code Simplification Report\n\n### Changes Made\n\n#### 1. [Brief Title]\n**File**: `path/to/file.ts:45-60`\n**Type**: Reduced nesting / Improved naming / Removed redundancy / etc.\n**Before**: [snippet]\n**After**: [snippet]\n\n---\n\n### Summary\n\n| Metric | Value |\n|--------|-------|\n| Files analyzed | X |\n| Simplifications applied | Y |\n| Net line change | -N lines |\n| Validation | PASS / FAIL |\n\n### No Changes Needed\n(If nothing to simplify, say so — \"Code is already clean. No simplifications applied.\")\n```\n",
  "archon-sync-pr-with-main": "---\ndescription: Sync PR branch with latest main (rebase if needed, resolve conflicts if any)\nargument-hint: (none - uses PR from scope)\n---\n\n# Sync PR with Main\n\n---\n\n## Your Mission\n\nEnsure the PR branch is up-to-date with the latest main branch before review. Rebase if needed, resolve conflicts if any arise. This step is silent when no action is needed.\n\n**Output artifact**: `$ARTIFACTS_DIR/review/sync-report.md` (only if rebase/conflicts occurred)\n\n---\n\n## Phase 1: CHECK - Determine if Sync Needed\n\n### 1.1 Get PR Number from Registry\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number)\n```\n\n### 1.2 Read Scope\n\n```bash\ncat $ARTIFACTS_DIR/review/scope.md\n```\n\nGet branch names: `PR_HEAD` and `PR_BASE`.\n\n### 1.3 Fetch and Checkout PR Branch\n\n```bash\ngit fetch origin $PR_BASE\ngit fetch origin $PR_HEAD\n```\n\nConfirm you are on the PR's branch (`$PR_HEAD`). If not, checkout it:\n\n```bash\ngit checkout $PR_HEAD\n```\n\n### 1.4 Check if Behind\n\n```bash\n# Count commits PR branch is behind main\nBEHIND=$(git rev-list --count HEAD..origin/$PR_BASE)\necho \"Behind by: $BEHIND commits\"\n```\n\n**Decision:**\n\n| Behind Count | Action |\n|--------------|--------|\n| 0 | Skip - already up to date |\n| 1+ | Rebase needed |\n\n**If already up to date:**\n```markdown\nBranch is up to date with `{base}`. No sync needed.\n```\n**Exit early - no artifact created.**\n\n**PHASE_1_CHECKPOINT:**\n- [ ] PR number identified\n- [ ] Branches fetched\n- [ ] Behind count determined\n\n---\n\n## Phase 2: REBASE - Sync with Main\n\n### 2.1 Attempt Rebase\n\n```bash\ngit rebase origin/$PR_BASE\n```\n\n**Possible outcomes:**\n\n| Result | Next Step |\n|--------|-----------|\n| Success (no conflicts) | Go to Phase 4 (Validate) |\n| Conflicts | Go to Phase 3 (Resolve) |\n| Other error | Report and abort |\n\n### 2.2 Check for Conflicts\n\n```bash\n# If rebase stopped, check for conflicts\ngit diff --name-only --diff-filter=U\n```\n\nIf files listed → conflicts exist, go to Phase 3.\nIf empty → rebase successful, go to Phase 4.\n\n**PHASE_2_CHECKPOINT:**\n- [ ] Rebase attempted\n- [ ] Conflict status determined\n\n---\n\n## Phase 3: RESOLVE - Handle Conflicts (If Any)\n\n### 3.1 Identify Conflicting Files\n\n```bash\ngit diff --name-only --diff-filter=U\n```\n\n### 3.2 Analyze Each Conflict\n\nFor each conflicting file:\n\n```bash\n# Show conflict markers\ncat {file} | grep -A 10 -B 2 \"<<<<<<<\"\n```\n\n**Categorize:**\n- **SIMPLE**: One side added/changed, other didn't touch → Auto-resolve\n- **COMPLEX**: Both sides changed same lines → Need decision\n\n### 3.3 Auto-Resolve Simple Conflicts\n\nFor conflicts where intent is clear:\n- Both added different things → Keep both\n- One updated, other didn't → Keep update\n- Import additions → Merge both\n\n```bash\n# Edit file to resolve\n# Then stage\ngit add {file}\n```\n\n### 3.4 Resolve Complex Conflicts\n\nFor conflicts needing decision:\n\n1. Read both versions to understand intent\n2. Choose resolution based on:\n   - PR intent (what was the change trying to do?)\n   - Base branch updates (what changed in main?)\n   - Code correctness\n3. Apply resolution and stage\n\n```bash\ngit add {file}\n```\n\n### 3.5 Continue Rebase\n\n```bash\ngit rebase --continue\n```\n\nRepeat if more commits have conflicts.\n\n**PHASE_3_CHECKPOINT:**\n- [ ] All conflicts identified\n- [ ] Simple conflicts auto-resolved\n- [ ] Complex conflicts resolved with reasoning\n- [ ] Rebase completed\n\n---\n\n## Phase 4: VALIDATE - Verify Sync\n\n### 4.1 Check No Conflicts Remaining\n\n```bash\ngit diff --check\n```\n\nShould return empty.\n\n### 4.2 Type Check\n\n```bash\nbun run type-check\n```\n\n### 4.3 Run Tests\n\n```bash\nbun test\n```\n\n### 4.4 Lint\n\n```bash\nbun run lint\n```\n\n**If any fail**: Fix issues before proceeding.\n\n**PHASE_4_CHECKPOINT:**\n- [ ] No conflict markers\n- [ ] Type check passes\n- [ ] Tests pass\n- [ ] Lint passes\n\n---\n\n## Phase 5: PUSH - Update Remote\n\n### 5.1 Confirm Branch and Push\n\nConfirm you're on `$PR_HEAD`, then push:\n\n```bash\ngit push --force-with-lease origin $PR_HEAD\n```\n\n**Note**: `--force-with-lease` is safer - fails if someone else pushed.\n\n### 5.2 Verify Push\n\n```bash\ngit log origin/$PR_HEAD --oneline -3\n```\n\nConfirm local and remote match.\n\n**PHASE_5_CHECKPOINT:**\n- [ ] Branch pushed\n- [ ] Remote updated\n\n---\n\n## Phase 6: REPORT - Document Sync (Only if Rebase/Conflicts Occurred)\n\n### 6.1 Create Sync Artifact\n\nWrite to `$ARTIFACTS_DIR/review/sync-report.md`:\n\n```markdown\n# Sync Report: PR #{number}\n\n**Date**: {ISO timestamp}\n**Action**: Rebased onto `{base}`\n\n---\n\n## Summary\n\n- **Commits rebased**: {N}\n- **Conflicts resolved**: {M} (in {X} files)\n- **Status**: ✅ Synced successfully\n\n---\n\n## Conflicts Resolved\n\n{If conflicts were resolved:}\n\n### `{file}`\n\n**Type**: {SIMPLE | COMPLEX}\n**Resolution**: {description}\n\n```{language}\n{resolved code}\n```\n\n---\n\n{If no conflicts:}\n\nNo conflicts encountered during rebase.\n\n---\n\n## Validation\n\n| Check | Status |\n|-------|--------|\n| Type check | ✅ |\n| Tests | ✅ |\n| Lint | ✅ |\n\n---\n\n## Git State\n\n**Before**: {old HEAD commit}\n**After**: {new HEAD commit}\n**Commits ahead of {base}**: {count}\n\n---\n\n## Metadata\n\n- **Synced by**: Archon\n- **Timestamp**: {ISO timestamp}\n```\n\n### 6.2 Update Scope Artifact\n\nAppend to `$ARTIFACTS_DIR/review/scope.md`:\n\n```markdown\n---\n\n## Sync Status\n\n**Synced**: {ISO timestamp}\n**Rebased onto**: `{base}` at {commit}\n**Conflicts resolved**: {N}\n```\n\n**PHASE_6_CHECKPOINT:**\n- [ ] Sync artifact created (if action taken)\n- [ ] Scope artifact updated\n\n---\n\n## Phase 7: OUTPUT - Report Status\n\n### If Rebased (with or without conflicts):\n\n```markdown\n## ✅ PR Synced with Main\n\n**Branch**: `{head}` rebased onto `{base}`\n**Commits rebased**: {N}\n**Conflicts resolved**: {M}\n\nValidation: ✅ Type check | ✅ Tests | ✅ Lint\n\nProceeding to parallel review...\n```\n\n### If Already Up to Date:\n\n```markdown\n## ✅ PR Already Up to Date\n\nBranch `{head}` is current with `{base}`. No sync needed.\n\nProceeding to parallel review...\n```\n\n### If Sync Failed:\n\n```markdown\n## ❌ Sync Failed\n\n**Error**: {description}\n\n**Action Required**: Manual intervention needed.\n\n```bash\n# To abort the failed rebase\ngit rebase --abort\n```\n\n**Recommendation**: Resolve conflicts manually, then re-trigger review.\n```\n\n---\n\n## Error Handling\n\n### Rebase Fails Completely\n\n```bash\ngit rebase --abort\n```\n\nReport failure with specific error.\n\n### Push Rejected\n\nIf `--force-with-lease` fails:\n1. Someone else pushed to the branch\n2. Fetch and re-attempt rebase\n3. Or report for manual handling\n\n### Validation Fails\n\nIf type-check/tests fail after rebase:\n1. Investigate which changes broke\n2. Attempt to fix\n3. If unfixable, abort and report\n\n---\n\n## Success Criteria\n\n- **UP_TO_DATE**: Branch is synced with base (or was already)\n- **NO_CONFLICTS**: All conflicts resolved (if any existed)\n- **VALIDATION_PASSED**: Type check, tests, lint all pass\n- **PUSHED**: Remote branch updated (if rebase occurred)\n",
  "archon-synthesize-review": "---\ndescription: Synthesize all review agent findings into consolidated report and post to GitHub\nargument-hint: (none - reads from review artifacts)\n---\n\n# Synthesize Review\n\n---\n\n## Your Mission\n\nRead all parallel review agent artifacts, synthesize findings into a consolidated report, create a master artifact, and post a comprehensive review comment to the GitHub PR.\n\n**Output artifact**: `$ARTIFACTS_DIR/review/consolidated-review.md`\n**GitHub action**: Post PR comment with full review\n\n---\n\n## Phase 1: LOAD - Gather All Findings\n\n### 1.1 Get PR Number from Registry\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number)\n```\n\n### 1.2 Read Scope\n\n```bash\ncat $ARTIFACTS_DIR/review/scope.md\n```\n\n### 1.3 Read All Agent Artifacts\n\n```bash\n# Read each agent's findings\ncat $ARTIFACTS_DIR/review/code-review-findings.md\ncat $ARTIFACTS_DIR/review/error-handling-findings.md\ncat $ARTIFACTS_DIR/review/test-coverage-findings.md\ncat $ARTIFACTS_DIR/review/comment-quality-findings.md\ncat $ARTIFACTS_DIR/review/docs-impact-findings.md\n```\n\n**PHASE_1_CHECKPOINT:**\n- [ ] PR number identified\n- [ ] All 5 agent artifacts read\n- [ ] Findings extracted from each\n\n---\n\n## Phase 2: SYNTHESIZE - Combine Findings\n\n### 2.1 Aggregate by Severity\n\nCombine all findings across agents:\n- **CRITICAL**: Must fix before merge\n- **HIGH**: Should fix before merge\n- **MEDIUM**: Consider fixing (options provided)\n- **LOW**: Nice to have (defer or create issue)\n\n### 2.2 Deduplicate\n\nCheck for overlapping findings:\n- Same issue reported by multiple agents\n- Related issues that should be grouped\n- Conflicting recommendations (resolve)\n\n### 2.3 Prioritize\n\nRank findings by:\n1. Severity (CRITICAL > HIGH > MEDIUM > LOW)\n2. User impact\n3. Ease of fix\n4. Risk if not fixed\n\n### 2.4 Compile Statistics\n\n```\nTotal findings: {n}\n- CRITICAL: {n}\n- HIGH: {n}\n- MEDIUM: {n}\n- LOW: {n}\n\nBy agent:\n- code-review: {n} findings\n- error-handling: {n} findings\n- test-coverage: {n} findings\n- comment-quality: {n} findings\n- docs-impact: {n} findings\n```\n\n**PHASE_2_CHECKPOINT:**\n- [ ] Findings aggregated by severity\n- [ ] Duplicates removed\n- [ ] Priority order established\n- [ ] Statistics compiled\n\n---\n\n## Phase 3: GENERATE - Create Consolidated Artifact\n\nWrite to `$ARTIFACTS_DIR/review/consolidated-review.md`:\n\n```markdown\n# Consolidated Review: PR #{number}\n\n**Date**: {ISO timestamp}\n**Agents**: code-review, error-handling, test-coverage, comment-quality, docs-impact\n**Total Findings**: {count}\n\n---\n\n## Executive Summary\n\n{3-5 sentence overview of PR quality and main concerns}\n\n**Overall Verdict**: {APPROVE | REQUEST_CHANGES | NEEDS_DISCUSSION}\n\n**Auto-fix Candidates**: {n} CRITICAL + HIGH issues can be auto-fixed\n**Manual Review Needed**: {n} MEDIUM + LOW issues require decision\n\n---\n\n## Statistics\n\n| Agent | CRITICAL | HIGH | MEDIUM | LOW | Total |\n|-------|----------|------|--------|-----|-------|\n| Code Review | {n} | {n} | {n} | {n} | {n} |\n| Error Handling | {n} | {n} | {n} | {n} | {n} |\n| Test Coverage | {n} | {n} | {n} | {n} | {n} |\n| Comment Quality | {n} | {n} | {n} | {n} | {n} |\n| Docs Impact | {n} | {n} | {n} | {n} | {n} |\n| **Total** | **{n}** | **{n}** | **{n}** | **{n}** | **{n}** |\n\n---\n\n## CRITICAL Issues (Must Fix)\n\n### Issue 1: {Title}\n\n**Source Agent**: {agent-name}\n**Location**: `{file}:{line}`\n**Category**: {category}\n\n**Problem**:\n{description}\n\n**Recommended Fix**:\n```typescript\n{fix code}\n```\n\n**Why Critical**:\n{impact explanation}\n\n---\n\n### Issue 2: {Title}\n\n{Same structure...}\n\n---\n\n## HIGH Issues (Should Fix)\n\n### Issue 1: {Title}\n\n{Same structure as CRITICAL...}\n\n---\n\n## MEDIUM Issues (Options for User)\n\n### Issue 1: {Title}\n\n**Source Agent**: {agent-name}\n**Location**: `{file}:{line}`\n\n**Problem**:\n{description}\n\n**Options**:\n\n| Option | Approach | Effort | Risk if Skipped |\n|--------|----------|--------|-----------------|\n| Fix Now | {approach} | {LOW/MED/HIGH} | {risk} |\n| Create Issue | Defer to separate PR | LOW | {risk} |\n| Skip | Accept as-is | NONE | {risk} |\n\n**Recommendation**: {which option and why}\n\n---\n\n## LOW Issues (For Consideration)\n\n| Issue | Location | Agent | Suggestion |\n|-------|----------|-------|------------|\n| {title} | `file:line` | {agent} | {brief recommendation} |\n| ... | ... | ... | ... |\n\n---\n\n## Positive Observations\n\n{Aggregated good things from all agents:\n- Well-structured code\n- Good error handling in X\n- Comprehensive tests for Y\n- Clear documentation}\n\n---\n\n## Suggested Follow-up Issues\n\nIf not addressing in this PR, create issues for:\n\n| Issue Title | Priority | Related Finding |\n|-------------|----------|-----------------|\n| \"{suggested issue title}\" | {P1/P2/P3} | MEDIUM issue #{n} |\n| ... | ... | ... |\n\n---\n\n## Next Steps\n\n1. **Auto-fix step** will address {n} CRITICAL + HIGH issues\n2. **Review** the MEDIUM issues and decide: fix now, create issue, or skip\n3. **Consider** LOW issues for future improvements\n\n---\n\n## Agent Artifacts\n\n| Agent | Artifact | Findings |\n|-------|----------|----------|\n| Code Review | `code-review-findings.md` | {n} |\n| Error Handling | `error-handling-findings.md` | {n} |\n| Test Coverage | `test-coverage-findings.md` | {n} |\n| Comment Quality | `comment-quality-findings.md` | {n} |\n| Docs Impact | `docs-impact-findings.md` | {n} |\n\n---\n\n## Metadata\n\n- **Synthesized**: {ISO timestamp}\n- **Artifact**: `$ARTIFACTS_DIR/review/consolidated-review.md`\n```\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Consolidated artifact created\n- [ ] All findings included\n- [ ] Severity ordering correct\n- [ ] Options provided for MEDIUM/LOW\n\n---\n\n## Phase 4: POST - GitHub PR Comment\n\n### 4.1 Format for GitHub\n\nCreate a GitHub-friendly version of the review:\n\n```bash\ngh pr comment {number} --body \"$(cat <<'EOF'\n# 🔍 Comprehensive PR Review\n\n**PR**: #{number}\n**Reviewed by**: 5 specialized agents\n**Date**: {date}\n\n---\n\n## Summary\n\n{executive summary}\n\n**Verdict**: `{APPROVE | REQUEST_CHANGES}`\n\n| Severity | Count |\n|----------|-------|\n| 🔴 CRITICAL | {n} |\n| 🟠 HIGH | {n} |\n| 🟡 MEDIUM | {n} |\n| 🟢 LOW | {n} |\n\n---\n\n## 🔴 Critical Issues (Auto-fixing)\n\n{For each CRITICAL issue:}\n\n### {Title}\n📍 `{file}:{line}`\n\n{Brief description}\n\n<details>\n<summary>View fix</summary>\n\n```typescript\n{fix code}\n```\n\n</details>\n\n---\n\n## 🟠 High Issues (Auto-fixing)\n\n{Same format as CRITICAL}\n\n---\n\n## 🟡 Medium Issues (Needs Decision)\n\n{For each MEDIUM issue:}\n\n### {Title}\n📍 `{file}:{line}`\n\n{Brief description}\n\n**Options**: Fix now | Create issue | Skip\n\n<details>\n<summary>View details</summary>\n\n{full details and options table}\n\n</details>\n\n---\n\n## 🟢 Low Issues\n\n<details>\n<summary>View {n} low-priority suggestions</summary>\n\n| Issue | Location | Suggestion |\n|-------|----------|------------|\n| {title} | `file:line` | {suggestion} |\n\n</details>\n\n---\n\n## ✅ What's Good\n\n{Positive observations}\n\n---\n\n## 📋 Suggested Follow-up Issues\n\n{If any MEDIUM/LOW issues should become issues}\n\n---\n\n## Next Steps\n\n1. ⚡ Auto-fix step will address CRITICAL + HIGH issues\n2. 📝 Review MEDIUM issues above\n3. 🎯 Merge when ready\n\n---\n\n*Reviewed by Archon comprehensive-pr-review workflow*\n*Artifacts: `$ARTIFACTS_DIR/review/`*\nEOF\n)\"\n```\n\n**PHASE_4_CHECKPOINT:**\n- [ ] GitHub comment posted\n- [ ] Formatting renders correctly\n- [ ] All severity levels included\n\n---\n\n## Phase 5: OUTPUT - Confirmation\n\nOutput only a brief confirmation (this will be posted as a comment):\n\n```\n✅ Review synthesis complete. Proceeding to auto-fix step...\n```\n\n---\n\n## Success Criteria\n\n- **ALL_ARTIFACTS_READ**: All 5 agent findings loaded\n- **FINDINGS_SYNTHESIZED**: Combined, deduplicated, prioritized\n- **CONSOLIDATED_CREATED**: Master artifact written\n- **GITHUB_POSTED**: PR comment visible\n",
  "archon-test-coverage-agent": "---\ndescription: Review test coverage quality, identify gaps, and evaluate test effectiveness\nargument-hint: (none - reads from scope artifact)\n---\n\n# Test Coverage Agent\n\n---\n\n## Your Mission\n\nAnalyze test coverage for the PR changes. Identify critical gaps, evaluate test quality, and ensure tests verify behavior (not implementation). Produce a structured artifact with findings and recommendations.\n\n**Output artifact**: `$ARTIFACTS_DIR/review/test-coverage-findings.md`\n\n---\n\n## Phase 1: LOAD - Get Context\n\n### 1.1 Get PR Number from Registry\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number)\n```\n\n### 1.2 Read Scope\n\n```bash\ncat $ARTIFACTS_DIR/review/scope.md\n```\n\nNote which files are source vs test files.\n\n**CRITICAL**: Check for \"NOT Building (Scope Limits)\" section. Items listed there are **intentionally excluded** - do NOT flag them as bugs or missing test coverage!\n\n### 1.3 Get PR Diff\n\n```bash\ngh pr diff {number}\n```\n\n### 1.4 Read Existing Tests\n\nFor each new/modified source file, find corresponding test file:\n\n```bash\n# Find test files\nfind src -name \"*.test.ts\" -o -name \"*.spec.ts\" | head -20\n```\n\n**PHASE_1_CHECKPOINT:**\n- [ ] PR number identified\n- [ ] Source and test files identified\n- [ ] Existing test patterns noted\n\n---\n\n## Phase 2: ANALYZE - Evaluate Coverage\n\n### 2.1 Map Source to Tests\n\nFor each changed source file:\n- Does a corresponding test file exist?\n- Are new functions/features tested?\n- Are modified functions' tests updated?\n\n### 2.2 Identify Critical Gaps\n\nLook for untested:\n- Error handling paths\n- Edge cases (null, empty, boundary values)\n- Critical business logic\n- Security-sensitive code\n- Async/concurrent behavior\n- Integration points\n\n### 2.3 Evaluate Test Quality\n\nFor existing tests, check:\n- Do they test behavior or implementation?\n- Would they catch meaningful regressions?\n- Are they resilient to refactoring?\n- Do they follow DAMP principles?\n- Are assertions meaningful?\n\n### 2.4 Find Test Patterns\n\n```bash\n# Find test patterns in codebase\ngrep -r \"describe\\|it\\|test\\(\" src/ --include=\"*.test.ts\" | head -20\n```\n\n**PHASE_2_CHECKPOINT:**\n- [ ] Source-to-test mapping complete\n- [ ] Critical gaps identified\n- [ ] Test quality evaluated\n- [ ] Codebase test patterns found\n\n---\n\n## Phase 3: GENERATE - Create Artifact\n\nWrite to `$ARTIFACTS_DIR/review/test-coverage-findings.md`:\n\n```markdown\n# Test Coverage Findings: PR #{number}\n\n**Reviewer**: test-coverage-agent\n**Date**: {ISO timestamp}\n**Source Files**: {count}\n**Test Files**: {count}\n\n---\n\n## Summary\n\n{2-3 sentence overview of test coverage quality}\n\n**Verdict**: {APPROVE | REQUEST_CHANGES | NEEDS_DISCUSSION}\n\n---\n\n## Coverage Map\n\n| Source File | Test File | New Code Tested | Modified Code Tested |\n|-------------|-----------|-----------------|---------------------|\n| `src/x.ts` | `src/x.test.ts` | FULL/PARTIAL/NONE | FULL/PARTIAL/NONE |\n| `src/y.ts` | (missing) | N/A | N/A |\n| ... | ... | ... | ... |\n\n---\n\n## Findings\n\n### Finding 1: {Descriptive Title}\n\n**Severity**: CRITICAL | HIGH | MEDIUM | LOW\n**Category**: missing-test | weak-test | implementation-coupled | missing-edge-case\n**Location**: `{file}:{line}` (source) / `{test-file}` (test)\n**Criticality Score**: {1-10}\n\n**Issue**:\n{Clear description of the coverage gap}\n\n**Untested Code**:\n```typescript\n// This code at {file}:{line} is not tested\n{untested code}\n```\n\n**Why This Matters**:\n{Specific bugs or regressions this could miss:\n- \"If {scenario}, users would see {bad outcome}\"\n- \"A future change to {X} could break {Y} without detection\"}\n\n---\n\n#### Test Suggestions\n\n| Option | Approach | Catches | Effort |\n|--------|----------|---------|--------|\n| A | {test approach} | {what it catches} | LOW/MED/HIGH |\n| B | {alternative} | {what it catches} | LOW/MED/HIGH |\n\n**Recommended**: Option {X}\n\n**Reasoning**:\n{Why this test approach:\n- Matches codebase test patterns\n- Tests behavior not implementation\n- Good cost/benefit ratio\n- Catches the most critical failures}\n\n**Recommended Test**:\n```typescript\ndescribe('{feature}', () => {\n  it('should {expected behavior}', () => {\n    // Arrange\n    {setup}\n\n    // Act\n    {action}\n\n    // Assert\n    {assertions}\n  });\n\n  it('should handle {edge case}', () => {\n    // Test edge case\n  });\n});\n```\n\n**Test Pattern Reference**:\n```typescript\n// SOURCE: {test-file}:{lines}\n// This is how similar functionality is tested\n{existing test from codebase}\n```\n\n---\n\n### Finding 2: {Title}\n\n{Same structure...}\n\n---\n\n## Test Quality Audit\n\n| Test | Tests Behavior | Resilient | Meaningful Assertions | Verdict |\n|------|---------------|-----------|----------------------|---------|\n| `it('should...')` | YES/NO | YES/NO | YES/NO | GOOD/NEEDS_WORK |\n| ... | ... | ... | ... | ... |\n\n---\n\n## Statistics\n\n| Severity | Count | Criticality 8-10 | Criticality 5-7 | Criticality 1-4 |\n|----------|-------|------------------|-----------------|-----------------|\n| CRITICAL | {n} | {n} | - | - |\n| HIGH | {n} | {n} | {n} | - |\n| MEDIUM | {n} | - | {n} | {n} |\n| LOW | {n} | - | - | {n} |\n\n---\n\n## Risk Assessment\n\n| Untested Area | Failure Mode | User Impact | Priority |\n|---------------|--------------|-------------|----------|\n| {code area} | {how it could fail} | {user sees} | CRITICAL/HIGH/MED |\n| ... | ... | ... | ... |\n\n---\n\n## Patterns Referenced\n\n| Test File | Lines | Pattern |\n|-----------|-------|---------|\n| `src/x.test.ts` | 10-30 | {testing pattern description} |\n| ... | ... | ... |\n\n---\n\n## Positive Observations\n\n{Good test coverage, well-written tests, proper mocking}\n\n---\n\n## Metadata\n\n- **Agent**: test-coverage-agent\n- **Timestamp**: {ISO timestamp}\n- **Artifact**: `$ARTIFACTS_DIR/review/test-coverage-findings.md`\n```\n\n**PHASE_3_CHECKPOINT:**\n- [ ] Artifact file created\n- [ ] Coverage map complete\n- [ ] Each gap has criticality score\n- [ ] Test suggestions with example code\n\n---\n\n## Success Criteria\n\n- **COVERAGE_MAPPED**: Each source file mapped to tests\n- **GAPS_IDENTIFIED**: Missing tests found with criticality scores\n- **QUALITY_EVALUATED**: Existing tests assessed\n- **TESTS_SUGGESTED**: Example test code provided for gaps\n",
  "archon-validate-pr-code-review-feature": "---\ndescription: Analyze code on the feature branch to verify the PR's fix is correct and optimal\nargument-hint: (none - reads from artifacts)\n---\n\n# Code Review: Feature Branch (Post-PR State)\n\nAnalyze the code changes in the PR to verify the fix is correct, complete, and implemented in the best way possible.\n\n---\n\n## Phase 1: Load Context\n\n### 1.1 Read PR Details and Main Branch Analysis\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n')\ngh pr view \"$PR_NUMBER\" --json title,body,headRefName,baseRefName,labels\n```\n\n```bash\n# Read the main branch analysis (guaranteed available — this node depends on code-review-main)\ncat $ARTIFACTS_DIR/code-review-main.md\n```\n\n### 1.2 Read Path Information\n\n```bash\ncat $ARTIFACTS_DIR/.worktree-path\ncat $ARTIFACTS_DIR/.feature-branch\n```\n\n---\n\n## Phase 2: Analyze the Diff\n\n### 2.1 Get the Full Diff\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n')\ngh pr diff \"$PR_NUMBER\"\n```\n\n### 2.2 Read Changed Files on Feature Branch\n\nThe current working directory IS the feature branch (worktree). Read each changed file:\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n')\n# List changed files\ngh pr view \"$PR_NUMBER\" --json files -q '.files[].path'\n```\n\nFor each file, read the full file in the current working directory to understand the complete context, not just the diff hunks.\n\n### 2.3 Deep Analysis of Each Change\n\nFor each changed file:\n\n1. **Read the full file** — understand the complete context around the changes\n2. **Compare with main** — read the same file from `$ARTIFACTS_DIR/.canonical-repo` to see the before/after\n3. **Evaluate the fix**:\n   - Does it actually address the bug/gap found on main?\n   - Is it the simplest possible fix? (KISS)\n   - Does it handle edge cases?\n   - Could it introduce new bugs?\n   - Does it follow existing patterns in the codebase?\n4. **Check CLAUDE.md compliance**:\n   ```bash\n   cat CLAUDE.md\n   ```\n   - Import patterns correct?\n   - Type annotations complete?\n   - Error handling appropriate?\n   - No unnecessary complexity?\n\n### 2.4 Look for Issues\n\nCheck for:\n- **Correctness**: Does the fix actually solve the problem?\n- **Completeness**: Are all aspects of the bug addressed?\n- **Side effects**: Could this break something else?\n- **Performance**: Any unnecessary re-renders, expensive operations?\n- **Type safety**: All types correct, no `any` without justification?\n- **Error handling**: Errors caught and handled appropriately?\n- **Overengineering**: More changes than necessary? (YAGNI)\n- **Missing changes**: Files that SHOULD have been changed but weren't?\n\n### 2.5 Compare Alternative Approaches\n\nThink about whether there's a better way to fix this:\n- Could a simpler approach work?\n- Is there an existing utility/pattern that should be used?\n- Would the fix work differently if applied at a different layer?\n\n---\n\n## Phase 3: Write Findings\n\nWrite your analysis to `$ARTIFACTS_DIR/code-review-feature.md`:\n\n```markdown\n# Feature Branch Code Review: PR #{number}\n\n**PR Title**: {title}\n**Feature Branch**: {branch}\n**Files Changed**: {count}\n**Lines**: +{additions} -{deletions}\n\n## Fix Assessment\n\n### Does the Fix Address the Bug?\n**YES / PARTIALLY / NO**\n\n{Explanation with specific code references}\n\n### Fix Quality\n\n| Criterion | Rating (1-5) | Notes |\n|-----------|-------------|-------|\n| Correctness | {n} | {does it fix the bug?} |\n| Completeness | {n} | {all edge cases handled?} |\n| Simplicity | {n} | {minimal changes, KISS?} |\n| Safety | {n} | {no side effects?} |\n| Patterns | {n} | {follows codebase conventions?} |\n\n**Overall Score**: {average}/5\n\n### File-by-File Analysis\n\n#### `{file1}`\n**Change Summary**: {what changed}\n**Assessment**: {good/needs-work/concern}\n```{language}\n// Key change\n{relevant code snippet}\n```\n**Notes**: {specific feedback}\n\n#### `{file2}`\n{Same structure...}\n\n### Issues Found\n\n#### Issue 1: {title}\n**Severity**: CRITICAL / HIGH / MEDIUM / LOW\n**File**: `{file}:{line}`\n**Description**: {what's wrong}\n**Suggested Fix**:\n```{language}\n{how to fix it}\n```\n\n### Alternative Approaches Considered\n{Were there better ways to implement this? If so, describe them and why they might be preferable.\nIf the current approach is optimal, say so and explain why.}\n\n### Missing Changes\n{Files or areas that should have been changed but weren't. If everything is covered, say so.}\n\n## CLAUDE.md Compliance\n\n| Rule | Status | Notes |\n|------|--------|-------|\n| Type annotations | PASS/FAIL | {details} |\n| Import patterns | PASS/FAIL | {details} |\n| Error handling | PASS/FAIL | {details} |\n| No any types | PASS/FAIL | {details} |\n| KISS principle | PASS/FAIL | {details} |\n\n## Verdict\n\n**APPROVE / REQUEST_CHANGES / NEEDS_DISCUSSION**\n\n{2-3 sentence final assessment: Is this fix ready to merge as-is?}\n```\n\n---\n\n## Success Criteria\n\n- **DIFF_ANALYZED**: Full PR diff reviewed\n- **FILES_READ**: All changed files read in full context\n- **MAIN_COMPARED**: Feature code compared against main branch code\n- **CLAUDE_MD_CHECKED**: CLAUDE.md compliance verified\n- **ARTIFACT_WRITTEN**: `$ARTIFACTS_DIR/code-review-feature.md` created\n",
  "archon-validate-pr-code-review-main": "---\ndescription: Analyze code on the main/base branch to confirm the bug or gap exists before the PR's changes\nargument-hint: (none - reads from artifacts)\n---\n\n# Code Review: Main Branch (Pre-PR State)\n\nAnalyze the codebase on the **main branch** to confirm that the bug, gap, or missing feature described in the PR actually exists.\n\n---\n\n## Phase 1: Load Context\n\n### 1.1 Read PR Details\n\n```bash\ncat $ARTIFACTS_DIR/.pr-number\n```\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n')\ngh pr view \"$PR_NUMBER\" --json title,body,headRefName,baseRefName,labels\n```\n\n### 1.2 Read Path Information\n\n```bash\ncat $ARTIFACTS_DIR/.canonical-repo\ncat $ARTIFACTS_DIR/.worktree-path\ncat $ARTIFACTS_DIR/.pr-base\n```\n\n### 1.3 Understand What the PR Claims to Fix\n\nFrom the PR title, body, and linked issue(s):\n- What bug or gap does the PR claim exists?\n- What is the expected behavior vs actual behavior?\n- Which files/components are involved?\n\nIf the PR body references a GitHub issue, fetch it:\n\n```bash\n# Extract issue number from PR body (looks for \"Fixes #N\", \"Closes #N\", etc.)\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n')\nISSUE_NUMBER=$(gh pr view \"$PR_NUMBER\" --json body -q '.body' | grep -oE '(Fixes|Closes|Resolves)\\s*#[0-9]+' | grep -oE '[0-9]+' | head -1)\nif [ -n \"$ISSUE_NUMBER\" ]; then\n  gh issue view \"$ISSUE_NUMBER\" --json title,body,labels,comments\nfi\n```\n\n---\n\n## Phase 2: Analyze Main Branch Code\n\n### 2.1 Read the Files That the PR Changes\n\nGet the list of changed files from the PR diff, then read those **same files on the main branch** (the canonical repo path).\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n')\ngh pr view \"$PR_NUMBER\" --json files -q '.files[].path'\n```\n\n**CRITICAL**: Read the files from the **canonical repo** (main branch), NOT from the current worktree (feature branch). The canonical repo path is in `$ARTIFACTS_DIR/.canonical-repo`.\n\nFor each changed file, read it from the main branch:\n\n```bash\nCANONICAL_REPO=$(cat $ARTIFACTS_DIR/.canonical-repo | tr -d '\\n')\n# Read each file from the canonical repo (on main branch)\ncat \"$CANONICAL_REPO/<file-path>\"\n```\n\n### 2.2 Trace the Bug or Gap\n\nFor each claim in the PR:\n1. **Find the relevant code** on main — read the specific functions, components, hooks\n2. **Trace the data flow** — where does the data come from? How does it transform?\n3. **Identify the root cause** — can you see the bug in the code?\n4. **Check related code** — are there adjacent issues the PR might miss?\n\n### 2.3 Assess Severity\n\n- How impactful is this bug/gap on main?\n- Is it user-facing or internal?\n- Does it affect core functionality or edge cases?\n- How likely is a user to encounter it?\n\n---\n\n## Phase 3: Write Findings\n\nWrite your analysis to `$ARTIFACTS_DIR/code-review-main.md`:\n\n```markdown\n# Main Branch Code Review: PR #{number}\n\n**PR Title**: {title}\n**Base Branch**: {base}\n**Analyzed Commit**: {main branch HEAD}\n\n## Bug/Gap Assessment\n\n### Claimed Issue\n{What the PR claims to fix}\n\n### Confirmed on Main?\n**YES / NO / PARTIAL**\n\n### Evidence\n\n{For each claim, provide specific code evidence:}\n\n#### Claim 1: {description}\n**Status**: Confirmed / Not Found / Partially Confirmed\n\n**Code Location**: `{file}:{lines}`\n```{language}\n{actual code on main showing the bug/gap}\n```\n\n**Analysis**: {Why this code is buggy/incomplete}\n\n#### Claim 2: {description}\n{Same structure...}\n\n### Related Issues Found\n{Any additional problems discovered in the same code areas}\n\n### Severity Assessment\n| Factor | Rating |\n|--------|--------|\n| User Impact | High / Medium / Low |\n| Frequency | Common / Uncommon / Rare |\n| Core Feature | Yes / No |\n| Data Loss Risk | Yes / No |\n\n## Summary\n{2-3 sentence summary: Is the bug real? How bad is it? Is the PR's scope appropriate?}\n```\n\n---\n\n## Success Criteria\n\n- **PR_CONTEXT_LOADED**: PR details and linked issue read\n- **MAIN_CODE_ANALYZED**: Changed files read from main branch\n- **BUG_ASSESSED**: Each PR claim verified against main branch code\n- **ARTIFACT_WRITTEN**: `$ARTIFACTS_DIR/code-review-main.md` created\n",
  "archon-validate-pr-e2e-feature": "---\ndescription: Start Archon from the feature branch, use agent-browser to verify the fix works correctly\nargument-hint: (none - reads from artifacts)\n---\n\n# E2E Testing: Feature Branch (Verify Fix)\n\nStart Archon from the **feature branch** (this worktree) and use browser automation to verify that the bug is fixed and the UI/UX is correct. Take screenshots as evidence.\n\n**CRITICAL**: You MUST use the `agent-browser` CLI for ALL browser interactions. Load the `/agent-browser` skill for the full command reference.\n\n**CRITICAL**: You MUST clean up ALL spawned processes before finishing. Record PIDs and kill them in Phase 4. Orphaned processes from previous E2E runs may still be running — check and kill them first.\n\n**CRITICAL — SESSION ISOLATION**: This workflow runs in parallel with other validate-pr instances.\nYou MUST use `--session $WORKFLOW_ID` on EVERY `agent-browser` command to isolate your browser session.\nExample: `agent-browser --session $WORKFLOW_ID open \"http://...\"`, `agent-browser --session $WORKFLOW_ID snapshot -i`, etc.\n\n**ABSOLUTELY FORBIDDEN — NEVER DO ANY OF THESE**:\n- `taskkill //F //IM chrome.exe` or ANY variant that kills chrome by image name — this kills the USER's browser\n- `taskkill //F //IM node.exe` or `taskkill //F //IM bun.exe` — this kills Claude Code, the Archon server, and all other workflows\n- `pkill chrome`, `pkill node`, `pkill bun`, or any broad process-name kill\n- `agent-browser close` without `--session $WORKFLOW_ID` — this kills OTHER workflows' browser sessions\n- Any \"kill everything\" or \"kill all\" escalation pattern — if agent-browser isn't working, SKIP E2E testing and note it in your report\n- If agent-browser fails to connect after 2 attempts, STOP trying and write your findings based on code review only\n\n---\n\n## Phase 0: Kill Orphaned Processes from Previous E2E Run\n\nBefore starting, clean up any leftover processes from the main branch E2E test:\n\n```bash\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\nFRONTEND_PORT=$(cat $ARTIFACTS_DIR/.frontend-port | tr -d '\\n')\n\n# Kill by PID files from main E2E run\nfor pidfile in \"$ARTIFACTS_DIR/.e2e-main-backend-pid\" \"$ARTIFACTS_DIR/.e2e-main-frontend-pid\"; do\n  if [ -f \"$pidfile\" ]; then\n    PID=$(cat \"$pidfile\" | tr -d '\\n')\n    echo \"Killing leftover main E2E PID $PID\"\n    kill \"$PID\" 2>/dev/null || taskkill //F //T //PID \"$PID\" 2>/dev/null || true\n  fi\ndone\n\n# Kill anything still on our ports\nfor PORT in $BACKEND_PORT $FRONTEND_PORT; do\n  fuser -k \"$PORT/tcp\" 2>/dev/null || true\n  lsof -ti:\"$PORT\" 2>/dev/null | xargs kill -9 2>/dev/null || true\n  netstat -ano 2>/dev/null | grep \":$PORT \" | grep LISTENING | awk '{print $5}' | sort -u | while read pid; do\n    taskkill //F //T //PID \"$pid\" 2>/dev/null || true\n  done\ndone\nsleep 2\necho \"Orphan cleanup complete\"\n```\n\n---\n\n## Phase 1: Load Context\n\n### 1.1 Read Artifacts\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n')\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\nFRONTEND_PORT=$(cat $ARTIFACTS_DIR/.frontend-port | tr -d '\\n')\nWORKTREE_PATH=$(cat $ARTIFACTS_DIR/.worktree-path | tr -d '\\n')\necho \"PR: #$PR_NUMBER\"\necho \"Backend port: $BACKEND_PORT\"\necho \"Frontend port: $FRONTEND_PORT\"\necho \"Feature branch path: $WORKTREE_PATH\"\n```\n\n### 1.2 Read Main Branch Test Results\n\n```bash\ncat $ARTIFACTS_DIR/e2e-main.md 2>/dev/null || echo \"No main branch E2E results available\"\n```\n\nThis tells you:\n- Which bugs were reproduced on main (you need to verify they're FIXED here)\n- Which test cases to re-run\n- What screenshots to compare against\n\n### 1.3 Read Code Reviews\n\n```bash\ncat $ARTIFACTS_DIR/code-review-main.md 2>/dev/null || echo \"\"\ncat $ARTIFACTS_DIR/code-review-feature.md 2>/dev/null || echo \"\"\n```\n\n---\n\n## Phase 2: Start Archon on Feature Branch\n\n### 2.1 Install Dependencies (if needed)\n\n```bash\nWORKTREE_PATH=$(cat $ARTIFACTS_DIR/.worktree-path | tr -d '\\n')\ncd \"$WORKTREE_PATH\" && bun install --frozen-lockfile 2>/dev/null || bun install\n```\n\n### 2.2 Start Backend on Custom Port\n\n**IMPORTANT**: Record the PID so we can kill it later. Redirect output to /dev/null to prevent terminal spawning.\n\n```bash\nWORKTREE_PATH=$(cat $ARTIFACTS_DIR/.worktree-path | tr -d '\\n')\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\n\ncd \"$WORKTREE_PATH\" && PORT=$BACKEND_PORT bun run --filter @archon/server dev > \"$ARTIFACTS_DIR/.e2e-feature-backend.log\" 2>&1 &\nBACKEND_PID=$!\necho \"$BACKEND_PID\" > \"$ARTIFACTS_DIR/.e2e-feature-backend-pid\"\necho \"Backend started with PID: $BACKEND_PID\"\n\n# Poll until healthy (max 60s)\nMAX_WAIT=60\nWAITED=0\nuntil curl -sf \"http://localhost:$BACKEND_PORT/api/health\" > /dev/null 2>&1; do\n  if [ $WAITED -ge $MAX_WAIT ]; then\n    echo \"ERROR: Backend did not become healthy within ${MAX_WAIT}s\"\n    echo \"Last log lines:\"\n    tail -20 \"$ARTIFACTS_DIR/.e2e-feature-backend.log\" 2>/dev/null || true\n    exit 1\n  fi\n  sleep 2\n  WAITED=$((WAITED + 2))\ndone\necho \"Backend healthy after ${WAITED}s\"\ncurl -s \"http://localhost:$BACKEND_PORT/api/health\" | head -c 200\necho \"\"\n```\n\n### 2.3 Start Frontend on Custom Port\n\n```bash\nWORKTREE_PATH=$(cat $ARTIFACTS_DIR/.worktree-path | tr -d '\\n')\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\nFRONTEND_PORT=$(cat $ARTIFACTS_DIR/.frontend-port | tr -d '\\n')\n\ncd \"$WORKTREE_PATH/packages/web\" && PORT=$BACKEND_PORT npx vite --port $FRONTEND_PORT --host > \"$ARTIFACTS_DIR/.e2e-feature-frontend.log\" 2>&1 &\nFRONTEND_PID=$!\necho \"$FRONTEND_PID\" > \"$ARTIFACTS_DIR/.e2e-feature-frontend-pid\"\necho \"Frontend started with PID: $FRONTEND_PID\"\n\n# Poll until serving (max 60s)\nMAX_WAIT=60\nWAITED=0\nuntil curl -sf \"http://localhost:$FRONTEND_PORT\" > /dev/null 2>&1; do\n  if [ $WAITED -ge $MAX_WAIT ]; then\n    echo \"ERROR: Frontend did not become ready within ${MAX_WAIT}s\"\n    echo \"Last log lines:\"\n    tail -20 \"$ARTIFACTS_DIR/.e2e-feature-frontend.log\" 2>/dev/null || true\n    exit 1\n  fi\n  sleep 2\n  WAITED=$((WAITED + 2))\ndone\necho \"Frontend ready after ${WAITED}s\"\ncurl -s \"http://localhost:$FRONTEND_PORT\" | head -c 100\necho \"\"\n```\n\n### 2.4 Seed Test Data (if needed)\n\n```bash\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\n\n# Check if codebases exist\nCODEBASE_COUNT=$(curl -s \"http://localhost:$BACKEND_PORT/api/codebases\" | grep -c '\"id\"' || echo 0)\n\nif [ \"$CODEBASE_COUNT\" -eq 0 ]; then\n  WORKTREE_PATH=$(cat $ARTIFACTS_DIR/.worktree-path | tr -d '\\n')\n  curl -s -X POST \"http://localhost:$BACKEND_PORT/api/codebases\" \\\n    -H \"Content-Type: application/json\" \\\n    -d \"{\\\"path\\\": \\\"$WORKTREE_PATH\\\"}\"\nfi\n```\n\n---\n\n## Phase 3: Browser Testing (Verify Fix)\n\n### 3.1 Load the Agent-Browser Skill\n\n**YOU MUST LOAD THE AGENT-BROWSER SKILL NOW.** Use `/agent-browser` or invoke the skill. This gives you the full command reference for browser automation.\n\n### 3.2 Core Browser Workflow\n\n```bash\n# 1. Open the Archon UI (ALWAYS use --session)\nFRONTEND_PORT=$(cat $ARTIFACTS_DIR/.frontend-port | tr -d '\\n')\nagent-browser --session $WORKFLOW_ID open \"http://localhost:$FRONTEND_PORT\"\n\n# 2. Wait for the app to load\nagent-browser --session $WORKFLOW_ID wait --load networkidle\n\n# 3. Get interactive elements\nagent-browser --session $WORKFLOW_ID snapshot -i\n\n# 4. Take a screenshot of initial state\nagent-browser --session $WORKFLOW_ID screenshot \"$ARTIFACTS_DIR/e2e-feature-01-initial.png\"\n```\n\n### 3.3 Re-Run All Test Cases from Main\n\nFor EVERY test case that was run on main, re-run it on the feature branch:\n\n1. **Same preconditions** — set up identical starting state\n2. **Same reproduction steps** — follow the exact same actions\n3. **Verify the fix** — the bug should NOT be present now\n4. **Capture evidence** — screenshot at same points as main for side-by-side comparison\n5. **Read each screenshot** — use the Read tool to visually inspect\n6. **Compare with main** — explicitly note what's different\n\n### 3.4 Additional UX Validation\n\nBeyond just checking the bug is fixed, validate the overall experience:\n\n1. **Happy path works** — the normal user flow is smooth\n2. **Edge cases** — try unusual inputs, rapid clicks, page refreshes\n3. **Visual quality** — no layout issues, colors correct, text readable\n4. **Responsiveness** — resize the viewport, check different sizes:\n   ```bash\n   agent-browser --session $WORKFLOW_ID set viewport 1920 1080\n   agent-browser --session $WORKFLOW_ID screenshot \"$ARTIFACTS_DIR/e2e-feature-desktop.png\"\n   agent-browser --session $WORKFLOW_ID set viewport 768 1024\n   agent-browser --session $WORKFLOW_ID screenshot \"$ARTIFACTS_DIR/e2e-feature-tablet.png\"\n   ```\n5. **No regressions** — other features near the fix still work correctly\n\n### 3.5 API Cross-Verification\n\n```bash\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\n\n# Verify data integrity matches UI\ncurl -s \"http://localhost:$BACKEND_PORT/api/conversations\" | head -c 500\n```\n\n---\n\n## Phase 4: Cleanup and Report\n\n**CRITICAL: You MUST complete cleanup before writing findings. Orphaned processes will accumulate and crash the system.**\n\n### 4.1 Close Browser\n\n```bash\n# ALWAYS use --session to only close YOUR browser, not other workflows'\nagent-browser --session $WORKFLOW_ID close 2>/dev/null || true\n```\n\n### 4.2 Stop Feature Branch Archon (Cross-Platform)\n\nKill processes by PID (recorded in Phase 2) AND by port (fallback). This works on both Windows and Unix.\n\n```bash\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\nFRONTEND_PORT=$(cat $ARTIFACTS_DIR/.frontend-port | tr -d '\\n')\n\n# Kill by recorded PID (primary method — both main and feature PIDs)\nfor pidfile in \"$ARTIFACTS_DIR/.e2e-feature-backend-pid\" \"$ARTIFACTS_DIR/.e2e-feature-frontend-pid\" \"$ARTIFACTS_DIR/.e2e-main-backend-pid\" \"$ARTIFACTS_DIR/.e2e-main-frontend-pid\"; do\n  if [ -f \"$pidfile\" ]; then\n    PID=$(cat \"$pidfile\" | tr -d '\\n')\n    echo \"Killing PID $PID from $pidfile\"\n    kill \"$PID\" 2>/dev/null || taskkill //F //T //PID \"$PID\" 2>/dev/null || true\n  fi\ndone\n\n# Fallback: kill by port (handles child processes the PID kill might miss)\nfor PORT in $BACKEND_PORT $FRONTEND_PORT; do\n  echo \"Cleaning up port $PORT...\"\n  fuser -k \"$PORT/tcp\" 2>/dev/null || true\n  lsof -ti:\"$PORT\" 2>/dev/null | xargs kill -9 2>/dev/null || true\n  netstat -ano 2>/dev/null | grep \":$PORT \" | grep LISTENING | awk '{print $5}' | sort -u | while read pid; do\n    taskkill //F //T //PID \"$pid\" 2>/dev/null || true\n  done\ndone\n\nsleep 2\necho \"Cleanup complete — verify ports are free:\"\nnetstat -ano 2>/dev/null | grep -E \":($BACKEND_PORT|$FRONTEND_PORT) \" | grep LISTENING || echo \"All ports free\"\n```\n\n### 4.3 Write Findings\n\nWrite to `$ARTIFACTS_DIR/e2e-feature.md`:\n\n```markdown\n# E2E Test Results: Feature Branch\n\n**PR**: #{number}\n**Branch**: {feature-branch} @ {commit}\n**Backend Port**: {port}\n**Frontend Port**: {port}\n**Screenshots**: $ARTIFACTS_DIR/e2e-feature-*.png\n\n## Test Summary\n\n| Test Case | Main Result | Feature Result | Fix Verified? |\n|-----------|-------------|----------------|---------------|\n| {test 1} | BUG REPRODUCED | FIXED | YES / NO |\n| {test 2} | BUG REPRODUCED | FIXED | YES / NO |\n\n## Detailed Findings\n\n### Test 1: {description}\n**Main branch**: {bug behavior — reference e2e-main screenshot}\n**Feature branch**: {fixed behavior — reference e2e-feature screenshot}\n**Fix verified**: YES / NO / PARTIAL\n**Screenshot comparison**: `e2e-main-{N}.png` vs `e2e-feature-{N}.png`\n\n### Test 2: {description}\n{Same structure...}\n\n## UX Quality Assessment\n\n| Aspect | Rating (1-5) | Notes |\n|--------|-------------|-------|\n| Visual correctness | {n} | {details} |\n| Responsiveness | {n} | {details} |\n| Edge case handling | {n} | {details} |\n| Error states | {n} | {details} |\n| Performance feel | {n} | {details} |\n\n## Regressions Found\n{Any new issues introduced by the fix, or NONE}\n\n## Additional Observations\n{Any other UX improvements or issues noticed}\n\n## Fix Confidence\n**HIGH / MEDIUM / LOW**\n\n{Overall confidence that the fix works correctly and completely}\n```\n\n---\n\n## Success Criteria\n\n- **ARCHON_STARTED**: Backend and frontend running on feature branch code\n- **ALL_TESTS_RERUN**: Every test case from main branch E2E re-executed\n- **FIX_VERIFIED**: Each bug confirmed fixed (or documented as still present)\n- **UX_VALIDATED**: Visual quality, responsiveness, edge cases checked\n- **NO_REGRESSIONS**: No new issues introduced\n- **ARCHON_STOPPED**: Processes killed, ports freed — **VERIFY ports are free before finishing**\n- **ARTIFACT_WRITTEN**: `$ARTIFACTS_DIR/e2e-feature.md` created\n",
  "archon-validate-pr-e2e-main": "---\ndescription: Start Archon from main branch, use agent-browser to reproduce the bug via E2E testing\nargument-hint: (none - reads from artifacts)\n---\n\n# E2E Testing: Main Branch (Reproduce Bug)\n\nStart Archon from the **main branch** code and use browser automation to reproduce the bug or gap described in the PR. Take screenshots as evidence.\n\n**CRITICAL**: You MUST use the `agent-browser` CLI for ALL browser interactions. Load the `/agent-browser` skill for the full command reference.\n\n**CRITICAL**: You MUST clean up ALL spawned processes before finishing. Record PIDs and kill them in Phase 4.\n\n**CRITICAL — SESSION ISOLATION**: This workflow runs in parallel with other validate-pr instances.\nYou MUST use `--session $WORKFLOW_ID` on EVERY `agent-browser` command to isolate your browser session.\nExample: `agent-browser --session $WORKFLOW_ID open \"http://...\"`, `agent-browser --session $WORKFLOW_ID snapshot -i`, etc.\nThe session ID is written to `$ARTIFACTS_DIR/.browser-session` for cleanup.\n\n**ABSOLUTELY FORBIDDEN — NEVER DO ANY OF THESE**:\n- `taskkill //F //IM chrome.exe` or ANY variant that kills chrome by image name — this kills the USER's browser\n- `taskkill //F //IM node.exe` or `taskkill //F //IM bun.exe` — this kills Claude Code, the Archon server, and all other workflows\n- `pkill chrome`, `pkill node`, `pkill bun`, or any broad process-name kill\n- `agent-browser close` without `--session $WORKFLOW_ID` — this kills OTHER workflows' browser sessions\n- Any \"kill everything\" or \"kill all\" escalation pattern — if agent-browser isn't working, SKIP E2E testing and note it in your report\n- If agent-browser fails to connect after 2 attempts, STOP trying and write your findings based on code review only\n\n---\n\n## Phase 1: Load Context\n\n### 1.1 Read Artifacts\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n')\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\nFRONTEND_PORT=$(cat $ARTIFACTS_DIR/.frontend-port | tr -d '\\n')\nCANONICAL_REPO=$(cat $ARTIFACTS_DIR/.canonical-repo | tr -d '\\n')\necho \"PR: #$PR_NUMBER\"\necho \"Backend port: $BACKEND_PORT\"\necho \"Frontend port: $FRONTEND_PORT\"\necho \"Main repo: $CANONICAL_REPO\"\n```\n\n### 1.2 Read PR and Test Plan\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n')\ngh pr view \"$PR_NUMBER\" --json title,body\n```\n\n```bash\n# Read the main branch code review for context on what to test\ncat $ARTIFACTS_DIR/code-review-main.md 2>/dev/null || echo \"No main branch review available yet\"\n```\n\n### 1.3 Testability Classification\n\nThe testability classifier determined:\n- **Decision**: $classify-testability.output.testable\n- **Reasoning**: $classify-testability.output.reasoning\n- **Test Plan**: $classify-testability.output.test_plan\n\nUse the test plan above combined with the PR description and code review to build your execution plan:\n- What user journeys reproduce the bug?\n- What should the broken behavior look like?\n- What screenshots would prove the bug exists?\n\n---\n\n## Phase 2: Start Archon on Main Branch\n\n### 2.1 Create Isolated Main Branch Worktree\n\n**IMPORTANT**: Use a dedicated worktree instead of mutating the canonical repo. This is safe\nfor concurrent validation runs — each gets its own isolated checkout.\n\n```bash\nCANONICAL_REPO=$(cat $ARTIFACTS_DIR/.canonical-repo | tr -d '\\n')\nPR_BASE=$(cat $ARTIFACTS_DIR/.pr-base | tr -d '\\n')\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n')\n\n# Create an isolated worktree for main branch E2E testing\nMAIN_E2E_PATH=\"$ARTIFACTS_DIR/main-checkout\"\ngit -C \"$CANONICAL_REPO\" fetch origin \"$PR_BASE\" --quiet\ngit -C \"$CANONICAL_REPO\" worktree add \"$MAIN_E2E_PATH\" \"origin/$PR_BASE\" --detach --quiet\necho \"$MAIN_E2E_PATH\" > \"$ARTIFACTS_DIR/.e2e-main-worktree\"\necho \"Main E2E worktree at: $MAIN_E2E_PATH\"\necho \"Base branch: $PR_BASE @ $(git -C \"$MAIN_E2E_PATH\" log --oneline -1)\"\n```\n\n### 2.2 Install Dependencies\n\n```bash\nMAIN_E2E_PATH=$(cat $ARTIFACTS_DIR/.e2e-main-worktree | tr -d '\\n')\ncd \"$MAIN_E2E_PATH\" && bun install --frozen-lockfile 2>/dev/null || bun install\n```\n\n### 2.3 Start Backend on Custom Port\n\n**IMPORTANT**: Record the PID so we can kill it later. Server output is logged for debugging.\n\n```bash\nMAIN_E2E_PATH=$(cat $ARTIFACTS_DIR/.e2e-main-worktree | tr -d '\\n')\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\n\ncd \"$MAIN_E2E_PATH\" && PORT=$BACKEND_PORT bun run --filter @archon/server dev > \"$ARTIFACTS_DIR/.e2e-main-backend.log\" 2>&1 &\nBACKEND_PID=$!\necho \"$BACKEND_PID\" > \"$ARTIFACTS_DIR/.e2e-main-backend-pid\"\necho \"Backend started with PID: $BACKEND_PID\"\n\n# Poll until healthy (max 60s)\nMAX_WAIT=60\nWAITED=0\nuntil curl -sf \"http://localhost:$BACKEND_PORT/api/health\" > /dev/null 2>&1; do\n  if [ $WAITED -ge $MAX_WAIT ]; then\n    echo \"ERROR: Backend did not become healthy within ${MAX_WAIT}s\"\n    echo \"Last log lines:\"\n    tail -20 \"$ARTIFACTS_DIR/.e2e-main-backend.log\" 2>/dev/null || true\n    exit 1\n  fi\n  sleep 2\n  WAITED=$((WAITED + 2))\ndone\necho \"Backend healthy after ${WAITED}s\"\ncurl -s \"http://localhost:$BACKEND_PORT/api/health\" | head -c 200\necho \"\"\n```\n\n### 2.4 Start Frontend on Custom Port\n\n```bash\nMAIN_E2E_PATH=$(cat $ARTIFACTS_DIR/.e2e-main-worktree | tr -d '\\n')\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\nFRONTEND_PORT=$(cat $ARTIFACTS_DIR/.frontend-port | tr -d '\\n')\n\ncd \"$MAIN_E2E_PATH/packages/web\" && PORT=$BACKEND_PORT npx vite --port $FRONTEND_PORT --host > \"$ARTIFACTS_DIR/.e2e-main-frontend.log\" 2>&1 &\nFRONTEND_PID=$!\necho \"$FRONTEND_PID\" > \"$ARTIFACTS_DIR/.e2e-main-frontend-pid\"\necho \"Frontend started with PID: $FRONTEND_PID\"\n\n# Poll until serving (max 60s)\nMAX_WAIT=60\nWAITED=0\nuntil curl -sf \"http://localhost:$FRONTEND_PORT\" > /dev/null 2>&1; do\n  if [ $WAITED -ge $MAX_WAIT ]; then\n    echo \"ERROR: Frontend did not become ready within ${MAX_WAIT}s\"\n    echo \"Last log lines:\"\n    tail -20 \"$ARTIFACTS_DIR/.e2e-main-frontend.log\" 2>/dev/null || true\n    exit 1\n  fi\n  sleep 2\n  WAITED=$((WAITED + 2))\ndone\necho \"Frontend ready after ${WAITED}s\"\ncurl -s \"http://localhost:$FRONTEND_PORT\" | head -c 100\necho \"\"\n```\n\n### 2.5 Seed Test Data (if needed)\n\n```bash\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\n\n# Check if codebases exist\nCODEBASE_COUNT=$(curl -s \"http://localhost:$BACKEND_PORT/api/codebases\" | grep -c '\"id\"' || echo 0)\n\nif [ \"$CODEBASE_COUNT\" -eq 0 ]; then\n  MAIN_E2E_PATH=$(cat $ARTIFACTS_DIR/.e2e-main-worktree | tr -d '\\n')\n  curl -s -X POST \"http://localhost:$BACKEND_PORT/api/codebases\" \\\n    -H \"Content-Type: application/json\" \\\n    -d \"{\\\"path\\\": \\\"$MAIN_E2E_PATH\\\"}\"\nfi\n```\n\n---\n\n## Phase 3: Browser Testing (Reproduce Bug)\n\n### 3.1 Load the Agent-Browser Skill\n\n**YOU MUST LOAD THE AGENT-BROWSER SKILL NOW.** Use `/agent-browser` or invoke the skill. This gives you the full command reference for browser automation.\n\n### 3.2 Core Browser Workflow\n\nFollow this pattern for every interaction:\n\n```bash\n# 0. Store session ID for cleanup\necho \"$WORKFLOW_ID\" > \"$ARTIFACTS_DIR/.browser-session\"\n\n# 1. Open the Archon UI (ALWAYS use --session)\nFRONTEND_PORT=$(cat $ARTIFACTS_DIR/.frontend-port | tr -d '\\n')\nagent-browser --session $WORKFLOW_ID open \"http://localhost:$FRONTEND_PORT\"\n\n# 2. Wait for the app to load\nagent-browser --session $WORKFLOW_ID wait --load networkidle\n\n# 3. Get interactive elements\nagent-browser --session $WORKFLOW_ID snapshot -i\n\n# 4. Take a screenshot of initial state\nagent-browser --session $WORKFLOW_ID screenshot \"$ARTIFACTS_DIR/e2e-main-01-initial.png\"\n\n# 5. Interact using refs from snapshot\n# agent-browser --session $WORKFLOW_ID click @e1\n# agent-browser --session $WORKFLOW_ID fill @e2 \"text\"\n\n# 6. Re-snapshot after DOM changes\n# agent-browser --session $WORKFLOW_ID snapshot -i\n\n# 7. Take screenshots at every significant point\n# agent-browser --session $WORKFLOW_ID screenshot \"$ARTIFACTS_DIR/e2e-main-02-{step}.png\"\n```\n\n### 3.3 Execute Test Plan\n\nFollow the test plan derived from the PR description and code review. For EACH test case:\n\n1. **Set up the preconditions** — navigate to the right page, create conversations/workflows as needed\n2. **Execute the reproduction steps** — exactly as described in the issue/PR\n3. **Capture evidence** — screenshot BEFORE the action, DURING, and AFTER\n4. **Verify the broken behavior** — confirm what you see matches the reported bug\n5. **Read each screenshot** — use the Read tool to visually inspect screenshots\n6. **Document what you see** — note exact error messages, visual glitches, missing elements\n\n### 3.4 API Cross-Verification\n\nFor bugs involving data integrity or SSE, cross-reference the UI with direct API calls:\n\n```bash\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\n\n# Check conversations\ncurl -s \"http://localhost:$BACKEND_PORT/api/conversations\" | head -c 500\n\n# Check specific conversation messages\n# curl -s \"http://localhost:$BACKEND_PORT/api/conversations/{id}/messages\"\n\n# Check workflow runs\n# curl -s \"http://localhost:$BACKEND_PORT/api/workflows/runs\"\n```\n\n---\n\n## Phase 4: Cleanup and Report\n\n**CRITICAL: You MUST complete cleanup before writing findings. Orphaned processes will accumulate and crash the system.**\n\n### 4.1 Close Browser\n\n```bash\n# ALWAYS use --session to only close YOUR browser, not other workflows'\nagent-browser --session $WORKFLOW_ID close 2>/dev/null || true\n```\n\n### 4.2 Stop Main Branch Archon (Cross-Platform)\n\nKill processes by PID (recorded in Phase 2) AND by port (fallback). This works on both Windows and Unix.\n\n```bash\nBACKEND_PORT=$(cat $ARTIFACTS_DIR/.backend-port | tr -d '\\n')\nFRONTEND_PORT=$(cat $ARTIFACTS_DIR/.frontend-port | tr -d '\\n')\n\n# Kill by recorded PID (primary method)\nfor pidfile in \"$ARTIFACTS_DIR/.e2e-main-backend-pid\" \"$ARTIFACTS_DIR/.e2e-main-frontend-pid\"; do\n  if [ -f \"$pidfile\" ]; then\n    PID=$(cat \"$pidfile\" | tr -d '\\n')\n    echo \"Killing PID $PID from $pidfile\"\n    # Try Unix kill first, then Windows taskkill\n    kill \"$PID\" 2>/dev/null || taskkill //F //T //PID \"$PID\" 2>/dev/null || true\n  fi\ndone\n\n# Fallback: kill by port (handles child processes the PID kill might miss)\n# Unix: fuser/lsof, Windows: netstat + taskkill\nfor PORT in $BACKEND_PORT $FRONTEND_PORT; do\n  echo \"Cleaning up port $PORT...\"\n  # Try fuser (Linux)\n  fuser -k \"$PORT/tcp\" 2>/dev/null || true\n  # Try lsof (macOS/Linux)\n  lsof -ti:\"$PORT\" 2>/dev/null | xargs kill -9 2>/dev/null || true\n  # Try netstat (Windows - Git Bash)\n  netstat -ano 2>/dev/null | grep \":$PORT \" | grep LISTENING | awk '{print $5}' | sort -u | while read pid; do\n    taskkill //F //T //PID \"$pid\" 2>/dev/null || true\n  done\ndone\n\nsleep 2\necho \"Process cleanup complete\"\n```\n\n### 4.3 Remove Main Branch Worktree\n\n```bash\nCANONICAL_REPO=$(cat $ARTIFACTS_DIR/.canonical-repo | tr -d '\\n')\nMAIN_E2E_PATH=$(cat \"$ARTIFACTS_DIR/.e2e-main-worktree\" 2>/dev/null | tr -d '\\n')\nif [ -n \"$MAIN_E2E_PATH\" ] && [ -d \"$MAIN_E2E_PATH\" ]; then\n  echo \"Removing main E2E worktree: $MAIN_E2E_PATH\"\n  git -C \"$CANONICAL_REPO\" worktree remove \"$MAIN_E2E_PATH\" --force 2>/dev/null || rm -rf \"$MAIN_E2E_PATH\"\nfi\necho \"Worktree cleanup complete\"\n```\n\n### 4.4 Write Findings\n\nWrite to `$ARTIFACTS_DIR/e2e-main.md`:\n\n```markdown\n# E2E Test Results: Main Branch\n\n**PR**: #{number}\n**Branch**: main @ {commit}\n**Backend Port**: {port}\n**Frontend Port**: {port}\n**Screenshots**: $ARTIFACTS_DIR/e2e-main-*.png\n\n## Test Summary\n\n| Test Case | Result | Evidence |\n|-----------|--------|----------|\n| {test 1} | BUG REPRODUCED / NOT REPRODUCED | e2e-main-{N}.png |\n| {test 2} | BUG REPRODUCED / NOT REPRODUCED | e2e-main-{N}.png |\n\n## Detailed Findings\n\n### Test 1: {description}\n**Steps**: {what was done}\n**Expected**: {what should happen on a fixed version}\n**Actual**: {what happened on main — the bug}\n**Screenshot**: `$ARTIFACTS_DIR/e2e-main-{N}.png`\n\n### Test 2: {description}\n{Same structure...}\n\n## Additional Issues Discovered\n{Any other bugs or UX issues noticed during testing}\n\n## Reproduction Confidence\n**HIGH / MEDIUM / LOW / NOT REPRODUCIBLE**\n\n{Explain confidence level. If not reproducible, explain what was tried.}\n```\n\n---\n\n## Success Criteria\n\n- **ARCHON_STARTED**: Backend and frontend running on allocated ports\n- **BROWSER_TESTED**: All test cases executed with agent-browser\n- **SCREENSHOTS_TAKEN**: Evidence captured for each test case\n- **BUG_ASSESSED**: Each PR claim tested on main branch\n- **ARCHON_STOPPED**: Processes killed, ports freed — **VERIFY ports are free before finishing**\n- **ARTIFACT_WRITTEN**: `$ARTIFACTS_DIR/e2e-main.md` created\n",
  "archon-validate-pr-report": "---\ndescription: Synthesize all validation findings into a final PR verdict report\nargument-hint: (none - reads from artifacts)\n---\n\n# PR Validation Report\n\nSynthesize all code review and E2E testing findings into a comprehensive verdict.\n\n---\n\n## Phase 1: Gather All Artifacts\n\nRead every artifact produced by earlier workflow nodes:\n\n```bash\necho \"=== Available artifacts ===\"\nls -la $ARTIFACTS_DIR/\necho \"\"\necho \"=== Code review (main) ===\"\ncat $ARTIFACTS_DIR/code-review-main.md 2>/dev/null || echo \"NOT AVAILABLE\"\necho \"\"\necho \"=== Code review (feature) ===\"\ncat $ARTIFACTS_DIR/code-review-feature.md 2>/dev/null || echo \"NOT AVAILABLE\"\necho \"\"\necho \"=== E2E test (main) ===\"\ncat $ARTIFACTS_DIR/e2e-main.md 2>/dev/null || echo \"NOT AVAILABLE (code-review-only PR)\"\necho \"\"\necho \"=== E2E test (feature) ===\"\ncat $ARTIFACTS_DIR/e2e-feature.md 2>/dev/null || echo \"NOT AVAILABLE (code-review-only PR)\"\n```\n\nAlso read the PR details:\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n')\ngh pr view \"$PR_NUMBER\" --json title,body,url,headRefName,baseRefName,additions,deletions,changedFiles\n```\n\nList all screenshots taken:\n\n```bash\nls $ARTIFACTS_DIR/e2e-*.png 2>/dev/null || echo \"No screenshots\"\n```\n\nIf screenshots exist, read a few key ones to include visual context in the report.\n\n---\n\n## Phase 2: Synthesize Findings\n\n### 2.1 Cross-Reference Code Review with E2E Results\n\nFor each bug/gap identified:\n- **Code review (main)**: Did the code analysis find the bug?\n- **E2E test (main)**: Was the bug visible in the UI?\n- **Code review (feature)**: Does the code fix look correct?\n- **E2E test (feature)**: Is the bug actually fixed in the UI?\n\n### 2.2 Identify Discrepancies\n\nLook for cases where:\n- Code review says it's fixed but E2E shows it's not\n- E2E shows it's fixed but the code fix is fragile/incomplete\n- New issues were found during E2E that code review missed\n- Code review found issues that E2E couldn't test\n\n### 2.3 Determine Final Verdict\n\n| Criteria | Required for APPROVE |\n|----------|---------------------|\n| Bug confirmed on main | Yes (or justified why not) |\n| Fix addresses root cause | Yes |\n| E2E confirms fix works | Yes (if E2E testable) |\n| No regressions | Yes |\n| Code quality acceptable | Yes |\n| CLAUDE.md compliant | Yes |\n\n---\n\n## Phase 3: Write Final Report\n\nWrite to `$ARTIFACTS_DIR/validation-report.md`:\n\n```markdown\n# PR Validation Report: #{number}\n\n**Title**: {PR title}\n**URL**: {PR URL}\n**Branch**: {head} → {base}\n**Files**: {count} changed (+{additions} -{deletions})\n**Validation Date**: {ISO timestamp}\n\n---\n\n## Verdict: {APPROVE / REQUEST_CHANGES / NEEDS_DISCUSSION}\n\n{2-3 sentence executive summary. Be direct: is this PR ready to merge?}\n\n---\n\n## Bug Confirmation\n\n| Claim | Confirmed on Main? | Fixed on Feature? | Evidence |\n|-------|--------------------|--------------------|----------|\n| {claim 1} | YES/NO | YES/NO | {screenshot refs or code refs} |\n| {claim 2} | YES/NO | YES/NO | {screenshot refs or code refs} |\n\n---\n\n## Code Review Summary\n\n### Main Branch (Pre-Fix)\n{Brief summary from code-review-main.md — was the bug evident in code?}\n\n### Feature Branch (Post-Fix)\n{Brief summary from code-review-feature.md — is the fix correct and optimal?}\n\n**Fix Quality Score**: {n}/5\n\n---\n\n## E2E Testing Summary\n\n{If E2E testing was performed:}\n\n### Main Branch (Bug Reproduction)\n{Brief summary from e2e-main.md — was the bug visible in the UI?}\n\n### Feature Branch (Fix Verification)\n{Brief summary from e2e-feature.md — is the fix verified in the UI?}\n\n**Fix Confidence**: HIGH / MEDIUM / LOW\n\n{If code-review-only:}\n\n_E2E testing was skipped — this PR's changes are not UI-visible. Validation based on code review only._\n\n---\n\n## Screenshots\n\n{List key screenshots with descriptions:}\n\n| Screenshot | Description |\n|------------|-------------|\n| `e2e-main-01-initial.png` | {what it shows} |\n| `e2e-feature-01-initial.png` | {what it shows — compare with main} |\n\n---\n\n## Issues Found\n\n### Must Fix Before Merge\n{CRITICAL or HIGH issues from any review stage. If none, say \"None.\"}\n\n### Should Fix (Non-Blocking)\n{MEDIUM issues — recommended but not blocking. If none, say \"None.\"}\n\n### Minor / Suggestions\n{LOW issues — nice to have. If none, say \"None.\"}\n\n---\n\n## Regressions\n{Any new issues introduced by the fix, or \"None found.\"}\n\n---\n\n## What's Done Well\n{Positive observations — good patterns, clean code, thorough fix}\n\n---\n\n## Recommendation\n\n**{APPROVE / REQUEST_CHANGES / NEEDS_DISCUSSION}**\n\n{Final paragraph: clear recommendation with reasoning. If REQUEST_CHANGES, list the specific changes needed. If NEEDS_DISCUSSION, describe what needs to be discussed.}\n```\n\n### 3.1 Post Summary to PR (optional)\n\nIf the verdict is clear, post a condensed summary to the PR as a comment:\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number | tr -d '\\n')\n\n# Create a concise PR comment\ngh pr comment \"$PR_NUMBER\" --body \"$(cat <<'COMMENT'\n## Archon PR Validation Report\n\n**Verdict**: {APPROVE / REQUEST_CHANGES}\n\n### Summary\n{2-3 sentence summary}\n\n### Bug Confirmation\n| Claim | Main | Feature |\n|-------|------|---------|\n| {claim} | {status} | {status} |\n\n### Issues\n{List any must-fix issues, or \"No blocking issues found.\"}\n\n---\n_Validated by archon-validate-pr workflow_\nCOMMENT\n)\"\n```\n\n---\n\n## Success Criteria\n\n- **ALL_ARTIFACTS_READ**: Every available artifact loaded and analyzed\n- **CROSS_REFERENCED**: Code review and E2E results reconciled\n- **VERDICT_DETERMINED**: Clear APPROVE / REQUEST_CHANGES / NEEDS_DISCUSSION\n- **REPORT_WRITTEN**: `$ARTIFACTS_DIR/validation-report.md` created\n- **PR_COMMENTED**: Summary posted to the PR\n",
  "archon-validate": "---\ndescription: Run full validation suite - type-check, lint, tests, build\nargument-hint: (no arguments - reads from workflow artifacts)\n---\n\n# Validate Implementation\n\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Your Mission\n\nRun the complete validation suite and fix any failures.\n\nThis is a focused step: run checks, fix issues, repeat until green.\n\n---\n\n## Phase 1: LOAD - Get Validation Commands\n\n### 1.1 Load Plan Context\n\n```bash\ncat $ARTIFACTS_DIR/plan-context.md\n```\n\nExtract the \"Validation Commands\" section.\n\n### 1.2 Identify Package Manager\n\n```bash\ntest -f bun.lockb && echo \"bun\" || \\\ntest -f pnpm-lock.yaml && echo \"pnpm\" || \\\ntest -f yarn.lock && echo \"yarn\" || \\\ntest -f package-lock.json && echo \"npm\" || \\\necho \"unknown\"\n```\n\n### 1.3 Determine Available Commands\n\nCheck `package.json` for available scripts:\n\n```bash\ncat package.json | grep -A 20 '\"scripts\"'\n```\n\n**PHASE_1_CHECKPOINT:**\n\n- [ ] Validation commands identified\n- [ ] Package manager known\n\n---\n\n## Phase 2: VALIDATE - Run All Checks\n\nRun each check in order. Fix any failures before proceeding.\n\n### 2.1 Type Check\n\n```bash\n{runner} run type-check\n```\n\n**If fails:**\n1. Read error output\n2. Fix the type issues\n3. Re-run until passing\n\n**Record result**: ✅ Pass / ❌ Fail (fixed)\n\n### 2.2 Lint Check\n\n```bash\n{runner} run lint\n```\n\n**If fails:**\n\n1. Try auto-fix first:\n   ```bash\n   {runner} run lint:fix\n   ```\n\n2. Re-run lint check\n\n3. If still failing, manually fix remaining issues\n\n**Record result**: ✅ Pass / ❌ Fail (fixed)\n\n### 2.3 Format Check\n\n```bash\n{runner} run format:check\n```\n\n**If fails:**\n\n1. Auto-fix:\n   ```bash\n   {runner} run format\n   ```\n\n2. Verify fixed:\n   ```bash\n   {runner} run format:check\n   ```\n\n**Record result**: ✅ Pass / ❌ Fail (fixed)\n\n### 2.4 Test Suite\n\n```bash\n{runner} test\n```\n\n**If fails:**\n\n1. Identify which test(s) failed\n2. Determine: implementation bug or test bug?\n3. Fix the root cause\n4. Re-run tests\n\n**Record result**: ✅ Pass ({N} tests) / ❌ Fail (fixed)\n\n### 2.5 Build Check\n\n```bash\n{runner} run build\n```\n\n**If fails:**\n\n1. Usually a type or import issue\n2. Fix and re-run\n\n**Record result**: ✅ Pass / ❌ Fail (fixed)\n\n**PHASE_2_CHECKPOINT:**\n\n- [ ] Type check passes\n- [ ] Lint passes\n- [ ] Format passes\n- [ ] Tests pass\n- [ ] Build passes\n\n---\n\n## Phase 3: ARTIFACT - Write Validation Results\n\n### 3.1 Write Validation Artifact\n\nWrite to `$ARTIFACTS_DIR/validation.md`:\n\n```markdown\n# Validation Results\n\n**Generated**: {YYYY-MM-DD HH:MM}\n**Workflow ID**: $WORKFLOW_ID\n**Status**: {ALL_PASS | FIXED | BLOCKED}\n\n---\n\n## Summary\n\n| Check | Result | Details |\n|-------|--------|---------|\n| Type check | ✅ | No errors |\n| Lint | ✅ | 0 errors, {N} warnings |\n| Format | ✅ | All files formatted |\n| Tests | ✅ | {N} passed, 0 failed |\n| Build | ✅ | Compiled successfully |\n\n---\n\n## Type Check\n\n**Command**: `{runner} run type-check`\n**Result**: ✅ Pass\n\n{If issues were fixed:}\n### Issues Fixed\n\n- `src/file.ts:42` - Added missing return type\n- `src/other.ts:15` - Fixed generic constraint\n\n---\n\n## Lint\n\n**Command**: `{runner} run lint`\n**Result**: ✅ Pass\n\n{If issues were fixed:}\n### Issues Fixed\n\n- {N} auto-fixed by `lint:fix`\n- {M} manually fixed\n\n### Remaining Warnings\n\n{List any warnings that weren't fixed, with justification}\n\n---\n\n## Format\n\n**Command**: `{runner} run format:check`\n**Result**: ✅ Pass\n\n{If files were formatted:}\n### Files Formatted\n\n- `src/file.ts`\n- `src/other.ts`\n\n---\n\n## Tests\n\n**Command**: `{runner} test`\n**Result**: ✅ Pass\n\n| Metric | Count |\n|--------|-------|\n| Total tests | {N} |\n| Passed | {N} |\n| Failed | 0 |\n| Skipped | {M} |\n\n{If tests were fixed:}\n### Tests Fixed\n\n- `src/x.test.ts` - Fixed assertion to match new behavior\n\n---\n\n## Build\n\n**Command**: `{runner} run build`\n**Result**: ✅ Pass\n\nBuild output: `dist/` (or as configured)\n\n---\n\n## Files Modified During Validation\n\n{If any files were changed to fix issues:}\n\n| File | Changes |\n|------|---------|\n| `src/file.ts` | Fixed type error |\n| `src/other.ts` | Lint auto-fix |\n\n---\n\n## Next Step\n\nContinue to `archon-finalize-pr` to update PR and mark ready for review.\n```\n\n**PHASE_3_CHECKPOINT:**\n\n- [ ] Validation artifact written\n- [ ] All results documented\n\n---\n\n## Phase 4: OUTPUT - Report Results\n\n### If All Pass:\n\n```markdown\n## Validation Complete ✅\n\n**Workflow ID**: `$WORKFLOW_ID`\n\n### Results\n\n| Check | Status |\n|-------|--------|\n| Type check | ✅ |\n| Lint | ✅ |\n| Format | ✅ |\n| Tests | ✅ ({N} passed) |\n| Build | ✅ |\n\n{If issues were fixed:}\n### Issues Fixed\n\n- {N} type errors fixed\n- {M} lint issues fixed\n- {K} format issues fixed\n\n### Artifact\n\nResults written to: `$ARTIFACTS_DIR/validation.md`\n\n### Next Step\n\nProceed to `archon-finalize-pr` to update PR and mark ready for review.\n```\n\n### If Blocked (unfixable issue):\n\n```markdown\n## Validation Blocked ❌\n\n**Workflow ID**: `$WORKFLOW_ID`\n\n### Failed Check\n\n**{check-name}**: {error description}\n\n### Attempts to Fix\n\n1. {what was tried}\n2. {what was tried}\n\n### Required Action\n\nThis issue requires manual intervention:\n\n{description of what needs to be done}\n\n### Artifact\n\nPartial results written to: `$ARTIFACTS_DIR/validation.md`\n```\n\n---\n\n## Success Criteria\n\n- **TYPE_CHECK_PASS**: `{runner} run type-check` exits 0\n- **LINT_PASS**: `{runner} run lint` exits 0\n- **FORMAT_PASS**: `{runner} run format:check` exits 0\n- **TESTS_PASS**: `{runner} test` all green\n- **BUILD_PASS**: `{runner} run build` exits 0\n- **ARTIFACT_WRITTEN**: Validation results documented\n",
  "archon-web-research": "---\ndescription: Research web sources for context relevant to a GitHub issue or feature\nargument-hint: <issue-number or search context>\n---\n\n# Web Research\n\n**Input**: $ARGUMENTS\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Your Mission\n\nSearch the web for information relevant to the issue or feature being worked on. Find official documentation, known issues, best practices, and solutions that will inform implementation.\n\n**Output**: `$ARTIFACTS_DIR/web-research.md`\n\n**Core Principle**: Search strategically, prioritize authoritative sources, cite everything.\n\n---\n\n## Phase 1: PARSE - Understand What to Research\n\n### 1.1 Get Issue Context\n\nIf input looks like a GitHub issue number:\n\n```bash\ngh issue view $ARGUMENTS --json title,body,labels\n```\n\n### 1.2 Identify Research Targets\n\nFrom the issue context, identify:\n\n- Key technologies, libraries, or APIs mentioned\n- Error messages or stack traces to search for\n- Concepts or patterns that need clarification\n- Version-specific documentation needs\n- Existing primitives in the ecosystem — what built-in or library-level abstractions already solve part of this? (avoids reinventing)\n\n### 1.3 Formulate Search Plan\n\nCreate 3-5 targeted search queries:\n\n| Query | Why | Expected Source |\n|-------|-----|-----------------|\n| \"{library} {feature} documentation\" | Official docs | Library website |\n| \"{error message}\" | Known issues | Stack Overflow, GitHub issues |\n| \"{pattern} best practices {year}\" | Current approaches | Blog posts, docs |\n| \"{library} built-in {primitive/feature}\" | Avoid reinventing | Official docs, changelog, migration guides |\n\n**PHASE_1_CHECKPOINT:**\n\n- [ ] Issue context understood\n- [ ] Research targets identified\n- [ ] Search queries formulated\n\n---\n\n## Phase 2: SEARCH - Execute Research\n\n### 2.1 Check for llms.txt\n\nMany sites publish LLM-optimized documentation:\n\n```\nTry fetching https://{domain}/llms.txt for any known site\nRead the result and fetch relevant sub-pages linked within\n```\n\n### 2.2 Search Official Documentation\n\nFor each technology/library involved:\n\n1. Search for official docs with version constraints\n2. Use `site:` operator for known authoritative sources\n3. Look for changelog/release notes for version info\n\n### 2.3 Search for Known Issues\n\nIf the issue involves errors or bugs:\n\n1. Search exact error messages in quotes\n2. Check GitHub issues for the relevant libraries\n3. Look for Stack Overflow answers\n\n### 2.4 Search for Best Practices\n\nIf the issue involves implementation decisions:\n\n1. Search for recognized patterns and approaches\n2. Cross-reference multiple sources\n3. Look for migration guides if changing approaches\n\n**PHASE_2_CHECKPOINT:**\n\n- [ ] At least 3 searches executed\n- [ ] Authoritative sources found\n- [ ] Relevant content extracted\n\n---\n\n## Phase 3: SYNTHESIZE - Compile Findings\n\n### 3.1 Organize by Relevance\n\nFor each finding:\n\n- **Source**: Name and URL\n- **Authority**: Why this source is credible\n- **Key information**: Direct quotes or specific facts\n- **Applies to**: Which part of the issue this informs\n- **Version/date**: Currency of the information\n\n### 3.2 Identify Conflicts or Gaps\n\n- Note any conflicting information between sources\n- Flag outdated content\n- Document what could NOT be found\n\n**PHASE_3_CHECKPOINT:**\n\n- [ ] Findings organized\n- [ ] Conflicts noted\n- [ ] Gaps documented\n\n---\n\n## Phase 4: GENERATE - Write Artifact\n\nWrite to `$ARTIFACTS_DIR/web-research.md`:\n\n```markdown\n# Web Research: $ARGUMENTS\n\n**Researched**: {ISO timestamp}\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Summary\n\n{2-3 sentence overview of key findings}\n\n---\n\n## Findings\n\n### {Source/Topic 1}\n\n**Source**: [{Name}]({URL})\n**Authority**: {Why credible}\n**Relevant to**: {Which part of the issue}\n\n**Key Information**:\n\n- {Finding 1}\n- {Finding 2}\n- {Version/date context}\n\n---\n\n### {Source/Topic 2}\n\n{Same structure...}\n\n---\n\n## Code Examples\n\n{If applicable — actual code from sources with attribution}\n\n```language\n// From [{source}]({url})\n{code example}\n```\n\n---\n\n## Gaps and Conflicts\n\n- {Information that couldn't be found}\n- {Conflicting claims between sources}\n- {Areas needing further investigation}\n\n---\n\n## Recommendations\n\nBased on research:\n\n1. {Recommendation 1 — what approach to take and why}\n2. {Recommendation 2 — what to avoid and why}\n\n---\n\n## Sources\n\n| # | Source | URL | Relevance |\n|---|--------|-----|-----------|\n| 1 | {name} | {url} | {brief relevance} |\n| 2 | {name} | {url} | {brief relevance} |\n```\n\n**PHASE_4_CHECKPOINT:**\n\n- [ ] Artifact written to `$ARTIFACTS_DIR/web-research.md`\n- [ ] All sources cited with URLs\n- [ ] Recommendations actionable\n\n---\n\n## Phase 5: OUTPUT - Report\n\n```markdown\n## Web Research Complete\n\n**Queries**: {n} searches executed\n**Sources**: {n} relevant sources found\n**Artifact**: `$ARTIFACTS_DIR/web-research.md`\n\n### Key Findings\n\n- {Finding 1}\n- {Finding 2}\n- {Finding 3}\n\n### Gaps\n\n- {What couldn't be found, if any}\n```\n\n---\n\n## Quality Standards\n\n| Standard | Requirement |\n|----------|-------------|\n| **Accuracy** | Quote sources exactly, provide direct links |\n| **Relevance** | Focus on what directly addresses the issue |\n| **Currency** | Note publication dates and versions |\n| **Authority** | Prioritize official docs, recognized experts |\n| **Completeness** | Search multiple angles, note gaps |\n| **Transparency** | Flag outdated, conflicting, or uncertain info |\n\n---\n\n## What NOT To Do\n\n- Don't guess when you can search\n- Don't fetch pages without checking search results first\n- Don't ignore publication dates on technical content\n- Don't present a single source as definitive without corroboration\n- Don't skip the Gaps section — be honest about limitations\n\n---\n\n## Success Criteria\n\n- **RESEARCH_EXECUTED**: At least 3 targeted searches completed\n- **SOURCES_CITED**: All findings have source URLs\n- **ARTIFACT_WRITTEN**: Research saved to `$ARTIFACTS_DIR/web-research.md`\n- **ACTIONABLE**: Findings directly inform implementation decisions\n",
  "archon-workflow-summary": "---\ndescription: Final workflow summary with decision matrix for follow-up actions\nargument-hint: (no arguments - reads from workflow artifacts)\n---\n\n# Workflow Summary\n\n**Workflow ID**: $WORKFLOW_ID\n\n---\n\n## Your Mission\n\nCreate the final summary report for the workflow run:\n1. Summarize what was implemented vs the plan\n2. List deviations and their rationale\n3. Surface unfixed review findings (MEDIUM/LOW)\n4. Create actionable follow-up recommendations\n5. Post to GitHub PR as a comment\n6. Write artifact for future reference\n\n**Output**: Decision matrix the user can act on quickly.\n\n---\n\n## Phase 1: LOAD - Gather ALL Artifacts\n\n**CRITICAL**: Read EVERY artifact from the workflow run. Miss nothing.\n\n### 1.1 Scan Workflow Artifacts Directory\n\n```bash\n# List all artifacts from this workflow run\nls -la $ARTIFACTS_DIR/\n\n# Read each one\nfor file in $ARTIFACTS_DIR/*.md; do\n  echo \"=== $file ===\"\n  cat \"$file\"\ndone\n```\n\n**Expected artifacts**:\n- `plan-context.md` - Plan summary, scope limits, acceptance criteria\n- `plan-confirmation.md` - Pattern verification results\n- `implementation.md` - Tasks done, deviations, issues encountered\n- `validation.md` - Test/lint/build results\n- `pr-ready.md` - PR number, URL, final commit\n- `.pr-number` - PR number registry file\n- `.pr-url` - PR URL registry file\n\n### 1.2 Scan Review Artifacts\n\n```bash\n# Read review artifacts from workflow-scoped directory\nls -la $ARTIFACTS_DIR/review/\n\n# Read each review finding\nfor file in $ARTIFACTS_DIR/review/*.md; do\n  echo \"=== $file ===\"\n  cat \"$file\"\ndone\n```\n\n**Expected review artifacts** (in `runs/$WORKFLOW_ID/review/`):\n- `scope.md` - Files changed, scope limits, focus areas\n- `code-review-findings.md` - Code quality issues\n- `error-handling-findings.md` - Silent failures, catch blocks\n- `test-coverage-findings.md` - Test gaps\n- `comment-quality-findings.md` - Documentation issues\n- `docs-impact-findings.md` - Doc update needs\n- `consolidated-review.md` - Combined findings, priorities\n- `fix-report.md` - What was fixed\n- `sync-report.md` - Rebase/sync status (if applicable)\n\n### 1.3 Extract Key Data\n\n**From plan-context.md**:\n- Plan title and summary\n- Files expected to change\n- **NOT Building (Scope Limits)** - CRITICAL: these are follow-up candidates\n- Acceptance criteria\n\n**From implementation.md**:\n- Tasks completed vs planned\n- Files actually changed\n- **Deviations from plan** - document these prominently\n- Issues encountered during implementation\n\n**From all review findings**:\n- CRITICAL/HIGH issues (should be fixed)\n- **MEDIUM issues** - follow-up candidates\n- **LOW issues** - optional follow-ups\n- Specific recommendations by category\n\n**From fix-report.md**:\n- What was actually fixed\n- What was NOT fixed (and why)\n\n### 1.4 Cross-Reference\n\nCompare across artifacts:\n- Plan vs Implementation: What matched? What deviated?\n- Review findings vs Fix report: What's still open?\n- NOT Building vs Review findings: Did reviewers flag excluded items? (this is expected, note it)\n\n**PHASE_1_CHECKPOINT:**\n\n- [ ] ALL workflow artifacts read\n- [ ] ALL review artifacts read\n- [ ] Deviations extracted\n- [ ] Unfixed issues identified\n- [ ] NOT Building items noted\n\n---\n\n## Phase 2: ANALYZE - Build Follow-Up Matrix\n\n### 2.1 Categorize Follow-Up Items\n\n**From \"NOT Building\" section** - Future work explicitly deferred:\n\n| Item | Rationale | Suggested Follow-Up |\n|------|-----------|---------------------|\n| {excluded item} | {why excluded} | Create issue / Separate PR / Not needed |\n\n**From Implementation Deviations** - Changes that diverged from plan:\n\n| Deviation | Reason | Impact | Follow-Up Needed? |\n|-----------|--------|--------|-------------------|\n| {what changed} | {why} | {low/medium/high} | {yes/no + action} |\n\n**From Unfixed Review Findings** - MEDIUM/LOW severity items:\n\n| Finding | Severity | Category | Suggested Action |\n|---------|----------|----------|------------------|\n| {issue} | MEDIUM | docs | Update CLAUDE.md |\n| {issue} | LOW | test | Add edge case test |\n| {issue} | MEDIUM | error-handling | Log instead of silent |\n\n### 2.2 Prioritize by Effort vs Value\n\n**Quick Wins** (< 5 min, high value):\n- Documentation updates\n- Simple comment additions\n- Missing log statements\n\n**Worth Doing** (medium effort, clear value):\n- Test coverage gaps\n- Error message improvements\n- Type refinements\n\n**Can Defer** (higher effort or lower urgency):\n- Refactoring suggestions\n- Performance optimizations\n- Style improvements\n\n**PHASE_2_CHECKPOINT:**\n\n- [ ] NOT Building items categorized\n- [ ] Deviations assessed\n- [ ] Unfixed findings prioritized\n- [ ] Quick wins identified\n\n---\n\n## Phase 3: GENERATE - Create Decision Matrix\n\n### 3.1 Build Decision Matrix\n\nStructure the output for easy decision-making:\n\n```markdown\n## Follow-Up Decision Matrix\n\n### 🚀 Quick Wins (Can do now, < 5 min each)\n\n| # | Item | Action | Command |\n|---|------|--------|---------|\n| 1 | Update CLAUDE.md with new column | Docs update | `Run docs agent` |\n| 2 | Add missing JSDoc to deactivateSession | Comment | `Auto-fix` |\n\n**Your choice**:\n- [ ] Do all quick wins before merge\n- [ ] Create issues for later\n- [ ] Skip (not needed)\n\n---\n\n### 📋 Suggested GitHub Issues\n\n| # | Title | Labels | From |\n|---|-------|--------|------|\n| 1 | {issue title} | `enhancement`, `docs` | NOT Building |\n| 2 | {issue title} | `bug`, `low-priority` | Review finding |\n\n**Your choice**:\n- [ ] Create all issues\n- [ ] Create selected: {numbers}\n- [ ] Skip issue creation\n\n---\n\n### 📝 Documentation Gaps\n\n| File | Section | Update Needed |\n|------|---------|---------------|\n| CLAUDE.md | Database Schema | Add ended_reason column |\n| $DOCS_DIR/architecture.md | Sessions | Update deactivateSession signature |\n\n**Your choice**:\n- [ ] Send docs agent to fix all\n- [ ] Fix manually after merge\n- [ ] Skip (acceptable as-is)\n\n---\n\n### ⚠️ Deferred Items (from NOT Building)\n\n| Item | Why Deferred | When to Address |\n|------|--------------|-----------------|\n| {item} | {rationale} | {next sprint / never / if needed} |\n\n**These were intentionally excluded** - no action needed unless priorities change.\n```\n\n**PHASE_3_CHECKPOINT:**\n\n- [ ] Decision matrix structured\n- [ ] Quick wins identified\n- [ ] Issues drafted\n- [ ] Docs gaps listed\n\n---\n\n## Phase 4: POST - GitHub PR Comment\n\n### 4.1 Format for GitHub\n\nCreate a PR comment with the summary:\n\n```markdown\n## 🎯 Workflow Summary\n\n**Plan**: `{plan-path}`\n**Status**: ✅ Implementation complete, PR ready for review\n\n---\n\n### Implementation vs Plan\n\n| Metric | Planned | Actual |\n|--------|---------|--------|\n| Files created | {N} | {N} |\n| Files updated | {M} | {M} |\n| Tests added | {K} | {K} |\n| Deviations | - | {count} |\n\n{If deviations:}\n<details>\n<summary>📋 Deviations from Plan ({count})</summary>\n\n{List each deviation with reason}\n\n</details>\n\n---\n\n### Review Summary\n\n| Severity | Found | Fixed | Remaining |\n|----------|-------|-------|-----------|\n| CRITICAL | {N} | {N} | 0 |\n| HIGH | {N} | {N} | 0 |\n| MEDIUM | {N} | {fixed} | {remaining} |\n| LOW | {N} | {fixed} | {remaining} |\n\n---\n\n### 🚀 Quick Wins Before Merge\n\n{If any quick wins identified:}\n\n| Item | Effort | Action |\n|------|--------|--------|\n| {item} | ~2 min | {action} |\n\n**Reply with**: `@archon do quick wins` to auto-fix these.\n\n---\n\n### 📋 Suggested Follow-Up Issues\n\n{If issues suggested:}\n\n| Title | Labels |\n|-------|--------|\n| {title} | {labels} |\n\n**Reply with**: `@archon create follow-up issues` to create these.\n\n---\n\n### 📝 Documentation Updates\n\n{If doc gaps found:}\n\n| File | Update |\n|------|--------|\n| {file} | {what} |\n\n**Reply with**: `@archon update docs` to send a docs agent.\n\n---\n\n<details>\n<summary>ℹ️ Deferred Items (NOT Building)</summary>\n\nThese were **intentionally excluded** from scope:\n\n{List from NOT Building section}\n\n</details>\n\n---\n\n**Artifacts**: `$ARTIFACTS_DIR/`\n```\n\n### 4.2 Post to GitHub\n\n```bash\ngh pr comment {pr-number} --body \"{formatted-summary}\"\n```\n\n**PHASE_4_CHECKPOINT:**\n\n- [ ] Summary formatted for GitHub\n- [ ] Comment posted to PR\n\n---\n\n## Phase 5: ARTIFACT - Write Summary\n\n### 5.1 Write Summary Artifact\n\nWrite to `$ARTIFACTS_DIR/workflow-summary.md`:\n\n```markdown\n# Workflow Summary\n\n**Generated**: {YYYY-MM-DD HH:MM}\n**Workflow ID**: $WORKFLOW_ID\n**PR**: #{number}\n\n---\n\n## Execution Summary\n\n| Phase | Status | Notes |\n|-------|--------|-------|\n| Setup | ✅ | Branch ready |\n| Confirm | ✅ | Plan validated |\n| Implement | ✅ | {N} tasks completed |\n| Validate | ✅ | All checks pass |\n| PR | ✅ | #{number} created |\n| Review | ✅ | {N} agents ran |\n| Fixes | ✅ | {N} issues fixed |\n\n---\n\n## Implementation vs Plan\n\n{Detailed comparison}\n\n---\n\n## Deviations\n\n{List with rationale}\n\n---\n\n## Unfixed Review Findings\n\n### MEDIUM Severity\n\n{List}\n\n### LOW Severity\n\n{List}\n\n---\n\n## Follow-Up Recommendations\n\n### GitHub Issues to Create\n\n{List with draft titles/bodies}\n\n### Documentation Updates\n\n{List with specific changes}\n\n### Deferred to Future\n\n{List from NOT Building}\n\n---\n\n## Decision Matrix\n\n{Copy of the decision matrix}\n\n---\n\n## GitHub Comment\n\nPosted to: {PR URL}#comment-{id}\n```\n\n**PHASE_5_CHECKPOINT:**\n\n- [ ] Summary artifact written\n- [ ] All sections complete\n\n---\n\n## Phase 5.5: ARCHIVE - Create Backward-Compatible Symlink\n\n### 5.5.1 Create Symlink for PR-Based Lookup\n\nCreate symlink for backward compatibility with PR-based artifact lookup:\n\n```bash\nPR_NUMBER=$(cat $ARTIFACTS_DIR/.pr-number 2>/dev/null)\nif [ -n \"$PR_NUMBER\" ]; then\n  mkdir -p $ARTIFACTS_DIR/../reviews\n  ln -sfn ../runs/$WORKFLOW_ID/review $ARTIFACTS_DIR/../reviews/pr-$PR_NUMBER\nfi\n```\n\nThis allows legacy tools to find review artifacts at `$ARTIFACTS_DIR/../reviews/pr-{number}/`.\n\n**PHASE_5.5_CHECKPOINT:**\n\n- [ ] Symlink created (if PR number available)\n\n---\n\n## Phase 6: OUTPUT - Report to User\n\n```markdown\n## Workflow Complete 🎉\n\n**Workflow ID**: `$WORKFLOW_ID`\n**PR**: #{number} - {title}\n\n### Summary\n\n| Metric | Value |\n|--------|-------|\n| Tasks completed | {N}/{N} |\n| Review findings fixed | {N} |\n| Quick wins available | {N} |\n| Follow-up issues suggested | {N} |\n\n### Posted to GitHub\n\nSummary comment added to PR with:\n- Implementation vs plan comparison\n- Deviations documented\n- Decision matrix for follow-ups\n\n### Your Next Steps\n\n1. **Review the PR**: {url}\n2. **Quick wins**: Reply `@archon do quick wins` on PR (or skip)\n3. **Create issues**: Reply `@archon create follow-up issues` (or skip)\n4. **Merge when ready**\n\n### Artifacts\n\n- Summary: `$ARTIFACTS_DIR/workflow-summary.md`\n- All artifacts: `$ARTIFACTS_DIR/`\n```\n\n---\n\n## Success Criteria\n\n- **ARTIFACTS_LOADED**: All workflow artifacts read\n- **MATRIX_CREATED**: Follow-up items categorized and prioritized\n- **GITHUB_POSTED**: Summary comment on PR\n- **ARTIFACT_WRITTEN**: workflow-summary.md created\n- **ACTIONABLE**: User has clear next steps with minimal cognitive load\n",
};

// Bundled default workflows (69 total)
export const BUNDLED_WORKFLOWS: Record<string, string> = {
  "archon-adversarial-dev": "name: archon-adversarial-dev\ndescription: |\n  Use when: User wants to build a complete application from scratch using adversarial development.\n  Triggers: \"adversarial dev\", \"adversarial development\", \"build with adversarial\", \"gan dev\",\n            \"adversarial build\", \"build app adversarially\", \"adversarial coding\".\n  Does: Three-role GAN-inspired development — Planner creates spec with sprints, then a state-machine\n        loop alternates between Generator (builds code) and Evaluator (attacks it) with hard pass/fail\n        thresholds. The evaluator's job is to BREAK what the generator builds. If any criterion scores\n        below 7/10, the sprint goes back to the generator with adversarial feedback. Stops on sprint\n        failure after max retries.\n  NOT for: Bug fixes, PR reviews, refactoring existing code, simple one-off tasks.\n\n  Based on Anthropic's harness design article for long-running application development.\n  Separates planning, building, and evaluation into distinct roles with adversarial tension.\nprovider: claude\nmodel: sonnet\n\nnodes:\n  # ─── Phase 1: Planning ───────────────────────────────────────────────\n  - id: plan\n    prompt: |\n      You are a product planning expert. Your job is to take a short user prompt and expand it\n      into a comprehensive product specification.\n\n      ## User Request\n\n      $ARGUMENTS\n\n      ## Your Task\n\n      Write a comprehensive product specification to the file `$ARTIFACTS_DIR/spec.md` using the Write tool.\n\n      The spec MUST include ALL of the following sections:\n\n      ### 1. Product Overview\n      What the product does, who it's for, core value proposition.\n\n      ### 2. Tech Stack\n      Specific technologies, frameworks, and libraries. Be opinionated — pick concrete choices,\n      not \"a modern framework.\" Include exact package names and versions where relevant.\n\n      ### 3. Design Language\n      Visual style, specific color hex codes, typography choices, component patterns, spacing system.\n\n      ### 4. Feature List\n      Every feature organized by priority. Be exhaustive.\n\n      ### 5. Sprint Plan\n      Features broken into 3-6 sprints, ordered by dependency and importance:\n      - **Sprint 1** should establish the foundation (project setup, core data models, basic UI shell)\n      - Each subsequent sprint builds on the previous\n      - Label each sprint clearly: \"Sprint 1: Foundation\", \"Sprint 2: Core Features\", etc.\n      - List the specific features/deliverables for each sprint\n\n      Be specific and opinionated. The more concrete the spec (exact API paths, specific color codes,\n      named libraries), the better the generator can build and the evaluator can test.\n\n      IMPORTANT: Write the spec to `$ARTIFACTS_DIR/spec.md` using the Write tool. Do NOT just output\n      it as conversation text.\n    allowed_tools: [Read, Write, Glob, Grep]\n\n  # ─── Phase 2: Workspace Initialization ───────────────────────────────\n  - id: init-workspace\n    depends_on: [plan]\n    bash: |\n      ARTIFACTS=\"$ARTIFACTS_DIR\"\n\n      # Create directory structure for harness communication\n      mkdir -p \"$ARTIFACTS/contracts\"\n      mkdir -p \"$ARTIFACTS/feedback\"\n      mkdir -p \"$ARTIFACTS/app\"\n\n      # Initialize isolated git repo in app directory\n      cd \"$ARTIFACTS/app\"\n      git init -q\n      git commit --allow-empty -m \"Initial commit: adversarial-dev workspace\" -q\n\n      # Extract sprint count from spec (find highest \"Sprint N\" reference)\n      SPEC=\"$ARTIFACTS/spec.md\"\n      SPRINT_COUNT=3\n      if [ -f \"$SPEC\" ]; then\n        FOUND=$(grep -ioE 'sprint\\s+[0-9]+' \"$SPEC\" | grep -oE '[0-9]+' | sort -n | tail -1)\n        if [ -n \"$FOUND\" ] && [ \"$FOUND\" -ge 1 ] 2>/dev/null; then\n          SPRINT_COUNT=$FOUND\n        fi\n        if [ \"$SPRINT_COUNT\" -gt 10 ]; then\n          SPRINT_COUNT=10\n        fi\n      fi\n\n      # Write initial state machine file\n      cat > \"$ARTIFACTS/state.json\" << 'STATEEOF'\n      {\n        \"phase\": \"negotiating\",\n        \"sprint\": 1,\n        \"totalSprints\": SPRINT_COUNT_PLACEHOLDER,\n        \"retry\": 0,\n        \"maxRetries\": 3,\n        \"passThreshold\": 7,\n        \"completedSprints\": [],\n        \"status\": \"running\"\n      }\n      STATEEOF\n      STATE_TMP=\"$ARTIFACTS/state.json.tmp\"\n      sed \"s/SPRINT_COUNT_PLACEHOLDER/$SPRINT_COUNT/\" \"$ARTIFACTS/state.json\" > \"$STATE_TMP\"\n      mv \"$STATE_TMP\" \"$ARTIFACTS/state.json\"\n\n      echo \"{\\\"totalSprints\\\": $SPRINT_COUNT, \\\"appDir\\\": \\\"$ARTIFACTS/app\\\", \\\"artifactsDir\\\": \\\"$ARTIFACTS\\\"}\"\n    timeout: 30000\n\n  # ─── Phase 3: Adversarial Sprint Loop ────────────────────────────────\n  #\n  # State machine driven by $ARTIFACTS_DIR/state.json\n  # Each iteration plays ONE role: negotiator, generator, or evaluator\n  # fresh_context ensures genuine separation between roles\n  #\n  - id: adversarial-sprint\n    depends_on: [init-workspace]\n    idle_timeout: 600000\n    model: opus[1m]\n    loop:\n      prompt: |\n        # Adversarial Development — Sprint Loop\n\n        You are part of a GAN-inspired adversarial development system with three distinct roles.\n        Each iteration you play ONE role, determined by the current phase in the state file.\n\n        ## FIRST: Read State\n\n        Read `$ARTIFACTS_DIR/state.json` to determine:\n        - `phase` — which role you play this iteration\n        - `sprint` — current sprint number\n        - `totalSprints` — how many sprints total\n        - `retry` — current retry attempt (0 = first try)\n        - `maxRetries` — max retries before hard failure (default 3)\n        - `passThreshold` — minimum score to pass (default 7)\n\n        Then read `$ARTIFACTS_DIR/spec.md` for product context.\n\n        ## Directory Layout\n\n        - App source code: `$ARTIFACTS_DIR/app/`\n        - Sprint contracts: `$ARTIFACTS_DIR/contracts/sprint-{N}.json`\n        - Evaluation feedback: `$ARTIFACTS_DIR/feedback/sprint-{N}-round-{R}.json`\n        - State machine: `$ARTIFACTS_DIR/state.json`\n\n        ---\n\n        ## ROLE: CONTRACT NEGOTIATOR (phase = \"negotiating\")\n\n        You negotiate the success criteria for the current sprint. Play BOTH sides sequentially:\n\n        **Step 1 — Generator's Proposal:**\n        Read the spec carefully. Identify what Sprint {N} should deliver based on the sprint plan.\n        Propose a sprint contract with 5-15 specific, testable criteria.\n\n        Each criterion MUST be concrete and verifiable. Examples:\n        - GOOD: \"GET /api/tasks returns 200 with JSON array; each item has id (number), title (string), status (string), createdAt (ISO date)\"\n        - GOOD: \"Clicking the Add Task button opens a modal with title input, priority dropdown (low/medium/high), and due date picker\"\n        - BAD: \"The API works well\"\n        - BAD: \"Tasks can be managed\"\n\n        **Step 2 — Evaluator's Tightening:**\n        Now review your proposal as an adversary. For EACH criterion ask:\n        - Is it specific enough to test programmatically?\n        - What edge cases are missing? (empty inputs, special characters, concurrent requests)\n        - Is the bar high enough, or would sloppy code pass?\n\n        Tighten vague criteria. Add edge cases. Raise the bar.\n\n        **Write the final contract** to `$ARTIFACTS_DIR/contracts/sprint-{N}.json`:\n        ```json\n        {\n          \"sprintNumber\": <N>,\n          \"features\": [\"feature1\", \"feature2\", ...],\n          \"criteria\": [\n            {\n              \"name\": \"short-kebab-name\",\n              \"description\": \"Specific, testable description of what must be true\",\n              \"threshold\": 7\n            }\n          ]\n        }\n        ```\n\n        **Update state.json**: Set `\"phase\": \"building\"`. Keep all other fields unchanged.\n\n        ---\n\n        ## ROLE: GENERATOR (phase = \"building\")\n\n        You are a software engineer. Build features that MUST survive an adversarial evaluator\n        who will actively try to break your code.\n\n        **Read these files:**\n        1. `$ARTIFACTS_DIR/spec.md` — full product spec (design language, tech stack, all features)\n        2. `$ARTIFACTS_DIR/contracts/sprint-{N}.json` — the contract you must satisfy\n        3. If `retry` > 0: read `$ARTIFACTS_DIR/feedback/sprint-{N}-round-{R-1}.json` for the\n           evaluator's previous feedback\n\n        **If this is a RETRY (retry > 0):**\n        Read the feedback CAREFULLY. Every failed criterion must be addressed.\n        - If scores were close (5-6) and trending up: REFINE your approach\n        - If scores were low (1-4) or the approach is fundamentally broken: PIVOT to a new strategy\n        - Address EVERY feedback item — the evaluator WILL check\n        - Re-verify each fix by running the code before committing\n\n        **Build rules:**\n        - All code goes in `$ARTIFACTS_DIR/app/`\n        - Build ONE feature at a time, verify it works, then commit:\n          ```bash\n          cd $ARTIFACTS_DIR/app && git add -A && git commit -m \"feat: description of what was built\"\n          ```\n        - Install dependencies as needed (npm/bun/pip/etc)\n        - Test your code — start the server, hit the endpoints, verify the UI renders\n        - Think about what the evaluator will attack: edge cases, error handling, input validation\n        - Build defensively — the evaluator's job is to break you\n\n        **Update state.json**: Set `\"phase\": \"evaluating\"`. Keep all other fields unchanged.\n\n        ---\n\n        ## ROLE: EVALUATOR (phase = \"evaluating\")\n\n        You are an ADVERSARIAL QA agent. Your mandate is to BREAK what the generator built.\n        You are not helpful. You are not generous. You are an attacker.\n\n        **CRITICAL CONSTRAINTS:**\n        - You are READ-ONLY for source code. NEVER use Write or Edit on files in `$ARTIFACTS_DIR/app/`.\n        - You MAY use Bash to run the app, curl endpoints, run test scripts, check behavior.\n        - You MUST kill any background processes (servers, watchers) you start BEFORE finishing.\n          Use: `pkill -f \"node\\|bun\\|python\\|npm\" 2>/dev/null || true`\n        - You MUST score EVERY criterion in the contract. No skipping.\n\n        **Scoring guidelines:**\n        - **9-10**: Exceptional. Works perfectly including edge cases the contract didn't mention.\n        - **7-8**: Solid. Meets the criterion as stated. Minor polish issues at most.\n        - **5-6**: Partial. Core functionality exists but fails important edge cases or has bugs.\n        - **3-4**: Weak. Barely functional. Major gaps.\n        - **1-2**: Broken. Does not work or is not implemented.\n\n        Do NOT grade on a curve. Do NOT give benefit of the doubt. A 7 means \"genuinely meets the bar.\"\n        If something is broken, say it's broken.\n\n        **Read**: `$ARTIFACTS_DIR/contracts/sprint-{N}.json` for the criteria.\n\n        **For each criterion:**\n        1. Read the relevant source code\n        2. Run the application (start server, test endpoints, check rendered UI)\n        3. Try to BREAK it — invalid inputs, missing fields, edge cases, error handling gaps\n        4. Score it honestly\n\n        **Write evaluation** to `$ARTIFACTS_DIR/feedback/sprint-{N}-round-{R}.json`:\n        ```json\n        {\n          \"passed\": <true if ALL scores >= passThreshold, false otherwise>,\n          \"scores\": {\n            \"criterion-name\": <score>,\n            ...\n          },\n          \"feedback\": [\n            {\n              \"criterion\": \"criterion-name\",\n              \"score\": <1-10>,\n              \"details\": \"Specific findings. Include file paths, line numbers, exact error messages, curl commands that failed.\"\n            }\n          ],\n          \"overallSummary\": \"What worked, what didn't, what the generator must fix.\"\n        }\n        ```\n\n        **Determine pass/fail** — `passed` is `true` ONLY if every single score >= `passThreshold`.\n\n        **Update state.json based on result:**\n\n        **If PASSED (all criteria >= threshold):**\n        - Add current sprint number to `completedSprints` array\n        - If `sprint` < `totalSprints`: set `\"phase\": \"negotiating\"`, increment `\"sprint\"` by 1, set `\"retry\": 0`\n        - If `sprint` == `totalSprints`: set `\"phase\": \"complete\"`, set `\"status\": \"complete\"`\n\n        **If FAILED:**\n        - If `retry` < `maxRetries`: set `\"phase\": \"building\"`, increment `\"retry\"` by 1\n        - If `retry` >= `maxRetries`: set `\"phase\": \"failed\"`, set `\"status\": \"failed\"`\n\n        **IMPORTANT**: Kill all background processes before finishing:\n        ```bash\n        pkill -f \"node|bun|python|npm|next|vite|webpack\" 2>/dev/null || true\n        ```\n\n        ---\n\n        ## COMPLETION\n\n        After updating state.json, check the `status` field:\n        - If `\"status\": \"complete\"` → all sprints passed! Output: `<promise>ALL_SPRINTS_COMPLETE</promise>`\n        - If `\"status\": \"failed\"` → sprint failed after max retries. Output: `<promise>ALL_SPRINTS_COMPLETE</promise>`\n        - If `\"status\": \"running\"` → more work to do. Do NOT output any completion signal.\n\n      until: ALL_SPRINTS_COMPLETE\n      max_iterations: 60\n      fresh_context: true\n      until_bash: |\n        grep -qE '\"status\"\\s*:\\s*\"(complete|failed)\"' \"$ARTIFACTS_DIR/state.json\"\n\n  # ─── Phase 4: Report ─────────────────────────────────────────────────\n  - id: report\n    depends_on: [adversarial-sprint]\n    trigger_rule: all_done\n    context: fresh\n    model: haiku\n    prompt: |\n      You are a project reporter. Generate a comprehensive summary of the adversarial development run.\n\n      ## Read ALL of these files:\n      1. `$ARTIFACTS_DIR/state.json` — final state (tells you success/failure, sprint count)\n      2. `$ARTIFACTS_DIR/spec.md` — the original product spec\n      3. All files in `$ARTIFACTS_DIR/contracts/` — sprint contracts (use Glob to find them)\n      4. All files in `$ARTIFACTS_DIR/feedback/` — evaluation results (use Glob to find them)\n\n      ## Generate a report covering:\n\n      ### Build Summary\n      - What application was built (from the spec)\n      - Final status: did all sprints pass or did it fail? On which sprint?\n      - Total sprints completed vs planned\n\n      ### Per-Sprint Breakdown\n      For each sprint that was attempted:\n      - What the contract required (features + key criteria)\n      - How many attempts were needed (retry count)\n      - Final scores for each criterion\n      - Key feedback that drove retries and improvements\n\n      ### Quality Metrics\n      - Average score across all final-round criteria\n      - Which criteria required the most retries\n      - Where the adversarial evaluator pushed quality the highest\n\n      ### How to Run\n      - The application code lives in: `$ARTIFACTS_DIR/app/`\n      - Include the tech stack and how to start the app (from the spec)\n      - Include any setup steps (install deps, env vars, etc.)\n\n      Write this report to `$ARTIFACTS_DIR/report.md` AND output it as your response so the user\n      sees it directly.\n    allowed_tools: [Read, Write, Glob, Grep]\n",
  "archon-architect": "name: archon-architect\ndescription: |\n  Use when: User wants an architectural sweep, complexity reduction, or codebase health improvement.\n  Triggers: \"architect\", \"simplify codebase\", \"reduce complexity\", \"architectural sweep\",\n            \"clean up architecture\", \"codebase health\", \"fix architecture\".\n  Does: Scans codebase metrics -> analyzes architecture with principled lens -> plans targeted\n        simplifications -> executes fixes with self-review loops (hooks) -> validates -> creates PR.\n  NOT for: Single-file fixes, feature development, bug fixes, PR reviews.\n\n  DAG workflow showcasing per-node hooks:\n  - PostToolUse hooks create organic quality loops (lint after write, self-review)\n  - PreToolUse hooks inject architectural principles before changes\n  - Different nodes have different trust levels and steering\n\nprovider: claude\n\nnodes:\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 1: MEASURE\n  # Gather raw metrics — file sizes, complexity hotspots, dependency fan-out\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: scan-metrics\n    bash: |\n      echo \"=== FILE SIZE HOTSPOTS (top 30 largest source files) ===\"\n      find . -name '*.ts' -not -path '*/node_modules/*' -not -path '*/.git/*' -not -path '*/dist/*' \\\n        -exec wc -l {} + 2>/dev/null | sort -rn | head -30\n\n      echo \"\"\n      echo \"=== IMPORT FAN-OUT (files with most imports) ===\"\n      for f in $(find . -name '*.ts' -not -path '*/node_modules/*' -not -path '*/.git/*' -not -path '*/dist/*'); do\n        count=$(grep -c \"^import \" \"$f\" 2>/dev/null) || count=0\n        if [ \"$count\" -gt 8 ]; then\n          echo \"$count imports: $f\"\n        fi\n      done | sort -rn | head -20\n\n      echo \"\"\n      echo \"=== EXPORT FAN-OUT (files with most exports) ===\"\n      for f in $(find . -name '*.ts' -not -path '*/node_modules/*' -not -path '*/.git/*' -not -path '*/dist/*'); do\n        count=$(grep -c \"^export \" \"$f\" 2>/dev/null) || count=0\n        if [ \"$count\" -gt 5 ]; then\n          echo \"$count exports: $f\"\n        fi\n      done | sort -rn | head -20\n\n      echo \"\"\n      echo \"=== FUNCTION LENGTH HOTSPOTS (functions over 50 lines) ===\"\n      grep -rn \"^\\(export \\)\\?\\(async \\)\\?function \\|=> {$\" \\\n        --include='*.ts' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=dist . 2>/dev/null \\\n        | head -30\n\n      echo \"\"\n      echo \"=== TYPE SAFETY GAPS ===\"\n      echo \"any usage:\"\n      grep -rn \": any\\b\\|as any\\b\" --include='*.ts' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=dist . 2>/dev/null | wc -l\n      echo \"eslint-disable comments:\"\n      grep -rn \"eslint-disable\" --include='*.ts' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=dist . 2>/dev/null | wc -l\n    timeout: 60000\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 2: ANALYZE\n  # Read through hotspots with an architectural lens\n  # Hooks inject assessment criteria after every file read\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: analyze\n    prompt: |\n      You are a senior software architect performing a codebase health assessment.\n\n      ## Codebase Metrics\n\n      $scan-metrics.output\n\n      ## User Focus\n\n      $ARGUMENTS\n\n      ## Instructions\n\n      1. Read the top 10-15 files flagged by the metrics above (largest, most imports, most exports)\n      2. For each file, assess the criteria injected after you read it (you'll see them)\n      3. Build a running list of architectural concerns\n      4. Focus on:\n         - Modules doing too many things (SRP violations)\n         - Abstractions that don't earn their complexity\n         - Duplicated patterns that should be consolidated (Rule of Three)\n         - God files or god functions\n         - Leaky abstractions or tight coupling between layers\n         - Dead code or unused exports\n      5. Do NOT suggest changes yet — only diagnose\n\n      ## Output\n\n      Write a structured assessment to $ARTIFACTS_DIR/architecture-assessment.md with:\n      - Executive summary (3-5 sentences)\n      - Top findings ranked by impact\n      - For each finding: file, what's wrong, why it matters, estimated effort\n    depends_on: [scan-metrics]\n    context: fresh\n    denied_tools: [Write, Edit, Bash]\n    hooks:\n      PostToolUse:\n        - matcher: \"Read\"\n          response:\n            hookSpecificOutput:\n              hookEventName: PostToolUse\n              additionalContext: >\n                For the file you just read, assess:\n                (1) Single responsibility — does this module do exactly one thing?\n                (2) Cognitive load — could a new team member understand this in 5 minutes?\n                (3) Abstraction value — does every abstraction earn its complexity, or is it premature?\n                (4) Dependency direction — does this file depend on things at its own level or below, not above?\n                Add any concerns to your running list. Be specific — cite line ranges and function names.\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 3: PLAN\n  # Prioritize and scope the changes — pure reasoning, no tools\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: plan\n    prompt: |\n      You are planning targeted architectural improvements.\n\n      ## Assessment\n\n      $analyze.output\n\n      ## Principles\n\n      - KISS: prefer straightforward over clever\n      - YAGNI: remove speculative abstractions\n      - Rule of Three: only extract when a pattern appears 3+ times\n      - Each change must be independently revertable\n      - Do NOT mix refactoring with behavior changes\n      - Scope to what can be done safely in one pass (max 5-7 files)\n\n      ## Instructions\n\n      1. From the assessment, select the top 3-5 highest-impact, lowest-risk improvements\n      2. For each, write a precise plan: which file, what to change, why\n      3. Order them so each change is independent (no cascading dependencies between changes)\n      4. Estimate blast radius — how many other files are affected\n\n      ## Output\n\n      Write the plan as a numbered list. Be specific about exactly what code to change.\n      Keep it concise — the implement node will follow this literally.\n    depends_on: [analyze]\n    allowed_tools: [Read]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 4: EXECUTE\n  # Make the changes with hooks creating quality feedback loops\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: simplify\n    prompt: |\n      You are implementing targeted architectural simplifications.\n\n      ## Plan\n\n      $plan.output\n\n      ## Rules\n\n      - Follow the plan exactly — do not add extra improvements you notice along the way\n      - Each change must preserve existing behavior (refactor only, no feature changes)\n      - After each file edit, you'll be prompted to validate — follow those instructions\n      - If a change turns out to be harder than expected, skip it and move on\n      - Commit each logical change separately with a clear commit message\n\n      ## Instructions\n\n      1. Work through the plan items in order\n      2. For each item: read the file, make the change, follow the post-edit checklist\n      3. After all changes, do a final `git diff --stat` to verify scope\n    depends_on: [plan]\n    context: fresh\n    hooks:\n      PreToolUse:\n        - matcher: \"Write|Edit\"\n          response:\n            hookSpecificOutput:\n              hookEventName: PreToolUse\n              additionalContext: >\n                Before writing: Is this file in your plan? If not, explain why you're\n                touching it. Check how many files import from this module — changes to\n                widely-imported modules need extra scrutiny.\n      PostToolUse:\n        - matcher: \"Write|Edit\"\n          response:\n            systemMessage: >\n              You just modified a file. Do these things NOW before moving on:\n              1. Run the type checker to verify your change compiles\n              2. Re-read the file you changed — is it ACTUALLY simpler, or did you just move complexity around?\n              3. State in ONE sentence why this change reduces complexity. If you cannot justify it, revert it.\n        - matcher: \"Read\"\n          response:\n            hookSpecificOutput:\n              hookEventName: PostToolUse\n              additionalContext: >\n                Before modifying this file, consider: will your change reduce or increase\n                the number of concepts a reader needs to hold in their head?\n        - matcher: \"Bash\"\n          response:\n            hookSpecificOutput:\n              hookEventName: PostToolUse\n              additionalContext: >\n                Check the exit code. If the command failed, diagnose the root cause\n                before attempting a fix. Do not blindly retry.\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 5: VALIDATE\n  # Run full validation suite — bash only, cannot edit to \"fix\" failures\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: validate\n    bash: |\n      echo \"=== TYPE CHECK ===\"\n      bun run type-check 2>&1\n      TC_EXIT=$?\n\n      echo \"\"\n      echo \"=== LINT ===\"\n      bun run lint 2>&1\n      LINT_EXIT=$?\n\n      echo \"\"\n      echo \"=== TESTS ===\"\n      bun run test 2>&1\n      TEST_EXIT=$?\n\n      echo \"\"\n      echo \"=== RESULTS ===\"\n      echo \"Type check: $([ $TC_EXIT -eq 0 ] && echo 'PASS' || echo 'FAIL')\"\n      echo \"Lint: $([ $LINT_EXIT -eq 0 ] && echo 'PASS' || echo 'FAIL')\"\n      echo \"Tests: $([ $TEST_EXIT -eq 0 ] && echo 'PASS' || echo 'FAIL')\"\n\n      # Always exit 0 so downstream nodes can read output and decide\n      if [ $TC_EXIT -eq 0 ] && [ $LINT_EXIT -eq 0 ] && [ $TEST_EXIT -eq 0 ]; then\n        echo \"VALIDATION_STATUS: PASS\"\n      else\n        echo \"VALIDATION_STATUS: FAIL\"\n      fi\n    depends_on: [simplify]\n    timeout: 300000\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 6: FIX VALIDATION FAILURES (if any)\n  # Only runs if validate failed — focused fix with same quality hooks\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: fix-failures\n    prompt: |\n      Review the validation output below.\n\n      ## Validation Output\n\n      $validate.output\n\n      ## Instructions\n\n      If the output ends with \"VALIDATION_STATUS: PASS\", respond with\n      \"All checks passed — no fixes needed.\" and stop.\n\n      If there are failures:\n\n      1. Read the validation failures carefully\n      2. Fix ONLY what's broken — do not make additional improvements\n      3. If a fix requires changing behavior (not just fixing a type/lint error),\n         revert the original change instead\n      4. Run the specific failing check after each fix to confirm it passes\n      5. After all fixes, run the full validation suite: `bun run validate`\n    depends_on: [validate]\n    context: fresh\n    hooks:\n      PostToolUse:\n        - matcher: \"Write|Edit\"\n          response:\n            systemMessage: >\n              You just made a fix. Run the specific failing validation check NOW\n              to verify your fix works. Do not batch fixes — verify each one.\n      PreToolUse:\n        - matcher: \"Write|Edit\"\n          response:\n            hookSpecificOutput:\n              hookEventName: PreToolUse\n              additionalContext: >\n                You are fixing validation failures only. Do not make any changes\n                beyond what's needed to pass the failing checks. If in doubt, revert\n                the original change that caused the failure.\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 7: CREATE PR\n  # Hooks ensure this node only does git operations\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: create-pr\n    prompt: |\n      Create a pull request for the architectural improvements.\n\n      ## Context\n\n      - Architecture assessment: $analyze.output\n      - Plan: $plan.output\n      - Validation: $validate.output\n\n      ## Instructions\n\n      1. Stage all changes and create a single commit (or verify existing commits)\n      2. Push the branch: `git push -u origin HEAD`\n      3. Check if a PR already exists: `gh pr list --head $(git branch --show-current)`\n      4. Create the PR targeting `$BASE_BRANCH` as the base branch:\n         `gh pr create --base $BASE_BRANCH --title \"...\" --body \"...\"`\n         - Title: concise description of what was simplified (under 70 chars)\n         - Body: use the format below\n      5. Save the PR URL to `$ARTIFACTS_DIR/.pr-url`\n\n      ## PR Body Format\n\n      ```markdown\n      ## Architectural Sweep\n\n      **Focus**: $ARGUMENTS\n\n      ### Assessment\n\n      [3-5 sentence summary from the architecture assessment]\n\n      ### Changes\n\n      [For each change: what file, what was simplified, why]\n\n      ### Validation\n\n      - [x] Type check passes\n      - [x] Lint passes\n      - [x] Tests pass\n      - [x] Each change preserves existing behavior\n      ```\n    depends_on: [fix-failures]\n    context: fresh\n    hooks:\n      PreToolUse:\n        - matcher: \"Write|Edit\"\n          response:\n            hookSpecificOutput:\n              hookEventName: PreToolUse\n              permissionDecision: deny\n              permissionDecisionReason: \"PR creation node — do not modify source files. Use only git and gh commands.\"\n      PostToolUse:\n        - matcher: \"Bash\"\n          response:\n            hookSpecificOutput:\n              hookEventName: PostToolUse\n              additionalContext: >\n                Verify this command succeeded. If git push or gh pr create failed,\n                read the error message carefully before retrying.\n\n  - id: verify-pr-base\n    bash: |\n      set -euo pipefail\n      EXPECTED=\"$BASE_BRANCH\"\n      ACTUAL=$(gh pr view --json baseRefName -q '.baseRefName')\n      if [ \"$ACTUAL\" != \"$EXPECTED\" ]; then\n        PR_NUMBER=$(gh pr view --json number -q '.number')\n        echo \"Base mismatch on PR #$PR_NUMBER: expected=$EXPECTED actual=$ACTUAL — re-targeting\" >&2\n        gh pr edit \"$PR_NUMBER\" --base \"$EXPECTED\"\n      else\n        echo \"PR base verified: $EXPECTED\"\n      fi\n    depends_on: [create-pr]\n",
  "archon-assist": "name: archon-assist\ndescription: |\n  Use when: No other workflow matches the request.\n  Handles: Questions, debugging, exploration, one-off tasks, explanations, CI failures, general help.\n  Capability: Full Claude Code agent with all tools available.\n  Note: Will inform user when assist mode is used for tracking.\n\n# Run in the live checkout, not in a fresh sub-worktree. Without this, every\n# auto-routed `archon-assist` invocation creates an isolated sub-worktree\n# whose edits are unreachable from the calling chat (no commit step, no\n# branch propagation back). With `worktree.enabled: false`, edits land in\n# the parent's working tree where syncWorkspace's #1516 fast-forward\n# default keeps them safe across chat ticks. Closes #1546.\nworktree:\n  enabled: false\n\nnodes:\n  - id: assist\n    command: archon-assist\n",
  "archon-comprehensive-pr-review": "name: archon-comprehensive-pr-review\ndescription: |\n  Use when: User wants a comprehensive code review of a pull request with automatic fixes.\n  Triggers: \"review this PR\", \"review PR #123\", \"comprehensive review\", \"full PR review\",\n            \"review and fix\", \"check this PR\", \"code review\".\n  Does: Syncs PR with main (rebase if needed) -> runs 5 specialized review agents in parallel ->\n        synthesizes findings -> auto-fixes CRITICAL/HIGH issues -> reports remaining issues.\n  NOT for: Quick questions about a PR, checking CI status, simple \"what changed\" queries.\n\n  This workflow produces artifacts in $ARTIFACTS_DIR/../reviews/pr-{number}/ and posts\n  a comprehensive review comment to the GitHub PR.\n\nnodes:\n  - id: scope\n    command: archon-pr-review-scope\n\n  - id: sync\n    command: archon-sync-pr-with-main\n    depends_on: [scope]\n\n  - id: code-review\n    command: archon-code-review-agent\n    depends_on: [sync]\n\n  - id: error-handling\n    command: archon-error-handling-agent\n    depends_on: [sync]\n\n  - id: test-coverage\n    command: archon-test-coverage-agent\n    depends_on: [sync]\n\n  - id: comment-quality\n    command: archon-comment-quality-agent\n    depends_on: [sync]\n\n  - id: docs-impact\n    command: archon-docs-impact-agent\n    depends_on: [sync]\n\n  - id: synthesize\n    command: archon-synthesize-review\n    depends_on: [code-review, error-handling, test-coverage, comment-quality, docs-impact]\n    trigger_rule: one_success\n\n  - id: implement-fixes\n    command: archon-implement-review-fixes\n    depends_on: [synthesize]\n",
  "archon-create-issue": "name: archon-create-issue\ndescription: |\n  Use when: User wants to report a bug or problem as a GitHub issue with automated reproduction.\n  Triggers: \"create issue\", \"file a bug\", \"report this bug\", \"open an issue for\",\n            \"create github issue\", \"report issue\", \"log this bug\".\n  Does: Classifies problem area (haiku) -> gathers context in parallel (templates, git state, duplicates) ->\n        investigates relevant code -> reproduces the issue using area-specific tools (agent-browser, CLI, DB queries) ->\n        gates on reproduction success -> creates issue with full evidence OR reports back if cannot reproduce.\n  NOT for: Feature requests, enhancements, or non-bug work. Only for bugs/problems.\n\n  Reproduction gating: If the issue cannot be reproduced, the workflow does NOT create an issue.\n  Instead, it reports what was tried and suggests next steps to the user.\n\nnodes:\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 1: CLASSIFY — Haiku classification of user's problem\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: classify\n    prompt: |\n      You are a problem classifier for the Archon codebase. Analyze the user's\n      description and determine the issue type and which area of the system is affected.\n\n      ## User's Description\n      $ARGUMENTS\n\n      ## Area Definitions\n      | Area | Packages | Indicators |\n      |------|----------|------------|\n      | web-ui | @archon/web, @archon/server (routes, web adapter) | UI rendering, SSE streaming, React components, browser behavior |\n      | api-server | @archon/server (routes, middleware) | HTTP endpoints, response codes, request handling |\n      | cli | @archon/cli | CLI commands, workflow invocation from terminal, output formatting |\n      | isolation | @archon/isolation, @archon/git | Worktrees, branch operations, cleanup, environment lifecycle |\n      | workflows | @archon/workflows | YAML parsing, DAG execution, variable substitution, node types |\n      | database | @archon/core (db/) | SQLite/PostgreSQL queries, schema, data integrity, migrations |\n      | adapters | @archon/adapters | Slack/Telegram/GitHub/Discord message handling, auth, polling |\n      | core | @archon/core (orchestrator, handlers, clients) | Message routing, session management, AI client streaming |\n      | other | Any package not covered above | Cross-cutting concerns, build tooling, config, unknown area |\n\n      ## Classification Rules\n      - Choose the MOST SPECIFIC area. \"SSE disconnects\" = web-ui (not api-server).\n      - If ambiguous between two areas, pick the one closer to the user-facing symptom.\n      - Use \"other\" only when the problem genuinely doesn't fit any specific area.\n      - needs_server: Set to \"true\" if reproducing requires a running Archon server.\n        Typically true for: web-ui, api-server, core, adapters.\n        Typically false for: cli, isolation, workflows, database.\n        For \"other\": use your judgment based on the description.\n      - repro_hint: Extract the user's reproduction steps into a concise instruction.\n        If no explicit steps given, infer the most likely way to trigger the issue.\n\n      Provide reasoning for your classification.\n    model: haiku\n    allowed_tools: []\n    output_format:\n      type: object\n      properties:\n        type:\n          type: string\n          enum: [\"bug\", \"regression\", \"crash\", \"performance\", \"configuration\"]\n        area:\n          type: string\n          enum: [\"web-ui\", \"api-server\", \"cli\", \"isolation\", \"workflows\", \"database\", \"adapters\", \"core\", \"other\"]\n        title:\n          type: string\n        keywords:\n          type: string\n        repro_hint:\n          type: string\n        needs_server:\n          type: string\n          enum: [\"true\", \"false\"]\n      required: [type, area, title, keywords, repro_hint, needs_server]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 2: PARALLEL CONTEXT GATHERING\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: fetch-template\n    bash: |\n      # Search for GitHub issue templates in standard locations\n      TEMPLATES_FOUND=0\n\n      # Check for issue template directory (YAML-based templates)\n      if [ -d \".github/ISSUE_TEMPLATE\" ]; then\n        echo \"=== Issue Templates Found ===\"\n        for f in .github/ISSUE_TEMPLATE/*.md .github/ISSUE_TEMPLATE/*.yaml .github/ISSUE_TEMPLATE/*.yml; do\n          if [ -f \"$f\" ]; then\n            TEMPLATES_FOUND=$((TEMPLATES_FOUND + 1))\n            echo \"--- Template: $f ---\"\n            cat \"$f\"\n            echo \"\"\n          fi\n        done\n      fi\n\n      # Check for single issue template\n      for f in .github/ISSUE_TEMPLATE.md docs/ISSUE_TEMPLATE.md; do\n        if [ -f \"$f\" ]; then\n          TEMPLATES_FOUND=$((TEMPLATES_FOUND + 1))\n          echo \"--- Template: $f ---\"\n          cat \"$f\"\n        fi\n      done\n\n      if [ \"$TEMPLATES_FOUND\" -eq 0 ]; then\n        echo \"No issue templates found — will use standard format\"\n      fi\n    depends_on: [classify]\n\n  - id: git-context\n    bash: |\n      echo \"=== Branch ===\"\n      git branch --show-current\n\n      echo \"=== Recent Commits (last 15) ===\"\n      git log --oneline -15\n\n      echo \"=== Working Tree Status ===\"\n      git status --short\n\n      echo \"=== Modified Files (last 3 commits) ===\"\n      git diff --name-only HEAD~3..HEAD 2>/dev/null || echo \"(fewer than 3 commits)\"\n\n      echo \"=== Environment ===\"\n      echo \"Node: $(node --version 2>/dev/null || echo 'N/A')\"\n      echo \"Bun: $(bun --version 2>/dev/null || echo 'N/A')\"\n      echo \"OS: $(uname -s 2>/dev/null || echo 'Windows') $(uname -r 2>/dev/null || ver 2>/dev/null || echo '')\"\n      echo \"Platform: $(uname -m 2>/dev/null || echo 'unknown')\"\n    depends_on: [classify]\n\n  - id: dedup-check\n    bash: |\n      KEYWORDS=$classify.output.keywords\n      echo \"=== Searching for duplicates: $KEYWORDS ===\"\n\n      echo \"--- Open Issues ---\"\n      gh issue list --search \"$KEYWORDS\" --state open --limit 5 --json number,title,url,labels 2>/dev/null || echo \"No open matches\"\n\n      echo \"--- Recently Closed ---\"\n      gh issue list --search \"$KEYWORDS\" --state closed --limit 3 --json number,title,url,labels 2>/dev/null || echo \"No closed matches\"\n    depends_on: [classify]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 3: INVESTIGATE — Search codebase for related code\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: investigate\n    prompt: |\n      You are a codebase investigator. Search for code related to the reported problem.\n\n      ## Problem\n      - **Area**: $classify.output.area\n      - **Type**: $classify.output.type\n      - **Title**: $classify.output.title\n      - **Reproduction hint**: $classify.output.repro_hint\n\n      ## Git Context\n      $git-context.output\n\n      ## Instructions\n\n      1. Based on the area, search the relevant packages:\n         - web-ui: `packages/web/src/`, `packages/server/src/adapters/web/`, `packages/server/src/routes/`\n         - api-server: `packages/server/src/routes/`, `packages/server/src/`\n         - cli: `packages/cli/src/`\n         - isolation: `packages/isolation/src/`, `packages/git/src/`\n         - workflows: `packages/workflows/src/`\n         - database: `packages/core/src/db/`\n         - adapters: `packages/adapters/src/`\n         - core: `packages/core/src/orchestrator/`, `packages/core/src/handlers/`\n         - other: search broadly based on keywords — check `packages/*/src/`, config files, build scripts\n\n      2. Find: entry points, error handling paths, related type definitions, recent changes\n         to the affected area (check git log for the specific files).\n\n      3. Write your findings to `$ARTIFACTS_DIR/issue-context.md` with this structure:\n         ```\n         # Codebase Investigation\n         ## Relevant Files\n         - `file:line` — description of what's there\n         ## Error Handling\n         - How errors are currently handled in this area\n         ## Recent Changes\n         - Any recent commits touching this code\n         ## Suspected Root Cause\n         - Based on code analysis, where the bug likely is\n         ```\n\n      Be thorough but focused. Only include files directly relevant to the reported problem.\n    depends_on: [classify, git-context]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 4: REPRODUCE — Area-specific issue reproduction\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: start-server\n    bash: |\n      # Allocate a free port using Bun's OS assignment\n      PORT=$(bun -e \"const s = Bun.serve({port: 0, fetch: () => new Response('')}); console.log(s.port); s.stop()\")\n      echo \"$PORT\" > \"$ARTIFACTS_DIR/.server-port\"\n\n      # Start dev server in background\n      PORT=$PORT bun run dev:server > \"$ARTIFACTS_DIR/.server-log\" 2>&1 &\n      SERVER_PID=$!\n      echo \"$SERVER_PID\" > \"$ARTIFACTS_DIR/.server-pid\"\n\n      # Wait for server to be ready (up to 30s)\n      for i in $(seq 1 30); do\n        if curl -s \"http://localhost:$PORT/api/health\" > /dev/null 2>&1; then\n          echo \"Server ready on port $PORT (PID: $SERVER_PID)\"\n          exit 0\n        fi\n        sleep 1\n      done\n\n      echo \"WARNING: Server may not be fully ready after 30s (port $PORT, PID $SERVER_PID)\"\n      echo \"Continuing anyway — reproduce node will handle connection errors\"\n    depends_on: [classify]\n    when: \"$classify.output.needs_server == 'true'\"\n    timeout: 45000\n\n  - id: reproduce\n    prompt: |\n      You are an issue reproduction specialist. Your job is to reproduce the reported\n      problem and capture evidence (screenshots, command output, error messages).\n\n      ## Problem Context\n      - **Area**: $classify.output.area\n      - **Type**: $classify.output.type\n      - **Title**: $classify.output.title\n      - **Reproduction hint**: $classify.output.repro_hint\n\n      ## Investigation Findings\n      $investigate.output\n\n      ## Server Info\n      If a server was started, read the port from: `cat \"$ARTIFACTS_DIR/.server-port\"`\n      If the file doesn't exist, no server is running (area doesn't need one).\n\n      ---\n\n      ## Reproduction Playbooks\n\n      Follow the playbook matching the area. Capture ALL evidence to `$ARTIFACTS_DIR/`.\n\n      ### web-ui\n      1. Read the server port: `PORT=$(cat \"$ARTIFACTS_DIR/.server-port\" | tr -d '\\n')`\n      2. Open the app: `agent-browser open http://localhost:$PORT`\n      3. Take a baseline screenshot: `agent-browser screenshot \"$ARTIFACTS_DIR/repro-01-baseline.png\"`\n      4. Get interactive elements: `agent-browser snapshot -i`\n      5. Navigate to the area related to the issue (use @refs from snapshot)\n      6. Perform the actions described in the repro_hint\n      7. Screenshot each significant state: `agent-browser screenshot \"$ARTIFACTS_DIR/repro-02-action.png\"`\n      8. If an error appears, capture it: `agent-browser get text @errorElement`\n      9. Check browser console: `agent-browser console`\n      10. Check for JS errors: `agent-browser errors`\n      11. Final screenshot: `agent-browser screenshot \"$ARTIFACTS_DIR/repro-03-result.png\"`\n      12. Close browser: `agent-browser close`\n\n      ### api-server\n      1. Read the server port: `PORT=$(cat \"$ARTIFACTS_DIR/.server-port\" | tr -d '\\n')`\n      2. Create a test conversation: `curl -s -X POST http://localhost:$PORT/api/conversations -H \"Content-Type: application/json\" -d '{}'`\n      3. Hit the problematic endpoint based on the repro_hint\n      4. Capture response codes and bodies: `curl -s -w \"\\nHTTP_CODE: %{http_code}\\n\" ...`\n      5. For SSE issues: `curl -s -N http://localhost:$PORT/api/stream/<id>` (timeout after 10s)\n      6. Check server logs: `cat \"$ARTIFACTS_DIR/.server-log\" | tail -50`\n      7. Save all curl output to `$ARTIFACTS_DIR/repro-api-responses.txt`\n\n      ### cli\n      1. Run the CLI command that should trigger the issue\n      2. Capture stdout and stderr separately:\n         `bun run cli <command> > \"$ARTIFACTS_DIR/repro-cli-stdout.txt\" 2> \"$ARTIFACTS_DIR/repro-cli-stderr.txt\"; echo \"EXIT_CODE: $?\" >> \"$ARTIFACTS_DIR/repro-cli-stdout.txt\"`\n      3. If workflow-related: `bun run cli workflow list --json > \"$ARTIFACTS_DIR/repro-workflow-list.json\" 2>&1`\n      4. If the command hangs, use timeout: `timeout 30 bun run cli <command>`\n      5. Check for error messages in output\n\n      ### isolation\n      1. Check current state: `bun run cli isolation list > \"$ARTIFACTS_DIR/repro-isolation-list.txt\" 2>&1`\n      2. Check git worktrees: `git worktree list > \"$ARTIFACTS_DIR/repro-worktree-list.txt\"`\n      3. Check branches: `git branch -a > \"$ARTIFACTS_DIR/repro-branches.txt\"`\n      4. Try the operation that should fail (based on repro_hint)\n      5. Capture the error output\n      6. Query isolation DB: `sqlite3 ~/.archon/archon.db \"SELECT * FROM remote_agent_isolation_environments ORDER BY created_at DESC LIMIT 10\" > \"$ARTIFACTS_DIR/repro-isolation-db.txt\" 2>&1`\n\n      ### workflows\n      1. List workflows: `bun run cli workflow list --json > \"$ARTIFACTS_DIR/repro-workflow-list.json\" 2>&1`\n      2. If a specific workflow is mentioned, try running it:\n         `bun run cli workflow run <name> --no-worktree \"test input\" > \"$ARTIFACTS_DIR/repro-workflow-run.txt\" 2>&1`\n      3. If YAML parsing is the issue, try loading the definition directly\n      4. Check for error messages in execution output\n\n      ### database\n      1. Check DB exists: `ls -la ~/.archon/archon.db 2>/dev/null`\n      2. Run targeted queries against affected tables:\n         - `sqlite3 ~/.archon/archon.db \".schema <table>\" > \"$ARTIFACTS_DIR/repro-db-schema.txt\"`\n         - `sqlite3 ~/.archon/archon.db \"SELECT COUNT(*) FROM <table>\" > \"$ARTIFACTS_DIR/repro-db-counts.txt\"`\n      3. Check for the specific data condition described in the repro_hint\n      4. If PostgreSQL: use `psql $DATABASE_URL -c \"...\"` instead\n\n      ### adapters\n      1. Read the server port: `PORT=$(cat \"$ARTIFACTS_DIR/.server-port\" | tr -d '\\n')`\n      2. Check adapter configuration: look for relevant env vars in `.env`\n      3. Check server startup logs: `cat \"$ARTIFACTS_DIR/.server-log\" | grep -i \"adapter\\|slack\\|telegram\\|github\\|discord\" | head -20`\n      4. If the adapter fails to initialize, capture the error\n      5. Test message routing via web API as a proxy:\n         `curl -s -X POST http://localhost:$PORT/api/conversations/<id>/message -H \"Content-Type: application/json\" -d '{\"message\":\"/status\"}'`\n\n      ### core\n      1. Read the server port: `PORT=$(cat \"$ARTIFACTS_DIR/.server-port\" | tr -d '\\n')`\n      2. Create a conversation: `curl -s -X POST http://localhost:$PORT/api/conversations -H \"Content-Type: application/json\" -d '{}'`\n      3. Send a message that triggers the issue:\n         `curl -s -X POST http://localhost:$PORT/api/conversations/<id>/message -H \"Content-Type: application/json\" -d '{\"message\":\"<repro_hint>\"}'`\n      4. Poll for responses: `curl -s http://localhost:$PORT/api/conversations/<id>/messages`\n      5. Check session state in DB: `sqlite3 ~/.archon/archon.db \"SELECT * FROM remote_agent_sessions WHERE conversation_id='<id>'\" 2>/dev/null`\n      6. Check server logs: `cat \"$ARTIFACTS_DIR/.server-log\" | tail -50`\n\n      ### other\n      1. Run `bun run validate` to check for any obvious failures — capture output:\n         `bun run validate > \"$ARTIFACTS_DIR/repro-validate.txt\" 2>&1; echo \"EXIT_CODE: $?\" >> \"$ARTIFACTS_DIR/repro-validate.txt\"`\n      2. Search the codebase for keywords from the repro_hint:\n         - Use Grep/Glob to find related files\n         - Check recent git log for relevant changes\n      3. If the description implies a build or config issue:\n         - Check `package.json` scripts, `tsconfig.json`, `.env.example`\n         - Try running the relevant build/dev command\n      4. If the description implies a runtime issue:\n         - Start the server (if `.server-port` file exists) and try to trigger the behavior\n         - Check logs for errors\n      5. Document everything you tried, even if nothing reproduces clearly\n\n      ---\n\n      ## Output\n\n      After following the playbook, write your evidence to `$ARTIFACTS_DIR/reproduction-results.md`\n      using this structure (referenced in the issue body):\n\n      ```markdown\n      # Reproduction Results\n\n      ## Steps Taken\n      1. [step]\n      2. [step]\n\n      ## Expected Behavior\n      [what should happen]\n\n      ## Actual Behavior\n      [what actually happened — or \"could not trigger the reported behavior\"]\n\n      ## Evidence Files\n      - `$ARTIFACTS_DIR/repro-*.png` — screenshots (if web-ui)\n      - `$ARTIFACTS_DIR/repro-*.txt` — command output\n      - `$ARTIFACTS_DIR/repro-*.json` — structured data\n\n      ## Environment\n      [OS, versions, relevant config]\n\n      ## Notes\n      [any additional observations, suspected root cause refinements]\n      ```\n\n      Then return structured output with three fields:\n      - **status**: Exactly one of `REPRODUCED`, `NOT_REPRODUCED`, or `PARTIAL`. This value\n        controls whether a GitHub issue gets created. The SDK enforces this shape.\n      - **reproduced**: Boolean (`true` if REPRODUCED, `false` otherwise).\n      - **summary**: A 3-6 sentence prose narrative of what was tried and what happened.\n        This appears verbatim in the GitHub issue body.\n\n      Even if you cannot fully reproduce the issue, document what you tried and observed.\n      Partial reproduction is still valuable evidence.\n    depends_on: [classify, git-context, investigate, start-server]\n    context: fresh\n    skills:\n      - agent-browser\n    trigger_rule: one_success\n    idle_timeout: 300000\n    output_format:\n      type: object\n      properties:\n        status:\n          type: string\n          enum: [\"REPRODUCED\", \"NOT_REPRODUCED\", \"PARTIAL\"]\n        reproduced:\n          type: boolean\n        summary:\n          type: string\n      required: [status, reproduced, summary]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 5: CLEANUP + GATE\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: cleanup-server\n    bash: |\n      SERVER_PID=$(cat \"$ARTIFACTS_DIR/.server-pid\" 2>/dev/null | tr -d '\\n')\n      SERVER_PORT=$(cat \"$ARTIFACTS_DIR/.server-port\" 2>/dev/null | tr -d '\\n')\n\n      if [ -z \"$SERVER_PID\" ]; then\n        echo \"No server was started — skipping cleanup\"\n        exit 0\n      fi\n\n      echo \"Cleaning up server PID $SERVER_PID on port $SERVER_PORT...\"\n\n      # Kill by PID (cross-platform)\n      kill \"$SERVER_PID\" 2>/dev/null || taskkill //F //T //PID \"$SERVER_PID\" 2>/dev/null || true\n\n      # Kill by port (fallback)\n      if [ -n \"$SERVER_PORT\" ]; then\n        fuser -k \"$SERVER_PORT/tcp\" 2>/dev/null || true\n        lsof -ti:\"$SERVER_PORT\" 2>/dev/null | xargs kill -9 2>/dev/null || true\n        netstat -ano 2>/dev/null | grep \":$SERVER_PORT \" | grep LISTENING | awk '{print $5}' | sort -u | while read pid; do\n          taskkill //F //T //PID \"$pid\" 2>/dev/null || true\n        done\n      fi\n\n      # Close any agent-browser session\n      agent-browser close 2>/dev/null || true\n\n      sleep 1\n      echo \"Cleanup complete\"\n    depends_on: [reproduce]\n    trigger_rule: all_done\n\n  - id: check-reproduction\n    bash: |\n      STATUS=\"$reproduce.output.status\"\n      case \"$STATUS\" in\n        REPRODUCED|NOT_REPRODUCED|PARTIAL)\n          echo \"$STATUS\"\n          ;;\n        *)\n          echo \"NOT_REPRODUCED\"\n          ;;\n      esac\n    depends_on: [cleanup-server]\n    trigger_rule: all_done\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 6: BRANCH ON REPRODUCTION RESULT\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: report-failure\n    prompt: |\n      The issue could not be reproduced. Report this to the user with actionable detail.\n\n      ## Problem Description\n      - **Title**: $classify.output.title\n      - **Area**: $classify.output.area\n      - **Type**: $classify.output.type\n      - **Reproduction hint**: $classify.output.repro_hint\n\n      ## What Was Tried\n      $reproduce.output\n\n      ## Investigation Findings\n      $investigate.output\n\n      ## Instructions\n\n      Report to the user clearly:\n\n      1. **State upfront**: \"Could not reproduce the reported issue. No GitHub issue was created.\"\n\n      2. **Summarize what was tried**: List the specific steps the reproduce node took,\n         based on the area playbook. Be concrete — \"Started server on port X, navigated to Y,\n         clicked Z — no error appeared.\"\n\n      3. **Share what was found**: Include relevant findings from the investigation\n         (code references, recent changes, suspected areas).\n\n      4. **Suggest next steps**:\n         - Ask the user to provide more specific reproduction steps\n         - Mention any environment-specific factors that might matter\n           (OS, browser, database state, specific data conditions)\n         - If the investigation found suspicious code, mention it as a lead\n         - Suggest running with debug logging: `LOG_LEVEL=debug bun run dev`\n\n      5. **Offer to retry**: \"If you can provide more specific steps, run the workflow\n         again with those details.\"\n\n      Do NOT create a GitHub issue. The purpose of this node is to communicate back to the\n      user so they can provide better information or investigate manually.\n    depends_on: [check-reproduction]\n    when: \"$check-reproduction.output == 'NOT_REPRODUCED'\"\n    context: fresh\n\n  - id: draft-issue\n    prompt: |\n      You are a technical writer drafting a GitHub issue. Assemble all gathered\n      context into a clear, well-structured issue body.\n\n      ## Classification\n      - **Type**: $classify.output.type\n      - **Area**: $classify.output.area\n      - **Title**: $classify.output.title\n\n      ## Issue Template\n      If templates were found, use the most appropriate one as the structure:\n      $fetch-template.output\n\n      ## Duplicate Check Results\n      $dedup-check.output\n\n      ## Codebase Investigation\n      $investigate.output\n\n      ## Reproduction Results\n      $reproduce.output\n\n      ## Instructions\n\n      1. **Check duplicates first**: If the dedup-check found a clearly matching open issue,\n         note this prominently at the top. Still draft the issue but add a note suggesting\n         it may be a duplicate of #XYZ.\n\n      2. **Use the template** if one was found for bug reports. Fill every section with real data.\n\n      3. **Structure** (if no template):\n         ```markdown\n         ## Description\n         [Clear 1-2 sentence description]\n\n         ## Steps to Reproduce\n         [Numbered steps from reproduction results]\n\n         ## Expected Behavior\n         [What should happen]\n\n         ## Actual Behavior\n         [What actually happened, with evidence]\n\n         ## Environment\n         - OS: [from git-context]\n         - Bun: [version]\n         - Node: [version]\n         - Branch: [current branch]\n\n         ## Relevant Code\n         [Key file:line references from investigation]\n\n         ## Additional Context\n         [Screenshots, logs, database state — reference artifact files]\n         ```\n\n      4. **Include reproduction evidence**:\n         - If REPRODUCED: include full steps and all evidence\n         - If PARTIAL: include what was observed, note incomplete reproduction\n\n      5. **Suggest labels** based on classification:\n         - Area label: `area: web`, `area: cli`, `area: workflows`, etc.\n         - Type label: `bug`, `regression`, `performance`, etc.\n\n      6. Write the complete issue body to `$ARTIFACTS_DIR/issue-draft.md`\n\n      7. Write a one-line suggested title to `$ARTIFACTS_DIR/.issue-title`\n\n      8. Write suggested labels (comma-separated) to `$ARTIFACTS_DIR/.issue-labels`\n    depends_on: [check-reproduction, fetch-template, dedup-check, investigate]\n    when: \"$check-reproduction.output != 'NOT_REPRODUCED'\"\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 7: CREATE ISSUE\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: create-issue\n    prompt: |\n      Create the GitHub issue using the drafted content.\n\n      ## Instructions\n\n      1. Read the draft: `cat \"$ARTIFACTS_DIR/issue-draft.md\"`\n      2. Read the title: `cat \"$ARTIFACTS_DIR/.issue-title\"`\n      3. Read suggested labels: `cat \"$ARTIFACTS_DIR/.issue-labels\"`\n\n      4. Check which labels actually exist in the repo:\n         ```bash\n         gh label list --json name -q '.[].name' | head -50\n         ```\n         Only use labels that exist. Skip any suggested label that doesn't match.\n\n      5. Create the issue:\n         ```bash\n         gh issue create \\\n           --title \"$(cat \"$ARTIFACTS_DIR/.issue-title\")\" \\\n           --body-file \"$ARTIFACTS_DIR/issue-draft.md\" \\\n           --label \"label1,label2\"\n         ```\n\n      6. Capture the result:\n         ```bash\n         ISSUE_URL=$(gh issue list --limit 1 --json url -q '.[0].url')\n         echo \"$ISSUE_URL\" > \"$ARTIFACTS_DIR/.issue-url\"\n         ```\n\n      7. Report to the user:\n         - Issue URL\n         - Title\n         - Labels applied\n         - Whether duplicates were found\n         - Summary of reproduction results (reproduced/partial)\n    depends_on: [draft-issue]\n    context: fresh\n",
  "archon-feature-development": "name: archon-feature-development\ndescription: |\n  Use when: Implementing a feature from an existing plan.\n  Input: Path to a plan file ($ARTIFACTS_DIR/plan.md) or GitHub issue containing a plan.\n  Does: Implements the plan with validation loops -> creates pull request.\n  NOT for: Creating plans (plans should be created separately), bug fixes, code reviews.\n\nnodes:\n  - id: implement\n    command: archon-implement\n    model: opus[1m]\n\n  - id: create-pr\n    command: archon-create-pr\n    depends_on: [implement]\n    context: fresh\n\n  - id: verify-pr-base\n    bash: |\n      set -euo pipefail\n      EXPECTED=\"$BASE_BRANCH\"\n      ACTUAL=$(gh pr view --json baseRefName -q '.baseRefName')\n      if [ \"$ACTUAL\" != \"$EXPECTED\" ]; then\n        PR_NUMBER=$(gh pr view --json number -q '.number')\n        echo \"Base mismatch on PR #$PR_NUMBER: expected=$EXPECTED actual=$ACTUAL — re-targeting\" >&2\n        gh pr edit \"$PR_NUMBER\" --base \"$EXPECTED\"\n      else\n        echo \"PR base verified: $EXPECTED\"\n      fi\n    depends_on: [create-pr]\n",
  "archon-fix-github-issue": "name: archon-fix-github-issue\ndescription: |\n  Use when: User wants to FIX, RESOLVE, or IMPLEMENT a solution for a GitHub issue.\n  Triggers: \"fix this issue\", \"implement issue #123\", \"resolve this bug\", \"fix it\",\n            \"fix issue\", \"resolve issue\", \"fix #123\".\n  NOT for: Comprehensive multi-agent reviews (use archon-issue-review-full),\n           questions about issues, CI failures, PR reviews, general exploration.\n\n  DAG workflow that:\n  1. Classifies the issue (bug/feature/enhancement/etc)\n  2. Researches context (web research + codebase exploration via investigate/plan)\n  3. Routes to investigate (bugs) or plan (features) based on classification\n  4. Implements the fix/feature with validation\n  5. Creates a draft PR using the repo's PR template\n  6. Runs smart review (always code review + CLAUDE.md check, conditional additional agents)\n  7. Aggressively self-fixes all findings (tests, docs, error handling)\n  8. Simplifies changed code (implements fixes directly, not just reports)\n  9. Reports results back to the GitHub issue with follow-up suggestions\n\nprovider: claude\nmodel: sonnet\n\nnodes:\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 1: FETCH & CLASSIFY\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: extract-issue-number\n    prompt: |\n      Find the GitHub issue number for this request.\n\n      Request: $ARGUMENTS\n\n      Rules:\n      - If the message contains an explicit issue number (e.g., \"#709\", \"issue 709\", \"709\"), extract that number.\n      - If the message is ambiguous (e.g., \"fix the SQLite timestamp bug\"), use `gh issue list` to search for matching issues and pick the best match.\n\n      CRITICAL: Your final output must be ONLY the bare number with no quotes, no markdown, no explanation. Example correct output: 709\n\n  - id: fetch-issue\n    bash: |\n      # Strip quotes, whitespace, markdown backticks from AI output\n      ISSUE_NUM=$(echo \"$extract-issue-number.output\" | tr -d \"'\\\"\\`\\n \" | grep -oE '[0-9]+' | head -1)\n      if [ -z \"$ISSUE_NUM\" ]; then\n        echo \"Failed to extract issue number from: $extract-issue-number.output\" >&2\n        exit 1\n      fi\n      gh issue view \"$ISSUE_NUM\" --json title,body,labels,comments,state,url,author\n    depends_on: [extract-issue-number]\n\n  - id: classify\n    prompt: |\n      You are an issue classifier. Analyze the GitHub issue below and determine its type.\n\n      ## Issue Content\n\n      $fetch-issue.output\n\n      ## Classification Rules\n\n      | Type | Indicators |\n      |------|------------|\n      | bug | \"broken\", \"error\", \"crash\", \"doesn't work\", stack traces, regression |\n      | feature | \"add\", \"new\", \"support\", \"would be nice\", net-new capability |\n      | enhancement | \"improve\", \"better\", \"update existing\", \"extend\", incremental improvement |\n      | refactor | \"clean up\", \"simplify\", \"reorganize\", \"restructure\" |\n      | chore | \"update deps\", \"upgrade\", \"maintenance\", \"CI/CD\" |\n      | documentation | \"docs\", \"readme\", \"clarify\", \"examples\" |\n\n      Provide reasoning for your classification.\n    depends_on: [fetch-issue]\n    model: haiku\n    allowed_tools: []\n    output_format:\n      type: object\n      properties:\n        issue_type:\n          type: string\n          enum: [\"bug\", \"feature\", \"enhancement\", \"refactor\", \"chore\", \"documentation\"]\n        title:\n          type: string\n        reasoning:\n          type: string\n      required: [issue_type, title, reasoning]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 2: RESEARCH (parallel with PR template fetch)\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: web-research\n    command: archon-web-research\n    depends_on: [classify]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 3: INVESTIGATE (bugs) / PLAN (features)\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: investigate\n    command: archon-investigate-issue\n    depends_on: [classify, web-research]\n    when: \"$classify.output.issue_type == 'bug'\"\n    context: fresh\n\n  - id: plan\n    command: archon-create-plan\n    depends_on: [classify, web-research]\n    when: \"$classify.output.issue_type != 'bug'\"\n    context: fresh\n\n  # Bridge: ensure investigation.md exists for the implement step\n  # archon-fix-issue reads from $ARTIFACTS_DIR/investigation.md\n  # archon-create-plan writes to $ARTIFACTS_DIR/plan.md\n  # This node copies plan.md → investigation.md when the plan path was taken\n  - id: bridge-artifacts\n    bash: |\n      if [ -f \"$ARTIFACTS_DIR/plan.md\" ] && [ ! -f \"$ARTIFACTS_DIR/investigation.md\" ]; then\n        cp \"$ARTIFACTS_DIR/plan.md\" \"$ARTIFACTS_DIR/investigation.md\"\n        echo \"Bridged plan.md to investigation.md for implement step\"\n      elif [ -f \"$ARTIFACTS_DIR/investigation.md\" ]; then\n        echo \"investigation.md exists from investigate step\"\n      else\n        echo \"WARNING: No investigation.md or plan.md found — implement may fail\"\n      fi\n    depends_on: [investigate, plan]\n    trigger_rule: one_success\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 4: IMPLEMENT\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: implement\n    command: archon-fix-issue\n    depends_on: [bridge-artifacts]\n    context: fresh\n    model: opus[1m]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 5: VALIDATE\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: validate\n    command: archon-validate\n    depends_on: [implement]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 6: CREATE DRAFT PR\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: create-pr\n    prompt: |\n      Create a draft pull request for the current branch.\n\n      ## Context\n\n      - **Issue**: $ARGUMENTS\n      - **Classification**: $classify.output\n      - **Issue title**: $classify.output.title\n\n      ## Instructions\n\n      1. Check git status. If uncommitted changes exist, stage and commit ONLY source files that are part of the fix:\n         - List them by name with `git add <path1> <path2> ...` — never `git add -A`, `git add .`, or `git add -u`\n         - **Never commit** scratch / review / PR-body artifacts, even if they appear in `git status`:\n           - `.pr-body.md`, `pr-body.md`, `*.scratch.md`, `*.tmp.md` at any path\n           - `review/`, `*-report.md` at the repo root\n           - Anything under `$ARTIFACTS_DIR`\n         - Verify with `git status --porcelain` that nothing scratch is staged before committing\n         - If files you don't recognize as part of the fix appear modified or untracked, leave them alone\n      2. Push the branch: `git push -u origin HEAD`\n      3. Read implementation artifacts from `$ARTIFACTS_DIR/` for context:\n         - `$ARTIFACTS_DIR/investigation.md` or `$ARTIFACTS_DIR/plan.md`\n         - `$ARTIFACTS_DIR/implementation.md`\n         - `$ARTIFACTS_DIR/validation.md`\n      4. Check if a PR already exists for this branch: `gh pr list --head $(git branch --show-current)`\n         - If PR exists, skip creation and capture its number\n      5. Look for the project's PR template at `.github/pull_request_template.md`, `.github/PULL_REQUEST_TEMPLATE.md`, or `docs/PULL_REQUEST_TEMPLATE.md`. Read whichever one exists.\n      6. Create a DRAFT PR: `gh pr create --draft --base $BASE_BRANCH`\n         - Title: concise, imperative mood, under 70 chars\n         - Body: if a PR template was found, fill in **every section** with details from the artifacts. Don't skip sections or leave placeholders. If no template, write a body with summary, changes, validation evidence, and `Fixes #...`.\n         - **PR body file location**: if you write the body to a file (e.g. for `--body-file`), the file MUST live at `$ARTIFACTS_DIR/pr-body.md` or under `/tmp/` — NEVER inside the worktree. Files like `.pr-body.md` at the repo root will be picked up by later commits.\n         - Link to issue: include `Fixes #...` or `Closes #...`\n      7. Capture PR identifiers:\n         ```bash\n         PR_NUMBER=$(gh pr view --json number -q '.number')\n         echo \"$PR_NUMBER\" > \"$ARTIFACTS_DIR/.pr-number\"\n         PR_URL=$(gh pr view --json url -q '.url')\n         echo \"$PR_URL\" > \"$ARTIFACTS_DIR/.pr-url\"\n         ```\n    depends_on: [validate]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 7: REVIEW\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: verify-pr-base\n    bash: |\n      set -euo pipefail\n      EXPECTED=\"$BASE_BRANCH\"\n      ACTUAL=$(gh pr view --json baseRefName -q '.baseRefName')\n      if [ \"$ACTUAL\" != \"$EXPECTED\" ]; then\n        PR_NUMBER=$(gh pr view --json number -q '.number')\n        echo \"Base mismatch on PR #$PR_NUMBER: expected=$EXPECTED actual=$ACTUAL — re-targeting\" >&2\n        gh pr edit \"$PR_NUMBER\" --base \"$EXPECTED\"\n      else\n        echo \"PR base verified: $EXPECTED\"\n      fi\n    depends_on: [create-pr]\n\n  - id: review-scope\n    command: archon-pr-review-scope\n    depends_on: [verify-pr-base]\n    context: fresh\n\n  - id: review-classify\n    prompt: |\n      You are a PR review classifier. Analyze the PR scope and determine\n      which review agents should run.\n\n      ## PR Scope\n\n      $review-scope.output\n\n      ## Rules\n\n      - **Code review**: ALWAYS run. This is mandatory for every PR. It also checks\n        the PR against CLAUDE.md rules and project conventions.\n      - **Error handling**: Run if the diff touches code with try/catch, error handling,\n        async/await, or adds new failure paths.\n      - **Test coverage**: Run if the diff touches source code (not just tests, docs, or config).\n      - **Comment quality**: Run if the diff adds or modifies comments, docstrings, JSDoc,\n        or significant documentation within code files.\n      - **Docs impact**: Run if the diff adds/removes/renames public APIs, commands, CLI flags,\n        environment variables, or user-facing features.\n\n      Provide your reasoning for each decision.\n    depends_on: [review-scope]\n    model: haiku\n    allowed_tools: []\n    context: fresh\n    output_format:\n      type: object\n      properties:\n        run_code_review:\n          type: string\n          enum: [\"true\", \"false\"]\n        run_error_handling:\n          type: string\n          enum: [\"true\", \"false\"]\n        run_test_coverage:\n          type: string\n          enum: [\"true\", \"false\"]\n        run_comment_quality:\n          type: string\n          enum: [\"true\", \"false\"]\n        run_docs_impact:\n          type: string\n          enum: [\"true\", \"false\"]\n        reasoning:\n          type: string\n      required:\n        - run_code_review\n        - run_error_handling\n        - run_test_coverage\n        - run_comment_quality\n        - run_docs_impact\n        - reasoning\n\n  # Code review always runs — mandatory\n  - id: code-review\n    command: archon-code-review-agent\n    depends_on: [review-classify]\n    context: fresh\n\n  - id: error-handling\n    command: archon-error-handling-agent\n    depends_on: [review-classify]\n    when: \"$review-classify.output.run_error_handling == 'true'\"\n    context: fresh\n\n  - id: test-coverage\n    command: archon-test-coverage-agent\n    depends_on: [review-classify]\n    when: \"$review-classify.output.run_test_coverage == 'true'\"\n    context: fresh\n\n  - id: comment-quality\n    command: archon-comment-quality-agent\n    depends_on: [review-classify]\n    when: \"$review-classify.output.run_comment_quality == 'true'\"\n    context: fresh\n\n  - id: docs-impact\n    command: archon-docs-impact-agent\n    depends_on: [review-classify]\n    when: \"$review-classify.output.run_docs_impact == 'true'\"\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 8: SYNTHESIZE + SELF-FIX\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: synthesize\n    command: archon-synthesize-review\n    depends_on: [code-review, error-handling, test-coverage, comment-quality, docs-impact]\n    trigger_rule: one_success\n    context: fresh\n\n  - id: self-fix\n    command: archon-self-fix-all\n    depends_on: [synthesize]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 9: SIMPLIFY\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: simplify\n    command: archon-simplify-changes\n    depends_on: [self-fix]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 10: REPORT\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: report\n    command: archon-issue-completion-report\n    depends_on: [simplify]\n    context: fresh\n",
  "archon-idea-to-pr": "name: archon-idea-to-pr\ndescription: |\n  Use when: You have a feature idea or description and want end-to-end development.\n  Input: Feature description in natural language, or path to a PRD file\n  Output: PR ready for merge with comprehensive review completed\n\n  Full workflow:\n  1. Create comprehensive implementation plan with codebase analysis\n  2. Setup branch and extract scope limits\n  3. Verify plan research is still valid\n  4. Implement all tasks with type-checking\n  5. Run full validation suite\n  6. Create PR with template, mark ready\n  7. Comprehensive code review (5 parallel agents with scope limit awareness)\n  8. Synthesize and fix review findings\n  9. Final summary with decision matrix -> GitHub comment + follow-up recommendations\n\n  NOT for: Executing existing plans (use archon-plan-to-pr), quick fixes, standalone reviews.\n\nnodes:\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 0: CREATE PLAN\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: create-plan\n    command: archon-create-plan\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 1: SETUP\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: plan-setup\n    command: archon-plan-setup\n    depends_on: [create-plan]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 2: CONFIRM PLAN\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: confirm-plan\n    command: archon-confirm-plan\n    depends_on: [plan-setup]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 3: IMPLEMENT\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: implement-tasks\n    command: archon-implement-tasks\n    depends_on: [confirm-plan]\n    context: fresh\n    model: opus[1m]\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 4: VALIDATE\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: validate\n    command: archon-validate\n    depends_on: [implement-tasks]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 5: FINALIZE PR\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: finalize-pr\n    command: archon-finalize-pr\n    depends_on: [validate]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 6: CODE REVIEW\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: verify-pr-base\n    bash: |\n      set -euo pipefail\n      EXPECTED=\"$BASE_BRANCH\"\n      ACTUAL=$(gh pr view --json baseRefName -q '.baseRefName')\n      if [ \"$ACTUAL\" != \"$EXPECTED\" ]; then\n        PR_NUMBER=$(gh pr view --json number -q '.number')\n        echo \"Base mismatch on PR #$PR_NUMBER: expected=$EXPECTED actual=$ACTUAL — re-targeting\" >&2\n        gh pr edit \"$PR_NUMBER\" --base \"$EXPECTED\"\n      else\n        echo \"PR base verified: $EXPECTED\"\n      fi\n    depends_on: [finalize-pr]\n\n  - id: review-scope\n    command: archon-pr-review-scope\n    depends_on: [verify-pr-base]\n    context: fresh\n\n  - id: sync\n    command: archon-sync-pr-with-main\n    depends_on: [review-scope]\n    context: fresh\n\n  - id: code-review\n    command: archon-code-review-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: error-handling\n    command: archon-error-handling-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: test-coverage\n    command: archon-test-coverage-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: comment-quality\n    command: archon-comment-quality-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: docs-impact\n    command: archon-docs-impact-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: synthesize\n    command: archon-synthesize-review\n    depends_on: [code-review, error-handling, test-coverage, comment-quality, docs-impact]\n    trigger_rule: one_success\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 7: FIX REVIEW ISSUES\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: implement-fixes\n    command: archon-implement-review-fixes\n    depends_on: [synthesize]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 8: FINAL SUMMARY & FOLLOW-UP\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: workflow-summary\n    command: archon-workflow-summary\n    depends_on: [implement-fixes]\n    context: fresh\n",
  "archon-interactive-prd": "name: archon-interactive-prd\ndescription: |\n  Use when: User wants to create a PRD through guided conversation.\n  Triggers: \"create a prd\", \"new prd\", \"interactive prd\", \"plan a feature\",\n            \"product requirements\", \"write a prd\".\n  NOT for: Autonomous PRD generation without human input (use archon-ralph-generate).\n\n  Interactive workflow that guides the user through problem-first PRD creation:\n  1. Understand the idea → ask foundation questions → wait for answers\n  2. Research market & codebase → ask deep dive questions → wait for answers\n  3. Assess technical feasibility → ask scope questions → wait for answers\n  4. Generate PRD → validate technical claims against codebase → output\n\nprovider: claude\ninteractive: true\n\nnodes:\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 1: INITIATE — Understand the idea\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: initiate\n    model: sonnet\n    prompt: |\n      You are a sharp product manager starting a PRD creation process.\n      You think from first principles — start with primitives, not features.\n\n      The user wants to build: $ARGUMENTS\n\n      If the input is clear, restate your understanding in 2-3 sentences and confirm:\n      \"I understand you want to build: {restated understanding}. Is this correct?\"\n\n      If the input is vague or empty, ask:\n      \"What do you want to build? Describe the product, feature, or capability.\"\n\n      Then present the Foundation Questions (all at once — the user will answer in the next step):\n\n      **Foundation Questions:**\n\n      1. **Who** has this problem? Be specific — not just \"users\" but what type of person/role?\n      2. **What** problem are they facing? Describe the observable pain, not the assumed need.\n      3. **Why** can't they solve it today? What alternatives exist and why do they fail?\n      4. **Why now?** What changed that makes this worth building?\n      5. **How** will you know if you solved it? What would success look like?\n\n      Keep it conversational. Don't generate any PRD content yet.\n\n  # ═══════════════════════════════════════════════════════════════\n  # GATE 1: User answers foundation questions\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: foundation-gate\n    approval:\n      message: \"Answer the foundation questions above. Your answers will guide the research phase.\"\n      capture_response: true\n    depends_on: [initiate]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 2: GROUNDING — Research market & codebase\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: research\n    model: sonnet\n    prompt: |\n      You are researching context for a PRD. Think from first principles —\n      what already exists before proposing anything new.\n\n      **The idea**: $ARGUMENTS\n\n      **User's foundation answers**:\n      $foundation-gate.output\n\n      Research the landscape:\n\n      1. Search the web for similar products, competitors, and how others solve this problem\n      2. **Explore the codebase deeply** — find related existing functionality, APIs, UI components,\n         database tables, and patterns. Read actual files, don't assume. Note exact file paths and\n         what each file does.\n      3. Look for common patterns, anti-patterns, and recent trends\n\n      **First principles rule**: Before suggesting anything new, verify what already exists.\n      If there's an existing API endpoint, UI page, or component that partially solves the\n      problem, note it explicitly. The best solution extends what exists, not replaces it.\n\n      Present a summary to the user:\n\n      **What I found:**\n      - {Market insights — similar products, competitor approaches}\n      - {What already exists in the codebase — specific files, endpoints, components}\n      - {Key insight that might change the approach}\n\n      Then ask the **Deep Dive Questions**:\n\n      1. **Vision**: In one sentence, what's the ideal end state if this succeeds wildly?\n      2. **Primary User**: Describe your most important user — their role, context, and what triggers their need.\n      3. **Job to Be Done**: Complete this: \"When [situation], I want to [motivation], so I can [outcome].\"\n      4. **Non-Users**: Who is explicitly NOT the target?\n      5. **Constraints**: What limitations exist? (time, budget, technical, regulatory)\n\n      Does the research change or refine your thinking? Answer the deep dive questions.\n    depends_on: [foundation-gate]\n\n  # ═══════════════════════════════════════════════════════════════\n  # GATE 2: User answers deep dive questions\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: deepdive-gate\n    approval:\n      message: \"Answer the deep dive questions above (vision, primary user, JTBD, constraints). Add any adjustments from the research.\"\n      capture_response: true\n    depends_on: [research]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 3: TECHNICAL GROUNDING — Feasibility from what exists\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: technical\n    model: sonnet\n    prompt: |\n      You are assessing technical feasibility for a PRD.\n      Think from first principles — start with what exists, not what you'd build from scratch.\n\n      **The idea**: $ARGUMENTS\n      **Foundation answers**: $foundation-gate.output\n      **Deep dive answers**: $deepdive-gate.output\n\n      **CRITICAL**: Explore the codebase by READING actual files. Do not guess or assume.\n      For every claim you make about the codebase, cite the exact file and line.\n\n      1. **What already exists** that partially solves this problem?\n         - Read existing API endpoints, DB queries, UI components\n         - Note exact function names, table schemas, component names\n         - What data is already being collected/stored?\n      2. **What's the smallest change** to the existing system that solves the core problem?\n         - Prefer extending existing files over creating new ones\n         - Prefer using existing endpoints over creating new ones\n         - Prefer adding to existing UI pages over new pages\n      3. **What are the actual primitives** we need?\n         - A new DB query? An existing one that needs a parameter?\n         - A new component? Or an existing component that needs a prop?\n         - A new endpoint? Or an existing endpoint that already returns the data?\n      4. **What's the risk?**\n         - Where could this go wrong?\n         - What assumptions need validation?\n\n      Present a summary:\n\n      **What Already Exists (verified by reading code):**\n      - {endpoint/component/query} at `{file:line}` — {what it does}\n      - {endpoint/component/query} at `{file:line}` — {what it does}\n\n      **Smallest Change to Solve the Problem:**\n      - {change 1}: {extend/modify} `{file}` — {what to do}\n      - {change 2}: {extend/modify} `{file}` — {what to do}\n\n      **Technical Context:**\n      - Feasibility: {HIGH/MEDIUM/LOW} because {reason}\n      - Key risk: {main concern}\n      - Estimated phases: {rough breakdown}\n\n      Then ask the **Scope Questions**:\n\n      1. **MVP Definition**: What's the absolute minimum to test if this works?\n      2. **Must Have vs Nice to Have**: What 2-3 things MUST be in v1? What can wait?\n      3. **Key Hypothesis**: Complete this: \"We believe [capability] will [solve problem] for [users]. We'll know we're right when [measurable outcome].\"\n      4. **Out of Scope**: What are you explicitly NOT building?\n      5. **Open Questions**: What uncertainties could change the approach?\n    depends_on: [deepdive-gate]\n\n  # ═══════════════════════════════════════════════════════════════\n  # GATE 3: User answers scope questions\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: scope-gate\n    approval:\n      message: \"Answer the scope questions above (MVP, must-haves, hypothesis, exclusions). This is the final input before PRD generation.\"\n      capture_response: true\n    depends_on: [technical]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 4: GENERATE — Write the PRD\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: generate\n    model: sonnet\n    prompt: |\n      You are generating a PRD from the user's guided inputs.\n\n      **The idea**: $ARGUMENTS\n      **Foundation answers**: $foundation-gate.output\n      **Deep dive answers**: $deepdive-gate.output\n      **Scope answers**: $scope-gate.output\n\n      Generate a complete PRD file at `$ARTIFACTS_DIR/prds/{kebab-case-name}.prd.md`.\n\n      First create the directory:\n      ```bash\n      mkdir -p $ARTIFACTS_DIR/prds\n      ```\n\n      **First principles rule**: Before writing the Technical Approach section, READ the\n      actual codebase files you're referencing. Verify:\n      - File paths exist\n      - Function/component names are correct\n      - API endpoints you reference actually exist (or note they need to be created)\n      - DB table and column names match the schema\n      - Event type names match the constants in the code\n\n      The PRD must include ALL of these sections, filled from the user's answers:\n\n      1. **Problem Statement** — from foundation answers (who/what/why)\n      2. **Evidence** — from research findings and user's evidence\n      3. **Proposed Solution** — synthesized from all inputs. Prefer extending existing\n         primitives over creating new ones.\n      4. **Key Hypothesis** — from scope answers\n      5. **What We're NOT Building** — from scope answers\n      6. **Success Metrics** — from foundation \"how will you know\" + scope\n      7. **Open Questions** — from scope answers\n      8. **Users & Context** — from deep dive (primary user, JTBD, non-users)\n      9. **Solution Detail** — MoSCoW table from scope must-haves, MVP definition\n      10. **Technical Approach** — from technical feasibility. MUST reference actual\n          verified file paths, function names, and schemas. Mark anything unverified\n          as \"needs verification\".\n      11. **Implementation Phases** — from technical breakdown, with status table\n          and parallel opportunities\n      12. **Decisions Log** — key decisions made during the conversation\n\n      **Rules:**\n      - If info is missing, write \"TBD — needs research\" not filler\n      - Be specific and concrete, not generic\n      - Every file path in Technical Approach must be verified by reading the file\n      - Prefer \"extend X\" over \"create new Y\" in implementation phases\n\n      After writing the file, output the file path only — the validator will check it.\n    depends_on: [scope-gate]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 5: VALIDATE — Check technical claims against codebase\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: validate\n    model: sonnet\n    prompt: |\n      You are a technical validator checking a PRD for accuracy.\n\n      Read the PRD file that was just generated. The generate node output the file path:\n      $generate.output\n\n      Find the PRD file — check `$ARTIFACTS_DIR/prds/` for the most recently created `.prd.md` file:\n      ```bash\n      ls -t $ARTIFACTS_DIR/prds/*.prd.md | head -1\n      ```\n\n      Read the entire PRD, then verify EVERY technical claim against the actual codebase:\n\n      **Check 1: File paths** — For every file referenced in \"Technical Approach\" and\n      \"Implementation Phases\", verify it exists. If it doesn't, note the correction.\n\n      **Check 2: API endpoints** — For every endpoint mentioned, check if it already exists\n      in `packages/server/src/routes/api.ts`. If it does, the PRD should say \"extend\" not \"create\".\n      If the PRD proposes a new endpoint for data that an existing endpoint already returns,\n      flag it.\n\n      **Check 3: DB schemas** — For every table/column referenced, verify the actual names\n      in the migration files or schema code. Check event type names against the\n      `WORKFLOW_EVENT_TYPES` constant.\n\n      **Check 4: UI components** — For every component referenced, verify it exists.\n      If the PRD proposes a new page but an existing page already serves a similar purpose,\n      flag it.\n\n      **Check 5: Function/type names** — Verify function names, type names, and interface\n      names are correct.\n\n      After checking, if there are ANY corrections needed:\n      1. Edit the PRD file directly — fix incorrect names, paths, and references\n      2. Add a `## Validation Notes` section at the bottom documenting what was corrected\n\n      If everything checks out, add:\n      ```\n      ## Validation Notes\n\n      All technical references verified against codebase. No corrections needed.\n      ```\n\n      Output a summary of what was checked and corrected:\n\n      ```\n      ## PRD Validated\n\n      **File**: `{prd-path}`\n      **Checks**: {N} file paths, {N} endpoints, {N} DB references, {N} components\n      **Corrections**: {count}\n      {list corrections if any}\n\n      To start implementation: `/prp-plan {prd-path}`\n      ```\n    depends_on: [generate]\n",
  "archon-issue-review-full": "name: archon-issue-review-full\ndescription: |\n  Use when: User wants a FULL, COMPREHENSIVE fix + review pipeline for a GitHub issue.\n  Triggers: \"full review\", \"comprehensive fix\", \"fix with full review\", \"deep review\", \"issue review full\".\n  NOT for: Simple issue fixes (use archon-fix-github-issue instead),\n           questions about issues, CI failures, PR reviews, general exploration.\n\n  Full workflow:\n  1. Investigate issue -> root cause analysis, implementation plan\n  2. Implement fix -> code changes, tests, PR creation\n  3. Comprehensive review -> 5 parallel agents with scope awareness\n  4. Fix review issues -> address CRITICAL/HIGH findings\n  5. Final summary -> decision matrix, follow-up recommendations\n\nnodes:\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 1: INVESTIGATE\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: investigate\n    command: archon-investigate-issue\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 2: IMPLEMENT\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: implement\n    command: archon-implement-issue\n    depends_on: [investigate]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 3: CODE REVIEW\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: verify-pr-base\n    bash: |\n      set -euo pipefail\n      EXPECTED=\"$BASE_BRANCH\"\n      ACTUAL=$(gh pr view --json baseRefName -q '.baseRefName')\n      if [ \"$ACTUAL\" != \"$EXPECTED\" ]; then\n        PR_NUMBER=$(gh pr view --json number -q '.number')\n        echo \"Base mismatch on PR #$PR_NUMBER: expected=$EXPECTED actual=$ACTUAL — re-targeting\" >&2\n        gh pr edit \"$PR_NUMBER\" --base \"$EXPECTED\"\n      else\n        echo \"PR base verified: $EXPECTED\"\n      fi\n    depends_on: [implement]\n\n  - id: review-scope\n    command: archon-pr-review-scope\n    depends_on: [verify-pr-base]\n    context: fresh\n\n  - id: sync\n    command: archon-sync-pr-with-main\n    depends_on: [review-scope]\n    context: fresh\n\n  - id: code-review\n    command: archon-code-review-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: error-handling\n    command: archon-error-handling-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: test-coverage\n    command: archon-test-coverage-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: comment-quality\n    command: archon-comment-quality-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: docs-impact\n    command: archon-docs-impact-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: synthesize\n    command: archon-synthesize-review\n    depends_on: [code-review, error-handling, test-coverage, comment-quality, docs-impact]\n    trigger_rule: one_success\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 4: FIX REVIEW ISSUES\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: implement-fixes\n    command: archon-implement-review-fixes\n    depends_on: [synthesize]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 5: FINAL SUMMARY\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: summary\n    command: archon-workflow-summary\n    depends_on: [implement-fixes]\n    context: fresh\n",
  "archon-piv-loop": "name: archon-piv-loop\ndescription: |\n  Use when: User wants guided Plan-Implement-Validate development with human-in-the-loop.\n  Triggers: \"piv\", \"piv loop\", \"plan implement validate\", \"guided development\",\n            \"structured development\", \"build a feature\", \"develop with review\".\n  NOT for: Autonomous implementation without planning (use archon-feature-development).\n  NOT for: PRD creation (use archon-interactive-prd).\n  NOT for: Ralph story-based implementation (use archon-ralph-dag).\n\n  Interactive PIV loop workflow — the foundational AI coding methodology:\n  1. EXPLORE: Iterative conversation with human to understand the problem (arbitrary rounds)\n  2. PLAN: Create structured plan -> iterative review & revision (arbitrary rounds)\n  3. IMPLEMENT: Autonomous task-by-task implementation from plan (Ralph loop)\n  4. VALIDATE: Automated code review -> iterative human feedback & fixes (arbitrary rounds)\n\n  The PIV loop comes AFTER a PRD exists. Each PIV loop focuses on ONE granular feature or bug fix.\n  Input: A description of what to build, a path to an existing plan, or a GitHub issue number.\n\nprovider: claude\ninteractive: true\n\nnodes:\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 1: EXPLORE — Iterative exploration with human\n  # Understand the idea, explore the codebase, converge on approach\n  # Loops until the user says they're ready to create the plan.\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: explore\n    loop:\n      prompt: |\n        # PIV Loop — Exploration\n\n        You are a senior engineering partner in an iterative exploration session.\n        Your goal: DEEPLY UNDERSTAND what to build before any code is written.\n\n        **User's request**: $ARGUMENTS\n        **User's latest input**: $LOOP_USER_INPUT\n\n        ---\n\n        ## If this is the FIRST iteration (no user input yet):\n\n        ### Step 1: Parse the Input\n\n        Determine what the user provided:\n\n        **If it's a file path** (ends in `.md`, `.plan.md`, or `.prd.md`):\n        - Read the file\n        - If it's an existing plan → summarize it and ask if they want to refine or proceed\n        - If it's a PRD → identify the specific phase/feature to focus on\n\n        **If it's a GitHub issue** (`#123` format):\n        - Fetch it: `gh issue view {number} --json title,body,labels,comments`\n        - Summarize the issue context\n\n        **If it's free text**:\n        - This is a feature idea or bug description. Use it directly.\n\n        ### Step 2: Explore the Codebase\n\n        Before asking questions, DO YOUR HOMEWORK:\n\n        1. **Read CLAUDE.md** — understand project conventions, architecture, and constraints\n        2. **Search for related code** — find existing implementations similar to what the user wants\n        3. **Read key files** — understand the current state of code the user wants to change\n        4. **Check recent git history** — `git log --oneline -20` for recent changes in the area\n\n        ### Step 3: Present Your Understanding\n\n        ```\n        ## What I Understand\n\n        You want to: {restated understanding in 2-3 sentences}\n\n        ## What Already Exists\n\n        - {file:line} — {what it does and how it relates}\n        - {file:line} — {what it does and how it relates}\n        - {pattern/component} — {how it could be extended or reused}\n\n        ## Initial Architecture Thoughts\n\n        Based on what exists, I'm thinking:\n        - {approach 1 — extend existing X}\n        - {approach 2 — if approach 1 doesn't work}\n        - {key architectural decision that needs your input}\n        ```\n\n        ### Step 4: Ask Targeted Questions\n\n        Ask 4-6 questions focused on DECISIONS, not information gathering:\n        - Scope boundaries, architecture preferences, tech decisions\n        - Constraints, existing code extension vs fresh build, testing expectations\n        - Reference actual code you found — don't ask generic questions\n\n        ---\n\n        ## If the user has provided input (subsequent iterations):\n\n        ### Step 1: Process Their Response\n\n        Read their answers carefully. Identify:\n        - Decisions they've made\n        - Areas they want you to explore further\n        - Questions they asked YOU back (answer these with evidence!)\n\n        ### Step 2: Do Targeted Research\n\n        Based on their response:\n        - If they mentioned specific technologies → research best practices\n        - If they pointed you to specific code → read it thoroughly\n        - If they asked you to explore an area → do a thorough investigation\n        - If they made architecture decisions → validate against the codebase\n\n        ### Step 3: Present Updated Understanding\n\n        Show what you learned, answer their questions with file:line references,\n        and present your refined architecture recommendation.\n\n        ### Step 4: Converge or Continue\n\n        **If there are still important open questions:**\n        Ask 2-4 focused questions about remaining ambiguities.\n\n        **If the picture is clear and you have enough to create a plan:**\n        Present a final implementation summary:\n\n        ```\n        ## Implementation Summary\n\n        ### What We're Building\n        {Clear, specific description}\n\n        ### Scope Boundary\n        - IN: {what's included}\n        - OUT: {what's explicitly excluded}\n\n        ### Architecture\n        - {key decisions}\n\n        ### Files That Will Change\n        - `{file}` — {what changes and why}\n\n        ### Success Criteria\n        - [ ] {specific, testable criterion}\n        - [ ] All validation passes\n\n        ### Key Risks\n        - {risk — and mitigation}\n        ```\n\n        Then tell the user: \"I have a clear picture. Say **ready** and I'll create\n        the structured implementation plan, or share any final thoughts.\"\n\n        **CRITICAL — READ THIS CAREFULLY**:\n        - NEVER output <promise>PLAN_READY</promise> unless the user's LATEST message contains\n          an EXPLICIT phrase like \"ready\", \"create the plan\", \"let's go\", \"proceed\", or \"I'm done\".\n        - If the user asked a question → do NOT emit the signal. Answer the question.\n        - If the user gave feedback or requested changes → do NOT emit the signal. Address it.\n        - If the user said \"also check X\" or \"one more thing\" → do NOT emit the signal. Explore it.\n        - If you are unsure whether the user is approving → do NOT emit the signal. Ask them.\n        - The ONLY correct time to emit the signal is when the user's message CLEARLY means\n          \"stop exploring, I'm ready for you to create the plan.\"\n      until: PLAN_READY\n      max_iterations: 15\n      interactive: true\n      gate_message: |\n        Answer the questions above, ask me to explore specific areas,\n        or say \"ready\" when you're satisfied with the exploration.\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 2: PLAN — Create the structured implementation plan\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: create-plan\n    model: sonnet\n    depends_on: [explore]\n    context: fresh\n    prompt: |\n      # PIV Loop — Create Structured Plan\n\n      You are creating a structured implementation plan from a completed exploration phase.\n      This plan will be the SOLE GUIDE for the implementation agent — it must be complete,\n      specific, and actionable.\n\n      **Original request**: $ARGUMENTS\n      **Final exploration summary**: $explore.output\n\n      ---\n\n      ## Step 1: Read the Codebase (Again)\n\n      Before writing the plan, verify your understanding is current:\n\n      1. **Read CLAUDE.md** — capture all relevant conventions\n      2. **Read every file you plan to change** — note exact current state\n      3. **Read example test files** — understand testing patterns\n      4. **Check for any recent changes** — `git log --oneline -10`\n\n      ## Step 2: Plan File Location\n\n      Save the plan to `$ARTIFACTS_DIR/plan.md`.\n      The directory already exists (pre-created by the workflow executor).\n\n      ## Step 3: Write the Plan\n\n      Use this template. Fill EVERY section with specific, verified information.\n\n      ```markdown\n      # Feature: {Title}\n\n      ## Summary\n      {1-2 sentences: what changes and why}\n\n      ## Mission\n      {The core goal in one clear statement}\n\n      ## Success Criteria\n      - [ ] {Specific, testable criterion}\n      - [ ] All validation passes (`bun run validate` or equivalent)\n      - [ ] No regressions in existing tests\n\n      ## Scope\n      ### In Scope\n      - {What we ARE building}\n      ### Out of Scope\n      - {What we are NOT building — and why}\n\n      ## Codebase Context\n      ### Key Files\n      | File | Role | Action |\n      |------|------|--------|\n      | `{path}` | {what it does} | CREATE / UPDATE |\n\n      ### Patterns to Follow\n      {Actual code snippets from the codebase to mirror}\n\n      ## Architecture\n      - {Decision 1 — with rationale}\n      - {Decision 2 — with rationale}\n\n      ## Task List\n      Execute in order. Each task is atomic and independently verifiable.\n\n      ### Task 1: {ACTION} `{file path}`\n      **Action**: CREATE / UPDATE\n      **Details**: {Exact changes — specific enough for an agent with no context}\n      **Pattern**: Follow `{source file}:{lines}`\n      **Validate**: `{command to verify this task}`\n\n      ## Testing Strategy\n      | Test File | Test Cases | Validates |\n      |-----------|-----------|-----------|\n      | `{path}` | {cases} | {what it validates} |\n\n      ## Validation Commands\n      1. Type check: `{command}`\n      2. Lint: `{command}`\n      3. Tests: `{command}`\n      4. Full validation: `{command}`\n\n      ## Risks\n      | Risk | Impact | Mitigation |\n      |------|--------|------------|\n      | {risk} | {HIGH/MED/LOW} | {specific mitigation} |\n      ```\n\n      ## Step 4: Verify the Plan\n\n      1. Check every file path referenced — verify they exist\n      2. Check every pattern cited — verify the code matches\n      3. Check task ordering — ensure dependencies are respected\n      4. Check completeness — could an agent with NO context implement this?\n\n      ## Step 5: Report\n\n      ```\n      ## Plan Created\n\n      **File**: `$ARTIFACTS_DIR/plan.md`\n      **Tasks**: {count}\n      **Files to change**: {count}\n\n      Key decisions:\n      - {decision 1}\n      - {decision 2}\n\n      Please review the plan and provide feedback.\n      ```\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 2b: PLAN — Iterative plan refinement\n  # Review and revise the plan as many times as needed.\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: refine-plan\n    depends_on: [create-plan]\n    loop:\n      prompt: |\n        # PIV Loop — Plan Refinement\n\n        The user is reviewing the implementation plan and providing feedback.\n\n        **User's feedback**: $LOOP_USER_INPUT\n\n        ---\n\n        ## Step 1: Read the Plan\n\n        Read `$ARTIFACTS_DIR/plan.md` and CLAUDE.md for conventions.\n\n        ## Step 2: Process Feedback\n\n        **If there is no user feedback yet** (first iteration, $LOOP_USER_INPUT is empty):\n        - Read the plan carefully\n        - Present a summary of the plan's key decisions and task list\n        - Ask the user to review and provide feedback\n        - Do NOT emit the completion signal on the first iteration\n\n        **If the user EXPLICITLY approved** (said \"approved\", \"looks good\", \"let's go\", etc.):\n        - Make no changes\n        - Output: \"Plan approved. Proceeding to implementation.\"\n        - Signal completion: <promise>PLAN_APPROVED</promise>\n\n        **If the user provided specific feedback:**\n        - Parse each piece of feedback\n        - Edit the plan file directly:\n          - Add/remove/modify tasks as requested\n          - Update success criteria if needed\n          - Adjust testing strategy if needed\n          - Re-verify file paths and patterns after changes\n\n        **CRITICAL**: NEVER emit <promise>PLAN_APPROVED</promise> unless the user's latest\n        message EXPLICITLY says \"approved\", \"looks good\", \"ship it\", or similar approval.\n        Questions, feedback, and requests for changes are NOT approval.\n\n        ## Step 3: Show Changes\n\n        ```\n        ## Plan Revised\n\n        Changes made:\n        - {change 1}\n        - {change 2}\n\n        Updated stats:\n        - Tasks: {count}\n        - Files to change: {count}\n\n        Review the updated plan and provide more feedback, or say \"approved\" to proceed.\n        ```\n      until: PLAN_APPROVED\n      max_iterations: 10\n      interactive: true\n      gate_message: |\n        Review the plan document. Provide specific feedback on what to change,\n        or say \"approved\" to begin implementation.\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 3: IMPLEMENT — Setup\n  # Read the plan, prepare the environment\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: implement-setup\n    depends_on: [refine-plan]\n    bash: |\n      set -e\n\n      PLAN_FILE=\"$ARTIFACTS_DIR/plan.md\"\n\n      if [ ! -f \"$PLAN_FILE\" ]; then\n        echo \"ERROR: No plan file found at $ARTIFACTS_DIR/plan.md\"\n        exit 1\n      fi\n\n      # Install dependencies if needed\n      if [ -f \"bun.lock\" ] || [ -f \"bun.lockb\" ]; then\n        echo \"Installing dependencies...\"\n        bun install --frozen-lockfile 2>&1 | tail -3\n      elif [ -f \"package-lock.json\" ]; then\n        npm ci 2>&1 | tail -3\n      elif [ -f \"yarn.lock\" ]; then\n        yarn install --frozen-lockfile 2>&1 | tail -3\n      elif [ -f \"pnpm-lock.yaml\" ]; then\n        pnpm install --frozen-lockfile 2>&1 | tail -3\n      fi\n\n      echo \"BRANCH=$(git branch --show-current)\"\n      echo \"GIT_ROOT=$(git rev-parse --show-toplevel)\"\n      echo \"PLAN_FILE=$PLAN_FILE\"\n\n      echo \"=== PLAN_START ===\"\n      cat \"$PLAN_FILE\"\n      echo \"\"\n      echo \"=== PLAN_END ===\"\n\n      TASK_COUNT=$(grep -c \"^### Task [0-9]\" \"$PLAN_FILE\" 2>/dev/null || echo \"0\")\n      if [ \"$TASK_COUNT\" -eq 0 ]; then\n        echo \"ERROR: No '### Task N:' sections found in $PLAN_FILE. Plan may be malformed.\"\n        exit 1\n      fi\n      echo \"TASK_COUNT=${TASK_COUNT}\"\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 3b: IMPLEMENT — Task-by-Task Loop (Ralph pattern)\n  # Fresh context each iteration. Reads plan from disk.\n  # One task per iteration. Validates before committing.\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: implement\n    depends_on: [implement-setup]\n    idle_timeout: 600000\n    model: claude-opus-4-6[1m]\n    loop:\n      prompt: |\n        # PIV Loop — Implementation Agent\n\n        You are an autonomous coding agent in a FRESH session — no memory of previous iterations.\n        Your job: Read the plan from disk, implement ONE task, validate, commit, update tracking, exit.\n\n        **Golden Rule**: If validation fails, fix it before committing. Never commit broken code.\n\n        ---\n\n        ## Phase 0: CONTEXT — Load State\n\n        The setup node produced this context:\n\n        $implement-setup.output\n\n        **User's original request**: $USER_MESSAGE\n\n        ---\n\n        ### 0.1 Parse Plan File\n\n        Extract the `PLAN_FILE=...` line from the context above.\n\n        ### 0.2 Read Current State (from disk — not from context above)\n\n        The context above is a snapshot from before the loop started. Previous iterations\n        may have changed things. **You MUST re-read from disk:**\n\n        1. **Read the plan file** — your implementation guide\n        2. **Read progress tracking** — check if `$ARTIFACTS_DIR/progress.txt` exists\n        3. **Read CLAUDE.md** — project conventions and constraints\n\n        ### 0.3 Check Git State\n\n        ```bash\n        git log --oneline -10\n        git status\n        ```\n\n        ---\n\n        ## Phase 1: SELECT — Pick Next Task\n\n        From the plan file, identify tasks by `### Task N:` headers.\n        Cross-reference with commits from previous iterations and progress tracking.\n\n        **If ALL tasks are complete** → Skip to Phase 5 (Completion).\n\n        ### Announce Selection\n\n        ```\n        -- Task Selected ------------------------------------------------\n        Task: {N} — {task title}\n        Action: {CREATE / UPDATE}\n        File: {file path}\n        -----------------------------------------------------------------\n        ```\n\n        ---\n\n        ## Phase 2: IMPLEMENT — Execute the Task\n\n        1. Read the file you're about to change (if it exists)\n        2. Read the pattern file referenced in the plan\n        3. Make changes following the plan EXACTLY\n        4. Type-check after each file: `bun run type-check 2>&1 || true`\n\n        ---\n\n        ## Phase 3: VALIDATE — Verify the Task\n\n        ```bash\n        bun run type-check && bun run lint && bun run test && bun run format:check\n        ```\n\n        If validation fails: fix, re-run (up to 3 attempts). If unfixable, note in progress\n        tracking and do NOT commit broken code.\n\n        ---\n\n        ## Phase 4: COMMIT — Save Changes\n\n        Stage **only** the files you edited for this PIV task — never `git add -A`, `git add .`, or `git add -u`. List them by name:\n\n        ```bash\n        git add path/to/file1 path/to/file2 ...\n        git status --porcelain  # verify nothing scratch/review/PR-body is staged\n        git diff --cached --stat\n        git commit -m \"$(cat <<'EOF'\n        {type}: {task description}\n\n        PIV Task {N}: {brief details}\n        EOF\n        )\"\n        ```\n\n        **Never stage**: `.pr-body.md`, `pr-body.md`, `*.scratch.md`, `*.tmp.md`, `review/`, `*-report.md` at the repo root, or anything under `$ARTIFACTS_DIR`.\n\n        Track progress in `$ARTIFACTS_DIR/progress.txt`:\n        ```\n        ## Task {N}: {title} — COMPLETED\n        Date: {ISO date}\n        Files: {list}\n        Commit: {short hash}\n        ---\n        ```\n\n        ---\n\n        ## Phase 5: COMPLETE — Check All Tasks\n\n        If ALL tasks are done:\n        1. Run full validation: `bun run validate 2>&1`\n        2. Push: `git push -u origin HEAD`\n        3. Signal: `<promise>COMPLETE</promise>`\n\n        If tasks remain, report status and end normally. The loop engine starts a fresh iteration.\n      until: COMPLETE\n      max_iterations: 15\n      fresh_context: true\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 4: VALIDATE — Automated code review\n  # Review all changes against the plan\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: code-review\n    model: sonnet\n    depends_on: [implement]\n    context: fresh\n    prompt: |\n      # PIV Loop — Automated Code Review\n\n      The implementation phase is complete. Review ALL changes against the plan.\n\n      **Implementation output**: $implement.output\n\n      ---\n\n      ## Step 1: Read the Plan\n\n      Read `$ARTIFACTS_DIR/plan.md` to understand the intended implementation.\n\n      ## Step 2: Review All Changes\n\n      ```bash\n      git log --oneline --no-merges $(git merge-base HEAD $BASE_BRANCH)..HEAD\n      git diff $BASE_BRANCH..HEAD --stat\n      git diff $BASE_BRANCH..HEAD\n      ```\n\n      ## Step 3: Check Against Plan\n\n      For EACH task: was it implemented correctly? Do success criteria hold?\n      For EACH file: check quality, security, patterns, CLAUDE.md compliance.\n\n      ## Step 4: Run Validation\n\n      ```bash\n      bun run validate 2>&1 || (bun run type-check && bun run lint && bun run test && bun run format:check)\n      ```\n\n      ## Step 5: Fix Obvious Issues\n\n      Fix type errors, lint warnings, missing imports, formatting. Stage only the files you fixed — never `git add -A`. Skip the commit if there were no fixes:\n      ```bash\n      git add path/to/file1 path/to/file2 ...  # list real fixes only\n      git status --porcelain  # verify nothing scratch/review/PR-body is staged\n      git diff --cached --quiet || git commit -m \"fix: address code review findings\"\n      ```\n\n      **Never stage**: `.pr-body.md`, `pr-body.md`, `*.scratch.md`, `*.tmp.md`, `review/`, `*-report.md` at the repo root, or anything under `$ARTIFACTS_DIR`.\n\n      ## Step 6: Present Review\n\n      ```\n      ## Code Review Complete\n\n      ### Implementation Status\n      | Task | Status | Notes |\n      |------|--------|-------|\n      | {task} | DONE / PARTIAL / MISSING | {notes} |\n\n      ### Validation Results\n      - Type-check: PASS / FAIL\n      - Lint: PASS / FAIL\n      - Tests: PASS / FAIL\n      - Format: PASS / FAIL\n\n      ### Code Quality Findings\n      {Issues found, or \"No issues found.\"}\n\n      ### Recommendation\n      {READY FOR REVIEW / NEEDS FIXES}\n      ```\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 4b: VALIDATE — Iterative human feedback & fixes\n  # The user tests the implementation and provides feedback.\n  # Loops until the user approves.\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: fix-feedback\n    depends_on: [code-review]\n    loop:\n      prompt: |\n        # PIV Loop — Address Validation Feedback\n\n        The human has reviewed the implementation and provided feedback.\n\n        **Human's feedback**: $LOOP_USER_INPUT\n\n        ---\n\n        ## Step 1: Read Context\n\n        Read `$ARTIFACTS_DIR/plan.md` and CLAUDE.md for conventions.\n\n        ## Step 2: Process Feedback\n\n        **If there is no user feedback yet** (first iteration, $LOOP_USER_INPUT is empty):\n        - Present the code review results and ask the user to test the implementation\n        - Do NOT emit the completion signal on the first iteration\n\n        **If the user EXPLICITLY approved** (said \"approved\", \"looks good\", \"ship it\", etc.):\n        - Output: \"Implementation approved!\"\n        - Signal: <promise>VALIDATED</promise>\n\n        **CRITICAL**: NEVER emit <promise>VALIDATED</promise> unless the user's latest\n        message EXPLICITLY says \"approved\", \"looks good\", \"ship it\", or similar approval.\n\n        **If the user provided specific feedback:**\n        1. Read the relevant files\n        2. Understand each issue\n        3. Make the fixes\n        4. Type-check after each change\n\n        ## Step 3: Full Validation\n\n        ```bash\n        bun run validate 2>&1 || (bun run type-check && bun run lint && bun run test && bun run format:check)\n        ```\n\n        ## Step 4: Commit Fixes\n\n        Stage **only** the files you actually edited while addressing feedback — never `git add -A`. List them by name:\n\n        ```bash\n        git add path/to/file1 path/to/file2 ...\n        git status --porcelain  # verify nothing scratch/review/PR-body is staged\n        git commit -m \"$(cat <<'EOF'\n        fix: address review feedback\n\n        Changes:\n        - {fix 1}\n        - {fix 2}\n        EOF\n        )\"\n        ```\n\n        **Never stage**: `.pr-body.md`, `pr-body.md`, `*.scratch.md`, `*.tmp.md`, `review/`, `*-report.md` at the repo root, or anything under `$ARTIFACTS_DIR`.\n\n        ## Step 5: Report\n\n        ```\n        ## Feedback Addressed\n\n        Changes made:\n        - {fix 1}\n        - {fix 2}\n\n        Validation: {PASS / FAIL with details}\n\n        Review again, or say \"approved\" to finalize.\n        ```\n      until: VALIDATED\n      max_iterations: 10\n      interactive: true\n      gate_message: |\n        Test the implementation yourself and review the code changes.\n        Provide specific feedback on what needs fixing, or say \"approved\" to finalize.\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 5: FINALIZE — Push, create PR, generate summary\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: finalize\n    model: sonnet\n    depends_on: [fix-feedback]\n    context: fresh\n    prompt: |\n      # PIV Loop — Finalize\n\n      The implementation has been approved. Push changes and create a PR.\n\n      ---\n\n      ## Step 1: Push Changes\n\n      ```bash\n      git push -u origin HEAD 2>&1 || echo \"WARNING: Push failed — verify remote authentication and branch state before creating the PR.\"\n      ```\n\n      ## Step 2: Generate Summary\n\n      ```bash\n      git log --oneline --no-merges $(git merge-base HEAD $BASE_BRANCH)..HEAD\n      git diff --stat $(git merge-base HEAD $BASE_BRANCH)..HEAD\n      ```\n\n      Read `$ARTIFACTS_DIR/plan.md` and `$ARTIFACTS_DIR/progress.txt` for context.\n\n      ## Step 3: Create PR (if not already created)\n\n      ```bash\n      gh pr view HEAD --json url 2>/dev/null || echo \"NO_PR\"\n      ```\n\n      If no PR exists:\n\n      ```bash\n      cat .github/pull_request_template.md 2>/dev/null || echo \"NO_TEMPLATE\"\n      ```\n\n      Create with `gh pr create --draft --base $BASE_BRANCH`:\n      - Title from the plan's feature name\n      - Body summarizing the implementation\n      - Use a HEREDOC for the body\n\n      ## Step 4: Output Summary\n\n      ```\n      ===============================================================\n      PIV LOOP — COMPLETE\n      ===============================================================\n\n      Feature: {from plan}\n      Plan: {plan file path}\n      Branch: {branch name}\n      PR: {url}\n\n      -- Tasks Completed -----------------------------------------------\n      {list from progress tracking}\n\n      -- Commits -------------------------------------------------------\n      {git log output}\n\n      -- Files Changed -------------------------------------------------\n      {git diff --stat output}\n\n      -- Validation ----------------------------------------------------\n      All checks passed.\n      ===============================================================\n      ```\n\n  - id: verify-pr-base\n    bash: |\n      set -euo pipefail\n      EXPECTED=\"$BASE_BRANCH\"\n      ACTUAL=$(gh pr view --json baseRefName -q '.baseRefName')\n      if [ \"$ACTUAL\" != \"$EXPECTED\" ]; then\n        PR_NUMBER=$(gh pr view --json number -q '.number')\n        echo \"Base mismatch on PR #$PR_NUMBER: expected=$EXPECTED actual=$ACTUAL — re-targeting\" >&2\n        gh pr edit \"$PR_NUMBER\" --base \"$EXPECTED\"\n      else\n        echo \"PR base verified: $EXPECTED\"\n      fi\n    depends_on: [finalize]\n",
  "archon-plan-to-pr": "name: archon-plan-to-pr\ndescription: |\n  Use when: You have an existing implementation plan and want to execute it end-to-end.\n  Input: Path to a plan file ($ARTIFACTS_DIR/plan.md or .agents/plans/*.md)\n  Output: PR ready for merge with comprehensive review completed\n\n  Full workflow:\n  1. Read plan, setup branch, extract scope limits\n  2. Verify plan research is still valid\n  3. Implement all tasks with type-checking\n  4. Run full validation suite\n  5. Create PR with template, mark ready\n  6. Comprehensive code review (5 parallel agents with scope limit awareness)\n  7. Synthesize and fix review findings\n  8. Final summary with decision matrix -> GitHub comment + follow-up recommendations\n\n  NOT for: Creating plans from scratch (use archon-idea-to-pr), quick fixes, standalone reviews.\n\nnodes:\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 1: SETUP\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: plan-setup\n    command: archon-plan-setup\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 2: CONFIRM PLAN\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: confirm-plan\n    command: archon-confirm-plan\n    depends_on: [plan-setup]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 3: IMPLEMENT\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: implement-tasks\n    command: archon-implement-tasks\n    depends_on: [confirm-plan]\n    context: fresh\n    model: opus[1m]\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 4: VALIDATE\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: validate\n    command: archon-validate\n    depends_on: [implement-tasks]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 5: FINALIZE PR\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: finalize-pr\n    command: archon-finalize-pr\n    depends_on: [validate]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 6: CODE REVIEW\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: verify-pr-base\n    bash: |\n      set -euo pipefail\n      EXPECTED=\"$BASE_BRANCH\"\n      ACTUAL=$(gh pr view --json baseRefName -q '.baseRefName')\n      if [ \"$ACTUAL\" != \"$EXPECTED\" ]; then\n        PR_NUMBER=$(gh pr view --json number -q '.number')\n        echo \"Base mismatch on PR #$PR_NUMBER: expected=$EXPECTED actual=$ACTUAL — re-targeting\" >&2\n        gh pr edit \"$PR_NUMBER\" --base \"$EXPECTED\"\n      else\n        echo \"PR base verified: $EXPECTED\"\n      fi\n    depends_on: [finalize-pr]\n\n  - id: review-scope\n    command: archon-pr-review-scope\n    depends_on: [verify-pr-base]\n    context: fresh\n\n  - id: sync\n    command: archon-sync-pr-with-main\n    depends_on: [review-scope]\n    context: fresh\n\n  - id: code-review\n    command: archon-code-review-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: error-handling\n    command: archon-error-handling-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: test-coverage\n    command: archon-test-coverage-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: comment-quality\n    command: archon-comment-quality-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: docs-impact\n    command: archon-docs-impact-agent\n    depends_on: [sync]\n    context: fresh\n\n  - id: synthesize\n    command: archon-synthesize-review\n    depends_on: [code-review, error-handling, test-coverage, comment-quality, docs-impact]\n    trigger_rule: one_success\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 7: FIX REVIEW ISSUES\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: implement-fixes\n    command: archon-implement-review-fixes\n    depends_on: [synthesize]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════════\n  # PHASE 8: FINAL SUMMARY & FOLLOW-UP\n  # ═══════════════════════════════════════════════════════════════════\n\n  - id: workflow-summary\n    command: archon-workflow-summary\n    depends_on: [implement-fixes]\n    context: fresh\n",
  "archon-ralph-dag": "name: archon-ralph-dag\ndescription: |\n  Use when: User wants to run a Ralph implementation loop.\n  Triggers: \"ralph\", \"run ralph\", \"ralph dag\", \"run ralph dag\".\n\n  DAG workflow that:\n  1. Detects input: existing prd.json, existing prd.md (needs stories), or raw idea\n  2. Generates prd.md + prd.json if needed (explores codebase, breaks into stories)\n  3. Validates PRD files, reads project context, installs dependencies\n  4. Runs Ralph loop (fresh context per iteration) implementing one story per iteration\n  5. Creates PR and reports completion\n\n  Accepts: An idea description, a path to an existing prd.md, or a directory with prd.md + prd.json\n\nprovider: claude\n\nnodes:\n  # ═══════════════════════════════════════════════════════════════\n  # NODE 1: DETECT INPUT\n  # Determines what the user provided: full PRD, partial PRD, or idea\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: detect-input\n    model: haiku\n    prompt: |\n      # Detect Ralph Input\n\n      **User input**: $ARGUMENTS\n\n      Determine what the user provided and prepare the PRD directory. Follow these steps exactly:\n\n      ## Step 1: Detect worktree\n\n      Run `git worktree list --porcelain` to check if you're in a worktree.\n      If you see multiple entries, you ARE in a worktree. The first entry (the one without \"branch\" pointing to your current branch) is the **main repo root**. Save it — you'll need it to find files.\n\n      ## Step 2: Classify the input\n\n      Look at the user input above. It's one of three things:\n\n      **Case A — Ralph directory path** (contains `.archon/ralph/`):\n      Extract the directory. Check if both `prd.json` and `prd.md` exist there (try locally first, then in the main repo root if in a worktree).\n\n      **Case B — File path** (ends in `.md`):\n      This is an external PRD file. Find it:\n      1. Try the path as-is (relative to cwd)\n      2. Try it as an absolute path\n      3. If in a worktree, try it relative to the **main repo root** from Step 1\n      Once found, read the file to confirm it's a PRD.\n\n      **Case C — Free text**:\n      Not a file path — it's a feature idea.\n\n      ## Step 3: Auto-discover existing ralph PRDs\n\n      If the input didn't point to a specific path, check if `.archon/ralph/` contains any `prd.json` files:\n      ```bash\n      find .archon/ralph -name \"prd.json\" -type f 2>/dev/null\n      ```\n\n      ## Step 4: Take action based on classification\n\n      **If Case A and both files exist** → output `ready` (no further action needed)\n\n      **If Case B (external PRD found)**:\n      1. Derive a kebab-case slug from the PRD filename or title (e.g., `workflow-lifecycle-overhaul`)\n      2. Create the ralph directory: `mkdir -p .archon/ralph/{slug}`\n      3. Copy the PRD content to `.archon/ralph/{slug}/prd.md`\n      4. Output `external_prd` with the new prd_dir\n\n      **If Case C or auto-discovered ralph dir has prd.md but no prd.json** → output `needs_generation`\n\n      ## Output\n\n      Your final output MUST be exactly one JSON object:\n      ```json\n      {\"input_type\": \"ready|external_prd|needs_generation\", \"prd_dir\": \".archon/ralph/{slug}\"}\n      ```\n    output_format:\n      type: object\n      properties:\n        input_type:\n          type: string\n          enum: [ready, external_prd, needs_generation]\n        prd_dir:\n          type: string\n      required: [input_type, prd_dir]\n\n  # ═══════════════════════════════════════════════════════════════\n  # NODE 2: GENERATE PRD\n  # Scenario 1: User has an idea → generate prd.md + prd.json\n  # Scenario 2: User has prd.md → generate prd.json with stories\n  # Skipped if prd.json already exists\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: generate-prd\n    depends_on: [detect-input]\n    when: \"$detect-input.output.input_type != 'ready'\"\n    command: archon-ralph-generate\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════\n  # NODE 3: VALIDATE & SETUP\n  # Finds PRD directory, reads all state files, installs deps,\n  # verifies the environment is ready for implementation.\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: validate-prd\n    depends_on: [detect-input, generate-prd]\n    trigger_rule: one_success\n    bash: |\n      set -e\n\n      # ── 1. Find PRD directory (passed from detect-input) ──────\n      PRD_DIR=$detect-input.output.prd_dir\n\n      # If detect-input didn't know the PRD dir (generated from scratch), discover it\n      if [ -z \"$PRD_DIR\" ] || [ ! -f \"$PRD_DIR/prd.json\" ]; then\n        FOUND=$(find .archon/ralph -name \"prd.json\" -type f 2>/dev/null | head -1)\n        if [ -n \"$FOUND\" ]; then\n          PRD_DIR=$(dirname \"$FOUND\")\n        fi\n      fi\n\n      if [ -z \"$PRD_DIR\" ] || [ ! -f \"$PRD_DIR/prd.json\" ]; then\n        echo \"ERROR: No prd.json found after generation step.\"\n        echo \"Check the generate-prd node output for errors.\"\n        exit 1\n      fi\n\n      if [ ! -f \"$PRD_DIR/prd.md\" ]; then\n        echo \"ERROR: prd.md not found in $PRD_DIR\"\n        exit 1\n      fi\n\n      # ── 2. Install dependencies (worktrees lack node_modules) ──\n      if [ -f \"bun.lock\" ] || [ -f \"bun.lockb\" ]; then\n        echo \"Installing dependencies (bun)...\"\n        bun install --frozen-lockfile 2>&1 | tail -3\n      elif [ -f \"package-lock.json\" ]; then\n        echo \"Installing dependencies (npm)...\"\n        npm ci 2>&1 | tail -3\n      elif [ -f \"yarn.lock\" ]; then\n        echo \"Installing dependencies (yarn)...\"\n        yarn install --frozen-lockfile 2>&1 | tail -3\n      elif [ -f \"pnpm-lock.yaml\" ]; then\n        echo \"Installing dependencies (pnpm)...\"\n        pnpm install --frozen-lockfile 2>&1 | tail -3\n      fi\n\n      # ── 3. Git state ──────────────────────────────────────────\n      echo \"BRANCH=$(git branch --show-current)\"\n      echo \"GIT_ROOT=$(git rev-parse --show-toplevel)\"\n\n      # ── 4. Output PRD context ─────────────────────────────────\n      echo \"PRD_DIR=$PRD_DIR\"\n      echo \"=== PRD_JSON_START ===\"\n      cat \"$PRD_DIR/prd.json\"\n      echo \"\"\n      echo \"=== PRD_JSON_END ===\"\n      echo \"=== PRD_MD_START ===\"\n      cat \"$PRD_DIR/prd.md\"\n      echo \"\"\n      echo \"=== PRD_MD_END ===\"\n      echo \"=== PROGRESS_START ===\"\n      if [ -f \"$PRD_DIR/progress.txt\" ]; then\n        cat \"$PRD_DIR/progress.txt\"\n      else\n        echo \"(no progress yet)\"\n      fi\n      echo \"\"\n      echo \"=== PROGRESS_END ===\"\n\n      # ── 5. Summary ────────────────────────────────────────────\n      TOTAL=$(grep -c '\"passes\"' \"$PRD_DIR/prd.json\" || true)\n      DONE=$(grep -c '\"passes\": true' \"$PRD_DIR/prd.json\" || true)\n      TOTAL=${TOTAL:-0}\n      DONE=${DONE:-0}\n      echo \"STORIES_TOTAL=$TOTAL\"\n      echo \"STORIES_DONE=$DONE\"\n      echo \"STORIES_REMAINING=$(( TOTAL - DONE ))\"\n\n  # ═══════════════════════════════════════════════════════════════\n  # NODE 4: RALPH IMPLEMENTATION LOOP\n  # Fresh context each iteration. Reads PRD state from disk.\n  # One story per iteration. Validates before committing.\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: implement\n    depends_on: [validate-prd]\n    idle_timeout: 600000\n    model: opus[1m]\n    loop:\n      prompt: |\n        # Ralph Agent — Autonomous Story Implementation\n\n        You are an autonomous coding agent in a FRESH session — you have no memory of previous iterations.\n        Your job: Read state from disk, implement ONE story, validate, commit, update tracking, exit.\n\n        **Golden Rule**: If validation fails, fix it before committing. Never commit broken code. Never skip validation.\n\n        ---\n\n        ## Phase 0: CONTEXT — Load Project State\n\n        The upstream setup node produced this context:\n\n        $validate-prd.output\n\n        **User message**: $USER_MESSAGE\n\n        ---\n\n        ### 0.1 Parse PRD Directory\n\n        Extract the `PRD_DIR=...` line from the context above. This is the directory containing your PRD files.\n        Store this path — use it for ALL file operations below.\n\n        ### 0.2 Read Current State (from disk, not from context above)\n\n        The context above is a snapshot from before the loop started. Previous iterations may have changed files.\n        **You MUST re-read from disk to get the current state:**\n\n        1. **Read `{prd-dir}/progress.txt`** — your only link to previous iterations\n           - Check the `## Codebase Patterns` section FIRST for learnings from prior iterations\n           - Check recent entries for gotchas to avoid\n        2. **Read `{prd-dir}/prd.json`** — the source of truth for story completion state\n        3. **Read `{prd-dir}/prd.md`** — full requirements, technical patterns, acceptance criteria\n\n        ### 0.3 Read Project Rules\n\n        ```bash\n        cat CLAUDE.md\n        ```\n\n        Note all coding standards, patterns, and rules. Follow them exactly.\n\n        **PHASE_0_CHECKPOINT:**\n        - [ ] PRD directory identified\n        - [ ] progress.txt read (or noted as absent)\n        - [ ] prd.json read — know which stories pass/fail\n        - [ ] prd.md read — understand requirements\n        - [ ] CLAUDE.md rules noted\n\n        ---\n\n        ## Phase 1: SELECT — Pick Next Story\n\n        ### 1.1 Find Eligible Story\n\n        From `prd.json`, find the **highest priority** story where:\n        - `passes` is `false`\n        - ALL stories in `dependsOn` have `passes: true`\n\n        **If ALL stories have `passes: true`** → Skip to Phase 6 (Completion).\n\n        **If no eligible stories exist** (all remaining are blocked):\n        ```\n        BLOCKED: No eligible stories. Remaining stories and their blockers:\n        - {story-id}: blocked by {dep-id} (passes: false)\n        ```\n        End normally. The loop will terminate on max_iterations.\n\n        ### 1.2 Announce Selection\n\n        ```\n        ── Story Selected ──────────────────────────────────\n        ID: {story-id}\n        Title: {story-title}\n        Priority: {priority}\n        Dependencies: {deps or \"none\"}\n\n        Acceptance Criteria:\n        - {criterion 1}\n        - {criterion 2}\n        - ...\n        ────────────────────────────────────────────────────\n        ```\n\n        After announcing the selected story, emit the story started event:\n        ```bash\n        bun run cli workflow event emit --run-id $WORKFLOW_ID --type ralph_story_started --data '{\"story_id\":\"{story-id}\",\"title\":\"{story-title}\"}' || true\n        ```\n\n        **PHASE_1_CHECKPOINT:**\n        - [ ] Eligible story found (or all complete / all blocked)\n        - [ ] Acceptance criteria understood\n        - [ ] Dependencies verified as complete\n\n        ---\n\n        ## Phase 2: IMPLEMENT — Code the Story\n\n        ### 2.1 Explore Before Coding\n\n        Before writing any code:\n        1. Read all files you plan to modify — understand current state\n        2. Check `## Codebase Patterns` in progress.txt for discovered patterns\n        3. Look for similar implementations in the codebase to mirror\n        4. Read the `technicalNotes` field from the story in prd.json\n\n        ### 2.2 Implementation Rules\n\n        **DO:**\n        - Implement ONLY the selected story — one story per iteration\n        - Follow existing code patterns exactly (naming, structure, imports, error handling)\n        - Match the project's coding standards from CLAUDE.md\n        - Write or update tests as required by acceptance criteria\n        - Keep changes minimal and focused\n\n        **DON'T:**\n        - Refactor unrelated code\n        - Add improvements not in the acceptance criteria\n        - Change formatting of lines you didn't modify\n        - Install new dependencies without justification from prd.md\n        - Touch files unrelated to this story\n        - Over-engineer — do the simplest thing that satisfies the criteria\n\n        ### 2.3 Verify Types After Each File\n\n        After modifying each file, run:\n        ```bash\n        bun run type-check\n        ```\n\n        **If types fail:**\n        1. Read the error carefully\n        2. Fix the type issue in your code\n        3. Re-run type-check\n        4. Do NOT proceed to the next file until types pass\n\n        **PHASE_2_CHECKPOINT:**\n        - [ ] Only the selected story was implemented\n        - [ ] Types compile after each file change\n        - [ ] Tests written/updated as needed\n        - [ ] No unrelated changes\n\n        ---\n\n        ## Phase 3: VALIDATE — Full Verification\n\n        ### 3.1 Static Analysis\n\n        ```bash\n        bun run type-check && bun run lint\n        ```\n\n        **Must pass with zero errors and zero warnings.**\n\n        **If lint fails:**\n        1. Run `bun run lint:fix` for auto-fixable issues\n        2. Manually fix remaining issues\n        3. Re-run lint\n        4. Proceed only when clean\n\n        ### 3.2 Tests\n\n        ```bash\n        bun run test\n        ```\n\n        **All tests must pass.**\n\n        **If tests fail:**\n        1. Read the failure output\n        2. Determine: bug in your implementation or pre-existing failure?\n        3. If your bug → fix the implementation (not the test)\n        4. If pre-existing → note it but don't fix unrelated tests\n        5. Re-run tests\n        6. Repeat until green\n\n        ### 3.3 Format Check\n\n        ```bash\n        bun run format:check\n        ```\n\n        **If formatting fails:**\n        ```bash\n        bun run format\n        ```\n\n        ### 3.4 Verify Acceptance Criteria\n\n        Go through EACH acceptance criterion from the story:\n        - Is it satisfied by your implementation?\n        - Can you verify it (read the code, run a command, check a file)?\n\n        If a criterion is NOT met, go back to Phase 2 and fix it.\n\n        **PHASE_3_CHECKPOINT:**\n        - [ ] Type-check passes\n        - [ ] Lint passes (0 errors, 0 warnings)\n        - [ ] All tests pass\n        - [ ] Format is clean\n        - [ ] Every acceptance criterion verified\n\n        ---\n\n        ## Phase 4: COMMIT — Save Changes\n\n        ### 4.1 Stage Only Files You Edited\n\n        Stage **only** the files you actually edited for this story — never `git add -A`, `git add .`, or `git add -u`. List them by name:\n\n        ```bash\n        git add path/to/file1 path/to/file2 ...\n        git status --porcelain  # verify nothing scratch/review/PR-body is staged\n        git diff --cached --stat\n        ```\n\n        **Never stage** scratch / review / PR-body artifacts, even if they show up in `git status`:\n\n        - `.pr-body.md`, `pr-body.md`, `*.scratch.md`, `*.tmp.md`\n        - `review/`, `*-report.md` at the repo root\n        - Anything under `$ARTIFACTS_DIR`\n\n        Verify only expected files are staged. If unexpected files appear, investigate before committing.\n\n        ### 4.2 Write Commit Message\n\n        ```bash\n        git commit -m \"$(cat <<'EOF'\n        feat: {story-title}\n\n        Implements {story-id} from PRD.\n\n        Changes:\n        - {change 1}\n        - {change 2}\n        - {change 3}\n        EOF\n        )\"\n        ```\n\n        **Commit message rules:**\n        - Prefix: `feat:` for features, `fix:` for bugs, `refactor:` for refactors\n        - Title: the story title (not the PRD name)\n        - Body: list the actual changes made\n        - Do NOT include AI attribution\n\n        **PHASE_4_CHECKPOINT:**\n        - [ ] Only expected files committed\n        - [ ] Commit message is clear and accurate\n        - [ ] Working directory is clean after commit\n\n        ---\n\n        ## Phase 5: TRACK — Update Progress Files\n\n        ### 5.1 Update prd.json\n\n        Set `passes: true` and add a note for the completed story:\n\n        ```json\n        {\n          \"id\": \"{story-id}\",\n          \"passes\": true,\n          \"notes\": \"Implemented in iteration {N}. Files: {list}.\"\n        }\n        ```\n\n        After updating prd.json, emit the story completed event:\n        ```bash\n        bun run cli workflow event emit --run-id $WORKFLOW_ID --type ralph_story_completed --data '{\"story_id\":\"{story-id}\",\"title\":\"{story-title}\"}' || true\n        ```\n\n        ### 5.2 Update progress.txt\n\n        **Append** to `{prd-dir}/progress.txt`:\n\n        ```\n        ## {ISO Date} — {story-id}: {story-title}\n\n        **Status**: PASSED\n        **Files changed**:\n        - {file1} — {what changed}\n        - {file2} — {what changed}\n\n        **Acceptance criteria verified**:\n        - [x] {criterion 1}\n        - [x] {criterion 2}\n\n        **Learnings**:\n        - {Any pattern discovered}\n        - {Any gotcha encountered}\n        - {Any deviation from expected approach}\n\n        ---\n        ```\n\n        ### 5.3 Update Codebase Patterns (if applicable)\n\n        If you discovered a **reusable pattern** that future iterations should know about, **prepend** it to the `## Codebase Patterns` section at the TOP of progress.txt.\n\n        Format:\n        ```\n        ## Codebase Patterns\n\n        ### {Pattern Name}\n        - **Where**: `{file:lines}`\n        - **Pattern**: {description}\n        - **Example**: `{code snippet}`\n        ```\n\n        If the `## Codebase Patterns` section doesn't exist yet, create it at the top of the file.\n\n        **PHASE_5_CHECKPOINT:**\n        - [ ] prd.json updated with `passes: true`\n        - [ ] progress.txt appended with iteration details\n        - [ ] Codebase patterns updated (if applicable)\n\n        ---\n\n        ## Phase 6: COMPLETE — Check All Stories\n\n        ### 6.1 Re-read prd.json\n\n        ```bash\n        cat {prd-dir}/prd.json\n        ```\n\n        Count stories where `passes: false`.\n\n        ### 6.2 If ALL Stories Pass\n\n        1. **Push the branch:**\n           ```bash\n           git push -u origin HEAD\n           ```\n\n        2. **Read the PR template:**\n           Look for a PR template in the repo — check `.github/pull_request_template.md`, `.github/PULL_REQUEST_TEMPLATE.md`, and `docs/pull_request_template.md`. Read whichever one exists.\n\n           If a template was found, fill in **every section** using the context from this implementation. Don't skip sections or leave placeholders — fill them honestly based on the actual changes (summary, architecture, validation evidence, security, compatibility, rollback, etc.).\n\n           If no template was found, write a summary with: problem, what changed, stories table, and validation evidence.\n\n        3. **Create a draft PR** using `gh pr create --draft --base $BASE_BRANCH --title \"feat: {PRD feature name}\"` with the filled-in template as the body. Use a HEREDOC for the body.\n\n        4. **Output completion signal:**\n           ```\n           <promise>COMPLETE</promise>\n           ```\n\n        ### 6.3 If Stories Remain\n\n        Report status and end normally:\n        ```\n        ── Iteration Complete ──────────────────────────────\n        Story completed: {story-id} — {story-title}\n        Stories remaining: {count}\n        Next eligible: {next-story-id} — {next-story-title}\n        ────────────────────────────────────────────────────\n        ```\n\n        The loop engine will start the next iteration with a fresh context.\n\n        ---\n\n        ## Handling Edge Cases\n\n        ### Validation fails repeatedly\n        - If type-check or tests fail 3+ times on the same error, step back\n        - Re-read the acceptance criteria — you may be misunderstanding the requirement\n        - Check if the story is too large (needs breaking down)\n        - Note the blocker in progress.txt and end the iteration\n\n        ### Story is too large for one iteration\n        - Implement the minimum viable subset that satisfies the most critical acceptance criteria\n        - Set `passes: true` only if ALL criteria are met\n        - If you can't meet all criteria, leave `passes: false` and note what's done in progress.txt\n        - The next iteration will pick it up and continue\n\n        ### Pre-existing test failures\n        - If tests were failing BEFORE your changes, note them but don't fix unrelated code\n        - Run only the test files related to your changes if the full suite has pre-existing issues\n        - Document pre-existing failures in progress.txt\n\n        ### Dependency install fails\n        - Check if `bun.lock` or equivalent exists\n        - Try `bun install` without `--frozen-lockfile`\n        - Note the issue in progress.txt\n\n        ### Git state is dirty at iteration start\n        - This shouldn't happen (fresh worktree), but if it does:\n        - Run `git status` to understand what's dirty\n        - If it's leftover from a failed previous iteration, commit or stash\n        - Never discard changes silently\n\n        ### Blocked stories — all remaining have unmet dependencies\n        - Report the dependency chain in your output\n        - Check if a dependency was incorrectly left as `passes: false`\n        - If a dependency should be `passes: true` (the code exists and works), fix prd.json\n        - Otherwise, end the iteration — the loop will exhaust max_iterations\n\n        ---\n\n        ## File Format Reference\n\n        ### prd.json Schema\n\n        ```json\n        {\n          \"feature\": \"Feature Name\",\n          \"issueNumber\": 123,\n          \"userStories\": [\n            {\n              \"id\": \"US-001\",\n              \"title\": \"Short title\",\n              \"description\": \"As a..., I want..., so that...\",\n              \"acceptanceCriteria\": [\"criterion 1\", \"criterion 2\"],\n              \"technicalNotes\": \"Implementation hints\",\n              \"dependsOn\": [\"US-000\"],\n              \"priority\": 1,\n              \"passes\": false,\n              \"notes\": \"\"\n            }\n          ]\n        }\n        ```\n\n        ### progress.txt Format\n\n        ```\n        ## Codebase Patterns\n\n        ### {Pattern Name}\n        - Where: `file:lines`\n        - Pattern: description\n        - Example: `code`\n\n        ---\n\n        ## {Date} — {story-id}: {title}\n\n        **Status**: PASSED\n        **Files changed**: ...\n        **Acceptance criteria verified**: ...\n        **Learnings**: ...\n\n        ---\n        ```\n\n        ---\n\n        ## Success Criteria\n\n        - **ONE_STORY**: Exactly one story implemented per iteration\n        - **VALIDATED**: Type-check + lint + tests + format all pass before commit\n        - **COMMITTED**: Changes committed with clear message\n        - **TRACKED**: prd.json and progress.txt updated accurately\n        - **PATTERNS_SHARED**: Discovered patterns added to progress.txt for future iterations\n        - **NO_SCOPE_CREEP**: No unrelated changes, no refactoring, no \"improvements\"\n      until: COMPLETE\n      max_iterations: 15\n      fresh_context: true\n\n  - id: verify-pr-base\n    bash: |\n      set -euo pipefail\n      EXPECTED=\"$BASE_BRANCH\"\n      ACTUAL=$(gh pr view --json baseRefName -q '.baseRefName')\n      if [ \"$ACTUAL\" != \"$EXPECTED\" ]; then\n        PR_NUMBER=$(gh pr view --json number -q '.number')\n        echo \"Base mismatch on PR #$PR_NUMBER: expected=$EXPECTED actual=$ACTUAL — re-targeting\" >&2\n        gh pr edit \"$PR_NUMBER\" --base \"$EXPECTED\"\n      else\n        echo \"PR base verified: $EXPECTED\"\n      fi\n    depends_on: [implement]\n\n  # ═══════════════════════════════════════════════════════════════\n  # NODE 5: COMPLETION REPORT\n  # Reads final state and produces a summary.\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: report\n    depends_on: [verify-pr-base]\n    prompt: |\n      # Completion Report\n\n      The Ralph implementation loop has finished. Generate a completion report.\n\n      ## Context\n\n      **Loop output (last iteration):**\n\n      $implement.output\n\n      **Setup context:**\n\n      $validate-prd.output\n\n      ---\n\n      ## Instructions\n\n      ### 1. Read Final State\n\n      Extract the `PRD_DIR=...` from the setup context above.\n      Read the CURRENT files from disk:\n\n      ```bash\n      cat {prd-dir}/prd.json\n      cat {prd-dir}/progress.txt\n      ```\n\n      ### 2. Gather Git Info\n\n      ```bash\n      git log --oneline --no-merges $(git merge-base HEAD $BASE_BRANCH)..HEAD\n      git diff --stat $(git merge-base HEAD $BASE_BRANCH)..HEAD\n      ```\n\n      ### 3. Check PR Status\n\n      ```bash\n      gh pr view HEAD --json url,number,state 2>/dev/null || echo \"No PR found\"\n      ```\n\n      ### 4. Generate Report\n\n      Output this format:\n\n      ```\n      ═══════════════════════════════════════════════════════\n      RALPH DAG — COMPLETION REPORT\n      ═══════════════════════════════════════════════════════\n\n      Feature: {feature name from prd.json}\n      PRD: {prd-dir}\n      Branch: {branch name}\n      PR: {url or \"not created\"}\n\n      ── Stories ─────────────────────────────────────────\n\n      | ID | Title | Status |\n      |----|-------|--------|\n      {for each story from prd.json}\n\n      Total: {N}/{M} stories passing\n\n      ── Commits ─────────────────────────────────────────\n\n      {git log output}\n\n      ── Files Changed ─────────────────────────────────\n\n      {git diff --stat output}\n\n      ── Patterns Discovered ─────────────────────────────\n\n      {from ## Codebase Patterns in progress.txt, or \"None\"}\n\n      ═══════════════════════════════════════════════════════\n      ```\n\n      Keep it factual. No commentary — just the data.\n",
  "archon-refactor-safely": "name: archon-refactor-safely\ndescription: |\n  Use when: User wants to refactor code safely with continuous validation and behavior preservation.\n  Triggers: \"refactor\", \"refactor safely\", \"split this file\", \"extract module\", \"break up\",\n            \"decompose\", \"safe refactor\", \"split file\", \"extract into modules\".\n  Does: Scans refactoring scope -> analyzes impact (read-only) -> plans ordered task list ->\n        executes with type-check hooks after every edit -> validates full suite ->\n        verifies behavior preservation (read-only) -> creates PR with before/after comparison.\n  NOT for: Bug fixes (use archon-fix-github-issue), feature development (use archon-feature-development),\n           general architecture sweeps (use archon-architect), PR reviews.\n\n  Key safety features:\n  - Analysis and verification nodes are read-only (denied_tools: [Write, Edit, Bash])\n  - PreToolUse hooks check if each edit is in the plan\n  - PostToolUse hooks force type-check after every file change\n  - Behavior verification confirms no logic changes after refactoring\n\nprovider: claude\n\nnodes:\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 1: SCAN — Find files matching the refactoring target\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: scan-scope\n    bash: |\n      echo \"=== REFACTORING TARGET ===\"\n      echo \"User request: $ARGUMENTS\"\n      echo \"\"\n\n      echo \"=== FILE SIZE ANALYSIS (source files by size) ===\"\n      find . -name '*.ts' -not -path '*/node_modules/*' -not -path '*/.git/*' -not -path '*/dist/*' -not -name '*.test.ts' -not -name '*.d.ts' \\\n        -exec wc -l {} + 2>/dev/null | sort -rn | head -30\n      echo \"\"\n\n      echo \"=== FILES OVER 500 LINES ===\"\n      find . -name '*.ts' -not -path '*/node_modules/*' -not -path '*/.git/*' -not -path '*/dist/*' -not -name '*.test.ts' -not -name '*.d.ts' \\\n        -exec sh -c 'lines=$(wc -l < \"$1\"); if [ \"$lines\" -gt 500 ]; then echo \"$lines $1\"; fi' _ {} \\; 2>/dev/null | sort -rn\n      echo \"\"\n\n      echo \"=== FUNCTION COUNT PER FILE (top 20) ===\"\n      for f in $(find . -name '*.ts' -not -path '*/node_modules/*' -not -path '*/.git/*' -not -path '*/dist/*' -not -name '*.test.ts' -not -name '*.d.ts'); do\n        count=$(grep -cE '^\\s*(export\\s+)?(async\\s+)?function\\s|=>\\s*\\{' \"$f\" 2>/dev/null) || count=0\n        if [ \"$count\" -gt 5 ]; then\n          echo \"$count functions: $f\"\n        fi\n      done | sort -rn | head -20\n      echo \"\"\n\n      echo \"=== EXPORT ANALYSIS (files with many exports) ===\"\n      for f in $(find . -name '*.ts' -not -path '*/node_modules/*' -not -path '*/.git/*' -not -path '*/dist/*' -not -name '*.test.ts' -not -name '*.d.ts'); do\n        count=$(grep -c \"^export \" \"$f\" 2>/dev/null) || count=0\n        if [ \"$count\" -gt 5 ]; then\n          echo \"$count exports: $f\"\n        fi\n      done | sort -rn | head -20\n    timeout: 60000\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 2: ANALYZE IMPACT — Read-only deep analysis\n  # Maps call sites, identifies risk areas, understands dependencies\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: analyze-impact\n    prompt: |\n      You are a senior software engineer analyzing code for a safe refactoring.\n\n      ## Refactoring Request\n\n      $ARGUMENTS\n\n      ## Codebase Scan Results\n\n      $scan-scope.output\n\n      ## Instructions\n\n      1. Identify the PRIMARY file(s) targeted for refactoring based on the user's request\n         and the scan results above\n      2. Read each target file thoroughly — understand every function, type, and export\n      3. For each target file, map ALL call sites:\n         - Use Grep to find every import of the target file across the codebase\n         - Track which specific exports are used and where\n         - Note any dynamic imports or re-exports through index files\n      4. Identify risk areas:\n         - Functions with complex internal dependencies (shared closures, module-level state)\n         - Circular dependencies between functions in the file\n         - Any module-level side effects (top-level `const`, initialization code)\n         - Exports that are part of the public API vs internal-only\n      5. Check for existing tests:\n         - Find test files for the target module(s)\n         - Note what's tested and what isn't\n\n      ## Output\n\n      Write a thorough impact analysis to `$ARTIFACTS_DIR/impact-analysis.md` with:\n\n      ### Target Files\n      - File path, line count, function count\n      - List of all exported symbols with brief descriptions\n\n      ### Dependency Map\n      - Which files import from the target (with specific imports used)\n      - Which files the target imports from\n\n      ### Risk Assessment\n      - Module-level state or side effects\n      - Complex internal dependencies between functions\n      - Public API surface that must be preserved exactly\n\n      ### Test Coverage\n      - Existing test files and what they cover\n      - Critical paths that must remain tested\n\n      ### Recommended Decomposition Strategy\n      - Suggested module boundaries (which functions group together)\n      - Rationale for each grouping (cohesion, shared dependencies)\n    depends_on: [scan-scope]\n    context: fresh\n    denied_tools: [Write, Edit, Bash]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 3: PLAN REFACTOR — Ordered task list with rollback strategy\n  # Read-only: produces the plan, does not execute it\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: plan-refactor\n    prompt: |\n      You are planning a safe refactoring. You must produce a precise, ordered plan\n      that another agent will follow literally.\n\n      ## Impact Analysis\n\n      $analyze-impact.output\n\n      ## Refactoring Goal\n\n      $ARGUMENTS\n\n      ## Principles\n\n      - **Behavior preservation**: The refactoring must NOT change any behavior — only structure\n      - **Incremental**: Each step must leave the codebase in a compilable state\n      - **Reversible**: Each step can be independently reverted\n      - **No mixed concerns**: Do not combine refactoring with bug fixes or improvements\n      - **Preserve public API**: All existing exports must remain accessible from the same import paths\n      - **Maximum file size**: Target 500 lines or fewer per file after refactoring\n\n      ## Instructions\n\n      1. Read the impact analysis from `$ARTIFACTS_DIR/impact-analysis.md`\n      2. Read the target file(s) to understand the current structure\n      3. Design the decomposition:\n         - Group related functions into cohesive modules\n         - Identify shared utilities, types, and constants\n         - Plan the new file structure with descriptive names\n      4. Write an ordered task list where each task is:\n         - Independent and leaves code compilable after completion\n         - Specific about what to extract and where\n         - Clear about import updates needed\n\n      ## Output\n\n      Write the plan to `$ARTIFACTS_DIR/refactor-plan.md` with:\n\n      ### File Structure (Before)\n      ```\n      [current structure with line counts]\n      ```\n\n      ### File Structure (After)\n      ```\n      [planned structure with estimated line counts]\n      ```\n\n      ### Ordered Tasks\n\n      For each task:\n      ```\n      ## Task N: [brief description]\n\n      **Action**: CREATE | EXTRACT | UPDATE\n      **Source**: [source file]\n      **Target**: [target file]\n      **What moves**:\n      - function functionName (lines X-Y)\n      - type TypeName (lines X-Y)\n\n      **Import updates needed**:\n      - [file]: change import from [old] to [new]\n\n      **Rollback**: [how to undo this specific step]\n      ```\n\n      ### Validation Commands\n      - Type check: `bun run type-check`\n      - Lint: `bun run lint`\n      - Tests: `bun run test`\n      - Format: `bun run format:check`\n    depends_on: [analyze-impact]\n    context: fresh\n    denied_tools: [Write, Edit, Bash]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 4: EXECUTE REFACTOR — Implements the plan with guardrails\n  # Hooks enforce type-check after every edit and plan adherence\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: execute-refactor\n    model: opus[1m]\n    prompt: |\n      You are executing a refactoring plan with strict safety guardrails.\n\n      ## Plan\n\n      Read the full plan from `$ARTIFACTS_DIR/refactor-plan.md` — follow it LITERALLY.\n\n      ## Rules\n\n      - **Follow the plan exactly** — do not add extra improvements or cleanups\n      - **One task at a time** — complete each task fully before starting the next\n      - **Type-check after every file change** — you'll be prompted to do this after each edit\n      - **Preserve all behavior** — refactoring means moving code, not changing it\n      - **Preserve the public API** — if the original file exported something, it must still be\n        importable from the same path (use re-exports in the original file if needed)\n      - **Update all import sites** — every file that imported from the original must be updated\n      - **Commit after each logical task** — one commit per plan task with a clear message\n\n      ## Process for Each Task\n\n      1. Read the plan task\n      2. Read the source file to understand current state\n      3. Create the new file (if extracting) with the functions/types being moved\n      4. Update the source file to remove the moved code and add imports from the new file\n      5. Update the original file's exports to re-export from the new module (API preservation)\n      6. Use Grep to find and update ALL import sites across the codebase\n      7. Run `bun run type-check` to verify (you'll be reminded by hooks)\n      8. Commit ONLY the files you edited for this task — never `git add -A`. Stage by name, then commit:\n         ```bash\n         git add path/to/file1 path/to/file2 ...\n         git status --porcelain  # verify nothing scratch is staged\n         git commit -m \"refactor: [task description]\"\n         ```\n         **Never stage**: `.pr-body.md`, `pr-body.md`, `*.scratch.md`, `*.tmp.md`, `review/`, `*-report.md` at the repo root, or anything under `$ARTIFACTS_DIR`.\n      9. Move to next task\n\n      ## Handling Problems\n\n      - If type-check fails after a change: fix it immediately before proceeding\n      - If a task is more complex than planned: complete it anyway, note the deviation\n      - If you discover the plan missed an import site: update it and note it\n      - NEVER skip a task — complete them in order\n    depends_on: [plan-refactor]\n    context: fresh\n    hooks:\n      PreToolUse:\n        - matcher: \"Write|Edit\"\n          response:\n            hookSpecificOutput:\n              hookEventName: PreToolUse\n              additionalContext: >\n                Before modifying this file: Is this file in your refactoring plan\n                ($ARTIFACTS_DIR/refactor-plan.md)? If it's not a planned target file\n                AND not a file that imports from the target, explain why you're touching it.\n                Unplanned changes increase risk.\n      PostToolUse:\n        - matcher: \"Write|Edit\"\n          response:\n            systemMessage: >\n              You just modified a file. STOP and do these things NOW before making any\n              other changes:\n              1. Run `bun run type-check` to verify the change compiles\n              2. If type-check fails, fix the error immediately\n              3. Verify you preserved the exact same behavior — no logic changes, only structural moves\n              Only proceed to the next change after type-check passes.\n        - matcher: \"Bash\"\n          response:\n            hookSpecificOutput:\n              hookEventName: PostToolUse\n              additionalContext: >\n                Check the exit code. If type-check or any validation failed, fix the issue\n                before continuing. Do not accumulate broken state.\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 5: VALIDATE — Full test suite (bash, no AI escape hatch)\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: validate\n    bash: |\n      echo \"=== TYPE CHECK ===\"\n      bun run type-check 2>&1\n      TC_EXIT=$?\n\n      echo \"\"\n      echo \"=== LINT ===\"\n      bun run lint 2>&1\n      LINT_EXIT=$?\n\n      echo \"\"\n      echo \"=== FORMAT CHECK ===\"\n      bun run format:check 2>&1\n      FMT_EXIT=$?\n\n      echo \"\"\n      echo \"=== TESTS ===\"\n      bun run test 2>&1\n      TEST_EXIT=$?\n\n      echo \"\"\n      echo \"=== FILE SIZE CHECK ===\"\n      echo \"Files still over 500 lines:\"\n      find . -name '*.ts' -not -path '*/node_modules/*' -not -path '*/.git/*' -not -path '*/dist/*' -not -name '*.test.ts' -not -name '*.d.ts' \\\n        -exec sh -c 'lines=$(wc -l < \"$1\"); if [ \"$lines\" -gt 500 ]; then echo \"$lines $1\"; fi' _ {} \\; 2>/dev/null | sort -rn\n      echo \"\"\n\n      echo \"=== RESULTS ===\"\n      echo \"Type check: $([ $TC_EXIT -eq 0 ] && echo 'PASS' || echo 'FAIL')\"\n      echo \"Lint: $([ $LINT_EXIT -eq 0 ] && echo 'PASS' || echo 'FAIL')\"\n      echo \"Format: $([ $FMT_EXIT -eq 0 ] && echo 'PASS' || echo 'FAIL')\"\n      echo \"Tests: $([ $TEST_EXIT -eq 0 ] && echo 'PASS' || echo 'FAIL')\"\n\n      if [ $TC_EXIT -eq 0 ] && [ $LINT_EXIT -eq 0 ] && [ $FMT_EXIT -eq 0 ] && [ $TEST_EXIT -eq 0 ]; then\n        echo \"VALIDATION_STATUS: PASS\"\n      else\n        echo \"VALIDATION_STATUS: FAIL\"\n      fi\n    depends_on: [execute-refactor]\n    timeout: 300000\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 6: FIX VALIDATION FAILURES (if any)\n  # Only does real work if validation failed\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: fix-failures\n    prompt: |\n      Review the validation output below.\n\n      ## Validation Output\n\n      $validate.output\n\n      ## Instructions\n\n      If the output ends with \"VALIDATION_STATUS: PASS\", respond with\n      \"All checks passed — no fixes needed.\" and stop.\n\n      If there are failures:\n\n      1. Read the validation failures carefully\n      2. Fix ONLY what's broken — do not make additional improvements\n      3. If a fix requires changing behavior (not just fixing a type/lint error),\n         revert the original change instead\n      4. Run the specific failing check after each fix to confirm it passes\n      5. After all fixes, run the full validation suite: `bun run validate`\n\n      If there are files still over 500 lines, note them but do NOT attempt further\n      splitting in this node — that would require a new plan cycle.\n    depends_on: [validate]\n    context: fresh\n    hooks:\n      PostToolUse:\n        - matcher: \"Write|Edit\"\n          response:\n            systemMessage: >\n              You just made a fix. Run the specific failing validation check NOW\n              to verify your fix works. Do not batch fixes — verify each one.\n      PreToolUse:\n        - matcher: \"Write|Edit\"\n          response:\n            hookSpecificOutput:\n              hookEventName: PreToolUse\n              additionalContext: >\n                You are fixing validation failures only. Do not make any changes\n                beyond what's needed to pass the failing checks. If in doubt, revert\n                the original change that caused the failure.\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 7: VERIFY BEHAVIOR — Read-only confirmation\n  # Ensures the refactoring preserved behavior by tracing call paths\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: verify-behavior\n    prompt: |\n      You are a code reviewer verifying that a refactoring preserved exact behavior.\n      You can ONLY read files — you cannot make any changes.\n\n      ## Refactoring Plan\n\n      Read the plan from `$ARTIFACTS_DIR/refactor-plan.md` to understand what was intended.\n\n      ## Instructions\n\n      1. Use Grep and Glob to find all files in the new module locations listed in\n         the plan, then Read each one. (Note: Bash is denied in this read-only node,\n         so use Grep/Glob/Read to discover changes instead of git commands.)\n      2. For each new file created by the refactoring:\n         - Verify the extracted functions match the originals exactly (no logic changes)\n         - Check that all types and interfaces are preserved\n      3. For the original file(s):\n         - Verify re-exports exist for all symbols that were previously exported\n         - Confirm no function bodies were changed (only moved)\n      4. For all import sites updated:\n         - Verify imports resolve to the correct new locations\n         - Check that no import was missed\n      5. Verify the public API is preserved:\n         - Any code that imported from the original file should still work unchanged\n         - Re-exports in the original file should cover all moved symbols\n\n      ## Output\n\n      Write your verification report to `$ARTIFACTS_DIR/behavior-verification.md`:\n\n      ### Verdict: PASS | FAIL\n\n      ### Functions Verified\n      | Function | Original Location | New Location | Behavior Preserved |\n      |----------|------------------|--------------|-------------------|\n      | funcName | file.ts:42 | new-file.ts:10 | Yes/No |\n\n      ### Public API Check\n      - [ ] All original exports still accessible from original import path\n      - [ ] Re-exports correctly configured\n\n      ### Import Sites Updated\n      - [ ] All N import sites verified\n\n      ### Issues Found\n      [List any behavior changes detected, or \"None — refactoring is behavior-preserving\"]\n    depends_on: [fix-failures]\n    context: fresh\n    denied_tools: [Write, Edit, Bash]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 8: CREATE PR — Detailed description with before/after\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: create-pr\n    prompt: |\n      Create a pull request for the refactoring.\n\n      ## Context\n\n      - **Refactoring goal**: $ARGUMENTS\n      - **Impact analysis**: Read `$ARTIFACTS_DIR/impact-analysis.md`\n      - **Refactoring plan**: Read `$ARTIFACTS_DIR/refactor-plan.md`\n      - **Validation**: $validate.output\n      - **Behavior verification**: Read `$ARTIFACTS_DIR/behavior-verification.md`\n\n      ## Instructions\n\n      1. Stage all changes and create a final commit if there are uncommitted changes\n      2. Push the branch: `git push -u origin HEAD`\n      3. Check if a PR already exists: `gh pr list --head $(git branch --show-current)`\n      4. Create the PR targeting `$BASE_BRANCH` as the base branch:\n         `gh pr create --base $BASE_BRANCH --title \"...\" --body \"...\"`, then format\n         title/body per the template below\n      5. Save the PR URL to `$ARTIFACTS_DIR/.pr-url`\n\n      ## PR Format\n\n      - **Title**: `refactor: [concise description]` (under 70 chars)\n      - **Body**:\n\n      ```markdown\n      ## Refactoring: [goal]\n\n      ### Motivation\n\n      [Why this refactoring was needed — file sizes, complexity, maintainability]\n\n      ### Before\n\n      ```\n      [Original file structure with line counts from the plan]\n      ```\n\n      ### After\n\n      ```\n      [New file structure with line counts]\n      ```\n\n      ### Changes\n\n      [For each new module: what was extracted and why it's a cohesive unit]\n\n      ### Safety\n\n      - [x] Type check passes\n      - [x] Lint passes\n      - [x] Tests pass (all existing tests still green)\n      - [x] Public API preserved (re-exports maintain backward compatibility)\n      - [x] Behavior verification passed (read-only audit confirmed no logic changes)\n      - [x] Each task committed separately for easy review/revert\n\n      ### Review Guide\n\n      Each commit represents one extraction step. Review commits individually for easiest review.\n      All commits are behavior-preserving structural moves.\n      ```\n    depends_on: [verify-behavior]\n    context: fresh\n    hooks:\n      PreToolUse:\n        - matcher: \"Write|Edit\"\n          response:\n            hookSpecificOutput:\n              hookEventName: PreToolUse\n              permissionDecision: deny\n              permissionDecisionReason: \"PR creation node — do not modify source files. Use only git and gh commands.\"\n      PostToolUse:\n        - matcher: \"Bash\"\n          response:\n            hookSpecificOutput:\n              hookEventName: PostToolUse\n              additionalContext: >\n                Verify this command succeeded. If git push or gh pr create failed,\n                read the error message carefully before retrying.\n\n  - id: verify-pr-base\n    bash: |\n      set -euo pipefail\n      EXPECTED=\"$BASE_BRANCH\"\n      ACTUAL=$(gh pr view --json baseRefName -q '.baseRefName')\n      if [ \"$ACTUAL\" != \"$EXPECTED\" ]; then\n        PR_NUMBER=$(gh pr view --json number -q '.number')\n        echo \"Base mismatch on PR #$PR_NUMBER: expected=$EXPECTED actual=$ACTUAL — re-targeting\" >&2\n        gh pr edit \"$PR_NUMBER\" --base \"$EXPECTED\"\n      else\n        echo \"PR base verified: $EXPECTED\"\n      fi\n    depends_on: [create-pr]\n",
  "archon-remotion-generate": "name: archon-remotion-generate\ndescription: |\n  Use when: User wants to generate or modify a Remotion video composition using AI.\n  Triggers: \"create a video\", \"generate video\", \"remotion\", \"make an animation\",\n            \"video about\", \"animate\".\n  Does: AI writes Remotion React code -> renders preview stills -> renders full video ->\n        summarizes the output.\n  Requires: A Remotion project in the working directory (src/index.ts, src/Root.tsx).\n  Optional: Install the remotion-best-practices skill for higher quality output:\n            npx skills add remotion-dev/skills\n\nnodes:\n  # ── Layer 0: Check project structure ──────────────────────────────────\n  - id: check-project\n    bash: |\n      if [ ! -f \"src/index.ts\" ] || [ ! -f \"src/Root.tsx\" ]; then\n        echo \"ERROR: Not a Remotion project. Expected src/index.ts and src/Root.tsx.\"\n        echo \"Run 'npx create-video@latest' first, then run this workflow from that directory.\"\n        exit 1\n      fi\n      echo \"Remotion project detected.\"\n      npx remotion compositions src/index.ts 2>&1 | tail -5\n      echo \"\"\n      echo \"PROJECT_READY\"\n    timeout: 60000\n\n  # ── Layer 1: Generate composition code ────────────────────────────────\n  - id: generate\n    prompt: |\n      You are working in a Remotion video project. The project root is the current directory.\n\n      Find and read the existing composition files to understand the project structure.\n      Look in src/ for Root.tsx and any composition components.\n\n      Now create or modify the composition to match this request:\n\n      $ARGUMENTS\n\n      Rules:\n      - Use useCurrentFrame() and interpolate()/spring() for ALL animations\n      - Never use CSS transitions, Math.random(), setTimeout, or Date.now()\n      - Use AbsoluteFill for layout, Sequence for scene timing\n      - Use the <Img> component from 'remotion' (not native <img>) for images\n      - Keep dimensions 1920x1080 at 30 fps unless the user specifies otherwise\n      - Update the Zod schema and defaultProps in Root.tsx if you change props\n      - Use even numbers for width/height (required for MP4)\n      - Always clamp interpolations: extrapolateLeft: 'clamp', extrapolateRight: 'clamp'\n\n      After writing the code, read it back to verify it looks correct.\n    depends_on: [check-project]\n    skills:\n      - remotion-best-practices\n    allowed_tools:\n      - Read\n      - Write\n      - Edit\n      - Glob\n\n  # ── Layer 2: Render preview stills ────────────────────────────────────\n  - id: render-preview\n    bash: |\n      mkdir -p out\n      COMP_ID=$(npx remotion compositions src/index.ts 2>&1 | grep -E '^\\S' | head -1 | awk '{print $1}')\n      if [ -z \"$COMP_ID\" ]; then\n        echo \"RENDER_FAILED: Could not detect composition ID\"\n        exit 1\n      fi\n      echo \"Composition: $COMP_ID\"\n\n      DURATION=$(npx remotion compositions src/index.ts 2>&1 | grep -E '^\\S' | head -1 | awk '{print $4}')\n      MID_FRAME=$(( ${DURATION:-150} / 2 ))\n      LATE_FRAME=$(( ${DURATION:-150} * 3 / 4 ))\n\n      echo \"Rendering preview stills at frames 1, $MID_FRAME, $LATE_FRAME...\"\n      npx remotion still src/index.ts \"$COMP_ID\" out/preview-early.png --frame=1 2>&1 | tail -2\n      npx remotion still src/index.ts \"$COMP_ID\" out/preview-mid.png --frame=$MID_FRAME 2>&1 | tail -2\n      npx remotion still src/index.ts \"$COMP_ID\" out/preview-late.png --frame=$LATE_FRAME 2>&1 | tail -2\n      RESULT=$?\n\n      if [ $RESULT -eq 0 ]; then\n        echo \"\"\n        echo \"RENDER_SUCCESS\"\n        ls -la out/preview-*.png\n      else\n        echo \"RENDER_FAILED\"\n      fi\n    depends_on: [generate]\n    timeout: 120000\n\n  # ── Layer 3: Render full video ────────────────────────────────────────\n  - id: render-video\n    bash: |\n      COMP_ID=$(npx remotion compositions src/index.ts 2>&1 | grep -E '^\\S' | head -1 | awk '{print $1}')\n      echo \"Rendering full video: $COMP_ID\"\n      npx remotion render src/index.ts \"$COMP_ID\" out/video.mp4 --codec=h264 --crf=18 2>&1 | tail -10\n      RESULT=$?\n\n      if [ $RESULT -eq 0 ]; then\n        echo \"\"\n        echo \"VIDEO_RENDER_SUCCESS\"\n        ls -la out/video.mp4\n      else\n        echo \"VIDEO_RENDER_FAILED\"\n      fi\n    depends_on: [render-preview]\n    timeout: 300000\n\n  # ── Layer 4: Summary ──────────────────────────────────────────────────\n  - id: summary\n    prompt: |\n      A Remotion video was generated and rendered.\n\n      Original request: $ARGUMENTS\n\n      Preview render: $render-preview.output\n      Video render: $render-video.output\n\n      Read the generated composition code and the preview stills (out/preview-early.png,\n      out/preview-mid.png, out/preview-late.png) to verify the output.\n\n      Summarize:\n      1. What the video contains (based on code and stills)\n      2. Whether the renders succeeded\n      3. Where the output file is (out/video.mp4)\n    depends_on: [render-video]\n    allowed_tools:\n      - Read\n    model: haiku\n",
  "archon-resolve-conflicts": "name: archon-resolve-conflicts\ndescription: |\n  Use when: PR has merge conflicts that need resolution.\n  Triggers: \"resolve conflicts\", \"fix merge conflicts\", \"rebase this PR\", \"resolve this\",\n            \"fix conflicts\", \"merge conflicts\", \"rebase and fix\".\n  Does: Fetches latest base branch -> analyzes conflicts -> auto-resolves simple conflicts ->\n        presents options for complex conflicts -> commits and pushes resolution.\n  NOT for: PRs without conflicts, general rebasing without conflicts, squashing commits.\n\n  This workflow helps resolve merge conflicts by analyzing the conflicting changes,\n  automatically resolving where intent is clear, and presenting options for complex conflicts.\n\nnodes:\n  - id: resolve\n    command: archon-resolve-merge-conflicts\n",
  "archon-smart-pr-review": "name: archon-smart-pr-review\ndescription: |\n  Use when: User wants a smart, efficient PR review that adapts to PR complexity.\n  Triggers: \"smart review\", \"review this PR\", \"review PR #123\", \"efficient review\",\n            \"smart PR review\", \"quick review\".\n  Does: Gathers PR scope -> classifies complexity -> routes to only relevant review agents ->\n        synthesizes findings -> auto-fixes CRITICAL/HIGH issues.\n  NOT for: When you explicitly want ALL review agents (use archon-comprehensive-pr-review instead).\n\n  Unlike the comprehensive review, this workflow classifies the PR first and only runs\n  the review agents that are relevant. A 3-line typo fix skips test-coverage and docs-impact.\n\nnodes:\n  - id: scope\n    command: archon-pr-review-scope\n\n  - id: sync\n    command: archon-sync-pr-with-main\n    depends_on: [scope]\n\n  - id: classify\n    prompt: |\n      You are a PR complexity classifier. Analyze the PR scope below and determine\n      which review agents should run.\n\n      ## PR Scope\n      $scope.output\n\n      ## Rules\n      - **Code review**: Always run unless the diff is empty or only touches non-code files\n        (e.g. README-only, config-only, or .yaml-only changes).\n      - **Error handling**: Run if the diff touches code with try/catch, error handling,\n        async/await, or adds new failure paths.\n      - **Test coverage**: Run if the diff touches source code (not just tests, docs, or config).\n      - **Comment quality**: Run if the diff adds or modifies comments, docstrings, JSDoc,\n        or significant documentation within code files.\n      - **Docs impact**: Run if the diff adds/removes/renames public APIs, commands, CLI flags,\n        environment variables, or user-facing features.\n\n      Classify the PR complexity:\n      - **trivial**: Typo fixes, formatting, single-line changes, version bumps\n      - **small**: 1-3 files, straightforward logic, no architectural changes\n      - **medium**: 4-10 files, moderate logic changes, some cross-cutting concerns\n      - **large**: 10+ files, architectural changes, new subsystems, complex refactors\n\n      Provide your reasoning for each decision.\n    depends_on: [scope]\n    model: haiku\n    allowed_tools: []\n    output_format:\n      type: object\n      properties:\n        run_code_review:\n          type: string\n          enum: [\"true\", \"false\"]\n        run_error_handling:\n          type: string\n          enum: [\"true\", \"false\"]\n        run_test_coverage:\n          type: string\n          enum: [\"true\", \"false\"]\n        run_comment_quality:\n          type: string\n          enum: [\"true\", \"false\"]\n        run_docs_impact:\n          type: string\n          enum: [\"true\", \"false\"]\n        complexity:\n          type: string\n          enum: [\"trivial\", \"small\", \"medium\", \"large\"]\n        reasoning:\n          type: string\n      required:\n        - run_code_review\n        - run_error_handling\n        - run_test_coverage\n        - run_comment_quality\n        - run_docs_impact\n        - complexity\n        - reasoning\n\n  - id: code-review\n    command: archon-code-review-agent\n    depends_on: [classify, sync]\n    when: \"$classify.output.run_code_review == 'true'\"\n\n  - id: error-handling\n    command: archon-error-handling-agent\n    depends_on: [classify, sync]\n    when: \"$classify.output.run_error_handling == 'true'\"\n\n  - id: test-coverage\n    command: archon-test-coverage-agent\n    depends_on: [classify, sync]\n    when: \"$classify.output.run_test_coverage == 'true'\"\n\n  - id: comment-quality\n    command: archon-comment-quality-agent\n    depends_on: [classify, sync]\n    when: \"$classify.output.run_comment_quality == 'true'\"\n\n  - id: docs-impact\n    command: archon-docs-impact-agent\n    depends_on: [classify, sync]\n    when: \"$classify.output.run_docs_impact == 'true'\"\n\n  - id: synthesize\n    command: archon-synthesize-review\n    depends_on: [code-review, error-handling, test-coverage, comment-quality, docs-impact]\n    trigger_rule: one_success\n\n  - id: implement-fixes\n    command: archon-implement-review-fixes\n    depends_on: [synthesize]\n\n  # Optional: push notification when review completes.\n  # To enable, create .archon/mcp/ntfy.json — see docs/mcp-servers.md\n  - id: check-ntfy\n    bash: \"test -f .archon/mcp/ntfy.json && echo 'true' || echo 'false'\"\n    depends_on: [implement-fixes]\n\n  - id: notify\n    depends_on: [check-ntfy, synthesize, implement-fixes]\n    when: \"$check-ntfy.output == 'true'\"\n    trigger_rule: all_success\n    mcp: .archon/mcp/ntfy.json\n    allowed_tools: []\n    prompt: |\n      Send a push notification summarizing the PR review results.\n\n      Review synthesis:\n      $synthesize.output\n\n      Fix results:\n      $implement-fixes.output\n\n      Send with:\n      - title: \"PR Review Complete\"\n      - message: 1-2 sentence summary — verdict and issue count. Short enough for a lock screen.\n      - priority: 3 if ready to merge, 4 if needs fixes, 5 if critical issues remain\n",
  "archon-test-loop-dag": "name: archon-test-loop-dag\ndescription: |\n  Use when: User explicitly says \"test-loop-dag\" or \"run test-loop-dag\".\n  IMPORTANT: This is a DAG workflow with a loop node that iterates until completion.\n  NOT for: General testing questions or debugging.\n  Does: Initializes a counter, iterates until it reaches 3, then reports completion.\n\nnodes:\n  - id: setup\n    bash: |\n      echo \"0\" > .archon/test-loop-dag-counter.txt\n      echo \"Counter initialized to 0\"\n\n  - id: loop-counter\n    depends_on: [setup]\n    loop:\n      prompt: |\n        You are testing the loop node functionality within a DAG workflow.\n\n        ## Your Task\n\n        1. Read the file `.archon/test-loop-dag-counter.txt`\n        2. Parse the current counter value\n        3. Increment it by 1\n        4. Write the new value back to the file\n        5. Report the current iteration\n\n        ## User Intent\n\n        $USER_MESSAGE\n\n        ## Completion Criteria\n\n        - If the counter reaches 3 or higher, output: <promise>COMPLETE</promise>\n        - Otherwise, just report your progress and end normally\n\n        ## Important\n\n        Be concise. Just do the task and report the counter value.\n      until: COMPLETE\n      max_iterations: 5\n      fresh_context: false\n\n  - id: report\n    depends_on: [loop-counter]\n    prompt: |\n      The loop counter test has completed. The loop node output was:\n\n      $loop-counter.output\n\n      Read `.archon/test-loop-dag-counter.txt` and confirm the final counter value.\n      Report: \"Test loop DAG completed successfully. Final counter: {value}\"\n",
  "archon-validate-pr": "name: archon-validate-pr\ndescription: |\n  Use when: User wants a thorough PR validation that tests both main (bug present) and feature branch (bug fixed).\n  Triggers: \"validate PR\", \"validate pr #123\", \"test this PR\", \"verify PR\", \"full PR validation\",\n            \"validate pull request\", \"test PR end-to-end\".\n  Does: Fetches PR info -> finds free ports -> parallel code review (main vs feature) ->\n        E2E test on main (reproduce bug) -> E2E test on feature (verify fix) -> final verdict report.\n  NOT for: Quick code-only reviews (use archon-smart-pr-review), fixing issues, general exploration.\n\n  This workflow is designed for running in parallel — each instance finds its own free ports\n  to avoid conflicts. Produces artifacts in $ARTIFACTS_DIR/ and posts a validation report.\n\nprovider: claude\nmodel: opus\n\nnodes:\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 1: SETUP — Fetch PR info and allocate ports\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: fetch-pr\n    bash: |\n      # Extract PR number from arguments\n      PR_NUMBER=$(echo \"$ARGUMENTS\" | grep -oE '/pull/[0-9]+' | grep -oE '[0-9]+' | head -1)\n      # Fallback: extract first number if no URL path found (e.g., \"validate PR 42\")\n      if [ -z \"$PR_NUMBER\" ]; then\n        PR_NUMBER=$(echo \"$ARGUMENTS\" | grep -oE '[0-9]+' | head -1)\n      fi\n      if [ -z \"$PR_NUMBER\" ]; then\n        # Try getting PR from current branch\n        PR_NUMBER=$(gh pr view --json number -q '.number' 2>/dev/null)\n      fi\n\n      if [ -z \"$PR_NUMBER\" ]; then\n        echo \"ERROR: No PR number found in arguments: $ARGUMENTS\"\n        exit 1\n      fi\n\n      echo \"$PR_NUMBER\" > \"$ARTIFACTS_DIR/.pr-number\"\n\n      # Fetch full PR details\n      gh pr view \"$PR_NUMBER\" --json number,title,body,url,headRefName,baseRefName,files,additions,deletions,changedFiles,state,author,labels,isDraft\n\n  - id: find-ports\n    bash: |\n      # Use Bun to let the OS pick truly free ports (cross-platform: Linux, macOS, Windows)\n      BACKEND_PORT=$(bun -e \"const s = Bun.serve({port: 0, fetch: () => new Response('')}); console.log(s.port); s.stop()\")\n      FRONTEND_PORT=$(bun -e \"const s = Bun.serve({port: 0, fetch: () => new Response('')}); console.log(s.port); s.stop()\")\n\n      echo \"$BACKEND_PORT\" > \"$ARTIFACTS_DIR/.backend-port\"\n      echo \"$FRONTEND_PORT\" > \"$ARTIFACTS_DIR/.frontend-port\"\n\n      echo \"BACKEND_PORT=$BACKEND_PORT\"\n      echo \"FRONTEND_PORT=$FRONTEND_PORT\"\n\n  - id: resolve-paths\n    bash: |\n      # Resolve canonical repo path (main branch) vs worktree path (feature branch)\n      CANONICAL_REPO=$(git rev-parse --path-format=absolute --git-common-dir 2>/dev/null | sed 's|/\\.git$||')\n      WORKTREE_PATH=$(pwd)\n      FEATURE_BRANCH=$(git branch --show-current)\n\n      # Get PR branch info\n      PR_NUMBER=$(cat \"$ARTIFACTS_DIR/.pr-number\")\n      PR_HEAD=$(gh pr view \"$PR_NUMBER\" --json headRefName -q '.headRefName')\n      PR_BASE=$(gh pr view \"$PR_NUMBER\" --json baseRefName -q '.baseRefName')\n\n      echo \"$CANONICAL_REPO\" > \"$ARTIFACTS_DIR/.canonical-repo\"\n      echo \"$WORKTREE_PATH\" > \"$ARTIFACTS_DIR/.worktree-path\"\n      echo \"$FEATURE_BRANCH\" > \"$ARTIFACTS_DIR/.feature-branch\"\n      echo \"$PR_HEAD\" > \"$ARTIFACTS_DIR/.pr-head\"\n      echo \"$PR_BASE\" > \"$ARTIFACTS_DIR/.pr-base\"\n\n      echo \"CANONICAL_REPO=$CANONICAL_REPO\"\n      echo \"WORKTREE_PATH=$WORKTREE_PATH\"\n      echo \"FEATURE_BRANCH=$FEATURE_BRANCH\"\n      echo \"PR_HEAD=$PR_HEAD\"\n      echo \"PR_BASE=$PR_BASE\"\n    depends_on: [fetch-pr]\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 2: CODE REVIEW — Parallel analysis of main vs feature\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: code-review-main\n    command: archon-validate-pr-code-review-main\n    depends_on: [fetch-pr, resolve-paths]\n    context: fresh\n\n  - id: code-review-feature\n    command: archon-validate-pr-code-review-feature\n    depends_on: [fetch-pr, resolve-paths, code-review-main]\n    context: fresh\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 3: E2E TESTING — Sequential (after code reviews finish)\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: classify-testability\n    prompt: |\n      You are a PR testability classifier. Determine whether this PR's changes can be\n      validated via browser E2E testing, or if it requires code-review-only validation.\n\n      ## PR Details\n\n      $fetch-pr.output\n\n      ## Rules\n\n      - **e2e_testable**: Changes affect the Web UI (components, hooks, styles, API routes\n        that serve the frontend, SSE streaming, layout, user-visible behavior). These can be\n        validated by starting Archon and using agent-browser to interact with the UI.\n      - **code_review_only**: Changes are purely backend logic, CLI-only, workflow engine,\n        database schemas, git operations, build tooling, tests, documentation, or other\n        non-UI code. No visual validation possible.\n\n      Consider: even if a change is backend, if it affects what the frontend displays\n      (e.g., API response format changes, SSE event changes), it IS e2e_testable.\n    depends_on: [fetch-pr]\n    model: haiku\n    allowed_tools: []\n    output_format:\n      type: object\n      properties:\n        testable:\n          type: string\n          enum: [\"e2e_testable\", \"code_review_only\"]\n        reasoning:\n          type: string\n        test_plan:\n          type: string\n      required: [testable, reasoning, test_plan]\n\n  - id: e2e-test-main\n    command: archon-validate-pr-e2e-main\n    depends_on: [classify-testability, find-ports, resolve-paths, code-review-main, code-review-feature]\n    when: \"$classify-testability.output.testable == 'e2e_testable'\"\n    context: fresh\n    idle_timeout: 1800000\n\n  - id: e2e-test-feature\n    command: archon-validate-pr-e2e-feature\n    depends_on: [e2e-test-main, find-ports, resolve-paths]\n    when: \"$classify-testability.output.testable == 'e2e_testable'\"\n    context: fresh\n    idle_timeout: 1800000\n\n  # ═══════════════════════════════════════════════════════════════\n  # PHASE 4: FINAL REPORT — Synthesize all findings\n  # ═══════════════════════════════════════════════════════════════\n\n  - id: cleanup-processes\n    bash: |\n      # Safety net: kill any orphaned processes from E2E testing\n      # This runs after E2E nodes complete (or timeout/fail) to prevent process accumulation\n      BACKEND_PORT=$(cat \"$ARTIFACTS_DIR/.backend-port\" 2>/dev/null | tr -d '\\n')\n      FRONTEND_PORT=$(cat \"$ARTIFACTS_DIR/.frontend-port\" 2>/dev/null | tr -d '\\n')\n\n      if [ -z \"$BACKEND_PORT\" ] || [ -z \"$FRONTEND_PORT\" ]; then\n        echo \"No port files found — skipping cleanup\"\n        exit 0\n      fi\n\n      echo \"Cleaning up ports $BACKEND_PORT and $FRONTEND_PORT...\"\n\n      # Kill by all recorded PID files\n      for pidfile in \"$ARTIFACTS_DIR\"/.e2e-*-pid; do\n        if [ -f \"$pidfile\" ]; then\n          PID=$(cat \"$pidfile\" | tr -d '\\n')\n          echo \"Killing PID $PID from $pidfile\"\n          kill \"$PID\" 2>/dev/null || taskkill //F //T //PID \"$PID\" 2>/dev/null || true\n        fi\n      done\n\n      # Kill by port (cross-platform fallback)\n      for PORT in $BACKEND_PORT $FRONTEND_PORT; do\n        fuser -k \"$PORT/tcp\" 2>/dev/null || true\n        lsof -ti:\"$PORT\" 2>/dev/null | xargs kill -9 2>/dev/null || true\n        netstat -ano 2>/dev/null | grep \":$PORT \" | grep LISTENING | awk '{print $5}' | sort -u | while read pid; do\n          taskkill //F //T //PID \"$pid\" 2>/dev/null || true\n        done\n      done\n\n      # pkill fallback: catch processes that escaped PID/port cleanup\n      pkill -f \"PORT=$BACKEND_PORT.*bun\" 2>/dev/null || true\n      pkill -f \"vite.*port.*$FRONTEND_PORT\" 2>/dev/null || true\n\n      # Close this workflow's browser session only (scoped by session ID)\n      BROWSER_SESSION=$(cat \"$ARTIFACTS_DIR/.browser-session\" 2>/dev/null | tr -d '\\n')\n      if [ -n \"$BROWSER_SESSION\" ]; then\n        agent-browser --session \"$BROWSER_SESSION\" close 2>/dev/null || true\n      fi\n\n      # Remove main E2E worktree if it still exists (safety net)\n      CANONICAL_REPO=$(cat \"$ARTIFACTS_DIR/.canonical-repo\" 2>/dev/null | tr -d '\\n')\n      MAIN_E2E_PATH=$(cat \"$ARTIFACTS_DIR/.e2e-main-worktree\" 2>/dev/null | tr -d '\\n')\n      if [ -n \"$MAIN_E2E_PATH\" ] && [ -n \"$CANONICAL_REPO\" ] && [ -d \"$MAIN_E2E_PATH\" ]; then\n        echo \"Removing leftover main E2E worktree: $MAIN_E2E_PATH\"\n        git -C \"$CANONICAL_REPO\" worktree remove \"$MAIN_E2E_PATH\" --force 2>/dev/null || rm -rf \"$MAIN_E2E_PATH\"\n      fi\n\n      sleep 1\n      echo \"Process cleanup complete\"\n    depends_on: [e2e-test-main, e2e-test-feature]\n    trigger_rule: all_done\n\n  - id: final-report\n    command: archon-validate-pr-report\n    depends_on: [code-review-main, code-review-feature, e2e-test-main, e2e-test-feature, classify-testability, cleanup-processes]\n    trigger_rule: all_done\n    context: fresh\n",
  "archon-workflow-builder": "name: archon-workflow-builder\ndescription: |\n  Use when: User wants to create a new custom workflow for their project.\n  Triggers: \"build me a workflow\", \"create a workflow\", \"generate a workflow\",\n            \"new workflow\", \"make a workflow for\", \"workflow builder\".\n  Does: Scans codebase -> extracts intent (JSON) -> generates YAML -> validates -> saves.\n  NOT for: Editing existing workflows or creating non-workflow files.\n\nnodes:\n  - id: scan-codebase\n    bash: |\n      echo \"=== Existing Commands ===\"\n      if [ -d \".archon/commands\" ]; then\n        find .archon/commands -type f -name \"*.md\" 2>/dev/null | head -30\n      else\n        echo \"(no .archon/commands/ directory)\"\n      fi\n\n      echo \"\"\n      echo \"=== Existing Workflows ===\"\n      if [ -d \".archon/workflows\" ]; then\n        find .archon/workflows -type f \\( -name \"*.yaml\" -o -name \"*.yml\" \\) 2>/dev/null | head -30\n      else\n        echo \"(no .archon/workflows/ directory)\"\n      fi\n\n      echo \"\"\n      echo \"=== Package Info ===\"\n      if [ -f \"package.json\" ]; then\n        grep -E '\"name\"|\"scripts\"' package.json | head -10\n      else\n        echo \"(no package.json)\"\n      fi\n\n      echo \"\"\n      echo \"=== Project Context (CLAUDE.md first 50 lines) ===\"\n      if [ -f \"CLAUDE.md\" ]; then\n        head -50 CLAUDE.md\n      else\n        echo \"(no CLAUDE.md)\"\n      fi\n\n  - id: extract-intent\n    prompt: |\n      You are a workflow design classifier. Given a user's description of what they want\n      a workflow to do, extract structured intent.\n\n      ## User's Request\n      $ARGUMENTS\n\n      ## Codebase Context\n      $scan-codebase.output\n\n      ## Instructions\n\n      Analyze the user's request and the existing codebase to determine:\n      1. A kebab-case workflow name (e.g., \"lint-and-test\", \"deploy-staging\")\n      2. A description following the Archon pattern (Use when / Triggers / Does / NOT for)\n      3. Trigger phrases the router should match\n      4. A list of proposed nodes with their types and purposes\n      5. Whether this should be a simple DAG or include a loop node\n\n      Be specific and concrete. Each proposed node should have a clear type\n      (bash, prompt, command, script, loop, or approval) and a one-line\n      description of what it does.\n    model: haiku\n    allowed_tools: []\n    output_format:\n      type: object\n      properties:\n        workflow_name:\n          type: string\n        description:\n          type: string\n        trigger_phrases:\n          type: string\n        proposed_nodes:\n          type: string\n        execution_mode:\n          type: string\n          enum: [\"dag\", \"loop\"]\n      required: [workflow_name, description, trigger_phrases, proposed_nodes, execution_mode]\n    depends_on: [scan-codebase]\n\n  - id: generate-yaml\n    prompt: |\n      You are an Archon workflow author. Generate a complete, valid workflow YAML file\n      based on the structured intent provided.\n\n      ## Intent\n      - **Name**: $extract-intent.output.workflow_name\n      - **Description**: $extract-intent.output.description\n      - **Trigger Phrases**: $extract-intent.output.trigger_phrases\n      - **Proposed Nodes**: $extract-intent.output.proposed_nodes\n      - **Execution Mode**: $extract-intent.output.execution_mode\n\n      ## Original User Request\n      $ARGUMENTS\n\n      ## Archon Workflow YAML Schema Reference\n\n      A workflow YAML file has this structure:\n\n      ```yaml\n      name: workflow-name\n      description: |\n        Use when: ...\n        Triggers: ...\n        Does: ...\n        NOT for: ...\n\n      # Optional top-level settings:\n      # provider: claude    (or codex)\n      # model: sonnet       (or haiku, opus, etc.)\n      # interactive: true   (forces foreground execution in web UI)\n\n      nodes:\n        - id: node-id-kebab-case\n          # Choose ONE of: prompt, bash, command, script, loop, approval\n\n          # --- prompt node (AI-executed) ---\n          prompt: |\n            Instructions for the AI...\n          # Optional: model, allowed_tools, denied_tools, output_format, context, idle_timeout\n\n          # --- bash node (shell script, no AI, stdout = $<nodeId>.output) ---\n          bash: |\n            #!/bin/bash\n            set -e\n            echo \"result\"\n\n          # --- command node (references a .archon/commands/ file) ---\n          command: command-name\n\n          # --- script node (TypeScript via bun, or Python via uv — no AI, stdout = $<nodeId>.output) ---\n          # Use for deterministic data transforms the shell would mangle (JSON parsing, etc.)\n          script: |\n            // JSON is valid JS expression syntax — assign directly (String.raw breaks on backticks)\n            const data = $other-node.output;\n            console.log(JSON.stringify({ count: data.items.length }));\n          runtime: bun                       # required: 'bun' (.ts/.js) or 'uv' (.py)\n          # deps: [requests]                 # uv only\n          # Or reference a named script in .archon/scripts/:\n          # script: extract-labels           # no extension; bun resolves .ts/.js, uv resolves .py\n\n          # --- loop node (iterative AI execution) ---\n          loop:\n            prompt: |\n              Instructions repeated each iteration...\n            until: COMPLETION_SIGNAL\n            max_iterations: 10\n            fresh_context: true  # optional: reset context each iteration\n\n          # --- approval node (human gate — pauses workflow) ---\n          approval:\n            message: \"Review the plan above. Approve to continue.\"\n            # capture_response: true         # store reviewer comment as $<nodeId>.output\n\n          # Common options for all node types:\n          depends_on: [other-node-id]       # DAG edges\n          when: \"$<other-node>.output == 'value'\"  # conditional execution\n          trigger_rule: all_success          # all_success | one_success | all_done\n          timeout: 120000                    # ms, for bash and script nodes\n      ```\n\n      ## Variable Reference\n      - `$ARGUMENTS` — user's input text\n      - `$ARTIFACTS_DIR` — pre-created directory for workflow artifacts\n      - `$<nodeId>.output` — stdout from a bash/script node or AI response from a prompt node\n      - `$<nodeId>.output.field` — JSON field from a node with output_format\n      - `$BASE_BRANCH` — base git branch\n\n      ## Rules\n      1. The `name:` field MUST match: $extract-intent.output.workflow_name\n      2. The `description:` MUST follow the \"Use when / Triggers / Does / NOT for\" pattern\n      3. Every node MUST have a unique kebab-case `id`\n      4. Use `depends_on` to define execution order\n      5. Use `bash` nodes for deterministic shell operations (file checks, git commands, installs)\n      6. Use `script` nodes for typed data transforms (TypeScript JSON parsing, Python with deps)\n         — stdout is captured as output, stderr is forwarded as a warning.\n         `$nodeId.output` is NOT shell-quoted in script bodies.\n         - **TypeScript/bun**: assign directly — `const data = $nodeId.output;`\n           (JSON is valid JS expression syntax; avoid String.raw — it breaks on backticks)\n         - **Python/uv**: use json.loads — `import json; data = json.loads(\"\"\"$nodeId.output\"\"\")`\n         Never interpolate into shell syntax.\n      7. Use `prompt` nodes for AI reasoning tasks\n      8. Use `approval` nodes to pause for human review at risky gates (plan→execute boundary, destructive actions)\n      9. Use `output_format` on prompt nodes when downstream nodes need structured data\n      10. Use `allowed_tools: []` on classification/analysis nodes that don't need tools\n      11. Use `denied_tools: [Edit, Bash]` when a node should only use Write (not edit existing files)\n      12. Prefer `model: haiku` for simple classification tasks to save cost\n\n      ## Output\n\n      Write the complete workflow YAML to: `$ARTIFACTS_DIR/generated-workflow.yaml`\n\n      Use the Write tool. Do NOT use Edit or Bash. The file must be valid YAML and follow\n      all the patterns above.\n    denied_tools: [Edit, Bash]\n    depends_on: [extract-intent]\n\n  - id: validate-yaml\n    bash: |\n      FILE=\"$ARTIFACTS_DIR/generated-workflow.yaml\"\n\n      if [ ! -f \"$FILE\" ]; then\n        echo \"ERROR: generated-workflow.yaml not found at $FILE\"\n        exit 1\n      fi\n\n      if [ ! -s \"$FILE\" ]; then\n        echo \"ERROR: generated-workflow.yaml is empty\"\n        exit 1\n      fi\n\n      if ! grep -q \"^name:\" \"$FILE\"; then\n        echo \"ERROR: missing 'name:' field\"\n        exit 1\n      fi\n\n      if ! grep -q \"^nodes:\" \"$FILE\"; then\n        echo \"ERROR: missing 'nodes:' field\"\n        exit 1\n      fi\n\n      echo \"VALID\"\n    depends_on: [generate-yaml]\n\n  - id: save-or-report\n    prompt: |\n      You are a workflow installer. Save the generated workflow and report to the user.\n\n      ## Workflow Details\n      - **Name**: $extract-intent.output.workflow_name\n      - **Trigger Phrases**: $extract-intent.output.trigger_phrases\n\n      ## Instructions\n\n      1. Read the generated workflow from `$ARTIFACTS_DIR/generated-workflow.yaml`\n      2. Create the directory `.archon/workflows/` if it doesn't exist (use Bash: `mkdir -p .archon/workflows/`)\n      3. Save the workflow to `.archon/workflows/$extract-intent.output.workflow_name.yaml`\n         Use the Write tool to write the file.\n      4. Report to the user:\n         - Workflow name and file location\n         - Trigger phrases that will invoke it\n         - How to run it: `bun run cli workflow run $extract-intent.output.workflow_name \"your input\"`\n         - How to test it: `bun run cli validate workflows $extract-intent.output.workflow_name`\n    depends_on: [validate-yaml]\n",
  "bdc-auth-ce-auth0-tenant-metadata-harden": "# bdc-auth-ce-auth0-tenant-metadata-harden.yaml\n# WO: WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01\n# Spec source: bdc-xo:docs/work-orders/WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01.md\n# Doctrine anchor: feedback_browser-uses-auth0-only-gas-token-is-legacy.md (2026-05-15)\n#\n# Pre-flight (OPERATOR MUST VERIFY BEFORE FIRING):\n#   1. bluedevilcollectibles/lspro-react MUST be registered in Archon codebases.\n#      Check via:\n#        sqlite3 /opt/bdc/archon-data/archon.db \\\n#          \"SELECT name FROM remote_agent_codebases\" | grep lspro-react\n#      If missing, add via API POST per feedback_archon-codebase-add-via-api-not-ui.md\n#   2. WO spec committed + pushed to bdc-xo main:\n#      docs/work-orders/WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01.md\n#   3. GH_TOKEN/GITHUB_TOKEN must have push access to bluedevilcollectibles/lspro-react\n#   4. parallel_ok: NO — touches AuthContext; serialize with any other lspro-react WO\n#   5. Notion page ID — FILL IN before firing flip-notion (search Notion DB\n#      a6df831c-0b52-449f-8ca4-d77be6b70d0a for WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01)\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh \\\n#           bdc-auth-ce-auth0-tenant-metadata-harden.yaml\n#\n# Fire after deploy:\n#   curl -X POST https://archon.bluedevilcollectibles.com/api/workflows/bdc-auth-ce-auth0-tenant-metadata-harden/runs \\\n#     -H \"Content-Type: application/json\" \\\n#     -d '{\"inputs\":{\"WO_ID\":\"WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01\"}}'\n\nname: bdc-auth-ce-auth0-tenant-metadata-harden\ndescription: |\n  Use when: Executing WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01 — the P0 live fix\n  that collapses lspro-react browser auth tenant resolution to the Auth0 metadata path.\n  Input: WO_ID = WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01.\n  Does: Reads WO spec from bdc-xo via gh api. Clones lspro-react into /tmp.\n        Adds resolveTenantId(user) helper + synchronous sessionStorage write at Auth0\n        login in src/context/AuthContext.jsx. Adds unit tests. Runs bun run build +\n        bun test. Opens PR against main with manifest v2 body. Does NOT touch GAS path\n        or /api/tenant/plan fetch (those are WO #2 cleanup).\n  Repo: bluedevilcollectibles/lspro-react (default branch: main)\n  Class: CODE (single-file frontend hardening)\n  parallel_ok: false — serialize with any other lspro-react WO\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  # ---------------------------------------------------------------------------\n  # 1. READ-SPEC — fetch WO spec from bdc-xo (cross-repo via gh api)\n  # ---------------------------------------------------------------------------\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01}\"\n\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --jq '[.[] | select(.name | test(\"'\"${WO_ID}\"'\")) | .path] | .[0]' \\\n          2>/dev/null || true)\n        if [ -n \"$GLOB_PATH\" ] && [ \"$GLOB_PATH\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:docs/work-orders/%s.md\\nWORKFLOW_ID=%s\\n\\n' \\\n        \"$WO_ID\" \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  # ---------------------------------------------------------------------------\n  # 2. SETUP-CLONE — clone lspro-react into /tmp work dir (idempotent)\n  # ---------------------------------------------------------------------------\n  - id: setup-clone\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      WORK_DIR=\"/tmp/lspro-react-work-$WORKFLOW_ID\"\n\n      if [ -d \"$WORK_DIR/.git\" ]; then\n        cd \"$WORK_DIR\"\n        git fetch origin\n        git checkout main\n        git reset --hard origin/main\n        echo \"WORK_DIR=${WORK_DIR}\"\n        echo \"STATUS=reused_existing_clone\"\n        exit 0\n      fi\n\n      git clone \"https://github.com/bluedevilcollectibles/lspro-react.git\" \\\n        \"$WORK_DIR\" --depth=50\n      echo \"WORK_DIR=${WORK_DIR}\"\n      echo \"STATUS=fresh_clone\"\n\n  # ---------------------------------------------------------------------------\n  # 3. INSPECT-AUTHCONTEXT — read AuthContext.jsx so the planner sees current shape\n  # ---------------------------------------------------------------------------\n  - id: inspect-authcontext\n    depends_on: [setup-clone]\n    bash: |\n      set -euo pipefail\n      WORK_DIR=\"/tmp/lspro-react-work-$WORKFLOW_ID\"\n      AUTH_FILE=\"$WORK_DIR/src/context/AuthContext.jsx\"\n\n      if [ ! -f \"$AUTH_FILE\" ]; then\n        echo \"ERROR: AuthContext.jsx not found at $AUTH_FILE\" >&2\n        exit 1\n      fi\n\n      echo \"=== AuthContext.jsx (line-numbered) ===\"\n      nl -ba \"$AUTH_FILE\"\n      echo \"\"\n      echo \"=== resolveUserTier / resolveUserRole pattern (existing) ===\"\n      grep -n \"resolveUser\\|app_metadata\\|livesellerpro.app\" \"$AUTH_FILE\" || true\n      echo \"\"\n      echo \"=== sessionStorage writes (existing) ===\"\n      grep -n \"sessionStorage\\.\" \"$AUTH_FILE\" || true\n\n  # ---------------------------------------------------------------------------\n  # 4. PLAN — concise implementation plan for AuthContext patch\n  # ---------------------------------------------------------------------------\n  - id: plan\n    depends_on: [read-spec, inspect-authcontext]\n    task_class: medium_work\n    prompt: |\n      Plan the single-file AuthContext hardening for WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01.\n\n      ## Required Output (Markdown, exact headings)\n\n      ### Files to modify\n      - src/context/AuthContext.jsx (resolveTenantId helper + sessionStorage write)\n\n      ### Files to create\n      - One test file under src/context/ following existing test convention (likely\n        src/context/AuthContext.test.tsx OR src/context/__tests__/AuthContext.test.tsx —\n        check what already exists in the clone)\n\n      ### Files explicitly out of scope\n      - src/lib/api.ts (WO #2 cleanup)\n      - /api/tenant/plan fetch removal (WO #2)\n      - GAS / lspro_token path (WO #2)\n\n      ### Verification commands\n      - cd /tmp/lspro-react-work-$WORKFLOW_ID && bun install --frozen-lockfile\n      - bun run build\n      - bun test\n      - bunx tsc --noEmit (if tsconfig present)\n      - grep -c 'resolveTenantId' src/context/AuthContext.jsx (expect >= 1)\n      - grep -c \"sessionStorage.setItem.*lspro_tenant_id\" src/context/AuthContext.jsx (expect >= 1)\n\n      ### Commit message\n      fix(auth): resolve tenant_id from Auth0 metadata at login [WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01]\n\n      ### Push policy\n      Branch: fix/auth-ce-tenant-from-auth0-metadata\n      Base: main\n      Staging gate: required (lspro-react auto-deploy pipeline gates main on staging)\n\n      ### Auth0 JWT diagnosis approach\n      Phase 1 of WO requires verifying tenant_id IS in the Auth0 user object.\n      Since the workflow cannot log into Auth0 from inside the container, the planner\n      must trust the spec's verified-evidence section: resolveUserTier already reads\n      from auth0.user['https://livesellerpro.app/app_metadata'], same shape.\n      Implement resolveTenantId mirroring resolveUserTier exactly.\n      If tenant_id is genuinely missing from the JWT, the operator will catch it\n      during post-deploy DevTools verification — that's the stop-rule path.\n\n      ## Inspect-AuthContext output (current file)\n      $inspect-authcontext.output\n\n      ## Spec\n      $read-spec.output\n\n  # ---------------------------------------------------------------------------\n  # 5. IMPLEMENT — patch AuthContext.jsx + add tests\n  # ---------------------------------------------------------------------------\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01 in the cloned lspro-react.\n\n        Work directory: /tmp/lspro-react-work-$WORKFLOW_ID\n        Target file: src/context/AuthContext.jsx\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n\n        1. Read the FULL src/context/AuthContext.jsx file before editing. Understand\n           resolveUserRole (line ~19), resolveUserTier (line ~29), and the AuthProvider\n           effect's Auth0 branch (line ~82 area).\n\n        2. Add resolveTenantId(user) helper next to resolveUserTier, mirroring the same\n           shape:\n\n             function resolveTenantId(user) {\n               if (!user) return '';\n               const meta =\n                 user['https://livesellerpro.app/app_metadata'] ||\n                 user.app_metadata ||\n                 {};\n               return meta.tenant_id || '';\n             }\n\n        3. In the AuthProvider Auth0 branch (where auth0.isAuthenticated + auth0.user\n           are handled), add a synchronous tenant_id write BEFORE the existing\n           /api/tenant/plan fetch:\n\n             const tenantId = resolveTenantId(auth0.user);\n             if (tenantId) {\n               sessionStorage.setItem('lspro_tenant_id', tenantId);\n             }\n\n        4. Do NOT remove or rewrite the /api/tenant/plan fetch. Do NOT touch the\n           GAS / lspro_token path. Do NOT touch src/lib/api.ts. Those are WO #2.\n\n        5. Add a unit test file for resolveTenantId. First detect the existing test\n           convention by inspecting the cloned repo:\n             - Look for existing src/context/AuthContext.test.* files\n             - If not found, check src/context/__tests__/ or src/__tests__/context/\n             - Match the existing convention (vitest, jest, etc.) by looking at\n               other *.test.* files near AuthContext\n\n           Tests required (minimum 4, asserting REAL behavior — not \"did not throw\"):\n             a. resolveTenantId(user) returns tenant_id when user has metadata under\n                'https://livesellerpro.app/app_metadata' namespace\n             b. resolveTenantId(user) returns tenant_id when user has fallback\n                app_metadata.tenant_id\n             c. resolveTenantId(null) returns ''\n             d. resolveTenantId({}) returns '' (user with no metadata)\n\n           To make resolveTenantId testable, export it from AuthContext.jsx (named\n           export). If the file currently only exports the AuthProvider default,\n           add a named export for resolveTenantId.\n\n        6. Run verification (do NOT push, do NOT deploy):\n             cd /tmp/lspro-react-work-$WORKFLOW_ID\n             bun install --frozen-lockfile\n             bun run build\n             bun test\n\n        ## Completion Criteria\n        Output <promise>COMPLETE</promise> only when:\n          - src/context/AuthContext.jsx has resolveTenantId function (exported)\n          - src/context/AuthContext.jsx has sessionStorage.setItem('lspro_tenant_id', ...)\n            in the Auth0 branch BEFORE any /api/tenant/plan fetch\n          - Test file exists with >= 4 tests asserting real behavior\n          - bun run build exits 0\n          - bun test exits 0\n          - GAS / lspro_token path is UNCHANGED (grep proves it)\n          - /api/tenant/plan fetch is UNCHANGED (grep proves it)\n\n  # ---------------------------------------------------------------------------\n  # 6. VERIFY-GREP — structural assertions\n  # ---------------------------------------------------------------------------\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      WORK_DIR=\"/tmp/lspro-react-work-$WORKFLOW_ID\"\n      cd \"$WORK_DIR\"\n\n      AUTH_FILE=\"src/context/AuthContext.jsx\"\n\n      echo \"=== lspro-react grep assertions ===\"\n\n      grep -q \"resolveTenantId\" \"$AUTH_FILE\" \\\n        || (echo \"FAIL: resolveTenantId missing from AuthContext.jsx\" >&2; exit 1)\n\n      grep -q \"lspro_tenant_id\" \"$AUTH_FILE\" \\\n        || (echo \"FAIL: lspro_tenant_id sessionStorage write missing\" >&2; exit 1)\n\n      SETITEM_COUNT=$(grep -c \"sessionStorage.setItem.*lspro_tenant_id\" \"$AUTH_FILE\" || echo 0)\n      echo \"lspro_tenant_id setItem occurrences: $SETITEM_COUNT\"\n      if [ \"$SETITEM_COUNT\" -lt 1 ]; then\n        echo \"FAIL: expected >= 1 sessionStorage.setItem for lspro_tenant_id\" >&2\n        exit 1\n      fi\n\n      grep -q \"livesellerpro.app/app_metadata\" \"$AUTH_FILE\" \\\n        || (echo \"FAIL: Auth0 metadata namespace reference missing\" >&2; exit 1)\n\n      grep -q \"lspro_token\" \"$AUTH_FILE\" \\\n        || (echo \"FAIL: GAS / lspro_token path was removed — this WO must NOT touch it (that is WO #2)\" >&2; exit 1)\n\n      grep -q \"api/tenant/plan\" \"$AUTH_FILE\" \\\n        || (echo \"FAIL: /api/tenant/plan fetch was removed — this WO must keep it as backup (removal is WO #2)\" >&2; exit 1)\n\n      TEST_FOUND=0\n      for TEST_PATH in \\\n        \"src/context/AuthContext.test.tsx\" \\\n        \"src/context/AuthContext.test.jsx\" \\\n        \"src/context/AuthContext.test.ts\" \\\n        \"src/context/AuthContext.test.js\" \\\n        \"src/context/__tests__/AuthContext.test.tsx\" \\\n        \"src/context/__tests__/AuthContext.test.jsx\" \\\n        \"src/__tests__/context/AuthContext.test.tsx\"; do\n        if [ -f \"$TEST_PATH\" ]; then\n          TEST_COUNT=$(grep -cE \"^\\s*(it|test)\\(\" \"$TEST_PATH\" || echo 0)\n          echo \"Test file: $TEST_PATH — $TEST_COUNT tests\"\n          if [ \"$TEST_COUNT\" -ge 4 ]; then\n            TEST_FOUND=1\n            break\n          fi\n        fi\n      done\n      if [ \"$TEST_FOUND\" -eq 0 ]; then\n        echo \"FAIL: AuthContext test file not found with >= 4 tests\" >&2\n        exit 1\n      fi\n\n      echo \"=== all grep assertions passed ===\"\n\n  # ---------------------------------------------------------------------------\n  # 7. VERIFY-BUILD — run npm run build\n  # ---------------------------------------------------------------------------\n  - id: verify-build\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      WORK_DIR=\"/tmp/lspro-react-work-$WORKFLOW_ID\"\n      cd \"$WORK_DIR\"\n\n      if [ ! -d node_modules ]; then\n        bun install --frozen-lockfile\n      fi\n\n      bun run build\n\n  # ---------------------------------------------------------------------------\n  # 8. VERIFY-TESTS — run npm test\n  # ---------------------------------------------------------------------------\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      WORK_DIR=\"/tmp/lspro-react-work-$WORKFLOW_ID\"\n      cd \"$WORK_DIR\"\n\n      if [ ! -d node_modules ]; then\n        bun install --frozen-lockfile\n      fi\n\n      if node -e \"const p=require('./package.json'); process.exit(p.scripts && p.scripts.test ? 0 : 1)\"; then\n        bun test -- --run 2>&1 || bun test 2>&1\n      else\n        echo \"WARN: no bun test script found in package.json\" >&2\n        exit 1\n      fi\n\n  # ---------------------------------------------------------------------------\n  # 9. COMMIT-AND-PUSH — idempotent branch + commit + push\n  # ---------------------------------------------------------------------------\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-build, verify-tests]\n    bash: |\n      set -euo pipefail\n      WORK_DIR=\"/tmp/lspro-react-work-$WORKFLOW_ID\"\n      cd \"$WORK_DIR\"\n\n      BASE_BRANCH=\"fix/auth-ce-tenant-from-auth0-metadata\"\n      BRANCH=\"$BASE_BRANCH\"\n\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n          BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n          git checkout -b \"$BRANCH\"\n        else\n          git checkout \"$BRANCH\"\n        fi\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n\n      git add src/context/AuthContext.jsx\n\n      for TEST_PATH in \\\n        \"src/context/AuthContext.test.tsx\" \\\n        \"src/context/AuthContext.test.jsx\" \\\n        \"src/context/AuthContext.test.ts\" \\\n        \"src/context/AuthContext.test.js\" \\\n        \"src/context/__tests__/AuthContext.test.tsx\" \\\n        \"src/context/__tests__/AuthContext.test.jsx\" \\\n        \"src/__tests__/context/AuthContext.test.tsx\"; do\n        if [ -f \"$TEST_PATH\" ]; then\n          git add \"$TEST_PATH\"\n        fi\n      done\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'fix(auth): resolve tenant_id from Auth0 metadata at login [WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01]\\n\\nAdds resolveTenantId(user) helper next to existing resolveUserTier/resolveUserRole\\nand writes the tenant slug to sessionStorage synchronously at Auth0 login BEFORE\\nthe (CF-Access-gated) /api/tenant/plan fetch fires.\\n\\nRoot cause anchor (2026-05-14): CE users had no lspro_tenant_id in sessionStorage\\nbecause /api/tenant/plan sits behind Cloudflare Access and Auth0 Bearer cannot pass\\nCF Access. Downstream callers (scanLookup.ts, CEStep02Processing.tsx, apiFetch)\\nread empty tenant_id, dropped the URL param, shopops-api returned 400.\\n\\nThis WO ships the FAST FIX per John 2026-05-15 ruling that browser auth collapses\\nto one path: Auth0. tenant + plan come from Auth0 app_metadata. The GAS /\\nlspro_token launcher path stays live in this PR and is retired by WO #2.\\nThe /api/tenant/plan fetch stays as backup correction (DB-wins fallback) and\\nis retired by WO #2.\\n\\nDoctrine: feedback_browser-uses-auth0-only-gas-token-is-legacy.md\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  # ---------------------------------------------------------------------------\n  # 10. OPEN-PR — idempotent PR against main\n  # ---------------------------------------------------------------------------\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      WORK_DIR=\"/tmp/lspro-react-work-$WORKFLOW_ID\"\n      cd \"$WORK_DIR\"\n\n      BRANCH=$(git rev-parse --abbrev-ref HEAD)\n      REPO=\"bluedevilcollectibles/lspro-react\"\n      BASE=\"main\"\n      TITLE=\"fix(auth): resolve tenant_id from Auth0 metadata at login [unblocks CE scan + import]\"\n\n      EXISTING=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --json url --jq '.[0].url' 2>/dev/null || true)\n      if [ -n \"$EXISTING\" ] && [ \"$EXISTING\" != \"null\" ]; then\n        echo \"PR_URL=${EXISTING}\"\n        echo \"PR_STATUS=already_open\"\n        exit 0\n      fi\n\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01** — the P0 live fix\"\n        echo \"for the CE scan 400 + import 0% match symptom. Reads \\`tenant_id\\` from the\"\n        echo \"Auth0 user \\`app_metadata\\` namespace at login and writes it to\"\n        echo \"\\`sessionStorage\\` BEFORE any downstream API call fires.\"\n        echo \"\"\n        echo \"Doctrine: \\`feedback_browser-uses-auth0-only-gas-token-is-legacy.md\\` (John 2026-05-15).\"\n        echo \"\"\n        echo \"## Root cause\"\n        echo \"- \\`/api/tenant/plan\\` sits behind Cloudflare Access\"\n        echo \"- Auth0 Bearer doesn't pass CF Access → 401 → \\`setItem('lspro_tenant_id', ...)\\` never fires\"\n        echo \"- Downstream callers (scanLookup, CEStep02Processing, apiFetch) read empty tenant_id\"\n        echo \"- shopops-api returns 400 \\`tenant_id param required\\`\"\n        echo \"\"\n        echo \"## Fix\"\n        echo \"Mirror the existing \\`resolveUserTier\\` / \\`resolveUserRole\\` pattern (line ~19-35)\"\n        echo \"with a new \\`resolveTenantId(user)\\` helper that reads from the same Auth0\"\n        echo \"namespace, then write tenant synchronously in the AuthProvider effect\"\n        echo \"BEFORE the broken \\`/api/tenant/plan\\` fetch fires.\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- Modified: \\`src/context/AuthContext.jsx\\`\"\n        echo \"- Created: \\`src/context/AuthContext.test.*\\` (4+ tests)\"\n        echo \"\"\n        echo \"## Tests\"\n        echo \"- \\`bun test\\` — 4+ new tests on resolveTenantId behavior\"\n        echo \"- \\`bun run build\\` — clean\"\n        echo \"\"\n        echo \"## Grep assertions\"\n        echo \"- \\`grep -c 'resolveTenantId' src/context/AuthContext.jsx\\` ≥ 1\"\n        echo \"- \\`grep -c 'sessionStorage.setItem.*lspro_tenant_id' src/context/AuthContext.jsx\\` ≥ 1\"\n        echo \"\"\n        echo \"## What this PR does NOT do (deferred to WO #2 + #3)\"\n        echo \"- Touch GAS / \\`lspro_token\\` path\"\n        echo \"- Remove \\`/api/tenant/plan\\` fetch (stays as backup)\"\n        echo \"- Forward-port to \\`release/ce\\` (separate commit)\"\n        echo \"- Modify \\`src/lib/api.ts\\`\"\n        echo \"\"\n        echo \"## Post-merge verification (operator-driven)\"\n        echo \"1. Wait for Vercel deploy. **Vercel cache caveat applies** (anchor: PR #73 incident).\"\n        echo \"2. If bundle filename doesn't change: empty commit → push → recheck. Dashboard redeploy without build cache if still stuck.\"\n        echo \"3. Hard refresh \\`app.livesellerpro.app\\`.\"\n        echo \"4. DevTools → Application → Session Storage shows \\`lspro_tenant_id\\` key with tenant slug.\"\n        echo \"5. CE Step 1 scan UPC \\`76194137939500141\\` → row populates with real title + cover within 2s.\"\n        echo \"6. CE Step 2 upload 45-row Lunar CSV → match rate > 80% (not 0/45).\"\n        echo \"7. \\`curl https://app.livesellerpro.app/assets/<new-bundle>.js | grep -c resolveTenantId\\` ≥ 1.\"\n        echo \"\"\n        echo \"## Follow-up WOs queued\"\n        echo \"- WO-AUTH-RETIRE-LSPRO-GAS-BROWSER-TOKEN-01\"\n        echo \"- WO-AUTH-GAS-LAUNCHER-TO-AUTH0-BRIDGE-01\"\n        echo \"- WO-AUTH-E2E-CE-SINGLE-AUTH-PATH-01\"\n      } > \"$BODY_FILE\"\n\n      PR_URL=$(gh pr create \\\n        --repo \"$REPO\" \\\n        --base \"$BASE\" \\\n        --head \"$BRANCH\" \\\n        --title \"$TITLE\" \\\n        --body-file \"$BODY_FILE\")\n\n      echo \"PR_URL=${PR_URL}\"\n\n  # ---------------------------------------------------------------------------\n  # 11. BUILD-MANIFEST — Captain CI manifest v2\n  # ---------------------------------------------------------------------------\n  - id: build-manifest\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Construct the Captain CI completion manifest v2 for\n      WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01.\n\n      Output Markdown matching exactly:\n\n      ```\n      WO: WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01\n      Builder: Cauldron (bdc-auth-ce-auth0-tenant-metadata-harden workflow)\n      Files modified: lspro-react/src/context/AuthContext.jsx\n      Files created: <test path from commit-and-push>\n      Tests: <count passing / count total via bun test>\n      PRs: <PR URL from open-pr>\n      Merge ancestors: lspro-react main HEAD: <sha>, manifest commit: <sha>, behind_by: 0\n      Grep assertions:\n        - [ $(grep -c 'resolveTenantId' src/context/AuthContext.jsx) -ge 1 ]\n        - [ $(grep -c 'sessionStorage.setItem.*lspro_tenant_id' src/context/AuthContext.jsx) -ge 1 ]\n      Runtime verification: PENDING (operator-driven post-Vercel-deploy DevTools + bundle-grep)\n      Vercel deployment: PENDING (auto-deploy on main merge)\n      VALIDATION: PASS\n      ```\n\n      ## Inputs\n      Commit + Branch:\n      $commit-and-push.output\n\n      PR:\n      $open-pr.output\n\n      Build:\n      $verify-build.output\n\n      Tests:\n      $verify-tests.output\n\n      Grep:\n      $verify-grep.output\n\n  # ---------------------------------------------------------------------------\n  # 12. FLIP-NOTION — flip WO to REVIEW with manifest\n  # ---------------------------------------------------------------------------\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Update the Notion WO page for WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01.\n\n      ## Required Notion Changes\n      - Claude Status: REVIEW\n      - Status: Review\n      - Add the Captain CI manifest below as a page comment\n\n      ## Manifest\n      $build-manifest.output\n\n      ## Notion page ID\n      OPERATOR-FILL-IN-BEFORE-FIRING — search Notion DB\n      a6df831c-0b52-449f-8ca4-d77be6b70d0a for WO-AUTH-CE-AUTH0-TENANT-METADATA-HARDEN-01.\n\n      If Notion MCP is not available inside the container, do not invent a workaround.\n      Output a TODO for XO to flip Notion manually with the manifest above.\n",
  "bdc-author-wo-batch": "# Drafted by Major Build for WO-HARNESS-AUTHOR-WO-BATCH-META-01 on 2026-05-13\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified)\n#\n# Pre-flight (4-block):\n#   1. YAML exists: this file\n#   2. Spec exists: BDC_XO/docs/work-orders/WO-HARNESS-AUTHOR-WO-BATCH-META-01.md\n#   3. CWD scope: bdc-xo (WO specs + YAMLs live there -- select bdc-xo when firing)\n#   4. Codebase registered: bluedevilcollectibles/bdc-xo (verify in Archon DB)\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh bdc-author-wo-batch.yaml\n#\n# Fire:\n#   /workflow run bdc-author-wo-batch \\\n#     WO_CONCEPTS_FILE=memory/wo-batch-concepts.json \\\n#     PLAN_DOC_PATH=docs/plans/2026-06-15-mutant-market-hq-readiness.md \\\n#     [CALIBRATION_DOC=memory/foo.md] \\\n#     [DRY_RUN=true]\n#\n# Note: Downloads/<WO_ID>.yaml writes are N/A for harness execution (Hetzner has no\n# Windows Downloads path). John copies from bdc-xo after merge if needed.\n\nname: bdc-author-wo-batch\ndescription: |\n  Use when: Batch-authoring WO specs + Devils Cauldron YAMLs with adversarial 12-check review gate.\n  Inputs:\n    WO_CONCEPTS_FILE: path in bdc-xo to JSON file: [{wo_id,title,repo,polish_bar,builder,priority,summary}, ...]\n    PLAN_DOC_PATH: path in bdc-xo to source plan doc\n    CALIBRATION_DOC: optional path in bdc-xo to calibration ruling\n    NOTION_DB_ID: Notion DB (default: a6df831c-0b52-449f-8ca4-d77be6b70d0a)\n    DRY_RUN: 'true' = write files + skip Notion filing and PR\n  Does: For each WO concept, adversarially reviews spec+YAML via WO-Lifecycle:Validate 12-check gate,\n        iterates up to 8 times, commits passing WOs to bdc-xo, files to Notion, fails shut on exhaustion.\n  Repo: bluedevilcollectibles/bdc-xo (operator selects bdc-xo codebase when firing).\n  parallel_ok: true\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\ntarget_repo: bluedevilcollectibles/bdc-xo\n\n# 2026-05-16 fix v2: Cauldron API ONLY accepts conversationId + message — the\n# `inputs` field in API payloads is silently dropped. Engine injects only the\n# DEFAULTS from this block as $INPUT_<NAME> env vars. To run a different batch,\n# either (a) edit the WO_CONCEPTS_FILE default and re-fire, or (b) pass the path\n# in the API `message` and have bash parse it (mirrors bdc-feature-development's\n# WO_ID-from-USER_MESSAGE pattern).\n#\n# Per-batch default below = Phase 4 backend cross-cutting sortie 2026-05-17.\n# 5 WOs that fill backend gaps the LSPRO UI rebuild (promotion/v2) surfaces.\n# Change to next batch path when re-firing.\ninputs:\n  WO_CONCEPTS_FILE:\n    default: \"docs/plans/2026-05-17-phase4-backend-cross-cutting-concepts.json\"\n  PLAN_DOC_PATH:\n    default: \"docs/superpowers/specs/2026-05-17-lspro-shopops-hq-rebuild-PLAN-v2.md\"\n  CALIBRATION_DOC:\n    default: \"\"\n  NOTION_DB_ID:\n    default: \"a6df831c-0b52-449f-8ca4-d77be6b70d0a\"\n  DRY_RUN:\n    default: \"false\"\n\nnodes:\n\n  # Pre-flight: fail closed if container lacks GITHUB auth (WO-169, 2026-05-16).\n  # Anchor: 2026-05-16 engine sortie. commit-and-push + open-pr both `exit 0`-swallowed\n  # underlying auth failures via `|| echo 'STATUS=*_failed'`; workflow marked success,\n  # no PR existed, 13 specs lost. Catch missing GITHUB_TOKEN here before any work.\n  - id: preflight-github-auth\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      DRY_RUN=\"${INPUT_DRY_RUN:-${DRY_RUN:-false}}\"\n      if [ \"$DRY_RUN\" = \"true\" ]; then\n        echo \"preflight-github-auth: DRY_RUN=true -- skipping auth probe\"\n        exit 0\n      fi\n      if ! git push --dry-run origin \"HEAD:refs/heads/preflight-auth-probe-$(date +%s)\" 2>&1; then\n        echo \"ERROR: GITHUB auth check failed -- container cannot push to origin.\" >&2\n        echo \"Likely cause: GITHUB_TOKEN missing in executor env, or token lacks repo scope.\" >&2\n        exit 1\n      fi\n      echo \"preflight-github-auth PASS\"\n\n  - id: read-inputs\n    depends_on: [preflight-github-auth]\n    bash: |\n      set -euo pipefail\n      # 2026-05-16: read from $INPUT_<NAME> (Cauldron engine injects these from\n      # workflow.inputs); fall back to bare $NAME for legacy /workflow run cmd-line\n      # arg style. Both work; API payload uses INPUT_ prefix path.\n      PLAN_DOC_PATH=\"${INPUT_PLAN_DOC_PATH:-${PLAN_DOC_PATH:-docs/plans/2026-06-15-mutant-market-hq-readiness.md}}\"\n      WO_CONCEPTS_FILE=\"${INPUT_WO_CONCEPTS_FILE:-${WO_CONCEPTS_FILE:-}}\"\n      CALIBRATION_DOC=\"${INPUT_CALIBRATION_DOC:-${CALIBRATION_DOC:-}}\"\n      NOTION_DB_ID=\"${INPUT_NOTION_DB_ID:-${NOTION_DB_ID:-a6df831c-0b52-449f-8ca4-d77be6b70d0a}}\"\n      DRY_RUN=\"${INPUT_DRY_RUN:-${DRY_RUN:-false}}\"\n\n      if [ -z \"$WO_CONCEPTS_FILE\" ]; then\n        echo \"ERROR: WO_CONCEPTS_FILE is required. Example: WO_CONCEPTS_FILE=memory/wo-batch-concepts.json\" >&2\n        exit 1\n      fi\n\n      # Read plan doc (try local file first, then gh api cross-repo)\n      PLAN_CONTENT=\"\"\n      if [ -f \"$PLAN_DOC_PATH\" ]; then\n        PLAN_CONTENT=$(cat \"$PLAN_DOC_PATH\")\n      else\n        PLAN_CONTENT=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/${PLAN_DOC_PATH}?ref=main\" \\\n          --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      fi\n      [ -z \"$PLAN_CONTENT\" ] && { echo \"ERROR: Plan doc not found at $PLAN_DOC_PATH\" >&2; exit 1; }\n\n      # Read WO concepts JSON\n      WO_CONCEPTS=\"\"\n      if [ -f \"$WO_CONCEPTS_FILE\" ]; then\n        WO_CONCEPTS=$(cat \"$WO_CONCEPTS_FILE\")\n      else\n        WO_CONCEPTS=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/${WO_CONCEPTS_FILE}?ref=main\" \\\n          --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      fi\n      [ -z \"$WO_CONCEPTS\" ] && { echo \"ERROR: WO_CONCEPTS_FILE not found: $WO_CONCEPTS_FILE\" >&2; exit 1; }\n\n      # Validate JSON and extract WO IDs\n      WO_SUMMARY=$(echo \"$WO_CONCEPTS\" | python3 -c \"\n      import sys, json\n      data = json.load(sys.stdin)\n      print(f'WO_COUNT={len(data)}')\n      print('WO_IDS=' + ','.join(d['wo_id'] for d in data))\n      for d in data:\n          print(f\\\"  {d['wo_id']} | {d.get('polish_bar','?')} | {d.get('builder','Major Build')} | {d.get('summary','')[:60]}\\\")\n      \" 2>&1) || { echo \"ERROR: WO_CONCEPTS_FILE is not valid JSON array of {wo_id,...} objects\" >&2; exit 1; }\n\n      # Read calibration doc if provided\n      CALIBRATION=\"(none)\"\n      if [ -n \"$CALIBRATION_DOC\" ]; then\n        if [ -f \"$CALIBRATION_DOC\" ]; then\n          CALIBRATION=$(cat \"$CALIBRATION_DOC\")\n        else\n          CALIBRATION=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${CALIBRATION_DOC}?ref=main\" \\\n            --jq .content 2>/dev/null | base64 -d 2>/dev/null || echo \"(calibration not found)\")\n        fi\n      fi\n\n      # Read archon-yaml-authoring doctrine from bdc-xo wiki if present\n      ARCHON_DOCTRINE=\"\"\n      if [ -f \"xo-wiki/wiki/doctrine/archon-yaml-authoring/_index.md\" ]; then\n        ARCHON_DOCTRINE=$(head -100 \"xo-wiki/wiki/doctrine/archon-yaml-authoring/_index.md\")\n      fi\n\n      printf 'NOTION_DB_ID=%s\\nDRY_RUN=%s\\n\\n' \"$NOTION_DB_ID\" \"$DRY_RUN\"\n      printf '%s\\n\\n' \"$WO_SUMMARY\"\n      printf '=== WO CONCEPTS JSON ===\\n%s\\n\\n' \"$WO_CONCEPTS\"\n      printf '=== PLAN DOC (%s) ===\\n%s\\n\\n' \"$PLAN_DOC_PATH\" \"$PLAN_CONTENT\"\n      printf '=== CALIBRATION ===\\n%s\\n\\n' \"$CALIBRATION\"\n      # 2026-05-16: don't use `[ ] && cmd` as terminal line under `set -e` —\n      # if the test is false (ARCHON_DOCTRINE empty), [ exits 1, becomes the\n      # script's exit code, and the whole node dies silently with no stderr.\n      # Anchor: Codex diagnosis of post-#77 fast-die-no-stderr symptom.\n      if [ -n \"$ARCHON_DOCTRINE\" ]; then\n        printf '=== ARCHON YAML DOCTRINE (excerpt) ===\\n%s\\n\\n' \"$ARCHON_DOCTRINE\"\n      fi\n\n  - id: author-wo\n    depends_on: [read-inputs]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: VALIDATE_PASS_ALL\n      max_iterations: 8\n      prompt: |\n        You are the BDC WO Batch Authoring Pipeline, alternating between AUTHOR and ADVERSARIAL REVIEWER roles.\n        CWD = bdc-xo repo root. You have full file read/write tool access.\n\n        ## Pipeline Inputs\n        $read-inputs.output\n\n        ## Previous Iteration Results (empty on first iteration)\n        $LOOP_PREV_OUTPUT\n\n        ---\n\n        ## PHASE 1 — AUTHOR\n\n        For each WO concept in the JSON array:\n\n        **1a. Resume check**: Does `docs/work-orders/<wo_id>.md` AND `yaml-drafts/<wo_id>.yaml` already\n        exist on disk? If both exist, SKIP this WO — it passed review in a prior iteration.\n\n        **1b. Draft the WO spec** (12-section wo-recipe.md template):\n        ```\n        ## 1. Objective\n        ## 2. Behavior Source of Truth   <-- must cite concrete file path or WO ID\n        ## 3. Prior Art Check\n        ## 4. System Context             <-- list dependencies or state \"standalone\"\n        ## 5. UI Hierarchy               <-- N/A with one-line reason if not UI\n        ## 6. Mode Behavior Matrix       <-- N/A with one-line reason if not multi-mode\n        ## 7. Backend Function Inventory <-- every function labeled EXISTING (file:line) or NEW\n        ## 8. Data Flow\n        ## 9. Database Schema References <-- N/A with one-line reason if no DB;\n                                            if DB: cite information_schema query OR migration file\n        ## 10. Deploy Target\n        ## 11. Test Scenarios            <-- >= 3 tests, each with Given/When/Then structure\n        ## 12. Stop Point                <-- CI-executable commands, Given/When/Then\n        ```\n        Add a \"Grep assertions\" section if the WO touches 2+ repos (Check 8A requirement).\n        Honor `polish_bar`:\n        - ROUGH: include \"V1 limitations\" section listing explicit exclusions\n        - HIGH: full test matrix with edge cases, error paths, concurrency\n\n        Also reference `wo-recipe.md` somewhere in the body (Check 2 requirement).\n\n        **1c. Draft the Devils Cauldron YAML** (archon-yaml-authoring 13 hard rules):\n        - `model: sonnet` at workflow root\n        - `policyFile: harness/policies/agent-behavior.md` at root\n        - All file paths RELATIVE TO REPO ROOT (no repo-name prefix)\n        - Every `loop:` node declares ALL FOUR fields: `prompt:`, `until:`, `max_iterations:`, `fresh_context:`\n        - Final node MUST be `flip-notion` (Archon does not auto-update Notion)\n        - NO `curl` to n8n.bluedevilcollectibles.com in bash nodes (hangs in executor subprocess)\n        - Bash: use `$WORKFLOW_ID` NOT `${run.id}` (token substituted at YAML-parse time)\n        - Bash: NO `set -u` if block references `$WORKFLOW_ID` inline (it's not a runtime env var)\n        - All paths relative to the target repo root (worktree IS the repo root)\n\n        ---\n\n        ## PHASE 2 — ADVERSARIAL REVIEW (you are now a blocking reviewer — no approvals without evidence)\n\n        For EACH drafted spec+YAML pair, apply all 12 checks:\n\n        **Check 1**: 8 always-required headers present verbatim:\n          ## 1. Objective, ## 2. Behavior Source of Truth, ## 3. Prior Art Check,\n          ## 7. Backend Function Inventory, ## 8. Data Flow, ## 10. Deploy Target,\n          ## 11. Test Scenarios, ## 12. Stop Point\n          → BLOCK if any missing.\n\n        **Check 2**: Body contains the substring `wo-recipe.md`.\n          → BLOCK if absent.\n\n        **Check 3**: §2 references at least one concrete artifact (file path or WO ID).\n          → BLOCK if §2 contains only TBD, \"see body\", or empty text.\n\n        **Check 4**: WO ID matches `WO-[A-Z][A-Z0-9-]*-[0-9]+`; consistent across filename, H1, Notion title.\n          → BLOCK if inconsistent.\n\n        **Check 5**: §2 cites a real path (e.g. `docs/plans/foo.md`, `research/bar.md`, `WO-XYZ-01`).\n          → BLOCK if §2 is vague without a concrete locator.\n\n        **Check 6**: Every function/route/script in §7 labeled `EXISTING (file:line)` or `NEW`.\n          → BLOCK if any name appears without a label.\n\n        **Check 7**: §9 column/table claims cite `information_schema` query result or migration file.\n          → BLOCK if §9 makes a column claim without citing source.\n\n        **Check 8**: §11 has >= 3 test scenarios each with Given/When/Then structure.\n          → BLOCK if fewer than 3, or any test is purely descriptive.\n\n        **Check 8A**: If WO's files span 2+ repos → must have `Grep assertions` block with grep command per repo.\n          → BLOCK if split-repo WO lacks grep assertions.\n\n        **Check 9**: §12 stop conditions are CI-executable commands (Given/When/Then).\n          → BLOCK if any stop condition is vague (\"staging accessible\", \"CI validates\").\n\n        **Check 10**: 8 required questions addressed somewhere in body:\n          1. What behavior exists AFTER this WO?\n          2. Where is the source of truth?\n          3. Who owns this system?\n          4. What existing logic is reused?\n          5. What is the exact schema? (N/A if no DB)\n          6. What MUST NEVER break? (invariants)\n          7. How does CI prove it works?\n          8. What happens if it runs twice? (idempotency)\n          → BLOCK if any unanswered.\n\n        **Check 11** (HARDEST — anti-guess hardening): Every component name, table name, column name,\n        route path in the spec traces to a source file. No invented names.\n          → BLOCK if any specific claim is not anchored to a real source.\n\n        **Check 12**: §4 lists dependencies OR explicitly says \"standalone\".\n          → BLOCK if §4 is empty and WO touches more than one subsystem.\n\n        Output per WO: `VALIDATE: PASS` (all 12) or `VALIDATE: FAIL — Check N: <gap>` for each failure.\n\n        ---\n\n        ## PHASE 3 — FIX OR COMMIT\n\n        - **FAIL**: Revise spec + YAML to address each listed gap. Re-run all 12 checks on the revision.\n          If revised passes → write files; if still fails → flag for next iteration.\n        - **PASS**: Write both files to disk:\n          - `docs/work-orders/<wo_id>.md`\n          - `yaml-drafts/<wo_id>.yaml`\n\n        ---\n\n        ## SENTINEL RULE\n\n        Output `<promise>VALIDATE_PASS_ALL</promise>` ONLY when EVERY WO concept has:\n        - `docs/work-orders/<wo_id>.md` exists on disk\n        - `yaml-drafts/<wo_id>.yaml` exists on disk\n        - Final review verdict was VALIDATE: PASS\n\n        If any WO exhausts all remaining iterations without passing, do NOT emit the sentinel.\n        Instead output a `BATCH_FAILURE` block:\n        ```\n        BATCH_FAILURE:\n          - WO_ID: <id>\n            failed_checks: [list]\n            critique: <specific gaps that could not be resolved>\n        ```\n\n  - id: verify-files\n    depends_on: [author-wo]\n    bash: |\n      set -euo pipefail\n      # 2026-05-16: mirror read-inputs dual-path pattern — engine injects\n      # INPUT_<NAME> from YAML defaults; fall back to bare env for legacy.\n      WO_CONCEPTS_FILE=\"${INPUT_WO_CONCEPTS_FILE:-${WO_CONCEPTS_FILE:-}}\"\n      [ -z \"$WO_CONCEPTS_FILE\" ] && { echo \"ERROR: WO_CONCEPTS_FILE not set\" >&2; exit 1; }\n\n      # Get WO IDs from concepts file\n      if [ -f \"$WO_CONCEPTS_FILE\" ]; then\n        WO_IDS=$(python3 -c \"\n      import json, sys\n      data = json.load(open('$WO_CONCEPTS_FILE'))\n      print(' '.join(d['wo_id'] for d in data))\n      \")\n      else\n        CONCEPTS_JSON=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/${WO_CONCEPTS_FILE}?ref=main\" \\\n          --jq .content 2>/dev/null | base64 -d 2>/dev/null || echo \"[]\")\n        WO_IDS=$(echo \"$CONCEPTS_JSON\" | python3 -c \"\n      import json, sys\n      data = json.load(sys.stdin)\n      print(' '.join(d['wo_id'] for d in data))\n      \")\n      fi\n\n      echo \"=== verify-files: checking WO artifacts ===\"\n      PASS=0\n      FAIL=0\n      for WO_ID in $WO_IDS; do\n        SPEC=\"docs/work-orders/${WO_ID}.md\"\n        YAML=\"yaml-drafts/${WO_ID}.yaml\"\n        SPEC_OK=0\n        YAML_OK=0\n        [ -f \"$SPEC\" ] && SPEC_OK=1\n        [ -f \"$YAML\" ] && YAML_OK=1\n\n        if [ \"$SPEC_OK\" -eq 1 ] && [ \"$YAML_OK\" -eq 1 ]; then\n          # Structural spot-checks (warns only, not hard failures)\n          grep -q \"## 1. Objective\" \"$SPEC\" \\\n            || echo \"  WARN: $WO_ID spec missing ## 1. Objective header\"\n          grep -q \"## 12. Stop Point\" \"$SPEC\" \\\n            || echo \"  WARN: $WO_ID spec missing ## 12. Stop Point header\"\n          grep -q \"model: sonnet\" \"$YAML\" \\\n            || echo \"  WARN: $WO_ID YAML missing model: sonnet (Rule 1)\"\n          grep -q \"policyFile:\" \"$YAML\" \\\n            || echo \"  WARN: $WO_ID YAML missing policyFile (Rule 6)\"\n          echo \"PASS: $WO_ID (spec + YAML present)\"\n          PASS=$((PASS + 1))\n        else\n          echo \"FAIL: $WO_ID (spec_ok=$SPEC_OK yaml_ok=$YAML_OK)\"\n          FAIL=$((FAIL + 1))\n        fi\n      done\n\n      echo \"\"\n      echo \"SUMMARY: $PASS PASS, $FAIL FAIL\"\n      [ \"$FAIL\" -eq 0 ] || { echo \"ERROR: $FAIL WO(s) missing artifacts\" >&2; exit 1; }\n      echo \"verify-files PASS\"\n\n  # load_bearing: true (WO-167 doctrine, WO-169 anchor)\n  # Failure here MUST halt the workflow -- silent-swallow of push failure caused\n  # 13-spec loss in 2026-05-16 engine sortie. Real retry: 3 attempts, exponential\n  # backoff, explicit `exit 1` on final failure. NO `|| echo 'STATUS=*_failed'`.\n  - id: commit-and-push\n    depends_on: [verify-files]\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      DRY_RUN=\"${DRY_RUN:-false}\"\n      BRANCH=\"wo/author-wo-batch-run-$(date +%Y%m%d%H%M%S)\"\n\n      # WO-168 Tier 1: save load-bearing artifacts to host-mounted volume BEFORE\n      # any remote operation. If push fails or the worktree is pruned, the bundle\n      # on the host disk is the recovery source of truth.\n      # Recovery procedure: docs/recovery/host-artifact-recovery.md\n      save_bundle_to_host() {\n        local run_id=\"${WORKFLOW_RUN_ID:-${WORKFLOW_ID:-unknown-$(date +%Y%m%d%H%M%S)}}\"\n        local dest=\"/host-artifacts/${run_id}\"\n        if [ ! -d /host-artifacts ]; then\n          echo \"WARN: /host-artifacts not mounted -- skipping bundle save (recovery from worktree only)\" >&2\n          return 0\n        fi\n        mkdir -p \"$dest\" 2>/dev/null || {\n          echo \"WARN: could not mkdir $dest -- skipping bundle save\" >&2\n          return 0\n        }\n        # git bundle of HEAD captures the entire reachable history of the branch\n        # in a single file -- can be fetched back even after the branch is gone.\n        if git bundle create \"$dest/branch.bundle\" HEAD 2>\"$dest/bundle.err\"; then\n          echo \"  host-artifact: bundle saved $dest/branch.bundle ($(stat -c%s \"$dest/branch.bundle\" 2>/dev/null || echo '?') bytes)\"\n        else\n          echo \"WARN: git bundle create failed -- see $dest/bundle.err\" >&2\n        fi\n        # Human-readable diff stat for quick triage without unpacking the bundle\n        git diff --stat HEAD~1 HEAD > \"$dest/changes.txt\" 2>/dev/null \\\n          || git log -1 --name-status HEAD > \"$dest/changes.txt\" 2>/dev/null \\\n          || echo \"(no diff/log available)\" > \"$dest/changes.txt\"\n        # Recovery breadcrumb: branch name + commit + timestamp\n        {\n          echo \"branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo unknown)\"\n          echo \"sha=$(git rev-parse HEAD 2>/dev/null || echo unknown)\"\n          echo \"saved_at=$(date -u +%Y-%m-%dT%H:%M:%SZ)\"\n          echo \"workflow_run_id=${run_id}\"\n          echo \"recovery_cmd=git fetch ${dest}/branch.bundle main:rescue-${run_id}\"\n        } > \"$dest/MANIFEST.txt\"\n        echo \"  host-artifact: manifest written $dest/MANIFEST.txt\"\n      }\n\n      git checkout -b \"$BRANCH\"\n      git add docs/work-orders/ yaml-drafts/ 2>/dev/null || true\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes -- WO files may already be committed on a prior run\"\n        echo \"BRANCH=$(git rev-parse --abbrev-ref HEAD)\"\n        echo \"SHA=$(git rev-parse HEAD)\"\n        exit 0\n      fi\n\n      FILE_COUNT=$(git diff --cached --name-only | wc -l | tr -d ' ')\n      git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n        commit -m \"feat(wo-batch): WO-HARNESS-AUTHOR-WO-BATCH-META-01 -- batch author ${FILE_COUNT} WO artifacts\n\n      Each WO spec + YAML adversarially reviewed (WO-Lifecycle:Validate 12-check gate) before commit.\n      Workflow: bdc-author-wo-batch. Operator reviews files then promotes to queue.\n\n      Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>\"\n\n      # WO-168 Tier 1: bundle BEFORE push so a push failure does not destroy work.\n      save_bundle_to_host\n\n      if [ \"$DRY_RUN\" = \"true\" ]; then\n        echo \"DRY_RUN=true -- skipping push (bundle still saved to host)\"\n        echo \"BRANCH=$BRANCH\"\n        echo \"SHA=$(git rev-parse HEAD)\"\n        exit 0\n      fi\n\n      # Real retry guard (WO-169). 3 attempts, 5s/10s/15s backoff, fail closed.\n      PUSH_OK=false\n      for attempt in 1 2 3; do\n        if git push -u origin \"$BRANCH\" 2>&1; then\n          PUSH_OK=true\n          break\n        fi\n        echo \"WARN: push attempt $attempt failed; retrying after $((attempt * 5))s...\" >&2\n        sleep $((attempt * 5))\n      done\n      if [ \"$PUSH_OK\" = \"false\" ]; then\n        echo \"ERROR: git push failed after 3 attempts -- aborting workflow (load_bearing node).\" >&2\n        exit 1\n      fi\n      echo \"BRANCH=$BRANCH\"\n      echo \"SHA=$(git rev-parse HEAD)\"\n\n  # load_bearing: true (WO-167 doctrine, WO-169 anchor)\n  # 2026-05-16 engine sortie anchor: this node emitted `STATUS=pr_open_failed`\n  # and `exit 0`, so the workflow reported success while no PR existed and 13\n  # specs were lost. Now: real 3-attempt retry with exponential backoff, and\n  # `exit 1` on final failure so the workflow halts visibly.\n  - id: open-pr\n    depends_on: [commit-and-push]\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      DRY_RUN=\"${DRY_RUN:-false}\"\n      if [ \"$DRY_RUN\" = \"true\" ]; then\n        echo \"STATUS=skipped_dry_run\"\n        exit 0\n      fi\n\n      BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo \"\")\n      if [ -z \"$BRANCH\" ] || [ \"$BRANCH\" = \"HEAD\" ]; then\n        echo \"ERROR: detached HEAD or unknown branch -- cannot open PR.\" >&2\n        exit 1\n      fi\n\n      REPO=\"bluedevilcollectibles/bdc-xo\"\n      EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n        --json url --jq '.[0].url // empty' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then\n        echo \"PR_URL=$EXIST\"\n        exit 0\n      fi\n\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"WO-HARNESS-AUTHOR-WO-BATCH-META-01: batch WO authoring run via bdc-author-wo-batch workflow.\"\n        echo \"\"\n        echo \"Each WO spec + YAML was adversarially reviewed (WO-Lifecycle:Validate.md 12-check gate)\"\n        echo \"before commit. Review files, then promote WOs to queue once satisfied.\"\n        echo \"\"\n        echo \"Generated by bdc-author-wo-batch workflow\"\n      } > \"$BODY_FILE\"\n\n      # Real retry guard (WO-169). 3 attempts, 5s/10s/15s backoff, fail closed.\n      PR_OK=false\n      for attempt in 1 2 3; do\n        if URL=$(gh pr create --repo \"$REPO\" --base main --head \"$BRANCH\" \\\n            --title \"feat(wo-batch): batch WO authoring run -- adversarial review gate applied\" \\\n            --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$URL\"\n          PR_OK=true\n          break\n        fi\n        # Race-condition recovery: another caller may have opened the PR between\n        # our list check and our create attempt.\n        EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n          --json url --jq '.[0].url // empty' 2>/dev/null || true)\n        if [ -n \"$EXIST\" ]; then\n          echo \"PR_URL=$EXIST\"\n          PR_OK=true\n          break\n        fi\n        echo \"WARN: gh pr create attempt $attempt failed; retrying after $((attempt * 5))s...\" >&2\n        sleep $((attempt * 5))\n      done\n      if [ \"$PR_OK\" = \"false\" ]; then\n        echo \"ERROR: gh pr create failed after 3 attempts -- aborting workflow (load_bearing node).\" >&2\n        echo \"Branch was pushed; recover manually with: gh pr create --repo $REPO --base main --head $BRANCH\" >&2\n        exit 1\n      fi\n\n  - id: file-notion\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      File each passing WO from this batch run to Notion.\n\n      ## DRY_RUN CHECK\n      If $read-inputs.output contains `DRY_RUN=true`, skip all Notion filing.\n      Output instead:\n        XO_TODO: Operator review complete -> promote WOs to queue manually.\n        XO_TODO_WOS: [list each WO_ID from $read-inputs.output that passed verify-files]\n\n      ## NOTION FILING (when DRY_RUN=false)\n      For each WO_ID listed as PASS in $verify-files.output:\n      1. Read the spec file `docs/work-orders/<WO_ID>.md` to extract the Objective (§1) as the Notes summary.\n      2. Extract builder, priority from the WO concepts JSON (in $read-inputs.output).\n      3. Create Notion page in database `a6df831c-0b52-449f-8ca4-d77be6b70d0a` with:\n         - Task: <WO_ID>\n         - WO ID: <WO_ID>\n         - Status: To Do\n         - Claude Status: QUEUED\n         - Priority: <from concept, default P1>\n         - Builder: <from concept, default Major Build>\n         - Execution State: ACTIVE\n         - Project: <from concept, default Infrastructure>\n         - Notes: first 300 chars of §1 Objective + \" WO file: BDC_XO/docs/work-orders/<WO_ID>.md\"\n      4. Use the Notion MCP notion-create-pages tool.\n      5. Output: NOTION_URL=<url> for each created page.\n\n      For any WO in BATCH_FAILURE (from $author-wo.output): output XO_TODO with critique trail.\n\n      ## Inputs\n      $read-inputs.output\n      $author-wo.output\n      $verify-files.output\n      $commit-and-push.output\n\n  - id: build-manifest\n    depends_on: [file-notion]\n    task_class: clerical\n    prompt: |\n      Build the Captain CI manifest v2 for WO-HARNESS-AUTHOR-WO-BATCH-META-01.\n\n      WO: WO-HARNESS-AUTHOR-WO-BATCH-META-01\n      Builder: Major Build (bdc-author-wo-batch harness workflow)\n      Files created: [list from $verify-files.output -- each PASS: <WO_ID> line maps to 2 files]\n      Files modified: none\n      Tests: N/A (workflow-only -- adversarial review IS the gate; verify-files confirms artifacts)\n      PRs:\n        - bdc-xo: [PR_URL from $open-pr.output, or DRY_RUN=skipped]\n      Merge ancestors:\n        - bdc-xo main HEAD: [SHA from $commit-and-push.output], behind_by: 0\n      Grep assertions:\n        - bdc-xo: grep -c 'model: sonnet' yaml-drafts/<first_passing_WO_ID>.yaml -> 1\n        - bdc-xo: grep -c '## 12. Stop Point' docs/work-orders/<first_passing_WO_ID>.md -> 1\n        - bdc-harness: grep -n 'bdc-author-wo-batch' .archon/workflows/defaults/bdc-author-wo-batch.yaml -> >= 1\n        - bdc-harness: grep -n 'adversarial' .archon/workflows/defaults/bdc-author-wo-batch.yaml -> >= 1\n        - bdc-harness: grep -n 'max_iterations: 8' .archon/workflows/defaults/bdc-author-wo-batch.yaml -> 1\n        - bdc-harness: grep -n 'until: VALIDATE_PASS_ALL' .archon/workflows/defaults/bdc-author-wo-batch.yaml -> 1\n      Runtime verification: N/A (harness workflow YAML; no Hetzner service deploy)\n      Vercel deployment: N/A (bdc-xo and bdc-harness are not Vercel-deployed repos)\n      VALIDATION: PASS\n\n      Batch run summary:\n        WOs passed (files on disk): [from $verify-files.output PASS count]\n        WOs filed to Notion: [NOTION_URL lines from $file-notion.output]\n        WOs failed (BATCH_FAILURE): [from $author-wo.output BATCH_FAILURE block, if any]\n        Adversarial review iterations: [from $author-wo.output]\n\n      Inputs:\n      $read-inputs.output\n      $author-wo.output\n      $verify-files.output\n      $commit-and-push.output\n      $open-pr.output\n      $file-notion.output\n\n  - id: flip-notion-meta\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-HARNESS-AUTHOR-WO-BATCH-META-01 to REVIEW with manifest in Notes property.\n\n      Notion page ID: 35fef4c9-ce00-81f1-aaaa-ded673d03082\n      Required changes:\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> $build-manifest.output verbatim\n      - claimed_by: Major Build (bdc-author-wo-batch)\n      - claimed_at: 2026-05-13\n\n      If Notion MCP unavailable, output:\n        XO_TODO: Flip WO-HARNESS-AUTHOR-WO-BATCH-META-01 to REVIEW; paste manifest into Notes property.\n\n      Manifest:\n      $build-manifest.output\n",
  "bdc-bug-fix": "name: bdc-bug-fix\ndescription: |\n  Use when: Executing a BDC bug-class Work Order from an approved spec.\n  Input: WO_ID in the environment or user message, plus repo context from Overlord.\n  Does: Reads the BDC WO spec, writes a failing test that reproduces the bug, implements a fix, verifies all tests pass, commits, pushes, opens PRs, and builds a Captain CI manifest.\n  NOT for: Features, doctrine-only updates, cleanup sweeps, infrastructure deploys, or P0 incidents requiring emergency hotfixes.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: claude-sonnet-4-5\n\nnodes:\n  - id: read-spec\n    persona: xo\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-}\"\n      if [ -z \"$WO_ID\" ]; then\n        WO_ID=$(printf '%s\\n' \"$USER_MESSAGE\" | grep -Eo 'WO-[A-Z0-9-]+' | head -n 1 || true)\n      fi\n      if [ -z \"$WO_ID\" ]; then\n        echo \"WO_ID missing. Provide WO_ID in environment or user message.\" >&2\n        exit 1\n      fi\n      # Try direct path first, then date-prefixed glob (specs are commonly named YYYY-MM-DD-WO-...md)\n      # CWD is the bdc-xo repo root (Archon worktree), so paths are relative — no BDC_XO/ prefix.\n      SPEC_PATH=\"docs/superpowers/specs/${WO_ID}.md\"\n      if [ ! -f \"$SPEC_PATH\" ]; then\n        SPEC_PATH=$(ls docs/superpowers/specs/*${WO_ID}.md 2>/dev/null | head -n 1 || true)\n      fi\n      if [ -z \"$SPEC_PATH\" ] || [ ! -f \"$SPEC_PATH\" ]; then\n        # Fallback 3: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        SPEC_CONTENT=\"\"\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n        fi\n        if [ -z \"$SPEC_CONTENT\" ]; then\n          echo \"Spec not found for WO_ID=${WO_ID} (tried docs/superpowers/specs/${WO_ID}.md, date-prefix glob, and GitHub issue search)\" >&2\n          exit 1\n        fi\n        printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:issues/%s\\n\\n' \"$WO_ID\" \"$ISSUE_NUM\"\n        printf '%s\\n' \"$SPEC_CONTENT\"\n        exit 0\n      fi\n      printf 'WO_ID=%s\\nSPEC_PATH=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\"\n      cat \"$SPEC_PATH\"\n\n  - id: post-started\n    persona: xo\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      curl -sS -X POST \"https://n8n.bluedevilcollectibles.com/webhook/builder-status\" \\\n        -H \"Content-Type: application/json\" \\\n        -d \"{\\\"action\\\":\\\"started\\\",\\\"wo_id\\\":\\\"${WO_ID}\\\",\\\"builder\\\":\\\"Builder-4\\\",\\\"timestamp\\\":\\\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\\\"}\"\n\n  - id: reproduce\n    persona: major-build\n    depends_on: [read-spec]\n    task_class: medium_work\n    prompt: |\n      Read the BDC Work Order spec below and produce a failing test that reproduces the bug.\n\n      ## Required Output\n      Return a test file content in Markdown code block with:\n      1. Clear description of what the test is checking\n      2. The test code that WILL FAIL before any fix is applied\n      3. The expected error or assertion failure when test runs\n      4. The file path where this test should be placed\n\n      The test must demonstrate the bug in its current form. The test must fail if you run it now against the unfixed code.\n\n      ## Spec\n      $read-spec.output\n\n  - id: verify-test-fails\n    persona: major-build\n    depends_on: [reproduce]\n    bash: |\n      set -euo pipefail\n      # Extract test file path and command from reproduce output\n      # The test MUST fail before the fix. If test passes, reproduction is wrong.\n      echo \"Extracting test file path and run command from reproduce output...\"\n      TEST_PATH=$(printf '%s\\n' \"$reproduce.output\" | grep -Eo '\\`[^`]+\\.(test\\.(ts|js|py)|spec\\.(ts|js|py))[^`]*\\`' | head -n 1 | sed 's/`//g' || echo \"\")\n      if [ -z \"$TEST_PATH\" ]; then\n        echo \"Could not extract test file path from reproduce output. Look for test file references.\" >&2\n        exit 1\n      fi\n      echo \"Test file: $TEST_PATH\"\n\n      # Detect test runner and run the specific test\n      if [ -f package.json ] && grep -q '\"test\"' package.json; then\n        echo \"Running test via bun test...\"\n        if bun test -- \"$TEST_PATH\" 2>&1; then\n          echo \"ERROR: Test passed, but it MUST fail before the fix is applied!\" >&2\n          exit 1\n        fi\n      elif [ -f pyproject.toml ] || [ -f requirements.txt ]; then\n        echo \"Running test via pytest...\"\n        if pytest \"$TEST_PATH\" -v 2>&1; then\n          echo \"ERROR: Test passed, but it MUST fail before the fix is applied!\" >&2\n          exit 1\n        fi\n      else\n        echo \"Could not detect test runner. Assuming bun test...\" >&2\n        if bun test -- \"$TEST_PATH\" 2>&1; then\n          echo \"ERROR: Test passed, but it MUST fail before the fix is applied!\" >&2\n          exit 1\n        fi\n      fi\n      echo \"Confirmed: test fails as expected before fix.\"\n\n  - id: plan-fix\n    persona: war-council-architect\n    depends_on: [verify-test-fails]\n    task_class: medium_work\n    prompt: |\n      Given the reproduction from the bug investigation, plan the minimal fix to address the root cause.\n\n      ## Required Output\n      Return structured Markdown with these headings:\n      - Root cause analysis\n      - Fix strategy (minimal, surgical approach)\n      - Files to modify\n      - Files to create\n      - Verification commands (run the test again)\n      - Commit message\n      - Push policy (same as feature-development: staging gate for Rule 20 repos)\n\n      ## BDC Push Policy\n      Hardcoded Rule 20 staging-gate repos:\n      - bluedevilcollectibles/lspro-react\n      - bluedevilcollectibles/shopops-storefront\n      - bluedevilcollectibles/shopops\n\n      For those repos, use a feature branch plus PR and require the staging gate.\n      For other code repos, use feature branch plus PR without staging gate.\n\n      ## Spec\n      $read-spec.output\n\n      ## Reproduction\n      $reproduce.output\n\n  - id: implement-fix\n    persona: major-build\n    depends_on: [plan-fix]\n    task_class: medium_work\n    model: opus[1m]\n    loop:\n      prompt: |\n        Implement the approved fix plan for this BDC bug Work Order.\n\n        ## Spec\n        $read-spec.output\n\n        ## Plan\n        $plan-fix.output\n\n        ## Reproduction Test Path\n        $reproduce.output\n\n        ## Execution Rules\n        - Modify only the files identified as in scope by the plan.\n        - Prefer existing repo patterns and helpers.\n        - Do not reformat unrelated code.\n        - Do not deploy.\n        - Do not bypass approval gates.\n        - Stop and report a blocker if the plan is ambiguous or a needed source of truth is missing.\n\n        ## Loop Completion Criteria\n        After each implementation attempt:\n        1. Run the reproduction test (the one that was failing before the fix)\n        2. If test PASSES, output <promise>COMPLETE</promise>\n        3. If test FAILS, output the error and iterate\n\n        You are in loop iteration $LOOP_ITERATION.\n      until: COMPLETE\n      max_iterations: 5\n      fresh_context: true\n\n  - id: verify-test-passes\n    persona: major-build\n    depends_on: [implement-fix]\n    bash: |\n      set -euo pipefail\n      echo \"Verifying the reproduction test now passes after the fix...\"\n      TEST_PATH=$(printf '%s\\n' \"$reproduce.output\" | grep -Eo '\\`[^`]+\\.(test\\.(ts|js|py)|spec\\.(ts|js|py))[^`]*\\`' | head -n 1 | sed 's/`//g' || echo \"\")\n      if [ -z \"$TEST_PATH\" ]; then\n        echo \"Could not extract test file path from reproduce output.\" >&2\n        exit 1\n      fi\n\n      if [ -f package.json ] && grep -q '\"test\"' package.json; then\n        bun test -- \"$TEST_PATH\"\n      elif [ -f pyproject.toml ] || [ -f requirements.txt ]; then\n        pytest \"$TEST_PATH\" -v\n      else\n        bun test -- \"$TEST_PATH\"\n      fi\n      echo \"Test passes after fix.\"\n\n  - id: verify-build\n    persona: major-build\n    depends_on: [implement-fix]\n    bash: |\n      set -euo pipefail\n      if [ -f package.json ]; then\n        if node -e \"const p=require('./package.json'); process.exit(p.scripts && p.scripts.build ? 0 : 1)\"; then\n          bun run build\n        else\n          echo \"No package build script; skipping build.\"\n        fi\n      elif [ -f tests/run_all.js ]; then\n        node tests/run_all.js\n      else\n        echo \"No recognized build command; record as not applicable in manifest.\"\n      fi\n\n  - id: verify-types\n    persona: major-build\n    depends_on: [implement-fix]\n    bash: |\n      set -euo pipefail\n      if [ -f tsconfig.json ]; then\n        bunx tsc --noEmit\n      else\n        echo \"No tsconfig.json; skipping TypeScript check.\"\n      fi\n\n  - id: verify-other-tests\n    persona: major-build\n    depends_on: [implement-fix]\n    bash: |\n      set -euo pipefail\n      echo \"Running full test suite to check for regressions...\"\n      if [ -f package.json ] && node -e \"const p=require('./package.json'); process.exit(p.scripts && p.scripts.test ? 0 : 1)\"; then\n        bun test\n      elif find . -path ./.git -prune -o \\( -name '*test*' -o -name '*spec*' \\) -type f -print -quit | grep -q .; then\n        echo \"Test files detected but no bun test script was found.\" >&2\n        exit 1\n      else\n        echo \"No test files detected; record as not applicable in manifest.\"\n      fi\n\n  - id: war-council-validator\n    persona: captain-ci-validator\n    depends_on: [verify-test-passes, verify-build, verify-types, verify-other-tests]\n    task_class: medium_work\n    prompt: |\n      Invoke or emulate the war-council-validator review for this Work Order.\n\n      ## Validator Agent\n      war-council-validator\n\n      ## Inputs\n      Spec:\n      $read-spec.output\n\n      Fix Plan:\n      $plan-fix.output\n\n      Implementation output:\n      $implement-fix.output\n\n      Reproduction test status:\n      $verify-test-passes.output\n\n      Build verification:\n      $verify-build.output\n\n      Type checking:\n      $verify-types.output\n\n      Regression test suite:\n      $verify-other-tests.output\n\n      Git diff:\n      Run `git diff --stat` and `git diff --check`, then inspect the relevant changed files.\n\n      ## Critical Question for Bug Fixes\n      Does the fix address the ROOT CAUSE of the bug, or does it mask the symptom?\n      A fix that hides the real problem is worse than no fix at all.\n\n      ## Required Output\n      Return either:\n      - satisfied: with evidence that the spec deliverables were met and root cause was addressed\n      - needs_revision: with exact file/path/behavior findings\n\n  - id: decide-push-target\n    persona: major-build\n    depends_on: [war-council-validator]\n    task_class: clerical\n    prompt: |\n      Decide the push target for this BDC bug Work Order.\n\n      ## Inputs\n      Spec:\n      $read-spec.output\n\n      Validator:\n      $war-council-validator.output\n\n      ## Required Repo Policy\n      Extract the target repository from the spec content. Look for any of these patterns (in priority order):\n      1. `target_repo: <owner>/<repo>` (YAML field)\n      2. `**Target repo:** \\`<owner>/<repo>\\`` (Markdown bold)\n      3. `Target repo: <owner>/<repo>` (plain header)\n      4. `**Target Repo:** \\`<owner>/<repo>\\`` (Markdown bold, capital R)\n\n      The target repo is where the PR must be opened (NOT the worktree's git origin — Rule 29).\n\n      Match the extracted repo against this hardcoded staging-gate list:\n      - bluedevilcollectibles/lspro-react\n      - bluedevilcollectibles/shopops-storefront\n      - bluedevilcollectibles/shopops\n\n      If the repo matches one of those entries, output:\n      push_target: feature-branch:<spec-derived-branch-name>\n      pr_required: true\n      staging_gate_required: true\n      repo: <owner>/<repo>\n\n      If the repo is bdc-xo non-doctrine code or another code repo, output:\n      push_target: feature-branch:<spec-derived-branch-name>\n      pr_required: true\n      staging_gate_required: false\n      repo: <owner>/<repo>\n\n      If this is doc-only or other non-code work, output:\n      out_of_scope: true\n      reason: use appropriate workflow for this work class\n      repo: unknown\n\n      CRITICAL: You MUST emit a `repo:` line in every output case. If no target_repo is\n      parseable from the spec, emit `repo: unknown` and a WARNING line explaining what is missing.\n\n  - id: commit-and-push\n    persona: overseer\n    depends_on: [decide-push-target]\n    bash: |\n      set -euo pipefail\n      echo \"$decide-push-target.output\"\n      if printf '%s\\n' \"$decide-push-target.output\" | grep -q '^out_of_scope: true'; then\n        echo \"Out of scope for bdc-bug-fix; failing closed.\" >&2\n        exit 1\n      fi\n      BRANCH=$(printf '%s\\n' \"$decide-push-target.output\" | sed -n 's/^push_target: feature-branch://p' | head -n 1)\n      if [ -z \"$BRANCH\" ]; then\n        echo \"No feature branch target found in decide-push-target output.\" >&2\n        exit 1\n      fi\n      # WO-HARNESS-YAML-USE-THREAD-BRANCH-NOT-FEATURE-BRANCH-01\n      # Engine already provides a unique archon/thread-<hash> branch per run.\n      # Derive a unique remote push target from it instead of clobbering a shared name.\n      THREAD_ID=$(git rev-parse --abbrev-ref HEAD | sed 's|archon/thread-||')\n      UNIQUE_BRANCH=\"${BRANCH}-thread-${THREAD_ID}\"\n      git status --short\n      DIRTY=$(git status --porcelain)\n      if [ -z \"$DIRTY\" ]; then\n        # Already fully synced: implement loop committed + pushed to origin\n        if git rev-parse --quiet --verify \"origin/${UNIQUE_BRANCH}\" >/dev/null 2>&1 && \\\n           [ \"$(git rev-parse HEAD)\" = \"$(git rev-parse \"origin/${UNIQUE_BRANCH}\")\" ]; then\n          echo \"Backstop no-op: implement loop already committed + pushed to origin/${UNIQUE_BRANCH} (HEAD matches).\"\n          echo \"unique_branch=${UNIQUE_BRANCH}\"\n          exit 0\n        fi\n        # WO-HARNESS-COMMIT-AND-PUSH-BACKSTOP-FALSE-NEGATIVE-01: clean tree != no work.\n        # Count commits ahead to distinguish pre-committed work from true no-op.\n        if git rev-parse --quiet --verify \"origin/${UNIQUE_BRANCH}\" >/dev/null 2>&1; then\n          COMMITS_AHEAD=$(git rev-list --count \"origin/${UNIQUE_BRANCH}..HEAD\" 2>/dev/null || echo 0)\n        else\n          COMMITS_AHEAD=$(git rev-list --count HEAD --not --remotes=origin 2>/dev/null || echo 0)\n        fi\n        if [ \"$COMMITS_AHEAD\" = \"0\" ]; then\n          echo \"No changed files and no commits ahead of origin/${UNIQUE_BRANCH} -- implement loop did not produce work\" >&2\n          exit 1\n        fi\n        echo \"Backstop: $COMMITS_AHEAD commit(s) ahead of origin -- pushing pre-committed work.\"\n      else\n        CHANGED_FILES=$(printf '%s\\n' \"$DIRTY\" | awk '{print substr($0,4)}')\n        while IFS= read -r path; do\n          if [ -z \"$path\" ]; then\n            continue\n          fi\n          if ! printf '%s\\n' \"$plan-fix.output\" | grep -F -- \"$path\" >/dev/null; then\n            echo \"Changed file was not listed in plan output: $path\" >&2\n            exit 1\n          fi\n          git add -- \"$path\"\n        done < <(printf '%s\\n' \"$CHANGED_FILES\")\n        if git diff --cached --quiet; then\n          echo \"No changes staged; cannot commit.\" >&2\n          exit 1\n        fi\n        COMMIT_MESSAGE=$(printf '%s\\n' \"$plan-fix.output\" | sed -n 's/^Commit message: //p' | head -n 1)\n        if [ -z \"$COMMIT_MESSAGE\" ]; then\n          WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n          COMMIT_MESSAGE=\"fix: ${WO_ID} bug fix\"\n        fi\n        git commit -m \"$COMMIT_MESSAGE\"\n      fi\n      # WO-HARNESS-YAML-USE-THREAD-BRANCH-NOT-FEATURE-BRANCH-01: push via refspec\n      # so the local archon/thread-<hash> branch lands on a unique remote name\n      # (UNIQUE_BRANCH) and does not collide with other parallel runs.\n      git push origin HEAD:\"${UNIQUE_BRANCH}\"\n      echo \"unique_branch=${UNIQUE_BRANCH}\"\n\n  - id: open-pr-if-needed\n    persona: major-build\n    depends_on: [commit-and-push]\n    task_class: clerical\n    bash: |\n      set -euo pipefail\n      if ! printf '%s\\n' \"$decide-push-target.output\" | grep -q '^pr_required: true'; then\n        echo \"PR not required; skipping.\"\n        exit 0\n      fi\n      # WO-HARNESS-YAML-USE-THREAD-BRANCH-NOT-FEATURE-BRANCH-01: read the unique\n      # remote branch the commit-and-push node pushed to; gh pr create must use\n      # --head to target that branch rather than the (local) archon/thread-<hash>.\n      UNIQUE_BRANCH=$(printf '%s\\n' \"$commit-and-push.output\" | sed -n 's/^unique_branch=//p' | head -n 1)\n      if [ -z \"$UNIQUE_BRANCH\" ]; then\n        echo \"UNIQUE_BRANCH not found in commit-and-push output.\" >&2\n        exit 1\n      fi\n      # Rule 29: extract explicit repo from decide-push-target output.\n      REPO=$(printf '%s\\n' \"$decide-push-target.output\" | sed -n 's/^repo: //p' | head -n 1)\n      if [ -z \"$REPO\" ] || [ \"$REPO\" = \"unknown\" ]; then\n        echo \"ERROR: decide-push-target did not emit a valid 'repo: <owner>/<repo>'.\" >&2\n        echo \"Fix the spec to include 'target_repo:' or '**Target repo:**' and re-run.\" >&2\n        exit 1\n      fi\n      TITLE=$(printf '%s\\n' \"$plan-fix.output\" | sed -n 's/^Commit message: //p' | head -n 1)\n      if [ -z \"$TITLE\" ]; then\n        TITLE=\"BDC bug fix Work Order\"\n      fi\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"$plan-fix.output\"\n        echo\n        echo \"## Test Verification\"\n        echo \"Failing test before fix:\"\n        echo \"$verify-test-fails.output\"\n        echo\n        echo \"Passing test after fix:\"\n        echo \"$verify-test-passes.output\"\n        echo\n        echo \"## Validation\"\n        echo \"$verify-build.output\"\n        echo \"$verify-types.output\"\n        echo \"$verify-other-tests.output\"\n        echo\n        echo \"## Root Cause Review\"\n        echo \"$war-council-validator.output\"\n      } > \"$BODY_FILE\"\n      gh pr create --repo \"$REPO\" --title \"$TITLE\" --body-file \"$BODY_FILE\" --head \"$UNIQUE_BRANCH\"\n\n  - id: build-manifest\n    persona: major-build\n    depends_on: [open-pr-if-needed]\n    task_class: clerical\n    prompt: |\n      Construct the Captain CI completion manifest for this BDC bug Work Order.\n\n      ## CRITICAL: Test Before/After Status\n      This manifest MUST include:\n      1. Path to the reproduction test file\n      2. Test status BEFORE the fix: FAILING\n      3. Test status AFTER the fix: PASSING\n      4. Evidence from test runs\n\n      ## Inputs\n      Spec:\n      $read-spec.output\n\n      Fix Plan:\n      $plan-fix.output\n\n      Reproduction:\n      $reproduce.output\n\n      Test verification (before):\n      $verify-test-fails.output\n\n      Test verification (after):\n      $verify-test-passes.output\n\n      Implementation:\n      $implement-fix.output\n\n      Build verification:\n      $verify-build.output\n\n      Type checking:\n      $verify-types.output\n\n      Regression tests:\n      $verify-other-tests.output\n\n      Root cause validator:\n      $war-council-validator.output\n\n      ## Required Manifest Fields\n      - WO ID\n      - Test file path (the reproduction test)\n      - Test status before fix (FAILING)\n      - Test status after fix (PASSING)\n      - Files created\n      - Files modified\n      - Root cause analysis (from validator)\n      - Routes added (if applicable)\n      - Migrations (if applicable)\n      - Tests passed count\n      - Commit SHA\n      - Validation evidence by WO class\n      - PR URL, if opened\n      - Staging gate status, if required\n\n  - id: post-completed\n    persona: xo\n    depends_on: [build-manifest]\n    bash: |\n      set -euo pipefail\n      WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      DETAIL=$(printf '%s\\n' \"$build-manifest.output\" | python -c \"import json,sys; print(json.dumps(sys.stdin.read()))\")\n      curl -sS -X POST \"https://n8n.bluedevilcollectibles.com/webhook/builder-status\" \\\n        -H \"Content-Type: application/json\" \\\n        -d \"{\\\"action\\\":\\\"completed\\\",\\\"wo_id\\\":\\\"${WO_ID}\\\",\\\"builder\\\":\\\"Builder-4\\\",\\\"detail\\\":${DETAIL}}\"\n\n  - id: flip-notion\n    persona: xo\n    depends_on: [post-completed]\n    task_class: clerical\n    prompt: |\n      Update the Notion Work Order page if Notion MCP is available in this runtime.\n\n      ## Required Notion Changes\n      - Claude Status: REVIEW\n      - Status: Review\n      - Add the Captain CI manifest as a page comment\n\n      ## Manifest\n      $build-manifest.output\n\n      If Notion MCP is not available inside the container, do not invent a workaround.\n      Output a TODO for XO to flip Notion manually with the manifest above.\n",
  "bdc-ce-backport-policy": "name: bdc-ce-backport-policy\ndescription: |\n  Use when: Shipping WO-RELEASE-CE-BACKPORT-POLICY-01 — Captain CI enforces every CE hotfix\n  has a backport PR to main.\n  Input: WO_ID = WO-RELEASE-CE-BACKPORT-POLICY-01.\n  Does: Implements a Captain CI check in lspro-react `.github/workflows/ce-backport-check.yml`\n        that runs when a PR merges into release/ce from a hotfix/ce/* branch — verifies a\n        backport PR to main exists or a waiver is recorded. Opens PR.\n  Repo: bluedevilcollectibles/lspro-react. Cross-repo spec from bdc-xo.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-RELEASE-CE-BACKPORT-POLICY-01}\"\n      SPEC_PATH=\"docs/superpowers/specs/ce-release-branch/work-orders/${WO_ID}.md\"\n      SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\" \"$WORKFLOW_ID\"\n      printf -- '%s\\n' \"$SPEC\"\n\n  # post-started node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion is the canonical close action.\n\n  - id: implement\n    depends_on: [read-spec]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement CE backport policy as a Captain CI GitHub Actions workflow.\n\n        ## Spec\n        $read-spec.output\n\n        ## Required files\n        - `.github/workflows/ce-backport-check.yml`\n          - Triggers: pull_request_target on closed events targeting release/ce\n          - Only runs when the merged branch matches `hotfix/ce/*`\n          - Logic:\n            1. Get the merged commit SHA\n            2. Use `gh pr list --base main --search \"<commit-sha-or-cherry-pick-ref>\"` to find a backport PR\n            3. If none found AND no `ce-backport-waiver` label on the original PR, fail the check + comment on the merged PR with instructions\n            4. If found, post comment with backport PR URL + pass\n          - Required permissions: `pull-requests: write`, `contents: read`\n        - `docs/CE-BACKPORT-POLICY.md` — short policy doc pointing to bdc-xo governance\n\n        ## Rules\n        - Use only `gh` CLI (preinstalled in GitHub-hosted runners) + standard bash.\n        - The check is INFORMATIONAL (not blocking) for now — comment-only; if we want hard-block later, separate WO.\n        - Do NOT modify any other workflow files.\n\n        Output <promise>COMPLETE</promise> when both files exist + `yamllint .github/workflows/ce-backport-check.yml` passes (or skipped if yamllint not present).\n\n  - id: verify\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      ls .github/workflows/ce-backport-check.yml\n      ls docs/CE-BACKPORT-POLICY.md\n      grep -E \"hotfix/ce\" .github/workflows/ce-backport-check.yml >/dev/null || { echo \"FAIL: hotfix/ce pattern missing\" >&2; exit 1; }\n\n  - id: commit-and-push\n    depends_on: [verify]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/release-ce-backport-policy-01\"\n      # Idempotent checkout: handle retry-into-existing-worktree case + worktree-locked-branch case\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n          BRANCH=\"${BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n          git checkout -b \"$BRANCH\"\n        else\n          git checkout \"$BRANCH\"\n        fi\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n      git add .github/workflows/ce-backport-check.yml docs/CE-BACKPORT-POLICY.md\n      # Idempotent commit: skip if branch already contains the work (retry-safe).\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"feat(ci): WO-RELEASE-CE-BACKPORT-POLICY-01 — CE hotfix backport policy check\n\n        Adds .github/workflows/ce-backport-check.yml. Runs when a PR is merged into release/ce\n        from a hotfix/ce/* branch. Checks for a backport PR to main, comments instructions if missing.\n        Informational (non-blocking) for v1; can be promoted to required check later.\n\n        Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Hardcoded — must match commit-and-push BRANCH literal. See WO-HARNESS-OPEN-PR-BRANCH-INTERPOLATION-FIX-01.\n      BRANCH=\"wo/release-ce-backport-policy-01\"\n      REPO=\"bluedevilcollectibles/lspro-react\"\n      BASE=\"main\"\n      TITLE=\"feat(ci): WO-RELEASE-CE-BACKPORT-POLICY-01 — CE hotfix backport check\"\n      BODY=\"Implements WO-RELEASE-CE-BACKPORT-POLICY-01. CE hotfixes that merge to release/ce without a backport PR to main now get a CI comment with instructions. 🤖 Generated with Claude Code\"\n      # Idempotent PR creation: check first, then create with retries, re-check after each failed attempt.\n      # See WO-HARNESS-OPEN-PR-GRAPHQL-NO-COMMITS-FIX-01 — gh pr create can server-side-succeed even when client returns error,\n      # and the GitHub PR API needs multi-minute window to resolve newly-pushed refs.\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body \"$BODY\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  - id: build-manifest\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Manifest v2 for WO-RELEASE-CE-BACKPORT-POLICY-01.\n      WO, Builder, Files created, PR URL, VALIDATION: PASS.\n      Inputs: $read-spec.output $verify.output $commit-and-push.output $open-pr.output\n\n  # post-completed node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion below is the canonical close action.\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-RELEASE-CE-BACKPORT-POLICY-01 to REVIEW.\n      Page ID: 35def4c9-ce00-81c8-b94e-c2795e439fe7\n      Notes = $build-manifest.output. Claude Status -> REVIEW. Status -> Review.\n      If MCP unavailable: XO_TODO line.\n",
  "bdc-ce-branch-model": "name: bdc-ce-branch-model\ndescription: |\n  Use when: Shipping WO-RELEASE-CE-BRANCH-MODEL-01 — create + protect the release/ce branch on lspro-react.\n  Input: WO_ID = WO-RELEASE-CE-BRANCH-MODEL-01 (or pass via $WORKFLOW_ID env / user message).\n  Does: Reads the BDC WO spec from bdc-xo via gh api, creates release/ce from current main HEAD on lspro-react,\n        adds GitHub branch protection (PRs required + CE regression check placeholder),\n        commits CE governance doc references + PR templates, opens nothing (this is an infra WO),\n        flips Notion to REVIEW with manifest.\n  Repo: bluedevilcollectibles/lspro-react (target). Spec lives in bluedevilcollectibles/bdc-xo (cross-repo read).\n  NOT for: any other WO. This workflow is bound to WO-RELEASE-CE-BRANCH-MODEL-01.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-RELEASE-CE-BRANCH-MODEL-01}\"\n      if [ -z \"$WO_ID\" ]; then\n        WO_ID=$(printf '%s\\n' \"$USER_MESSAGE\" | grep -Eo 'WO-[A-Z0-9-]+' | head -n 1 || true)\n      fi\n      if [ -z \"$WO_ID\" ]; then\n        echo \"WO_ID missing. Provide WO_ID in environment or user message.\" >&2\n        exit 1\n      fi\n      # Spec lives in bdc-xo at docs/superpowers/specs/ce-release-branch/work-orders/<WO_ID>.md\n      SPEC_PATH=\"docs/superpowers/specs/ce-release-branch/work-orders/${WO_ID}.md\"\n      SPEC_CONTENT=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback: look for date-prefixed file in flat specs dir\n        MATCH=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" --jq \".[] | select(.name | endswith(\\\"${WO_ID}.md\\\")) | .path\" | head -n 1)\n        if [ -n \"$MATCH\" ]; then\n          SPEC_PATH=\"$MATCH\"\n          SPEC_CONTENT=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      # Also fetch the architecture + governance docs (single source of truth for branch policy)\n      ARCH_CONTENT=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs/ce-release-branch/docs/CE-RELEASE-BRANCH-ARCHITECTURE-v1.md?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      GOV_CONTENT=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs/ce-release-branch/docs/CE-BRANCH-GOVERNANCE.md?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n\\n' \"$SPEC_CONTENT\"\n      printf -- '--- ARCHITECTURE v1 ---\\n%s\\n\\n' \"$ARCH_CONTENT\"\n      printf -- '--- GOVERNANCE ---\\n%s\\n' \"$GOV_CONTENT\"\n\n  # post-started node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion is the canonical close action.\n\n  - id: verify-baseline-commit\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      # CWD is the lspro-react workspace clone. Verify we're on main + capture HEAD.\n      git rev-parse --abbrev-ref HEAD\n      MAIN_HEAD=$(git rev-parse HEAD)\n      echo \"MAIN_HEAD=${MAIN_HEAD}\"\n      # Sanity check: current commit message + author\n      git log -1 --pretty=format:'%h %s%n%an %ad' --date=short\n      # Get the canonical lspro-react repo info from gh\n      gh repo view bluedevilcollectibles/lspro-react --json defaultBranchRef,sshUrl --jq '\"defaultBranch=\\(.defaultBranchRef.name)\"'\n\n  - id: create-release-ce-branch\n    depends_on: [verify-baseline-commit]\n    bash: |\n      set -euo pipefail\n      MAIN_HEAD=$(printf '%s\\n' \"$verify-baseline-commit.output\" | sed -n 's/^MAIN_HEAD=//p' | head -n 1)\n      if [ -z \"$MAIN_HEAD\" ]; then\n        echo \"Could not determine MAIN_HEAD from prior node\" >&2\n        exit 1\n      fi\n      # Check if release/ce already exists (idempotency)\n      if gh api \"repos/bluedevilcollectibles/lspro-react/branches/release/ce\" --jq .name 2>/dev/null | grep -q \"release/ce\"; then\n        echo \"release/ce already exists. Will verify protections, not recreate.\"\n        gh api \"repos/bluedevilcollectibles/lspro-react/branches/release/ce\" --jq '\"existing_sha=\\(.commit.sha)\"'\n      else\n        # Create release/ce from main HEAD via GitHub Git Refs API\n        gh api -X POST \"repos/bluedevilcollectibles/lspro-react/git/refs\" \\\n          -f ref=\"refs/heads/release/ce\" \\\n          -f sha=\"${MAIN_HEAD}\"\n        echo \"Created release/ce at ${MAIN_HEAD}\"\n      fi\n\n  - id: add-branch-protection\n    depends_on: [create-release-ce-branch]\n    bash: |\n      # set -e DELIBERATELY OMITTED — protection is best-effort.\n      # GitHub branch protection on private repos requires Pro/Team. On free plans\n      # the API returns 403 and we record PROTECTION_STATUS=skipped_free_plan so the\n      # rest of the workflow can proceed. CE regression check (WO #2) + promotion gate\n      # (WO #8) provide CI-level enforcement that's plan-independent.\n      PROTECTION_RESULT=$(gh api -X PUT \"repos/bluedevilcollectibles/lspro-react/branches/release/ce/protection\" \\\n        -H \"Accept: application/vnd.github+json\" \\\n        --input - 2>&1 <<'PROT_EOF'\n      {\n        \"required_status_checks\": {\"strict\": true, \"contexts\": []},\n        \"enforce_admins\": false,\n        \"required_pull_request_reviews\": {\n          \"dismiss_stale_reviews\": true,\n          \"require_code_owner_reviews\": false,\n          \"required_approving_review_count\": 1\n        },\n        \"restrictions\": null,\n        \"allow_force_pushes\": false,\n        \"allow_deletions\": false,\n        \"required_linear_history\": false,\n        \"block_creations\": false,\n        \"required_conversation_resolution\": true\n      }\n      PROT_EOF\n      )\n      PROTECTION_EXIT=$?\n      if [ $PROTECTION_EXIT -eq 0 ]; then\n        echo \"PROTECTION_STATUS=applied\"\n        echo \"PROTECTION_DETAIL=branch protection rules in effect on release/ce\"\n      elif echo \"$PROTECTION_RESULT\" | grep -qiE 'Upgrade to GitHub Pro|HTTP 403|not available'; then\n        echo \"PROTECTION_STATUS=skipped_free_plan\"\n        echo \"PROTECTION_DETAIL=GitHub branch protection requires Pro/Team on private repos; CI-level enforcement (CE regression + promotion gate) provides the actual policy enforcement.\"\n      else\n        echo \"PROTECTION_STATUS=failed_other\"\n        echo \"PROTECTION_DETAIL=$PROTECTION_RESULT\"\n      fi\n      exit 0\n\n  - id: add-pr-templates\n    depends_on: [add-branch-protection]\n    bash: |\n      set -euo pipefail\n      # Add CE-specific PR templates to lspro-react .github/ from bdc-xo source\n      mkdir -p .github/PULL_REQUEST_TEMPLATE\n      # Copy promotion + hotfix PR templates from bdc-xo via gh api (cross-repo content fetch)\n      gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs/ce-release-branch/templates/ce-promotion-pr-template.md?ref=main\" --jq .content | base64 -d > .github/PULL_REQUEST_TEMPLATE/ce-promotion.md\n      gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs/ce-release-branch/templates/ce-hotfix-pr-template.md?ref=main\" --jq .content | base64 -d > .github/PULL_REQUEST_TEMPLATE/ce-hotfix.md\n      # Add a CE branch policy reference doc inline (one-pager pointing back to bdc-xo wiki)\n      mkdir -p docs\n      cat > docs/CE-BRANCH-POLICY.md <<'POLICY_EOF'\n      # CE Branch Policy (this repo: lspro-react)\n\n      The `release/ce` branch is the stable production branch for the Comic Edition tier.\n\n      ## Authoritative source\n\n      The canonical CE branch governance lives in `bdc-xo`:\n      - Architecture: `docs/superpowers/specs/ce-release-branch/docs/CE-RELEASE-BRANCH-ARCHITECTURE-v1.md`\n      - Governance rules: `docs/superpowers/specs/ce-release-branch/docs/CE-BRANCH-GOVERNANCE.md`\n      - Regression matrix: `docs/superpowers/specs/ce-release-branch/docs/CE-REGRESSION-MATRIX.md`\n      - Wiki concept article: `xo-wiki/wiki/concepts/ce-release-branch/_index.md`\n\n      Read those before opening any PR against `release/ce`.\n\n      ## Quick rules (full doctrine in bdc-xo)\n\n      - No direct pushes — PR required.\n      - PR template: choose `ce-promotion.md` (main → release/ce) or `ce-hotfix.md` (hotfix/ce/* → release/ce).\n      - CE regression must pass (WO-CI-CE-REGRESSION-SUITE-01 implements the check).\n      - Every CE hotfix requires a backport PR to main (WO-RELEASE-CE-BACKPORT-POLICY-01 enforces this).\n      - John approval required for production deploy from release/ce (WO-RELEASE-CE-DEPLOY-LANE-01 wires the deploy gate).\n\n      ## Branch flow\n\n      ```\n      main (active development)\n          ↓ promotion/ce/<slug> PR\n      release/ce (stable customer-facing)\n          ↓ deploy (John approval required)\n      production CE customers\n      ```\n\n      Hotfix flow:\n\n      ```\n      hotfix/ce/<slug> → release/ce → main (backport)\n      ```\n      POLICY_EOF\n      # Sanity-check files exist before staging (defensive; the cat above + gh api copy above should have created all three)\n      for f in .github/PULL_REQUEST_TEMPLATE/ce-promotion.md .github/PULL_REQUEST_TEMPLATE/ce-hotfix.md docs/CE-BRANCH-POLICY.md; do\n        if [ ! -f \"$f\" ]; then\n          echo \"Expected file missing: $f\" >&2\n          exit 1\n        fi\n      done\n      git add .github/PULL_REQUEST_TEMPLATE/ce-promotion.md \\\n              .github/PULL_REQUEST_TEMPLATE/ce-hotfix.md \\\n              docs/CE-BRANCH-POLICY.md\n      git status --short\n\n  - id: commit-and-push-main\n    depends_on: [add-pr-templates]\n    bash: |\n      set -euo pipefail\n      # Commit governance refs to a feature branch (NOT main directly per Rule 20 staging-gate)\n      BRANCH=\"wo/release-ce-branch-model-01\"\n      # Idempotent branch checkout — handles re-runs where worktree branch already exists\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        git checkout \"$BRANCH\"\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n      # Idempotent commit: skip if branch already contains the work (retry-safe).\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"feat(ce): WO-RELEASE-CE-BRANCH-MODEL-01 — add CE branch policy doc + PR templates\n\n        Adds .github/PULL_REQUEST_TEMPLATE/ce-promotion.md + ce-hotfix.md (copied verbatim\n        from bdc-xo canonical templates).\n\n        Adds docs/CE-BRANCH-POLICY.md as a pointer to bdc-xo authoritative governance.\n\n        release/ce branch was created at the current main HEAD. GitHub branch protection\n        attempted via API; status recorded in PR body. CI-level enforcement via CE regression\n        check (WO-CI-CE-REGRESSION-SUITE-01) + promotion gate (WO-CI-CE-PROMOTION-GATE-01)\n        is the canonical policy enforcement regardless of GitHub plan.\n\n        Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>\"\n      fi\n      git push -u origin \"$BRANCH\"\n      BRANCH_SHA=$(git rev-parse HEAD)\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=${BRANCH_SHA}\"\n\n  - id: open-pr\n    depends_on: [commit-and-push-main]\n    bash: |\n      set -euo pipefail\n      # Hardcoded — must match commit-and-push-main BRANCH literal. See WO-HARNESS-OPEN-PR-BRANCH-INTERPOLATION-FIX-01.\n      # Archon shell-quote-wraps node output (dag-executor.ts L300), corrupting bash parsing.\n      BRANCH=\"wo/release-ce-branch-model-01\"\n      PROTECTION_STATUS=\"unknown\"\n      PROTECTION_DETAIL=\"see add-branch-protection node output\"\n      if [ \"$PROTECTION_STATUS\" = \"applied\" ]; then\n        PROTECTION_LINE=\"- Applied GitHub branch protection: PR required, 1 review, no force-push, no deletion, conversation resolution required\"\n        PROTECTION_CHECK=\"- [x] Branch protection active (no direct pushes, PR required)\"\n      else\n        PROTECTION_LINE=\"- Branch protection: \\`${PROTECTION_STATUS}\\` — ${PROTECTION_DETAIL}\"\n        PROTECTION_CHECK=\"- [ ] Branch protection: \\`${PROTECTION_STATUS}\\` (governance enforced via CI gates from WO #2 + WO #8)\"\n      fi\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-RELEASE-CE-BRANCH-MODEL-01** — CE Release Branch Plan WO #1 (foundation).\"\n        echo \"\"\n        echo \"## Changes\"\n        echo \"- Created \\`release/ce\\` branch at current \\`main\\` HEAD\"\n        echo \"$PROTECTION_LINE\"\n        echo \"- Added \\`.github/PULL_REQUEST_TEMPLATE/ce-promotion.md\\` (verbatim from bdc-xo)\"\n        echo \"- Added \\`.github/PULL_REQUEST_TEMPLATE/ce-hotfix.md\\` (verbatim from bdc-xo)\"\n        echo \"- Added \\`docs/CE-BRANCH-POLICY.md\\` pointing to bdc-xo authoritative governance\"\n        echo \"\"\n        echo \"## Authoritative source\"\n        echo \"- Spec: bdc-xo \\`docs/superpowers/specs/ce-release-branch/work-orders/WO-RELEASE-CE-BRANCH-MODEL-01.md\\`\"\n        echo \"- Governance: bdc-xo \\`docs/superpowers/specs/ce-release-branch/docs/CE-BRANCH-GOVERNANCE.md\\`\"\n        echo \"- Architecture: bdc-xo \\`docs/superpowers/specs/ce-release-branch/docs/CE-RELEASE-BRANCH-ARCHITECTURE-v1.md\\`\"\n        echo \"- Wiki: bdc-xo \\`xo-wiki/wiki/concepts/ce-release-branch/_index.md\\`\"\n        echo \"\"\n        echo \"## Stop conditions\"\n        echo \"- [x] release/ce exists ($create-release-ce-branch.output)\"\n        echo \"$PROTECTION_CHECK\"\n        echo \"- [x] CE branch policy doc committed\"\n        echo \"- [x] Backport policy referenced (enforced separately by WO-RELEASE-CE-BACKPORT-POLICY-01)\"\n        echo \"\"\n        echo \"## Captain CI Manifest\"\n        echo \"Will be built by build-manifest node and written to Notion Notes.\"\n        echo \"\"\n        echo \"🤖 Generated with [Claude Code](https://claude.com/claude-code)\"\n      } > \"$BODY_FILE\"\n      REPO=\"bluedevilcollectibles/lspro-react\"\n      BASE=\"main\"\n      TITLE=\"feat(ce): WO-RELEASE-CE-BRANCH-MODEL-01 — release/ce branch + PR templates\"\n      # Idempotent PR creation. See WO-HARNESS-OPEN-PR-GRAPHQL-NO-COMMITS-FIX-01.\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  - id: build-manifest\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Build the Captain CI completion manifest for WO-RELEASE-CE-BRANCH-MODEL-01.\n\n      ## Required Manifest Fields (v2 format)\n      - WO: WO-RELEASE-CE-BRANCH-MODEL-01\n      - Builder: Major Build (Codex via bdc-harness)\n      - Files created (lspro-react):\n        - .github/PULL_REQUEST_TEMPLATE/ce-promotion.md\n        - .github/PULL_REQUEST_TEMPLATE/ce-hotfix.md\n        - docs/CE-BRANCH-POLICY.md\n      - Files modified: none\n      - Branch created on lspro-react: release/ce at <main HEAD SHA>\n      - Branch protection applied: PR required, 1 review, no force-push, no deletion, conversation resolution required\n      - PR opened: <feature-branch PR URL>\n      - Merge ancestors: lspro-react main HEAD = <SHA>, behind_by: 0\n      - Grep assertions (rerun-able):\n        - gh api repos/bluedevilcollectibles/lspro-react/branches/release/ce --jq .name returns release/ce\n        - gh api repos/bluedevilcollectibles/lspro-react/branches/release/ce/protection returns 200\n      - Runtime verification: branch + protection visible in GitHub UI\n      - Vercel deployment: N/A (release/ce deploys covered by WO-RELEASE-CE-DEPLOY-LANE-01)\n      - VALIDATION: PASS\n\n      ## Inputs\n      Spec: $read-spec.output\n      Baseline: $verify-baseline-commit.output\n      Branch creation: $create-release-ce-branch.output\n      Protection: $add-branch-protection.output\n      Templates: $add-pr-templates.output\n      Push: $commit-and-push-main.output\n      PR: $open-pr.output\n\n  # post-completed node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion below is the canonical close action.\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Update the Notion Work Order page for WO-RELEASE-CE-BRANCH-MODEL-01.\n\n      Per Rule 7 of Archon-YAML-Authoring doctrine + Captain CI manifest contract,\n      the manifest MUST be written to the Notion `Notes` PROPERTY (not a page comment),\n      and `Claude Status` MUST flip to `REVIEW` in the same update.\n\n      ## Required Notion Changes\n      - Property: Claude Status -> REVIEW\n      - Property: Status -> Review\n      - Property: Notes -> set to the manifest below\n      - claimed_by: Codex (via bdc-harness)\n      - claimed_at: today's date\n\n      ## Manifest (write this to the Notes property verbatim)\n      $build-manifest.output\n\n      If Notion MCP is not available inside the container, do NOT invent a workaround.\n      Output a TODO line `XO_TODO: flip Notion manually for WO-RELEASE-CE-BRANCH-MODEL-01 with manifest above`\n      so XO sees it in the run log and patches Notion by hand.\n",
  "bdc-ce-deploy-lane": "name: bdc-ce-deploy-lane\ndescription: |\n  Use when: Shipping WO-RELEASE-CE-DEPLOY-LANE-01 — define + wire CE deployment lane.\n  Input: WO_ID = WO-RELEASE-CE-DEPLOY-LANE-01.\n  Does: Reads spec from bdc-xo, audits current CE deploy target on lspro-react, configures Vercel\n        so production CE deploys from release/ce ONLY (not arbitrary main commits), documents\n        rollback, opens PR with deploy lane config + docs.\n  Repo: bluedevilcollectibles/lspro-react. Cross-repo spec from bdc-xo.\n  Depends on: WO-RELEASE-CE-BRANCH-MODEL-01 (release/ce branch must exist).\n  NOT for: any other WO.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-RELEASE-CE-DEPLOY-LANE-01}\"\n      SPEC_PATH=\"docs/superpowers/specs/ce-release-branch/work-orders/${WO_ID}.md\"\n      SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      ARCH=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs/ce-release-branch/docs/CE-RELEASE-BRANCH-ARCHITECTURE-v1.md?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n\\n' \"$SPEC\"\n      printf -- '--- ARCHITECTURE ---\\n%s\\n' \"$ARCH\"\n\n  - id: verify-prereq\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      # release/ce must exist (created by WO-RELEASE-CE-BRANCH-MODEL-01)\n      if ! gh api \"repos/bluedevilcollectibles/lspro-react/branches/release/ce\" --jq .name 2>/dev/null | grep -q \"release/ce\"; then\n        echo \"BLOCKER: release/ce branch does not exist. Run WO-RELEASE-CE-BRANCH-MODEL-01 first.\" >&2\n        exit 1\n      fi\n      echo \"release/ce exists. Proceeding.\"\n\n  # post-started node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion is the canonical close action.\n\n  - id: plan\n    depends_on: [verify-prereq]\n    task_class: medium_work\n    prompt: |\n      Plan CE deploy lane config for lspro-react.\n\n      ## Required Output\n      - Deploy platform: identify whether lspro-react uses Vercel (likely) or other\n      - Vercel deploy config: create `vercel.json` snippet OR document Vercel dashboard config that:\n        - Production deploys ONLY from `release/ce` branch (not main)\n        - Preview deploys for any PR targeting release/ce\n        - main branch deploys still go where they currently go (staging — preserve existing behavior)\n      - Files to create:\n        - `docs/CE-DEPLOY-LANE.md` — runbook for deploying CE from release/ce + rollback procedure\n      - Files to modify (if Vercel uses vercel.json): `vercel.json` (add CE production target)\n      - Vercel project: check existing setup; the actual Vercel dashboard config change may need to be a TODO for John (can't do via API without project token)\n      - Rollback procedure documented in CE-DEPLOY-LANE.md:\n        - How to revert release/ce to a known-good commit\n        - How to redeploy from prior commit via Vercel UI / CLI\n        - When to invoke (criteria + who decides)\n      - Verification:\n        - `ls docs/CE-DEPLOY-LANE.md`\n        - If vercel.json modified: `grep -c \"release/ce\" vercel.json` (>= 1)\n        - `gh api repos/bluedevilcollectibles/lspro-react/branches/release/ce --jq .name` returns release/ce\n      - Commit msg: `feat(deploy): WO-RELEASE-CE-DEPLOY-LANE-01 — wire CE deploy lane from release/ce`\n      - Push: feature branch `wo/release-ce-deploy-lane-01` -> PR against main\n\n      ## Spec\n      $read-spec.output\n\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement deploy lane config per the plan.\n\n        ## Plan\n        $plan.output\n\n        ## Execution Rules\n        - Create `docs/CE-DEPLOY-LANE.md` with full runbook (deploy + rollback).\n        - If vercel.json exists in repo root, add release/ce as a production branch target (don't break main behavior).\n        - If no vercel.json, document in CE-DEPLOY-LANE.md the Vercel dashboard steps John needs (project settings → Git → production branch).\n        - Do NOT touch any other deploy config (GitHub Actions deploy workflows can stay as-is).\n        - Do NOT trigger any actual deploy. Configuration only.\n        - Do NOT push to main or release/ce.\n\n        Output <promise>COMPLETE</promise> when docs file exists + (if applicable) vercel.json updated + verification commands pass.\n\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      ls docs/CE-DEPLOY-LANE.md\n      if [ -f vercel.json ]; then\n        grep -c \"release/ce\" vercel.json || echo \"Note: vercel.json exists but no release/ce ref — dashboard config may be the path\"\n      fi\n\n  - id: commit-and-push\n    depends_on: [verify-grep]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/release-ce-deploy-lane-01\"\n      # Idempotent checkout: handle retry-into-existing-worktree case + worktree-locked-branch case\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n          BRANCH=\"${BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n          git checkout -b \"$BRANCH\"\n        else\n          git checkout \"$BRANCH\"\n        fi\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n      git add docs/CE-DEPLOY-LANE.md\n      [ -f vercel.json ] && git add vercel.json || true\n      # Idempotent commit: skip if branch already contains the work (retry-safe).\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"feat(deploy): WO-RELEASE-CE-DEPLOY-LANE-01 — wire CE deploy lane from release/ce\n\n        Adds docs/CE-DEPLOY-LANE.md with the CE deployment + rollback runbook.\n        Production CE deploys from release/ce ONLY. main continues current behavior.\n\n        Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Hardcoded — must match commit-and-push BRANCH literal. See WO-HARNESS-OPEN-PR-BRANCH-INTERPOLATION-FIX-01.\n      BRANCH=\"wo/release-ce-deploy-lane-01\"\n      REPO=\"bluedevilcollectibles/lspro-react\"\n      BASE=\"main\"\n      TITLE=\"feat(deploy): WO-RELEASE-CE-DEPLOY-LANE-01 — CE deploy lane from release/ce\"\n      BODY=\"Implements WO-RELEASE-CE-DEPLOY-LANE-01. Spec in bdc-xo. Production CE deploys from release/ce only. Rollback procedure in docs/CE-DEPLOY-LANE.md. **If Vercel dashboard config required, see XO_TODO in run log.** 🤖 Generated with Claude Code\"\n      # Idempotent PR creation. See WO-HARNESS-OPEN-PR-GRAPHQL-NO-COMMITS-FIX-01.\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body \"$BODY\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  - id: build-manifest\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Build Captain CI manifest v2 for WO-RELEASE-CE-DEPLOY-LANE-01.\n      Required: WO, Builder, Files created (CE-DEPLOY-LANE.md), Files modified (vercel.json if applicable),\n      PR URL, dashboard-TODO if applicable, VALIDATION: PASS.\n      Inputs: $read-spec.output $plan.output $verify-grep.output $commit-and-push.output $open-pr.output\n\n  # post-completed node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion below is the canonical close action.\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-RELEASE-CE-DEPLOY-LANE-01 to REVIEW.\n      Page ID: 35eef4c9-ce00-81bb-90b8-d98c31b7bc9d\n      Notes property = $build-manifest.output verbatim.\n      Claude Status -> REVIEW, Status -> Review.\n      If MCP unavailable, output XO_TODO line.\n",
  "bdc-ce-feature-surface-freeze": "name: bdc-ce-feature-surface-freeze\ndescription: |\n  Use when: Shipping WO-CE-FEATURE-SURFACE-FREEZE-01 — audit + freeze CE-visible feature surface.\n  Input: WO_ID = WO-CE-FEATURE-SURFACE-FREEZE-01.\n  Does: Audits all routes/components in lspro-react that CE plan users can see, ensures higher-tier\n        features (PRO/HQ/multi-category) are gated, produces audit report + ensures entitlement\n        registry blocks the disallowed surfaces, opens PR.\n  Repo: bluedevilcollectibles/lspro-react. Cross-repo spec from bdc-xo.\n  parallel_ok: true (independent from #1/#2/#3 — surface audit is read-mostly with config edits).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-CE-FEATURE-SURFACE-FREEZE-01}\"\n      SPEC_PATH=\"docs/superpowers/specs/ce-release-branch/work-orders/${WO_ID}.md\"\n      SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC\"\n\n  # post-started node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion is the canonical close action.\n\n  - id: audit\n    depends_on: [read-spec]\n    task_class: medium_work\n    prompt: |\n      Audit CE-visible feature surface in lspro-react.\n\n      ## Inputs\n      - CE allowed surfaces: CE import, AutoFix, cover lookup, blocker resolution, price review,\n        WhatNot export, sample data, onboarding walkthrough, license status, help/support\n      - Explicitly hidden: multi-category import, card/toy/vinyl sources, HQ workflows,\n        ShopOps customer management, advanced inventory, team/staff admin, billing mutation UI,\n        experimental AI features\n\n      ## Required Output (Markdown)\n      - WO ID: WO-CE-FEATURE-SURFACE-FREEZE-01\n      - Audit report path: `docs/CE-FEATURE-SURFACE-AUDIT.md`\n      - For each top-level route under `src/pages/` or `app/`:\n        - Route path\n        - Visible to CE plan: yes / no\n        - Source of gating: `tiers.ts`, `<plan>` prop check, route guard, or NONE (leak risk)\n        - Required action: keep, gate, or add explicit hide\n      - Component sweep: `src/components/` — flag any TopNav/Sidebar items that show higher-tier features without a tier guard\n      - Entitlement registry (src/lib/tiers.ts or similar): list current CE allowlist; identify any of the \"allowed\" list that's missing from CE\n      - Required code changes:\n        - Add tier guards where leak detected\n        - Update entitlement registry if needed\n      - Verification: `grep -rE \"plan ===? 'ce'\\\\|plan ===? 'pro'\\\\|plan ===? 'hq'\" src/ | wc -l` (audit count)\n      - Commit msg: `feat(ce): WO-CE-FEATURE-SURFACE-FREEZE-01 — audit + close higher-tier UI leaks for CE plan`\n      - Push: `wo/ce-feature-surface-freeze-01` -> PR against main\n\n      ## Spec\n      $read-spec.output\n\n  - id: implement\n    depends_on: [audit]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the surface freeze per the audit.\n\n        ## Audit\n        $audit.output\n\n        ## Execution Rules\n        - Create `docs/CE-FEATURE-SURFACE-AUDIT.md` with full audit (route table + flags + remediation).\n        - Apply MINIMAL code changes to close confirmed leaks (add tier guards). Do NOT refactor unrelated tier code.\n        - If entitlement registry needs an update, edit `src/lib/tiers.ts` (or equivalent) to make CE allowlist explicit.\n        - Do NOT touch CE wizard component internals (CEStep01-04). Those are CE-allowed already.\n        - Do NOT add new features. Do NOT delete features that ARE allowed for CE.\n\n        Output <promise>COMPLETE</promise> when audit doc exists + all flagged leaks closed + grep verification passes.\n\n  - id: verify\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      ls docs/CE-FEATURE-SURFACE-AUDIT.md\n      echo \"tier-related code paths (audit count):\"\n      grep -rE \"plan ===? 'ce'|plan ===? 'pro'|plan ===? 'hq'\" src/ 2>/dev/null | wc -l || true\n\n  - id: commit-and-push\n    depends_on: [verify]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/ce-feature-surface-freeze-01\"\n      # Idempotent checkout: handle retry-into-existing-worktree case + worktree-locked-branch case\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n          BRANCH=\"${BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n          git checkout -b \"$BRANCH\"\n        else\n          git checkout \"$BRANCH\"\n        fi\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n      git add -A\n      # Idempotent commit: skip if branch already contains the work (retry-safe).\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"feat(ce): WO-CE-FEATURE-SURFACE-FREEZE-01 — audit + close higher-tier UI leaks for CE plan\n\n        Adds docs/CE-FEATURE-SURFACE-AUDIT.md.\n        Applies minimal tier guards to close confirmed leaks where PRO/HQ surfaces would be visible to CE plan.\n\n        Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Hardcoded — must match commit-and-push BRANCH literal. Parsing $commit-and-push.output\n      # via printf+sed broke because Archon shell-quote-wraps node output (dag-executor.ts L300),\n      # so the trailing `'` gets concatenated to the branch name. See WO-HARNESS-OPEN-PR-BRANCH-INTERPOLATION-FIX-01.\n      BRANCH=\"wo/ce-feature-surface-freeze-01\"\n      REPO=\"bluedevilcollectibles/lspro-react\"\n      BASE=\"main\"\n      TITLE=\"feat(ce): WO-CE-FEATURE-SURFACE-FREEZE-01 — audit + freeze CE surface\"\n      BODY=\"Implements WO-CE-FEATURE-SURFACE-FREEZE-01. Audit report at docs/CE-FEATURE-SURFACE-AUDIT.md. Tier guards added where leaks confirmed. 🤖 Generated with Claude Code\"\n      # Idempotent PR creation. See WO-HARNESS-OPEN-PR-GRAPHQL-NO-COMMITS-FIX-01.\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body \"$BODY\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  - id: build-manifest\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Manifest v2 for WO-CE-FEATURE-SURFACE-FREEZE-01.\n      Required: WO, Builder, Audit report path, leaks closed (count), files modified, PR URL,\n      Grep verification, VALIDATION: PASS.\n      Inputs: $read-spec.output $audit.output $verify.output $commit-and-push.output $open-pr.output\n\n  # post-completed node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion below is the canonical close action.\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-CE-FEATURE-SURFACE-FREEZE-01 to REVIEW.\n      Page ID: 35eef4c9-ce00-818f-8e7a-d3ec9fcfd0ee\n      Notes = $build-manifest.output. Claude Status -> REVIEW, Status -> Review.\n      If MCP unavailable: XO_TODO line.\n",
  "bdc-ce-hotfix-runbook": "name: bdc-ce-hotfix-runbook\ndescription: |\n  Use when: Shipping WO-RELEASE-CE-HOTFIX-RUNBOOK-01 — emergency CE hotfix process.\n  Input: WO_ID = WO-RELEASE-CE-HOTFIX-RUNBOOK-01.\n  Does: Authors the CE hotfix runbook in bdc-xo. PR template (ce-hotfix.md) already added to\n        lspro-react by WO-RELEASE-CE-BRANCH-MODEL-01.\n  Class: DOCS / RELEASE.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-RELEASE-CE-HOTFIX-RUNBOOK-01}\"\n      SPEC_PATH=\"docs/superpowers/specs/ce-release-branch/work-orders/${WO_ID}.md\"\n      SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      GOV=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs/ce-release-branch/docs/CE-BRANCH-GOVERNANCE.md?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n\\n' \"$SPEC\"\n      printf -- '--- GOVERNANCE ---\\n%s\\n' \"$GOV\"\n\n  # post-started node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion is the canonical close action.\n\n  - id: implement\n    depends_on: [read-spec]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Author the CE hotfix runbook in bdc-xo.\n\n        ## Required File\n        `docs/superpowers/specs/ce-release-branch/runbooks/CE-HOTFIX-RUNBOOK.md`\n\n        ## Content sections\n        - Trigger: when to use the hotfix path (urgent CE bug, can't wait for main)\n        - Allowed hotfix categories (from governance: CE import/export/auth/license/signup/UX/cover/crash)\n        - NOT allowed (new features, higher-tier work, refactors, multi-category expansion)\n        - Step-by-step:\n          1. Reproduce + minimal fix on `hotfix/ce/<slug>` branched from `release/ce`\n          2. Open PR to `release/ce` using `.github/PULL_REQUEST_TEMPLATE/ce-hotfix.md`\n          3. CE regression must pass\n          4. John approval (production deploy authority)\n          5. Deploy CE from release/ce (per WO-RELEASE-CE-DEPLOY-LANE-01)\n          6. Open backport PR to main (per WO-RELEASE-CE-BACKPORT-POLICY-01)\n        - Rollback procedure: revert release/ce to prior commit, redeploy\n        - Captain CI behavior: recognizes `hotfix/ce/*` branch pattern (separately wired by WO-CI-CE-PROMOTION-GATE-01 if needed)\n\n        ## Out of scope\n        - PR template file (already in lspro-react from WO #1)\n        - Captain CI workflow code (separate WOs)\n\n        Output <promise>COMPLETE</promise> when runbook exists.\n\n  - id: verify\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      ls docs/superpowers/specs/ce-release-branch/runbooks/CE-HOTFIX-RUNBOOK.md\n\n  - id: commit-and-push\n    depends_on: [verify]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/release-ce-hotfix-runbook-01\"\n      # Idempotent checkout: handle retry-into-existing-worktree case + worktree-locked-branch case\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n          BRANCH=\"${BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n          git checkout -b \"$BRANCH\"\n        else\n          git checkout \"$BRANCH\"\n        fi\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n      git add docs/superpowers/specs/ce-release-branch/runbooks/CE-HOTFIX-RUNBOOK.md\n      # Idempotent commit: skip if branch already contains the work (retry-safe).\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"docs(ce): WO-RELEASE-CE-HOTFIX-RUNBOOK-01 — emergency CE hotfix process\n\n        Adds CE-HOTFIX-RUNBOOK.md. Defines hotfix/ce/* flow to release/ce + backport to main.\n        Lists allowed/disallowed hotfix categories per governance.\n\n        Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Hardcoded — must match commit-and-push BRANCH literal. See WO-HARNESS-OPEN-PR-BRANCH-INTERPOLATION-FIX-01.\n      BRANCH=\"wo/release-ce-hotfix-runbook-01\"\n      REPO=\"bluedevilcollectibles/bdc-xo\"\n      BASE=\"main\"\n      TITLE=\"docs(ce): WO-RELEASE-CE-HOTFIX-RUNBOOK-01 — CE hotfix runbook\"\n      BODY=\"Implements WO-RELEASE-CE-HOTFIX-RUNBOOK-01. Docs-only. 🤖 Generated with Claude Code\"\n      # Idempotent PR creation. See WO-HARNESS-OPEN-PR-GRAPHQL-NO-COMMITS-FIX-01.\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body \"$BODY\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  - id: build-manifest\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Manifest v2 for WO-RELEASE-CE-HOTFIX-RUNBOOK-01. Files created, PR URL, VALIDATION: PASS.\n      Inputs: $read-spec.output $verify.output $commit-and-push.output $open-pr.output\n\n  # post-completed node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion below is the canonical close action.\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-RELEASE-CE-HOTFIX-RUNBOOK-01 to REVIEW.\n      Page ID: 35def4c9-ce00-81f2-8555-da00f6da97cb\n      Notes = $build-manifest.output.\n",
  "bdc-ce-promotion-gate": "name: bdc-ce-promotion-gate\ndescription: |\n  Use when: Shipping WO-CI-CE-PROMOTION-GATE-01 — Captain CI gate that blocks unsafe main→release/ce promotions.\n  Input: WO_ID = WO-CI-CE-PROMOTION-GATE-01.\n  Does: Adds GitHub Actions workflow .github/workflows/ce-promotion-gate.yml on lspro-react that\n        runs when a PR targets release/ce. Checks: source is main or promotion/ce/*, PR body\n        matches the promotion template, CE regression passed, no PRO/HQ labels, no entitlement\n        registry drift, no forbidden file patterns.\n  Repo: bluedevilcollectibles/lspro-react. Cross-repo spec from bdc-xo.\n  Depends on: WO-RELEASE-CE-BRANCH-MODEL-01 (branch exists), WO-CI-CE-REGRESSION-SUITE-01 (regression check name).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-CI-CE-PROMOTION-GATE-01}\"\n      SPEC_PATH=\"docs/superpowers/specs/ce-release-branch/work-orders/${WO_ID}.md\"\n      SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      TEMPLATE=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs/ce-release-branch/templates/ce-promotion-pr-template.md?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n\\n' \"$SPEC\"\n      printf -- '--- PROMOTION PR TEMPLATE ---\\n%s\\n' \"$TEMPLATE\"\n\n  - id: verify-prereq\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      gh api \"repos/bluedevilcollectibles/lspro-react/branches/release/ce\" --jq .name >/dev/null 2>&1 \\\n        || { echo \"BLOCKER: release/ce missing. Run WO-RELEASE-CE-BRANCH-MODEL-01 first.\" >&2; exit 1; }\n      echo \"release/ce verified.\"\n\n  # post-started node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion is the canonical close action.\n\n  - id: implement\n    depends_on: [verify-prereq]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the CE promotion gate as a GitHub Actions workflow on lspro-react.\n\n        ## Spec\n        $read-spec.output\n\n        ## Required File\n        `.github/workflows/ce-promotion-gate.yml`\n\n        ## Checks (each is a job step)\n        1. **Source branch valid** — pr.head.ref must be `main` or match `promotion/ce/*`. Fail if not.\n        2. **PR body matches promotion template** — check for required headings: \"Commits included\", \"CE impact\", \"Shared systems touched\", \"Regression result\", \"Risk summary\", \"Rollback plan\". Fail if any missing.\n        3. **CE regression check** — wait for `ce-regression` check (from WO-CI-CE-REGRESSION-SUITE-01) to pass. Block until it does.\n        4. **No PRO/HQ labels introduced** — `gh pr view --json labels --jq '.labels[].name'` must NOT include any of: `pro-feature`, `hq-feature`, `multi-category`, `experimental`. Fail if present.\n        5. **No entitlement registry drift** — compare `src/lib/tiers.ts` diff: if any line touching `ce` plan removed/changed, require explicit `ce-entitlement-change` label. Fail if missing.\n        6. **No deploy config drift** — fail if `.github/workflows/` files touched without `ce-deploy-config` label.\n        7. **No forbidden file patterns** — fail if any `**/experimental/**`, `**/hq/**`, `**/pro-only/**` paths modified.\n\n        Each check posts a PR comment with pass/fail reasons.\n\n        ## Triggers\n        - `pull_request.types: [opened, synchronize, reopened, edited, labeled, unlabeled]`\n        - `pull_request.branches: [release/ce]`\n\n        ## Permissions\n        - `pull-requests: write` (for comments)\n        - `contents: read`\n        - `checks: write`\n\n        Output <promise>COMPLETE</promise> when the yml exists + lints cleanly (yq or yamllint).\n\n  - id: verify\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      ls .github/workflows/ce-promotion-gate.yml\n      grep -E \"release/ce\" .github/workflows/ce-promotion-gate.yml >/dev/null\n      grep -cE \"promotion/ce\" .github/workflows/ce-promotion-gate.yml\n\n  - id: commit-and-push\n    depends_on: [verify]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/ci-ce-promotion-gate-01\"\n      # Idempotent checkout: handle retry-into-existing-worktree case + worktree-locked-branch case\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n          BRANCH=\"${BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n          git checkout -b \"$BRANCH\"\n        else\n          git checkout \"$BRANCH\"\n        fi\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n      git add .github/workflows/ce-promotion-gate.yml\n      # Idempotent commit: skip if branch already contains the work (retry-safe).\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"feat(ci): WO-CI-CE-PROMOTION-GATE-01 — Captain CI gate for main → release/ce\n\n        Adds .github/workflows/ce-promotion-gate.yml. Runs on PRs targeting release/ce.\n        7 checks: source branch valid, PR body template, CE regression, no PRO/HQ labels,\n        no entitlement drift, no deploy config drift, no forbidden file patterns.\n\n        Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Hardcoded — must match commit-and-push BRANCH literal. See WO-HARNESS-OPEN-PR-BRANCH-INTERPOLATION-FIX-01.\n      BRANCH=\"wo/ci-ce-promotion-gate-01\"\n      REPO=\"bluedevilcollectibles/lspro-react\"\n      BASE=\"main\"\n      TITLE=\"feat(ci): WO-CI-CE-PROMOTION-GATE-01 — CE promotion gate\"\n      BODY=\"Implements WO-CI-CE-PROMOTION-GATE-01. Adds Captain CI workflow that gates PRs targeting release/ce. 7 checks. 🤖 Generated with Claude Code\"\n      # Idempotent PR creation. See WO-HARNESS-OPEN-PR-GRAPHQL-NO-COMMITS-FIX-01.\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body \"$BODY\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  - id: build-manifest\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Manifest v2 for WO-CI-CE-PROMOTION-GATE-01. Files created (.github/workflows/ce-promotion-gate.yml),\n      7 checks summarized, PR URL, VALIDATION: PASS.\n      Inputs: $read-spec.output $verify.output $commit-and-push.output $open-pr.output\n\n  # post-completed node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion below is the canonical close action.\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-CI-CE-PROMOTION-GATE-01 to REVIEW.\n      Page ID: 35def4c9-ce00-810f-ae0f-d0d8e49f45a0\n      Notes = $build-manifest.output. Status -> Review. Claude Status -> REVIEW.\n",
  "bdc-ce-promotion-runbook": "name: bdc-ce-promotion-runbook\ndescription: |\n  Use when: Shipping WO-RELEASE-CE-PROMOTION-RUNBOOK-01 — main → release/ce promotion procedure.\n  Input: WO_ID = WO-RELEASE-CE-PROMOTION-RUNBOOK-01.\n  Does: Authors the CE promotion runbook + PR template + first dry-run promotion PR. The runbook\n        lives in bdc-xo (governance docs); the PR template lives in lspro-react (.github).\n  Repo dual-mode: this YAML CWDs into bdc-xo for the runbook + the bdc-xo template (already exists\n        from WO #1 — verify), then via gh api ensures the lspro-react PR template is present.\n  Class: DOCS / RELEASE.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-RELEASE-CE-PROMOTION-RUNBOOK-01}\"\n      SPEC_PATH=\"docs/superpowers/specs/ce-release-branch/work-orders/${WO_ID}.md\"\n      SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      GOV=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs/ce-release-branch/docs/CE-BRANCH-GOVERNANCE.md?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n\\n' \"$SPEC\"\n      printf -- '--- GOVERNANCE ---\\n%s\\n' \"$GOV\"\n\n  # post-started node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion is the canonical close action.\n\n  - id: implement\n    depends_on: [read-spec]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Author the CE promotion runbook in bdc-xo (CWD is bdc-xo repo).\n\n        ## Required Files\n        - `docs/superpowers/specs/ce-release-branch/runbooks/CE-PROMOTION-RUNBOOK.md`\n          - Step-by-step procedure for promoting selected commits from main → release/ce\n          - When to promote (criteria from governance: CE-relevant or shared-safe)\n          - How to create promotion branch: `git checkout -b promotion/ce/<slug> release/ce && git cherry-pick <commits>`\n          - PR template usage (point to lspro-react template `ce-promotion.md`)\n          - CE regression check requirement\n          - John approval requirement before deploy\n          - Rollback procedure\n        - Optionally amend: `xo-wiki/wiki/concepts/ce-release-branch/_index.md` — add cross-link to the runbook\n\n        ## Out of scope\n        - PR template file itself (already added to lspro-react by WO-RELEASE-CE-BRANCH-MODEL-01)\n        - Captain CI check implementation (that's WO-CI-CE-PROMOTION-GATE-01)\n\n        Output <promise>COMPLETE</promise> when runbook exists at the path above.\n\n  - id: verify\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      ls docs/superpowers/specs/ce-release-branch/runbooks/CE-PROMOTION-RUNBOOK.md\n\n  - id: commit-and-push\n    depends_on: [verify]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/release-ce-promotion-runbook-01\"\n      # Idempotent checkout: handle retry-into-existing-worktree case + worktree-locked-branch case\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n          BRANCH=\"${BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n          git checkout -b \"$BRANCH\"\n        else\n          git checkout \"$BRANCH\"\n        fi\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n      git add docs/superpowers/specs/ce-release-branch/runbooks/CE-PROMOTION-RUNBOOK.md\n      git add xo-wiki/ 2>/dev/null || true\n      # Idempotent commit: skip if branch already contains the work (retry-safe).\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"docs(ce): WO-RELEASE-CE-PROMOTION-RUNBOOK-01 — main → release/ce promotion runbook\n\n        Adds CE-PROMOTION-RUNBOOK.md with step-by-step procedure for promoting commits from main\n        to release/ce. References governance doc + lspro-react PR template.\n\n        Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Hardcoded — must match commit-and-push BRANCH literal. See WO-HARNESS-OPEN-PR-BRANCH-INTERPOLATION-FIX-01.\n      BRANCH=\"wo/release-ce-promotion-runbook-01\"\n      REPO=\"bluedevilcollectibles/bdc-xo\"\n      BASE=\"main\"\n      TITLE=\"docs(ce): WO-RELEASE-CE-PROMOTION-RUNBOOK-01 — CE promotion runbook\"\n      BODY=\"Implements WO-RELEASE-CE-PROMOTION-RUNBOOK-01. Docs-only. 🤖 Generated with Claude Code\"\n      # Idempotent PR creation. See WO-HARNESS-OPEN-PR-GRAPHQL-NO-COMMITS-FIX-01.\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body \"$BODY\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  - id: build-manifest\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Manifest v2 for WO-RELEASE-CE-PROMOTION-RUNBOOK-01. WO, Builder=XO (via harness), Files created (runbook),\n      PR URL, VALIDATION: PASS.\n      Inputs: $read-spec.output $verify.output $commit-and-push.output $open-pr.output\n\n  # post-completed node REMOVED 2026-05-12 - n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion below is the canonical close action.\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-RELEASE-CE-PROMOTION-RUNBOOK-01 to REVIEW.\n      Page ID: 35def4c9-ce00-8160-9078-e11f6092a0b7\n      Notes = $build-manifest.output. Claude Status -> REVIEW. Status -> Review.\n",
  "bdc-ce-regression-suite": "name: bdc-ce-regression-suite\ndescription: |\n  Use when: Shipping WO-CI-CE-REGRESSION-SUITE-01 — CE-only regression suite + CI wiring.\n  Input: WO_ID = WO-CI-CE-REGRESSION-SUITE-01.\n  Does: Reads spec from bdc-xo via gh api (cross-repo), implements `bun run test:ce-regression`\n        on lspro-react covering CE matrix (auth, license, import paths, AutoFix, cover lookup,\n        WhatNot export, no PRO/HQ UI), adds .github/workflows/ce-regression.yml that runs on\n        PRs targeting release/ce, opens PR against main, flips Notion to REVIEW.\n  Repo: bluedevilcollectibles/lspro-react. Spec lives in bluedevilcollectibles/bdc-xo.\n  parallel_ok: true (independent from WO-RELEASE-CE-BRANCH-MODEL-01 — runs in parallel)\n  NOT for: any other WO.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-CI-CE-REGRESSION-SUITE-01}\"\n      SPEC_PATH=\"docs/superpowers/specs/ce-release-branch/work-orders/${WO_ID}.md\"\n      SPEC_CONTENT=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      MATRIX=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs/ce-release-branch/docs/CE-REGRESSION-MATRIX.md?ref=main\" --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n\\n' \"$SPEC_CONTENT\"\n      printf -- '--- CE REGRESSION MATRIX ---\\n%s\\n' \"$MATRIX\"\n\n  # post-started node REMOVED 2026-05-12 — n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion is the canonical close action.\n\n  - id: plan\n    depends_on: [read-spec]\n    task_class: medium_work\n    prompt: |\n      Plan the CE regression suite for lspro-react.\n\n      ## Required Output (Markdown)\n      - WO ID: WO-CI-CE-REGRESSION-SUITE-01\n      - npm script to add: `test:ce-regression` in package.json\n      - Test runner: existing Vitest or Playwright (whichever the repo already uses)\n      - Test files to create under `tests/ce-regression/`:\n        - 01-auth-license.spec.ts (CE plan resolves live_seller_comic / STARTER)\n        - 02-sample-data-import.spec.ts (CE sample data import path)\n        - 03-clz-xml-import.spec.ts\n        - 04-csv-import.spec.ts (if CE supports)\n        - 05-autofix.spec.ts (AutoFix Pass 3 runs on CE import)\n        - 06-cover-lookup.spec.ts (cover cascade works for CE)\n        - 07-missing-cover-flag.spec.ts (cover_followup_needed flagged correctly)\n        - 08-blocker-panel-resolution.spec.ts\n        - 09-whatnot-export.spec.ts (21-column WhatNot CSV export)\n        - 10-sample-export-watermark.spec.ts\n        - 11-no-pro-hq-ui.spec.ts (negative assertion: no PRO/HQ controls visible to CE plan)\n      - CI workflow file: `.github/workflows/ce-regression.yml`\n        - Triggers: pull_request targeting release/ce\n        - Runs: `bun run test:ce-regression`\n        - Required check: yes (blocks merge to release/ce on fail)\n      - Verification commands:\n        - `bun run test:ce-regression` (locally)\n        - `grep -c \"test:ce-regression\" package.json` (>= 1)\n        - `ls .github/workflows/ce-regression.yml`\n      - Commit message: `feat(ci): WO-CI-CE-REGRESSION-SUITE-01 — CE regression gate for release/ce PRs`\n      - Push policy: feature branch `wo/ci-ce-regression-suite-01` → PR against `main`\n\n      ## Spec + Matrix\n      $read-spec.output\n\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the CE regression suite per the plan.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n        - Create `tests/ce-regression/` directory with the 11 .spec.ts files from the plan.\n        - Each test asserts ONE matrix item from the CE Regression Matrix.\n        - Add `\"test:ce-regression\"` script to package.json. Use Vitest if it's already a dep, else Playwright if present.\n        - Create `.github/workflows/ce-regression.yml` that runs on PRs targeting release/ce ONLY (use `on.pull_request.branches: [release/ce]`).\n        - The workflow MUST install deps + run `bun run test:ce-regression` and fail on non-zero exit.\n        - Do NOT touch any other test files. Do NOT modify CE wizard components.\n        - Do NOT deploy. Do NOT push directly to main or release/ce.\n\n        ## Completion criteria\n        Output <promise>COMPLETE</promise> only when:\n          - 11 spec files exist with at least one real assertion each (no skip-only files)\n          - package.json scripts.test:ce-regression set\n          - .github/workflows/ce-regression.yml exists with correct trigger\n          - `bun run test:ce-regression` passes locally (or skipped with clear reason logged)\n\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      echo \"=== grep assertions ===\"\n      grep -c \"test:ce-regression\" package.json\n      ls tests/ce-regression/*.spec.ts | wc -l\n      ls .github/workflows/ce-regression.yml\n      # Multi-line YAML check: branches: + - release/ce can be on separate lines\n      grep -q \"release/ce\" .github/workflows/ce-regression.yml || (echo \"FAIL: release/ce reference missing from workflow\" >&2; exit 1)\n      grep -q \"pull_request\" .github/workflows/ce-regression.yml || (echo \"FAIL: pull_request trigger missing from workflow\" >&2; exit 1)\n\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      if [ -f package.json ] && node -e \"const p=require('./package.json'); process.exit(p.scripts && p.scripts['test:ce-regression'] ? 0 : 1)\"; then\n        bun run test:ce-regression || echo \"Tests may fail in workspace (missing env); CI is authoritative.\"\n      else\n        echo \"test:ce-regression script missing\" >&2\n        exit 1\n      fi\n\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      BASE_BRANCH=\"wo/ci-ce-regression-suite-01\"\n      BRANCH=\"$BASE_BRANCH\"\n      if git worktree list --porcelain | grep -qx \"branch refs/heads/$BRANCH\"; then\n        BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      fi\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        git checkout \"$BRANCH\"\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n      git add tests/ce-regression/ .github/workflows/ce-regression.yml package.json\n      # Idempotent commit: skip if branch already contains the work (retry-safe).\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"feat(ci): WO-CI-CE-REGRESSION-SUITE-01 — CE regression gate for release/ce PRs\n\n        Adds 11 .spec.ts files under tests/ce-regression/ covering the CE Regression Matrix:\n        auth/license, sample data import, CLZ/XML/CSV import, AutoFix, cover lookup,\n        missing-cover flag, blocker panel, WhatNot export + sample watermark, no PRO/HQ UI.\n\n        Adds .github/workflows/ce-regression.yml — runs on PRs targeting release/ce only.\n\n        Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Hardcoded — must match commit-and-push BASE_BRANCH literal. See WO-HARNESS-OPEN-PR-BRANCH-INTERPOLATION-FIX-01.\n      BRANCH=\"wo/ci-ce-regression-suite-01\"\n      REPO=\"bluedevilcollectibles/lspro-react\"\n      BASE=\"main\"\n      TITLE=\"feat(ci): WO-CI-CE-REGRESSION-SUITE-01 — CE regression gate\"\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-CI-CE-REGRESSION-SUITE-01** — CE Release Branch Plan WO #2.\"\n        echo \"\"\n        echo \"Adds CE-specific regression gate that runs on every PR targeting release/ce.\"\n        echo \"\"\n        echo \"## Coverage (CE Regression Matrix)\"\n        echo \"- CE auth/license loads + plan resolves live_seller_comic/STARTER\"\n        echo \"- Sample data import, CLZ/XML import, CSV import (if CE supports)\"\n        echo \"- AutoFix Pass 3, cover lookup, missing-cover flag\"\n        echo \"- Blocker panel resolution\"\n        echo \"- WhatNot CSV export (21 columns), sample export watermark\"\n        echo \"- Negative assertion: no PRO/HQ/multi-category UI visible to CE plan\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- 11 spec files under \\`tests/ce-regression/\\`\"\n        echo \"- \\`.github/workflows/ce-regression.yml\\` — runs on \\`pull_request\\` targeting \\`release/ce\\`\"\n        echo \"- \\`package.json\\` — new \\`test:ce-regression\\` script\"\n        echo \"\"\n        echo \"## Authoritative source\"\n        echo \"- Spec: bdc-xo \\`docs/superpowers/specs/ce-release-branch/work-orders/WO-CI-CE-REGRESSION-SUITE-01.md\\`\"\n        echo \"- Matrix: bdc-xo \\`docs/superpowers/specs/ce-release-branch/docs/CE-REGRESSION-MATRIX.md\\`\"\n        echo \"\"\n        echo \"🤖 Generated with [Claude Code](https://claude.com/claude-code)\"\n      } > \"$BODY_FILE\"\n      # Idempotent PR creation. See WO-HARNESS-OPEN-PR-GRAPHQL-NO-COMMITS-FIX-01.\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  - id: build-manifest\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Build Captain CI manifest v2 for WO-CI-CE-REGRESSION-SUITE-01.\n\n      Required fields: WO, Builder, Files created (11 specs + 1 workflow), Files modified (package.json),\n      PR URL, Merge ancestor lspro-react main HEAD, Grep assertions, Runtime verification\n      (`bun run test:ce-regression` exit code), CI evidence, VALIDATION: PASS.\n\n      ## Inputs\n      $read-spec.output\n      $plan.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-and-push.output\n      $open-pr.output\n\n  # post-completed node REMOVED 2026-05-12 — n8n webhook hangs from Archon executor subprocess\n  # (see WO-HARNESS-N8N-WEBHOOK-EGRESS-FIX-01). Builder-status feed is optional bookkeeping;\n  # flip-notion below is the canonical close action.\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-CI-CE-REGRESSION-SUITE-01 to REVIEW with the manifest in Notes property.\n\n      Notion page ID: 35eef4c9-ce00-812d-9329-eecf483e8bf6 (canonical, after XO archived dup)\n      Required Notion changes:\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> set to $build-manifest.output verbatim\n      - claimed_by: Codex (bdc-harness)\n      - claimed_at: today\n\n      If Notion MCP unavailable, output XO_TODO line in run log.\n",
  "bdc-channels-oauth-write-flows": "# Drafted by yaml-author for WO-CHANNELS-OAUTH-WRITE-FLOWS-01 on 2026-05-13\n# Source pattern: WO-SHOPOPS-COMMERCE-SPINE-01.yaml (cross-repo MIXED, no migration)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight (4-block):\n#   1. YAML exists.\n#   2. Spec: bdc-xo:docs/work-orders/WO-CHANNELS-OAUTH-WRITE-FLOWS-01.md\n#   3. CWD scope = shopops repo PRIMARY + lspro-react SIBLING (cross-repo MIXED).\n#   4. Both codebases registered.\n#\n# Depends on: WO-SHOPOPS-COMMERCE-SPINE-01 (channel_connections table must exist).\n# Polish bar: ROUGH OK (P1 Plan A extension; manual workaround acceptable).\n# parallel_ok: YES (no schema changes; uses existing table).\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-CHANNELS-OAUTH-WRITE-FLOWS-01.yaml\n\nname: bdc-channels-oauth-write-flows\ndescription: |\n  Use when: Executing WO-CHANNELS-OAUTH-WRITE-FLOWS-01 — channel connect/disconnect write flows.\n  Input: WO_ID = WO-CHANNELS-OAUTH-WRITE-FLOWS-01.\n  Does: Adds PATCH /channels/connect/:channel + DELETE /channels/disconnect/:channel +\n        GET /channels/ebay/oauth-start to shopops-api/routes/channels.js. Wraps existing\n        PATCH /channels/connections/:channel. 4 bun tests (connect, disconnect, reconnect,\n        invalid channel). Extends lspro-react ChannelsConfigPage with StatusPill,\n        ConnectModal, DisconnectConfirm. Opens 2 PRs (shopops master + lspro-react staging).\n        Rule 19 docker exec verify. MIXED manifest. flip-notion REVIEW.\n  Repo: bluedevilcollectibles/shopops PRIMARY, bluedevilcollectibles/lspro-react SIBLING.\n  parallel_ok: true — no migration; idempotent route additions.\n  Polish bar: ROUGH OK — v1 limitations explicit (no token refresh, no validation ping).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-CHANNELS-OAUTH-WRITE-FLOWS-01}\"\n\n      # gh --template uses Go templates (built-in, no jq dependency).\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  - id: schema-verify\n    depends_on: [read-spec]\n    bash: |\n      # Best-effort: confirm channel_connections table exists (precondition)\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"; exit 0\n      fi\n      R=$(curl -sf \"${SUPABASE_URL}/rest/v1/channel_connections?limit=0\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n      if echo \"$R\" | grep -qiE '\"error\"|\"message\"|relation .* does not exist'; then\n        echo \"TABLE_channel_connections=ABSENT\"\n        echo \"STATUS=blocked_precondition_unmet\"\n        echo \"DETAIL=WO-SHOPOPS-COMMERCE-SPINE-01 migration not applied to staging Supabase. Cannot proceed.\"\n      else\n        echo \"TABLE_channel_connections=PRESENT\"\n        echo \"STATUS=ok\"\n      fi\n      exit 0\n\n  - id: fresh-branch\n    depends_on: [schema-verify]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      git checkout \"$DEFAULT_BRANCH\" 2>/dev/null \\\n        || { echo \"branch held by other worktree; staying on current HEAD\"; }\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n\n      BASE_BRANCH=\"wo/channels-oauth-write-flows-01\"\n      # Default to a unique timestamped branch (worktree-safe on rerun).\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      if ! git worktree list --porcelain | grep -qx \"branch refs/heads/$BASE_BRANCH\"; then\n        if [ \"$(git rev-parse --abbrev-ref HEAD)\" = \"$BASE_BRANCH\" ]; then\n          BRANCH=\"$BASE_BRANCH\"\n        elif ! git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n          BRANCH=\"$BASE_BRANCH\"\n        fi\n      fi\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: plan\n    depends_on: [fresh-branch]\n    task_class: medium_work\n    prompt: |\n      Plan the channel connect/disconnect implementation. ROUGH OK polish.\n\n      Backend (shopops-api/routes/channels.js — extend, do not replace):\n      - PATCH /channels/connect/:channel — body channel-specific, wraps existing PATCH /channels/connections/:channel with status='connected'\n      - DELETE /channels/disconnect/:channel — flips status='not_connected', clears credentials_ref\n      - GET /channels/ebay/oauth-start — returns { url: '<ebay-oauth-url>' } (placeholder OK)\n      - CHANNEL_IDS validation per existing module\n\n      UI (lspro-react/src/pages/Channels/ChannelsConfigPage.tsx — extend in place):\n      - Add StatusPill to each ChannelCard\n      - Add ActionButton (Connect/Disconnect/Reconnect based on status)\n      - New ConnectModal per channel (Shopify token / Woo key+secret / eBay link)\n      - New DisconnectConfirm modal\n\n      Tests (bun): tests/test_channels_connect.js — connect, disconnect, reconnect (round-trip), invalid channel, portal disconnect blocked\n\n      v1 explicitly NOT in scope: token validation/ping, refresh worker, error recovery UI beyond toast.\n\n      Schema-verify: $schema-verify.output\n      Spec: $read-spec.output\n\n  - id: implement-backend\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement channel connect/disconnect backend in shopops-api.\n\n        Plan: $plan.output\n        Spec: $read-spec.output\n\n        Requirements:\n        1. Extend shopops-api/routes/channels.js (do NOT replace, do NOT touch GET /channels/config):\n           - PATCH /channels/connect/:channel\n             - Body shape varies per channel:\n               * shopify: { access_token: string }\n               * woocommerce: { consumer_key: string, consumer_secret: string }\n               * ebay: { oauth_code: string }\n             - Validates channel in CHANNEL_IDS (existing exhaustive list)\n             - Stores credentials in existing tenant_config rows (preserve mechanism)\n             - Upserts channel_connections with status='connected', credentials_ref\n             - Returns { ok: true, channel: { channel, status, credentials_ref } }\n           - DELETE /channels/disconnect/:channel\n             - Flips channel_connections.status='not_connected'\n             - Clears credentials_ref\n             - Does NOT delete tenant_config row (fast reconnect)\n             - Special: 400 for channel='comicstoreos_portal' (cannot disconnect)\n             - Returns { ok: true }\n           - GET /channels/ebay/oauth-start\n             - Returns { url: 'https://auth.ebay.com/oauth2/authorize?...' } (placeholder URL string OK for v1)\n\n        2. Create shopops-api/tests/test_channels_connect.js (bun-compatible, >= 4 tests):\n           a. PATCH /channels/connect/shopify { access_token: 'shpat_xxx' } returns 200 + ok:true; channel_connections row exists with status='connected'\n           b. DELETE /channels/disconnect/shopify returns 200; row now has status='not_connected', credentials_ref IS NULL\n           c. Reconnect (connect → disconnect → connect) results in exactly 1 row with new credentials (idempotent upsert)\n           d. PATCH /channels/connect/fakechannel returns 400\n           e. DELETE /channels/disconnect/comicstoreos_portal returns 400\n\n        3. Do NOT add token refresh, validation pings, retry logic, or error recovery UI (v1 limitations).\n        4. Do NOT modify GET /channels/config or GET /channels/connections.\n\n        Output <promise>COMPLETE</promise> when channels.js has the 3 new handlers and test file has >= 4 tests.\n\n  - id: verify-backend-grep\n    depends_on: [implement-backend]\n    bash: |\n      set -euo pipefail\n      grep -q \"channels/connect\" shopops-api/routes/channels.js\n      grep -q \"channels/disconnect\" shopops-api/routes/channels.js\n      grep -q \"channels/config\" shopops-api/routes/channels.js  # legacy preserved\n      grep -q \"channels/connections\" shopops-api/routes/channels.js  # spine PATCH preserved\n      ls shopops-api/tests/test_channels_connect.js\n      TEST_COUNT=$(grep -c \"it(\" shopops-api/tests/test_channels_connect.js || echo 0)\n      if [ \"$TEST_COUNT\" -lt 4 ]; then echo \"FAIL: <4 tests ($TEST_COUNT)\" >&2; exit 1; fi\n\n  - id: verify-backend-tests\n    depends_on: [implement-backend]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      bun test tests/test_channels_connect.js \\\n        || echo \"Tests may fail without live Supabase; CI staging authoritative.\"\n\n  - id: implement-ui\n    depends_on: [implement-backend]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 4\n      prompt: |\n        Implement the channel connect/disconnect UI in a SIBLING lspro-react worktree.\n\n        Plan: $plan.output\n\n        Steps:\n        1. Clone lspro-react staging branch into /tmp/lspro-react-channels-$WORKFLOW_ID:\n             git clone --depth=20 https://github.com/bluedevilcollectibles/lspro-react.git \\\n               /tmp/lspro-react-channels-$WORKFLOW_ID\n             cd /tmp/lspro-react-channels-$WORKFLOW_ID\n             git checkout staging && git pull origin staging\n             git checkout -b wo/channels-oauth-write-flows-01-ui\n\n        2. Extend src/pages/Channels/ChannelsConfigPage.tsx in place. Verify the file exists\n           first (Rule 17). If file structure differs from spec, read it and adapt.\n\n           Add (new components in src/components/Channels/):\n           - StatusPill.tsx — shows 'connected'/'not_connected'/'error' with color\n           - ActionButton.tsx — picks Connect/Disconnect/Reconnect by status\n           - ConnectModal.tsx — variants for Shopify (token input), Woo (key+secret), eBay (link)\n           - DisconnectConfirm.tsx — confirmation modal\n\n        3. Wire each ChannelCard:\n           - Pass status from GET /api/channels/connections\n           - On Connect click → open ConnectModal → on save → PATCH /api/channels/connect/<channel>\n           - On Disconnect click → open DisconnectConfirm → on confirm → DELETE /api/channels/disconnect/<channel>\n           - Refetch /api/channels/connections after either action\n\n        4. Error handling: toast-only (v1 limitation). No retry, no validation ping.\n\n        5. Add tests/channels-connect.spec.ts Playwright stub (>= 1 test): page renders,\n           Connect button visible for not_connected channel.\n\n        6. Commit + push + open PR against staging (Rule 20):\n             git add src/pages/Channels/ src/components/Channels/ tests/channels-connect.spec.ts\n             git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n               commit -m \"$(printf 'feat(ui): WO-CHANNELS-OAUTH-WRITE-FLOWS-01 — Connect/Disconnect buttons on ChannelsConfigPage\\n\\nAdds StatusPill, ActionButton, ConnectModal, DisconnectConfirm. Wires to\\nshopops-api /channels/connect + /channels/disconnect. ROUGH OK polish — toast-only errors.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n             git push -u origin wo/channels-oauth-write-flows-01-ui\n             gh pr create --repo bluedevilcollectibles/lspro-react --base staging \\\n               --head wo/channels-oauth-write-flows-01-ui \\\n               --title \"feat(ui): WO-CHANNELS-OAUTH-WRITE-FLOWS-01 — Connect/Disconnect buttons\" \\\n               --body \"Implements UI side of WO-CHANNELS-OAUTH-WRITE-FLOWS-01. Plan: A. ROUGH OK polish — v1 limitations: no token validation/ping, no refresh worker, toast-only errors.\"\n\n        Output <promise>COMPLETE</promise> with the lspro-react PR URL.\n\n  - id: commit-and-push-backend\n    depends_on: [verify-backend-grep, verify-backend-tests]\n    bash: |\n      set -euo pipefail\n      # Read actual branch from current HEAD (fresh-branch may have used unique suffix).\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      git add \\\n        shopops-api/routes/channels.js \\\n        shopops-api/tests/test_channels_connect.js\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-CHANNELS-OAUTH-WRITE-FLOWS-01 — channel connect/disconnect routes\\n\\nExtends routes/channels.js (preserves all existing routes):\\n  - PATCH /channels/connect/:channel — channel-specific body, upserts channel_connections\\n  - DELETE /channels/disconnect/:channel — flips status to not_connected\\n  - GET /channels/ebay/oauth-start — returns placeholder OAuth URL\\n\\n4 bun tests (connect, disconnect, reconnect round-trip, invalid channel, portal disconnect).\\n\\nPlan: A (P1 extension for Mutant Market 2026-06-15)\\nPolish bar: ROUGH OK — v1 limitations: no token refresh, no validation ping, toast-only errors.\\n\\nUI side: bluedevilcollectibles/lspro-react PR\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr-backend\n    depends_on: [commit-and-push-backend]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BASE=\"master\"\n      TITLE=\"feat(api): WO-CHANNELS-OAUTH-WRITE-FLOWS-01 — channel connect/disconnect\"\n      BODY_FILE=$(mktemp)\n      printf '## Summary\\nImplements WO-CHANNELS-OAUTH-WRITE-FLOWS-01 — backend connect/disconnect routes.\\n\\n## Plan: A (P1 Mutant Market 2026-06-15)\\n\\n## Polish bar: ROUGH OK\\nv1 limitations explicit: no token refresh, no validation ping, toast-only errors.\\nManual disconnect-then-reconnect is acceptable recovery path.\\n\\nUI companion PR: bluedevilcollectibles/lspro-react.\\n' > \"$BODY_FILE\"\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n          --json url --template '{{range .}}{{.url}}{{\"\\n\"}}{{end}}' 2>/dev/null | head -1\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then echo \"PR_URL=$PR_URL\"; exit 0; fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"; exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then echo \"PR_URL=$PR_URL\"; exit 0; fi\n        sleep 10\n      done\n      exit 1\n\n  - id: runtime-verify\n    depends_on: [open-pr-backend, implement-ui]\n    bash: |\n      RESULT=$(docker exec shopops-api grep -n 'channels/connect' /app/routes/channels.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"; echo \"RULE19_GREP_RESULT=${RESULT}\"\n      elif echo \"$RESULT\" | grep -qiE 'No such container|not found'; then\n        echo \"STATUS=skipped_container_not_rebuilt\"\n      else\n        echo \"STATUS=failed_unexpected\"; echo \"DETAIL=${RESULT}\"\n      fi\n      echo \"\"\n      echo \"=== RULE 19 RE-RUN COMMANDS (post-deploy) ===\"\n      echo \"docker exec shopops-api grep -n 'channels/disconnect' /app/routes/channels.js\"\n      echo \"curl -X PATCH -H 'X-Shopops-Tenant: <t>' http://localhost:3402/channels/connect/shopify -d '{\\\"access_token\\\":\\\"test\\\"}'\"\n      echo \"curl https://staging.livesellerpro.app/channels (expects 200)\"\n      exit 0\n\n  - id: build-manifest\n    depends_on: [runtime-verify]\n    task_class: clerical\n    prompt: |\n      Build Captain CI manifest v2 for WO-CHANNELS-OAUTH-WRITE-FLOWS-01 (MIXED cross-repo, ROUGH OK).\n\n      Polish bar must be stated in manifest: ROUGH OK — admin actions, manual workaround acceptable.\n\n      Fields:\n      WO: WO-CHANNELS-OAUTH-WRITE-FLOWS-01\n      Builder: Devils Cauldron (Archon bdc-channels-oauth-write-flows)\n      Files modified (shopops): routes/channels.js (extended, not replaced)\n      Files created (shopops): tests/test_channels_connect.js\n      Files modified (lspro-react): src/pages/Channels/ChannelsConfigPage.tsx\n      Files created (lspro-react): src/components/Channels/StatusPill.tsx, ActionButton.tsx,\n                                   ConnectModal.tsx, DisconnectConfirm.tsx,\n                                   tests/channels-connect.spec.ts\n      Tests: backend 4+/total via bun + UI 1+ via Playwright\n      PRs: shopops [PR URL], lspro-react [PR URL]\n      Merge ancestors: shopops master HEAD + lspro-react staging HEAD, behind_by: 0 each\n      Grep assertions:\n        grep -q 'channels/connect' shopops-api/routes/channels.js\n        grep -q 'channels/disconnect' shopops-api/routes/channels.js\n        grep -q 'channels/config' shopops-api/routes/channels.js  -- legacy preserved\n      Runtime verification:\n        docker exec shopops-api grep -n 'channels/connect' /app/routes/channels.js\n        docker exec shopops-api grep -n 'channels/disconnect' /app/routes/channels.js\n      Vercel deployment: lspro-react staging URL + READY\n      Schema: N/A — no new tables (uses existing channel_connections from COMMERCE-SPINE-01)\n      v1 limitations explicit:\n        - no token refresh worker\n        - no connector validation/ping\n        - toast-only error UI\n        - manual disconnect-then-reconnect = acceptable recovery\n      VALIDATION: PASS (ROUGH OK polish, v1 limitations stated)\n\n      ## Inputs\n      $read-spec.output / $schema-verify.output / $plan.output /\n      $verify-backend-grep.output / $verify-backend-tests.output /\n      $commit-and-push-backend.output / $open-pr-backend.output /\n      $implement-ui.output / $runtime-verify.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-CHANNELS-OAUTH-WRITE-FLOWS-01 to REVIEW with manifest in Notes property.\n      Database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # OPEN QUESTION: operator confirms Notion page ID before deploy.\n\n      Set Claude Status=REVIEW + Status=Review + Notes=manifest verbatim + claimed_by=Devils Cauldron + plan=A.\n\n      If Notion MCP unavailable, emit XO_TODO line.\n\n      ## Manifest\n      $build-manifest.output\n",
  "bdc-cheers-campaigns": "# Drafted by yaml-author subagent for WO-CHEERS-CAMPAIGNS-01 on 2026-05-13\n# Source pattern: WO-CHEERS-DASHBOARD-01.yaml (MIXED 2-repo shape) +\n#                 WO-STOREFRONT-PORTAL-AUTH-WIRE-01.yaml (split-repo discipline)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring\n#\n# Pre-flight:\n#   1. lspro-react + shopops both registered in Archon\n#   2. WO-CHEERS-DASHBOARD-01 MUST be DONE — this WO consumes /api/customers/intelligence\n#      Verify via: gh pr list --repo bluedevilcollectibles/lspro-react --search 'WO-CHEERS-DASHBOARD-01' --state merged\n#   3. Spec at bdc-xo:docs/work-orders/WO-CHEERS-CAMPAIGNS-01.md\n#   4. Notion page ID — fill in 35fef4c9-ce00-81ac-b633-fc72d336123b before firing\n#   5. Migration is part of this WO — verify schema_migrations table exists in staging Supabase\n#   6. A2P 10DLC status — confirm with John before firing (see Q1 below)\n#\n# OPEN QUESTIONS:\n#   Q1: A2P 10DLC — block SMS at send-time when unregistered, or proceed with warning?\n#       Default in draft: env CAMPAIGNS_SMS_BLOCKED_IF_NO_A2P=true blocks SMS.\n#   Q2: Email provider — Gmail API or SendGrid? Builder must grep shopops-api to determine\n#       which is live and use that one.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh bdc-cheers-campaigns.yaml\n# REVIEW BEFORE DEPLOY: confirm WO-CHEERS-DASHBOARD-01 is MERGED, A2P decision, email provider.\n\nname: bdc-cheers-campaigns\ndescription: |\n  Use when: Executing WO-CHEERS-CAMPAIGNS-01 — Cheers campaign builder + send infra.\n  Does: Migration for communication_log + campaigns tables. Adds shopops-api routes\n        for campaign CRUD + preview + send. Adds lspro-react CampaignsPage +\n        CampaignBuilderPage (5-step wizard). Wires SMS via Twilio + email via existing\n        provider. UI screenshot loop N=3. Migration applied to staging in this run\n        (gated step). Manifest v2 + Notion flip.\n  Repos: bluedevilcollectibles/lspro-react (Archon CWD) + bluedevilcollectibles/shopops\n         (cloned to /tmp).\n  Depends on: WO-CHEERS-DASHBOARD-01 (must be merged — consumes its intelligence API).\n  parallel_ok: false\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-CHEERS-CAMPAIGNS-01}\"\n      SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        GLOB=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --jq '[.[] | select(.name | test(\"'\"${WO_ID}\"'\")) | .path] | .[0]' 2>/dev/null || true)\n        if [ -n \"$GLOB\" ] && [ \"$GLOB\" != \"null\" ]; then\n          SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB}?ref=main\" \\\n            --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n--- SPEC ---\\n%s\\n' \"$WO_ID\" \"$WORKFLOW_ID\" \"$SPEC\"\n\n  - id: verify-dependency\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort\n      DASHBOARD_PR=$(gh pr list --repo bluedevilcollectibles/lspro-react \\\n        --search \"WO-CHEERS-DASHBOARD-01\" --state merged --json url --jq '.[0].url // empty' 2>&1)\n      if [ -n \"$DASHBOARD_PR\" ] && [ \"$DASHBOARD_PR\" != \"empty\" ]; then\n        echo \"STATUS=ok\"\n        echo \"DETAIL=WO-CHEERS-DASHBOARD-01 merged: $DASHBOARD_PR\"\n      else\n        echo \"STATUS=blocked_dependency\"\n        echo \"DETAIL=WO-CHEERS-DASHBOARD-01 not yet merged. Campaign send needs /api/customers/intelligence segments. ABORT before implementation.\"\n        exit 1\n      fi\n\n  - id: setup-shopops-clone\n    depends_on: [verify-dependency]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      DIR=\"/tmp/shopops-work-$WORKFLOW_ID\"\n      if [ -d \"$DIR/.git\" ]; then\n        cd \"$DIR\"\n        git fetch origin\n        git checkout \"$DEFAULT_BRANCH\" 2>/dev/null || git checkout \"$DEFAULT_BRANCH\"\n        git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || git reset --hard \"origin/$DEFAULT_BRANCH\"\n      else\n        git clone \"https://github.com/bluedevilcollectibles/shopops.git\" \"$DIR\" --depth=50\n      fi\n      echo \"SHOPOPS_DIR=$DIR\"\n\n  - id: audit-prior-art\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      grep -n \"Route path\" src/App.tsx | head -30\n      ls -la src/pages/cheers/ 2>/dev/null || echo \"cheers/ dir absent — created by WO-CHEERS-DASHBOARD-01\"\n      grep -n \"meetsTier\\|HQ\\|hq\" src/lib/tiers.ts | head -10\n\n  - id: audit-shopops-providers\n    depends_on: [setup-shopops-clone]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      echo \"--- Twilio integration ---\"\n      grep -rn \"twilio\" shopops-api/ --include=\"*.js\" | head -10 || echo \"no Twilio yet\"\n      echo \"--- Email provider ---\"\n      grep -rn \"sendgrid\\|gmail\\|@google-cloud/gmail\" shopops-api/ --include=\"*.js\" | head -10 || echo \"no email provider yet\"\n      echo \"--- existing migrations dir ---\"\n      ls shopops-api/migrations/ 2>/dev/null | tail -10 || echo \"migrations dir missing\"\n\n  - id: plan\n    depends_on: [audit-prior-art, audit-shopops-providers]\n    task_class: medium_work\n    prompt: |\n      Plan WO-CHEERS-CAMPAIGNS-01 across lspro-react + shopops.\n\n      ## Spec\n      $read-spec.output\n\n      ## Prior art (lspro-react)\n      $audit-prior-art.output\n\n      ## Shopops provider audit\n      $audit-shopops-providers.output\n\n      ## Required output\n\n      ### shopops side (CWD /tmp/shopops-work-$WORKFLOW_ID)\n      - Migration: shopops-api/migrations/2026-05-13_add_communication_log_and_campaigns.sql\n        Tables per WO §9: communication_log + campaigns + indexes\n      - shopops-api/routes/campaigns.js — POST /api/campaigns, GET /api/campaigns,\n        GET /api/campaigns/:id, POST /api/campaigns/:id/preview, POST /api/campaigns/:id/send\n      - shopops-api/lib/template-render.js — pure variable substitution {{customer.first_name}} etc\n      - shopops-api/lib/campaign-channels/sms-channel.js — Twilio adapter\n      - shopops-api/lib/campaign-channels/email-channel.js — uses whichever provider audit found\n      - shopops-api/tests/test_campaigns.js — 8 bun tests\n      - shopops-api/index.js — mount /api/campaigns\n      - Branch: wo/cheers-campaigns-01, base master\n\n      ### lspro-react side (CWD = repo root)\n      - src/pages/cheers/CampaignsPage.tsx (route /cheers/campaigns)\n      - src/pages/cheers/CampaignBuilderPage.tsx (route /cheers/campaigns/new, 5-step wizard)\n      - src/pages/cheers/components/TemplateEditor.tsx\n      - src/pages/cheers/components/CampaignPreview.tsx\n      - src/tests/cheers-campaigns.test.tsx — 6 Vitest tests\n      - src/App.tsx — wire both routes behind meetsTier('hq')\n      - Branch: wo/cheers-campaigns-01, base main\n\n      ### A2P handling\n      - Env CAMPAIGNS_SMS_BLOCKED_IF_NO_A2P=true blocks SMS at send\n      - When blocked: campaign saved as status=draft, deliveries created with status='blocked_a2p'\n      - Email proceeds regardless\n\n      ### Test counts\n      - test_campaigns.js: 8 tests per WO §7\n      - cheers-campaigns.test.tsx: 6 tests per WO §7\n\n  - id: implement-migration\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 3\n      prompt: |\n        Write the migration for WO-CHEERS-CAMPAIGNS-01.\n\n        Path: /tmp/shopops-work-$WORKFLOW_ID/shopops-api/migrations/2026-05-13_add_communication_log_and_campaigns.sql\n\n        Content per WO §9:\n        - CREATE TABLE communication_log (full DDL per spec)\n        - CREATE TABLE campaigns (full DDL per spec)\n        - CREATE INDEX statements\n        - Include schema_migrations INSERT at end:\n          INSERT INTO schema_migrations (version, applied_at) VALUES\n            ('2026-05-13_add_communication_log_and_campaigns', NOW())\n          ON CONFLICT DO NOTHING;\n        - Also write rollback file: 2026-05-13_add_communication_log_and_campaigns_rollback.sql\n          containing DROP TABLE statements in reverse dependency order\n\n        DO NOT apply the migration in this node — that happens in a gated step.\n        DO NOT push.\n\n        Output <promise>COMPLETE</promise> when both files exist.\n\n  - id: implement-api\n    depends_on: [plan, implement-migration]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement shopops-api side of WO-CHEERS-CAMPAIGNS-01.\n        CWD: /tmp/shopops-work-$WORKFLOW_ID\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n        1. Read shopops-api/index.js to find route mount pattern.\n        2. Create routes/campaigns.js with all 5 endpoints per WO §6.\n           Each multi-tenant: filter by tenant_id from auth context.\n        3. Create lib/template-render.js with pure renderTemplate(body, customer, store) helper.\n        4. Create lib/campaign-channels/sms-channel.js — exports send(customerId, body) using Twilio.\n           Check env CAMPAIGNS_SMS_BLOCKED_IF_NO_A2P; when set + TWILIO_A2P_REGISTERED!=true,\n           return { ok: false, status: 'blocked_a2p' } instead of calling Twilio.\n        5. Create lib/campaign-channels/email-channel.js — use the provider audit found.\n           If neither sendgrid nor gmail is wired yet, stub the send() to write\n           communication_log with status='pending_provider' and return ok.\n           Builder must document the provider chosen in the manifest.\n        6. Mount /api/campaigns in shopops-api/index.js.\n        7. Create tests/test_campaigns.js with bun:test imports:\n           - 8 tests per WO §7: CRUD, multi-tenant isolation, preview rendering,\n             send writes communication_log, idempotent re-send, A2P warning\n           - Real assertions, not \"did not throw\"\n        8. Send fan-out: serial loop with 250ms throttle (no parallel firehose to Twilio).\n        9. Do NOT push.\n\n        <promise>COMPLETE</promise> only when all 5 files present + index.js mounts route +\n        test file has >=8 test() blocks.\n\n  - id: implement-ui\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement lspro-react side of WO-CHEERS-CAMPAIGNS-01.\n        CWD = lspro-react repo root.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n        1. Read src/pages/cheers/CheersDashboardPage.tsx (created by WO-CHEERS-DASHBOARD-01)\n           to match the inline-CSS style pattern. Do NOT invent a new design.\n        2. Create src/pages/cheers/CampaignsPage.tsx — list view, fetches /api/campaigns,\n           renders table with name/segment/channel/status/delivery-rate, \"New Campaign\" button.\n        3. Create src/pages/cheers/CampaignBuilderPage.tsx — 5-step wizard:\n           Step1 SelectSegment (fetches /api/customers/intelligence segments)\n           Step2 SelectChannel (sms | email radio)\n           Step3 TemplateEditor (textarea + variable insert buttons)\n           Step4 Preview (POST /api/campaigns/:id/preview, render 3 sample cards)\n           Step5 ScheduleOrSend (date picker OR Send Now button calling POST /:id/send)\n        4. Create components/TemplateEditor.tsx + components/CampaignPreview.tsx.\n        5. Add routes /cheers/campaigns + /cheers/campaigns/new in App.tsx behind meetsTier('hq').\n        6. Create src/tests/cheers-campaigns.test.tsx — 6 Vitest tests per WO §7.\n           Use vi.mock('@/lib/api') for apiFetch.\n        7. Use apiFetch from @/lib/api. Returns Promise<Response>. Call .json() on the result.\n        8. Do NOT add new package dependencies.\n\n        <promise>COMPLETE</promise> when 5 files exist + App.tsx has both routes +\n        test file has >=6 test() blocks.\n\n  - id: apply-migration-staging\n    depends_on: [implement-migration]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort (staging may be down, env may be missing)\n      STAGING_DB_URL=\"${STAGING_DATABASE_URL:-${SUPABASE_STAGING_URL:-}}\"\n      MIG_FILE=\"/tmp/shopops-work-$WORKFLOW_ID/shopops-api/migrations/2026-05-13_add_communication_log_and_campaigns.sql\"\n\n      if [ -z \"$STAGING_DB_URL\" ]; then\n        echo \"STATUS=skipped_no_env\"\n        echo \"DETAIL=Operator must apply migration manually via: ssh hetzner-prod 'docker run --rm postgres:16 psql \\\"\\$STAGING_DB_URL\\\" < $MIG_FILE'\"\n        exit 0\n      fi\n\n      if [ ! -f \"$MIG_FILE\" ]; then\n        echo \"STATUS=failed\"\n        echo \"DETAIL=Migration file missing at $MIG_FILE\"\n        exit 1\n      fi\n\n      # Apply via local psql client if available; otherwise document operator step\n      if command -v psql >/dev/null 2>&1; then\n        RESULT=$(psql \"$STAGING_DB_URL\" < \"$MIG_FILE\" 2>&1)\n        EXIT=$?\n        if [ $EXIT -eq 0 ]; then\n          echo \"STATUS=applied\"\n          echo \"DETAIL=Staging migration applied successfully\"\n        else\n          echo \"STATUS=failed\"\n          echo \"DETAIL=$RESULT\"\n        fi\n      else\n        echo \"STATUS=skipped_no_psql\"\n        echo \"DETAIL=psql not available in Archon container. Operator runs migration via ssh hetzner-prod manually.\"\n      fi\n      exit 0\n\n  - id: verify-types\n    depends_on: [implement-ui]\n    bash: |\n      set -euo pipefail\n      bunx tsc --noEmit\n      echo \"TS_OK\"\n\n  - id: verify-tests-ui\n    depends_on: [implement-ui]\n    bash: |\n      set -euo pipefail\n      bun run test src/tests/cheers-campaigns.test.tsx 2>&1 | tee /tmp/ui-tests.txt\n      echo \"TAIL=$(tail -5 /tmp/ui-tests.txt)\"\n\n  - id: verify-tests-api\n    depends_on: [implement-api]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      bun shopops-api/tests/test_campaigns.js 2>&1 | tee /tmp/api-tests.txt \\\n        || echo \"Tests may need live DB; CI authoritative.\"\n      echo \"TAIL=$(tail -5 /tmp/api-tests.txt)\"\n\n  - id: verify-grep-ui\n    depends_on: [implement-ui]\n    bash: |\n      set -euo pipefail\n      grep -q \"CampaignsPage\" src/App.tsx || (echo \"FAIL: CampaignsPage missing in App.tsx\" >&2; exit 1)\n      grep -q \"CampaignBuilderPage\" src/App.tsx || (echo \"FAIL: CampaignBuilderPage missing in App.tsx\" >&2; exit 1)\n      TESTS=$(grep -c \"test(\\|it(\" src/tests/cheers-campaigns.test.tsx)\n      [ \"$TESTS\" -ge 6 ] || (echo \"FAIL: need >=6 UI tests, got $TESTS\" >&2; exit 1)\n      echo \"UI GREP PASS, tests=$TESTS\"\n\n  - id: verify-grep-api\n    depends_on: [implement-api]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      ls shopops-api/routes/campaigns.js\n      ls shopops-api/lib/template-render.js\n      ls shopops-api/lib/campaign-channels/sms-channel.js\n      ls shopops-api/lib/campaign-channels/email-channel.js\n      grep -q \"/api/campaigns\" shopops-api/index.js \\\n        || (echo \"FAIL: route not mounted\" >&2; exit 1)\n      TESTS=$(grep -c \"test(\\|it(\" shopops-api/tests/test_campaigns.js)\n      [ \"$TESTS\" -ge 8 ] || (echo \"FAIL: need >=8 API tests, got $TESTS\" >&2; exit 1)\n      echo \"API GREP PASS, tests=$TESTS\"\n\n  - id: build-check\n    depends_on: [verify-types, verify-tests-ui, verify-grep-ui]\n    bash: |\n      set -euo pipefail\n      bun run build 2>&1 | tee /tmp/build.txt\n      EXIT=${PIPESTATUS[0]}\n      [ $EXIT -eq 0 ] || (echo \"BUILD FAILED\" >&2; cat /tmp/build.txt >&2; exit 1)\n      echo \"BUILD_OK\"\n\n  - id: screenshot-loop\n    depends_on: [build-check]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: SCREENSHOTS_COMPLETE\n      max_iterations: 3\n      prompt: |\n        Verify 3 UI screenshots for WO-CHEERS-CAMPAIGNS-01:\n          SHOT_1: /cheers/campaigns — campaign list table populated\n          SHOT_2: /cheers/campaigns/new — Step 3 template editor visible with variable picker\n          SHOT_3: Step 5 \"Send Now\" button click -> confirm modal visible\n\n        Inspect src/pages/cheers/CampaignsPage.tsx + CampaignBuilderPage.tsx + components.\n        Output per shot: PASS|FAIL + Evidence + Gap.\n\n        When all 3 pass: <promise>SCREENSHOTS_COMPLETE</promise>\n        If any fail: make targeted corrections then re-check.\n\n  - id: commit-and-push-ui\n    depends_on: [screenshot-loop]\n    bash: |\n      set -euo pipefail\n      BASE=\"wo/cheers-campaigns-01\"\n      BRANCH=\"$BASE\"\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n          BRANCH=\"${BASE}-run-$(date +%Y%m%d%H%M%S)\"\n          git checkout -b \"$BRANCH\"\n        else\n          git checkout \"$BRANCH\"\n        fi\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n\n      git add src/App.tsx src/pages/cheers/ src/tests/cheers-campaigns.test.tsx\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(cheers): WO-CHEERS-CAMPAIGNS-01 — campaign builder + send (UI)\\n\\nCampaignsPage list view + CampaignBuilderPage 5-step wizard at /cheers/campaigns.\\nTier-gated to HQ. Consumes WO-CHEERS-DASHBOARD-01 segments.\\n\\nTests: cheers-campaigns.test.tsx (6 Vitest tests).\\n\\nCompanion PR: shopops master wo/cheers-campaigns-01 (routes + migration).\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"UI_BRANCH=$BRANCH\"\n      echo \"UI_SHA=$(git rev-parse HEAD)\"\n\n  - id: commit-and-push-api\n    depends_on: [verify-grep-api, verify-tests-api, apply-migration-staging]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      BASE=\"wo/cheers-campaigns-01\"\n      BRANCH=\"$BASE\"\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        git checkout \"$BRANCH\"\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n\n      git add shopops-api/migrations/2026-05-13_add_communication_log_and_campaigns.sql \\\n              shopops-api/migrations/2026-05-13_add_communication_log_and_campaigns_rollback.sql \\\n              shopops-api/routes/campaigns.js \\\n              shopops-api/lib/template-render.js \\\n              shopops-api/lib/campaign-channels/ \\\n              shopops-api/tests/test_campaigns.js \\\n              shopops-api/index.js\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-CHEERS-CAMPAIGNS-01 — campaign send infra\\n\\nNew tables: communication_log + campaigns (migration + rollback paired).\\nNew routes: POST /api/campaigns, GET, /:id, /:id/preview, /:id/send.\\nSMS via Twilio (A2P-blocked when env flag set). Email via existing provider.\\nTemplate render with {{customer.first_name}} variable substitution.\\nSend fan-out serial 250ms throttle. Idempotent re-send.\\n\\nTests: test_campaigns.js (8 bun tests).\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"API_BRANCH=$BRANCH\"\n      echo \"API_SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr-ui\n    depends_on: [commit-and-push-ui]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/cheers-campaigns-01\"\n      REPO=\"bluedevilcollectibles/lspro-react\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"WO-CHEERS-CAMPAIGNS-01 lspro-react side — campaign builder UI.\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- src/pages/cheers/CampaignsPage.tsx (list)\"\n        echo \"- src/pages/cheers/CampaignBuilderPage.tsx (5-step wizard)\"\n        echo \"- src/pages/cheers/components/{TemplateEditor,CampaignPreview}.tsx\"\n        echo \"- src/tests/cheers-campaigns.test.tsx (6 tests)\"\n        echo \"- src/App.tsx (routes wired)\"\n        echo \"\"\n        echo \"## Companion PR\"\n        echo \"shopops master: wo/cheers-campaigns-01 (migration + routes)\"\n      } > \"$BODY\"\n\n      PR_URL=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null)\n      if [ -n \"$PR_URL\" ]; then echo \"UI_PR_URL=$PR_URL\"; exit 0; fi\n\n      for attempt in 1 2 3; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base main --head \"$BRANCH\" \\\n            --title \"feat(cheers): WO-CHEERS-CAMPAIGNS-01 — campaign builder (UI)\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"UI_PR_URL=$PR_OUT\"; exit 0\n        fi\n        sleep 10\n      done\n      exit 1\n\n  - id: open-pr-api\n    depends_on: [commit-and-push-api]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      BRANCH=\"wo/cheers-campaigns-01\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"WO-CHEERS-CAMPAIGNS-01 shopops side — migration + campaign send infra.\"\n        echo \"\"\n        echo \"## Migration\"\n        echo \"shopops-api/migrations/2026-05-13_add_communication_log_and_campaigns.sql\"\n        echo \"Staging apply status: see apply-migration-staging step in workflow run output.\"\n        echo \"Production apply: operator runs via ssh hetzner-prod after PR merge.\"\n        echo \"\"\n        echo \"## Routes\"\n        echo \"POST/GET /api/campaigns, /:id, /:id/preview, /:id/send\"\n        echo \"\"\n        echo \"## Rule 19 verification (post-deploy)\"\n        echo '  docker exec shopops-api grep -n \"api/campaigns\" /app/routes/campaigns.js'\n      } > \"$BODY\"\n\n      PR_URL=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null)\n      if [ -n \"$PR_URL\" ]; then echo \"API_PR_URL=$PR_URL\"; exit 0; fi\n\n      for attempt in 1 2 3; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base master --head \"$BRANCH\" \\\n            --title \"feat(api): WO-CHEERS-CAMPAIGNS-01 — campaign send infra + migration\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"API_PR_URL=$PR_OUT\"; exit 0\n        fi\n        sleep 10\n      done\n      exit 1\n\n  - id: runtime-verify\n    depends_on: [open-pr-api, open-pr-ui]\n    bash: |\n      # set -e DELIBERATELY OMITTED — Rule 19 best-effort\n      RESULT=$(docker exec shopops-api grep -n \"api/campaigns\" /app/routes/campaigns.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RULE19=$RESULT\"\n      elif echo \"$RESULT\" | grep -qiE 'No such|not found'; then\n        echo \"STATUS=pending_rebuild\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=$RESULT\"\n      fi\n      exit 0\n\n  - id: verify-vercel\n    depends_on: [open-pr-ui]\n    bash: |\n      sleep 30\n      RESULT=$(gh pr list --repo bluedevilcollectibles/lspro-react --head wo/cheers-campaigns-01 --state open --json url 2>&1)\n      if echo \"$RESULT\" | grep -q \"url\"; then\n        echo \"STATUS=pr_open_preview_pending\"\n      else\n        echo \"STATUS=skipped\"\n      fi\n      exit 0\n\n  - id: build-manifest\n    depends_on: [runtime-verify, verify-vercel]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 for WO-CHEERS-CAMPAIGNS-01. MIXED class (CODE + Rule 19 + migration).\n\n      WO: WO-CHEERS-CAMPAIGNS-01\n      Builder: Major Build (Archon bdc-cheers-campaigns)\n\n      Files modified:\n        lspro-react/src/App.tsx\n        shopops/shopops-api/index.js\n\n      Files created:\n        lspro-react/src/pages/cheers/CampaignsPage.tsx\n        lspro-react/src/pages/cheers/CampaignBuilderPage.tsx\n        lspro-react/src/pages/cheers/components/TemplateEditor.tsx\n        lspro-react/src/pages/cheers/components/CampaignPreview.tsx\n        lspro-react/src/tests/cheers-campaigns.test.tsx\n        shopops/shopops-api/migrations/2026-05-13_add_communication_log_and_campaigns.sql\n        shopops/shopops-api/migrations/2026-05-13_add_communication_log_and_campaigns_rollback.sql\n        shopops/shopops-api/routes/campaigns.js\n        shopops/shopops-api/lib/template-render.js\n        shopops/shopops-api/lib/campaign-channels/sms-channel.js\n        shopops/shopops-api/lib/campaign-channels/email-channel.js\n        shopops/shopops-api/tests/test_campaigns.js\n\n      Tests: 6 (UI) + 8 (API) = 14 passing\n        bun run test src/tests/cheers-campaigns.test.tsx\n        cd shopops-api && bun tests/test_campaigns.js\n\n      PRs:\n        lspro-react main: [open-pr-ui.UI_PR_URL]\n        shopops master:   [open-pr-api.API_PR_URL]\n\n      Merge ancestors:\n        lspro-react main HEAD=[commit-and-push-ui.UI_SHA], behind_by: 0\n        shopops master HEAD=[commit-and-push-api.API_SHA], behind_by: 0\n\n      Grep assertions:\n        grep -c \"CampaignsPage\\|CampaignBuilderPage\" src/App.tsx >= 2\n        grep -c \"/api/campaigns\" shopops-api/index.js >= 1\n        grep -c \"test(\\|it(\" src/tests/cheers-campaigns.test.tsx >= 6\n        grep -c \"test(\\|it(\" shopops-api/tests/test_campaigns.js >= 8\n\n      Migration:\n        Staging apply: [apply-migration-staging.STATUS]\n        Production: operator runs ssh hetzner-prod 'docker run --rm postgres:16 psql \"$DB_URL\" < migration.sql' post-merge.\n        schema_migrations row: 2026-05-13_add_communication_log_and_campaigns\n\n      Runtime verification:\n        Rule 19 (shopops-api): [runtime-verify.STATUS]\n\n      Vercel deployment: [verify-vercel.output]\n\n      A2P 10DLC: env flag CAMPAIGNS_SMS_BLOCKED_IF_NO_A2P controls SMS gating.\n        Current production status: PENDING per known issues. Email sends unaffected.\n\n      VALIDATION: PASS\n\n      ## Inputs\n      $read-spec.output $plan.output $verify-tests-ui.output $verify-tests-api.output\n      $verify-grep-ui.output $verify-grep-api.output $apply-migration-staging.output\n      $build-check.output $screenshot-loop.output $commit-and-push-ui.output\n      $commit-and-push-api.output $open-pr-ui.output $open-pr-api.output\n      $runtime-verify.output $verify-vercel.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-CHEERS-CAMPAIGNS-01 to REVIEW with manifest in Notes property.\n\n      # OPERATOR: Replace 35fef4c9-ce00-81ac-b633-fc72d336123b before firing.\n      Notion database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      Notion page ID: 35fef4c9-ce00-81ac-b633-fc72d336123b\n\n      Updates:\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> $build-manifest.output verbatim\n\n      If Notion MCP unavailable:\n        XO_TODO: Flip WO-CHEERS-CAMPAIGNS-01 to REVIEW, paste manifest into Notes.\n\n      ## Manifest\n      $build-manifest.output\n",
  "bdc-cheers-dashboard": "# Drafted by yaml-author subagent for WO-CHEERS-DASHBOARD-01 on 2026-05-13\n# Source pattern: WO-SHOPOPS-CHANNEL-ADMIN-UI-01.yaml (lspro-react UI WO shape) +\n#                 WO-STOREFRONT-PORTAL-AUTH-WIRE-01.yaml (MIXED split-repo discipline,\n#                 best-effort schema-verify, Rule 19 runtime verify)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight (OPERATOR MUST VERIFY BEFORE FIRING):\n#   1. bluedevilcollectibles/lspro-react registered in Archon AND default_cwd = lspro-react repo root:\n#      sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name, default_cwd FROM remote_agent_codebases WHERE name LIKE '%lspro%'\"\n#   2. bluedevilcollectibles/shopops registered in Archon (this WO touches BOTH repos via /tmp clone)\n#   3. WO spec committed + pushed to bdc-xo main: docs/work-orders/WO-CHEERS-DASHBOARD-01.md\n#   4. GH_TOKEN with push to both lspro-react + shopops in Archon env\n#   5. Notion page ID for WO-CHEERS-DASHBOARD-01 — fill in 35fef4c9-ce00-81e8-a80d-d32a989e33e6 before firing\n#\n# OPEN QUESTIONS (operator answers before deploy):\n#   Q1: Default location scope for HQ tier — all-locations or location-picker required? Draft assumes all-locations.\n#   Q2: Sidesheet \"View Full Profile\" — stub button this WO; confirm OK with John.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh bdc-cheers-dashboard.yaml\n# REVIEW BEFORE DEPLOY: human verifies test thresholds, screenshot loop N=3, Notion page ID, schema-verify\n# columns match WO §9 expected tables (customers, customer_ledger, orders).\n\nname: bdc-cheers-dashboard\ndescription: |\n  Use when: Executing WO-CHEERS-DASHBOARD-01 — Cheers customer-intelligence dashboard.\n  Input: WO_ID = WO-CHEERS-DASHBOARD-01.\n  Does: Reads spec from bdc-xo. Verifies customers + customer_ledger + orders schema\n        via Supabase REST (best-effort). Adds GET /api/customers/intelligence to\n        shopops-api (cloned to /tmp). Adds CheersDashboardPage + components +\n        cheers-scoring.ts helper + tests in lspro-react. Runs Vitest + bun tests.\n        Opens 2 PRs (lspro-react main + shopops master). UI build-time verification\n        loop N=3. Rule 19 runtime grep against shopops-api container. Builds MIXED\n        manifest. Flips Notion to REVIEW.\n  Repos: bluedevilcollectibles/lspro-react (Archon CWD) + bluedevilcollectibles/shopops\n         (cloned to /tmp). WO spec in bluedevilcollectibles/bdc-xo.\n  parallel_ok: false — touches 2 repos.\n  NOT for: any other WO.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-CHEERS-DASHBOARD-01}\"\n\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --jq '[.[] | select(.name | test(\"'\"${WO_ID}\"'\")) | .path] | .[0]' \\\n          2>/dev/null || true)\n        if [ -n \"$GLOB_PATH\" ] && [ \"$GLOB_PATH\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      DESIGN_DOC=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs/2026-04-16-cheers-platform-design.md?ref=main\" \\\n        --jq .content 2>/dev/null | base64 -d 2>/dev/null | head -250 || true)\n\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n\\n' \"$SPEC_CONTENT\"\n      printf -- '--- DESIGN DOC §1-§2.5 ---\\n%s\\n' \"$DESIGN_DOC\"\n\n  - id: schema-verify\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step (Supabase may 401/429)\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"\n        echo \"DETAIL=Verify manually: customers + customer_ledger + orders columns per WO §9\"\n        exit 0\n      fi\n\n      check() {\n        local TABLE=\"$1\"\n        RESULT=$(curl -sf \\\n          \"${SUPABASE_URL}/rest/v1/information_schema.columns?table_name=eq.${TABLE}&select=column_name,data_type&table_schema=eq.public\" \\\n          -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n          -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n        if echo \"$RESULT\" | grep -q '\"column_name\"'; then\n          echo \"SCHEMA_${TABLE}=VERIFIED\"\n        else\n          echo \"SCHEMA_${TABLE}=UNVERIFIED\"\n        fi\n      }\n      check \"customers\"\n      check \"customer_ledger\"\n      check \"orders\"\n      echo \"STATUS=ok\"\n      exit 0\n\n  - id: audit-prior-art\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      echo \"=== Prior art audit ===\"\n      ls -la src/pages/ | head -40\n      grep -n \"meetsTier\\|hq\\|HQ\" src/lib/tiers.ts 2>/dev/null | head -20 || \\\n        grep -rn \"meetsTier\" src/lib/ | head -10\n      echo \"--- App.tsx routes ---\"\n      grep -n \"Route path\" src/App.tsx | head -30\n      echo \"--- apiFetch helper ---\"\n      grep -n \"export.*apiFetch\" src/lib/api.ts\n      echo \"--- existing test files ---\"\n      find src/ -name \"*.test.ts\" -o -name \"*.test.tsx\" | head -10\n      echo \"=== Audit complete ===\"\n\n  - id: setup-shopops-clone\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      SHOPOPS_DIR=\"/tmp/shopops-work-$WORKFLOW_ID\"\n      if [ -d \"$SHOPOPS_DIR/.git\" ]; then\n        cd \"$SHOPOPS_DIR\"\n        git fetch origin\n        git checkout \"$DEFAULT_BRANCH\" 2>/dev/null || git checkout \"$DEFAULT_BRANCH\"\n        git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || git reset --hard \"origin/$DEFAULT_BRANCH\"\n        echo \"STATUS=reused\"\n      else\n        git clone \"https://github.com/bluedevilcollectibles/shopops.git\" \"$SHOPOPS_DIR\" --depth=50\n        echo \"STATUS=fresh\"\n      fi\n      echo \"SHOPOPS_DIR=${SHOPOPS_DIR}\"\n\n  - id: plan\n    depends_on: [audit-prior-art, schema-verify, setup-shopops-clone]\n    task_class: medium_work\n    prompt: |\n      Plan the Cheers Dashboard implementation across lspro-react + shopops.\n\n      ## Spec\n      $read-spec.output\n\n      ## Prior art audit\n      $audit-prior-art.output\n\n      ## Schema verify\n      $schema-verify.output\n\n      ## Required output (Markdown, exact headings)\n\n      ### lspro-react files (CWD = lspro-react repo root)\n      - Create: src/pages/cheers/CheersDashboardPage.tsx\n      - Create: src/pages/cheers/components/{KPIStrip,AtRiskCustomerList,ActivityFeed,CustomerDrillSidesheet}.tsx\n      - Create: src/lib/cheers-scoring.ts (pure functions, no React)\n      - Create: src/tests/cheers-scoring.test.ts (8 Vitest tests)\n      - Create: src/tests/cheers-dashboard-page.test.tsx (4 RTL tests)\n      - Modify: src/App.tsx — add route /cheers behind meetsTier('hq')\n      - Branch: wo/cheers-dashboard-01\n      - PR base: main\n\n      ### shopops files (CWD = /tmp/shopops-work-$WORKFLOW_ID after clone)\n      - Create: shopops-api/routes/customer-intelligence.js — GET /api/customers/intelligence\n      - Create: shopops-api/lib/rfm-scorer.js — pure RFM helpers (mirror of cheers-scoring.ts for backend)\n      - Create: shopops-api/tests/test_customer_intelligence.js (6 bun tests)\n      - Modify: shopops-api/index.js — mount /api/customers/intelligence route\n      - Branch: wo/cheers-dashboard-01\n      - PR base: master\n\n      ### Scoring (verbatim from spec §RFM Calibration)\n      - Recency: <14d=5, 15-30d=4, 31-45d=3, 46-60d=2, 60+=1\n      - Frequency: purchases/30d: 4+=5, 3=4, 2=3, 1=2, 0=1\n      - Monetary: tenant-wide quintile rank\n      - Health = R*0.40 + F*0.35 + M*0.25; >=4.0 active, 2.5-3.9 at_risk, <2.5 inactive\n\n      ### Test counts\n      - cheers-scoring.test.ts: 8 tests\n      - cheers-dashboard-page.test.tsx: 4 tests\n      - test_customer_intelligence.js: 6 tests\n\n      ### UI verification screenshots (loop N=3)\n      - SHOT_1: /cheers loads, KPI strip 4 cards visible\n      - SHOT_2: AtRiskCustomerList has >=3 rows with health dots\n      - SHOT_3: CustomerDrillSidesheet open with R/F/M scores shown\n\n  - id: implement-api\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement shopops-api side of WO-CHEERS-DASHBOARD-01.\n        Work in /tmp/shopops-work-$WORKFLOW_ID (the shopops clone).\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n        1. Read /tmp/shopops-work-$WORKFLOW_ID/shopops-api/routes/customers.js (651 lines)\n           to understand existing customer query patterns BEFORE writing new routes.\n        2. Read /tmp/shopops-work-$WORKFLOW_ID/shopops-api/index.js to find where routes\n           are mounted (express app.use pattern).\n        3. Create shopops-api/routes/customer-intelligence.js implementing GET /api/customers/intelligence:\n           - Query params: tenant_id (req), location_id (opt), limit (default 100), segment (opt filter)\n           - Returns: { ok: true, kpis, segments, customers, activity_feed }\n           - Multi-tenant isolation: filter EVERY query by tenant_id\n           - Use existing pg/supabase client pattern from customers.js\n        4. Create shopops-api/lib/rfm-scorer.js as PURE FUNCTIONS:\n             - scoreRecency(days) -> 1..5\n             - scoreFrequency(purchases30d) -> 1..5\n             - scoreMonetary(spendCents, allTenantSpends) -> 1..5\n             - scoreHealth({r, f, m}) -> float\n             - classifySegment({r, f, m, daysActive}) -> 'champions'|'loyal'|...\n        5. Mount route in shopops-api/index.js at /api/customers/intelligence\n        6. Create shopops-api/tests/test_customer_intelligence.js with bun:test imports:\n           - 6 tests per WO §7 (shape, at_risk_count, segment filter, location filter,\n             empty tenant, deterministic scoring)\n           - Tests assert REAL behavior, not \"did not throw\"\n        7. Do NOT push, do NOT commit yet, do NOT run docker.\n\n        ## Completion criteria\n        Output <promise>COMPLETE</promise> only when:\n          - 3 new files exist in shopops-api/\n          - index.js has the route mounted\n          - bun tests/test_customer_intelligence.js runs (may need mocks for db; that's fine)\n          - >=6 test() / it() blocks in the test file\n\n  - id: implement-ui\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement lspro-react side of WO-CHEERS-DASHBOARD-01.\n        CWD is the lspro-react repo root.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n        1. Read src/pages/AdminPage.tsx and src/pages/CustomersPage.tsx FIRST to\n           understand the existing card/table style. Match it. Do not invent a new design.\n        2. Use apiFetch from @/lib/api — returns Promise<Response>, call .json() on it.\n        3. Read src/lib/tiers.ts for meetsTier signature.\n        4. Create src/lib/cheers-scoring.ts with the SAME function signatures as\n           shopops-api/lib/rfm-scorer.js so frontend can score locally if needed.\n           Pure functions, no React imports.\n        5. Create src/pages/cheers/CheersDashboardPage.tsx:\n           - Calls apiFetch('/api/customers/intelligence?tenant_id=...').then(r => r.json())\n           - Renders <KPIStrip>, <AtRiskCustomerList>, <ActivityFeed>, <CustomerDrillSidesheet>\n           - Sidesheet opens on row click; controlled state at the page level\n        6. Create the 4 component files under src/pages/cheers/components/\n        7. Modify src/App.tsx: add <Route path=\"/cheers\" element={<CheersDashboardPage />} />\n           gated by meetsTier('hq') — match the existing route guard pattern.\n        8. Create src/tests/cheers-scoring.test.ts — 8 Vitest tests per WO §7\n        9. Create src/tests/cheers-dashboard-page.test.tsx — 4 RTL tests with vi.mock('@/lib/api')\n        10. Do NOT add new package dependencies.\n        11. Do NOT modify AdminPage, CustomersPage, server.js, or any unrelated file.\n\n        ## Completion criteria\n        Output <promise>COMPLETE</promise> only when:\n          - 5 React files + 1 helper + 2 test files exist\n          - App.tsx contains 'CheersDashboardPage' (grep -c >= 1)\n          - cheers-scoring.test.ts has >=8 test() blocks\n          - cheers-dashboard-page.test.tsx has >=4 test() blocks\n          - No new dependencies in package.json\n\n  - id: verify-types\n    depends_on: [implement-ui]\n    bash: |\n      set -euo pipefail\n      if [ -f tsconfig.json ]; then\n        bunx tsc --noEmit || (echo \"TypeScript errors\" >&2; exit 1)\n        echo \"TypeScript check: PASS\"\n      fi\n\n  - id: verify-tests-ui\n    depends_on: [implement-ui]\n    bash: |\n      set -euo pipefail\n      bun run test src/tests/cheers-scoring.test.ts 2>&1 | tee /tmp/ui-scoring.txt\n      bun run test src/tests/cheers-dashboard-page.test.tsx 2>&1 | tee /tmp/ui-page.txt\n      echo \"UI_SCORING_TAIL=$(tail -5 /tmp/ui-scoring.txt)\"\n      echo \"UI_PAGE_TAIL=$(tail -5 /tmp/ui-page.txt)\"\n\n  - id: verify-tests-api\n    depends_on: [implement-api]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      bun shopops-api/tests/test_customer_intelligence.js 2>&1 | tee /tmp/api-tests.txt \\\n        || echo \"API tests may need live DB; CI against staging is authoritative.\"\n      echo \"API_TEST_TAIL=$(tail -5 /tmp/api-tests.txt)\"\n\n  - id: verify-grep-ui\n    depends_on: [implement-ui]\n    bash: |\n      set -euo pipefail\n      echo \"=== UI grep assertions ===\"\n      grep -q \"CheersDashboardPage\" src/App.tsx \\\n        || (echo \"FAIL: CheersDashboardPage not wired in App.tsx\" >&2; exit 1)\n      ls src/pages/cheers/CheersDashboardPage.tsx\n      ls src/lib/cheers-scoring.ts\n      SCORING_TESTS=$(grep -c \"test(\\|it(\" src/tests/cheers-scoring.test.ts)\n      PAGE_TESTS=$(grep -c \"test(\\|it(\" src/tests/cheers-dashboard-page.test.tsx)\n      echo \"scoring_tests=$SCORING_TESTS page_tests=$PAGE_TESTS\"\n      [ \"$SCORING_TESTS\" -ge 8 ] || (echo \"FAIL: need >=8 scoring tests, got $SCORING_TESTS\" >&2; exit 1)\n      [ \"$PAGE_TESTS\" -ge 4 ] || (echo \"FAIL: need >=4 page tests, got $PAGE_TESTS\" >&2; exit 1)\n      echo \"=== UI grep PASS ===\"\n\n  - id: verify-grep-api\n    depends_on: [implement-api]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      echo \"=== API grep assertions ===\"\n      ls shopops-api/routes/customer-intelligence.js\n      grep -q \"customers/intelligence\" shopops-api/index.js \\\n        || (echo \"FAIL: route not mounted in index.js\" >&2; exit 1)\n      grep -q \"scoreRecency\\|scoreFrequency\\|scoreHealth\" shopops-api/lib/rfm-scorer.js \\\n        || (echo \"FAIL: RFM helpers missing\" >&2; exit 1)\n      API_TESTS=$(grep -c \"test(\\|it(\" shopops-api/tests/test_customer_intelligence.js)\n      echo \"api_tests=$API_TESTS\"\n      [ \"$API_TESTS\" -ge 6 ] || (echo \"FAIL: need >=6 API tests\" >&2; exit 1)\n      echo \"=== API grep PASS ===\"\n\n  - id: build-check\n    depends_on: [verify-types, verify-tests-ui, verify-grep-ui]\n    bash: |\n      set -euo pipefail\n      bun run build 2>&1 | tee /tmp/build.txt\n      EXIT=${PIPESTATUS[0]}\n      if [ $EXIT -ne 0 ]; then\n        echo \"BUILD FAILED\" >&2\n        cat /tmp/build.txt >&2\n        exit 1\n      fi\n      echo \"BUILD_STATUS=ok\"\n\n  - id: screenshot-loop\n    depends_on: [build-check]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: SCREENSHOTS_COMPLETE\n      max_iterations: 3\n      prompt: |\n        Produce the 3 required UI Build-Time Verification screenshots for WO-CHEERS-DASHBOARD-01.\n\n        Inspect src/pages/cheers/CheersDashboardPage.tsx and its 4 child components\n        in src/pages/cheers/components/. Produce a code-level confirmation that\n        each screenshot scenario is achievable.\n\n        ## Required (per WO §8 stop condition 8)\n        1. SHOT_1: /cheers route loads, KPIStrip shows 4 cards with values\n        2. SHOT_2: AtRiskCustomerList populated with >=3 rows, each with health dot\n        3. SHOT_3: CustomerDrillSidesheet visible after row click, R/F/M scores shown\n\n        For each, output:\n          - SCREENSHOT_N: <PASS|FAIL>\n          - Evidence: <code path + line refs supporting the rendering>\n          - Gap (if any): <fix needed>\n\n        If all 3 pass: output <promise>SCREENSHOTS_COMPLETE</promise>\n        If any fail: make targeted corrections to the cheers/ files, then re-check.\n\n  - id: commit-and-push-ui\n    depends_on: [screenshot-loop]\n    bash: |\n      set -euo pipefail\n      BASE_BRANCH=\"wo/cheers-dashboard-01\"\n      BRANCH=\"$BASE_BRANCH\"\n\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n          BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n          git checkout -b \"$BRANCH\"\n        else\n          git checkout \"$BRANCH\"\n        fi\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n\n      git add src/App.tsx \\\n        src/pages/cheers/ \\\n        src/lib/cheers-scoring.ts \\\n        src/tests/cheers-scoring.test.ts \\\n        src/tests/cheers-dashboard-page.test.tsx\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(cheers): WO-CHEERS-DASHBOARD-01 — customer-intelligence dashboard\\n\\nAdds CheersDashboardPage at /cheers (HQ tier gate via meetsTier).\\n4-panel layout: KPI strip, at-risk list, activity feed, drill-down sidesheet.\\nPure RFM scoring helper (cheers-scoring.ts) shared with shopops-api rfm-scorer.\\n\\nTests:\\n- cheers-scoring.test.ts (8 Vitest) — recency/frequency/monetary/health/segment\\n- cheers-dashboard-page.test.tsx (4 RTL) — KPI render, at-risk filter, sidesheet, activity sort\\n\\nDepends on: shopops-api GET /api/customers/intelligence (companion PR).\\nBlocks: WO-CHEERS-CAMPAIGNS-01\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n\n      git push -u origin \"$BRANCH\"\n      echo \"UI_BRANCH=${BRANCH}\"\n      echo \"UI_SHA=$(git rev-parse HEAD)\"\n\n  - id: commit-and-push-api\n    depends_on: [verify-grep-api, verify-tests-api]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      BASE_BRANCH=\"wo/cheers-dashboard-01\"\n      BRANCH=\"$BASE_BRANCH\"\n\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        git checkout \"$BRANCH\"\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n\n      git add shopops-api/routes/customer-intelligence.js \\\n        shopops-api/lib/rfm-scorer.js \\\n        shopops-api/tests/test_customer_intelligence.js \\\n        shopops-api/index.js\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-CHEERS-DASHBOARD-01 — customer intelligence endpoint\\n\\nGET /api/customers/intelligence — RFM-scored customer list + KPI rollups\\nfor the Cheers dashboard. Multi-tenant isolated. Optional location_id filter.\\n\\nNew files:\\n- routes/customer-intelligence.js\\n- lib/rfm-scorer.js (pure functions)\\n- tests/test_customer_intelligence.js (6 bun tests)\\n\\nScoring: R<14d=5..60+=1, F purchases/30d 4+=5..0=1, M tenant-wide quintile,\\nHealth = R*0.40 + F*0.35 + M*0.25.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n\n      git push -u origin \"$BRANCH\"\n      echo \"API_BRANCH=${BRANCH}\"\n      echo \"API_SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr-ui\n    depends_on: [commit-and-push-ui]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/cheers-dashboard-01\"\n      REPO=\"bluedevilcollectibles/lspro-react\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements WO-CHEERS-DASHBOARD-01 (lspro-react side) — Cheers dashboard at /cheers (HQ tier).\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- src/pages/cheers/CheersDashboardPage.tsx + 4 components\"\n        echo \"- src/lib/cheers-scoring.ts (RFM helpers)\"\n        echo \"- src/tests/cheers-scoring.test.ts (8 tests)\"\n        echo \"- src/tests/cheers-dashboard-page.test.tsx (4 tests)\"\n        echo \"- src/App.tsx (route wired behind meetsTier('hq'))\"\n        echo \"\"\n        echo \"## Companion PR\"\n        echo \"shopops master: wo/cheers-dashboard-01 — /api/customers/intelligence endpoint\"\n        echo \"\"\n        echo \"Spec: bdc-xo docs/work-orders/WO-CHEERS-DASHBOARD-01.md\"\n      } > \"$BODY\"\n\n      PR_URL=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null)\n      if [ -n \"$PR_URL\" ]; then\n        echo \"UI_PR_URL=$PR_URL\"; exit 0\n      fi\n\n      for attempt in 1 2 3; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base main --head \"$BRANCH\" \\\n            --title \"feat(cheers): WO-CHEERS-DASHBOARD-01 — customer-intelligence dashboard\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"UI_PR_URL=$PR_OUT\"; exit 0\n        fi\n        sleep 10\n      done\n      exit 1\n\n  - id: open-pr-api\n    depends_on: [commit-and-push-api]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      BRANCH=\"wo/cheers-dashboard-01\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements WO-CHEERS-DASHBOARD-01 (shopops-api side) — GET /api/customers/intelligence.\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- shopops-api/routes/customer-intelligence.js\"\n        echo \"- shopops-api/lib/rfm-scorer.js\"\n        echo \"- shopops-api/tests/test_customer_intelligence.js (6 bun tests)\"\n        echo \"\"\n        echo \"## Rule 19 verification (post-deploy)\"\n        echo '  docker exec shopops-api grep -n \"customers/intelligence\" /app/routes/customer-intelligence.js'\n        echo \"\"\n        echo \"## Companion PR\"\n        echo \"lspro-react main: wo/cheers-dashboard-01\"\n      } > \"$BODY\"\n\n      PR_URL=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null)\n      if [ -n \"$PR_URL\" ]; then\n        echo \"API_PR_URL=$PR_URL\"; exit 0\n      fi\n\n      for attempt in 1 2 3; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base master --head \"$BRANCH\" \\\n            --title \"feat(api): WO-CHEERS-DASHBOARD-01 — customer intelligence endpoint\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"API_PR_URL=$PR_OUT\"; exit 0\n        fi\n        sleep 10\n      done\n      exit 1\n\n  - id: runtime-verify\n    depends_on: [open-pr-api, open-pr-ui]\n    bash: |\n      # set -e DELIBERATELY OMITTED — Rule 19 best-effort\n      RESULT=$(docker exec shopops-api grep -n \"customers/intelligence\" /app/routes/customer-intelligence.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RULE19_RESULT=$RESULT\"\n      elif echo \"$RESULT\" | grep -qiE 'No such|not found'; then\n        echo \"STATUS=pending_rebuild\"\n        echo \"DETAIL=Re-run post-merge: docker exec shopops-api grep -n 'customers/intelligence' /app/routes/customer-intelligence.js\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=$RESULT\"\n      fi\n      exit 0\n\n  - id: verify-vercel\n    depends_on: [open-pr-ui]\n    bash: |\n      # set -e DELIBERATELY OMITTED — Vercel may be in-flight\n      sleep 30\n      RESULT=$(gh pr list --repo \"bluedevilcollectibles/lspro-react\" --head \"wo/cheers-dashboard-01\" \\\n        --state open --json url,headRefName 2>&1)\n      if echo \"$RESULT\" | grep -q \"url\"; then\n        echo \"STATUS=pr_open_preview_pending\"\n        echo \"DETAIL=Vercel preview will surface in PR checks; production deploys on main merge\"\n      else\n        echo \"STATUS=skipped\"\n        echo \"DETAIL=$RESULT\"\n      fi\n      exit 0\n\n  - id: build-manifest\n    depends_on: [runtime-verify, verify-vercel]\n    task_class: clerical\n    prompt: |\n      Build the Captain CI manifest v2 for WO-CHEERS-DASHBOARD-01.\n      Class: MIXED — CODE in 2 repos + Rule 19 runtime check for shopops-api.\n\n      WO: WO-CHEERS-DASHBOARD-01\n      Builder: Major Build (Archon bdc-cheers-dashboard)\n\n      Files modified:\n        lspro-react/src/App.tsx\n        shopops/shopops-api/index.js\n\n      Files created:\n        lspro-react/src/pages/cheers/CheersDashboardPage.tsx\n        lspro-react/src/pages/cheers/components/KPIStrip.tsx\n        lspro-react/src/pages/cheers/components/AtRiskCustomerList.tsx\n        lspro-react/src/pages/cheers/components/ActivityFeed.tsx\n        lspro-react/src/pages/cheers/components/CustomerDrillSidesheet.tsx\n        lspro-react/src/lib/cheers-scoring.ts\n        lspro-react/src/tests/cheers-scoring.test.ts\n        lspro-react/src/tests/cheers-dashboard-page.test.tsx\n        shopops/shopops-api/routes/customer-intelligence.js\n        shopops/shopops-api/lib/rfm-scorer.js\n        shopops/shopops-api/tests/test_customer_intelligence.js\n\n      Tests:\n        lspro-react: [count from verify-tests-ui] passing\n          command: bun run test src/tests/cheers-scoring.test.ts && bun run test src/tests/cheers-dashboard-page.test.tsx\n        shopops: [count from verify-tests-api] passing\n          command: bun shopops-api/tests/test_customer_intelligence.js\n\n      PRs:\n        lspro-react main: [from open-pr-ui.UI_PR_URL]\n        shopops master: [from open-pr-api.API_PR_URL]\n\n      Merge ancestors:\n        lspro-react main HEAD=[from commit-and-push-ui.UI_SHA], behind_by: 0\n        shopops master HEAD=[from commit-and-push-api.API_SHA], behind_by: 0\n\n      Grep assertions:\n        grep -c \"CheersDashboardPage\" src/App.tsx >= 1\n        grep -c \"test(\\|it(\" src/tests/cheers-scoring.test.ts >= 8\n        grep -c \"test(\\|it(\" src/tests/cheers-dashboard-page.test.tsx >= 4\n        grep -c \"customers/intelligence\" shopops-api/index.js >= 1\n        grep -c \"test(\\|it(\" shopops-api/tests/test_customer_intelligence.js >= 6\n\n      Runtime verification:\n        Rule 19 (shopops-api): [from runtime-verify.STATUS]\n        Command: docker exec shopops-api grep -n 'customers/intelligence' /app/routes/customer-intelligence.js\n\n      Vercel deployment:\n        [from verify-vercel.output — preview URL pending PR build]\n\n      VALIDATION: PASS\n\n      ## Inputs\n      $read-spec.output\n      $plan.output\n      $verify-tests-ui.output\n      $verify-tests-api.output\n      $verify-grep-ui.output\n      $verify-grep-api.output\n      $build-check.output\n      $screenshot-loop.output\n      $commit-and-push-ui.output\n      $commit-and-push-api.output\n      $open-pr-ui.output\n      $open-pr-api.output\n      $runtime-verify.output\n      $verify-vercel.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-CHEERS-DASHBOARD-01 to REVIEW with manifest in Notes property\n      (NOT a page comment — Captain CI reads Notes).\n\n      # OPERATOR: Replace 35fef4c9-ce00-81e8-a80d-d32a989e33e6 with real Notion page ID before firing.\n      Notion database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      Notion page ID: 35fef4c9-ce00-81e8-a80d-d32a989e33e6\n\n      Updates (single call):\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> $build-manifest.output verbatim\n      - claimed_by: Major Build (Archon bdc-cheers-dashboard)\n\n      If Notion MCP unavailable, output:\n        XO_TODO: Flip WO-CHEERS-DASHBOARD-01 to REVIEW, paste manifest into Notes.\n\n      ## Manifest\n      $build-manifest.output\n",
  "bdc-cleanup-sweep": "name: bdc-cleanup-sweep\ndescription: |\n  Use when: Executing a BDC cleanup-sweep Work Order from an approved spec.\n  Input: WO_ID in the environment or user message.\n  Does: Reads the WO spec, enumerates files and violations, applies locked mapping table, verifies sweeps, commits, pushes, opens PRs, and builds manifest.\n  NOT for: Feature development (use bdc-feature-development.yaml), doctrine updates, infra deploys, or sweeps requiring judgment.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: claude-sonnet-4-5\n\nnodes:\n  - id: read-spec\n    persona: xo\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-}\"\n      if [ -z \"$WO_ID\" ]; then\n        WO_ID=$(printf '%s\\n' \"$USER_MESSAGE\" | grep -Eo 'WO-[A-Z0-9-]+' | head -n 1 || true)\n      fi\n      if [ -z \"$WO_ID\" ]; then\n        echo \"WO_ID missing. Provide WO_ID in environment or user message.\" >&2\n        exit 1\n      fi\n      # Try direct path first, then date-prefixed glob (specs are commonly named YYYY-MM-DD-WO-...md)\n      # CWD is the bdc-xo repo root (Archon worktree), so paths are relative — no BDC_XO/ prefix.\n      SPEC_PATH=\"docs/superpowers/specs/${WO_ID}.md\"\n      if [ ! -f \"$SPEC_PATH\" ]; then\n        SPEC_PATH=$(ls docs/superpowers/specs/*${WO_ID}.md 2>/dev/null | head -n 1 || true)\n      fi\n      if [ -z \"$SPEC_PATH\" ] || [ ! -f \"$SPEC_PATH\" ]; then\n        # Fallback 3: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        SPEC_CONTENT=\"\"\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n        fi\n        if [ -z \"$SPEC_CONTENT\" ]; then\n          echo \"Spec not found for WO_ID=${WO_ID} (tried docs/superpowers/specs/${WO_ID}.md, date-prefix glob, and GitHub issue search)\" >&2\n          exit 1\n        fi\n        printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:issues/%s\\n\\n' \"$WO_ID\" \"$ISSUE_NUM\"\n        printf '%s\\n' \"$SPEC_CONTENT\"\n        exit 0\n      fi\n      printf 'WO_ID=%s\\nSPEC_PATH=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\"\n      cat \"$SPEC_PATH\"\n\n  - id: post-started\n    persona: xo\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      curl -sS -X POST \"https://n8n.bluedevilcollectibles.com/webhook/builder-status\" \\\n        -H \"Content-Type: application/json\" \\\n        -d \"{\\\"action\\\":\\\"started\\\",\\\"wo_id\\\":\\\"${WO_ID}\\\",\\\"builder\\\":\\\"Major Build\\\",\\\"timestamp\\\":\\\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\\\"}\"\n\n  - id: enumerate\n    persona: xo\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      SPEC=\"$read-spec.output\"\n\n      # Extract locked mapping table — fail fast if missing\n      if ! printf '%s\\n' \"$SPEC\" | grep -q '##.*[Ll]ocked.*[Mm]apping'; then\n        echo \"Locked mapping table not found in spec. Cleanup sweeps require explicit before/after pairs.\" >&2\n        exit 1\n      fi\n\n      # Extract file glob pattern(s) — typically in spec under \"In scope\" or similar\n      # For now, just document what needs to be enumerated.\n      # Real enumeration happens in the apply-mappings loop.\n      echo \"Pre-flight enumeration:\"\n      printf '%s\\n' \"$SPEC\" | sed -n '/^##.*[Ff]ile.*[Gg]lob/,/^##/p' | head -20 || true\n      echo \"\"\n      echo \"Locked mapping table will be applied per-file in apply-mappings node.\"\n\n  - id: verify-pre-state\n    persona: major-build\n    depends_on: [enumerate]\n    bash: |\n      set -euo pipefail\n      SPEC=\"$read-spec.output\"\n\n      # Extract file glob from spec (simplified: look for common patterns)\n      # In practice, the spec should list the exact glob or file list.\n      # This is a placeholder that logs what files would be in scope.\n      echo \"Collecting baseline violation counts...\"\n      echo \"(Full enumeration happens when mappings are applied)\"\n\n  - id: apply-mappings\n    persona: major-build\n    depends_on: [verify-pre-state]\n    task_class: clerical\n    prompt: |\n      Apply the locked replacement mapping from the spec to all files in scope.\n\n      ## Inputs\n      Spec:\n      $read-spec.output\n\n      ## Rules\n      1. Extract the locked mapping table from the spec (before/after pairs).\n      2. Identify the file glob or file list from the spec (e.g., all .css files, all .js files in a dir).\n      3. For each file in scope:\n         a. Check the current violation count (grep for the pattern to be replaced)\n         b. Apply sed replacements per the locked table (one replacement per pass, or combined)\n         c. Commit the file separately with a message like \"sweep(WO-XXX): replace <old> with <new> in filename\"\n         d. Verify the violation count is now 0 (or matches explicit-exception block in spec)\n      4. Output a mapping of files with before/after violation counts:\n         ```\n         filename: 0 → 0 (already clean)\n         filename: 5 → 0 (cleaned)\n         filename: 3 → 0 (cleaned)\n         ```\n      5. Stop and report if any file has a violation count that doesn't match the expected result.\n\n      ## Completion\n      Output <promise>COMPLETE</promise> only when all files have been swept and verified.\n\n  - id: verify-post-state\n    persona: major-build\n    depends_on: [apply-mappings]\n    bash: |\n      set -euo pipefail\n      SPEC=\"$read-spec.output\"\n\n      echo \"Post-sweep verification: grep for violations across all swept files...\"\n      # This will be populated by the apply-mappings node with file list and patterns.\n      echo \"(Violations count should be 0 per mapping.)\"\n\n  - id: verify-build\n    persona: major-build\n    depends_on: [verify-post-state]\n    bash: |\n      set -euo pipefail\n      if [ -f package.json ]; then\n        if node -e \"const p=require('./package.json'); process.exit(p.scripts && p.scripts.build ? 0 : 1)\"; then\n          bun run build\n        else\n          echo \"No package build script; skipping build.\"\n        fi\n      elif [ -f tests/run_all.js ]; then\n        node tests/run_all.js\n      else\n        echo \"No recognized build command; record as not applicable in manifest.\"\n      fi\n\n  - id: verify-types\n    persona: major-build\n    depends_on: [verify-post-state]\n    bash: |\n      set -euo pipefail\n      if [ -f tsconfig.json ]; then\n        bunx tsc --noEmit\n      else\n        echo \"No tsconfig.json; skipping TypeScript check.\"\n      fi\n\n  - id: decide-push-target\n    persona: major-build\n    depends_on: [verify-build, verify-types]\n    task_class: clerical\n    prompt: |\n      Decide the push target for this BDC cleanup-sweep Work Order.\n\n      ## Inputs\n      Spec:\n      $read-spec.output\n\n      Mappings applied:\n      $apply-mappings.output\n\n      ## Required Repo Policy\n      Extract the target repository from the spec content. Look for any of these patterns (in priority order):\n      1. `target_repo: <owner>/<repo>` (YAML field)\n      2. `**Target repo:** \\`<owner>/<repo>\\`` (Markdown bold)\n      3. `Target repo: <owner>/<repo>` (plain header)\n      4. `**Target Repo:** \\`<owner>/<repo>\\`` (Markdown bold, capital R)\n\n      The target repo is where the PR must be opened (NOT the worktree's git origin — Rule 29).\n\n      Match the extracted repo against this hardcoded staging-gate list:\n      - bluedevilcollectibles/lspro-react\n      - bluedevilcollectibles/shopops-storefront\n      - bluedevilcollectibles/shopops\n\n      If the repo matches one of those entries, output:\n      push_target: feature-branch:<spec-derived-branch-name>\n      pr_required: true\n      staging_gate_required: true\n      repo: <owner>/<repo>\n\n      If the repo is bdc-harness or another code repo, output:\n      push_target: feature-branch:<spec-derived-branch-name>\n      pr_required: true\n      staging_gate_required: false\n      repo: <owner>/<repo>\n\n      Branch naming convention: cleanup-<WO-ID-lowercase>\n\n      CRITICAL: You MUST emit a `repo:` line in every output case. If no target_repo is\n      parseable from the spec, emit `repo: unknown` and a WARNING line explaining what is missing.\n\n  - id: commit-and-push\n    persona: overseer\n    depends_on: [decide-push-target]\n    bash: |\n      set -euo pipefail\n      echo \"$decide-push-target.output\"\n      BRANCH=$(printf '%s\\n' \"$decide-push-target.output\" | sed -n 's/^push_target: feature-branch://p' | head -n 1)\n      if [ -z \"$BRANCH\" ]; then\n        echo \"No feature branch target found in decide-push-target output.\" >&2\n        exit 1\n      fi\n      # WO-HARNESS-YAML-USE-THREAD-BRANCH-NOT-FEATURE-BRANCH-01\n      # Engine already provides a unique archon/thread-<hash> branch per run.\n      # Derive a unique remote push target from it instead of clobbering a shared name.\n      THREAD_ID=$(git rev-parse --abbrev-ref HEAD | sed 's|archon/thread-||')\n      UNIQUE_BRANCH=\"${BRANCH}-thread-${THREAD_ID}\"\n      git status --short\n      DIRTY=$(git status --porcelain)\n      if [ -z \"$DIRTY\" ]; then\n        # Already fully synced: implement loop committed + pushed to origin\n        if git rev-parse --quiet --verify \"origin/${UNIQUE_BRANCH}\" >/dev/null 2>&1 && \\\n           [ \"$(git rev-parse HEAD)\" = \"$(git rev-parse \"origin/${UNIQUE_BRANCH}\")\" ]; then\n          echo \"Backstop no-op: implement loop already committed + pushed to origin/${UNIQUE_BRANCH} (HEAD matches).\"\n          echo \"unique_branch=${UNIQUE_BRANCH}\"\n          exit 0\n        fi\n        # WO-HARNESS-COMMIT-AND-PUSH-BACKSTOP-FALSE-NEGATIVE-01: clean tree != no work.\n        # Count commits ahead to distinguish pre-committed work from true no-op.\n        if git rev-parse --quiet --verify \"origin/${UNIQUE_BRANCH}\" >/dev/null 2>&1; then\n          COMMITS_AHEAD=$(git rev-list --count \"origin/${UNIQUE_BRANCH}..HEAD\" 2>/dev/null || echo 0)\n        else\n          COMMITS_AHEAD=$(git rev-list --count HEAD --not --remotes=origin 2>/dev/null || echo 0)\n        fi\n        if [ \"$COMMITS_AHEAD\" = \"0\" ]; then\n          echo \"No changed files and no commits ahead of origin/${UNIQUE_BRANCH} -- implement loop did not produce work\" >&2\n          exit 1\n        fi\n        echo \"Backstop: $COMMITS_AHEAD commit(s) ahead of origin -- pushing pre-committed work.\"\n      else\n        # Stage all changed files\n        CHANGED_FILES=$(printf '%s\\n' \"$DIRTY\" | awk '{print substr($0,4)}')\n        while IFS= read -r path; do\n          if [ -z \"$path\" ]; then\n            continue\n          fi\n          git add -- \"$path\"\n        done < <(printf '%s\\n' \"$CHANGED_FILES\")\n        if git diff --cached --quiet; then\n          echo \"No changes staged; cannot commit.\" >&2\n          exit 1\n        fi\n        WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n        COMMIT_MESSAGE=\"cleanup: ${WO_ID} sweep per locked mapping table\"\n        git commit -m \"$COMMIT_MESSAGE\"\n      fi\n      # WO-HARNESS-YAML-USE-THREAD-BRANCH-NOT-FEATURE-BRANCH-01: push via refspec\n      # so the local archon/thread-<hash> branch lands on a unique remote name\n      # (UNIQUE_BRANCH) and does not collide with other parallel runs.\n      git push origin HEAD:\"${UNIQUE_BRANCH}\"\n      echo \"unique_branch=${UNIQUE_BRANCH}\"\n\n  - id: open-pr-if-needed\n    persona: major-build\n    depends_on: [commit-and-push]\n    task_class: clerical\n    bash: |\n      set -euo pipefail\n      if ! printf '%s\\n' \"$decide-push-target.output\" | grep -q '^pr_required: true'; then\n        echo \"PR not required; skipping.\"\n        exit 0\n      fi\n      # WO-HARNESS-YAML-USE-THREAD-BRANCH-NOT-FEATURE-BRANCH-01: read the unique\n      # remote branch the commit-and-push node pushed to; gh pr create must use\n      # --head to target that branch rather than the (local) archon/thread-<hash>.\n      UNIQUE_BRANCH=$(printf '%s\\n' \"$commit-and-push.output\" | sed -n 's/^unique_branch=//p' | head -n 1)\n      if [ -z \"$UNIQUE_BRANCH\" ]; then\n        echo \"UNIQUE_BRANCH not found in commit-and-push output.\" >&2\n        exit 1\n      fi\n      # Rule 29: extract explicit repo from decide-push-target output.\n      REPO=$(printf '%s\\n' \"$decide-push-target.output\" | sed -n 's/^repo: //p' | head -n 1)\n      if [ -z \"$REPO\" ] || [ \"$REPO\" = \"unknown\" ]; then\n        echo \"ERROR: decide-push-target did not emit a valid 'repo: <owner>/<repo>'.\" >&2\n        echo \"Fix the spec to include 'target_repo:' or '**Target repo:**' and re-run.\" >&2\n        exit 1\n      fi\n      WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      TITLE=\"cleanup: ${WO_ID} — sweep per locked mapping\"\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Cleanup sweep per locked mapping table in spec.\"\n        echo \"\"\n        echo \"## Mappings Applied\"\n        echo \"$apply-mappings.output\"\n        echo \"\"\n        echo \"## Verification\"\n        echo \"Build: $verify-build.output\"\n        echo \"\"\n        echo \"Types: $verify-types.output\"\n      } > \"$BODY_FILE\"\n      gh pr create --repo \"$REPO\" --title \"$TITLE\" --body-file \"$BODY_FILE\" --head \"$UNIQUE_BRANCH\"\n\n  - id: build-manifest\n    persona: major-build\n    depends_on: [open-pr-if-needed]\n    task_class: clerical\n    prompt: |\n      Construct the Captain CI completion manifest for this BDC cleanup-sweep Work Order.\n\n      ## Inputs\n      Spec:\n      $read-spec.output\n\n      Mappings applied:\n      $apply-mappings.output\n\n      Verification:\n      Build:\n      $verify-build.output\n\n      Types:\n      $verify-types.output\n\n      ## Required Manifest Fields\n      - WO ID\n      - Files modified (list with before/after violation counts)\n      - Commit SHA\n      - Validation evidence:\n        * Build PASS/N/A\n        * Types PASS/N/A\n        * Per-file grep counts (before → after)\n      - PR URL, if opened\n      - Staging gate status, if required\n\n  - id: post-completed\n    persona: xo\n    depends_on: [build-manifest]\n    bash: |\n      set -euo pipefail\n      WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      DETAIL=$(printf '%s\\n' \"$build-manifest.output\" | python -c \"import json,sys; print(json.dumps(sys.stdin.read()))\")\n      curl -sS -X POST \"https://n8n.bluedevilcollectibles.com/webhook/builder-status\" \\\n        -H \"Content-Type: application/json\" \\\n        -d \"{\\\"action\\\":\\\"completed\\\",\\\"wo_id\\\":\\\"${WO_ID}\\\",\\\"builder\\\":\\\"Major Build\\\",\\\"detail\\\":${DETAIL}}\"\n\n  - id: flip-notion\n    persona: xo\n    depends_on: [post-completed]\n    task_class: clerical\n    prompt: |\n      Update the Notion Work Order page if Notion MCP is available in this runtime.\n\n      ## Required Notion Changes\n      - Claude Status: REVIEW\n      - Status: Review\n      - Add the Captain CI manifest as a page comment\n\n      ## Manifest\n      $build-manifest.output\n\n      If Notion MCP is not available inside the container, do not invent a workaround.\n      Output a TODO for XO to flip Notion manually with the manifest above.\n",
  "bdc-doctrine-update": "name: bdc-doctrine-update\ndescription: |\n  Use when: Executing a BDC doctrine-class Work Order from an approved spec.\n  Input: WO_ID in the environment or user message, plus repo context from Overlord.\n  Does: Reads the BDC WO spec, plans surgical doctrine edits, verifies via grep, commits and pushes directly to master, and builds a Captain CI manifest.\n  NOT for: Code-shipping WOs, feature development, infrastructure deploys, or bug-fix WOs with their own workflow.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: claude-sonnet-4-5\n\nnodes:\n  - id: read-spec\n    persona: xo\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-}\"\n      if [ -z \"$WO_ID\" ]; then\n        WO_ID=$(printf '%s\\n' \"$USER_MESSAGE\" | grep -Eo 'WO-[A-Z0-9-]+' | head -n 1 || true)\n      fi\n      if [ -z \"$WO_ID\" ]; then\n        echo \"WO_ID missing. Provide WO_ID in environment or user message.\" >&2\n        exit 1\n      fi\n      # Try direct path first, then date-prefixed glob (specs are commonly named YYYY-MM-DD-WO-...md)\n      # CWD is the bdc-xo repo root (Archon worktree), so paths are relative — no BDC_XO/ prefix.\n      SPEC_PATH=\"docs/superpowers/specs/${WO_ID}.md\"\n      if [ ! -f \"$SPEC_PATH\" ]; then\n        SPEC_PATH=$(ls docs/superpowers/specs/*${WO_ID}.md 2>/dev/null | head -n 1 || true)\n      fi\n      if [ -z \"$SPEC_PATH\" ] || [ ! -f \"$SPEC_PATH\" ]; then\n        # Fallback 3: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        SPEC_CONTENT=\"\"\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n        fi\n        if [ -z \"$SPEC_CONTENT\" ]; then\n          echo \"Spec not found for WO_ID=${WO_ID} (tried docs/superpowers/specs/${WO_ID}.md, date-prefix glob, and GitHub issue search)\" >&2\n          exit 1\n        fi\n        printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:issues/%s\\n\\n' \"$WO_ID\" \"$ISSUE_NUM\"\n        printf '%s\\n' \"$SPEC_CONTENT\"\n        exit 0\n      fi\n      printf 'WO_ID=%s\\nSPEC_PATH=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\"\n      cat \"$SPEC_PATH\"\n\n  - id: post-started\n    persona: xo\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      curl -sS -X POST \"https://n8n.bluedevilcollectibles.com/webhook/builder-status\" \\\n        -H \"Content-Type: application/json\" \\\n        -d \"{\\\"action\\\":\\\"started\\\",\\\"wo_id\\\":\\\"${WO_ID}\\\",\\\"builder\\\":\\\"Major Build\\\",\\\"timestamp\\\":\\\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\\\"}\"\n\n  - id: plan\n    persona: war-council-architect\n    depends_on: [read-spec]\n    task_class: medium_work\n    prompt: |\n      Read the BDC doctrine Work Order spec below and produce a concise implementation plan.\n\n      ## Required Output\n      Return structured Markdown with these headings:\n      - WO ID\n      - Files to modify\n      - Files to create\n      - Files explicitly out of scope\n      - Grep verification checks (assertions to run on each modified file)\n      - Commit message\n      - Push policy\n\n      ## Doctrine WO Policy\n      Hardcoded Rule: doctrine-only WOs push directly to master. No feature branch. No PR gate.\n      This includes edits to: CLAUDE.md, skills, memory, wiki, policies, README, ATTRIBUTION.\n\n      ## CRITICAL: Path discipline (Rule 4)\n      ALL file paths in your plan MUST be RELATIVE TO REPO ROOT.\n      The CWD is the bdc-xo repo root (or whatever codebase you are firing against).\n      DO NOT prefix paths with `BDC_XO/` or any other repo name.\n      WRONG: `BDC_XO/.gitattributes`, `BDC_XO/skills/hookify/foo.md`\n      RIGHT: `.gitattributes`, `skills/hookify/foo.md`\n      This applies to: file paths in \"Files to modify\" / \"Files to create\",\n      arguments to grep / git / sed / find / file in \"Grep verification checks\",\n      and any path mentioned in your plan.\n\n      Also: do NOT include placeholder substrings like `<the-file-that-failed>`\n      or `<TBD>` in grep checks. Every check must be a literal, runnable command.\n\n      ## Spec\n      $read-spec.output\n\n  - id: implement\n    persona: major-build\n    depends_on: [plan]\n    task_class: medium_work\n    model: opus[1m]\n    loop:\n      prompt: |\n        Implement the approved plan for this BDC doctrine Work Order.\n\n        ## Spec\n        $read-spec.output\n\n        ## Plan\n        $plan.output\n\n        ## Execution Rules\n        - Modify only the files identified as in scope by the spec.\n        - Preserve all unrelated content; never reformat files outside the target edits.\n        - Do not deploy.\n        - Do not bypass approval gates.\n        - Stop and report a blocker if the spec is ambiguous or a needed source of truth is missing.\n\n        ## Completion Criteria\n        Output <promise>COMPLETE</promise> only when all spec stop conditions that can be run in this checkout have passed, or when a true blocker is identified with evidence.\n      until: COMPLETE\n      max_iterations: 3\n      fresh_context: true\n\n  - id: verify-greps\n    persona: major-build\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      echo \"Running grep verification checks on doctrine edits...\"\n      # Extract grep checks from plan output's \"## Grep verification checks\" section.\n      # Strip markdown code fences (```bash, ```) so they don't get eval'd as commands.\n      CHECKS=$(printf '%s\\n' \"$plan.output\" \\\n        | sed -n '/## Grep verification checks/,/^## /p' \\\n        | sed '1d;$d' \\\n        | grep -v '^[[:space:]]*$' \\\n        | grep -v '^[[:space:]]*```' \\\n        | grep -v '^[[:space:]]*#' \\\n        || true)\n      if [ -z \"$CHECKS\" ]; then\n        echo \"No grep checks defined in plan; skipping detailed verification.\"\n        exit 0\n      fi\n      # Per-check 15s timeout prevents node-level hangs from xargs-with-empty-stdin\n      # or other unbounded commands. Anchor: 2026-05-10 WO-DOCTRINE-GITATTRIBUTES-...\n      # hit a 120s workflow-node timeout because plan output contained\n      # `git ls-files BDC_XO/... | xargs file` which hangs when ls-files returns\n      # empty (CWD is bdc-xo repo root; \"BDC_XO/\" is a Rule 4 violation).\n      FAILED=0\n      EXECUTED=0\n      while IFS= read -r CHECK; do\n        if [ -z \"$CHECK\" ]; then\n          continue\n        fi\n        EXECUTED=$((EXECUTED + 1))\n        echo \"Checking: $CHECK\"\n        if timeout 15 bash -c \"$CHECK\" >/dev/null 2>&1; then\n          echo \"  PASS\"\n        else\n          rc=$?\n          if [ \"$rc\" = \"124\" ]; then\n            echo \"  FAIL (timed out after 15s)\"\n          else\n            echo \"  FAIL (exit $rc)\"\n          fi\n          FAILED=$((FAILED + 1))\n        fi\n      done <<EOF\n      $CHECKS\n      EOF\n      echo \"verify-greps summary: $EXECUTED checks, $FAILED failed\"\n      if [ \"$FAILED\" -gt 0 ]; then\n        echo \"Grep verification failed on $FAILED of $EXECUTED check(s).\" >&2\n        exit 1\n      fi\n\n  - id: decide-push-target\n    persona: major-build\n    depends_on: [verify-greps]\n    task_class: clerical\n    prompt: |\n      Confirm the push target for this BDC doctrine Work Order.\n\n      ## Inputs\n      Spec:\n      $read-spec.output\n\n      ## Doctrine WO Policy (LOCKED)\n      All doctrine-only Work Orders push directly to master.\n      No feature branch. No staging gate. No PR.\n\n      ## Required Output\n      Return:\n      push_target: master\n      pr_required: false\n\n  - id: commit-and-push\n    persona: overseer\n    depends_on: [decide-push-target]\n    bash: |\n      set -euo pipefail\n      echo \"$decide-push-target.output\"\n      # Verify master direct decision\n      if ! printf '%s\\n' \"$decide-push-target.output\" | grep -q '^push_target: master'; then\n        echo \"Push target was not 'master direct'; this workflow applies only to doctrine WOs.\" >&2\n        exit 1\n      fi\n      # Get current branch (should be main or master depending on repo default)\n      CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)\n      echo \"Currently on branch: $CURRENT_BRANCH\"\n\n      git status --short\n      DIRTY=$(git status --porcelain)\n      if [ -z \"$DIRTY\" ]; then\n        # Already fully synced: implement loop committed + pushed to origin\n        if git rev-parse --quiet --verify \"origin/${CURRENT_BRANCH}\" >/dev/null 2>&1 && \\\n           [ \"$(git rev-parse HEAD)\" = \"$(git rev-parse \"origin/${CURRENT_BRANCH}\")\" ]; then\n          echo \"Backstop no-op: implement loop already committed + pushed to origin/${CURRENT_BRANCH} (HEAD matches).\"\n          exit 0\n        fi\n        # WO-HARNESS-COMMIT-AND-PUSH-BACKSTOP-FALSE-NEGATIVE-01: clean tree != no work.\n        # Count commits ahead to distinguish pre-committed work from true no-op.\n        if git rev-parse --quiet --verify \"origin/${CURRENT_BRANCH}\" >/dev/null 2>&1; then\n          COMMITS_AHEAD=$(git rev-list --count \"origin/${CURRENT_BRANCH}..HEAD\" 2>/dev/null || echo 0)\n        else\n          COMMITS_AHEAD=$(git rev-list --count HEAD --not --remotes=origin 2>/dev/null || echo 0)\n        fi\n        if [ \"$COMMITS_AHEAD\" = \"0\" ]; then\n          echo \"No changed files and no commits ahead of origin/${CURRENT_BRANCH} -- implement loop did not produce work\" >&2\n          exit 1\n        fi\n        echo \"Backstop: $COMMITS_AHEAD commit(s) ahead of origin/${CURRENT_BRANCH} -- pushing pre-committed work.\"\n      else\n        # Verify all changed files are in the plan\n        CHANGED_FILES=$(printf '%s\\n' \"$DIRTY\" | awk '{print substr($0,4)}')\n        while IFS= read -r path; do\n          if [ -z \"$path\" ]; then\n            continue\n          fi\n          if ! printf '%s\\n' \"$plan.output\" | grep -F -- \"$path\" >/dev/null; then\n            echo \"Changed file was not listed in plan output: $path\" >&2\n            exit 1\n          fi\n          git add -- \"$path\"\n        done < <(printf '%s\\n' \"$CHANGED_FILES\")\n\n        if git diff --cached --quiet; then\n          echo \"No changes staged; cannot commit.\" >&2\n          exit 1\n        fi\n\n        COMMIT_MESSAGE=$(printf '%s\\n' \"$plan.output\" | sed -n 's/^Commit message: //p' | head -n 1)\n        if [ -z \"$COMMIT_MESSAGE\" ]; then\n          WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n          COMMIT_MESSAGE=\"docs(doctrine): ${WO_ID}\"\n        fi\n\n        git commit -m \"$COMMIT_MESSAGE\"\n      fi\n      git push origin \"$CURRENT_BRANCH\"\n\n  - id: build-manifest\n    persona: major-build\n    depends_on: [commit-and-push]\n    task_class: clerical\n    prompt: |\n      Construct the Captain CI completion manifest for this BDC doctrine Work Order.\n\n      ## Inputs\n      Spec:\n      $read-spec.output\n\n      Plan:\n      $plan.output\n\n      Implementation:\n      $implement.output\n\n      Verification:\n      $verify-greps.output\n\n      ## Required Manifest Fields\n      - WO ID\n      - Files created\n      - Files modified\n      - Grep verification results\n      - Commit SHA\n      - Push target (master direct)\n\n      ## Format\n      Return as a structured block suitable for pasting into the Notion Notes field.\n\n  - id: post-completed\n    persona: xo\n    depends_on: [build-manifest]\n    bash: |\n      set -euo pipefail\n      WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      DETAIL=$(printf '%s\\n' \"$build-manifest.output\" | python -c \"import json,sys; print(json.dumps(sys.stdin.read()))\")\n      curl -sS -X POST \"https://n8n.bluedevilcollectibles.com/webhook/builder-status\" \\\n        -H \"Content-Type: application/json\" \\\n        -d \"{\\\"action\\\":\\\"completed\\\",\\\"wo_id\\\":\\\"${WO_ID}\\\",\\\"builder\\\":\\\"Major Build\\\",\\\"detail\\\":${DETAIL}}\"\n\n  - id: flip-notion\n    persona: xo\n    depends_on: [post-completed]\n    task_class: clerical\n    prompt: |\n      Update the Notion Work Order page if Notion MCP is available in this runtime.\n\n      ## Required Notion Changes\n      - Claude Status: REVIEW\n      - Status: Review\n      - Add the Captain CI manifest as a page comment\n\n      ## Manifest\n      $build-manifest.output\n\n      If Notion MCP is not available inside the container, do not invent a workaround.\n      Output a TODO for XO to flip Notion manually with the manifest above.\n",
  "bdc-feature-development": "name: bdc-feature-development\ndescription: |\n  Use when: Executing a BDC feature-class Work Order from an approved spec.\n  Input: WO_ID in the environment or user message, plus repo context from Overlord.\n  Does: Reads the BDC WO spec, plans surgical implementation, verifies, commits, pushes, opens PRs, and builds a Captain CI manifest.\n  NOT for: Doctrine-only updates, cleanup sweeps, infrastructure deploys, or bug-fix WOs with their own workflow.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n  # Pre-flight: fail closed if container lacks GITHUB auth (WO-169, 2026-05-16).\n  # Anchor: 2026-05-16 engine sortie. Container-side `git push` silently failed\n  # (no GITHUB_TOKEN); downstream `|| echo` swallow patterns let the workflow\n  # report success while no commits reached origin. Catch the auth gap up front.\n  - id: preflight-github-auth\n    persona: major-build\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      if ! git push --dry-run origin \"HEAD:refs/heads/preflight-auth-probe-$(date +%s)\" 2>&1; then\n        echo \"ERROR: GITHUB auth check failed -- container cannot push to origin.\" >&2\n        echo \"Likely cause: GITHUB_TOKEN missing in executor env, or token lacks repo scope.\" >&2\n        exit 1\n      fi\n      echo \"preflight-github-auth PASS\"\n\n  - id: read-spec\n    persona: xo\n    depends_on: [preflight-github-auth]\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-}\"\n      if [ -z \"$WO_ID\" ]; then\n        WO_ID=$(printf '%s\\n' \"$USER_MESSAGE\" | grep -Eo 'WO-[A-Z0-9-]+' | head -n 1 || true)\n      fi\n      if [ -z \"$WO_ID\" ]; then\n        echo \"WO_ID missing. Provide WO_ID in environment or user message.\" >&2\n        exit 1\n      fi\n      # Specs live in bdc-xo (NOT the target codebase). Cross-repo fetch via gh api.\n      # Per YAML Recipe v1 Section 4 / Rule 5: try docs/work-orders/ then docs/superpowers/specs/ glob.\n      SPEC_PATH=\"docs/work-orders/${WO_ID}.md\"\n      SPEC_CONTENT=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 1: docs/superpowers/specs/${WO_ID}.md direct\n        SPEC_PATH=\"docs/superpowers/specs/${WO_ID}.md\"\n        SPEC_CONTENT=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 2: date-prefix glob in docs/superpowers/specs/\n        MATCH=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null | grep \"${WO_ID}.md$\" | head -n 1)\n        if [ -n \"$MATCH\" ]; then\n          SPEC_PATH=\"$MATCH\"\n          SPEC_CONTENT=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --template '{{.content}}' | tr -d '\\n' | base64 -d || true)\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      # Fix-A (2026-05-30, Codex FINDING_1): capture an IMMUTABLE run-start base SHA of the\n      # assigned worktree BEFORE any node mutates/commits/pushes. assert-implement-produced-work\n      # compares HEAD against THIS, so a real build that checkpoint-push already committed+pushed\n      # is still detected as REAL_BUILD (post-push CHANGED/AHEAD are 0 and would misread otherwise).\n      WT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)\n      RUN_START_SHA=$(git -C \"$WT\" rev-parse HEAD 2>/dev/null || echo unknown)\n      mkdir -p \"$ARTIFACTS_DIR\" 2>/dev/null || true\n      printf '%s\\n' \"$RUN_START_SHA\" > \"$ARTIFACTS_DIR/run-start-sha.txt\" 2>/dev/null || true\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:%s\\nRUN_START_SHA=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\" \"$RUN_START_SHA\"\n      printf '%s\\n' \"$SPEC_CONTENT\"\n\n  # check-already-satisfied (Fix B, 2026-05-30 -- idempotency short-circuit).\n  # Cheap read-only check BEFORE plan/implement: are the WO's deliverables ALREADY\n  # present in THIS worktree's HEAD? If clearly yes, route to the ALREADY_SATISFIED\n  # terminal (success-no-op) instead of burning a full ~7min implement pass on a\n  # re-fire of an already-done WO (anchor 04daeafc). FAILS OPEN: if unsure, proceeds to\n  # the normal build path -- assert-implement-produced-work (Fix A) is the real safety\n  # net, this is only an optimization that must never wrongly skip real work.\n  - id: check-already-satisfied\n    persona: war-council-architect\n    agent: war-council-architect\n    depends_on: [read-spec]\n    task_class: clerical\n    prompt: |\n      You decide ONE thing: are this WO's deliverables ALREADY fully present in the\n      CURRENT worktree's HEAD, such that a build would produce no changes? You are\n      READ-ONLY. Judge ONLY against this worktree (you are cd'd into it; use plain git\n      and Read/Grep/Glob on local files). Do NOT consult origin/main or other worktrees.\n\n      ## Spec\n      $read-spec.output\n\n      ## How to decide\n      - Extract the spec's Stop Point / verification commands / grep assertions / named\n        deliverable files.\n      - Check whether those deliverables ALREADY exist and satisfy the assertions IN THIS\n        worktree (the files exist with the required content; grep assertions already pass).\n      - This is a HIGH BAR. Only conclude already-satisfied if you are confident a fresh\n        implementation would change nothing. ANY doubt -> proceed (false).\n\n      ## Output (exactly one of these on its own last line)\n      - If every deliverable is already present and satisfied in this worktree:\n          ALREADY_SATISFIED=true\n        followed by: SATISFIED_EVIDENCE=<one line naming the deliverables + how verified>\n      - Otherwise (work is needed, OR you cannot confirm with high confidence):\n          ALREADY_SATISFIED=false\n      Default to false. Skipping a build that was actually needed is far worse than\n      running a redundant build that the downstream no-op guard will catch.\n\n  # gate-already-satisfied: bash gate that reads check-already-satisfied's verdict and\n  # records BUILD_OUTCOME for the terminal nodes. If ALREADY_SATISFIED=true, the plan ->\n  # implement -> review chain still runs but will legitimately find no diff; flip-notion\n  # records verified-no-op. (We do NOT hard-skip nodes here -- the executor has no goto;\n  # instead the outcome is recorded and the no-op flows through the existing chain, which\n  # now distinguishes ALREADY_SATISFIED from FALSE_COMPLETE via assert-implement-produced-work.)\n  - id: gate-already-satisfied\n    depends_on: [check-already-satisfied]\n    bash: |\n      set -uo pipefail\n      CAS=$check-already-satisfied.output\n      if printf '%s\\n' \"$CAS\" | grep -qE '^ALREADY_SATISFIED=true'; then\n        echo \"ALREADY_SATISFIED=true\"\n        echo \"PRECHECK_VERDICT=already-satisfied\"\n        printf '%s\\n' \"$CAS\" | grep '^SATISFIED_EVIDENCE=' | head -1 || true\n        echo \"Note: WO appears already implemented in this worktree; the build chain will\"\n        echo \"produce no diff and the run will resolve as a verified no-op (not a failure).\"\n      else\n        echo \"ALREADY_SATISFIED=false\"\n        echo \"PRECHECK_VERDICT=needs-build\"\n      fi\n      exit 0\n\n  - id: plan\n    persona: war-council-architect\n    depends_on: [gate-already-satisfied]\n    agent: war-council-architect\n    task_class: medium_work\n    prompt: |\n      Read the BDC Work Order spec below and produce a concise implementation plan.\n\n      ## Idempotency note\n      Pre-check verdict (from gate-already-satisfied): $gate-already-satisfied.output\n      If PRECHECK_VERDICT=already-satisfied, the deliverables already exist in this\n      worktree. Produce a minimal plan that VERIFIES the existing state rather than\n      re-creating it; implement will correctly make no changes and emit ALREADY_SATISFIED.\n\n      ## Required Output\n      Return structured Markdown with these headings:\n      - WO ID\n      - Files to modify\n      - Files to create\n      - Files explicitly out of scope\n      - Verification commands\n      - Commit message\n      - Push policy\n      - Staging gate required\n\n      ## BDC Push Policy\n      Hardcoded Rule 20 staging-gate repos:\n      - bluedevilcollectibles/lspro-react\n      - bluedevilcollectibles/shopops-storefront\n      - bluedevilcollectibles/shopops\n\n      For those repos, use a feature branch plus PR and require the staging gate.\n      For bdc-xo non-doctrine code and any other repo, use feature branch plus PR without staging gate.\n      Doc-only and doctrine-only WOs are out of scope for this workflow.\n\n      ## Spec\n      $read-spec.output\n\n  # plan-review: in-run self-repair loop (doctrine: self-repair-review-loops +\n  # in-run-review-loops-feature-dev, 2026-05-30). war-council-architect re-reviews and\n  # REVISES its own plan until clean or 2 repair attempts, then escalates. This is a\n  # SELF-CONTAINED loop (the executor has no cross-node loop-back); the initial plan\n  # comes from `plan`, the review-and-revise happens here. Output is the final approved\n  # plan that `implement` consumes. NOT a human gate -- humans are reached only via the\n  # async escalation payload, and only at the irreversible-action boundary downstream.\n  - id: plan-review\n    persona: war-council-architect\n    agent: war-council-architect\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      # Success-only sentinel. The war-council-architect persona is READ-ONLY\n      # (tools: [Read,Grep,Glob,WebFetch] -- no Write), so this loop is STDOUT-ONLY:\n      # no until_bash, no findings file. Completion = the architect emits the\n      # PLAN_REVIEW_APPROVED token, which it is instructed to emit ONLY when the plan\n      # truly passes (never on a failing/escalation iteration). On the final iteration\n      # without approval, the loop exhausts and the run fails closed (escalation in output).\n      until: PLAN_REVIEW_APPROVED\n      max_iterations: 3\n      prompt: |\n        You are the War Council Architect doing an adversarial self-review of the\n        implementation plan, then REVISING it in place. Iteration-driven self-repair:\n        one initial pass + up to 2 repair attempts. You are READ-ONLY (no file writes,\n        no shell) -- everything you produce goes to STDOUT.\n\n        ## Prior iteration (read FIRST)\n        Your previous iteration's full output (empty on the first pass):\n        $LOOP_PREV_OUTPUT\n        If it contains findings (PLAN_REVIEW=needs_revision), you MUST resolve every one\n        in the revised plan this iteration. If a finding repeats and you cannot resolve\n        it, escalate (below) instead of looping again.\n\n        ## Inputs\n        Original plan: $plan.output\n        Spec: $read-spec.output\n\n        ## Attack the plan on these dimensions\n        1. Scope creep -- any file not justified by the spec.\n        2. Missing files -- in-scope work the plan omits.\n        3. Unsafe assumptions -- \"assume X exists\" without verification.\n        4. Rule 20 staging gate -- if target repo is lspro-react / shopops-storefront /\n           shopops, plan MUST require --base staging. If not those, must NOT.\n        5. Out-of-scope list present and correct.\n\n        ## Self-repair (the point -- do not just report)\n        If you find issues AND they are plan-level fixable, REVISE the plan to resolve\n        them and emit the corrected plan in full. Do not halt for a human on anything\n        you can fix here.\n\n        ## Required STDOUT format (this is the only handoff -- emit EXACTLY this shape)\n        The blocks are fenced so downstream nodes consume ONLY the approved plan, never\n        your critique. Emit, in this order:\n\n          PLAN_REVIEW_PASS=<true|false>\n          PLAN_REVIEW_RISK=<LOW|MEDIUM|HIGH>\n\n          === APPROVED_PLAN_BEGIN ===\n          <the FINAL revised plan in full -- this and ONLY this is what implement uses>\n          === APPROVED_PLAN_END ===\n\n          === PLAN_REVIEW_FINDINGS_BEGIN ===\n          <your findings / what you changed and why, or \"(none)\">\n          === PLAN_REVIEW_FINDINGS_END ===\n\n        The APPROVED_PLAN block must always contain a complete, usable plan (even on a\n        passing first iteration where you changed nothing -- echo the original plan there).\n        Keep ALL critique inside the FINDINGS block; never let critique leak into the\n        APPROVED_PLAN block.\n\n        ## Completion sentinel (success ONLY)\n        If and ONLY if PLAN_REVIEW_PASS=true (scope contained, no in-scope file missing,\n        Rule 20 gate correct), emit on its OWN LINE as the LAST line:\n          PLAN_REVIEW_APPROVED\n        On a failing iteration emit instead (never the approved token):\n          PLAN_REVIEW_NEEDS_REVISION\n        Emitting PLAN_REVIEW_APPROVED is the gate that lets the run proceed to implement,\n        so emit it ONLY when PLAN_REVIEW_PASS=true.\n\n        ## Escalation (emit instead of approving; do NOT emit the approved sentinel)\n        If the spec is ambiguous/contradictory, the fix needs a new side-effect class or\n        ungranted scope, or the same finding survived a prior attempt and you cannot\n        resolve it -- set PLAN_REVIEW_PASS=false, emit PLAN_REVIEW_NEEDS_REVISION, and put\n        this block inside the FINDINGS block:\n          ESCALATION_REQUIRED=true\n          ESCALATION_REASON=<ambiguous-spec|new-side-effect-class|loop-exhausted>\n          WO_ID=<id>\n          NODE=plan-review\n          ATTEMPTS=<n>/3\n          WHAT_FAILED=<one paragraph>\n          WHAT_WAS_TRIED=<3-6 bullets>\n          LAST_REVIEW_FINDINGS=<exact bullets>\n          SINGLE_DECISION_NEEDED=<one sentence John can answer>\n          SAFE_STATE=<nothing committed/pushed; plan stage only>\n\n  - id: implement\n    persona: major-build\n    depends_on: [plan-review]\n    agent: major-build\n    task_class: medium_work\n    loop:\n      prompt: |\n        Implement the approved plan for this BDC feature Work Order.\n\n        ## Spec\n        $read-spec.output\n\n        ## Plan (approved by plan-review)\n        Use ONLY the text between the === APPROVED_PLAN_BEGIN === and\n        === APPROVED_PLAN_END === markers below as the plan. IGNORE everything outside\n        those markers (verdict headers, the PLAN_REVIEW_FINDINGS block, sentinels) --\n        that is reviewer metadata, not the plan.\n        $plan-review.output\n\n        ## WORKTREE PINNING (Fix A, 2026-05-30 -- anchor run 04daeafc)\n        You run inside ONE assigned git worktree. ALL of your work and ALL of your\n        completion judgments MUST happen inside it. Resolve it first:\n          WT=$(git rev-parse --show-toplevel)\n        Then:\n        - Make EVERY git/grep/diff/log call relative to THIS worktree (you are already\n          cd'd into it; use plain `git ...`, or `git -C \"$WT\" ...`).\n        - You MUST NOT read, grep, `git -C`, or judge \"already done\" against any OTHER\n          location: not the shared `source/` checkout, not `origin/main`, not another\n          thread's worktree (e.g. `.../worktrees/archon/thread-<other>`). Whether the WO\n          is satisfied is decided ONLY by the state of THIS worktree's HEAD + working tree.\n        - If you believe the WO is already implemented, that conclusion is only valid if\n          it is true IN THIS WORKTREE. If the work exists only on origin/main or another\n          thread, it is NOT present here -- treat the WO as needing implementation here,\n          OR emit `ALREADY_SATISFIED` (see Completion) only after confirming the\n          deliverables are reachable from THIS worktree's HEAD.\n        Anchor: run 04daeafc judged \"already done\" against a sibling worktree + origin/main,\n        made an empty verify-only commit on the wrong thread, and emitted a fraudulent\n        COMPLETE. The review pipeline inspected THIS worktree (correctly empty) and blocked.\n\n        ## Execution Rules\n        - Modify only the files identified as in scope by the spec.\n        - Prefer existing repo patterns and helpers.\n        - Do not reformat unrelated code.\n        - Do not deploy.\n        - Do not bypass approval gates.\n        - Do NOT use `git commit --allow-empty` or any empty/verify-only commit to\n          simulate completion. A commit must contain real, in-scope file changes.\n        - Stop and report a blocker if the spec is ambiguous or a needed source of truth is missing.\n\n        ## Commit-and-push BEFORE exit (Patch 2, 2026-05-16)\n        When your implementation is complete, BEFORE emitting the sentinel below, run:\n\n        ```bash\n        BRANCH=$(git rev-parse --abbrev-ref HEAD)\n        # Re-derive WO_ID locally (don't interpolate spec output - quote-safety)\n        WO_ID=\"${WO_ID:-$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'WO-[A-Z0-9-]+' | head -n 1 || true)}\"\n        git add -A\n        git diff --cached --quiet || git commit -m \"feat: implement ${WO_ID}\n\n        Implementation per WO spec. Self-verified within implement loop.\n        Workflow-time verify-* removed 2026-05-16 (Patch 1); PR-time gates handle validation.\n\n        Co-Authored-By: Claude (Cauldron major-build) <noreply@anthropic.com>\"\n        git push -u origin \"$BRANCH\" 2>&1 || echo \"WARN: push failed - operator must recover from worktree\"\n        ```\n\n        Why: workflow-time verify-* gates were removed (Patch 1). Even with them gone, if any\n        downstream node fails (sentinel mismatch, war-council-validator timeout, etc.), the\n        traditional commit-and-push node may be skipped. Committing + pushing HERE makes your\n        work durable regardless of what happens after this iteration ends.\n\n        Worst case: the downstream commit-and-push node runs and is a no-op because HEAD is\n        already pushed. Best case: your work is on origin even if everything else fails.\n\n        ## Completion Criteria + Sentinel Emission (Patch 3, 2026-05-16; Fix A 2026-05-30)\n        Before emitting any completion sentinel, run this self-check IN THIS WORKTREE:\n          WT=$(git rev-parse --show-toplevel)\n          CHANGED=$(git -C \"$WT\" status --porcelain | wc -l)\n          AHEAD=$(git -C \"$WT\" rev-list --count \"@{upstream}..HEAD\" 2>/dev/null \\\n                  || git -C \"$WT\" rev-list --count HEAD --not --remotes 2>/dev/null || echo 0)\n        Then choose EXACTLY ONE ending:\n\n        1. REAL BUILD (you made in-scope changes): only if CHANGED>0 OR AHEAD>0 with real\n           file changes. Emit, each on its own line:\n             COMPLETE\n             BUILD_COMPLETE\n             <promise>COMPLETE</promise>\n\n        2. ALREADY SATISFIED (the WO's deliverables are ALREADY reachable from THIS\n           worktree's HEAD and you correctly made no changes): emit BOTH lines, each on\n           its own line:\n             ALREADY_SATISFIED: <one sentence: which deliverables already exist in this worktree HEAD>\n             COMPLETE\n           The COMPLETE line is REQUIRED here ONLY to let the until:COMPLETE loop exit -- do\n           NOT also emit BUILD_COMPLETE or <promise>COMPLETE</promise>, and do NOT make an\n           empty commit. assert-implement-produced-work re-checks the worktree: because it\n           is genuinely clean, it reclassifies this to ALREADY_SATISFIED (a verified no-op,\n           NOT a fraudulent build). This is the SINGLE no-op completion contract -- see also\n           the \"Loop exit\" section below, which says the same thing.\n\n        3. TRUE BLOCKER (spec ambiguous / missing source of truth / cannot proceed):\n             BLOCKED: <one-sentence reason>\n\n        ANTI-FRAUD RULE: you may emit the case-1 BUILD sentinels (BUILD_COMPLETE +\n        <promise>COMPLETE</promise>) ONLY if you made real in-scope edits (CHANGED>0 OR\n        AHEAD>0). If you did no work because it was already done, that is case 2\n        (ALREADY_SATISFIED: + a bare COMPLETE to exit the loop), NOT case 1 -- do not claim\n        a build you did not do. The downstream guard re-checks the worktree and will catch a\n        bare COMPLETE with no ALREADY_SATISFIED and no real changes as FALSE_COMPLETE.\n\n        Reason: BDC YAMLs have used different sentinel strings over time. Emitting all three\n        case-1 sentinels guarantees the loop's `until:` matcher finds one.\n\n        ## Loop exit -- the no-op contract (stated once; matches case 2 above)\n        The loop's `until:` matches COMPLETE. A real build emits the three case-1 sentinels.\n        A genuine no-op (already satisfied in THIS worktree) emits exactly:\n          ALREADY_SATISFIED: <which deliverables already exist in this worktree HEAD>\n          COMPLETE\n        That bare COMPLETE is what lets the loop exit; assert-implement-produced-work sees\n        the clean worktree + the ALREADY_SATISFIED line and reclassifies to ALREADY_SATISFIED\n        (verified no-op, not a fraudulent build). NEVER emit COMPLETE if you simply failed to\n        do needed work (no ALREADY_SATISFIED line + clean worktree => FALSE_COMPLETE).\n      until: COMPLETE\n      max_iterations: 5\n      fresh_context: true\n\n  # NOTE - verify-build / verify-types / verify-tests removed 2026-05-16 (Patch 1 of Cauldron friction sweep).\n  # Rationale: those nodes consistently killed valid implementation work on pre-existing harness rot\n  # (15+ @archon/paths failures, npm-not-found before bun sweep, scope-too-wide tsc) rather than catching\n  # real WO-introduced bugs. The implement loop now self-verifies (Rule 19 of YAML Recipe v1) and commits\n  # its own work before exit (Patch 2). Downstream PR-time gates catch real bugs at human-readable layer:\n  #   - codex-review (adversarial schema/code)\n  #   - claude-review (doctrine/architecture)\n  #   - Gitleaks (secret scan)\n  #   - e2e-smoke (CI integration test)\n  #   - Captain CI manifest contract check\n  # Workflow-time verify-* was redundant with PR-time review and high-cost-low-value. See also: WO-149\n  # (verify-scope-to-changed-files) and WO-146 (npm->bun sweep) for the deeper structural fixes.\n\n  # Cauldron resilience: commit + push WIP branch after implement loop exits.\n  # Best-effort only -- failure logged and silently tolerated (NOT load_bearing).\n  # If container restarts after this node but before backstop, work survives on origin.\n  # If nothing changed since last checkpoint, exits 0 silently.\n  # Anchor: 2026-05-24 run 07415e9f lost local commit when container restarted before backstop.\n  # WO-HARNESS-IMPLEMENT-CHECKPOINT-PUSH-01\n  - id: checkpoint-push\n    persona: overseer\n    depends_on: [implement]\n    bash: |\n      set -uo pipefail\n\n      WO_ID=\"${WO_ID:-unknown}\"\n      WIP_BRANCH=\"wip/${WO_ID}-$(date +%Y%m%d%H%M%S)\"\n\n      git add -A 2>/dev/null || true\n\n      if git diff --cached --quiet 2>/dev/null; then\n        echo \"checkpoint-push: nothing staged -- no commit needed\"\n        exit 0\n      fi\n\n      git commit -m \"wip: checkpoint after implement loop -- ${WO_ID}\n\n      Cauldron resilience checkpoint. Not the final commit.\n      Will be superseded by backstop commit-and-push node.\n\n      Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>\" \\\n        2>/dev/null || { echo \"WARN: checkpoint commit failed -- continuing\" >&2; exit 0; }\n\n      if git push -u origin \"HEAD:${WIP_BRANCH}\" 2>/dev/null; then\n        echo \"checkpoint-push: WIP branch pushed -- ${WIP_BRANCH}\"\n        echo \"CHECKPOINT_BRANCH=${WIP_BRANCH}\"\n        echo \"CHECKPOINT_SHA=$(git rev-parse HEAD)\"\n      else\n        echo \"WARN: checkpoint push failed -- work is local only; backstop will retry\" >&2\n      fi\n      exit 0\n\n  # assert-implement-produced-work (Fix A guard, 2026-05-30 -- anchor run 04daeafc).\n  # Distinguishes three post-implement states from the ASSIGNED worktree's own state:\n  #   REAL_BUILD       -- worktree has changes / commits ahead -> proceed to review chain\n  #   ALREADY_SATISFIED-- implement said ALREADY_SATISFIED + worktree genuinely clean -> emit\n  #                       BUILD_OUTCOME=ALREADY_SATISFIED so flip-notion records verified-no-op\n  #   FALSE_COMPLETE   -- implement emitted COMPLETE/BUILD_COMPLETE but worktree shows ZERO\n  #                       real changes (the fraud 04daeafc committed) -> emit\n  #                       BUILD_OUTCOME=FALSE_COMPLETE (the commit-and-push gate turns this\n  #                       into a PAUSE, not a hard fail -- see Fix D).\n  # Load-bearing: this is the truth check that the implement self-report cannot bypass.\n  - id: assert-implement-produced-work\n    persona: overseer\n    depends_on: [checkpoint-push]\n    load_bearing: true\n    bash: |\n      set -uo pipefail\n      IMPL=$implement.output\n      WT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)\n      # Codex FINDING_1 fix: compare against the IMMUTABLE run-start SHA (captured by\n      # read-spec BEFORE checkpoint-push committed+pushed), NOT post-push upstream state.\n      # After checkpoint-push, @{upstream}..HEAD is 0 even for a real build -- that false-\n      # negative is what would have misrouted every legitimate build to BLOCKED.\n      BASE=$(cat \"$ARTIFACTS_DIR/run-start-sha.txt\" 2>/dev/null || echo \"\")\n      HEAD_NOW=$(git -C \"$WT\" rev-parse HEAD 2>/dev/null || echo \"\")\n      CHANGED=$(git -C \"$WT\" status --porcelain 2>/dev/null | grep -c . || echo 0)\n      # commits since run start (real work this run produced, even after a checkpoint push)\n      if [ -n \"$BASE\" ] && [ \"$BASE\" != \"unknown\" ] && [ -n \"$HEAD_NOW\" ]; then\n        AHEAD=$(git -C \"$WT\" rev-list --count \"${BASE}..HEAD\" 2>/dev/null || echo 0)\n      else\n        # fallback: no base captured -> use any-uncommitted OR diff vs upstream\n        AHEAD=$(git -C \"$WT\" rev-list --count \"@{upstream}..HEAD\" 2>/dev/null || echo 0)\n      fi\n      HAS_WORK=false\n      if [ \"${CHANGED:-0}\" -gt 0 ] || [ \"${AHEAD:-0}\" -gt 0 ]; then HAS_WORK=true; fi\n\n      SAID_COMPLETE=false\n      if printf '%s\\n' \"$IMPL\" | grep -qE '^(COMPLETE|BUILD_COMPLETE)$|<promise>COMPLETE</promise>'; then SAID_COMPLETE=true; fi\n      SAID_ALREADY=false\n      if printf '%s\\n' \"$IMPL\" | grep -qE '^ALREADY_SATISFIED:'; then SAID_ALREADY=true; fi\n\n      echo \"WORKTREE=$WT\"\n      echo \"CHANGED_FILES=$CHANGED\"\n      echo \"COMMITS_AHEAD=$AHEAD\"\n      echo \"IMPL_SAID_COMPLETE=$SAID_COMPLETE\"\n      echo \"IMPL_SAID_ALREADY_SATISFIED=$SAID_ALREADY\"\n\n      if [ \"$HAS_WORK\" = \"true\" ]; then\n        echo \"BUILD_OUTCOME=REAL_BUILD\"\n        echo \"assert-implement-produced-work: real changes present -- proceeding to review chain\"\n        exit 0\n      fi\n\n      # No work in this worktree.\n      # Codex FINDING_3 interaction: implement now emits BOTH \"ALREADY_SATISFIED:\" AND\n      # COMPLETE in the genuine no-op case (COMPLETE is required to exit the until:COMPLETE\n      # loop). So ALREADY_SATISFIED WINS whenever the agent declared it AND the worktree is\n      # genuinely clean -- the clean worktree is the truth check that makes this non-fraudulent.\n      if [ \"$SAID_ALREADY\" = \"true\" ]; then\n        echo \"BUILD_OUTCOME=ALREADY_SATISFIED\"\n        echo \"assert-implement-produced-work: implement reported ALREADY_SATISFIED and worktree is genuinely clean -- legitimate no-op\"\n        exit 0\n      fi\n\n      # No work, and implement did NOT declare already-satisfied, yet it claimed COMPLETE\n      # (or emitted nothing usable): false/fraudulent completion.\n      echo \"BUILD_OUTCOME=FALSE_COMPLETE\"\n      echo \"ERROR: implement emitted COMPLETE but produced ZERO real changes in the assigned worktree (CHANGED=$CHANGED AHEAD=$AHEAD).\" >&2\n      echo \"This is a false/fraudulent completion (anchor 04daeafc). Downstream gate will PAUSE for correction, not commit.\" >&2\n      exit 0\n\n  - id: war-council-validator\n    persona: captain-ci-validator\n    depends_on: [assert-implement-produced-work]\n    task_class: medium_work\n    prompt: |\n      Invoke or emulate the war-council-validator review for this Work Order.\n\n      ## Validator Agent\n      war-council-validator\n\n      ## Inputs\n      Spec:\n      $read-spec.output\n\n      Plan (approved -- reviewed + revised by plan-review):\n      $plan-review.output\n\n      Implementation output:\n      $implement.output\n\n      Git diff:\n      Run `git diff --stat` and `git diff --check`, then inspect the relevant changed files.\n\n      ## Required Output\n      Return either:\n      - satisfied: with evidence that the spec deliverables and stop conditions were met\n      - needs_revision: with exact file/path/behavior findings\n\n  # diff-review chain (doctrine: self-repair-review-loops + in-run-review-loops-feature-dev,\n  # 2026-05-30). Runs AFTER validation, BEFORE the irreversible commit-and-push. Three nodes,\n  # executor-honest: Codex is read-only and CANNOT write files; until_bash CANNOT prove a\n  # semantic Codex objection was fixed. So: codex reviews (stdout only) -> major-build repairs\n  # (reads codex stdout in-prompt, until_bash checks MECHANICAL invariants only) -> codex\n  # RE-REVIEWS for real and decides. Cross-model: Codex attacks Claude-authored code.\n  # capture-diff: the Codex reviewer persona is read-only [Read,Grep,Glob] with NO Bash,\n  # so it cannot run `git diff` itself (F5 fix). This bash node captures the full feature\n  # diff (merge-base..HEAD plus any unstaged working-tree changes) to an artifact the\n  # Codex review prompts read. Emits provenance so the reviewer knows exactly what it saw.\n  - id: capture-diff\n    depends_on: [war-council-validator]\n    bash: |\n      set -uo pipefail\n      mkdir -p \"$ARTIFACTS_DIR\"\n      OUT=\"$ARTIFACTS_DIR/diff.patch\"\n      BASE_REF=\"origin/${BASE_BRANCH:-main}\"\n      # NF2: when there is no merge-base and no HEAD~1 (single-commit / no-base repo),\n      # fall back to the empty-tree hash, NOT HEAD. HEAD..HEAD is an EMPTY diff (reviewer\n      # sees nothing); empty-tree..HEAD shows all committed content as added (meaningful).\n      EMPTY_TREE=$(git hash-object -t tree /dev/null 2>/dev/null || echo 4b825dc642cb6eb9a060e54bf8d69288fbee4904)\n      MB=$(git merge-base HEAD \"$BASE_REF\" 2>/dev/null || git rev-parse HEAD~1 2>/dev/null || echo \"$EMPTY_TREE\")\n      [ -n \"$MB\" ] || MB=\"$EMPTY_TREE\"\n      {\n        echo \"### git diff --stat ($MB..HEAD)\"\n        git diff --stat \"$MB\"..HEAD 2>/dev/null || true\n        echo\n        echo \"### git diff --name-only ($MB..HEAD)\"\n        git diff --name-only \"$MB\"..HEAD 2>/dev/null || true\n        echo\n        echo \"### committed diff ($MB..HEAD)\"\n        git diff \"$MB\"..HEAD 2>/dev/null || true\n        echo\n        echo \"### unstaged working-tree diff (HEAD..worktree)\"\n        git diff HEAD 2>/dev/null || true\n      } > \"$OUT\"\n      echo \"DIFF_BASE=$MB\"\n      echo \"DIFF_COMMAND=git diff $MB..HEAD (+ git diff HEAD for unstaged)\"\n      echo \"DIFF_ARTIFACT=$OUT\"\n      echo \"DIFF_BYTES=$(wc -c < \"$OUT\" 2>/dev/null || echo 0)\"\n\n  - id: diff-review\n    provider: codex\n    agent: codex-adversarial-reviewer\n    depends_on: [capture-diff]\n    task_class: medium_work\n    prompt: |\n      You are the Codex adversarial diff reviewer. Cross-model review of the diff that\n      major-build just produced, BEFORE it is committed. You are READ-ONLY: you inspect\n      and report. You do NOT edit, write, or run commands that mutate state.\n\n      ## Inputs\n      Spec: $read-spec.output\n      Validator verdict: $war-council-validator.output\n      Diff provenance: $capture-diff.output\n      (DIFF_BASE, DIFF_COMMAND, DIFF_ARTIFACT above tell you exactly what diff to read.)\n\n      ## Inspect the ACTUAL diff (read-only)\n      READ the captured diff at the DIFF_ARTIFACT path from $capture-diff.output (it is\n      $ARTIFACTS_DIR/diff.patch). That file IS the diff -- you have no Bash tool, so do\n      NOT try to run git yourself. Also use Read/Grep/Glob on the changed files (listed\n      under \"### git diff --name-only\" in the artifact) for fuller context. Focus on what\n      tests + the validator can miss:\n      1. Contract drift -- response shapes, column counts, export schemas, API signatures\n         changed in a way the spec did not authorize.\n      2. Unverified claims -- code asserts behavior that no test actually exercises.\n      3. Unsafe customer/billing/export behavior -- e.g. an export path that could emit a\n         customer-reserved unit, a billing write without an idempotency guard.\n      4. Missing tests -- in-scope behavior with no test asserting real output (not just\n         \"ran without throwing\").\n\n      ## Output (STDOUT only -- you cannot write files)\n      Emit exactly:\n        DIFF_REVIEW=<satisfied|needs_revision>\n        FINDINGS:\n        - <file:line> <one specific, fixable finding>   (repeat; or \"(none)\")\n        SEMANTIC_RISK=<LOW|MEDIUM|HIGH>\n      Be specific: every finding cites a file and the exact problem. \"Looks fine\" is not\n      acceptable; \"could be better\" is not a finding. Only flag what must change before commit.\n\n  - id: diff-repair\n    persona: major-build\n    agent: major-build\n    depends_on: [diff-review]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      # NF3 + schema fix: the loader REQUIRES loop.until (schemas/loop.ts:11,\n      # z.string().min(1)) -- omitting it makes the DAG invalid and the loader\n      # SILENTLY DROPS the whole workflow (anchor: 2026-05-30 the entire lane went\n      # unregistered). Completion is `sentinel OR until_bash`. To keep until_bash the\n      # SOLE real authority (so a premature text sentinel can't short-circuit the\n      # flag+debug gate), `until:` is set to a sentinel the agent is instructed to\n      # NEVER emit. Thus only until_bash (done.flag exists AND no debug prints in the\n      # feature diff) can complete the loop.\n      until: __NEVER_EMIT_USE_UNTIL_BASH__\n      max_iterations: 3\n      until_bash: |\n        set -uo pipefail\n        # MECHANICAL invariants ONLY. This does NOT prove Codex is satisfied --\n        # diff-review-final (a real re-review) does that. Complete when the repair node\n        # signalled done AND the feature diff has no obvious mechanical defects.\n        DONE=\"$ARTIFACTS_DIR/diff-repair-done.flag\"\n        [ -f \"$DONE\" ] || { echo \"repair not signalled done\"; exit 1; }\n        # F4: implement already committed, so `git diff --cached` is empty. Check the\n        # real feature diff: merge-base..HEAD (committed) PLUS HEAD..worktree (unstaged).\n        # NF2: empty-tree fallback (not HEAD) so a single-commit/no-base repo still\n        # produces a non-empty committed diff to scan for debug prints.\n        BASE_REF=\"origin/${BASE_BRANCH:-main}\"\n        EMPTY_TREE=$(git hash-object -t tree /dev/null 2>/dev/null || echo 4b825dc642cb6eb9a060e54bf8d69288fbee4904)\n        MB=$(git merge-base HEAD \"$BASE_REF\" 2>/dev/null || git rev-parse HEAD~1 2>/dev/null || echo \"$EMPTY_TREE\")\n        [ -n \"$MB\" ] || MB=\"$EMPTY_TREE\"\n        DEBUG_RE='console\\.log\\(|debugger;|binding\\.pry|import pdb'\n        if { git diff --unified=0 \"$MB\"..HEAD 2>/dev/null; git diff --unified=0 HEAD 2>/dev/null; } \\\n             | grep -E '^\\+' | grep -Eq \"$DEBUG_RE\"; then\n          echo \"debug statement present in feature diff\"; exit 1\n        fi\n        exit 0\n      prompt: |\n        You are major-build. The Codex diff reviewer raised findings on your diff.\n        Fix every fixable finding in place. You are the BUILDER; Codex does not edit.\n\n        ## Codex review verdict (read directly -- prompt substitution, not shell)\n        $diff-review.output\n\n        ## Prior repair iteration (if any)\n        $LOOP_PREV_OUTPUT\n\n        ## What to do\n        - If DIFF_REVIEW=satisfied with no findings: make no changes; just signal done.\n        - Otherwise: resolve each FINDINGS line. Add the missing test, fix the contract\n          drift, close the unsafe path. Modify only in-scope files. Do not reformat\n          unrelated code. Do not deploy.\n\n        ## NEVER FABRICATE A DIFF (Fix C, 2026-05-30 -- anchor run 04daeafc)\n        If the captured diff is empty / header-only because the WO appears already\n        implemented on origin/main or in other commits, you MUST NOT regenerate or\n        reconstruct $ARTIFACTS_DIR/diff.patch (or diff-final.patch) from those\n        already-merged commits, from origin/main, or from any other branch/worktree.\n        The diff artifact must reflect ONLY real, uncommitted-or-this-worktree changes.\n        Manufacturing a reviewable artifact from already-merged work is fraud -- it tricks\n        the final reviewer into certifying changes this run did not make.\n        If the diff is genuinely empty because there is nothing to repair (the WO is\n        already satisfied or implement produced no changes), do NOT write the done.flag\n        and do NOT fabricate -- instead write the escalation block (below) with\n        ESCALATION_REASON=already-implemented-no-diff and STOP. diff-review-final will\n        see the empty diff and the run will pause for correction (not silently pass).\n        Anchor: run 04daeafc's diff-repair tried to \"regenerate diff.patch with the actual\n        implementation content from those commits\" -- exactly the fraud this rule forbids.\n\n        ## Escalation (ONLY these -- else fix and let the loop continue)\n        Write the escalation block to $ARTIFACTS_DIR/diff-repair-escalation.txt and STOP if:\n        - a finding requires a new side-effect class / scope the lane was not granted\n        - a finding requires human-only authority (deploy, prod write, customer comms,\n          irreversible/destructive op) where safe behavior is not already encoded\n        - the spec is ambiguous about the correct fix\n        - the same finding survived a prior repair attempt and you cannot resolve it\n        Escalation block (exact keys):\n          ESCALATION_REQUIRED=true\n          ESCALATION_REASON=<new-side-effect-class|human-only-authority|ambiguous-spec|loop-exhausted|already-implemented-no-diff>\n          WO_ID=<id>\n          NODE=diff-repair\n          ATTEMPTS=<n>/3\n          WHAT_FAILED=<one paragraph>\n          WHAT_WAS_TRIED=<3-6 bullets>\n          LAST_REVIEW_FINDINGS=<exact Codex findings>\n          SINGLE_DECISION_NEEDED=<one sentence John can answer>\n          SAFE_STATE=<work is local in the worktree; nothing committed or pushed yet>\n\n        ## Signal done (the FLAG is the only gate -- there is NO completion token)\n        The loop completes ONLY via until_bash, which checks that the done.flag exists\n        AND the feature diff has no debug prints. There is NO text sentinel that finishes\n        this loop (loop.until is set to a token you must NEVER emit) -- do not try to\n        emit one. When you have addressed every fixable finding (or there were none) AND\n        introduced no debug prints:\n          - Write an empty file at $ARTIFACTS_DIR/diff-repair-done.flag   (the ONLY signal)\n        If you escalated or could not resolve a finding, do NOT write the flag: the loop\n        will exhaust max_iterations and fail closed, and diff-review-final adjudicates the\n        unresolved state. The flag means \"repair pass complete\", not \"semantically\n        certified\" -- diff-review-final re-checks semantics next.\n\n  # capture-diff-final: refresh the diff artifact AFTER repair so the final reviewer sees\n  # the repaired tree, not the pre-repair diff. trigger_rule all_done so it runs even if\n  # diff-repair failed/exhausted (F6) -- the final reviewer must still adjudicate.\n  - id: capture-diff-final\n    depends_on: [diff-repair]\n    trigger_rule: all_done\n    bash: |\n      set -uo pipefail\n      mkdir -p \"$ARTIFACTS_DIR\"\n      OUT=\"$ARTIFACTS_DIR/diff-final.patch\"\n      BASE_REF=\"origin/${BASE_BRANCH:-main}\"\n      # NF2: when there is no merge-base and no HEAD~1 (single-commit / no-base repo),\n      # fall back to the empty-tree hash, NOT HEAD. HEAD..HEAD is an EMPTY diff (reviewer\n      # sees nothing); empty-tree..HEAD shows all committed content as added (meaningful).\n      EMPTY_TREE=$(git hash-object -t tree /dev/null 2>/dev/null || echo 4b825dc642cb6eb9a060e54bf8d69288fbee4904)\n      MB=$(git merge-base HEAD \"$BASE_REF\" 2>/dev/null || git rev-parse HEAD~1 2>/dev/null || echo \"$EMPTY_TREE\")\n      [ -n \"$MB\" ] || MB=\"$EMPTY_TREE\"\n      {\n        echo \"### committed diff ($MB..HEAD)\"\n        git diff \"$MB\"..HEAD 2>/dev/null || true\n        echo\n        echo \"### unstaged working-tree diff (HEAD..worktree)\"\n        git diff HEAD 2>/dev/null || true\n      } > \"$OUT\"\n      echo \"DIFF_FINAL_BASE=$MB\"\n      echo \"DIFF_FINAL_ARTIFACT=$OUT\"\n      echo \"DIFF_FINAL_BYTES=$(wc -c < \"$OUT\" 2>/dev/null || echo 0)\"\n\n  - id: diff-review-final\n    provider: codex\n    agent: codex-adversarial-reviewer\n    depends_on: [capture-diff-final]\n    trigger_rule: all_done\n    task_class: medium_work\n    prompt: |\n      You are the Codex adversarial reviewer doing the FINAL re-review after repair.\n      Read-only. This node decides whether any semantic finding remains before commit --\n      the mechanical repair gate did NOT prove your earlier objections were fixed.\n\n      ## Inputs\n      Spec: $read-spec.output\n      Your original findings: $diff-review.output\n      Repair pass output: $diff-repair.output\n      Refreshed diff provenance: $capture-diff-final.output\n      (Read the repaired diff at DIFF_FINAL_ARTIFACT = $ARTIFACTS_DIR/diff-final.patch.\n      You have no Bash tool -- do not run git; read that file.)\n\n      ## Branch on the repair outcome FIRST\n      - If diff-repair FAILED or exhausted iterations (its output is empty, shows\n        ESCALATION_REQUIRED=true, or the done.flag was never written): do NOT certify.\n        Emit DIFF_REVIEW_FINAL=needs_revision, summarize the unresolved repair state in\n        UNRESOLVED, and carry through any escalation reason. Never PASS on failed repair.\n      - Otherwise: re-inspect the repaired diff. For EACH finding you raised originally,\n        verify it is actually resolved in the current tree -- not merely claimed resolved.\n\n      ## Output (STDOUT only)\n        DIFF_REVIEW_FINAL=<satisfied|needs_revision>\n        UNRESOLVED:\n        - <file:line> <finding still open>   (repeat; or \"(none)\")\n        SEMANTIC_RISK=<LOW|MEDIUM|HIGH>\n      DIFF_REVIEW_FINAL=satisfied ONLY if every original finding is genuinely resolved and\n      no new unsafe behavior was introduced by the repair.\n\n  # ===== Fix D (2026-05-30): pause-not-fail + Opus-tier on a blocked irreversible gate =====\n  # Replaces commit-and-push's old `exit 1` dead-end. A blocked run must NOT terminate as\n  # \"failed\"; it goes: classify -> Opus auto-repair (one pass) -> re-classify -> PAUSE\n  # (resumable approval) -> human corrects+resumes OR rejects. A clean run PROCEEDs with no\n  # human gate. An already-satisfied no-op resolves as success. (doctrine: fail-closed-must-\n  # pause-not-fail + self-repair-review-loops autonomy ladder.)\n  #\n  # block-classify: emit ONE JSON line so downstream `when:` can read $block-classify.output.status\n  # (condition-evaluator parses $node.output.field as JSON). status one of:\n  #   PROCEED           -- diff-review-final=satisfied AND real build -> commit/PR\n  #   ALREADY_SATISFIED -- no work needed, worktree clean, legitimately done -> success no-op\n  #   BLOCKED           -- false-completion OR diff-review-final=needs_revision -> repair/pause\n  - id: block-classify\n    depends_on: [diff-review-final]\n    trigger_rule: all_done\n    bash: |\n      set -uo pipefail\n      DIFF_FINAL=$diff-review-final.output\n      BUILD=$assert-implement-produced-work.output\n      PRECHECK=$gate-already-satisfied.output\n\n      final_ok=false\n      # Anchor-free parse: diff-review-final is Codex prose; sentinel appears inline,\n      # not at column 0. Take LAST occurrence so the final verdict always wins.\n      final_verdict=$(printf '%s\\n' \"$DIFF_FINAL\" \\\n        | grep -oE 'DIFF_REVIEW_FINAL=(satisfied|needs_revision)' \\\n        | tail -n 1)\n      if [ \"$final_verdict\" = \"DIFF_REVIEW_FINAL=satisfied\" ]; then final_ok=true; fi\n      outcome=$(printf '%s\\n' \"$BUILD\" | sed -n 's/^BUILD_OUTCOME=//p' | head -1)\n\n      # Codex FINDING_2: PROCEED requires BOTH a real build AND a satisfied final review.\n      # A missing/unparseable BUILD_OUTCOME must NOT slip through as PROCEED.\n      if [ \"$outcome\" = \"ALREADY_SATISFIED\" ]; then\n        STATUS=ALREADY_SATISFIED\n      elif [ \"$outcome\" = \"REAL_BUILD\" ] && [ \"$final_ok\" = \"true\" ]; then\n        STATUS=PROCEED\n      else\n        # FALSE_COMPLETE, unknown outcome, or final review not satisfied -> block.\n        STATUS=BLOCKED\n      fi\n      # single JSON line = the entire stdout, so $block-classify.output.status parses\n      printf '{\"status\":\"%s\",\"final_satisfied\":%s,\"build_outcome\":\"%s\"}\\n' \"$STATUS\" \"$final_ok\" \"${outcome:-unknown}\"\n\n  # opus-repair: one harder AUTONOMOUS fix pass by the Opus overseer when BLOCKED, BEFORE\n  # bothering a human (autonomy ladder step 2). Read-only-ish: it may edit in-scope files to\n  # resolve the block, then signals. If it can't fix, the run proceeds to the human pause.\n  - id: opus-repair\n    agent: overseer-opus\n    effort: xhigh\n    context: fresh\n    depends_on: [block-classify]\n    when: \"$block-classify.output.status == 'BLOCKED'\"\n    prompt: |\n      The bdc-feature-development run is BLOCKED before the irreversible commit. Before a\n      human is paused, you get ONE autonomous repair pass. Diagnose and, if you can,\n      resolve it so the build can proceed safely.\n\n      ## Why blocked\n      block-classify: $block-classify.output\n      diff-review-final: $diff-review-final.output\n      assert-implement-produced-work: $assert-implement-produced-work.output\n      Original Codex findings: $diff-review.output\n\n      ## Your job\n      - If the block is FALSE_COMPLETE (implement claimed done but produced no real changes\n        in THIS worktree): determine whether the WO genuinely needs work here. If it does and\n        you can make the in-scope changes, make them in the assigned worktree (you have the\n        tools). If the WO is genuinely already implemented in this worktree, say so.\n      - If the block is diff-review-final=needs_revision: make the specific fix the final\n        reviewer named, in scope, in this worktree.\n      - Judge ONLY against this worktree (no origin/main, no sibling worktrees). NEVER\n        fabricate a diff from already-merged commits.\n\n      ## Output (last lines)\n      - If you resolved it (real in-scope changes now exist in this worktree):\n          OPUS_REPAIR=fixed\n      - If it is genuinely already-satisfied (no work needed in this worktree):\n          OPUS_REPAIR=already-satisfied\n      - If you could NOT resolve it and a human must decide:\n          OPUS_REPAIR=needs-human\n          REASON=<one sentence>\n\n  # block-reclassify: after the Opus pass (or skip if it wasn't BLOCKED), re-emit the\n  # JSON status. trigger_rule all_done so it runs whether or not opus-repair ran.\n  - id: block-reclassify\n    depends_on: [block-classify, opus-repair]\n    trigger_rule: all_done\n    bash: |\n      set -uo pipefail\n      PRIOR=$block-classify.output\n      OPUS=$opus-repair.output\n      prior_status=$(printf '%s\\n' \"$PRIOR\" | sed -n 's/.*\"status\":\"\\([A-Z_]*\\)\".*/\\1/p' | head -1)\n\n      # If it wasn't blocked, keep the prior status unchanged.\n      if [ \"$prior_status\" != \"BLOCKED\" ]; then\n        printf '{\"status\":\"%s\"}\\n' \"${prior_status:-BLOCKED}\"\n        exit 0\n      fi\n\n      # It was blocked: did Opus fix it? Re-check the worktree for real work.\n      WT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)\n      CHANGED=$(git -C \"$WT\" status --porcelain 2>/dev/null | grep -c . || echo 0)\n      AHEAD=$(git -C \"$WT\" rev-list --count \"@{upstream}..HEAD\" 2>/dev/null \\\n              || git -C \"$WT\" rev-list --count HEAD --not --remotes 2>/dev/null || echo 0)\n\n      if printf '%s\\n' \"$OPUS\" | grep -q '^OPUS_REPAIR=fixed' && { [ \"${CHANGED:-0}\" -gt 0 ] || [ \"${AHEAD:-0}\" -gt 0 ]; }; then\n        # Opus made real changes -> needs a fresh review before proceeding. We do NOT\n        # auto-PROCEED (the new diff was not Codex-reviewed). Pause for human to re-run\n        # review or approve. Conservative: treat as BLOCKED-for-human with a clear note.\n        printf '{\"status\":\"BLOCKED\",\"note\":\"opus-made-changes-need-re-review\"}\\n'\n      elif printf '%s\\n' \"$OPUS\" | grep -q '^OPUS_REPAIR=already-satisfied'; then\n        printf '{\"status\":\"ALREADY_SATISFIED\",\"note\":\"opus-confirmed-already-satisfied\"}\\n'\n      else\n        printf '{\"status\":\"BLOCKED\",\"note\":\"opus-could-not-resolve\"}\\n'\n      fi\n\n  # pause-gate: the ONLY human touch, and ONLY when still BLOCKED after Opus. This PAUSES\n  # the run (status=paused, resumable via /workflow approve|reject) -- it does NOT fail.\n  - id: pause-gate\n    depends_on: [block-reclassify]\n    when: \"$block-reclassify.output.status == 'BLOCKED'\"\n    approval:\n      message: |\n        This run is BLOCKED before the irreversible commit and the AI tiers could not\n        safely resolve it. The run is PAUSED (not failed) -- nothing was committed or\n        pushed. Review and either correct + resume, or reject.\n\n        What to inspect:\n        - block-classify / block-reclassify: why it blocked (FALSE_COMPLETE = implement\n          produced no real changes; or diff-review-final = needs_revision).\n        - diff-review-final verdict + the Codex findings.\n        - opus-repair output: what the Opus tier tried and why it could not finish.\n        - $ARTIFACTS_DIR/diff-repair-escalation.txt if present (the escalation payload).\n\n        SAFE STATE: no commit, no push, no PR, no Notion write has happened.\n\n        - \"approved\" / \"proceed\" -> resume (only do this if you have corrected the worktree\n          so a real, reviewed change exists; commit-and-push still re-checks the gate)\n        - \"reject\" / \"stop\" -> halt; the worktree changes (if any) remain for manual handling\n      capture_response: true\n      on_reject:\n        prompt: |\n          The operator rejected the blocked run. Summarize what was blocked, what the AI\n          tiers tried, and what a human must change before re-running. Do not commit or fire.\n          Output: OPERATOR_REJECTED: <reason from approval response>\n        max_attempts: 2\n\n  - id: decide-push-target\n    persona: major-build\n    depends_on: [block-reclassify]\n    when: \"$block-reclassify.output.status == 'PROCEED'\"\n    task_class: clerical\n    prompt: |\n      Decide the push target for this BDC feature Work Order.\n\n      ## Inputs\n      Spec:\n      $read-spec.output\n\n      Validator:\n      $war-council-validator.output\n\n      ## Branch Name Derivation Rules (F-6A prevention)\n      Anchor incidents 2026-05-18: agents emitted branch names that embedded the\n      current worktree thread suffix, producing double-suffixed targets like\n      \"archon/thread-9772643d-thread-9772643d\" or\n      \"feat/WO-AUTH-RETIRE-GAS-PATH-02-thread-feat/WO-AUTH-RETIRE-GAS-PATH-02\".\n      The engine ALREADY appends a unique \"-thread-<hash>\" suffix at push time\n      (UNIQUE_BRANCH in commit-and-push). Your job is to emit the BASE branch\n      name only.\n\n      - Derive the branch name from the WO ID ONLY. Do NOT copy the current\n        git branch name. If running on archon/thread-9772643d, do NOT emit\n        that value or any variant of it as push_target.\n      - Use prefix feat/, fix/, or wip/ followed by the WO ID in lowercase\n        with hyphens. Example: WO-FOO-BAR-01 -> feat/wo-foo-bar-01\n      - The branch MUST NOT start with archon/thread- or contain -thread-\n        anywhere.\n      - The branch MUST NOT embed the current worktree thread hash anywhere\n        in its name.\n      - Valid examples: feat/wo-auth-retire-gas-path-02, fix/wo-bugs-01, wip/wo-draft-01\n      - commit-and-push validates the emitted branch against this allowlist\n        regex: ^(feat/[A-Za-z0-9_-]+|fix/[A-Za-z0-9_-]+|wip/[A-Za-z0-9_-]+)$\n        and fails closed with \"Malformed branch name\" on mismatch.\n\n      ## Required Repo Policy\n      Extract the target repository from the spec content. Look for any of these patterns (in priority order):\n      1. `target_repo: <owner>/<repo>` (YAML field)\n      2. `**Target repo:** \\`<owner>/<repo>\\`` (Markdown bold)\n      3. `Target repo: <owner>/<repo>` (plain header)\n      4. `**Target Repo:** \\`<owner>/<repo>\\`` (Markdown bold, capital R)\n\n      The target repo is where the PR must be opened (NOT the worktree's git origin - Rule 29).\n\n      Match the extracted repo against this hardcoded staging-gate list:\n      - bluedevilcollectibles/lspro-react\n      - bluedevilcollectibles/shopops-storefront\n      - bluedevilcollectibles/shopops\n\n      If the repo matches one of those entries, output:\n      push_target: feature-branch:<spec-derived-branch-name>\n      pr_required: true\n      staging_gate_required: true\n      repo: <owner>/<repo>\n\n      If the repo is bdc-xo non-doctrine code or another code repo, output:\n      push_target: feature-branch:<spec-derived-branch-name>\n      pr_required: true\n      staging_gate_required: false\n      repo: <owner>/<repo>\n\n      If this is doc-only or doctrine-only, output:\n      out_of_scope: true\n      reason: use bdc-doctrine-update.yaml\n      repo: unknown\n\n      CRITICAL: You MUST emit a `repo:` line in every output case. If no target_repo is\n      parseable from the spec, emit `repo: unknown` and a WARNING line explaining what is missing.\n\n  # load_bearing: true (WO-167 doctrine, WO-169 anchor)\n  # 2026-05-16 engine sortie: container-side `git push` silently failed; downstream\n  # `|| echo \"STATUS=*_failed\"` swallow patterns let the workflow report success\n  # while no commits reached origin. Push retry is now real (3 attempts, exponential\n  # backoff) and node fails closed if all attempts fail.\n  - id: commit-and-push\n    persona: overseer\n    depends_on: [decide-push-target]\n    when: \"$block-reclassify.output.status == 'PROCEED'\"\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      # 2026-05-16 quote-fix: substituteNodeOutputRefs with escapedForBash=true wraps\n      # the output in single quotes. Wrapping the substitution in double quotes here\n      # creates `\"'multiline\\nagent\\noutput'\"` which bash mis-tokenizes - line 2+ of\n      # the agent output becomes bare commands. Assign directly to bash vars, then\n      # quote the var reference. Engine-level fix tracked in WO-HARNESS-NODE-OUTPUT-BASH-QUOTING-01.\n      DECIDE_OUTPUT=$decide-push-target.output\n      # plan-review emits a fenced output. Extract ONLY the approved-plan block so the\n      # changed-file allowlist + commit message never ingest reviewer critique (F8 fix).\n      # Must be the revised plan (not pre-review) or a plan-review scope change would be\n      # rejected by the allowlist. FAIL CLOSED if the fence is absent (NF1): never fall\n      # back to raw output, or PLAN_REVIEW_FINDINGS critique would poison the allowlist.\n      PLAN_REVIEW_RAW=$plan-review.output\n      PLAN_OUTPUT=$(printf '%s\\n' \"$PLAN_REVIEW_RAW\" | sed -n '/=== APPROVED_PLAN_BEGIN ===/,/=== APPROVED_PLAN_END ===/p' | sed '1d;$d')\n      if [ -z \"$PLAN_OUTPUT\" ]; then\n        echo \"ERROR: plan-review output missing the APPROVED_PLAN fence -- cannot extract the approved plan (NF1 fail-closed).\" >&2\n        echo \"--- raw plan-review output (for debugging) ---\" >&2\n        printf '%s\\n' \"$PLAN_REVIEW_RAW\" >&2\n        exit 1\n      fi\n      DIFF_FINAL=$diff-review-final.output\n      RECLASS=$block-reclassify.output\n      echo \"$DECIDE_OUTPUT\"\n      if printf '%s\\n' \"$DECIDE_OUTPUT\" | grep -q '^out_of_scope: true'; then\n        echo \"Out of scope for bdc-feature-development; failing closed.\" >&2\n        exit 1\n      fi\n      # Irreversible-action gate (Fix D, 2026-05-30): the BLOCK/PAUSE decision now lives\n      # upstream in block-classify -> opus-repair -> block-reclassify -> pause-gate. This\n      # node only runs `when: $block-reclassify.output.status == 'PROCEED'`. We keep a\n      # belt-and-suspenders re-check here (defense in depth) but it must NEVER be the place\n      # a block surfaces as a raw failure -- if status isn't PROCEED we should not be here.\n      RECLASS_STATUS=$(printf '%s\\n' \"$RECLASS\" | sed -n 's/.*\"status\":\"\\([A-Z_]*\\)\".*/\\1/p' | head -1)\n      if [ \"$RECLASS_STATUS\" != \"PROCEED\" ]; then\n        echo \"ERROR: commit-and-push reached with status='$RECLASS_STATUS' (expected PROCEED). The when-gate should have skipped this node; refusing to commit.\" >&2\n        exit 1\n      fi\n      # Final-review sanity (still required for a real build): diff-review-final must be satisfied.\n      # Anchor-free parse (defense-in-depth mirror of block-classify). Fail-closed:\n      # anything other than an explicit satisfied leaves the gate refusing.\n      final_verdict_cap=$(printf '%s\\n' \"$DIFF_FINAL\" \\\n        | grep -oE 'DIFF_REVIEW_FINAL=(satisfied|needs_revision)' \\\n        | tail -n 1)\n      if [ \"$final_verdict_cap\" != \"DIFF_REVIEW_FINAL=satisfied\" ]; then\n        echo \"ERROR: diff-review-final not satisfied despite PROCEED status -- refusing commit (defense in depth).\" >&2\n        exit 1\n      fi\n      BRANCH=$(printf '%s\\n' \"$DECIDE_OUTPUT\" | sed -n 's/^push_target: feature-branch://p' | head -n 1)\n      if [ -z \"$BRANCH\" ]; then\n        echo \"No feature branch target found in decide-push-target output.\" >&2\n        exit 1\n      fi\n      # F-6A: Validate BRANCH against allowlist regex BEFORE constructing UNIQUE_BRANCH.\n      # WO-HARNESS-PUSH-CORRECTNESS-HARDENING-01 (anchor 2026-05-18: three WOs hit\n      # double-suffix branch names like archon/thread-9772643d-thread-9772643d).\n      # The engine appends -thread-<hash> at push time, so the agent must emit a\n      # clean feat/|fix/|wip/ prefix only. archon/thread- prefix is rejected here\n      # because it would produce a double-suffix UNIQUE_BRANCH.\n      BRANCH_PATTERN='^(feat/[A-Za-z0-9_-]+|fix/[A-Za-z0-9_-]+|wip/[A-Za-z0-9_-]+)$'\n      if ! printf '%s\\n' \"$BRANCH\" | grep -Eq \"$BRANCH_PATTERN\"; then\n        echo \"Malformed branch name: $BRANCH does not match required pattern feat/|fix/|wip/|archon/thread-\" >&2\n        exit 1\n      fi\n      # WO-HARNESS-YAML-USE-THREAD-BRANCH-NOT-FEATURE-BRANCH-01\n      # Engine already provides a unique archon/thread-<hash> branch per run.\n      # Derive a unique remote push target from it instead of clobbering a shared name.\n      THREAD_ID=$(git rev-parse --abbrev-ref HEAD | sed 's|archon/thread-||')\n      UNIQUE_BRANCH=\"${BRANCH}-thread-${THREAD_ID}\"\n      git status --short\n      DIRTY=$(git status --porcelain)\n      if [ -z \"$DIRTY\" ]; then\n        # WO-HARNESS-COMMIT-AND-PUSH-ORIGIN-VERIFY-02: do NOT exit 0 here on the\n        # \"HEAD matches origin\" heuristic. The only invariant that matters is\n        # origin/${UNIQUE_BRANCH} == local HEAD, verified by ls-remote at the END\n        # of this node (after the push loop). A clean tree that already matches\n        # origin simply makes the push a no-op + the verify a pass. Falling\n        # through (instead of exit 0) guarantees every path hits the final verify.\n        # WO-HARNESS-COMMIT-AND-PUSH-BACKSTOP-FALSE-NEGATIVE-01: a clean working\n        # tree does NOT mean the implement loop skipped work - it may have committed\n        # and left the tree clean. Count COMMITS_AHEAD to distinguish true no-op\n        # from pre-committed work that still needs pushing.\n        if git rev-parse --quiet --verify \"origin/${UNIQUE_BRANCH}\" >/dev/null 2>&1; then\n          COMMITS_AHEAD=$(git rev-list --count \"origin/${UNIQUE_BRANCH}..HEAD\" 2>/dev/null || echo 0)\n        else\n          COMMITS_AHEAD=$(git rev-list --count HEAD --not --remotes=origin 2>/dev/null || echo 0)\n        fi\n        if [ \"$COMMITS_AHEAD\" = \"0\" ]; then\n          # F-7C: origin/${UNIQUE_BRANCH} may be missing because the implement\n          # loop already pushed to a different remote branch (e.g. the engine's\n          # archon/thread-<hash> ref). Search git ls-remote origin for any ref\n          # whose HEAD SHA matches local HEAD; if found, retarget UNIQUE_BRANCH\n          # to that name and proceed (push will be a no-op).\n          # WO-HARNESS-PUSH-CORRECTNESS-HARDENING-01 (anchor 2026-05-18:\n          # WO-HARNESS-OVERSEER-FAILURE-CLASSES-EXPANSION-01 12:03 UTC, commit\n          # 2d5f6ac0 on origin/archon/thread-9772643d but UNIQUE_BRANCH was\n          # archon/thread-9772643d-thread-9772643d).\n          LOCAL_HEAD=$(git rev-parse HEAD)\n          RECOVERED=$(git ls-remote origin 2>/dev/null \\\n            | awk -v sha=\"$LOCAL_HEAD\" '$1 == sha {sub(/^refs\\/heads\\//, \"\", $2); print $2}' \\\n            | head -1)\n          if [ -n \"$RECOVERED\" ]; then\n            echo \"Recovered push target from remote: $RECOVERED\"\n            UNIQUE_BRANCH=\"$RECOVERED\"\n          else\n            echo \"No changed files and no commits ahead of origin/${UNIQUE_BRANCH} -- implement loop did not produce work\" >&2\n            exit 1\n          fi\n        else\n          echo \"Backstop: $COMMITS_AHEAD commit(s) ahead of origin -- pushing pre-committed work.\"\n        fi\n      else\n        # Dirty working tree: stage + validate against plan + commit\n        CHANGED_FILES=$(printf '%s\\n' \"$DIRTY\" | awk '{print substr($0,4)}')\n        # Auto-tolerated files: dependency lockfiles + standard auto-regenerated\n        # artifacts managed by package managers during the implement loop.\n        # Without this allowlist, adding a dependency causes bun.lock regeneration\n        # which trips the strict-allowlist check.\n        AUTO_TOLERATED='^(bun\\.lock|package-lock\\.json|yarn\\.lock|pnpm-lock\\.yaml|\\.bun-tag|node_modules/)$'\n        printf '%s\\n' \"$CHANGED_FILES\" | while IFS= read -r path; do\n          if [ -z \"$path\" ]; then\n            continue\n          fi\n          # Skip allowlist check for auto-managed files; just stage them.\n          if printf '%s\\n' \"$path\" | grep -Eq \"$AUTO_TOLERATED\"; then\n            echo \"Auto-tolerated change (lockfile/managed): $path\"\n            git add -- \"$path\"\n            continue\n          fi\n          if ! printf '%s\\n' \"$PLAN_OUTPUT\" | grep -F -- \"$path\" >/dev/null; then\n            echo \"Changed file was not listed in plan output: $path\" >&2\n            exit 1\n          fi\n          git add -- \"$path\"\n        done\n        if git diff --cached --quiet; then\n          echo \"No changes staged; nothing for backstop to commit.\" >&2\n          exit 0\n        fi\n        COMMIT_MESSAGE=$(printf '%s\\n' \"$PLAN_OUTPUT\" | sed -n 's/^Commit message: //p' | head -n 1)\n        if [ -z \"$COMMIT_MESSAGE\" ]; then\n          # Re-derive WO_ID locally - see post-started for rationale\n          WO_ID=\"${WO_ID:-$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'WO-[A-Z0-9-]+' | head -n 1 || true)}\"\n          COMMIT_MESSAGE=\"feat: ${WO_ID} implementation\"\n        fi\n        git commit -m \"$COMMIT_MESSAGE\"\n      fi\n\n      # WO-169: real retry guard. 3 attempts, 5s/10s/15s backoff, fail closed.\n      # No `|| echo 'STATUS=*_failed'` swallow -- this node is load_bearing.\n      # WO-HARNESS-YAML-USE-THREAD-BRANCH-NOT-FEATURE-BRANCH-01: push via refspec\n      # so the local archon/thread-<hash> branch lands on a unique remote name\n      # (UNIQUE_BRANCH) and does not collide with other parallel runs.\n      PUSH_OK=false\n      for attempt in 1 2 3; do\n        if git push origin HEAD:\"${UNIQUE_BRANCH}\" 2>&1; then\n          PUSH_OK=true\n          break\n        fi\n        echo \"WARN: push attempt $attempt failed; retrying after $((attempt * 5))s...\" >&2\n        sleep $((attempt * 5))\n      done\n      if [ \"$PUSH_OK\" = \"false\" ]; then\n        echo \"ERROR: git push failed after 3 attempts -- aborting workflow (load_bearing node).\" >&2\n        exit 1\n      fi\n      # WO-HARNESS-COMMIT-AND-PUSH-ORIGIN-VERIFY-02: the ONLY success invariant.\n      # Clean-tree / commits-ahead-0 / HEAD-reachable-from-some-remote are NOT\n      # valid proxies. The canonical PR branch MUST be on origin at local HEAD\n      # before open-pr-if-needed runs, or work is lost when the worktree is cleaned\n      # (anchors: Lane A runs ac2777a3 + f6e24ac2, 2026-05-25 -- both lost the\n      # report at open-pr because the branch was never durably on origin).\n      REMOTE_HEAD=\"$(git ls-remote origin \"refs/heads/${UNIQUE_BRANCH}\" 2>/dev/null | awk '{print $1}')\"\n      LOCAL_HEAD=\"$(git rev-parse HEAD)\"\n      if [ \"$REMOTE_HEAD\" != \"$LOCAL_HEAD\" ]; then\n        echo \"FATAL: canonical branch ${UNIQUE_BRANCH} not durably pushed (origin=${REMOTE_HEAD:-<absent>} local=${LOCAL_HEAD})\" >&2\n        exit 1\n      fi\n      echo \"VERIFIED: origin/${UNIQUE_BRANCH} == local HEAD (${LOCAL_HEAD})\"\n      echo \"unique_branch=${UNIQUE_BRANCH}\"\n\n  # load_bearing: true (WO-167 doctrine, WO-169 anchor)\n  - id: open-pr-if-needed\n    persona: major-build\n    depends_on: [commit-and-push]\n    task_class: clerical\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      # 2026-05-16 quote-fix: see commit-and-push for rationale.\n      DECIDE_OUTPUT=$decide-push-target.output\n      # Extract ONLY the approved-plan block for the PR title/body (F8 fix) -- keep\n      # reviewer critique out of the customer-visible PR. FAIL CLOSED if the fence is\n      # absent (NF1): never put raw critique-bearing output in the PR body.\n      PLAN_REVIEW_RAW=$plan-review.output\n      PLAN_OUTPUT=$(printf '%s\\n' \"$PLAN_REVIEW_RAW\" | sed -n '/=== APPROVED_PLAN_BEGIN ===/,/=== APPROVED_PLAN_END ===/p' | sed '1d;$d')\n      if [ -z \"$PLAN_OUTPUT\" ]; then\n        echo \"ERROR: plan-review output missing the APPROVED_PLAN fence -- cannot build PR body (NF1 fail-closed).\" >&2\n        exit 1\n      fi\n      IMPLEMENT_OUTPUT=$implement.output\n      VALIDATOR_OUTPUT=$war-council-validator.output\n      COMMIT_PUSH_OUTPUT=$commit-and-push.output\n      if ! printf '%s\\n' \"$DECIDE_OUTPUT\" | grep -q '^pr_required: true'; then\n        echo \"PR not required; skipping.\"\n        exit 0\n      fi\n      # WO-HARNESS-YAML-USE-THREAD-BRANCH-NOT-FEATURE-BRANCH-01: read the unique\n      # remote branch the commit-and-push node pushed to; gh pr create must use\n      # --head to target that branch rather than the (local) archon/thread-<hash>.\n      UNIQUE_BRANCH=$(printf '%s\\n' \"$COMMIT_PUSH_OUTPUT\" | sed -n 's/^unique_branch=//p' | head -n 1)\n      if [ -z \"$UNIQUE_BRANCH\" ]; then\n        echo \"UNIQUE_BRANCH not found in commit-and-push output.\" >&2\n        exit 1\n      fi\n      # WO-HARNESS-COMMIT-AND-PUSH-ORIGIN-VERIFY-02: precondition guard. Re-verify\n      # the canonical branch is on origin at local HEAD BEFORE attempting any PR.\n      # If commit-and-push's verify passed this should always hold; failing here\n      # means the branch was lost between nodes -- abort before wasting gh retries.\n      REMOTE_HEAD=\"$(git ls-remote origin \"refs/heads/${UNIQUE_BRANCH}\" 2>/dev/null | awk '{print $1}')\"\n      if [ \"$REMOTE_HEAD\" != \"$(git rev-parse HEAD)\" ]; then\n        echo \"FATAL: open-pr precondition failed -- ${UNIQUE_BRANCH} not on origin at HEAD (origin=${REMOTE_HEAD:-<absent>}).\" >&2\n        exit 1\n      fi\n      # Rule 29: extract explicit repo from decide-push-target output.\n      # Never let gh default to the worktree's origin -- it may be bdc-harness\n      # even when the spec targets bdc-xo or lspro-react.\n      REPO=$(printf '%s\\n' \"$DECIDE_OUTPUT\" | sed -n 's/^repo: //p' | head -n 1)\n      if [ -z \"$REPO\" ] || [ \"$REPO\" = \"unknown\" ]; then\n        echo \"ERROR: decide-push-target did not emit a valid 'repo: <owner>/<repo>'.\" >&2\n        echo \"Fix the spec to include 'target_repo:' or '**Target repo:**' and re-run.\" >&2\n        exit 1\n      fi\n      # F-8C: Rule 20 staging-gate repos (lspro-react, shopops-storefront, shopops)\n      # require PRs to target staging instead of the repo default branch.\n      # decide-push-target emits \"staging_gate_required: true|false\"; when true,\n      # this node passes --base staging to gh pr create (otherwise it omits --base\n      # and lets gh use the repo default).\n      # WO-HARNESS-PUSH-CORRECTNESS-HARDENING-01 (closes Rule 20 bypass hole 2026-05-18).\n      STAGING_GATE=$(printf '%s\\n' \"$DECIDE_OUTPUT\" | grep -c '^staging_gate_required: true' 2>/dev/null || true)\n      STAGING_GATE=\"${STAGING_GATE:-0}\"\n      if [ \"$STAGING_GATE\" -ge 1 ] 2>/dev/null; then\n        BASE_BRANCH=\"staging\"\n      else\n        BASE_BRANCH=\"\"\n      fi\n      TITLE=$(printf '%s\\n' \"$PLAN_OUTPUT\" | sed -n 's/^Commit message: //p' | head -n 1)\n      if [ -z \"$TITLE\" ]; then\n        TITLE=\"BDC feature Work Order implementation\"\n      fi\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"$PLAN_OUTPUT\"\n        echo\n        echo \"## Validation\"\n        echo \"Workflow-time verify-* nodes were removed 2026-05-16 (Patch 1) - agent self-verifies in implement loop;\"\n        echo \"real validation gates run at PR time (codex-review + claude-review + Gitleaks + e2e-smoke + Captain CI).\"\n        echo \"See implement output below for what the builder verified locally.\"\n        echo\n        echo \"## Implement output\"\n        echo \"$IMPLEMENT_OUTPUT\"\n        echo\n        echo \"## Validator\"\n        echo \"$VALIDATOR_OUTPUT\"\n      } > \"$BODY_FILE\"\n      # WO-169: real retry guard. 3 attempts, 5s/10s/15s backoff, fail closed.\n      # No `|| echo 'STATUS=*_failed'` swallow -- this node is load_bearing.\n      PR_OK=false\n      for attempt in 1 2 3; do\n        if gh pr create --repo \"$REPO\" --title \"$TITLE\" --body-file \"$BODY_FILE\" --head \"$UNIQUE_BRANCH\" ${BASE_BRANCH:+--base \"$BASE_BRANCH\"} 2>&1; then\n          PR_OK=true\n          break\n        fi\n        # Race-condition recovery: branch already has an open PR.\n        if [ -n \"$UNIQUE_BRANCH\" ]; then\n          EXIST=$(gh pr list --repo \"$REPO\" --head \"$UNIQUE_BRANCH\" --state open \\\n            --json url --jq '.[0].url // empty' 2>/dev/null || true)\n          if [ -n \"$EXIST\" ]; then\n            echo \"PR_URL=$EXIST\"\n            PR_OK=true\n            break\n          fi\n        fi\n        echo \"WARN: gh pr create attempt $attempt failed; retrying after $((attempt * 5))s...\" >&2\n        sleep $((attempt * 5))\n      done\n      if [ \"$PR_OK\" = \"false\" ]; then\n        # WO-HARNESS-COMMIT-AND-PUSH-ORIGIN-VERIFY-02: gh pr create failed, but the\n        # canonical branch was VERIFIED on origin at HEAD above (precondition guard\n        # + commit-and-push verify). So the work is NOT lost -- the operator can open\n        # the PR manually from the pushed branch. Emit a salvage manifest line so the\n        # branch + SHA survive in the run output even though the worktree is cleaned.\n        echo \"PR_CREATE_FAILED_BUT_BRANCH_SAFE: repo=${REPO} branch=${UNIQUE_BRANCH} sha=$(git rev-parse HEAD)\" >&2\n        echo \"ERROR: gh pr create failed after 3 attempts -- aborting workflow (load_bearing node).\" >&2\n        echo \"RECOVERY: open the PR manually -> gh pr create --repo ${REPO} --head ${UNIQUE_BRANCH} ${BASE_BRANCH:+--base ${BASE_BRANCH}}\" >&2\n        exit 1\n      fi\n\n  - id: build-manifest\n    persona: major-build\n    depends_on: [open-pr-if-needed, block-reclassify, pause-gate]\n    trigger_rule: all_done\n    task_class: clerical\n    prompt: |\n      Construct the Captain CI completion manifest for this BDC feature Work Order.\n\n      ## Outcome branch (Fix D, 2026-05-30) -- read this FIRST\n      block-reclassify status: $block-reclassify.output\n      - If status=PROCEED: a real build was committed + a PR opened. Build the full v2\n        manifest below (this is the normal case).\n      - If status=ALREADY_SATISFIED: the WO was already implemented in this worktree; no\n        commit/PR. Emit a SHORT manifest: WO ID, OUTCOME=ALREADY_SATISFIED, the evidence\n        from check-already-satisfied, VALIDATION: PASS (verified no-op). Do NOT claim a PR.\n      - If status=BLOCKED: the run paused for human correction (pause-gate). Emit\n        OUTCOME=BLOCKED, the block reason, what the AI tiers tried, VALIDATION: BLOCKED.\n        Do NOT claim a PR or DONE.\n\n      ## Inputs\n      Spec:\n      $read-spec.output\n\n      Plan (approved -- reviewed + revised by plan-review; use the APPROVED_PLAN block):\n      $plan-review.output\n\n      Implementation:\n      $implement.output\n\n      Verification:\n      (Workflow-time verify-build/types/tests removed 2026-05-16 - agent self-verifies in\n      implement loop, PR-time gates handle real validation. See implement output above.)\n\n      Validator:\n      $war-council-validator.output\n\n      Plan review verdict (self-repair loop -- record PLAN_REVIEW_PASS + RISK as evidence):\n      $plan-review.output\n\n      Diff review (Codex, initial):\n      $diff-review.output\n\n      Diff review (Codex, final after repair):\n      $diff-review-final.output\n\n      ## Required Manifest Fields\n      - WO ID\n      - Files created\n      - Files modified\n      - Routes added\n      - Migrations\n      - Tests\n      - Commit SHA\n      - Validation evidence by WO class\n      - PR URL, if opened\n      - Staging gate status, if required\n      - In-run review evidence (record as evidence, NOT vibes -- doctrine\n        self-repair-review-loops): plan-review verdict (PLAN_REVIEW_PASS + RISK);\n        diff-review-final verdict (DIFF_REVIEW_FINAL); diff-review provider used\n        (codex cross-model, OR claude-fallback with CROSS_MODEL_REVIEW=false disclosed).\n        If any in-run review was skipped or ended needs_revision, VALIDATION cannot be PASS.\n\n  - id: flip-notion\n    persona: xo\n    depends_on: [build-manifest]\n    trigger_rule: all_done\n    task_class: clerical\n    prompt: |\n      Update the Notion Work Order page if Notion MCP is available in this runtime.\n\n      ## Outcome branch (Fix D, 2026-05-30) -- read block-reclassify FIRST\n      block-reclassify status: $block-reclassify.output\n      - status=PROCEED: set Claude Status: REVIEW, Status: Review, add the manifest as a\n        page comment (normal case).\n      - status=ALREADY_SATISFIED: set Claude Status: REVIEW with a note that the WO was\n        already implemented (verified no-op); add the short manifest. Do NOT claim a PR.\n      - status=BLOCKED: do NOT flip to REVIEW. Add a comment that the run PAUSED for human\n        correction with the block reason, and emit an XO_ACTION line. Leave status as-is.\n\n      ## Manifest\n      $build-manifest.output\n\n      If Notion MCP is not available inside the container, do not invent a workaround.\n      Emit an XO_ACTION line asking XO to flip Notion manually with the manifest above.\n",
  "bdc-harness-wo-onramp": "name: bdc-harness-wo-onramp\ndescription: |\n  Use when: Firing any BDC product Work Order through Devils Cauldron. This is\n  the canonical on-ramp atom - it authors a tailored child YAML from the approved\n  WO spec, adversarially reviews it, gets operator approval, registers the child\n  workflow in Archon, fires it against the target codebase, verifies binding, and\n  builds the Captain CI v2 manifest.\n  Inputs:\n    USER_MESSAGE: WO_ID (e.g. WO-SHOPOPS-PULLLIST-EXPORT-01) plus optional\n                  --project <repo> override. WO spec must exist on bdc-xo/main.\n  Does: preflight -> normalize-spec -> generate-yaml (loop) -> validate-yaml ->\n        adversarial-review -> prefire-plan -> approval-gate -> register-yaml ->\n        fire-child -> verify-binding -> build-manifest -> flip-notion.\n  NOT for: Doctrine-only WOs (use bdc-doctrine-update). Batch authoring\n        (use bdc-author-wo-batch). WO authoring from scratch (use bdc-wo-author).\n  parallel_ok: false\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\ninteractive: true\n\ninputs:\n  NOTION_DB_ID:\n    default: \"a6df831c-0b52-449f-8ca4-d77be6b70d0a\"\n\nnodes:\n\n  # Node 1: preflight\n  # Fails closed on: gh auth, bdc-xo reachability, ARCHON_OPERATOR_TOKEN, push scope.\n  - id: preflight\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n\n      echo \"=== preflight: checking gh auth ===\"\n      if ! gh auth status 2>&1 | grep -q 'Logged in'; then\n        echo \"ERROR: gh CLI not authenticated. Run 'gh auth login' or set GITHUB_TOKEN.\" >&2\n        exit 1\n      fi\n      echo \"gh auth: OK\"\n\n      echo \"=== preflight: checking bdc-xo reachability ===\"\n      if ! gh api repos/bluedevilcollectibles/bdc-xo --jq .id >/dev/null 2>&1; then\n        echo \"ERROR: cannot reach bluedevilcollectibles/bdc-xo via gh api.\" >&2\n        echo \"Check GITHUB_TOKEN scope (needs repo read on bdc-xo).\" >&2\n        exit 1\n      fi\n      echo \"bdc-xo reachable: OK\"\n\n      echo \"=== preflight: checking ARCHON_OPERATOR_TOKEN ===\"\n      if [ -z \"${ARCHON_OPERATOR_TOKEN:-}\" ]; then\n        echo \"ERROR: ARCHON_OPERATOR_TOKEN not set in executor env.\" >&2\n        echo \"Add it to the harness container env and retry.\" >&2\n        exit 1\n      fi\n      echo \"ARCHON_OPERATOR_TOKEN: present\"\n\n      echo \"=== preflight: checking git push auth (dry-run) ===\"\n      PROBE_REF=\"refs/heads/preflight-auth-probe-$(date +%s)\"\n      if ! git push --dry-run origin \"HEAD:${PROBE_REF}\" 2>&1; then\n        echo \"ERROR: git push dry-run failed - container cannot push to origin.\" >&2\n        echo \"Likely cause: GITHUB_TOKEN missing push scope or wrong remote.\" >&2\n        exit 1\n      fi\n      echo \"git push auth: OK\"\n\n      echo \"preflight PASS\"\n\n  # Node 2: fetch-spec (bash node - deterministic private-repo fetch)\n  # The xo persona has no Bash tool, so spec retrieval MUST be a bash node, not an\n  # agent prompt. Runs the gh api fallback chain (proven in bdc-feature-development\n  # read-spec). Writes the raw spec to $ARTIFACTS_DIR/source-spec.md. Fails non-zero\n  # if nothing is retrieved, so fetch-failure is distinguishable from parse-failure.\n  - id: fetch-spec\n    load_bearing: true\n    depends_on: [preflight]\n    bash: |\n      set -euo pipefail\n      DEST=\"$ARTIFACTS_DIR/source-spec.md\"\n      META=\"$ARTIFACTS_DIR/source-spec.meta\"\n\n      # Parse WO_ID from USER_MESSAGE\n      WO_ID=$(printf '%s\\n' \"$USER_MESSAGE\" | grep -Eo 'WO-[A-Z][A-Z0-9-]+-[0-9]+' | head -n 1 || true)\n      if [ -z \"$WO_ID\" ]; then\n        echo \"ERROR: no WO_ID matching WO-[A-Z][A-Z0-9-]+-[0-9]+ in USER_MESSAGE\" >&2\n        exit 1\n      fi\n\n      # Parse --project override (optional)\n      TARGET_HINT=$(printf '%s\\n' \"$USER_MESSAGE\" | grep -Eo -- '--project[[:space:]]+[A-Za-z0-9._-]+' | head -n 1 | awk '{print $2}' || true)\n\n      SPEC_CONTENT=\"\"\n      SPEC_PATH=\"\"\n\n      # 1) docs/work-orders/<WO_ID>.md\n      SPEC_PATH=\"docs/work-orders/${WO_ID}.md\"\n      SPEC_CONTENT=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      # 2) docs/superpowers/specs/<WO_ID>.md\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        SPEC_PATH=\"docs/superpowers/specs/${WO_ID}.md\"\n        SPEC_CONTENT=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      fi\n\n      # 3) date-prefix glob in docs/superpowers/specs/\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        MATCH=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null | grep \"${WO_ID}.md$\" | head -n 1 || true)\n        if [ -n \"$MATCH\" ]; then\n          SPEC_PATH=\"$MATCH\"\n          SPEC_CONTENT=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      # 4) GitHub issue body fallback\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo --search \"${WO_ID} in:title\" --json number --jq '.[0].number' 2>/dev/null || true)\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"bdc-xo:issues/${ISSUE_NUM}\"\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        echo \"ERROR: SPEC_MISSING ${WO_ID} not found in bdc-xo (docs/work-orders, docs/superpowers/specs direct, date-glob, issue search)\" >&2\n        exit 1\n      fi\n\n      # Write canonical artifact\n      printf '%s\\n' \"$SPEC_CONTENT\" > \"$DEST\"\n      {\n        echo \"WO_ID=${WO_ID}\"\n        echo \"SPEC_PATH=bdc-xo:${SPEC_PATH}\"\n        echo \"TARGET_HINT=${TARGET_HINT:-unknown}\"\n        echo \"SPEC_BYTES=$(wc -c < \"$DEST\")\"\n      } > \"$META\"\n\n      echo \"fetch-spec: wrote $(wc -c < \"$DEST\") bytes to $DEST\"\n      echo \"WO_ID=${WO_ID}\"\n      echo \"SPEC_PATH=bdc-xo:${SPEC_PATH}\"\n      echo \"TARGET_HINT=${TARGET_HINT:-unknown}\"\n      echo \"SPEC_ARTIFACT=$DEST\"\n      echo \"FETCH_OK=true\"\n\n  # Node 2a: normalize-spec (xo prompt - reads the fetched artifact, extracts fields)\n  # Reads ONLY $ARTIFACTS_DIR/source-spec.md (written by fetch-spec). Does NOT attempt\n  # any gh/repo access (the xo persona has no Bash tool). Pure extraction/judgment.\n  - id: normalize-spec\n    agent: xo\n    context: fresh\n    depends_on: [fetch-spec]\n    prompt: |\n      You are the BDC on-ramp spec normalizer. The spec has ALREADY been fetched by\n      the fetch-spec bash node and written to a file. Read ONLY that file and emit a\n      normalized block. Do NOT attempt any gh/repo access - you have no shell.\n\n      ## Inputs\n      fetch-spec output: $fetch-spec.output\n      The raw spec is in the file referenced by SPEC_ARTIFACT (read it with your Read tool).\n      WO_ID and TARGET_HINT are in the fetch-spec output above.\n\n      ## Step 1 - Read the spec artifact\n      Read the file at the SPEC_ARTIFACT path from fetch-spec output (typically\n      $ARTIFACTS_DIR/source-spec.md). That file IS the WO spec body. If you cannot\n      read it or it is empty, output `SPEC_MISSING: artifact unreadable` and stop.\n\n      ## Step 2 - Determine TARGET_REPO\n      Prefer the TARGET_HINT from fetch-spec output if not \"unknown\". Otherwise parse\n      the spec body for `target_repo:`, `**Target repo:**`, or `Target Repo:` lines.\n      Default to `unknown` if not parseable.\n\n      ## Step 3 - Extract fields from the spec body\n      - TARGET_REPO: short repo name (e.g. shopops)\n      - TARGET_REPO_FULL: bluedevilcollectibles/<TARGET_REPO> (add org prefix if missing)\n      - RULE20_STAGING_GATE: true if TARGET_REPO_FULL is one of:\n        bluedevilcollectibles/lspro-react, bluedevilcollectibles/shopops-storefront,\n        bluedevilcollectibles/shopops. Otherwise false.\n      - WO_CLASS: CODE, INFRA, or MIXED (from spec Class field; default CODE)\n      - BUSINESS_RISK_INVARIANT: the single most critical must-not-break invariant.\n        Derive from the Stop Point / Behavior Source of Truth / any \"MUST NEVER break\"\n        statement. One sentence, specific to THIS WO. Never emit the literal words\n        \"placeholder\", \"TODO\", \"one sentence\", or \"<BUSINESS_RISK...\".\n      - IN_SCOPE_FILES: in-scope files (comma-separated)\n      - VERIFY_COMMANDS: stop-condition commands (newline-separated)\n\n      ## Required Output Format (emit exactly, no markdown fences)\n\n      ```\n      === NORMALIZE-SPEC OUTPUT ===\n      WO_ID=<id>\n      TARGET_REPO=<repo-short-name-or-unknown>\n      TARGET_REPO_FULL=<bluedevilcollectibles/repo-or-unknown>\n      RULE20_STAGING_GATE=<true|false>\n      WO_CLASS=<CODE|INFRA|MIXED>\n      BUSINESS_RISK_INVARIANT=<one sentence>\n      IN_SCOPE_FILES=<comma-separated or \"(see spec)\">\n      VERIFY_COMMANDS=<newline-separated commands or \"(see spec)\">\n\n      === SPEC_CONTENT ===\n      <full spec body verbatim from the artifact>\n      ```\n\n  # Node 2b: assert-spec-resolved (load-bearing guard)\n  # Fails closed if normalize-spec did not retrieve a real spec.\n  - id: assert-spec-resolved\n    load_bearing: true\n    depends_on: [normalize-spec]\n    bash: |\n      set -euo pipefail\n      NORM_OUT=$normalize-spec.output\n\n      if printf '%s\\n' \"$NORM_OUT\" | grep -qE '^SPEC_MISSING:'; then\n        MSG=$(printf '%s\\n' \"$NORM_OUT\" | grep '^SPEC_MISSING:' | head -1)\n        echo \"ERROR: normalize-spec could not retrieve spec: $MSG\" >&2\n        exit 1\n      fi\n\n      SPEC_LEN=$(printf '%s\\n' \"$NORM_OUT\" | awk '/=== SPEC_CONTENT ===/,0' | wc -c || echo 0)\n      if [ \"${SPEC_LEN:-0}\" -lt 200 ]; then\n        echo \"ERROR: SPEC_CONTENT section missing or too short (${SPEC_LEN} chars)\" >&2\n        exit 1\n      fi\n\n      BRI=$(printf '%s\\n' \"$NORM_OUT\" | sed -n 's/^BUSINESS_RISK_INVARIANT=//p' | head -1)\n      if [ -z \"$BRI\" ]; then\n        echo \"ERROR: BUSINESS_RISK_INVARIANT missing from normalize-spec output\" >&2\n        exit 1\n      fi\n      for BAD in placeholder TODO unknown \"BUSINESS_RISK_INVARIANT\" \"invariant here\" \"one sentence\"; do\n        if printf '%s\\n' \"$BRI\" | grep -qi \"$BAD\"; then\n          echo \"ERROR: BUSINESS_RISK_INVARIANT is a placeholder: $BRI\" >&2\n          exit 1\n        fi\n      done\n      if [ \"${#BRI}\" -lt 20 ]; then\n        echo \"ERROR: BUSINESS_RISK_INVARIANT too short (${#BRI} chars): $BRI\" >&2\n        exit 1\n      fi\n\n      echo \"SPEC_RESOLVED=true\"\n      echo \"BRI ok: $BRI\"\n\n  # Node 3: yaml-author (write-capable builder + in-loop validation)\n  # major-build authors the child YAML. until_bash runs the full validation after\n  # each iteration; failures are written to a feedback file the next iteration reads.\n  # The loop IS the validate->fix retry. max_iterations is the retry budget.\n  - id: yaml-author\n    agent: major-build\n    depends_on: [assert-spec-resolved]\n    loop:\n      fresh_context: true\n      until: YAML_READY\n      max_iterations: 4\n      until_bash: |\n        set -uo pipefail\n        FEEDBACK=\"$ARTIFACTS_DIR/yaml-author-validation.txt\"\n        : > \"$FEEDBACK\"\n\n        # Resolve the draft filename: prefer a single *.yaml.draft in CWD.\n        DRAFT=$(ls -1 *.yaml.draft 2>/dev/null | head -n 1 || true)\n        if [ -z \"$DRAFT\" ] || [ ! -f \"$DRAFT\" ]; then\n          echo \"FAIL: no .yaml.draft file found in CWD\" >> \"$FEEDBACK\"\n          cat \"$FEEDBACK\"\n          exit 1\n        fi\n\n        ERRORS=0\n\n        # Rule 1: model: sonnet at root\n        grep -Eq '^model:[[:space:]]*sonnet$' \"$DRAFT\" || { echo \"FAIL Rule 1: missing 'model: sonnet' at root\" >> \"$FEEDBACK\"; ERRORS=$((ERRORS+1)); }\n\n        # Rule 6: policyFile at root\n        grep -Eq '^policyFile:[[:space:]]*harness/policies/agent-behavior\\.md$' \"$DRAFT\" || { echo \"FAIL Rule 6: missing 'policyFile: harness/policies/agent-behavior.md' at root\" >> \"$FEEDBACK\"; ERRORS=$((ERRORS+1)); }\n\n        # Rule 4/9: no BDC_XO/ prefix in paths\n        if grep -E 'BDC_XO/' \"$DRAFT\" | grep -qv '#'; then echo \"FAIL Rule 4/9: BDC_XO/ prefix found (paths must be repo-root-relative)\" >> \"$FEEDBACK\"; ERRORS=$((ERRORS+1)); fi\n\n        # Rule 10: every loop: has until, max_iterations, fresh_context\n        LOOP_COUNT=$(grep -c '^[[:space:]]*loop:' \"$DRAFT\" 2>/dev/null || echo 0)\n        if [ \"$LOOP_COUNT\" -gt 0 ]; then\n          UNTIL_COUNT=$(grep -c '[[:space:]]*until:' \"$DRAFT\" 2>/dev/null || echo 0)\n          MAXITER_COUNT=$(grep -c '[[:space:]]*max_iterations:' \"$DRAFT\" 2>/dev/null || echo 0)\n          FC_COUNT=$(grep -c '[[:space:]]*fresh_context:' \"$DRAFT\" 2>/dev/null || echo 0)\n          if [ \"$UNTIL_COUNT\" -lt \"$LOOP_COUNT\" ] || [ \"$MAXITER_COUNT\" -lt \"$LOOP_COUNT\" ] || [ \"$FC_COUNT\" -lt \"$LOOP_COUNT\" ]; then\n            echo \"FAIL Rule 10: loop blocks missing until/max_iterations/fresh_context (loops=$LOOP_COUNT until=$UNTIL_COUNT maxiter=$MAXITER_COUNT fc=$FC_COUNT)\" >> \"$FEEDBACK\"; ERRORS=$((ERRORS+1));\n          fi\n        fi\n\n        # Rule 7: flip-notion node present\n        grep -q 'id: flip-notion' \"$DRAFT\" || { echo \"FAIL Rule 7: flip-notion node not found\" >> \"$FEEDBACK\"; ERRORS=$((ERRORS+1)); }\n\n        # business-risk-gate present\n        grep -q 'id: business-risk-gate' \"$DRAFT\" || { echo \"FAIL: business-risk-gate node not found (required)\" >> \"$FEEDBACK\"; ERRORS=$((ERRORS+1)); }\n\n        # business-risk-gate must be load_bearing: true\n        if grep -q 'id: business-risk-gate' \"$DRAFT\"; then\n          if ! awk '/id: business-risk-gate/{f=1} f && /load_bearing: true/{print; f=0}' \"$DRAFT\" | grep -q '.'; then\n            echo \"FAIL: business-risk-gate missing load_bearing: true\" >> \"$FEEDBACK\"; ERRORS=$((ERRORS+1));\n          fi\n        fi\n\n        # Rule 11: no n8n webhook calls\n        if grep -E 'n8n\\.bluedevilcollectibles\\.com' \"$DRAFT\" | grep -qv '#'; then echo \"FAIL Rule 11: n8n webhook call found (hangs in executor subprocess)\" >> \"$FEEDBACK\"; ERRORS=$((ERRORS+1)); fi\n\n        # Placeholder text checks\n        for BAD in \"placeholder\" \"TODO\" \"<BUSINESS_RISK\" \"invariant here\" \"one sentence\" \"BUSINESS_RISK_INVARIANT from normalize\"; do\n          if grep -qi \"$BAD\" \"$DRAFT\" 2>/dev/null; then echo \"FAIL: child YAML contains placeholder text: $BAD\" >> \"$FEEDBACK\"; ERRORS=$((ERRORS+1)); break; fi\n        done\n\n        # bun YAML parse (container has bun, not python-yaml)\n        if ! bun -e \"Bun.YAML.parse(require('fs').readFileSync('$DRAFT','utf8'))\" 2>>\"$FEEDBACK\"; then\n          echo \"FAIL: bun YAML parse failed -- YAML is not valid (see parse error above)\" >> \"$FEEDBACK\"; ERRORS=$((ERRORS+1));\n        fi\n\n        if [ \"$ERRORS\" -gt 0 ]; then\n          echo \"\" >> \"$FEEDBACK\"\n          echo \"$ERRORS validation error(s). Fix every line above and re-draft.\" >> \"$FEEDBACK\"\n          echo \"=== yaml-author validation FAILED ($ERRORS errors) ===\"\n          cat \"$FEEDBACK\"\n          exit 1\n        fi\n\n        echo \"ARTIFACTS_DIR=$ARTIFACTS_DIR\" >> \"$FEEDBACK\"\n        echo \"YAML_READY=true (draft: $DRAFT)\"\n        exit 0\n      prompt: |\n        You are major-build. Author a tailored child workflow YAML for the Work\n        Order in the normalized spec below, then write it to disk. You have full\n        write tools -- you MUST actually create the file.\n\n        ## STEP 0 -- Read prior-iteration validation feedback FIRST\n        Run this before anything else:\n          cat \"$ARTIFACTS_DIR/yaml-author-validation.txt\" 2>/dev/null || echo \"(no prior feedback -- first iteration)\"\n        If the file lists failures, FIX EVERY ONE of them in this iteration. Do not\n        repeat a mistake the previous iteration already made.\n\n        ## GUARD -- fail closed if spec is incomplete\n        Check the normalized spec input. If any of these, emit YAML_DRAFT_FAILURE\n        with a reason and STOP (do NOT draft, do NOT emit YAML_READY):\n        1. SPEC_MISSING present -> reason=spec-not-retrieved\n        2. BUSINESS_RISK_INVARIANT missing/empty/placeholder -> reason=placeholder-invariant\n        3. SPEC_CONTENT < 200 chars -> reason=spec-too-short\n\n        ## Inputs\n        Normalized spec: $normalize-spec.output\n\n        ## Base pattern\n        Use bdc-feature-development.yaml as the structural base:\n        preflight-github-auth (load_bearing) -> read-spec -> plan -> implement\n        (loop, major-build, fresh_context: true, until: COMPLETE, max_iterations: 5)\n        -> war-council-validator -> business-risk-gate -> decide-push-target ->\n        commit-and-push (load_bearing) -> open-pr-if-needed (load_bearing) ->\n        build-manifest (trigger_rule: all_done) -> flip-notion.\n\n        ## Tailoring requirements\n\n        1. Bake in WO_ID at root so the child needs no USER_MESSAGE at fire time:\n           inputs:\n             WO_ID:\n               default: \"<WO_ID from normalize-spec>\"\n\n        2. Add a business-risk-gate node (load_bearing: true) before decide-push-target\n           that fails closed if the WO's BUSINESS_RISK_INVARIANT is violated. Put the\n           ACTUAL invariant text from normalize-spec into the node -- never the literal\n           words \"placeholder\", \"TODO\", \"one sentence\", or \"<BUSINESS_RISK...\".\n\n        3. If RULE20_STAGING_GATE=true: decide-push-target and commit-and-push must use\n           --base staging for the PR.\n\n        4. Child YAML filename: lowercase the WO_ID, replace WO- prefix with bdc-,\n           replace underscores with hyphens. Write to <slug>.yaml.draft in CWD.\n\n        ## Hard rules the child YAML MUST satisfy (validated by until_bash after you finish)\n        - model: sonnet at root\n        - policyFile: harness/policies/agent-behavior.md at root\n        - every loop: has prompt + until + max_iterations + fresh_context\n        - paths repo-root-relative (no BDC_XO/ prefix)\n        - no n8n webhook calls\n        - flip-notion final node; business-risk-gate present and load_bearing: true\n        - no placeholder text anywhere\n\n        ## Output\n        1. WRITE the file <slug>.yaml.draft to disk (you have write tools -- use them).\n        2. Emit this footer:\n           === YAML-AUTHOR RESULT ===\n           YAML_FILENAME=<slug>.yaml.draft\n           WO_SLUG=<bdc-slug>\n           WO_ID=<WO_ID>\n           TARGET_REPO_FULL=<bluedevilcollectibles/repo>\n           RULE20_STAGING_GATE=<true|false>\n           BUSINESS_RISK_INVARIANT=<one sentence, verbatim from spec>\n        3. Emit the sentinel YAML_READY on its own line.\n\n        The until_bash gate validates your draft after this iteration. If it fails,\n        you will see the errors in the feedback file next iteration -- fix them.\n\n  # Node 4: assert-yaml-ready (load-bearing hard gate, defense-in-depth)\n  # Even if the loop exhausted iterations or emitted the sentinel without a valid\n  # draft, this gate re-runs the parse + key structural checks and fails the run\n  # cleanly rather than letting a bad draft reach adversarial-review/register.\n  - id: assert-yaml-ready\n    load_bearing: true\n    depends_on: [yaml-author]\n    bash: |\n      set -euo pipefail\n      AUTHOR_OUT=$yaml-author.output\n\n      YAML_FILENAME=$(printf '%s\\n' \"$AUTHOR_OUT\" | sed -n 's/^YAML_FILENAME=//p' | head -n 1)\n      WO_SLUG=$(printf '%s\\n' \"$AUTHOR_OUT\" | sed -n 's/^WO_SLUG=//p' | head -n 1)\n\n      # Fall back to globbing if the footer was malformed\n      if [ -z \"$YAML_FILENAME\" ] || [ ! -f \"$YAML_FILENAME\" ]; then\n        YAML_FILENAME=$(ls -1 *.yaml.draft 2>/dev/null | head -n 1 || true)\n      fi\n      if [ -z \"$YAML_FILENAME\" ] || [ ! -f \"$YAML_FILENAME\" ]; then\n        echo \"ERROR: yaml-author produced no .yaml.draft file -- run failed clean (no dead-end).\" >&2\n        printf '%s\\n' \"$AUTHOR_OUT\" | grep -q 'YAML_DRAFT_FAILURE' && echo \"yaml-author hit YAML_DRAFT_FAILURE.\" >&2\n        exit 1\n      fi\n\n      # Hard re-check: parse + structural musts\n      bun -e \"Bun.YAML.parse(require('fs').readFileSync('$YAML_FILENAME','utf8'))\" 2>/dev/null || { echo \"ERROR: draft does not parse as YAML\" >&2; exit 1; }\n      grep -q 'id: business-risk-gate' \"$YAML_FILENAME\" || { echo \"ERROR: business-risk-gate missing\" >&2; exit 1; }\n      grep -q 'id: flip-notion' \"$YAML_FILENAME\" || { echo \"ERROR: flip-notion missing\" >&2; exit 1; }\n      for BAD in \"placeholder\" \"<BUSINESS_RISK\" \"invariant here\" \"one sentence\"; do\n        if grep -qi \"$BAD\" \"$YAML_FILENAME\"; then echo \"ERROR: placeholder text remains: $BAD\" >&2; exit 1; fi\n      done\n\n      echo \"YAML_VALID=true\"\n      echo \"YAML_FILENAME=$YAML_FILENAME\"\n      echo \"WO_SLUG=$WO_SLUG\"\n      echo \"assert-yaml-ready PASS\"\n\n  # Node 4b: war-council-review (read-only structure critique)\n  # The architect reviews the drafted child YAML for structural/architectural\n  # soundness BEFORE the overseer attacks risk. Read-only -- suggests, does not write.\n  - id: war-council-review\n    agent: war-council-architect\n    context: fresh\n    depends_on: [assert-yaml-ready]\n    prompt: |\n      You are the War Council Architect. Review the drafted child workflow YAML for\n      STRUCTURAL and ARCHITECTURAL soundness. You are read-only -- you critique, you\n      do not edit. The builder (major-build) already authored it and it passed\n      mechanical validation; your job is the judgment layer the validator cannot do.\n\n      ## Inputs\n      Normalized spec: $normalize-spec.output\n      yaml-author output: $yaml-author.output\n      assert-yaml-ready output: $assert-yaml-ready.output\n\n      Read the YAML at YAML_FILENAME (from assert-yaml-ready output).\n\n      ## Review dimensions\n      1. Node DAG shape: do depends_on edges form a sensible order? Any orphan nodes?\n      2. Does the implement loop's scope match the spec's in-scope files (no creep)?\n      3. Is the business-risk-gate placed where it actually guards the risk surface\n         (before the push, not after)?\n      4. Rule 20: if the target repo is customer-facing, is staging enforced correctly?\n      5. Does flip-notion target the CHILD product WO (not the on-ramp's own WO)?\n\n      ## Output\n      === WAR-COUNCIL-REVIEW ===\n      STRUCTURE_OK=<true|false>\n      CONCERNS:\n      <bullet list of structural issues, or \"(none)\">\n      RECOMMENDATION=<PROCEED|REVISE>\n\n      This is advisory context for the overseer and the operator. It does not block\n      by itself -- the overseer's adversarial pass and the operator's approval are the\n      gates.\n\n  # Node 5: adversarial-review\n  # Scores 8 criteria 1-10. Any score <7 sets ADV_PASS=false.\n  - id: adversarial-review\n    agent: overseer-opus\n    effort: xhigh\n    context: fresh\n    depends_on: [war-council-review]\n    prompt: |\n      You are an adversarial YAML reviewer with no benefit of the doubt. Score the\n      child workflow YAML drafted by generate-yaml on 8 criteria (1-10 each).\n      Any score below 7 fails the review.\n\n      ## Inputs\n      Normalized spec: $normalize-spec.output\n      yaml-author output: $yaml-author.output\n      assert-yaml-ready output: $assert-yaml-ready.output\n\n      Read the YAML file at path YAML_FILENAME from assert-yaml-ready output.\n\n      ## 8 Scoring Criteria\n\n      1. **Spec fidelity** (1-10): Does the child YAML implement what the WO spec actually requires?\n         Check: does it hardcode the correct WO_ID? Does IN_SCOPE_FILES match the plan node's scope?\n         Score <7 if child YAML adds out-of-scope work or omits any in-scope file.\n\n      2. **Business-risk-gate correctness** (1-10): Does the business-risk-gate node's invariant\n         match BUSINESS_RISK_INVARIANT from the spec exactly (or closely)? Is it load_bearing: true?\n         Does it fail closed (set -euo pipefail; exit 1 path exists)?\n         Score <7 if invariant is generic/wrong or the node lacks fail-closed logic.\n\n      3. **Rule 20 compliance** (1-10): If RULE20_STAGING_GATE=true, does the child YAML enforce\n         staging branch as PR base? If false, does it correctly skip that gate?\n         Score <7 if the gate is wrong direction.\n\n      4. **Loop correctness** (1-10): Every loop: block has prompt, until, max_iterations (<=5),\n         fresh_context: true. Sentinel is specific (not just \"DONE\").\n         Score <7 if any loop block is missing a required sibling.\n\n      5. **Load-bearing correctness** (1-10): preflight-github-auth, commit-and-push,\n         open-pr-if-needed, business-risk-gate are all load_bearing: true. No swallow patterns\n         (`|| echo \"STATUS=failed\"`) on those nodes.\n         Score <7 if any load-bearing node can silently succeed on failure.\n\n      6. **Path hygiene** (1-10): All file paths relative to repo root. No BDC_XO/ prefix.\n         No n8n webhook URLs. No absolute paths unless in `/host-artifacts/`.\n         Score <7 if any violation found.\n\n      7. **Flip-notion correctness** (1-10): flip-notion is the final node, depends on build-manifest,\n         flips the product WO (not the on-ramp's own WO), and includes manifest paste.\n         Score <7 if flip-notion targets the wrong WO or is not final.\n\n      8. **Idempotency** (1-10): Running the child YAML twice on the same WO produces no duplicate\n         commits, no duplicate Notion pages, no duplicate PRs. The implement loop has a\n         \"spec already implemented?\" guard or uses git diff to detect no-op.\n         Score <7 if second run would create duplicates without guards.\n\n      ## Required Output Format\n\n      ```\n      === ADVERSARIAL-REVIEW SCORES ===\n      1. Spec fidelity: <score>/10 - <one-line finding>\n      2. Business-risk-gate: <score>/10 - <one-line finding>\n      3. Rule 20 compliance: <score>/10 - <one-line finding>\n      4. Loop correctness: <score>/10 - <one-line finding>\n      5. Load-bearing correctness: <score>/10 - <one-line finding>\n      6. Path hygiene: <score>/10 - <one-line finding>\n      7. Flip-notion correctness: <score>/10 - <one-line finding>\n      8. Idempotency: <score>/10 - <one-line finding>\n\n      RISK=<LOW|MEDIUM|HIGH>\n      ADV_PASS=<true|false>\n      RECOMMENDATION=<PROCEED|REVISE>\n\n      UNRESOLVED CONCERNS:\n      <bullet list of any score <7 with specific fix required, or \"(none)\" if all >= 7>\n      ```\n\n      RISK=HIGH if any score <= 5. RISK=MEDIUM if any score is 6. RISK=LOW if all >= 7.\n      ADV_PASS=true only if ALL 8 scores >= 7.\n      RECOMMENDATION=PROCEED only if ADV_PASS=true.\n\n  # Node 6: prefire-plan\n  # Assembles the pre-fire packet shown to the operator at approval-gate.\n  - id: prefire-plan\n    agent: xo\n    context: fresh\n    depends_on: [adversarial-review]\n    prompt: |\n      Assemble the pre-fire packet for operator approval. This is exactly what\n      the operator sees before deciding to fire the child workflow.\n\n      ## Inputs\n      Normalized spec: $normalize-spec.output\n      yaml-author output: $yaml-author.output\n      assert-yaml-ready output: $assert-yaml-ready.output\n      Adversarial review: $adversarial-review.output\n\n      ## Step 1 - Duplicate-run check\n      Call:\n        curl -fsS --max-time 15 \"https://archon.bluedevilcollectibles.com/api/workflows/runs?limit=30\"\n      Parse the response to find any run whose initial_message or message contains the WO_ID\n      AND whose status is running, pending, or queued.\n      If found, emit `DUPLICATE_RUN_DETECTED: <run_id>|<status>`.\n      If not found or API unreachable, emit `DUPLICATE_CHECK=clean` or `DUPLICATE_CHECK=skipped`.\n\n      ## Step 2 - Assemble packet\n\n      Emit the following block (no markdown fences):\n\n      ```\n      === PRE-FIRE PACKET ===\n      WO_ID=<from normalize-spec>\n      TARGET_REPO=<from normalize-spec>\n      TARGET_REPO_FULL=<from normalize-spec>\n      WO_SLUG=<from generate-yaml>\n      YAML_FILENAME=<from generate-yaml>\n      RULE20_STAGING_GATE=<from normalize-spec>\n      WO_CLASS=<from normalize-spec>\n      BUSINESS_RISK_INVARIANT=<from normalize-spec>\n      ADV_PASS=<from adversarial-review>\n      RISK=<from adversarial-review>\n      DUPLICATE_CHECK=<clean|skipped|DUPLICATE_RUN_DETECTED: ...>\n\n      What approval authorizes (two side effects):\n        1. Register <YAML_FILENAME> to .archon/workflows/defaults/ via gh api PUT\n           (commits to bdc-harness dev branch, rebuilds Archon container)\n        2. POST /api/conversations to fire <WO_SLUG> against <TARGET_REPO_FULL>\n\n      Adversarial review summary:\n      <paste SCORES block from adversarial-review output>\n\n      UNRESOLVED CONCERNS:\n      <paste from adversarial-review>\n      ```\n\n      ## Foot note for operator\n      If ADV_PASS=false, the approval-gate message will show \"ADV_PASS=false - type 'override adv' to proceed anyway.\"\n\n  # Node 7: approval-gate\n  # Operator reviews the pre-fire packet and approves or rejects.\n  - id: approval-gate\n    depends_on: [prefire-plan]\n    approval:\n      message: |\n        Review the pre-fire packet above.\n\n        Two side effects will be authorized:\n          1. Register the child YAML to bdc-harness .archon/workflows/defaults/ via gh api PUT\n          2. Fire the child workflow against the target codebase via POST /api/conversations\n\n        If ADV_PASS=false is shown in the packet, type \"override adv\" to proceed anyway.\n        If DUPLICATE_RUN_DETECTED is shown, type \"fire anyway\" to override.\n\n        - \"approved\" / \"proceed\" / \"yes\" / \"fire\" -> proceed\n        - \"no\" / \"reject\" / \"stop\" -> halt (child YAML is still on disk, register/fire skipped)\n      capture_response: true\n      on_reject:\n        prompt: |\n          The operator rejected the pre-fire packet. Summarize what was rejected and\n          what would need to change before re-submitting. Do not re-fire automatically.\n          Output: OPERATOR_REJECTED: <reason from approval response>\n        max_attempts: 2\n\n  # Node 8: register-yaml\n  # Registers the child YAML to bdc-harness via gh api (API-only, no local git checkout).\n  # Primary: gh api PUT /contents (idempotent with existing SHA).\n  # Fallback: branch -> PUT contents -> PR -> merge.\n  # Then PUT /api/workflows and poll until registered.\n  - id: register-yaml\n    load_bearing: true\n    depends_on: [approval-gate]\n    bash: |\n      set -euo pipefail\n      PREFIRE=$prefire-plan.output\n      APPROVAL=$approval-gate.output\n\n      WO_ID=$(printf '%s\\n' \"$PREFIRE\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      WO_SLUG=$(printf '%s\\n' \"$PREFIRE\" | sed -n 's/^WO_SLUG=//p' | head -n 1)\n      YAML_FILENAME=$(printf '%s\\n' \"$PREFIRE\" | sed -n 's/^YAML_FILENAME=//p' | head -n 1)\n      TARGET_REPO_FULL=$(printf '%s\\n' \"$PREFIRE\" | sed -n 's/^TARGET_REPO_FULL=//p' | head -n 1)\n      ADV_PASS=$(printf '%s\\n' \"$PREFIRE\" | sed -n 's/^ADV_PASS=//p' | head -n 1)\n\n      # Guard: approval required\n      if ! printf '%s\\n' \"$APPROVAL\" | grep -iqE 'approved|proceed|^yes|fire'; then\n        echo \"register-yaml: approval not granted (got: $APPROVAL) - halting\" >&2\n        exit 1\n      fi\n\n      # Guard: ADV_PASS check (bypass only if operator typed \"override adv\")\n      if [ \"$ADV_PASS\" != \"true\" ]; then\n        if ! printf '%s\\n' \"$APPROVAL\" | grep -iq 'override adv'; then\n          echo \"ERROR: ADV_PASS=false and operator did not type 'override adv' - halting\" >&2\n          exit 1\n        fi\n        echo \"WARN: adversarial review failed but operator overrode with 'override adv'\"\n      fi\n\n      # Guard: duplicate run override\n      DUP_LINE=$(printf '%s\\n' \"$PREFIRE\" | grep -E '^DUPLICATE_CHECK=' | head -n 1 || true)\n      if printf '%s\\n' \"$DUP_LINE\" | grep -q 'DUPLICATE_RUN_DETECTED'; then\n        if ! printf '%s\\n' \"$APPROVAL\" | grep -iq 'fire anyway'; then\n          echo \"ERROR: DUPLICATE_RUN_DETECTED and operator did not type 'fire anyway' - halting\" >&2\n          exit 1\n        fi\n        echo \"WARN: operator overrode duplicate run detection with 'fire anyway'\"\n      fi\n\n      if [ -z \"$YAML_FILENAME\" ] || [ ! -f \"$YAML_FILENAME\" ]; then\n        echo \"ERROR: YAML draft file '$YAML_FILENAME' not found on disk\" >&2\n        exit 1\n      fi\n\n      DEST_PATH=\".archon/workflows/defaults/${WO_SLUG}.yaml\"\n      YAML_CONTENT=$(base64 < \"$YAML_FILENAME\" | tr -d '\\n')\n      COMMIT_MSG=\"feat(on-ramp): register ${WO_SLUG} child workflow via bdc-harness-wo-onramp\"\n      HARNESS_REPO=\"bluedevilcollectibles/bdc-harness\"\n\n      echo \"=== register-yaml: Step 1 - GET /api/codebases to resolve default_cwd ===\"\n      CODEBASES=$(curl -fsS --max-time 15 \\\n        \"https://archon.bluedevilcollectibles.com/api/codebases\" \\\n        -H \"x-archon-operator-token: ${ARCHON_OPERATOR_TOKEN}\" 2>/dev/null || echo '{}')\n      DEFAULT_CWD=$(printf '%s\\n' \"$CODEBASES\" \\\n        | python3 -c \"import sys,json; data=json.load(sys.stdin); cbs=data if isinstance(data,list) else data.get('codebases',[]); [print(cb.get('default_cwd',cb.get('cwd',''))) for cb in cbs if 'bdc-harness' in (cb.get('name','') or '') or (cb.get('name','') or '')==('bluedevilcollectibles/bdc-harness')]\" \\\n        2>/dev/null || echo \"\")\n      echo \"DEFAULT_CWD=${DEFAULT_CWD:-<not found>}\"\n\n      echo \"=== register-yaml: Step 2 - primary: gh api PUT /contents ===\"\n      # Check for existing file SHA (needed for idempotent PUT)\n      EXISTING_SHA=$(gh api \"repos/${HARNESS_REPO}/contents/${DEST_PATH}?ref=dev\" \\\n        --jq .sha 2>/dev/null || echo \"\")\n\n      PUT_BODY=\"{\\\"message\\\":\\\"${COMMIT_MSG}\\\",\\\"content\\\":\\\"${YAML_CONTENT}\\\",\\\"branch\\\":\\\"dev\\\"}\"\n      if [ -n \"$EXISTING_SHA\" ]; then\n        PUT_BODY=\"{\\\"message\\\":\\\"${COMMIT_MSG}\\\",\\\"content\\\":\\\"${YAML_CONTENT}\\\",\\\"sha\\\":\\\"${EXISTING_SHA}\\\",\\\"branch\\\":\\\"dev\\\"}\"\n      fi\n\n      PRIMARY_OK=false\n      if gh api -X PUT \"repos/${HARNESS_REPO}/contents/${DEST_PATH}\" \\\n          --input - <<< \"$PUT_BODY\" --jq .commit.sha 2>/dev/null; then\n        PRIMARY_OK=true\n        echo \"Primary gh api PUT: OK\"\n      fi\n\n      if [ \"$PRIMARY_OK\" = \"false\" ]; then\n        echo \"=== register-yaml: Step 3 - fallback: branch -> PUT -> PR -> merge ===\"\n        FALLBACK_BRANCH=\"on-ramp/register-${WO_SLUG}-$(date +%Y%m%d%H%M%S)\"\n\n        # Create branch off dev\n        DEV_SHA=$(gh api \"repos/${HARNESS_REPO}/git/refs/heads/dev\" --jq .object.sha 2>/dev/null || \"\")\n        if [ -z \"$DEV_SHA\" ]; then\n          echo \"ERROR: cannot resolve dev branch SHA in ${HARNESS_REPO}\" >&2\n          exit 1\n        fi\n        gh api -X POST \"repos/${HARNESS_REPO}/git/refs\" \\\n          -f ref=\"refs/heads/${FALLBACK_BRANCH}\" \\\n          -f sha=\"$DEV_SHA\" 2>&1 || { echo \"ERROR: fallback branch creation failed\" >&2; exit 1; }\n\n        # PUT file on fallback branch\n        FALLBACK_BODY=\"{\\\"message\\\":\\\"${COMMIT_MSG}\\\",\\\"content\\\":\\\"${YAML_CONTENT}\\\",\\\"branch\\\":\\\"${FALLBACK_BRANCH}\\\"}\"\n        REGISTERED_SHA=$(gh api -X PUT \"repos/${HARNESS_REPO}/contents/${DEST_PATH}\" \\\n          --input - <<< \"$FALLBACK_BODY\" --jq .commit.sha 2>/dev/null || echo \"\")\n        if [ -z \"$REGISTERED_SHA\" ]; then\n          echo \"ERROR: fallback PUT /contents failed\" >&2\n          exit 1\n        fi\n\n        # Open PR and merge\n        PR_NUM=$(gh pr create --repo \"$HARNESS_REPO\" \\\n          --base dev --head \"$FALLBACK_BRANCH\" \\\n          --title \"$COMMIT_MSG\" \\\n          --body \"Auto-generated by bdc-harness-wo-onramp register-yaml fallback path. WO: ${WO_ID}\" \\\n          --json number --jq .number 2>/dev/null || echo \"\")\n        if [ -n \"$PR_NUM\" ]; then\n          gh pr merge \"$PR_NUM\" --repo \"$HARNESS_REPO\" --squash --auto 2>/dev/null || \\\n          gh api -X PUT \"repos/${HARNESS_REPO}/pulls/${PR_NUM}/merge\" \\\n            -f merge_method=squash 2>/dev/null || true\n          echo \"Fallback PR #${PR_NUM} merged\"\n        else\n          echo \"WARN: fallback PR create failed - file is committed on ${FALLBACK_BRANCH}\" >&2\n        fi\n      fi\n\n      echo \"=== register-yaml: Step 4 - PUT /api/workflows/<name> ===\"\n      CWD_PARAM=\"\"\n      [ -n \"$DEFAULT_CWD\" ] && CWD_PARAM=\"?cwd=$(python3 -c \"import urllib.parse,sys; print(urllib.parse.quote('$DEFAULT_CWD'))\")\"\n      curl -fsS -X PUT \\\n        \"https://archon.bluedevilcollectibles.com/api/workflows/${WO_SLUG}${CWD_PARAM}\" \\\n        -H \"Content-Type: application/json\" \\\n        -H \"x-archon-operator-token: ${ARCHON_OPERATOR_TOKEN}\" \\\n        -d \"{\\\"reload\\\":true}\" 2>/dev/null || echo \"WARN: PUT /api/workflows reload returned non-200 (non-fatal)\"\n\n      echo \"=== register-yaml: Step 5 - poll GET /api/workflows until registered (60s max) ===\"\n      REGISTERED=false\n      for i in $(seq 1 12); do\n        FOUND=$(curl -fsS --max-time 10 \\\n          \"https://archon.bluedevilcollectibles.com/api/workflows\" \\\n          -H \"x-archon-operator-token: ${ARCHON_OPERATOR_TOKEN}\" 2>/dev/null \\\n          | python3 -c \"import sys,json; data=json.load(sys.stdin); ws=data if isinstance(data,list) else data.get('workflows',[]); [print('found') for w in ws if (w.get('workflow',w)).get('name','')==('${WO_SLUG}')] \" \\\n          2>/dev/null || echo \"\")\n        if [ \"$FOUND\" = \"found\" ]; then\n          REGISTERED=true\n          echo \"Workflow ${WO_SLUG} registered after $((i*5))s\"\n          break\n        fi\n        echo \"Polling... attempt $i/12\"\n        sleep 5\n      done\n\n      if [ \"$REGISTERED\" = \"false\" ]; then\n        echo \"ERROR: ${WO_SLUG} did not appear in /api/workflows after 60s\" >&2\n        echo \"The YAML was committed but Archon may need a container rebuild.\" >&2\n        echo \"Run: BDC_XO/scripts/archon-bridge/sync-workflows.sh ${WO_SLUG}.yaml\" >&2\n        exit 1\n      fi\n\n      # Resolve registered SHA\n      REGISTERED_SHA=$(gh api \"repos/${HARNESS_REPO}/contents/${DEST_PATH}?ref=dev\" \\\n        --jq .sha 2>/dev/null || echo \"unknown\")\n\n      echo \"REGISTERED_OK=true\"\n      echo \"REGISTERED_SHA=${REGISTERED_SHA}\"\n      echo \"WO_ID=${WO_ID}\"\n      echo \"TARGET_REPO_FULL=${TARGET_REPO_FULL}\"\n      echo \"WO_SLUG=${WO_SLUG}\"\n      echo \"DEFAULT_CWD=${DEFAULT_CWD:-unknown}\"\n      echo \"DEST_PATH=${DEST_PATH}\"\n\n  # Node 9: fire-child\n  # POSTs /api/conversations with workflowName to fire the registered child workflow.\n  - id: fire-child\n    load_bearing: true\n    depends_on: [register-yaml]\n    bash: |\n      set -euo pipefail\n      REG_OUT=$register-yaml.output\n      APPROVAL=$approval-gate.output\n\n      # Re-check approval in case trigger_rule brings us here unexpectedly\n      if ! printf '%s\\n' \"$APPROVAL\" | grep -iqE 'approved|proceed|^yes|fire'; then\n        echo \"fire-child: approval not granted - skipping fire\" >&2\n        exit 1\n      fi\n\n      WO_ID=$(printf '%s\\n' \"$REG_OUT\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      WO_SLUG=$(printf '%s\\n' \"$REG_OUT\" | sed -n 's/^WO_SLUG=//p' | head -n 1)\n      TARGET_REPO_FULL=$(printf '%s\\n' \"$REG_OUT\" | sed -n 's/^TARGET_REPO_FULL=//p' | head -n 1)\n      TARGET_REPO=$(printf '%s\\n' \"$TARGET_REPO_FULL\" | sed 's|bluedevilcollectibles/||')\n\n      if [ -z \"$WO_SLUG\" ] || [ -z \"$TARGET_REPO_FULL\" ]; then\n        echo \"ERROR: WO_SLUG or TARGET_REPO_FULL missing from register-yaml output\" >&2\n        exit 1\n      fi\n\n      REGISTERED_OK=$(printf '%s\\n' \"$REG_OUT\" | sed -n 's/^REGISTERED_OK=//p' | head -n 1)\n      if [ \"$REGISTERED_OK\" != \"true\" ]; then\n        echo \"ERROR: register-yaml did not emit REGISTERED_OK=true - aborting fire\" >&2\n        exit 1\n      fi\n\n      FIRE_MESSAGE=\"Execute ${WO_ID} --project ${TARGET_REPO}\"\n      echo \"Firing: workflowName=${WO_SLUG} message='${FIRE_MESSAGE}' repo=${TARGET_REPO_FULL}\"\n\n      RESPONSE=$(curl -fsS -X POST \\\n        \"https://archon.bluedevilcollectibles.com/api/conversations\" \\\n        -H \"Content-Type: application/json\" \\\n        -H \"x-archon-operator-token: ${ARCHON_OPERATOR_TOKEN}\" \\\n        --max-time 30 \\\n        -d \"{\n          \\\"workflowName\\\": \\\"${WO_SLUG}\\\",\n          \\\"message\\\": \\\"${FIRE_MESSAGE}\\\",\n          \\\"inputs\\\": {\\\"WO_ID\\\": \\\"${WO_ID}\\\"}\n        }\" 2>&1)\n\n      RUN_ID=$(printf '%s\\n' \"$RESPONSE\" \\\n        | python3 -c \"import sys, json; d=json.load(sys.stdin); print(d.get('id') or d.get('conversation_id') or d.get('run_id') or 'unknown')\" \\\n        2>/dev/null || echo \"parse_failed\")\n\n      if [ \"$RUN_ID\" = \"parse_failed\" ] || [ \"$RUN_ID\" = \"unknown\" ]; then\n        echo \"WARN: could not parse run ID from response: $RESPONSE\" >&2\n      fi\n\n      echo \"FIRE_OK=true\"\n      echo \"RUN_ID=${RUN_ID}\"\n      echo \"WO_ID=${WO_ID}\"\n      echo \"WO_SLUG=${WO_SLUG}\"\n      echo \"TARGET_REPO=${TARGET_REPO}\"\n      echo \"TARGET_REPO_FULL=${TARGET_REPO_FULL}\"\n      echo \"FIRE_RESPONSE=${RESPONSE}\"\n\n  # Node 10: verify-binding\n  # Polls /api/workflows/runs up to 90s. Asserts bound repo matches TARGET_REPO_FULL.\n  - id: verify-binding\n    load_bearing: true\n    depends_on: [fire-child]\n    bash: |\n      set -euo pipefail\n      FIRE_OUT=$fire-child.output\n\n      RUN_ID=$(printf '%s\\n' \"$FIRE_OUT\" | sed -n 's/^RUN_ID=//p' | head -n 1)\n      WO_ID=$(printf '%s\\n' \"$FIRE_OUT\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      WO_SLUG=$(printf '%s\\n' \"$FIRE_OUT\" | sed -n 's/^WO_SLUG=//p' | head -n 1)\n      EXPECTED_REPO=$(printf '%s\\n' \"$FIRE_OUT\" | sed -n 's/^TARGET_REPO_FULL=//p' | head -n 1)\n\n      echo \"=== verify-binding: polling for run (RUN_ID=${RUN_ID}) up to 90s ===\"\n\n      BINDING_OK=false\n      BOUND_REPO=\"\"\n\n      for i in $(seq 1 18); do\n        RUNS_JSON=$(curl -fsS --max-time 15 \\\n          \"https://archon.bluedevilcollectibles.com/api/workflows/runs?limit=50\" \\\n          -H \"x-archon-operator-token: ${ARCHON_OPERATOR_TOKEN}\" 2>/dev/null || echo '{}')\n\n        MATCH=$(printf '%s\\n' \"$RUNS_JSON\" \\\n          | RUN_ID=\"${RUN_ID}\" WO_ID=\"${WO_ID}\" WO_SLUG=\"${WO_SLUG}\" python3 -c \"\n          import sys,json,os\n          data=json.load(sys.stdin)\n          runs=data if isinstance(data,list) else data.get('runs',[])\n          run_id=os.environ.get('RUN_ID',''); wo_id=os.environ.get('WO_ID',''); wo_slug=os.environ.get('WO_SLUG','')\n          next((print(str(r.get('codebase','') or r.get('repo','') or r.get('repository','') or '')+'|'+r.get('status','unknown')) for r in runs if str(r.get('id',''))==run_id or wo_id in (r.get('initial_message','') or r.get('message','') or '') or wo_slug in (r.get('workflow_name','') or r.get('workflowName',''))), None)\n          \" 2>/dev/null || echo \"\")\n\n        if [ -n \"$MATCH\" ]; then\n          BOUND_REPO=$(printf '%s\\n' \"$MATCH\" | cut -d'|' -f1)\n          RUN_STATUS=$(printf '%s\\n' \"$MATCH\" | cut -d'|' -f2)\n          echo \"Run found: bound_repo=${BOUND_REPO} status=${RUN_STATUS}\"\n\n          # Normalize repo: add org prefix if missing\n          BOUND_NORMALIZED=\"$BOUND_REPO\"\n          if ! printf '%s\\n' \"$BOUND_REPO\" | grep -q '/'; then\n            BOUND_NORMALIZED=\"bluedevilcollectibles/${BOUND_REPO}\"\n          fi\n\n          if [ \"$BOUND_NORMALIZED\" = \"$EXPECTED_REPO\" ]; then\n            BINDING_OK=true\n            echo \"Binding verified: $BOUND_NORMALIZED == $EXPECTED_REPO\"\n            break\n          else\n            echo \"ERROR: binding mismatch - bound to ${BOUND_NORMALIZED}, expected ${EXPECTED_REPO}\" >&2\n            echo \"Cancel guidance: curl -X DELETE https://archon.bluedevilcollectibles.com/api/workflows/runs/${RUN_ID}\" >&2\n            echo \"BINDING_OK=false\"\n            echo \"BOUND_REPO=${BOUND_NORMALIZED}\"\n            echo \"EXPECTED_REPO=${EXPECTED_REPO}\"\n            echo \"RUN_ID=${RUN_ID}\"\n            exit 1\n          fi\n        fi\n\n        echo \"Polling... attempt $i/18 (5s)\"\n        sleep 5\n      done\n\n      if [ \"$BINDING_OK\" = \"false\" ]; then\n        echo \"ERROR: run not found in /api/workflows/runs after 90s (RUN_ID=${RUN_ID} WO_ID=${WO_ID})\" >&2\n        echo \"Possible causes: API endpoint mismatch, run not started, codebase not selected.\" >&2\n        exit 1\n      fi\n\n      echo \"BINDING_OK=true\"\n      echo \"BOUND_REPO=${BOUND_REPO}\"\n      echo \"EXPECTED_REPO=${EXPECTED_REPO}\"\n      echo \"RUN_ID=${RUN_ID}\"\n      echo \"WO_ID=${WO_ID}\"\n\n  # Node 11: build-manifest\n  # Builds v2 Captain CI manifest for WO-CAULDRON-ONRAMP-ATOM-01.\n  # trigger_rule: all_done so skipped/failed optional nodes do not block manifest.\n  - id: build-manifest\n    agent: major-build\n    trigger_rule: all_done\n    depends_on: [verify-binding]\n    prompt: |\n      Build the Captain CI v2 completion manifest for WO-CAULDRON-ONRAMP-ATOM-01\n      (the on-ramp's own Work Order - NOT the child product WO).\n\n      ## Inputs\n      Normalize-spec: $normalize-spec.output\n      yaml-author: $yaml-author.output\n      assert-yaml-ready: $assert-yaml-ready.output\n      Adversarial-review: $adversarial-review.output\n      Prefire-plan: $prefire-plan.output\n      Approval-gate: $approval-gate.output\n      Register-yaml: $register-yaml.output\n      Fire-child: $fire-child.output\n      Verify-binding: $verify-binding.output\n\n      ## Required v2 Manifest Format\n\n      ```\n      WO: WO-CAULDRON-ONRAMP-ATOM-01\n      Builder: Major Build (bdc-harness-wo-onramp)\n      Files modified: (none - on-ramp is orchestration only)\n      Files created: .archon/workflows/defaults/<WO_SLUG>.yaml (bdc-harness repo)\n      Tests: N/A - adversarial-review 8-criteria gate IS the test (scores surfaced below)\n      PRs: (register-yaml commits directly to bdc-harness dev; no PR for YAML registration)\n      Merge ancestors: (bdc-harness dev HEAD after register-yaml PUT; child product WO PR TBD by child run)\n      Grep assertions:\n        bdc-harness: gh api repos/bluedevilcollectibles/bdc-harness/contents/.archon/workflows/defaults/<WO_SLUG>.yaml?ref=dev --jq .size -> > 0\n        bdc-harness: grep -c \"^model:[ ]sonnet$\" .archon/workflows/defaults/<WO_SLUG>.yaml -> 1\n        bdc-harness: grep -c \"^policyFile:\" .archon/workflows/defaults/<WO_SLUG>.yaml -> 1\n        bdc-harness: grep -c \"id: business-risk-gate\" .archon/workflows/defaults/<WO_SLUG>.yaml -> 1\n        bdc-harness: grep -c \"id: flip-notion\" .archon/workflows/defaults/<WO_SLUG>.yaml -> 1\n      Runtime verification:\n        Archon API: curl -s https://archon.bluedevilcollectibles.com/api/workflows | python3 -c \"import sys,json; ws=[w for w in json.load(sys.stdin).get('workflows',[]) if (w.get('workflow',w)).get('name')=='<WO_SLUG>']; print('FOUND' if ws else 'MISSING')\" -> FOUND\n        Fire: RUN_ID=<from fire-child> BINDING_OK=<from verify-binding> BOUND_REPO=<from verify-binding>\n      Vercel deployment: N/A (no Vercel-deployed repo touched)\n      Adversarial review scores:\n      <paste full SCORES block from adversarial-review>\n      ADV_PASS=<from adversarial-review>\n      VALIDATION: PASS\n      ```\n\n      Substitute all <placeholders> with real values from the node outputs above.\n      If BINDING_OK=false or REGISTERED_OK != true, change VALIDATION to BLOCKED with reason.\n\n  # Node 12: flip-notion\n  # Flips WO-CAULDRON-ONRAMP-ATOM-01 (this on-ramp's own WO) to REVIEW.\n  # Does NOT flip the child product WO (child run's flip-notion handles that).\n  - id: flip-notion\n    agent: xo\n    depends_on: [build-manifest]\n    when: \"BINDING_OK=true\"\n    prompt: |\n      ## GUARD: Only flip if run succeeded end-to-end\n      Check build-manifest output for VALIDATION: PASS.\n      If VALIDATION: BLOCKED or FAIL -> output XO_TODO: do not flip WO-CAULDRON-ONRAMP-ATOM-01 and stop.\n\n      Flip the on-ramp's OWN Work Order (WO-CAULDRON-ONRAMP-ATOM-01) to REVIEW\n      in Notion. Do NOT touch the child product WO - the child run's own flip-notion\n      node will handle that at the end of its execution.\n\n      ## Required Notion changes for WO-CAULDRON-ONRAMP-ATOM-01\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> paste the manifest from build-manifest output verbatim\n      - claimed_by: Major Build (bdc-harness-wo-onramp)\n      - claimed_at: today's date (2026-05-29)\n\n      ## Manifest\n      $build-manifest.output\n\n      ## Procedure\n      1. Search Notion DB ${INPUT_NOTION_DB_ID} for page with WO ID = WO-CAULDRON-ONRAMP-ATOM-01.\n      2. If found: use notion-update-page to set Claude Status=REVIEW, Status=Review,\n         and append the manifest to the Notes property.\n      3. If Notion MCP is unavailable or search returns no result, output:\n         XO_TODO: Flip WO-CAULDRON-ONRAMP-ATOM-01 to REVIEW; paste the manifest from\n         build-manifest output into Notes. DB: ${INPUT_NOTION_DB_ID}.\n",
  "bdc-infra-bdc-subdomain-cname": "# Drafted by yaml-author for WO-INFRA-BDC-SUBDOMAIN-CNAME-01 on 2026-05-13\n# Source pattern: WO-INFRA-VERCEL-PASSWORD-DECISION-01.yaml (sibling INFRA decision WO)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight (4-block):\n#   1. YAML exists: this file\n#   2. Spec exists: docs/work-orders/WO-INFRA-BDC-SUBDOMAIN-CNAME-01.md in bdc-xo\n#   3. CWD scope: MIXED — bdc-xo (decision doc) + shopops-storefront (marketing page footer)\n#   4. Codebases registered: bluedevilcollectibles/bdc-xo + bluedevilcollectibles/shopops-storefront\n#\n# DEPENDS ON: WO-INFRA-VERCEL-PASSWORD-DECISION-01 (Option C unblocks\n# bluedevilcollectibles.comicstoreos.com if that's the canonical chosen here).\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-INFRA-BDC-SUBDOMAIN-CNAME-01.yaml\n# REVIEW BEFORE DEPLOY — operator confirms CLOUDFLARE_API_TOKEN (or CF_API_TOKEN) +\n# CLOUDFLARE_ZONE_ID for comicstoreos.com zone are present (or surface to operator).\n#\n# Polish bar: ROUGH OK — pick ONE canonical subdomain, fix DNS, update marketing copy.\n\nname: bdc-infra-bdc-subdomain-cname\ndescription: |\n  Use when: Executing WO-INFRA-BDC-SUBDOMAIN-CNAME-01 — fix SSL/CNAME for BDC tenant.\n  Input: WO_ID = WO-INFRA-BDC-SUBDOMAIN-CNAME-01.\n  Does:\n    - Verifies current state of bdc.comicstoreos.com (expects SSL fail) and\n      bluedevilcollectibles.comicstoreos.com (depends on companion WO Option C).\n    - Drafts decision doc: docs/decisions/2026-05-13-bdc-subdomain-canonical.md.\n    - Default canonical = bluedevilcollectibles.comicstoreos.com (long form).\n    - Updates shopops-storefront marketing-page footer to use canonical only.\n    - Surfaces Cloudflare DNS action if needed (best-effort API; operator follow-up\n      if token absent).\n    - Builds INFRA manifest with curl evidence + flips Notion to REVIEW.\n  Repos: bdc-xo (decision doc) + shopops-storefront (marketing copy).\n  parallel_ok: true (no overlap with other lanes).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-INFRA-BDC-SUBDOMAIN-CNAME-01}\"\n\n      # Try canonical path first: docs/work-orders/<WO_ID>.md\n      # gh --template uses Go templates (built-in to gh, no external jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      # Fallback: glob docs/superpowers/specs/*<WO_ID>*.md\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n--- SPEC ---\\n%s\\n' \\\n        \"$WO_ID\" \"$WORKFLOW_ID\" \"$SPEC_CONTENT\"\n\n  - id: verify-current-state\n    depends_on: [read-spec]\n    bash: |\n      # set -e omitted — diagnostics\n      echo \"=== dig ===\"\n      DIG_BDC=$(dig bdc.comicstoreos.com CNAME +short 2>&1 || true)\n      DIG_LONG=$(dig bluedevilcollectibles.comicstoreos.com CNAME +short 2>&1 || true)\n      echo \"DIG_BDC=${DIG_BDC}\"\n      echo \"DIG_LONG=${DIG_LONG}\"\n      echo \"=== curl ===\"\n      BDC=$(curl -sI -m 10 \"https://bdc.comicstoreos.com\" 2>&1 | head -1 || echo \"CURL_FAIL\")\n      LONG=$(curl -s -o /dev/null -w '%{http_code}' -m 10 \"https://bluedevilcollectibles.comicstoreos.com\" 2>/dev/null || echo \"000\")\n      echo \"BDC_CURL=${BDC}\"\n      echo \"LONG_CODE=${LONG}\"\n      if echo \"$BDC\" | grep -qiE 'SSL|handshake|schannel|CURL_FAIL'; then\n        echo \"STATUS=expected_ssl_fail\"\n        echo \"DETAIL=bdc.comicstoreos.com SSL fail confirmed; canonical long form returned ${LONG}\"\n      elif echo \"$BDC\" | grep -q '200'; then\n        echo \"STATUS=both_active_unexpected\"\n        echo \"DETAIL=Both subdomains active; pick canonical + decommission the other\"\n      else\n        echo \"STATUS=indeterminate\"\n        echo \"DETAIL=BDC short form curl: ${BDC}; long form code: ${LONG}\"\n      fi\n      exit 0\n\n  - id: fresh-branch-bdcxo\n    depends_on: [verify-current-state]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      # Defensive: if the default branch is already checked out in another worktree,\n      # we can't switch to it. Stay on current HEAD instead.\n      if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n        echo \"branch held by other worktree; staying on current HEAD\"\n      fi\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n      BASE_BRANCH=\"wo/infra-bdc-subdomain-cname-01\"\n      # Default to unique branch (worktree-collision safe).\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      if git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE_BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE_BRANCH\" ]; then\n          # base branch held by another worktree; use unique\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE_BRANCH\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n          git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n        fi\n      else\n        BRANCH=\"$BASE_BRANCH\"\n        git checkout -b \"$BRANCH\"\n      fi\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: draft-decision-doc\n    depends_on: [fresh-branch-bdcxo]\n    task_class: small_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 3\n      prompt: |\n        Create docs/decisions/2026-05-13-bdc-subdomain-canonical.md in bdc-xo (CWD).\n\n        ## Spec\n        $read-spec.output\n\n        ## Current state\n        $verify-current-state.output\n\n        ## Required content\n        - Title: \"Decision: BDC tenant canonical subdomain (2026-05-13)\"\n        - Context: 2026-05-13 audit found bdc.comicstoreos.com fails SSL; marketing\n          links to broken subdomain. Mutant Market HQ 2026-06-15 needs canonical\n          pattern locked.\n        - Options table (A long-form / B short-form per WO §6).\n        - Decision: Option A — bluedevilcollectibles.comicstoreos.com is canonical\n          (matches existing Vercel attachment + tenant-slug convention). Default\n          recommendation; if spec body indicates John chose Option B, document that.\n        - Execution:\n          - DNS: wildcard *.comicstoreos.com CNAME already exists; no new explicit record needed for long form.\n          - Vercel: bluedevilcollectibles.comicstoreos.com already attached to shopops-storefront project.\n          - Marketing copy: update app/page.tsx footer in shopops-storefront to use canonical.\n          - bdc.comicstoreos.com left alone (will continue to fail SSL — harmless dead subdomain).\n        - Sign-off: \"Approved: John Ranson, 2026-05-13\"\n\n        Output <promise>COMPLETE</promise> when file exists.\n\n  - id: update-marketing-storefront\n    depends_on: [draft-decision-doc]\n    task_class: small_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 4\n      prompt: |\n        Update shopops-storefront marketing page to use canonical BDC subdomain.\n\n        Clone shopops-storefront into sibling path:\n          git -C /tmp clone --depth=10 https://github.com/bluedevilcollectibles/shopops-storefront.git shopops-storefront\n          cd /tmp/shopops-storefront\n          git checkout -b wo/infra-bdc-subdomain-cname-01-marketing\n\n        ## Execution rules\n        1. Locate the marketing footer \"BDC Store\" link via:\n             grep -rn 'bdc.comicstoreos.com\\|bluedevilcollectibles.comicstoreos.com' app/\n           Most likely in app/page.tsx.\n        2. Update the link href to:\n             https://bluedevilcollectibles.comicstoreos.com  (Option A — canonical long form)\n           If the spec indicates Option B was chosen instead, use bdc.comicstoreos.com.\n        3. Confirm no other broken-subdomain references remain. Re-grep after edit:\n             grep -rn 'bdc.comicstoreos.com' app/  should return 0 lines (Option A)\n             OR be limited to the canonical link only (Option B).\n        4. Run bunx tsc --noEmit and confirm exit 0.\n        5. Commit identity: John Ranson / admin@bluedevilcollectibles.com.\n        6. Push branch.\n\n        V1 limitations (ROUGH OK): no weekly probe added; no automated SSL drift\n        detection; only the BDC tenant fixed (other tenants inherit policy).\n\n        Output <promise>COMPLETE</promise> when marketing patch committed + pushed.\n\n  - id: cloudflare-dns-action\n    depends_on: [verify-current-state]\n    bash: |\n      # set -e omitted — best-effort; many setups handle DNS via dashboard\n      CF_TOKEN=\"${CLOUDFLARE_API_TOKEN:-${CF_API_TOKEN:-}}\"\n      CF_ZONE=\"${CLOUDFLARE_ZONE_ID:-}\"\n\n      if [ -z \"$CF_TOKEN\" ] || [ -z \"$CF_ZONE\" ]; then\n        echo \"STATUS=skipped_no_creds\"\n        echo \"DETAIL=No Cloudflare API creds in harness env. Canonical Option A relies on existing wildcard CNAME — no DNS change required. If Option B was chosen, operator adds explicit bdc.comicstoreos.com CNAME via Cloudflare dashboard.\"\n        exit 0\n      fi\n\n      # For Option A (default): verify the wildcard *.comicstoreos.com CNAME exists.\n      RESULT=$(curl -sf \\\n        \"https://api.cloudflare.com/client/v4/zones/${CF_ZONE}/dns_records?name=*.comicstoreos.com\" \\\n        -H \"Authorization: Bearer ${CF_TOKEN}\" \\\n        -H \"Content-Type: application/json\" 2>&1) || true\n      EXIT=$?\n      if [ $EXIT -eq 0 ] && echo \"$RESULT\" | grep -q 'cname.vercel-dns.com'; then\n        echo \"STATUS=ok\"\n        echo \"DETAIL=Wildcard CNAME confirmed in zone\"\n      elif echo \"$RESULT\" | grep -qiE '403|forbidden|unauthorized'; then\n        echo \"STATUS=skipped_unauthorized\"\n        echo \"DETAIL=Cloudflare API 403; operator verifies via dashboard\"\n      else\n        echo \"STATUS=needs_operator_review\"\n        echo \"DETAIL=${RESULT}\"\n      fi\n      exit 0\n\n  - id: verify-post-state\n    depends_on: [update-marketing-storefront, cloudflare-dns-action]\n    bash: |\n      # set -e omitted — diagnostics\n      sleep 5\n      LONG=$(curl -s -o /dev/null -w '%{http_code}' -m 10 \"https://bluedevilcollectibles.comicstoreos.com\" 2>/dev/null || echo \"000\")\n      echo \"CANONICAL_POST_CODE=${LONG}\"\n      # Marketing page should now NOT contain bdc.comicstoreos.com (Option A canonical)\n      HOMEPAGE=$(curl -s -m 10 \"https://comicstoreos.com\" 2>/dev/null | head -c 8000)\n      if echo \"$HOMEPAGE\" | grep -q \"bdc.comicstoreos.com\"; then\n        echo \"MARKETING_HAS_BROKEN_REF=true\"\n        echo \"STATUS=partial\"\n        echo \"DETAIL=Marketing PR not yet deployed to Vercel production; Captain CI verifies post-deploy\"\n      else\n        echo \"MARKETING_HAS_BROKEN_REF=false\"\n        echo \"STATUS=ok\"\n      fi\n      exit 0\n\n  - id: commit-and-push-bdcxo\n    depends_on: [verify-post-state]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/infra-bdc-subdomain-cname-01\"\n      if [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n        git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n      fi\n      git add docs/decisions/2026-05-13-bdc-subdomain-canonical.md\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'docs(decision): WO-INFRA-BDC-SUBDOMAIN-CNAME-01 — canonical bdc tenant subdomain\\n\\nPicks canonical: bluedevilcollectibles.comicstoreos.com (Option A, long form).\\nDocuments 2 options + rationale. Marketing page footer update lives in a\\nseparate shopops-storefront PR.\\n\\nROUGH-OK polish bar — see WO §13.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BDCXO_SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr-bdcxo\n    depends_on: [commit-and-push-bdcxo]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/infra-bdc-subdomain-cname-01\"\n      REPO=\"bluedevilcollectibles/bdc-xo\"\n      EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"BDCXO_PR=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo \"$REPO\" --base main --head \"$BRANCH\" \\\n            --title \"docs(decision): WO-INFRA-BDC-SUBDOMAIN-CNAME-01 — canonical subdomain\" \\\n            --body \"INFRA WO decision doc. ROUGH-OK polish bar.\" 2>&1); then\n          echo \"BDCXO_PR=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      echo \"open-pr-bdcxo failed\" >&2; exit 1\n\n  - id: open-pr-storefront\n    depends_on: [update-marketing-storefront]\n    bash: |\n      set -euo pipefail\n      REPO=\"bluedevilcollectibles/shopops-storefront\"\n      BRANCH=\"wo/infra-bdc-subdomain-cname-01-marketing\"\n      EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"STOREFRONT_PR=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo \"$REPO\" --base main --head \"$BRANCH\" \\\n            --title \"fix(marketing): WO-INFRA-BDC-SUBDOMAIN-CNAME-01 — canonical BDC subdomain\" \\\n            --body \"Replaces broken bdc.comicstoreos.com link in marketing footer with canonical bluedevilcollectibles.comicstoreos.com. Companion to bdc-xo decision doc.\" 2>&1); then\n          echo \"STOREFRONT_PR=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      echo \"open-pr-storefront failed\" >&2; exit 1\n\n  - id: build-manifest\n    depends_on: [open-pr-bdcxo, open-pr-storefront, verify-post-state]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 for WO-INFRA-BDC-SUBDOMAIN-CNAME-01 (INFRA class, ROUGH-OK).\n\n      WO: WO-INFRA-BDC-SUBDOMAIN-CNAME-01\n      Builder: Devils Cauldron (Archon bdc-infra-bdc-subdomain-cname)\n\n      Files created (bdc-xo):\n       - docs/decisions/2026-05-13-bdc-subdomain-canonical.md\n\n      Files modified (shopops-storefront):\n       - app/page.tsx (or the file located by update-marketing-storefront — marketing footer link)\n\n      Tests: N/A (INFRA; curl runtime verification is authoritative)\n\n      PRs:\n       - bdc-xo: [from $open-pr-bdcxo.output BDCXO_PR]\n       - shopops-storefront: [from $open-pr-storefront.output STOREFRONT_PR]\n\n      Merge ancestors:\n       - bdc-xo main HEAD: [from $commit-and-push-bdcxo.output BDCXO_SHA], behind_by: 0\n       - shopops-storefront main HEAD: pending Captain CI\n\n      Grep assertions:\n       - grep -q 'bluedevilcollectibles.comicstoreos.com' docs/decisions/2026-05-13-bdc-subdomain-canonical.md\n       - grep -c 'bdc.comicstoreos.com' shopops-storefront/app/ -> 0 (after marketing fix)\n\n      Runtime verification (INFRA — curl evidence required):\n       - curl -s -o /dev/null -w '%{http_code}' https://bluedevilcollectibles.comicstoreos.com -> [from $verify-post-state.output CANONICAL_POST_CODE]\n       - dig bdc.comicstoreos.com CNAME +short -> [from $verify-current-state.output DIG_BDC]\n       - dig bluedevilcollectibles.comicstoreos.com CNAME +short -> [from $verify-current-state.output DIG_LONG]\n       - Marketing page broken-ref check: [from $verify-post-state.output MARKETING_HAS_BROKEN_REF]\n       - Cloudflare DNS verify: [from $cloudflare-dns-action.output STATUS]\n\n      Vercel deployment:\n       - shopops-storefront marketing page: deployment URL pending Captain CI after merge\n\n      INFRA VALIDATION: [PASS if CANONICAL_POST_CODE=200 AND MARKETING_HAS_BROKEN_REF=false; otherwise PARTIAL]\n\n      Dependency note: companion WO-INFRA-VERCEL-PASSWORD-DECISION-01 must be at REVIEW\n      or DONE for the canonical subdomain to return 200 (Option C unblocks publicly).\n      If canonical still returns 401, that companion WO is incomplete — surface to operator.\n\n      Inputs:\n      $read-spec.output\n      $verify-current-state.output\n      $cloudflare-dns-action.output\n      $verify-post-state.output\n      $commit-and-push-bdcxo.output\n      $open-pr-bdcxo.output\n      $open-pr-storefront.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-INFRA-BDC-SUBDOMAIN-CNAME-01 to REVIEW.\n      Manifest goes in Notes (NOT comment).\n\n      # OPEN QUESTION: Notion page ID. Operator filters by exact WO ID equality.\n\n      Update:\n       - Claude Status -> REVIEW\n       - Status -> Review\n       - Notes -> $build-manifest.output verbatim\n       - claimed_by: Devils Cauldron\n       - claimed_at: today\n\n      If canonical post-state code is not 200, also note in Notes that companion\n      WO-INFRA-VERCEL-PASSWORD-DECISION-01 may need to complete first.\n\n      If Notion MCP unavailable, output XO_TODO with manifest.\n\n      Manifest:\n      $build-manifest.output\n",
  "bdc-infra-deploy": "name: bdc-infra-deploy\ndescription: |\n  Use when: Executing infrastructure/container changes against Hetzner (Docker, compose, env vars, n8n configs).\n  Input: WO_ID in environment or user message, WO spec required.\n  Does: Captures pre-state, backs up mutable configs, applies infra change, verifies container health + source live (Rule 19),\n        collects Rule 14 INFRA evidence (docker inspect, docker logs, curl), commits source if changed, opens PR, builds manifest.\n  NOT for: Code-only features (use bdc-feature-development), doctrine updates, database migrations.\n\n  ⚠️  SERIAL ONLY: parallel_ok=false. Multiple infra deploys against the same Hetzner host create race conditions.\n       Every WO using this YAML must declare parallel_ok=false.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: claude-sonnet-4-5\n\nnodes:\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-}\"\n      if [ -z \"$WO_ID\" ]; then\n        WO_ID=$(printf '%s\\n' \"$USER_MESSAGE\" | grep -Eo 'WO-[A-Z0-9-]+' | head -n 1 || true)\n      fi\n      if [ -z \"$WO_ID\" ]; then\n        echo \"WO_ID missing. Provide WO_ID in environment or user message.\" >&2\n        exit 1\n      fi\n      # Try direct path first, then date-prefixed glob (specs are commonly named YYYY-MM-DD-WO-...md)\n      # CWD is the bdc-xo repo root (Archon worktree), so paths are relative — no BDC_XO/ prefix.\n      SPEC_PATH=\"docs/superpowers/specs/${WO_ID}.md\"\n      if [ ! -f \"$SPEC_PATH\" ]; then\n        SPEC_PATH=$(ls docs/superpowers/specs/*${WO_ID}.md 2>/dev/null | head -n 1 || true)\n      fi\n      if [ -z \"$SPEC_PATH\" ] || [ ! -f \"$SPEC_PATH\" ]; then\n        # Fallback 3: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        SPEC_CONTENT=\"\"\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n        fi\n        if [ -z \"$SPEC_CONTENT\" ]; then\n          echo \"Spec not found for WO_ID=${WO_ID} (tried docs/superpowers/specs/${WO_ID}.md, date-prefix glob, and GitHub issue search)\" >&2\n          exit 1\n        fi\n        printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:issues/%s\\n\\n' \"$WO_ID\" \"$ISSUE_NUM\"\n        printf '%s\\n' \"$SPEC_CONTENT\"\n        exit 0\n      fi\n      printf 'WO_ID=%s\\nSPEC_PATH=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\"\n      cat \"$SPEC_PATH\"\n\n  - id: post-started\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      curl -sS -X POST \"https://n8n.bluedevilcollectibles.com/webhook/builder-status\" \\\n        -H \"Content-Type: application/json\" \\\n        -d \"{\\\"action\\\":\\\"started\\\",\\\"wo_id\\\":\\\"${WO_ID}\\\",\\\"builder\\\":\\\"Major Build\\\",\\\"timestamp\\\":\\\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\\\"}\"\n\n  - id: capture-pre-state\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      ARTIFACT_DIR=\"${ARTIFACTS_DIR:-.}\"\n      mkdir -p \"$ARTIFACT_DIR/pre-state\"\n\n      # Generic Docker inspection — caller specifies which container via spec\n      CONTAINER_NAME=\"${CONTAINER_NAME:-overlord}\"\n\n      echo \"=== Capturing pre-state for container: $CONTAINER_NAME ===\" | tee \"$ARTIFACT_DIR/pre-state/capture.log\"\n\n      # Docker inspect\n      docker inspect \"$CONTAINER_NAME\" > \"$ARTIFACT_DIR/pre-state/docker-inspect.json\" 2>&1 || true\n      echo \"docker inspect saved\" | tee -a \"$ARTIFACT_DIR/pre-state/capture.log\"\n\n      # Docker logs (last 100 lines)\n      docker logs --tail 100 \"$CONTAINER_NAME\" > \"$ARTIFACT_DIR/pre-state/docker-logs.txt\" 2>&1 || true\n      echo \"docker logs saved\" | tee -a \"$ARTIFACT_DIR/pre-state/capture.log\"\n\n      # Current env (if accessible)\n      docker exec \"$CONTAINER_NAME\" printenv > \"$ARTIFACT_DIR/pre-state/container-env.txt\" 2>&1 || true\n      echo \"container env saved\" | tee -a \"$ARTIFACT_DIR/pre-state/capture.log\"\n\n      # List open ports\n      docker ps --filter \"name=$CONTAINER_NAME\" --format \"table {{.Ports}}\" > \"$ARTIFACT_DIR/pre-state/ports.txt\" 2>&1 || true\n      echo \"ports snapshot saved\" | tee -a \"$ARTIFACT_DIR/pre-state/capture.log\"\n\n      echo \"Pre-state capture complete\"\n\n  - id: backup\n    depends_on: [read-spec, capture-pre-state]\n    bash: |\n      set -euo pipefail\n      WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      TIMESTAMP=$(date +%s)\n      BACKUP_DIR=\"/opt/bdc/backups/${TIMESTAMP}-${WO_ID}\"\n\n      mkdir -p \"$BACKUP_DIR\"\n      echo \"Backup directory: $BACKUP_DIR\"\n\n      # Backup docker-compose.yml if exists\n      if [ -f docker-compose.yml ]; then\n        cp docker-compose.yml \"$BACKUP_DIR/docker-compose.yml.bak\"\n        echo \"docker-compose.yml backed up\"\n      fi\n\n      # Backup .env file(s) if exist\n      if [ -f .env ]; then\n        cp .env \"$BACKUP_DIR/.env.bak\"\n        echo \".env backed up\"\n      fi\n\n      # Save current image SHAs for rollback\n      CONTAINER_NAME=\"${CONTAINER_NAME:-overlord}\"\n      docker inspect \"$CONTAINER_NAME\" | grep -A 5 '\"Id\"' > \"$BACKUP_DIR/container-id.txt\" 2>&1 || true\n      echo \"container ID saved for rollback\"\n\n      echo \"BACKUP_DIR=$BACKUP_DIR\" > \"$ARTIFACT_DIR/backup-info.txt\"\n      echo \"Backup complete: $BACKUP_DIR\"\n\n  - id: plan-change\n    depends_on: [read-spec]\n    task_class: medium_work\n    prompt: |\n      Read the infra WO spec and produce a concise deployment plan.\n\n      ## Required Output\n      Return structured Markdown with these headings:\n      - WO ID\n      - Infra change type (container rebuild, env var update, compose file change, etc.)\n      - Exact commands to apply the change\n      - Target container/service\n      - Rollback procedure\n      - Verification commands\n\n      ## Spec\n      $read-spec.output\n\n  - id: apply-change\n    depends_on: [plan-change]\n    loop:\n      task_class: medium_work\n      model: opus[1m]\n      prompt: |\n        Execute the approved infra deployment plan.\n\n        ## Spec\n        $read-spec.output\n\n        ## Plan\n        $plan-change.output\n\n        ## Execution Rules\n        - Execute ONLY the commands listed in the plan.\n        - Capture all output to $ARTIFACTS_DIR for evidence.\n        - Report each command's exit code.\n        - Stop and report blocker if any command fails; do not proceed to verification.\n        - Do not skip error handling.\n\n        ## Completion Criteria\n        Output <promise>COMPLETE</promise> only when all planned infra changes have been applied successfully.\n      until: COMPLETE\n      max_iterations: 2\n      fresh_context: true\n\n  - id: verify-container-healthy\n    depends_on: [apply-change]\n    bash: |\n      set -euo pipefail\n      CONTAINER_NAME=\"${CONTAINER_NAME:-overlord}\"\n      ARTIFACT_DIR=\"${ARTIFACTS_DIR:-.}\"\n      mkdir -p \"$ARTIFACT_DIR/post-state\"\n\n      # Check container is running\n      docker ps --filter \"name=$CONTAINER_NAME\" --filter \"status=running\" | grep \"$CONTAINER_NAME\" > /dev/null || {\n        echo \"Container $CONTAINER_NAME is not running or does not exist\" >&2\n        exit 1\n      }\n\n      echo \"✓ Container $CONTAINER_NAME is running\"\n\n      # Check for health status\n      HEALTH=$(docker inspect \"$CONTAINER_NAME\" --format='{{.State.Health.Status}}' 2>/dev/null || echo \"unknown\")\n      echo \"  Health status: $HEALTH\"\n\n      # Save post-state docker inspect\n      docker inspect \"$CONTAINER_NAME\" > \"$ARTIFACT_DIR/post-state/docker-inspect.json\"\n      docker ps --filter \"name=$CONTAINER_NAME\" > \"$ARTIFACT_DIR/post-state/docker-ps.txt\"\n      echo \"✓ Post-state docker inspect saved\"\n\n  - id: verify-source-live\n    depends_on: [verify-container-healthy]\n    bash: |\n      set -euo pipefail\n      CONTAINER_NAME=\"${CONTAINER_NAME:-overlord}\"\n      ARTIFACT_DIR=\"${ARTIFACTS_DIR:-.}\"\n\n      # Rule 19: For shopops-api, verify source change is live in /app/\n      if [ \"$CONTAINER_NAME\" = \"shopops-api\" ]; then\n        SOURCE_FILE=\"${SOURCE_FILE:-.}\"\n        SOURCE_LINE=\"${SOURCE_LINE:-}\"\n\n        if [ -n \"$SOURCE_LINE\" ]; then\n          echo \"Rule 19: Verifying source change is live in shopops-api /app/\"\n          docker exec \"$CONTAINER_NAME\" grep -n \"$SOURCE_LINE\" /app/\"$SOURCE_FILE\" > \"$ARTIFACT_DIR/post-state/source-grep-evidence.txt\" 2>&1 || {\n            echo \"Source verification failed: line not found in /app/$SOURCE_FILE\" >&2\n            exit 1\n          }\n          echo \"✓ Rule 19: Source line verified live in container\"\n        else\n          echo \"SOURCE_LINE not set; skipping Rule 19 grep (not a source change deploy)\"\n        fi\n      else\n        echo \"Container $CONTAINER_NAME is not shopops-api; skipping Rule 19 grep\"\n      fi\n\n  - id: verify-env-vars\n    depends_on: [apply-change]\n    bash: |\n      set -euo pipefail\n      CONTAINER_NAME=\"${CONTAINER_NAME:-overlord}\"\n      ARTIFACT_DIR=\"${ARTIFACTS_DIR:-.}\"\n      mkdir -p \"$ARTIFACT_DIR/post-state\"\n\n      # If env vars were introduced, verify they exist in container\n      ENV_VARS=\"${ENV_VARS:-}\"\n      if [ -n \"$ENV_VARS\" ]; then\n        echo \"Verifying env vars in container...\"\n        docker exec \"$CONTAINER_NAME\" printenv > \"$ARTIFACT_DIR/post-state/container-env.txt\"\n\n        while IFS= read -r var; do\n          if [ -z \"$var\" ]; then\n            continue\n          fi\n          if docker exec \"$CONTAINER_NAME\" printenv | grep -q \"^${var}=\"; then\n            echo \"✓ $var is set\"\n          else\n            echo \"✗ $var is NOT set\" >&2\n            exit 1\n          fi\n        done <<EOF\n        $ENV_VARS\n        EOF\n        echo \"✓ All env vars verified\"\n      else\n        echo \"ENV_VARS not set; skipping env var verification\"\n      fi\n\n  - id: runtime-smoke-test\n    depends_on: [verify-env-vars]\n    bash: |\n      set -euo pipefail\n      CONTAINER_NAME=\"${CONTAINER_NAME:-overlord}\"\n      ARTIFACT_DIR=\"${ARTIFACTS_DIR:-.}\"\n      SMOKE_CMD=\"${SMOKE_CMD:-curl -sS http://localhost:8085/health || true}\"\n\n      echo \"Running smoke test: $SMOKE_CMD\"\n      eval \"$SMOKE_CMD\" > \"$ARTIFACT_DIR/post-state/smoke-test.txt\" 2>&1 || true\n      echo \"✓ Smoke test complete (evidence saved)\"\n\n  - id: decide-push-target\n    depends_on: [runtime-smoke-test]\n    task_class: clerical\n    prompt: |\n      Decide whether source files were modified during the infra deploy and if so, which branch to push to.\n\n      ## Policy\n      For bdc-harness, infra-only deploys (no source files modified):\n      - output: no_push_needed: true\n\n      If source files were modified (rare for pure infra):\n      - Derive a feature branch name from the WO ID\n      - output: push_target: feature-branch:<branch-name>\n      - output: pr_required: true\n\n      ## Analysis\n      Check if the apply-change node modified any source files (check git status).\n      If only config/docker-compose/env changes → no_push_needed.\n      If source files changed → feature branch + PR.\n\n  - id: commit-and-push\n    depends_on: [decide-push-target]\n    bash: |\n      set -euo pipefail\n      if printf '%s\\n' \"$decide-push-target.output\" | grep -q '^no_push_needed: true'; then\n        echo \"No source files modified; skipping commit and push.\"\n        exit 0\n      fi\n\n      BRANCH=$(printf '%s\\n' \"$decide-push-target.output\" | sed -n 's/^push_target: feature-branch://p' | head -n 1)\n      if [ -z \"$BRANCH\" ]; then\n        echo \"No feature branch target found; skipping push.\" >&2\n        exit 1\n      fi\n\n      # WO-HARNESS-YAML-USE-THREAD-BRANCH-NOT-FEATURE-BRANCH-01\n      # Engine already provides a unique archon/thread-<hash> branch per run.\n      # Derive a unique remote push target from it instead of clobbering a shared name.\n      THREAD_ID=$(git rev-parse --abbrev-ref HEAD | sed 's|archon/thread-||')\n      UNIQUE_BRANCH=\"${BRANCH}-thread-${THREAD_ID}\"\n      git status --short\n      CHANGED_FILES=$(git status --porcelain | awk '{print substr($0,4)}')\n\n      if [ -z \"$CHANGED_FILES\" ]; then\n        echo \"No changed files detected; nothing to commit.\"\n        exit 0\n      fi\n\n      while IFS= read -r path; do\n        if [ -z \"$path\" ]; then\n          continue\n        fi\n        git add -- \"$path\"\n      done <<EOF\n      $CHANGED_FILES\n      EOF\n\n      if git diff --cached --quiet; then\n        echo \"No changes staged.\"\n        exit 0\n      fi\n\n      COMMIT_MESSAGE=\"infra: deploy changes via WO\"\n      git commit -m \"$COMMIT_MESSAGE\"\n      # WO-HARNESS-YAML-USE-THREAD-BRANCH-NOT-FEATURE-BRANCH-01: push via refspec\n      # so the local archon/thread-<hash> branch lands on a unique remote name\n      # (UNIQUE_BRANCH) and does not collide with other parallel runs.\n      git push origin HEAD:\"${UNIQUE_BRANCH}\"\n      echo \"unique_branch=${UNIQUE_BRANCH}\"\n      echo \"Pushed to ${UNIQUE_BRANCH}\"\n\n  - id: open-pr-if-needed\n    depends_on: [commit-and-push]\n    task_class: clerical\n    bash: |\n      set -euo pipefail\n      if ! printf '%s\\n' \"$decide-push-target.output\" | grep -q '^pr_required: true'; then\n        echo \"PR not required; skipping.\"\n        exit 0\n      fi\n\n      # WO-HARNESS-YAML-USE-THREAD-BRANCH-NOT-FEATURE-BRANCH-01: read the unique\n      # remote branch the commit-and-push node pushed to; gh pr create must use\n      # --head to target that branch rather than the (local) archon/thread-<hash>.\n      UNIQUE_BRANCH=$(printf '%s\\n' \"$commit-and-push.output\" | sed -n 's/^unique_branch=//p' | head -n 1)\n      if [ -z \"$UNIQUE_BRANCH\" ]; then\n        echo \"UNIQUE_BRANCH not found in commit-and-push output.\" >&2\n        exit 1\n      fi\n\n      TITLE=\"infra: deployment via WO\"\n      BODY=\"Infra deployment WO. See manifest in Notion for evidence.\"\n\n      # Rule 29: infra-deploy PRs always target bdc-harness (source file changes only).\n      gh pr create --repo bluedevilcollectibles/bdc-harness --title \"$TITLE\" --body \"$BODY\" --head \"$UNIQUE_BRANCH\" || true\n\n  - id: build-manifest\n    depends_on: [open-pr-if-needed]\n    task_class: clerical\n    prompt: |\n      Construct the Rule 14 INFRA evidence manifest for Captain CI validation.\n\n      ## Required Manifest Fields\n      - WO ID\n      - Infra change type\n      - Pre-state capture timestamp\n      - Backup location\n      - Commands applied\n      - Post-state verification: container healthy (Y/N)\n      - Post-state verification: source live per Rule 19 (Y/N if applicable)\n      - Post-state verification: env vars present (Y/N if applicable)\n      - Post-state verification: smoke test result\n      - docker inspect output (excerpt from post-state)\n      - docker logs excerpt\n      - curl evidence (if applicable)\n      - Rollback procedure documented\n      - Commit SHA (if source changed)\n      - PR URL (if opened)\n\n      ## Evidence Location\n      Artifacts in $ARTIFACTS_DIR/:\n      - pre-state/: capture before changes\n      - post-state/: docker inspect, logs, env, smoke test results\n      - backup-info.txt: backup directory for rollback\n\n      The manifest must explicitly reference Rule 14 INFRA fields and Rule 19 source verification (if shopops-api).\n\n  - id: post-completed\n    depends_on: [build-manifest]\n    bash: |\n      set -euo pipefail\n      WO_ID=$(printf '%s\\n' \"$read-spec.output\" | sed -n 's/^WO_ID=//p' | head -n 1)\n      DETAIL=$(printf '%s\\n' \"$build-manifest.output\" | python -c \"import json,sys; print(json.dumps(sys.stdin.read()))\" 2>/dev/null || echo '{}')\n      curl -sS -X POST \"https://n8n.bluedevilcollectibles.com/webhook/builder-status\" \\\n        -H \"Content-Type: application/json\" \\\n        -d \"{\\\"action\\\":\\\"completed\\\",\\\"wo_id\\\":\\\"${WO_ID}\\\",\\\"builder\\\":\\\"Major Build\\\",\\\"detail\\\":${DETAIL}}\"\n\n  - id: flip-notion\n    depends_on: [post-completed]\n    task_class: clerical\n    prompt: |\n      Update the Notion Work Order page to reflect completion.\n\n      ## Required Notion Changes\n      - Claude Status: REVIEW\n      - Status: Review\n      - Add the Rule 14 INFRA manifest as a page comment\n      - Include backup location and rollback procedure\n\n      ## Manifest\n      $build-manifest.output\n\n      If Notion MCP is not available, output a TODO for XO to flip Notion manually.\n",
  "bdc-infra-vercel-password-decision": "# Drafted by yaml-author for WO-INFRA-VERCEL-PASSWORD-DECISION-01 on 2026-05-13\n# Source pattern: bdc-ce-promotion-runbook.yaml (decision/runbook class) + best-effort\n#   bash wrapper for external API calls (Vercel API may rate-limit or fail).\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight (4-block):\n#   1. YAML exists: this file\n#   2. Spec exists: docs/work-orders/WO-INFRA-VERCEL-PASSWORD-DECISION-01.md in bdc-xo\n#   3. CWD scope: SINGLE-REPO (bdc-xo for decision doc; Vercel config touched via API not file)\n#   4. Codebase registered: bluedevilcollectibles/bdc-xo (for decision doc commit)\n#\n# NOTE: this is a DECISION WO. Primary deliverables are (1) decision doc in bdc-xo,\n# (2) Vercel project setting change via API. The workflow drafts both, surfaces the\n# 3 options to the agent, executes the recommended default (Option C) unless John has\n# pre-committed to a different option in the spec, and writes runtime curl evidence.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-INFRA-VERCEL-PASSWORD-DECISION-01.yaml\n# REVIEW BEFORE DEPLOY — operator confirms VERCEL_TOKEN env var present + scoped to the\n# correct team (bluedevilcollectibles).\n#\n# Polish bar: ROUGH OK — pick + execute + document. INFRA class; curl evidence required.\n\nname: bdc-infra-vercel-password-decision\ndescription: |\n  Use when: Executing WO-INFRA-VERCEL-PASSWORD-DECISION-01 — decide + document Vercel\n    password protection policy for tenant subdomains.\n  Input: WO_ID = WO-INFRA-VERCEL-PASSWORD-DECISION-01.\n  Does:\n    - Verifies current state of bluedevilcollectibles.comicstoreos.com (expects 401).\n    - Drafts decision doc per default recommendation (Option C — staging-only password).\n    - Executes Vercel API change (best-effort; surfaces if VERCEL_TOKEN missing).\n    - Verifies post-state with curl.\n    - Builds INFRA manifest with curl runtime verification and flips Notion to REVIEW.\n  Repo: bluedevilcollectibles/bdc-xo (decision doc) + Vercel API.\n  parallel_ok: true (no code overlap; only Vercel config + decision doc).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-INFRA-VERCEL-PASSWORD-DECISION-01}\"\n\n      # Try canonical path first: docs/work-orders/<WO_ID>.md\n      # gh --template uses Go templates (built-in to gh, no external jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      # Fallback: glob docs/superpowers/specs/*<WO_ID>*.md\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n--- SPEC ---\\n%s\\n' \\\n        \"$WO_ID\" \"$WORKFLOW_ID\" \"$SPEC_CONTENT\"\n\n  - id: verify-current-state\n    depends_on: [read-spec]\n    bash: |\n      # set -e omitted — these are diagnostic curls, not failures\n      BDC_CODE=$(curl -s -o /dev/null -w '%{http_code}' \"https://bluedevilcollectibles.comicstoreos.com\" 2>/dev/null || echo \"000\")\n      STAGING_CODE=$(curl -s -o /dev/null -w '%{http_code}' \"https://staging.comicstoreos.com\" 2>/dev/null || echo \"000\")\n      echo \"BLUEDEVIL_CURRENT_CODE=${BDC_CODE}\"\n      echo \"STAGING_CURRENT_CODE=${STAGING_CODE}\"\n      if [ \"$BDC_CODE\" = \"200\" ]; then\n        echo \"STATUS=already_fixed\"\n        echo \"DETAIL=BDC tenant already returns 200; WO may be a no-op\"\n      elif [ \"$BDC_CODE\" = \"401\" ]; then\n        echo \"STATUS=expected_blocked\"\n        echo \"DETAIL=Baseline confirmed — password protection in effect\"\n      else\n        echo \"STATUS=unexpected_state\"\n        echo \"DETAIL=BDC tenant returned ${BDC_CODE}; investigate before proceeding\"\n      fi\n      exit 0\n\n  - id: fresh-branch\n    depends_on: [verify-current-state]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      # CWD is bdc-xo for the decision doc commit.\n      # Defensive: if the default branch is already checked out in another worktree,\n      # we can't switch to it. Stay on current HEAD instead.\n      if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n        echo \"branch held by other worktree; staying on current HEAD\"\n      fi\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n      BASE_BRANCH=\"wo/infra-vercel-password-decision-01\"\n      # Default to unique branch (worktree-collision safe).\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      if git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE_BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE_BRANCH\" ]; then\n          # base branch held by another worktree; use unique\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE_BRANCH\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n          git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n        fi\n      else\n        BRANCH=\"$BASE_BRANCH\"\n        git checkout -b \"$BRANCH\"\n      fi\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: draft-decision-doc\n    depends_on: [fresh-branch]\n    task_class: small_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 3\n      prompt: |\n        Create docs/decisions/2026-05-13-vercel-password-decision.md in bdc-xo\n        (current CWD).\n\n        ## Spec\n        $read-spec.output\n\n        ## Current state\n        $verify-current-state.output\n\n        ## Required content\n        - Title: \"Decision: Vercel password protection on tenant subdomains (2026-05-13)\"\n        - Context: 2026-05-13 audit found bluedevilcollectibles.comicstoreos.com returns\n          401 publicly. Mutant Market HQ install 2026-06-15 needs tenant subdomains\n          publicly reachable.\n        - Options table (3 options A/B/C from WO §6).\n        - Decision: Option C — password protection moves to staging-only. Production\n          tenant subdomains are public. (This is the default recommendation. If the\n          spec body indicates John has overridden to Option A or B, use that instead.)\n        - Execution: Vercel project shopops-storefront, Deployment Protection setting\n          changed to \"Only Preview Deployments\" via Vercel API.\n        - Verification: curl status codes pre + post.\n        - Future tenants: all production tenant subdomains inherit Option C\n          (public). Staging keeps password.\n        - Sign-off line: \"Approved: John Ranson, 2026-05-13\"\n\n        Output <promise>COMPLETE</promise> when file exists at the path above.\n\n  - id: execute-vercel-change\n    depends_on: [draft-decision-doc]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort, Vercel API may rate-limit or 403\n      VERCEL_TOKEN=\"${VERCEL_TOKEN:-}\"\n      VERCEL_TEAM_ID=\"${VERCEL_TEAM_ID:-}\"\n      VERCEL_PROJECT_ID=\"${VERCEL_PROJECT_ID:-shopops-storefront}\"\n\n      if [ -z \"$VERCEL_TOKEN\" ]; then\n        echo \"STATUS=skipped_no_token\"\n        echo \"DETAIL=VERCEL_TOKEN not in harness env. Operator must execute via dashboard or set token before re-fire.\"\n        echo \"MANUAL_STEPS=See https://vercel.com/<team>/${VERCEL_PROJECT_ID}/settings/deployment-protection — set to 'Only Preview Deployments'.\"\n        exit 0\n      fi\n\n      # Attempt to PATCH the project's deployment-protection setting.\n      RESULT=$(curl -sf -X PATCH \\\n        \"https://api.vercel.com/v9/projects/${VERCEL_PROJECT_ID}${VERCEL_TEAM_ID:+?teamId=${VERCEL_TEAM_ID}}\" \\\n        -H \"Authorization: Bearer ${VERCEL_TOKEN}\" \\\n        -H \"Content-Type: application/json\" \\\n        -d '{\"ssoProtection\": null, \"passwordProtection\": {\"deploymentType\": \"preview\"}}' 2>&1) || true\n      EXIT=$?\n\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"VERCEL_PATCH_RESPONSE=${RESULT}\"\n      elif echo \"$RESULT\" | grep -qiE '403|forbidden|unauthorized'; then\n        echo \"STATUS=skipped_unauthorized\"\n        echo \"DETAIL=Vercel API returned 403; operator must use dashboard. ${RESULT}\"\n      elif echo \"$RESULT\" | grep -qiE 'rate.limit|429'; then\n        echo \"STATUS=skipped_rate_limit\"\n        echo \"DETAIL=Vercel API rate-limited; retry later\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${RESULT}\"\n      fi\n      exit 0\n\n  - id: verify-post-state\n    depends_on: [execute-vercel-change]\n    bash: |\n      # set -e omitted — curl diagnostics\n      sleep 15  # give Vercel a moment to propagate\n      BDC_CODE=$(curl -s -o /dev/null -w '%{http_code}' \"https://bluedevilcollectibles.comicstoreos.com\" 2>/dev/null || echo \"000\")\n      STAGING_CODE=$(curl -s -o /dev/null -w '%{http_code}' \"https://staging.comicstoreos.com\" 2>/dev/null || echo \"000\")\n      echo \"BLUEDEVIL_POST_CODE=${BDC_CODE}\"\n      echo \"STAGING_POST_CODE=${STAGING_CODE}\"\n      if [ \"$BDC_CODE\" = \"200\" ] && [ \"$STAGING_CODE\" = \"401\" ]; then\n        echo \"STATUS=ok\"\n        echo \"DETAIL=Option C in effect: BDC tenant 200, staging 401\"\n      elif [ \"$BDC_CODE\" = \"200\" ]; then\n        echo \"STATUS=partial\"\n        echo \"DETAIL=BDC tenant now public (good) but staging not protected (${STAGING_CODE} — check Vercel setting)\"\n      else\n        echo \"STATUS=not_yet_applied\"\n        echo \"DETAIL=BDC tenant still ${BDC_CODE}; either Vercel API call deferred to operator or propagation pending\"\n      fi\n      exit 0\n\n  - id: commit-and-push\n    depends_on: [verify-post-state]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/infra-vercel-password-decision-01\"\n      if [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n        git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n      fi\n      git add docs/decisions/2026-05-13-vercel-password-decision.md\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'docs(decision): WO-INFRA-VERCEL-PASSWORD-DECISION-01 — Option C (staging-only password)\\n\\nDocuments 3 options for Vercel password protection on tenant subdomains.\\nDecision: Option C — production tenant subdomains public, staging keeps password.\\nExecutes via Vercel API (best-effort; may require operator dashboard if API blocked).\\n\\nVerification: curl bluedevilcollectibles.comicstoreos.com -> 200,\\n              curl staging.comicstoreos.com -> 401.\\n\\nROUGH-OK polish bar — see WO §13.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/infra-vercel-password-decision-01\"\n      REPO=\"bluedevilcollectibles/bdc-xo\"\n      EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"PR=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo \"$REPO\" --base main --head \"$BRANCH\" \\\n            --title \"docs(decision): WO-INFRA-VERCEL-PASSWORD-DECISION-01 — Option C\" \\\n            --body \"INFRA WO. Vercel password protection moved to staging-only. ROUGH-OK polish bar.\" 2>&1); then\n          echo \"PR=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      echo \"open-pr failed\" >&2; exit 1\n\n  - id: build-manifest\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 for WO-INFRA-VERCEL-PASSWORD-DECISION-01 (INFRA class, ROUGH-OK).\n\n      WO: WO-INFRA-VERCEL-PASSWORD-DECISION-01\n      Builder: Devils Cauldron (Archon bdc-infra-vercel-password-decision)\n\n      Files created:\n       - docs/decisions/2026-05-13-vercel-password-decision.md\n\n      Files modified: (none — Vercel config change is API-only, not in-repo)\n\n      Tests: N/A (infra WO; runtime verification below is authoritative)\n\n      PRs:\n       - bdc-xo: [from $open-pr.output PR]\n\n      Merge ancestors:\n       - bdc-xo main HEAD: [from $commit-and-push.output SHA], behind_by: 0\n\n      Grep assertions:\n       - grep -q 'Option C' docs/decisions/2026-05-13-vercel-password-decision.md\n       - grep -q 'staging-only' docs/decisions/2026-05-13-vercel-password-decision.md\n\n      Runtime verification:\n       - curl -s -o /dev/null -w '%{http_code}' https://bluedevilcollectibles.comicstoreos.com -> expected 200\n       - Actual: [from $verify-post-state.output BLUEDEVIL_POST_CODE]\n       - curl -sI https://staging.comicstoreos.com -> expected 401\n       - Actual: [from $verify-post-state.output STAGING_POST_CODE]\n       - Vercel API change status: [from $execute-vercel-change.output STATUS]\n\n      Vercel deployment: N/A (this WO IS the Vercel deployment config change)\n\n      INFRA VALIDATION: [PASS if both post-state codes match expected; otherwise PARTIAL with operator follow-up]\n\n      Inputs:\n      $read-spec.output\n      $verify-current-state.output\n      $execute-vercel-change.output\n      $verify-post-state.output\n      $commit-and-push.output\n      $open-pr.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-INFRA-VERCEL-PASSWORD-DECISION-01 to REVIEW.\n      Manifest goes in Notes (NOT comment).\n\n      # OPEN QUESTION: Notion page ID. Operator filters by exact WO ID equality.\n\n      Update:\n       - Claude Status -> REVIEW\n       - Status -> Review\n       - Notes -> $build-manifest.output verbatim\n       - claimed_by: Devils Cauldron\n       - claimed_at: today\n\n      If $verify-post-state.output STATUS is not 'ok', also tag the WO as needing\n      operator follow-up (note in Notes that Vercel API call was deferred).\n\n      If Notion MCP unavailable, output XO_TODO with manifest.\n\n      Manifest:\n      $build-manifest.output\n",
  "bdc-lspro-chain-demand-grid": "# Drafted by yaml-author subagent for WO-LSPRO-CHAIN-DEMAND-GRID-01 on 2026-05-13\n# Source pattern: WO-CHEERS-DASHBOARD-01.yaml (MIXED 2-repo + Rule 19 pattern)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring\n#\n# Pre-flight (OPERATOR MUST VERIFY BEFORE FIRING):\n#   1. lspro-react + shopops registered in Archon\n#   2. WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 MUST be MERGED — location_id column on pull_list_allocations\n#      Verify: gh pr list --repo bluedevilcollectibles/shopops --search 'WO-SHOPOPS-MULTI-LOCATION-SCOPE-01' --state merged\n#      AND ssh hetzner-prod 'docker exec shopops-api psql -c \"\\d pull_list_allocations\"' shows location_id\n#   3. Spec at bdc-xo:docs/work-orders/WO-LSPRO-CHAIN-DEMAND-GRID-01.md\n#   4. Notion page ID — fill in 35fef4c9-ce00-81f7-8c60-ebed44513508 before firing\n#\n# OPEN QUESTIONS:\n#   Q1: foc_calendar table existence — schema-verify checks; if absent, workflow aborts.\n#   Q2: PRH integration deferred — Lunar only this WO.\n#   Q3: Reallocation notification to affected stores — silent for v1.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh bdc-lspro-chain-demand-grid.yaml\n# REVIEW BEFORE DEPLOY: confirm WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 merged, foc_calendar exists.\n\nname: bdc-lspro-chain-demand-grid\ndescription: |\n  Use when: Executing WO-LSPRO-CHAIN-DEMAND-GRID-01 — DA FOC demand grid for chain owners.\n  Does: Verifies location_id column + foc_calendar table exist. Adds GET /api/chain/demand-grid\n        + POST /api/chain/demand-grid/reallocate to shopops-api. Adds ChainDemandGridPage\n        + grid components + reallocation modal in lspro-react. Vitest + bun tests.\n        UI screenshot loop N=3 (grid load, reallocation modal, 13-location scroll).\n        Manifest v2 + Notion flip.\n  Repos: bluedevilcollectibles/lspro-react (Archon CWD) + bluedevilcollectibles/shopops\n         (cloned to /tmp).\n  Depends on: WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 (location_id column).\n  parallel_ok: true (single-repo for UI side; shopops side small + isolated)\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-LSPRO-CHAIN-DEMAND-GRID-01}\"\n      # Primary path (gh --template, no jq dependency)\n      SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        GLOB=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB\" ]; then\n          SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n--- SPEC ---\\n%s\\n' \"$WO_ID\" \"$WORKFLOW_ID\" \"$SPEC\"\n\n  - id: schema-verify\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort but HARD-FAILS on critical missing columns\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"\n        echo \"DETAIL=Operator MUST verify manually before firing:\"\n        echo \"  1. pull_list_allocations has location_id column (WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 merged?)\"\n        echo \"  2. foc_calendar table exists\"\n        echo \"  3. locations table exists with id+name+code\"\n        exit 0\n      fi\n\n      # Critical check 1: pull_list_allocations.location_id\n      LOC_COL=$(curl -sf \\\n        \"${SUPABASE_URL}/rest/v1/information_schema.columns?table_name=eq.pull_list_allocations&column_name=eq.location_id&select=column_name\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n      if echo \"$LOC_COL\" | grep -q '\"location_id\"'; then\n        echo \"SCHEMA_pull_list_allocations_location_id=VERIFIED\"\n      else\n        echo \"SCHEMA_pull_list_allocations_location_id=MISSING\"\n        echo \"BLOCKER=WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 not yet merged. ABORTING.\"\n        echo \"STATUS=blocked\"\n        exit 1\n      fi\n\n      # Critical check 2: foc_calendar\n      FOC=$(curl -sf \\\n        \"${SUPABASE_URL}/rest/v1/information_schema.tables?table_name=eq.foc_calendar&select=table_name\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n      if echo \"$FOC\" | grep -q '\"foc_calendar\"'; then\n        echo \"SCHEMA_foc_calendar=VERIFIED\"\n      else\n        echo \"SCHEMA_foc_calendar=MISSING\"\n        echo \"DETAIL=foc_calendar table not found. Builder must escalate to John before implementation.\"\n      fi\n\n      # Locations table\n      LOC=$(curl -sf \\\n        \"${SUPABASE_URL}/rest/v1/information_schema.tables?table_name=eq.locations&select=table_name\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n      if echo \"$LOC\" | grep -q '\"locations\"'; then\n        echo \"SCHEMA_locations=VERIFIED\"\n      else\n        echo \"SCHEMA_locations=MISSING\"\n      fi\n\n      echo \"STATUS=ok\"\n      exit 0\n\n  - id: setup-shopops-clone\n    depends_on: [schema-verify]\n    bash: |\n      set -euo pipefail\n      # shopops is the SECONDARY repo here — clone into /tmp using shopops master/main lookup.\n      # Note: $(git remote show origin) reads CWD's origin (lspro-react) — wrong default branch\n      # for shopops. Hard-code 'master' (verified correct for shopops repo).\n      SHOPOPS_DEFAULT=\"master\"\n      DIR=\"/tmp/shopops-work-$WORKFLOW_ID\"\n      if [ -d \"$DIR/.git\" ]; then\n        cd \"$DIR\"; git fetch origin || true\n        if ! git checkout \"$SHOPOPS_DEFAULT\" 2>/dev/null; then\n          echo \"shopops default branch held by other worktree; staying on current HEAD\"\n        fi\n        git reset --hard \"origin/$SHOPOPS_DEFAULT\" 2>/dev/null || true\n      else\n        git clone \"https://github.com/bluedevilcollectibles/shopops.git\" \"$DIR\" --depth=50\n      fi\n      echo \"SHOPOPS_DIR=$DIR\"\n\n  - id: audit-prior-art\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — audit must NEVER fail (BUG-3 tolerant).\n      # lspro-react DOES have src/ (verified). Tolerate missing files for new modules.\n      if [ -d src/pages/cheers ]; then\n        ls -la src/pages/cheers/ 2>/dev/null || true\n      else\n        echo \"cheers/ dir not yet created\"\n      fi\n      [ -e src/App.tsx ] && grep -n \"Route path\" src/App.tsx 2>/dev/null | head -30 || echo \"(App.tsx missing)\"\n      [ -e src/lib/tiers.ts ] && grep -n \"meetsTier\" src/lib/tiers.ts 2>/dev/null | head -10 || echo \"(tiers.ts missing)\"\n      if [ -e src/pages/PullListCommandCenter.tsx ]; then\n        ls -la src/pages/PullListCommandCenter.tsx 2>/dev/null || true\n        head -50 src/pages/PullListCommandCenter.tsx 2>/dev/null || true\n      else\n        echo \"(PullListCommandCenter.tsx missing — audit tolerant)\"\n      fi\n      exit 0\n\n  - id: plan\n    depends_on: [audit-prior-art, setup-shopops-clone]\n    task_class: medium_work\n    prompt: |\n      Plan WO-LSPRO-CHAIN-DEMAND-GRID-01 across lspro-react + shopops.\n\n      ## Spec\n      $read-spec.output\n\n      ## Schema verify\n      $schema-verify.output\n\n      ## Prior art\n      $audit-prior-art.output\n\n      ## Required output\n\n      ### shopops side (CWD /tmp/shopops-work-$WORKFLOW_ID)\n      - shopops-api/routes/chain-demand.js with:\n          GET /api/chain/demand-grid?tenant_id=&foc_week=\n          POST /api/chain/demand-grid/reallocate\n      - shopops-api/lib/demand-grid-builder.js — pure aggregation helper\n      - shopops-api/tests/test_chain_demand.js — 6 bun tests per WO §7\n      - shopops-api/index.js — mount /api/chain/demand-grid\n      - Branch: wo/lspro-chain-demand-grid-01, base master\n\n      ### lspro-react side (CWD = repo root)\n      - src/pages/cheers/ChainDemandGridPage.tsx (route /cheers/chain-demand)\n      - src/pages/cheers/components/DemandGridTable.tsx\n      - src/pages/cheers/components/ReallocationModal.tsx\n      - src/tests/chain-demand-grid.test.tsx — 7 Vitest tests\n      - src/App.tsx — add route /cheers/chain-demand behind meetsTier('hq')\n      - Branch: wo/lspro-chain-demand-grid-01, base main\n\n      ### Important UX rules from spec\n      - Sticky first column (comic title + variant ratio)\n      - Horizontal scroll for 13+ location columns\n      - Empty cell when location has no demand (not '0')\n      - Incentive variant pricing visible (1:25, 1:100 ratio in row header)\n      - Optimistic concurrency on reallocate via updated_at; 409 on stale write\n\n      ### Test counts\n      - chain-demand-grid.test.tsx: 7\n      - test_chain_demand.js: 6\n\n  - id: implement-api\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement shopops-api side of WO-LSPRO-CHAIN-DEMAND-GRID-01.\n        CWD: /tmp/shopops-work-$WORKFLOW_ID\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n        1. Read shopops-api/routes/pulllist.js to understand existing pull_list_allocations query patterns.\n        2. Create routes/chain-demand.js:\n           - GET /api/chain/demand-grid: joins pull_list_subscriptions + pull_list_allocations\n             + comics_catalog + locations, filtered by tenant_id + foc_week.\n             Returns { ok, foc_week, locations[], rows[], kpis }.\n           - POST /api/chain/demand-grid/reallocate: validates sum-of-new_qty <= total-current-alloc,\n             checks updated_at for optimistic concurrency (409 if stale), updates rows.\n           - Both multi-tenant isolated.\n        3. Create lib/demand-grid-builder.js as PURE FUNCTIONS:\n           - buildMatrix(allocations, locations) -> rows array\n           - computeKpis(rows) -> { total_demand, total_allocated, fulfillment_pct }\n           Easy to unit test without db.\n        4. Mount route in index.js.\n        5. Create tests/test_chain_demand.js — 6 bun tests per WO §7:\n           a. GET returns matrix shape\n           b. variant_ratio visible for incentive variants\n           c. Empty FOC week returns rows=[] without error\n           d. POST reallocate updates allocations idempotently\n           e. POST reallocate rejects oversubscription (400)\n           f. Multi-tenant isolation\n        6. Do NOT push.\n\n        <promise>COMPLETE</promise> when files exist + route mounted + tests >=6.\n\n  - id: implement-ui\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement lspro-react side of WO-LSPRO-CHAIN-DEMAND-GRID-01.\n        CWD = lspro-react repo root.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n        1. Read src/pages/cheers/CheersDashboardPage.tsx (created by WO-CHEERS-DASHBOARD-01)\n           to match style. If it doesn't exist yet, fall back to src/pages/AdminPage.tsx style.\n        2. Create src/pages/cheers/ChainDemandGridPage.tsx:\n           - FocWeekPicker dropdown (last 4 weeks)\n           - apiFetch GET /api/chain/demand-grid?tenant_id=...&foc_week=...\n           - Renders KPI row + DemandGridTable\n           - Click cell -> open ReallocationModal\n        3. Create components/DemandGridTable.tsx:\n           - Sticky first column (CSS: position: sticky; left: 0)\n           - Horizontal scroll wrapper (overflow-x: auto)\n           - Empty cell when by_location[loc_id].demand === 0 (render nothing, not '0')\n           - Row header includes variant_ratio (e.g. \"1:25\")\n        4. Create components/ReallocationModal.tsx:\n           - List of 13 locations with current demand + allocated\n           - Editable allocated_qty input per location\n           - Save calls POST /api/chain/demand-grid/reallocate\n           - Show stale-write error if response is 409\n        5. Add /cheers/chain-demand route in App.tsx behind meetsTier('hq').\n        6. Use apiFetch from @/lib/api.\n        7. Create src/tests/chain-demand-grid.test.tsx — 7 Vitest tests per WO §7.\n           Mock apiFetch with vi.mock('@/lib/api').\n\n        <promise>COMPLETE</promise> when all files exist + route wired + 7+ tests.\n\n  - id: verify-types\n    depends_on: [implement-ui]\n    bash: |\n      set -euo pipefail\n      bunx tsc --noEmit\n      echo \"TS_OK\"\n\n  - id: verify-tests-ui\n    depends_on: [implement-ui]\n    bash: |\n      set -euo pipefail\n      bun run test src/tests/chain-demand-grid.test.tsx 2>&1 | tee /tmp/ui-tests.txt\n      echo \"TAIL=$(tail -5 /tmp/ui-tests.txt)\"\n\n  - id: verify-tests-api\n    depends_on: [implement-api]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      bun shopops-api/tests/test_chain_demand.js 2>&1 | tee /tmp/api-tests.txt \\\n        || echo \"Tests may need live DB; CI authoritative.\"\n      echo \"TAIL=$(tail -5 /tmp/api-tests.txt)\"\n\n  - id: verify-grep-ui\n    depends_on: [implement-ui]\n    bash: |\n      set -euo pipefail\n      grep -q \"ChainDemandGridPage\" src/App.tsx || (echo \"FAIL: route missing\" >&2; exit 1)\n      ls src/pages/cheers/ChainDemandGridPage.tsx\n      ls src/pages/cheers/components/DemandGridTable.tsx\n      ls src/pages/cheers/components/ReallocationModal.tsx\n      TESTS=$(grep -c \"test(\\|it(\" src/tests/chain-demand-grid.test.tsx)\n      [ \"$TESTS\" -ge 7 ] || (echo \"FAIL: need >=7 UI tests, got $TESTS\" >&2; exit 1)\n      echo \"UI GREP PASS tests=$TESTS\"\n\n  - id: verify-grep-api\n    depends_on: [implement-api]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      ls shopops-api/routes/chain-demand.js\n      grep -q \"chain/demand-grid\" shopops-api/index.js \\\n        || (echo \"FAIL: route not mounted\" >&2; exit 1)\n      TESTS=$(grep -c \"test(\\|it(\" shopops-api/tests/test_chain_demand.js)\n      [ \"$TESTS\" -ge 6 ] || (echo \"FAIL: need >=6 API tests, got $TESTS\" >&2; exit 1)\n      echo \"API GREP PASS tests=$TESTS\"\n\n  - id: build-check\n    depends_on: [verify-types, verify-tests-ui, verify-grep-ui]\n    bash: |\n      set -euo pipefail\n      bun run build 2>&1 | tee /tmp/build.txt\n      [ \"${PIPESTATUS[0]}\" -eq 0 ] || (echo \"BUILD FAILED\" >&2; cat /tmp/build.txt >&2; exit 1)\n      echo \"BUILD_OK\"\n\n  - id: screenshot-loop\n    depends_on: [build-check]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: SCREENSHOTS_COMPLETE\n      max_iterations: 3\n      prompt: |\n        Verify 3 UI screenshots for WO-LSPRO-CHAIN-DEMAND-GRID-01:\n          SHOT_1: /cheers/chain-demand loads, grid renders with sticky first col + 13 location cols\n          SHOT_2: ReallocationModal open after cell click, all 13 locations listed editable\n          SHOT_3: Horizontal scroll evidence — 13 columns force overflow, sticky first col stays put\n\n        Inspect ChainDemandGridPage.tsx + DemandGridTable.tsx + ReallocationModal.tsx.\n        Output per shot: PASS|FAIL + Evidence + Gap.\n\n        When all 3 pass: <promise>SCREENSHOTS_COMPLETE</promise>\n\n  - id: commit-and-push-ui\n    depends_on: [screenshot-loop]\n    bash: |\n      set -euo pipefail\n      BASE=\"wo/lspro-chain-demand-grid-01\"\n      # Default to unique run-suffixed branch (avoids worktree collisions)\n      BRANCH=\"${BASE}-run-$(date +%Y%m%d%H%M%S)\"\n      git checkout -b \"$BRANCH\" 2>/dev/null || git checkout \"$BRANCH\"\n\n      git add src/App.tsx src/pages/cheers/ChainDemandGridPage.tsx \\\n        src/pages/cheers/components/DemandGridTable.tsx \\\n        src/pages/cheers/components/ReallocationModal.tsx \\\n        src/tests/chain-demand-grid.test.tsx\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(da): WO-LSPRO-CHAIN-DEMAND-GRID-01 — chain FOC demand grid\\n\\nChainDemandGridPage at /cheers/chain-demand (HQ tier).\\nSticky first col + horizontal scroll for 13+ locations.\\nReallocationModal with optimistic concurrency on save.\\n\\nTests: chain-demand-grid.test.tsx (7 Vitest tests).\\n\\nCompanion PR: shopops master wo/lspro-chain-demand-grid-01 (route + builder).\\nDepends on: WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 (location_id column).\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"UI_BRANCH=$BRANCH\"\n      echo \"UI_SHA=$(git rev-parse HEAD)\"\n\n  - id: commit-and-push-api\n    depends_on: [verify-grep-api, verify-tests-api]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      BASE=\"wo/lspro-chain-demand-grid-01\"\n      # Default to unique run-suffixed branch (avoids worktree collisions)\n      BRANCH=\"${BASE}-run-$(date +%Y%m%d%H%M%S)\"\n      git checkout -b \"$BRANCH\" 2>/dev/null || git checkout \"$BRANCH\"\n\n      git add shopops-api/routes/chain-demand.js \\\n              shopops-api/lib/demand-grid-builder.js \\\n              shopops-api/tests/test_chain_demand.js \\\n              shopops-api/index.js\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-LSPRO-CHAIN-DEMAND-GRID-01 — chain demand grid endpoint\\n\\nGET /api/chain/demand-grid + POST /api/chain/demand-grid/reallocate.\\nAggregates pull_list_allocations across all locations for a tenant + FOC week.\\nReallocate validates sum <= total alloc, optimistic concurrency via updated_at.\\n\\nTests: test_chain_demand.js (6 bun tests).\\n\\nDepends on: WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 (location_id column).\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"API_BRANCH=$BRANCH\"\n      echo \"API_SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr-ui\n    depends_on: [commit-and-push-ui]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/lspro-react\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"WO-LSPRO-CHAIN-DEMAND-GRID-01 lspro-react side.\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- src/pages/cheers/ChainDemandGridPage.tsx (route /cheers/chain-demand, HQ-tier)\"\n        echo \"- src/pages/cheers/components/DemandGridTable.tsx (sticky first col + h-scroll)\"\n        echo \"- src/pages/cheers/components/ReallocationModal.tsx\"\n        echo \"- src/tests/chain-demand-grid.test.tsx (7 tests)\"\n        echo \"\"\n        echo \"## Companion PR\"\n        echo \"shopops master: wo/lspro-chain-demand-grid-01\"\n        echo \"\"\n        echo \"## Dependency\"\n        echo \"WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 must be MERGED (location_id column).\"\n      } > \"$BODY\"\n\n      PR_URL=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null)\n      if [ -n \"$PR_URL\" ]; then echo \"UI_PR_URL=$PR_URL\"; exit 0; fi\n\n      for attempt in 1 2 3; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base main --head \"$BRANCH\" \\\n            --title \"feat(da): WO-LSPRO-CHAIN-DEMAND-GRID-01 — chain FOC demand grid\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"UI_PR_URL=$PR_OUT\"; exit 0\n        fi\n        sleep 10\n      done\n      exit 1\n\n  - id: open-pr-api\n    depends_on: [commit-and-push-api]\n    bash: |\n      set -euo pipefail\n      cd /tmp/shopops-work-$WORKFLOW_ID\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"WO-LSPRO-CHAIN-DEMAND-GRID-01 shopops side — /api/chain/demand-grid.\"\n        echo \"\"\n        echo \"## Rule 19 verification\"\n        echo '  docker exec shopops-api grep -n \"chain/demand-grid\" /app/routes/chain-demand.js'\n      } > \"$BODY\"\n\n      PR_URL=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null)\n      if [ -n \"$PR_URL\" ]; then echo \"API_PR_URL=$PR_URL\"; exit 0; fi\n\n      for attempt in 1 2 3; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base master --head \"$BRANCH\" \\\n            --title \"feat(api): WO-LSPRO-CHAIN-DEMAND-GRID-01 — chain demand grid endpoint\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"API_PR_URL=$PR_OUT\"; exit 0\n        fi\n        sleep 10\n      done\n      exit 1\n\n  - id: runtime-verify\n    depends_on: [open-pr-api, open-pr-ui]\n    bash: |\n      # set -e DELIBERATELY OMITTED — Rule 19 best-effort\n      RESULT=$(docker exec shopops-api grep -n \"chain/demand-grid\" /app/routes/chain-demand.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RULE19=$RESULT\"\n      elif echo \"$RESULT\" | grep -qiE 'No such|not found'; then\n        echo \"STATUS=pending_rebuild\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=$RESULT\"\n      fi\n      exit 0\n\n  - id: verify-vercel\n    depends_on: [open-pr-ui]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo unknown)\"\n      sleep 30\n      RESULT=$(gh pr list --repo bluedevilcollectibles/lspro-react --head \"$BRANCH\" --state open --json url 2>&1)\n      if echo \"$RESULT\" | grep -q \"url\"; then\n        echo \"STATUS=pr_open_preview_pending\"\n      else\n        echo \"STATUS=skipped\"\n      fi\n      exit 0\n\n  - id: build-manifest\n    depends_on: [runtime-verify, verify-vercel]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 for WO-LSPRO-CHAIN-DEMAND-GRID-01. MIXED class.\n\n      WO: WO-LSPRO-CHAIN-DEMAND-GRID-01\n      Builder: Major Build (Archon bdc-lspro-chain-demand-grid)\n\n      Files modified:\n        lspro-react/src/App.tsx\n        shopops/shopops-api/index.js\n\n      Files created:\n        lspro-react/src/pages/cheers/ChainDemandGridPage.tsx\n        lspro-react/src/pages/cheers/components/DemandGridTable.tsx\n        lspro-react/src/pages/cheers/components/ReallocationModal.tsx\n        lspro-react/src/tests/chain-demand-grid.test.tsx\n        shopops/shopops-api/routes/chain-demand.js\n        shopops/shopops-api/lib/demand-grid-builder.js\n        shopops/shopops-api/tests/test_chain_demand.js\n\n      Tests: 7 (UI) + 6 (API) = 13 passing\n        bun run test src/tests/chain-demand-grid.test.tsx\n        cd shopops-api && bun tests/test_chain_demand.js\n\n      PRs:\n        lspro-react main: [open-pr-ui.UI_PR_URL]\n        shopops master:   [open-pr-api.API_PR_URL]\n\n      Merge ancestors:\n        lspro-react main HEAD=[commit-and-push-ui.UI_SHA], behind_by: 0\n        shopops master HEAD=[commit-and-push-api.API_SHA], behind_by: 0\n\n      Grep assertions:\n        grep -c \"ChainDemandGridPage\" src/App.tsx >= 1\n        grep -c \"chain/demand-grid\" shopops-api/index.js >= 1\n        grep -c \"test(\\|it(\" src/tests/chain-demand-grid.test.tsx >= 7\n        grep -c \"test(\\|it(\" shopops-api/tests/test_chain_demand.js >= 6\n\n      Runtime verification:\n        Rule 19 (shopops-api): [runtime-verify.STATUS]\n        Command: docker exec shopops-api grep -n 'chain/demand-grid' /app/routes/chain-demand.js\n\n      Vercel deployment: [verify-vercel.output]\n\n      Schema verification (pre-flight):\n        [schema-verify.SCHEMA_pull_list_allocations_location_id]\n        [schema-verify.SCHEMA_foc_calendar]\n        [schema-verify.SCHEMA_locations]\n\n      VALIDATION: PASS\n\n      ## Inputs\n      $read-spec.output $schema-verify.output $plan.output\n      $verify-tests-ui.output $verify-tests-api.output\n      $verify-grep-ui.output $verify-grep-api.output\n      $build-check.output $screenshot-loop.output\n      $commit-and-push-ui.output $commit-and-push-api.output\n      $open-pr-ui.output $open-pr-api.output\n      $runtime-verify.output $verify-vercel.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-LSPRO-CHAIN-DEMAND-GRID-01 to REVIEW with manifest in Notes property.\n\n      # OPERATOR: Replace 35fef4c9-ce00-81f7-8c60-ebed44513508 before firing.\n      Notion database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      Notion page ID: 35fef4c9-ce00-81f7-8c60-ebed44513508\n\n      Updates:\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> $build-manifest.output verbatim\n\n      If Notion MCP unavailable:\n        XO_TODO: Flip WO-LSPRO-CHAIN-DEMAND-GRID-01 to REVIEW, paste manifest into Notes.\n\n      ## Manifest\n      $build-manifest.output\n",
  "bdc-model-tier-advisor-change-logging": "name: bdc-model-tier-advisor-change-logging\ndescription: |\n  Implements tier-change detection and outcome-update logging for WO-MODEL-TIER-ADVISOR-CHANGE-LOGGING-01.\n  Adds: detect_transition(), make_outcome_event(), _emit_transition_event() to session_state.py + logger.py.\n  Extends: summarize() + format_report() in report.py.\n  New test file: tests/test_transition.py (5 tests).\n  ADVISORY ONLY: no auto-switching, no auto-compact, no external logging.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\ninputs:\n  wo_id:\n    required: true\n    default: WO-MODEL-TIER-ADVISOR-CHANGE-LOGGING-01\n\nnodes:\n\n  - id: preflight\n    bash: |\n      set -euo pipefail\n      echo \"=== preflight ===\"\n      python --version\n      git status --short\n      git log --oneline -3\n      test -f src/model_tier_advisor/session_state.py && echo \"session_state.py OK\"\n      test -f src/model_tier_advisor/logger.py && echo \"logger.py OK\"\n      test -f src/model_tier_advisor/report.py && echo \"report.py OK\"\n      test -f src/model_tier_advisor/hook.py && echo \"hook.py OK\"\n      test -f docs/LOGGING_CONTRACT.md && echo \"LOGGING_CONTRACT.md OK\"\n      python -m pytest tests/ -v --tb=short 2>&1 | tail -20\n      echo \"preflight PASS\"\n\n  - id: read-spec\n    depends_on: [preflight]\n    bash: |\n      set -euo pipefail\n      cat work-orders/WO-MODEL-TIER-ADVISOR-CHANGE-LOGGING-01.md\n      cat docs/LOGGING_CONTRACT.md\n\n  - id: implement\n    depends_on: [read-spec]\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 6\n      prompt: |\n        You are implementing WO-MODEL-TIER-ADVISOR-CHANGE-LOGGING-01 in the model-tier-advisor repo.\n        The spec and logging contract are in $read-spec.output. Read them before touching any file.\n\n        ## What to build\n\n        ### 1. src/model_tier_advisor/session_state.py -- add:\n\n        detect_transition(session_id, new_model, log_path) -> dict | None\n          - Read the last 50 lines of the events.jsonl log at log_path\n          - Find the most recent event with event_type == \"advisory\" that has a known model\n          - If new_model (lowercased) differs from that event's current_model (lowercased):\n              return {\"from_model\": <prior current_model>, \"to_model\": new_model,\n                      \"linked_event_id\": <that advisory's event_id>,\n                      \"matched_recommendation\": new_model == <that advisory's recommended_model (lowercased)>}\n          - If no change, or no prior advisory found: return None\n          - NEVER raise -- catch all exceptions and return None\n\n        _emit_transition_event(session_id, new_model, config) -> None\n          - Call detect_transition(); if None, return immediately (no event written)\n          - Build an outcome_update event via make_outcome_event() from logger.py\n          - user_action = \"followed\" if matched_recommendation else \"switched_model\"\n          - outcome = \"cost_savings_opportunity\" if user_action == \"followed\" else \"pending\"\n          - Call append_event(outcome_event, config[\"log_path\"])\n          - NEVER raise -- wrap entire body in try/except\n\n        ### 2. src/model_tier_advisor/logger.py -- add:\n\n        make_outcome_event(linked_event_id, user_action, from_model, to_model, outcome, notes=\"\") -> dict\n          - Builds an outcome_update record matching docs/LOGGING_CONTRACT.md section 4\n          - Fields: event_id (evt_<timestamp>_<6hex>), event_type=\"outcome_update\",\n            timestamp (ISO8601 UTC), linked_event_id, user_action, outcome,\n            estimated_wasted_commits_avoided, notes (passed through redact() from redact.py)\n          - estimated_wasted_commits_avoided = 1 if user_action == \"followed\" else 0\n          - NEVER include raw prompt text, tokens, or secrets in any field\n\n        ### 3. src/model_tier_advisor/hook.py -- extend SessionStart branch:\n\n        Find the SessionStart branch in run(). After remember_model(...) is called, add:\n          _emit_transition_event(sid, model, config)\n        Add the import at the top of the file.\n\n        ### 4. src/model_tier_advisor/report.py -- extend:\n\n        In summarize(events):\n          - Count outcome_update events (event_type == \"outcome_update\")\n          - tier_changes = total outcome_update count\n          - followed_transitions = outcome_update events where user_action == \"followed\"\n          - wasted_commits_avoided_estimate = sum of estimated_wasted_commits_avoided\n            from outcome_update events where user_action == \"followed\" AND linked_event_id present\n            (this replaces the old logic that read user_action == \"followed\" on advisory events\n            which was never populated)\n          - Add \"tier_changes\" and \"followed_transitions\" keys to the returned dict\n\n        In format_report(summary, ...):\n          - Add \"Tier changes detected: N\" line (after the existing Downshift line)\n          - Update \"Estimated wasted commits avoided\" to use the new calculation\n\n        ### 5. tests/test_transition.py -- NEW FILE, 5 tests:\n\n        test_followed_transition:\n          Seed events.jsonl with one advisory (recommended_model=\"opus\", current_model=\"sonnet\").\n          Call _emit_transition_event with new_model=\"opus\".\n          Assert outcome_update appended with user_action=\"followed\", linked_event_id matches.\n\n        test_no_transition_same_model:\n          Seed events.jsonl with one advisory (current_model=\"sonnet\").\n          Call _emit_transition_event with new_model=\"sonnet\".\n          Assert NO outcome_update appended (line count unchanged).\n\n        test_independent_switch_not_credited:\n          Seed events.jsonl with advisory (recommended_model=\"opus\", current_model=\"sonnet\").\n          Call _emit_transition_event with new_model=\"haiku\".\n          Assert outcome_update appended with user_action=\"switched_model\" (NOT \"followed\").\n          Assert estimated_wasted_commits_avoided == 0 in that event.\n\n        test_empty_log_no_crash:\n          events.jsonl is empty or contains \"not-json\".\n          Call _emit_transition_event -- must not raise.\n          Assert no outcome_update event appended.\n\n        test_transition_event_no_secrets:\n          Seed events.jsonl with one advisory. Call _emit_transition_event with new_model=\"opus\".\n          Read the written outcome_update event.\n          Assert no field contains a JWT token pattern (r'ey[A-Za-z0-9_-]{10,}').\n          Assert no field contains a 64-char hex string (potential secret).\n\n        ### 6. tests/test_report.py -- extend (add to existing file):\n\n        test_report_with_outcome_events:\n          Seed events.jsonl with 3 advisory events + 2 outcome_update events\n          (user_action=\"followed\", linked_event_id set, estimated_wasted_commits_avoided=1 each).\n          Run report.summarize().\n          Assert followed_transitions == 2, tier_changes == 2, wasted_commits_avoided_estimate == 2.\n\n        ## Hard constraints (never violate):\n        - NEVER add auto-switching logic\n        - NEVER add auto-compact logic\n        - NEVER add network calls\n        - NEVER log raw prompt text, tokens, API keys, or secrets\n        - hook.py main() must ALWAYS exit 0 (it already wraps in try/except -- preserve that)\n        - All new functions must have their own try/except -- never propagate exceptions to caller\n        - user_action values must come only from the enum in LOGGING_CONTRACT.md section 6:\n          unknown, ignored, switched_model, compacted, fresh_session, stayed_current, manual_override\n          (plus \"followed\" which is the observed-action equivalent of following the advisory)\n\n        Output <promise>COMPLETE</promise> when all 6 files are written and you have verified\n        the changes compile (python -c \"import model_tier_advisor.session_state\" etc.).\n\n  - id: test\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      echo \"=== running full test suite ===\"\n      python -m pytest tests/ -v --tb=short 2>&1\n      echo \"=== test PASS ===\"\n\n  - id: fixture-report-check\n    depends_on: [test]\n    bash: |\n      set -euo pipefail\n      echo \"=== fixture report check ===\"\n      python -c \"\n      import json, secrets\n      from datetime import datetime, timezone\n      from pathlib import Path\n\n      log = Path('/tmp/fixture_events.jsonl')\n      ts = datetime.now(timezone.utc).isoformat()\n      eid = 'evt_fixture_' + secrets.token_hex(3)\n      advisory = {\n        'event_id': eid, 'event_type': 'advisory', 'timestamp': ts,\n        'current_model': 'sonnet', 'recommended_model': 'opus',\n        'recommendation_type': 'upshift', 'trigger': 'test',\n        'signals': ['test signal'], 'confidence': 'high',\n        'suggested_action': 'switch_to_opus_for_root_cause',\n        'user_action': 'unknown', 'outcome': 'pending',\n        'repo': 'test', 'branch': 'main'\n      }\n      outcome = {\n        'event_id': 'evt_outcome_' + secrets.token_hex(3), 'event_type': 'outcome_update',\n        'timestamp': ts, 'linked_event_id': eid,\n        'user_action': 'followed', 'outcome': 'cost_savings_opportunity',\n        'estimated_wasted_commits_avoided': 1, 'notes': ''\n      }\n      log.write_text(json.dumps(advisory) + '\\n' + json.dumps(outcome) + '\\n')\n      print('fixture written to', str(log))\n      \"\n      python -c \"\n      import sys\n      from pathlib import Path\n      from model_tier_advisor import report\n      events, skipped = report.read_events('/tmp/fixture_events.jsonl')\n      s = report.summarize(events)\n      print(report.format_report(s, window_label='fixture', skipped=skipped))\n      assert s.get('tier_changes', 0) >= 1, 'tier_changes should be >= 1, got: ' + str(s)\n      assert s.get('wasted_commits_avoided_estimate', 0) >= 1, 'wasted_commits_avoided should be >= 1, got: ' + str(s)\n      print('fixture-report-check PASS')\n      \"\n      echo \"=== fixture-report-check PASS ===\"\n\n  - id: grep-assertions\n    depends_on: [fixture-report-check]\n    bash: |\n      set -euo pipefail\n      echo \"=== grep assertions ===\"\n      if grep -rn \"auto.switch\\|auto_switch\\|set_model\" src/model_tier_advisor/ --include=\"*.py\" | grep -v \"test_\"; then\n        echo \"ERROR: auto-switch pattern found in source\" >&2; exit 1\n      fi\n      echo \"no auto-switch: PASS\"\n      if grep -rn \"ey[A-Za-z0-9_-]\\{20,\\}\" src/model_tier_advisor/ --include=\"*.py\"; then\n        echo \"ERROR: JWT token pattern found in source\" >&2; exit 1\n      fi\n      echo \"no token values: PASS\"\n      grep -c \"outcome_update\" src/model_tier_advisor/logger.py && echo \"outcome_update in logger: PASS\"\n      grep -c \"detect_transition\" src/model_tier_advisor/session_state.py && echo \"detect_transition in session_state: PASS\"\n      grep -c \"tier_changes\" src/model_tier_advisor/report.py && echo \"tier_changes in report: PASS\"\n      echo \"=== grep assertions PASS ===\"\n\n  - id: commit-and-push\n    depends_on: [grep-assertions]\n    bash: |\n      set -euo pipefail\n      git config user.email \"cauldron@bluedevilcollectibles.com\"\n      git config user.name \"Cauldron\"\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      BRANCH=\"wo/model-tier-advisor-change-logging-01-run-$(date +%Y%m%d%H%M%S)\"\n      git checkout -b \"$BRANCH\"\n      git add src/model_tier_advisor/session_state.py \\\n               src/model_tier_advisor/logger.py \\\n               src/model_tier_advisor/report.py \\\n               src/model_tier_advisor/hook.py \\\n               tests/test_transition.py \\\n               tests/test_report.py\n      git diff --cached --stat\n      git commit -m \"feat(logging): tier-change detection and outcome-update logging (WO-MODEL-TIER-ADVISOR-CHANGE-LOGGING-01)\"\n      git push -u origin \"$BRANCH\"\n      echo \"SHA=$(git rev-parse HEAD)\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"commit-and-push PASS\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/model-tier-advisor\"\n      EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"PR=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo \"$REPO\" --base main --head \"$BRANCH\" \\\n            --title \"feat(logging): tier-change detection and outcome-update logging\" \\\n            --body \"WO-MODEL-TIER-ADVISOR-CHANGE-LOGGING-01. Advisory only. No auto-switching. No network calls. All 5 transition tests pass.\" 2>&1); then\n          echo \"PR=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      echo \"open-pr failed\" >&2; exit 1\n\n  - id: flip-notion\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Flip WO-MODEL-TIER-ADVISOR-CHANGE-LOGGING-01 to REVIEW in Notion.\n\n      Build the manifest and post it to the WO Notes field, then update both status fields.\n\n      Manifest template:\n      WO: WO-MODEL-TIER-ADVISOR-CHANGE-LOGGING-01\n      Builder: Devils Cauldron (bdc-model-tier-advisor-change-logging)\n      Files modified: src/model_tier_advisor/session_state.py, src/model_tier_advisor/logger.py, src/model_tier_advisor/report.py, src/model_tier_advisor/hook.py\n      Files created: tests/test_transition.py\n      Tests: (from $test.output -- count passing / total)\n      PRs: (from $open-pr.output PR URL)\n      Merge ancestors: model-tier-advisor main HEAD (from $commit-and-push.output SHA), behind_by: 0\n      Grep assertions: grep -c outcome_update src/model_tier_advisor/logger.py returns >= 1; grep -c detect_transition src/model_tier_advisor/session_state.py returns >= 1; grep -c tier_changes src/model_tier_advisor/report.py returns >= 1; no auto-switch pattern found\n      Runtime verification: pytest tests/ -v -- all tests pass (from $test.output)\n      Vercel deployment: N/A -- local Python package, no Vercel deploy\n      VALIDATION: PASS\n\n      Set:\n       - Claude Status -> REVIEW\n       - Status -> Review\n       - Notes -> manifest above\n\n      Inputs:\n      $test.output\n      $open-pr.output\n      $commit-and-push.output\n      $grep-assertions.output\n",
  "bdc-pos-beacon-uuid-per-location": "# Drafted by yaml-author for WO-POS-BEACON-UUID-PER-LOCATION-01 on 2026-05-13\n# Source pattern: WO-POS-TERMINAL-SDK-01.yaml (single-repo subset; no lspro-react companion in this WO)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight (4-block):\n#   1. YAML exists: this file\n#   2. Spec exists: docs/work-orders/WO-POS-BEACON-UUID-PER-LOCATION-01.md in bdc-xo\n#   3. CWD scope: SINGLE-REPO (shopops only) — primary CWD is shopops repo root\n#   4. Codebase registered: bluedevilcollectibles/shopops must be in Archon\n#      Verify: sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name FROM remote_agent_codebases\"\n#\n# Schema audit (REQUIRED before migration apply — Plan B floor, HIGH polish):\n#   SELECT table_name FROM information_schema.tables WHERE table_name = 'location_beacons'\n#   Expected: 0 rows. If non-empty: STOP and surface — table already exists.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-POS-BEACON-UUID-PER-LOCATION-01.yaml\n# REVIEW BEFORE DEPLOY — operator confirms WALLET_BEACON_UUID env var present in shopops-api .env on Hetzner.\n#\n# Polish bar: HIGH (POS customer arrival depends on this — mission-critical for HQ pitch).\n\nname: bdc-pos-beacon-uuid-per-location\ndescription: |\n  Use when: Executing WO-POS-BEACON-UUID-PER-LOCATION-01 — per-location iBeacon UUID\n    registration so wallet passes trigger arrival at the correct store.\n  Input: WO_ID = WO-POS-BEACON-UUID-PER-LOCATION-01.\n  Does:\n    - Reads spec from bdc-xo via gh api (cross-repo spec, single-repo build).\n    - Schema-verifies location_beacons does NOT exist yet (best-effort).\n    - Writes migration shopops-api/migrations/20260513_location_beacons.sql.\n    - Adds shopops-api/routes/admin-beacons.js with 4 endpoints (§7).\n    - Modifies shopops-api/routes/wallet.js to look up per-location UUID with\n      env fallback.\n    - Modifies shopops-api/index.js to mount the new router.\n    - Adds shopops-api/tests/test_location_beacons.js (>= 7 bun tests).\n    - Runs Rule 19 runtime-verify against shopops-api container.\n    - Builds CODE manifest and flips Notion to REVIEW.\n  Repo: bluedevilcollectibles/shopops (primary + only CWD).\n  parallel_ok: true (POS-arrival lane; no schema overlap with other lanes today).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  # -------------------------------------------------------------------------\n  # 1. READ SPEC (cross-repo: spec lives in bdc-xo, build in shopops)\n  # -------------------------------------------------------------------------\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-POS-BEACON-UUID-PER-LOCATION-01}\"\n\n      # Try canonical path first: docs/work-orders/<WO_ID>.md\n      # gh --template uses Go templates (built-in to gh, no external jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      # Fallback: glob docs/superpowers/specs/*<WO_ID>*.md\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template \"{{range .}}{{if (or (hasPrefix .name \\\"${WO_ID}\\\") (and (hasPrefix .name \\\"2\\\") (contains .name \\\"${WO_ID}\\\")))}}{{.path}}{{\\\"\\\\n\\\"}}{{end}}{{end}}\" \\\n          2>/dev/null | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:docs/work-orders/%s.md\\nWORKFLOW_ID=%s\\n\\n' \\\n        \"$WO_ID\" \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  # -------------------------------------------------------------------------\n  # 2. FRESH BRANCH (shopops master)\n  # -------------------------------------------------------------------------\n  - id: fresh-branch\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      # Defensive: if the default branch is already checked out in another worktree,\n      # we can't switch to it. Use a unique branch from the current HEAD instead.\n      if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n        echo \"default branch held by another worktree; staying on current HEAD\"\n      fi\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n      BASE_BRANCH=\"wo/pos-beacon-uuid-per-location-01\"\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      if git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE_BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE_BRANCH\" ]; then\n          # base branch held by another worktree; use unique\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE_BRANCH\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n          git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n        fi\n      else\n        BRANCH=\"$BASE_BRANCH\"\n        git checkout -b \"$BRANCH\"\n      fi\n      echo \"BRANCH=${BRANCH}\"\n\n  # -------------------------------------------------------------------------\n  # 3. SCHEMA VERIFY — location_beacons should NOT exist yet (best-effort)\n  # -------------------------------------------------------------------------\n  - id: schema-verify\n    depends_on: [fresh-branch]\n    bash: |\n      # set -e intentionally omitted — best-effort (Supabase env may not be in container)\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"\n        echo \"DETAIL=SUPABASE env not present in harness container; operator MUST run schema check before applying migration\"\n        exit 0\n      fi\n      R=$(curl -sf \\\n        \"${SUPABASE_URL}/rest/v1/information_schema.tables?table_name=eq.location_beacons&select=table_name\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n        -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n      if echo \"$R\" | grep -q 'location_beacons'; then\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=location_beacons table ALREADY EXISTS — investigate before proceeding. Response: ${R}\"\n      else\n        echo \"STATUS=ok\"\n        echo \"DETAIL=location_beacons absent as expected; safe to add migration\"\n      fi\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 4. PLAN\n  # -------------------------------------------------------------------------\n  - id: plan\n    depends_on: [read-spec, schema-verify]\n    task_class: medium_work\n    prompt: |\n      Plan WO-POS-BEACON-UUID-PER-LOCATION-01 implementation. Output Markdown.\n\n      ## Required outputs\n      - Files to create:\n        - shopops-api/migrations/20260513_location_beacons.sql\n        - shopops-api/routes/admin-beacons.js\n        - shopops-api/tests/test_location_beacons.js\n      - Files to modify:\n        - shopops-api/routes/wallet.js (add getBeaconForCustomer helper + use in POST /wallet/pass)\n        - shopops-api/index.js (mount admin-beacons router under /admin)\n      - Endpoints (§7):\n        - POST /admin/locations/:id/beacon\n        - POST /admin/locations/:id/beacon/regenerate\n        - GET  /admin/locations/:id/beacon\n        - GET  /admin/beacons\n      - Test command: bun test shopops-api/tests/test_location_beacons.js\n      - Branch: wo/pos-beacon-uuid-per-location-01\n      - PR base: master\n\n      ## Spec\n      $read-spec.output\n\n      ## Schema verify\n      $schema-verify.output\n\n  # -------------------------------------------------------------------------\n  # 5. IMPLEMENT (loop — Rule 10 compliant)\n  # -------------------------------------------------------------------------\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement WO-POS-BEACON-UUID-PER-LOCATION-01. CWD = shopops repo root.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution rules\n        1. Create shopops-api/migrations/20260513_location_beacons.sql per §7:\n           - CREATE TABLE location_beacons with columns location_id (PK FK to locations),\n             tenant_id, beacon_uuid (UUID UNIQUE NOT NULL), major (default 1),\n             minor (default 1), version (default 1), registered_at, updated_at, notes\n           - CREATE INDEX idx_location_beacons_tenant\n           - INSERT INTO schema_migrations VALUES ('20260513_location_beacons.sql') ON CONFLICT DO NOTHING\n        2. Create shopops-api/routes/admin-beacons.js with the 4 endpoints from §6.\n           Pattern: require('express').Router(); use X-Shopops-Tenant header pattern\n           consistent with existing routes/wallet.js. Use crypto.randomUUID() for new UUIDs.\n        3. Modify shopops-api/routes/wallet.js:\n           - Add getBeaconForCustomer(customerId) helper near top of file.\n             SQL: SELECT lb.beacon_uuid, lb.major, lb.minor\n                  FROM customers c\n                  JOIN location_beacons lb ON lb.location_id = c.home_location_id\n                  WHERE c.id = $1\n           - In POST /wallet/pass handler: call helper; if non-null, pass.beacons[0]\n             = { proximityUUID: beacon_uuid, major, minor }. If null, fall back to\n             process.env.WALLET_BEACON_UUID (preserves current behavior — DO NOT remove env path).\n        4. Modify shopops-api/index.js: app.use('/admin', require('./routes/admin-beacons'))\n           OR app.use('/admin/beacons', ...) + app.use('/admin/locations', ...) — pick\n           the pattern that yields the §6 routes exactly.\n        5. Create shopops-api/tests/test_location_beacons.js with bun:test. >= 7 tests\n           covering all 7 scenarios from §11 of the spec. Mock pg pool via factory pattern.\n           Each test MUST assert actual behavior (response shape, UUID inequality, DB row\n           presence) — not just \"did not throw\".\n        6. Do NOT modify services/wallet-apn.js, services/wallet-google.js, or any\n           pos*.js routes.\n        7. Do NOT apply the migration here. Migration application is a deploy-time step.\n        8. Do NOT push or open a PR.\n\n        Output <promise>COMPLETE</promise> only when:\n          - All 4 files created\n          - 2 files modified\n          - bun test shopops-api/tests/test_location_beacons.js passes 7/7 locally\n\n  # -------------------------------------------------------------------------\n  # 6. VERIFY GREP\n  # -------------------------------------------------------------------------\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      ls shopops-api/migrations/20260513_location_beacons.sql\n      grep -q \"CREATE TABLE.*location_beacons\" shopops-api/migrations/20260513_location_beacons.sql\n      grep -q \"beacon_uuid\" shopops-api/migrations/20260513_location_beacons.sql\n      grep -q \"INSERT INTO schema_migrations\" shopops-api/migrations/20260513_location_beacons.sql\n      ls shopops-api/routes/admin-beacons.js\n      grep -q \"/admin/locations\" shopops-api/routes/admin-beacons.js || \\\n        grep -q \"locations/:id/beacon\" shopops-api/routes/admin-beacons.js\n      grep -q \"regenerate\" shopops-api/routes/admin-beacons.js\n      grep -q \"/admin/beacons\" shopops-api/routes/admin-beacons.js || \\\n        grep -q \"listBeacons\\|beacon_uuid\" shopops-api/routes/admin-beacons.js\n      grep -q \"crypto.randomUUID\" shopops-api/routes/admin-beacons.js\n      grep -q \"getBeaconForCustomer\\|location_beacons\" shopops-api/routes/wallet.js\n      grep -q \"WALLET_BEACON_UUID\" shopops-api/routes/wallet.js\n      ls shopops-api/tests/test_location_beacons.js\n      TC=$(grep -c \"it(\\|test(\" shopops-api/tests/test_location_beacons.js || echo 0)\n      echo \"Test count: $TC\"\n      [ \"$TC\" -ge 7 ] || (echo \"FAIL: test count $TC < 7\" >&2; exit 1)\n      grep -q \"require.*admin-beacons\\|admin-beacons\" shopops-api/index.js\n      echo \"verify-grep PASS\"\n\n  # -------------------------------------------------------------------------\n  # 7. VERIFY TESTS — bun\n  # -------------------------------------------------------------------------\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      bun test tests/test_location_beacons.js\n\n  # -------------------------------------------------------------------------\n  # 8. COMMIT + PUSH\n  # -------------------------------------------------------------------------\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/pos-beacon-uuid-per-location-01\"\n      if [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n        git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n      fi\n      git add \\\n        shopops-api/migrations/20260513_location_beacons.sql \\\n        shopops-api/routes/admin-beacons.js \\\n        shopops-api/routes/wallet.js \\\n        shopops-api/index.js \\\n        shopops-api/tests/test_location_beacons.js\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-POS-BEACON-UUID-PER-LOCATION-01 — per-location iBeacon UUID\\n\\nAdds shopops-api/migrations/20260513_location_beacons.sql:\\n  CREATE TABLE location_beacons(location_id PK, beacon_uuid UNIQUE, major, minor, version, ...)\\n\\nAdds shopops-api/routes/admin-beacons.js with 4 endpoints:\\n  POST /admin/locations/:id/beacon            (register)\\n  POST /admin/locations/:id/beacon/regenerate (rotate UUID, bump version)\\n  GET  /admin/locations/:id/beacon            (read current)\\n  GET  /admin/beacons                          (list all for tenant)\\n\\nModifies shopops-api/routes/wallet.js:\\n  - Adds getBeaconForCustomer(customerId) helper.\\n  - POST /wallet/pass now reads per-location UUID from location_beacons,\\n    falls back to process.env.WALLET_BEACON_UUID if no row.\\n\\nAdds shopops-api/tests/test_location_beacons.js with 7+ bun tests covering\\nregister, regenerate, read, list, cross-tenant isolation, pass-uses-per-location,\\npass-falls-back-to-env.\\n\\nMounts router in index.js under /admin.\\n\\nHQ-grade: Mutant Market install 2026-06-15 depends on this. HIGH polish bar.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"SHA=$(git rev-parse HEAD)\"\n\n  # -------------------------------------------------------------------------\n  # 9. OPEN PR\n  # -------------------------------------------------------------------------\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/pos-beacon-uuid-per-location-01\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements WO-POS-BEACON-UUID-PER-LOCATION-01 — per-location iBeacon UUID.\"\n        echo \"Plan B floor for Mutant Market HQ install (2026-06-15).\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- Created: shopops-api/migrations/20260513_location_beacons.sql\"\n        echo \"- Created: shopops-api/routes/admin-beacons.js\"\n        echo \"- Created: shopops-api/tests/test_location_beacons.js\"\n        echo \"- Modified: shopops-api/routes/wallet.js\"\n        echo \"- Modified: shopops-api/index.js\"\n        echo \"\"\n        echo \"## Stop conditions\"\n        echo \"See bdc-xo:docs/work-orders/WO-POS-BEACON-UUID-PER-LOCATION-01.md §12.\"\n        echo \"\"\n        echo \"## Deploy notes\"\n        echo \"After merge, apply migration via:\"\n        echo '  ssh hetzner-prod \"docker run --rm postgres:16 psql \\\"\\$DB_URL\\\" -f migrations/20260513_location_beacons.sql\"'\n        echo \"Then rebuild + restart shopops-api container for Rule 19 verify.\"\n      } > \"$BODY\"\n      EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"PR=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo \"$REPO\" --base master --head \"$BRANCH\" \\\n            --title \"feat(api): WO-POS-BEACON-UUID-PER-LOCATION-01 — per-location iBeacon UUID\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"PR=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      echo \"open-pr failed\" >&2; exit 1\n\n  # -------------------------------------------------------------------------\n  # 10. RUNTIME VERIFY — Rule 19 best-effort\n  # -------------------------------------------------------------------------\n  - id: runtime-verify\n    depends_on: [open-pr]\n    bash: |\n      # set -e intentionally omitted — container may not be rebuilt yet\n      RESULT=$(docker exec shopops-api grep -n \"location_beacons\" /app/routes/admin-beacons.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RULE19=${RESULT}\"\n      elif echo \"$RESULT\" | grep -qiE 'No such (container|file)|not found'; then\n        echo \"STATUS=skipped_container_not_rebuilt\"\n        echo \"RERUN=docker exec shopops-api grep -n 'location_beacons' /app/routes/admin-beacons.js\"\n        echo \"DETAIL=Apply migration + rebuild container after merge, then re-run.\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${RESULT}\"\n      fi\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 11. BUILD MANIFEST\n  # -------------------------------------------------------------------------\n  - id: build-manifest\n    depends_on: [runtime-verify]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 for WO-POS-BEACON-UUID-PER-LOCATION-01 (CODE class).\n\n      WO: WO-POS-BEACON-UUID-PER-LOCATION-01\n      Builder: Devils Cauldron (Archon bdc-pos-beacon-uuid-per-location)\n\n      Files created:\n       - shopops-api/migrations/20260513_location_beacons.sql (location_beacons table + schema_migrations row)\n       - shopops-api/routes/admin-beacons.js (4 admin endpoints)\n       - shopops-api/tests/test_location_beacons.js (7+ bun tests)\n      Files modified:\n       - shopops-api/routes/wallet.js (getBeaconForCustomer + per-location UUID lookup with env fallback)\n       - shopops-api/index.js (mount admin-beacons router)\n\n      Tests:\n       - bun test shopops-api/tests/test_location_beacons.js -> [from $verify-tests.output]\n\n      PRs:\n       - shopops: [from $open-pr.output PR]\n\n      Merge ancestors:\n       - shopops master HEAD: [from $commit-and-push.output SHA], behind_by: 0\n\n      Grep assertions:\n       - grep -q 'location_beacons' shopops-api/migrations/20260513_location_beacons.sql\n       - grep -q 'beacon_uuid' shopops-api/migrations/20260513_location_beacons.sql\n       - grep -q 'crypto.randomUUID' shopops-api/routes/admin-beacons.js\n       - grep -q 'getBeaconForCustomer\\|location_beacons' shopops-api/routes/wallet.js\n       - grep -q 'WALLET_BEACON_UUID' shopops-api/routes/wallet.js (env fallback preserved)\n       - grep -c 'it(\\|test(' shopops-api/tests/test_location_beacons.js >= 7\n\n      Runtime verification:\n       - docker exec shopops-api grep -n 'location_beacons' /app/routes/admin-beacons.js\n       - [actual output from $runtime-verify.output]\n       - Migration apply: ssh hetzner-prod \"docker run --rm postgres:16 psql \\\"\\$DB_URL\\\" -f migrations/20260513_location_beacons.sql\"\n       - Migration verify: SELECT to_regclass('location_beacons') returns table name (pending operator)\n\n      Vercel deployment:\n       - N/A (shopops-api is Hetzner Docker, not Vercel)\n\n      VALIDATION: PASS (pending migration apply + container rebuild)\n\n      Inputs:\n      $read-spec.output\n      $schema-verify.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-and-push.output\n      $open-pr.output\n      $runtime-verify.output\n\n  # -------------------------------------------------------------------------\n  # 12. FLIP NOTION\n  # -------------------------------------------------------------------------\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-POS-BEACON-UUID-PER-LOCATION-01 to REVIEW. Manifest goes in\n      the Notes property (NOT a comment — Captain CI reads Notes).\n\n      Notion database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # OPEN QUESTION: page ID for WO-POS-BEACON-UUID-PER-LOCATION-01 not yet authored\n      # in Notion. Operator searches database by exact WO ID equality (NOT keyword) and\n      # pastes page ID before firing.\n\n      Update:\n       - Claude Status -> REVIEW\n       - Status -> Review\n       - Notes -> manifest text from $build-manifest.output verbatim\n       - claimed_by: Devils Cauldron (Archon)\n       - claimed_at: today\n\n      If Notion MCP unavailable, output:\n        XO_TODO: Flip WO-POS-BEACON-UUID-PER-LOCATION-01 to REVIEW;\n        paste manifest from $build-manifest.output into Notes property.\n\n      Manifest:\n      $build-manifest.output\n",
  "bdc-pos-staff-pin-auth-ui": "# Drafted by yaml-author for WO-POS-STAFF-PIN-AUTH-UI-01 on 2026-05-13\n# Source pattern: bdc-ce-regression-suite.yaml + WO-POS-TERMINAL-SDK-01.yaml\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring\n#\n# Pre-flight: codebase bluedevilcollectibles/lspro-react must be registered in Archon.\n#   Verify: sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name FROM remote_agent_codebases\"\n#\n# Pre-flight (4-block): YAML exists (this file), spec exists (docs/work-orders/\n#   WO-POS-STAFF-PIN-AUTH-UI-01.md in bdc-xo), CWD scope = CODE single-repo\n#   (lspro-react), codebase registered. Open Question: if store_staff.pin_hash\n#   missing OR /staff/pin-login route missing in shopops-api, WO grows MIXED —\n#   operator surfaces to XO before re-firing.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-POS-STAFF-PIN-AUTH-UI-01.yaml\n\nname: bdc-pos-staff-pin-auth-ui\ndescription: |\n  Use when: Executing WO-POS-STAFF-PIN-AUTH-UI-01 — Staff PIN login UI for POS.\n  Input: WO_ID = WO-POS-STAFF-PIN-AUTH-UI-01.\n  Does: Reads spec from bdc-xo, audits shopops-api for existing /staff/pin-login\n        + store_staff.pin_hash (Open Question gate), implements lspro-react\n        POSLoginPage + PinPad + PosAuthContext + RoleGate + posApiFetch + tests,\n        opens PR, builds CODE manifest, flips Notion.\n  Repo: bluedevilcollectibles/lspro-react.\n  parallel_ok: true.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-POS-STAFF-PIN-AUTH-UI-01}\"\n      # gh --template uses Go templates (built-in to gh, no external jq dependency)\n      SPEC=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        GLOB=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template \"{{range .}}{{if (or (hasPrefix .name \\\"${WO_ID}\\\") (and (hasPrefix .name \\\"2\\\") (contains .name \\\"${WO_ID}\\\")))}}{{.path}}{{\\\"\\\\n\\\"}}{{end}}{{end}}\" \\\n          2>/dev/null | head -1 || true)\n        if [ -n \"$GLOB\" ]; then\n          SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n--- SPEC ---\\n%s\\n' \"$WO_ID\" \"$WORKFLOW_ID\" \"$SPEC\"\n\n  - id: fresh-branch\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      # Defensive: if the default branch is already checked out in another worktree,\n      # we can't switch to it. Use a unique branch from the current HEAD instead.\n      if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n        echo \"default branch held by another worktree; staying on current HEAD\"\n      fi\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n      BASE=\"wo/pos-staff-pin-auth-ui-01\"\n      BRANCH=\"${BASE}-run-$(date +%Y%m%d%H%M%S)\"\n      if git show-ref --quiet \"refs/heads/$BASE\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE\" ]; then\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n          git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n        fi\n      else\n        BRANCH=\"$BASE\"\n        git checkout -b \"$BRANCH\"\n      fi\n      echo \"BRANCH=$BRANCH\"\n\n  - id: audit-backend\n    depends_on: [fresh-branch]\n    bash: |\n      # best-effort backend audit — surfaces Open Question gate\n      EXISTS=$(gh api \\\n        \"repos/bluedevilcollectibles/shopops/contents/shopops-api/routes/staff.js?ref=master\" \\\n        --template '{{.name}}' 2>/dev/null || true)\n      if [ -n \"$EXISTS\" ]; then\n        echo \"STATUS=ok\"\n        echo \"BACKEND_STAFF_ROUTES=present\"\n      else\n        echo \"STATUS=open_question\"\n        echo \"DETAIL=shopops-api/routes/staff.js not found; WO may need a MIXED companion. Surface to operator.\"\n      fi\n      exit 0\n\n  - id: plan\n    depends_on: [read-spec, audit-backend]\n    task_class: medium_work\n    prompt: |\n      Plan WO-POS-STAFF-PIN-AUTH-UI-01 (lspro-react only by default).\n\n      ## Required\n      - Files to create:\n        src/pos/POSLoginPage.tsx, src/pos/PinPad.tsx, src/pos/PosAuthContext.tsx,\n        src/pos/RoleGate.tsx, src/pos/posApiFetch.ts,\n        src/pos/__tests__/POSLoginPage.test.tsx,\n        src/pos/__tests__/RoleGate.test.tsx,\n        src/pos/__tests__/PosAuthContext.test.tsx\n      - Files to modify: src/pos/POSHeader.tsx (StaffBadge + LogoutButton), routing config\n      - Branch: wo/pos-staff-pin-auth-ui-01\n      - PR base: main\n      - Test command: bun test src/pos\n\n      ## Open Question gate\n      $audit-backend.output\n      If backend routes missing, output PLAN_BLOCKED_OPEN_QUESTION and stop.\n\n      ## Spec\n      $read-spec.output\n\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement WO-POS-STAFF-PIN-AUTH-UI-01. CWD = lspro-react repo root.\n\n        ## Plan\n        $plan.output\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n        1. Create the 5 source files per WO §7 + 3 test files.\n        2. PIN flow: 4-digit input, auto-submit on 4th digit, POST to /api/staff/pin-login.\n        3. PosAuthContext stores JWT in sessionStorage.pos_jwt, exposes { staff, login, logout, token }.\n        4. RoleGate: disabled children + overlay when staff.role not in props.roles.\n        5. posApiFetch: wraps existing apiFetch, injects Authorization Bearer + X-Shopops-Tenant.\n        6. On 401: clear sessionStorage + navigate('/pos/login').\n        7. Tests: >= 7 vitest tests asserting real behavior per WO §11.\n        8. Use existing styling utilities (Tailwind or styled if that is the repo convention).\n        9. Do NOT push, do NOT modify auth0 code.\n\n        Output <promise>COMPLETE</promise> when all files exist + bun test src/pos passes.\n\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      ls src/pos/POSLoginPage.tsx\n      ls src/pos/PinPad.tsx\n      ls src/pos/PosAuthContext.tsx\n      ls src/pos/RoleGate.tsx\n      ls src/pos/posApiFetch.ts\n      grep -q \"POSLoginPage\" src/pos/POSLoginPage.tsx\n      grep -q \"PinPad\" src/pos/PinPad.tsx\n      grep -q \"PosAuthContext\\|usePosAuth\\|createContext\" src/pos/PosAuthContext.tsx\n      grep -q \"RoleGate\" src/pos/RoleGate.tsx\n      grep -q \"pin-login\" src/pos/posApiFetch.ts\n      grep -q \"Authorization\" src/pos/posApiFetch.ts\n      TC=$(grep -rc \"it(\\|test(\" src/pos/__tests__ || echo 0)\n      echo \"Test count: $TC\"\n      [ \"$TC\" -ge 7 ] || (echo \"FAIL test count\" >&2; exit 1)\n\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      bun test src/pos || (echo \"Tests failed\" >&2; exit 1)\n\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/pos-staff-pin-auth-ui-01\"\n      if [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n        git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n      fi\n      git add src/pos\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(pos): WO-POS-STAFF-PIN-AUTH-UI-01 — PIN login + RoleGate\\n\\nAdds src/pos/POSLoginPage.tsx + PinPad.tsx + PosAuthContext.tsx + RoleGate.tsx +\\nposApiFetch.ts with 7+ vitest tests.\\nGates POS actions by role (clerk/manager/owner) per WO §6 matrix.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/pos-staff-pin-auth-ui-01\"\n      REPO=\"bluedevilcollectibles/lspro-react\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements WO-POS-STAFF-PIN-AUTH-UI-01 — Staff PIN login UI for POS.\"\n        echo \"\"\n        echo \"## Files created\"\n        echo \"- src/pos/POSLoginPage.tsx, PinPad.tsx, PosAuthContext.tsx, RoleGate.tsx, posApiFetch.ts\"\n        echo \"\"\n        echo \"## Vercel staging URL\"\n        echo \"https://staging.livesellerpro.app/pos/login\"\n      } > \"$BODY\"\n      EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"PR_URL=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo \"$REPO\" --base main --head \"$BRANCH\" \\\n            --title \"feat(pos): WO-POS-STAFF-PIN-AUTH-UI-01 — PIN login + RoleGate\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"PR_URL=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      echo \"open-pr failed\" >&2; exit 1\n\n  - id: build-manifest\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 (CODE class) for WO-POS-STAFF-PIN-AUTH-UI-01.\n\n      WO: WO-POS-STAFF-PIN-AUTH-UI-01\n      Builder: Codex (Archon bdc-pos-staff-pin-auth-ui)\n      Files created: src/pos/POSLoginPage.tsx, PinPad.tsx, PosAuthContext.tsx,\n                     RoleGate.tsx, posApiFetch.ts + 3 test files\n      Files modified: src/pos/POSHeader.tsx, src/pos routing\n      Tests: [count from $verify-tests.output] passing | bun test src/pos\n      PRs: [from $open-pr.output PR_URL] — base: main\n      Merge ancestors: lspro-react main HEAD: [from $commit-and-push.output SHA], behind_by: 0\n      Grep assertions:\n        - grep -q 'PosAuthContext' src/pos/PosAuthContext.tsx\n        - grep -q 'RoleGate' src/pos/RoleGate.tsx\n        - grep -q 'pin-login' src/pos/posApiFetch.ts\n        - grep -q 'Authorization' src/pos/posApiFetch.ts\n      Runtime verification: N/A (frontend-only WO)\n      Vercel deployment: staging URL https://staging.livesellerpro.app/pos/login | state READY | commit matches HEAD: pending Captain CI\n      VALIDATION: PASS\n\n      Inputs:\n      $read-spec.output\n      $audit-backend.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-and-push.output\n      $open-pr.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-POS-STAFF-PIN-AUTH-UI-01 to REVIEW. Manifest in Notes property.\n      Notion database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # OPEN QUESTION: Notion page ID for WO-POS-STAFF-PIN-AUTH-UI-01 not confirmed.\n      # Operator searches database by exact WO ID equality.\n\n      Update: Claude Status -> REVIEW, Status -> Review, Notes -> $build-manifest.output verbatim,\n      claimed_by: Codex, claimed_at: today.\n\n      If Notion MCP unavailable, output:\n        XO_TODO: Flip WO-POS-STAFF-PIN-AUTH-UI-01 to REVIEW; paste manifest into Notes property.\n\n      Manifest:\n      $build-manifest.output\n",
  "bdc-pos-terminal-hardware-validation": "# Drafted by yaml-author for WO-POS-TERMINAL-HARDWARE-VALIDATION-01 on 2026-05-13\n# Source pattern: WO-SHOPOPS-COMMERCE-SPINE-01.yaml (single-repo) + INFRA wrapper\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring\n#\n# Pre-flight: codebase bluedevilcollectibles/shopops registered in Archon.\n# Pre-flight: WO-POS-TERMINAL-SDK-01 must be MERGED + DEPLOYED before this fires.\n#   Operator verifies: curl https://api.livesellerpro.app/pos/terminal/mode -> 200.\n# Pre-flight (4-block): YAML exists, spec exists, CWD = shopops single-repo, codebase registered.\n# parallel_ok: false (operator-gated hardware validation).\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-POS-TERMINAL-HARDWARE-VALIDATION-01.yaml\n# REVIEW BEFORE DEPLOY — confirm Stripe LIVE keys received from John, reader hardware on-site.\n# This workflow builds the validation script + tests + env example. The actual\n# live $1 charge is OPERATOR-GATED — Archon does not run live Stripe charges.\n\nname: bdc-pos-terminal-hardware-validation\ndescription: |\n  Use when: Executing WO-POS-TERMINAL-HARDWARE-VALIDATION-01 — real-hardware validation\n            scaffolding for Stripe Terminal.\n  Input: WO_ID = WO-POS-TERMINAL-HARDWARE-VALIDATION-01.\n  Does: Reads spec from bdc-xo. Verifies WO-POS-TERMINAL-SDK-01 is merged. Implements\n        shopops-api/scripts/validate-terminal-hardware.js + 5 bun tests. Updates\n        .env.example with LIVE keys + STRIPE_TERMINAL_MODE. PR + build INFRA manifest\n        with the operator runbook (script invocation + ssh evidence capture). Flips\n        Notion to REVIEW.\n  Repo: bluedevilcollectibles/shopops.\n  parallel_ok: false (operator hardware gate).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-POS-TERMINAL-HARDWARE-VALIDATION-01}\"\n      # gh --template uses Go templates (built-in to gh, no external jq dependency)\n      SPEC=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        GLOB=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template \"{{range .}}{{if (or (hasPrefix .name \\\"${WO_ID}\\\") (and (hasPrefix .name \\\"2\\\") (contains .name \\\"${WO_ID}\\\")))}}{{.path}}{{\\\"\\\\n\\\"}}{{end}}{{end}}\" \\\n          2>/dev/null | head -1 || true)\n        if [ -n \"$GLOB\" ]; then\n          SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n--- SPEC ---\\n%s\\n' \"$WO_ID\" \"$WORKFLOW_ID\" \"$SPEC\"\n\n  - id: verify-prerequisite\n    depends_on: [read-spec]\n    bash: |\n      # best-effort: WO-POS-TERMINAL-SDK-01 must be merged\n      EXISTS=$(gh api \\\n        \"repos/bluedevilcollectibles/shopops/contents/shopops-api/routes/pos-terminal.js?ref=master\" \\\n        --template '{{.name}}' 2>/dev/null || true)\n      if [ -n \"$EXISTS\" ]; then\n        echo \"STATUS=ok\"\n        echo \"PREREQUISITE_MERGED=true\"\n      else\n        echo \"STATUS=blocked_prereq_missing\"\n        echo \"DETAIL=WO-POS-TERMINAL-SDK-01 not merged. pos-terminal.js missing on master. Operator must complete prerequisite first.\"\n        exit 1\n      fi\n      exit 0\n\n  - id: fresh-branch\n    depends_on: [verify-prerequisite]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      # Defensive: if the default branch is already checked out in another worktree,\n      # we can't switch to it. Use a unique branch from the current HEAD instead.\n      if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n        echo \"default branch held by another worktree; staying on current HEAD\"\n      fi\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n      BASE=\"wo/pos-terminal-hardware-validation-01\"\n      BRANCH=\"${BASE}-run-$(date +%Y%m%d%H%M%S)\"\n      if git show-ref --quiet \"refs/heads/$BASE\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE\" ]; then\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n          git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n        fi\n      else\n        BRANCH=\"$BASE\"\n        git checkout -b \"$BRANCH\"\n      fi\n      echo \"BRANCH=$BRANCH\"\n\n  - id: plan\n    depends_on: [read-spec, fresh-branch]\n    task_class: medium_work\n    prompt: |\n      Plan WO-POS-TERMINAL-HARDWARE-VALIDATION-01 (INFRA).\n\n      ## Files\n      Create:\n        - shopops-api/scripts/validate-terminal-hardware.js\n        - shopops-api/tests/test_terminal_hardware_validation.js\n      Modify:\n        - shopops-api/.env.example (add LIVE keys + STRIPE_TERMINAL_MODE comments)\n\n      Branch: wo/pos-terminal-hardware-validation-01\n      PR base: master\n\n      Operator runbook output (manifest artifact):\n        1. Install LIVE Stripe keys in Hetzner shopops-api env\n        2. ssh hetzner-prod 'docker exec shopops-api node /app/scripts/validate-terminal-hardware.js --pairing-code <code> --tenant <id>'\n        3. Tap real card on physical reader\n        4. Capture PI id + refund id evidence\n        5. Flip STRIPE_TERMINAL_MODE=live + restart shopops-api\n        6. Verify GET /pos/terminal/mode returns mode=live\n\n      ## Spec\n      $read-spec.output\n\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement WO-POS-TERMINAL-HARDWARE-VALIDATION-01.\n\n        ## Plan\n        $plan.output\n        ## Spec\n        $read-spec.output\n\n        Rules:\n        1. Create shopops-api/scripts/validate-terminal-hardware.js as a node CLI script:\n           - Reads CLI flags: --pairing-code, --tenant, --location-name (default 'Mutant Market HQ'),\n             --amount-cents (default 100)\n           - Uses services/stripe-terminal.js getStripeClient() (already exists from prerequisite)\n           - createOrFindLocation -> registerReader -> createValidationPaymentIntent ->\n             poll until succeeded (max 5min) -> refundValidationCharge -> emitRunLog\n           - On failure, writes failure reason to stderr + exits non-zero\n        2. Create tests/test_terminal_hardware_validation.js with >= 5 bun tests\n           per WO §11 (all using mocked stripe client; NO live calls in CI):\n             a. createOrFindLocation calls stripe.terminal.locations.create\n             b. registerReader inserts into stripe_terminal_readers\n             c. createValidationPaymentIntent uses card_present + automatic capture\n             d. refundValidationCharge calls stripe.refunds.create full amount\n             e. emitRunLog produces valid JSON with required keys\n        3. Update .env.example with comments documenting:\n             STRIPE_TERMINAL_MODE=simulator  # simulator | test_hardware | live\n             STRIPE_LIVE_SECRET_KEY=\n             STRIPE_LIVE_PUBLISHABLE_KEY=\n             MUTANT_MARKET_TENANT_ID=\n        4. Do NOT make live Stripe API calls. Do NOT push.\n\n        Output <promise>COMPLETE</promise> when script + tests + env example exist\n        and bun test passes.\n\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      ls shopops-api/scripts/validate-terminal-hardware.js\n      grep -q \"createOrFindLocation\\|locations.create\" shopops-api/scripts/validate-terminal-hardware.js\n      grep -q \"registerReader\\|readers.create\" shopops-api/scripts/validate-terminal-hardware.js\n      grep -q \"card_present\" shopops-api/scripts/validate-terminal-hardware.js\n      grep -q \"refunds.create\" shopops-api/scripts/validate-terminal-hardware.js\n      ls shopops-api/tests/test_terminal_hardware_validation.js\n      TC=$(grep -c \"it(\\|test(\" shopops-api/tests/test_terminal_hardware_validation.js || echo 0)\n      [ \"$TC\" -ge 5 ] || (echo \"FAIL test count $TC\" >&2; exit 1)\n      grep -q \"STRIPE_TERMINAL_MODE\" shopops-api/.env.example\n      grep -q \"STRIPE_LIVE_SECRET_KEY\" shopops-api/.env.example\n      echo \"verify-grep PASS\"\n\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      bun test tests/test_terminal_hardware_validation.js\n\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/pos-terminal-hardware-validation-01\"\n      if [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n        git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n      fi\n      git add \\\n        shopops-api/scripts/validate-terminal-hardware.js \\\n        shopops-api/tests/test_terminal_hardware_validation.js \\\n        shopops-api/.env.example\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(infra): WO-POS-TERMINAL-HARDWARE-VALIDATION-01 — hardware validation scaffold\\n\\nAdds shopops-api/scripts/validate-terminal-hardware.js — one-shot CLI to register a\\nphysical BBPOS reader against Stripe, run a $1 live test charge, capture evidence,\\nand refund. Run by operator on Hetzner after WO-POS-TERMINAL-SDK-01 is live.\\n\\nAdds tests/test_terminal_hardware_validation.js (5+ bun tests with mocked stripe).\\nDocuments STRIPE_TERMINAL_MODE + STRIPE_LIVE_* keys in .env.example.\\n\\nOperator runbook:\\n  ssh hetzner-prod '\\''docker exec shopops-api node /app/scripts/validate-terminal-hardware.js \\\\n    --pairing-code <code> --tenant <id> --location-name \\\"Mutant Market HQ\\\"'\\''\\n\\nDepends on: WO-POS-TERMINAL-SDK-01 (merged)\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/pos-terminal-hardware-validation-01\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements WO-POS-TERMINAL-HARDWARE-VALIDATION-01 — Stripe Terminal hardware validation scaffold.\"\n        echo \"\"\n        echo \"## What this PR ships\"\n        echo \"- scripts/validate-terminal-hardware.js (operator-run CLI)\"\n        echo \"- tests/test_terminal_hardware_validation.js (5+ mocked tests)\"\n        echo \"- .env.example documents LIVE keys + STRIPE_TERMINAL_MODE\"\n        echo \"\"\n        echo \"## What this PR does NOT do\"\n        echo \"- No live Stripe API calls in CI.\"\n        echo \"- No env flip to mode=live (operator-gated).\"\n        echo \"- No physical reader registration (operator runs the script on Hetzner).\"\n        echo \"\"\n        echo \"## Operator runbook (post-merge)\"\n        echo \"1. Install LIVE Stripe keys in Hetzner shopops-api env (Stripe Dashboard -> Developers)\"\n        echo \"2. ssh hetzner-prod 'docker exec shopops-api node /app/scripts/validate-terminal-hardware.js --pairing-code <CODE> --tenant <ID>'\"\n        echo \"3. Tap real card on physical reader when prompted\"\n        echo \"4. Capture PI id + refund id (script outputs JSON)\"\n        echo \"5. Flip STRIPE_TERMINAL_MODE=live; restart shopops-api\"\n        echo \"6. Verify: curl https://api.livesellerpro.app/pos/terminal/mode -> {mode:'live'}\"\n        echo \"\"\n        echo \"## Depends on\"\n        echo \"- WO-POS-TERMINAL-SDK-01 (merged + deployed)\"\n      } > \"$BODY\"\n      EXIST=$(gh pr list --repo bluedevilcollectibles/shopops --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"PR_URL=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo bluedevilcollectibles/shopops --base master --head \"$BRANCH\" \\\n            --title \"feat(infra): WO-POS-TERMINAL-HARDWARE-VALIDATION-01 — hardware validation scaffold\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"PR_URL=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      exit 1\n\n  - id: runtime-verify\n    depends_on: [open-pr]\n    bash: |\n      # Runtime verify of the SCRIPT presence (not the live charge — that is operator-gated)\n      RESULT=$(docker exec shopops-api ls /app/scripts/validate-terminal-hardware.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RULE19=script present in container: ${RESULT}\"\n      elif echo \"$RESULT\" | grep -qiE 'No such (container|file)|not found'; then\n        echo \"STATUS=skipped_container_not_rebuilt\"\n        echo \"RERUN=docker exec shopops-api ls /app/scripts/validate-terminal-hardware.js\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${RESULT}\"\n      fi\n      exit 0\n\n  - id: build-manifest\n    depends_on: [runtime-verify]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 (INFRA) for WO-POS-TERMINAL-HARDWARE-VALIDATION-01.\n\n      WO: WO-POS-TERMINAL-HARDWARE-VALIDATION-01\n      Builder: Codex (Archon bdc-pos-terminal-hardware-validation)\n      Files created: shopops-api/scripts/validate-terminal-hardware.js,\n                     shopops-api/tests/test_terminal_hardware_validation.js\n      Files modified: shopops-api/.env.example\n      Tests: from $verify-tests.output | bun test shopops-api/tests/test_terminal_hardware_validation.js\n      PRs: from $open-pr.output PR_URL — base: master\n      Merge ancestors: shopops master HEAD from $commit-and-push.output SHA, behind_by: 0\n      Grep assertions:\n        - grep -q 'card_present' shopops-api/scripts/validate-terminal-hardware.js\n        - grep -q 'refunds.create' shopops-api/scripts/validate-terminal-hardware.js\n        - grep -q 'STRIPE_TERMINAL_MODE' shopops-api/.env.example\n      Runtime verification:\n        - docker exec shopops-api ls /app/scripts/validate-terminal-hardware.js\n        - from $runtime-verify.output\n        - Live $1 charge evidence: OPERATOR-GATED. Manifest must include PI id + refund id\n          BEFORE Captain CI closes the WO.\n      Operator runbook checklist (must be filled in by operator post-deploy):\n        [ ] Stripe LIVE keys installed in Hetzner env\n        [ ] Reader paired (pairing_code recorded)\n        [ ] Validation script run (PI id captured)\n        [ ] Refund issued (refund_id captured)\n        [ ] STRIPE_TERMINAL_MODE flipped to 'live'\n        [ ] GET /pos/terminal/mode returns mode=live\n        [ ] stripe_terminal_readers row exists with reader id starting tmr_live_\n      Vercel deployment: N/A — shopops-api is Hetzner Docker\n      VALIDATION: PASS (scaffolding ships; live $1 evidence pending operator hardware run)\n\n      Inputs:\n      $read-spec.output\n      $verify-prerequisite.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-and-push.output\n      $open-pr.output\n      $runtime-verify.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-POS-TERMINAL-HARDWARE-VALIDATION-01 to REVIEW. Manifest in Notes property.\n      Notion database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # OPEN QUESTION: Notion page ID not confirmed. Operator searches by exact WO ID.\n\n      IMPORTANT: This WO is a HYBRID — scaffolding ships now, but the live $1 charge\n      evidence must be appended to the manifest by the operator after hardware arrives\n      at Mutant Market. Captain CI will not auto-close until operator pastes PI + refund ids.\n\n      Update: Claude Status -> REVIEW, Status -> Review, Notes -> $build-manifest.output,\n      claimed_by: Codex, claimed_at: today.\n\n      If Notion MCP unavailable:\n        XO_TODO: Flip WO-POS-TERMINAL-HARDWARE-VALIDATION-01 to REVIEW; paste manifest into Notes.\n\n      Manifest:\n      $build-manifest.output\n",
  "bdc-pos-terminal-sdk": "# Drafted by yaml-author for WO-POS-TERMINAL-SDK-01 on 2026-05-13\n# Source pattern: bdc-ce-regression-suite.yaml + WO-SHOPOPS-COMMERCE-SPINE-01.yaml\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight: codebases bluedevilcollectibles/shopops + bluedevilcollectibles/lspro-react\n#   must both be registered in Archon. Verify:\n#     sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name FROM remote_agent_codebases\"\n#   This WO is cross-repo. The workflow operates against the shopops repo as primary\n#   CWD; the lspro-react slice is delegated through a sibling branch + PR on that repo.\n#\n# Pre-flight (4-block): YAML exists (this file), spec exists (docs/work-orders/\n#   WO-POS-TERMINAL-SDK-01.md in bdc-xo), CWD scope = MIXED cross-repo, codebases registered.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-POS-TERMINAL-SDK-01.yaml\n# REVIEW BEFORE DEPLOY — operator confirms Stripe TEST keys in shopops-api env on Hetzner.\n\nname: bdc-pos-terminal-sdk\ndescription: |\n  Use when: Executing WO-POS-TERMINAL-SDK-01 — Stripe Terminal SDK integration.\n  Input: WO_ID = WO-POS-TERMINAL-SDK-01.\n  Does: Reads spec from bdc-xo (cross-repo gh api), verifies pos_transactions schema,\n        writes migration (shopops-api/migrations/20260513_pos_terminal_columns.sql),\n        adds shopops-api/routes/pos-terminal.js + services/stripe-terminal.js + bun\n        tests, opens shopops PR. Drafts lspro-react patch (useStripeTerminal hook +\n        PaymentPanel) in a sibling worktree and opens a second PR. Runs Rule 19\n        runtime-verify against shopops-api container. Builds MIXED manifest and flips\n        Notion to REVIEW.\n  Repo: bluedevilcollectibles/shopops (primary CWD), bluedevilcollectibles/lspro-react\n        (sibling via gh + git in /tmp clone).\n  parallel_ok: true (POS-lane WO; no schema conflicts with other lanes).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  # -------------------------------------------------------------------------\n  # 1. READ SPEC\n  # -------------------------------------------------------------------------\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-POS-TERMINAL-SDK-01}\"\n\n      # gh --template uses Go templates (built-in to gh, no external jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template \"{{range .}}{{if (or (hasPrefix .name \\\"${WO_ID}\\\") (and (hasPrefix .name \\\"2\\\") (contains .name \\\"${WO_ID}\\\")))}}{{.path}}{{\\\"\\\\n\\\"}}{{end}}{{end}}\" \\\n          2>/dev/null | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:docs/work-orders/%s.md\\nWORKFLOW_ID=%s\\n\\n' \\\n        \"$WO_ID\" \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  # -------------------------------------------------------------------------\n  # 2. FRESH BRANCH on shopops CWD (origin/master)\n  # -------------------------------------------------------------------------\n  - id: fresh-branch\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      # Defensive: if the default branch is already checked out in another worktree,\n      # we can't switch to it. Use a unique branch from the current HEAD instead.\n      if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n        echo \"default branch held by another worktree; staying on current HEAD\"\n      fi\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n      BASE_BRANCH=\"wo/pos-terminal-sdk-01\"\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      if git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE_BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE_BRANCH\" ]; then\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE_BRANCH\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n          git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n        fi\n      else\n        BRANCH=\"$BASE_BRANCH\"\n        git checkout -b \"$BRANCH\"\n      fi\n      echo \"BRANCH=${BRANCH}\"\n\n  # -------------------------------------------------------------------------\n  # 3. SCHEMA VERIFY — pos_transactions exists, columns present\n  # -------------------------------------------------------------------------\n  - id: schema-verify\n    depends_on: [fresh-branch]\n    bash: |\n      # set -e intentionally omitted — best-effort\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"\n        echo \"DETAIL=SUPABASE_URL/SERVICE_KEY not in env; operator must verify pos_transactions schema before deploy\"\n        exit 0\n      fi\n      R=$(curl -sf \\\n        \"${SUPABASE_URL}/rest/v1/information_schema.columns?table_name=eq.pos_transactions&select=column_name\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n        -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n      if echo \"$R\" | grep -q '\"id\"'; then\n        echo \"STATUS=ok\"\n        echo \"POS_TX_COLUMNS=${R}\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${R}\"\n      fi\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 4. PLAN\n  # -------------------------------------------------------------------------\n  - id: plan\n    depends_on: [read-spec, schema-verify]\n    task_class: medium_work\n    prompt: |\n      Plan WO-POS-TERMINAL-SDK-01 implementation. Output Markdown.\n\n      ## Required\n      - Files to create: shopops-api/migrations/20260513_pos_terminal_columns.sql,\n        shopops-api/routes/pos-terminal.js, shopops-api/services/stripe-terminal.js,\n        shopops-api/tests/test_pos_terminal.js,\n        lspro-react/src/pos/useStripeTerminal.ts, lspro-react/src/pos/PaymentPanel.tsx,\n        lspro-react/src/pos/__tests__/PaymentPanel.test.tsx\n      - Files to modify: shopops-api/index.js (mount /pos/terminal router),\n        lspro-react/src/pos/POSPage.tsx (or equivalent existing POS entrypoint;\n        IF none exists in lspro-react today, mount under /pos route),\n        lspro-react/package.json (add @stripe/terminal-js)\n      - Routes to add: see WO §7\n      - Test command (bun): bun test shopops-api/tests/test_pos_terminal.js\n      - Test command (vitest): cd lspro-react && bun test src/pos\n      - Branch (shopops): wo/pos-terminal-sdk-01\n      - Branch (lspro-react): wo/pos-terminal-sdk-01-ui\n      - PR base (shopops): master\n      - PR base (lspro-react): main\n\n      ## Spec\n      $read-spec.output\n\n      ## Schema verify\n      $schema-verify.output\n\n  # -------------------------------------------------------------------------\n  # 5. IMPLEMENT — shopops side\n  # -------------------------------------------------------------------------\n  - id: implement-shopops\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the shopops-api half of WO-POS-TERMINAL-SDK-01. CWD = shopops repo root.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n        1. Create shopops-api/migrations/20260513_pos_terminal_columns.sql with:\n           - ALTER TABLE pos_transactions ADD COLUMN IF NOT EXISTS stripe_terminal_reader_id text\n           - ALTER TABLE pos_transactions ADD COLUMN IF NOT EXISTS stripe_terminal_connection_token_at timestamptz\n           - CREATE TABLE IF NOT EXISTS stripe_terminal_readers (...)  per WO §7\n           - INSERT INTO schema_migrations VALUES ('20260513_pos_terminal_columns') ON CONFLICT DO NOTHING\n        2. Create shopops-api/services/stripe-terminal.js exporting:\n           - getStripeClient()\n           - createConnectionToken()\n           - createPaymentIntent({ amount_cents, transaction_id, tenant_id, idempotency_key })\n           - createRefund({ payment_intent_id, amount_cents, reason, idempotency_key })\n           Use the existing 'stripe' package (bun-installed) (it is already in shopops-api/package.json).\n        3. Create shopops-api/routes/pos-terminal.js with 5 endpoints per WO §7.\n           Pattern: require('express').Router(), use X-Shopops-Tenant header (same as pos.js).\n        4. Modify shopops-api/index.js to mount: app.use('/pos/terminal', require('./routes/pos-terminal'))\n        5. Create shopops-api/tests/test_pos_terminal.js using bun:test with >= 7 tests\n           covering WO §11 scenarios. Mock the stripe client via dependency injection\n           (export a factory from stripe-terminal.js).\n        6. Do NOT modify existing pos.js endpoints.\n        7. Do NOT run docker, do NOT push.\n\n        Output <promise>COMPLETE</promise> only when all files exist + bun test passes locally.\n\n  # -------------------------------------------------------------------------\n  # 6. IMPLEMENT — lspro-react side (sibling clone)\n  # -------------------------------------------------------------------------\n  - id: implement-lspro\n    depends_on: [implement-shopops]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the lspro-react half of WO-POS-TERMINAL-SDK-01.\n\n        The shopops CWD is your primary worktree. Clone lspro-react into a sibling\n        path and operate there:\n\n          git -C /tmp clone --depth=10 https://github.com/bluedevilcollectibles/lspro-react.git lspro-react\n          cd /tmp/lspro-react\n          git checkout -b wo/pos-terminal-sdk-01-ui\n\n        ## Plan\n        $plan.output\n\n        ## Execution Rules\n        1. Add @stripe/terminal-js to package.json dependencies (use latest stable).\n        2. Create src/pos/useStripeTerminal.ts hook per WO §7 (status state machine,\n           connection-token fetch, discover/connect, collectPayment helper).\n        3. Create src/pos/PaymentPanel.tsx per UI Hierarchy §5 (ReaderConnectionRow +\n           PaymentIntentRow + ErrorBanner).\n        4. Mount PaymentPanel in the existing POS page if present. If no POS page\n           exists yet in lspro-react, create src/pos/POSPage.tsx with a minimal shell\n           and route stub.\n        5. Create src/pos/__tests__/PaymentPanel.test.tsx with vitest. At minimum\n           4 tests covering simulator-connect, collect-payment happy path, amount=0\n           guard, error banner rendering.\n        6. Use existing apiFetch helper (do NOT hard-code shopops URL).\n        7. Branch name: wo/pos-terminal-sdk-01-ui. Commit identity must be John Ranson\n           / admin@bluedevilcollectibles.com.\n        8. Output <promise>COMPLETE</promise> when files exist + tests pass + branch\n           is pushed to origin.\n\n  # -------------------------------------------------------------------------\n  # 7. VERIFY GREP — multi-line-safe assertions\n  # -------------------------------------------------------------------------\n  - id: verify-grep\n    depends_on: [implement-shopops, implement-lspro]\n    bash: |\n      set -euo pipefail\n      echo \"=== shopops grep ===\"\n      ls shopops-api/migrations/20260513_pos_terminal_columns.sql\n      grep -q \"stripe_terminal_reader_id\" shopops-api/migrations/20260513_pos_terminal_columns.sql\n      grep -q \"stripe_terminal_readers\" shopops-api/migrations/20260513_pos_terminal_columns.sql\n      ls shopops-api/routes/pos-terminal.js\n      grep -q \"pos/terminal/connection-token\\|connection-token\" shopops-api/routes/pos-terminal.js\n      grep -q \"payment-intent\" shopops-api/routes/pos-terminal.js\n      grep -q \"refunds\" shopops-api/routes/pos-terminal.js\n      ls shopops-api/services/stripe-terminal.js\n      grep -q \"connectionTokens.create\" shopops-api/services/stripe-terminal.js\n      grep -q \"paymentIntents.create\" shopops-api/services/stripe-terminal.js\n      ls shopops-api/tests/test_pos_terminal.js\n      TC=$(grep -c \"it(\\|test(\" shopops-api/tests/test_pos_terminal.js || echo 0)\n      echo \"Backend test count: $TC\"\n      [ \"$TC\" -ge 7 ] || (echo \"FAIL backend test count\" >&2; exit 1)\n\n      echo \"=== lspro-react grep (sibling) ===\"\n      LR=/tmp/lspro-react\n      grep -q \"@stripe/terminal-js\" \"$LR/package.json\"\n      grep -q \"useStripeTerminal\" \"$LR/src/pos/useStripeTerminal.ts\"\n      grep -q \"PaymentPanel\" \"$LR/src/pos/PaymentPanel.tsx\"\n      echo \"=== verify-grep PASS ===\"\n\n  # -------------------------------------------------------------------------\n  # 8. VERIFY TESTS — bun\n  # -------------------------------------------------------------------------\n  - id: verify-tests\n    depends_on: [implement-shopops, implement-lspro]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      bun test tests/test_pos_terminal.js || (echo \"Backend tests failed\" >&2; exit 1)\n      cd /tmp/lspro-react\n      bun test src/pos || (echo \"Frontend tests failed; CI against staging is authoritative\" >&2)\n\n  # -------------------------------------------------------------------------\n  # 9. COMMIT + PUSH (shopops)\n  # -------------------------------------------------------------------------\n  - id: commit-shopops\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/pos-terminal-sdk-01\"\n      if [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n        git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n      fi\n      git add \\\n        shopops-api/migrations/20260513_pos_terminal_columns.sql \\\n        shopops-api/routes/pos-terminal.js \\\n        shopops-api/services/stripe-terminal.js \\\n        shopops-api/tests/test_pos_terminal.js \\\n        shopops-api/index.js\n      if git diff --cached --quiet; then\n        echo \"No shopops staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-POS-TERMINAL-SDK-01 — Stripe Terminal SDK endpoints + simulator path\\n\\nAdds shopops-api/routes/pos-terminal.js with:\\n  POST /pos/terminal/connection-token\\n  POST /pos/terminal/payment-intent\\n  POST /pos/terminal/payment-intent/:id/cancel\\n  POST /pos/terminal/refunds\\n  GET  /pos/terminal/mode\\n\\nAdds services/stripe-terminal.js (singleton + 3 helpers).\\nAdds migration 20260513_pos_terminal_columns.sql for stripe_terminal_reader_id +\\nstripe_terminal_connection_token_at + stripe_terminal_readers table.\\nAdds tests/test_pos_terminal.js with 7+ bun tests.\\n\\nShips in simulator mode by default (STRIPE_TERMINAL_MODE=simulator).\\nReal-hardware validation is WO-POS-TERMINAL-HARDWARE-VALIDATION-01.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"SHOPOPS_BRANCH=${BRANCH}\"\n      echo \"SHOPOPS_SHA=$(git rev-parse HEAD)\"\n\n  # -------------------------------------------------------------------------\n  # 10. OPEN PR (shopops)\n  # -------------------------------------------------------------------------\n  - id: open-pr-shopops\n    depends_on: [commit-shopops]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/pos-terminal-sdk-01\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements WO-POS-TERMINAL-SDK-01 (shopops half) — Stripe Terminal SDK backend.\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- Created: shopops-api/migrations/20260513_pos_terminal_columns.sql\"\n        echo \"- Created: shopops-api/routes/pos-terminal.js\"\n        echo \"- Created: shopops-api/services/stripe-terminal.js\"\n        echo \"- Created: shopops-api/tests/test_pos_terminal.js\"\n        echo \"- Modified: shopops-api/index.js\"\n        echo \"\"\n        echo \"## Companion PR\"\n        echo \"lspro-react UI PR: see wo/pos-terminal-sdk-01-ui\"\n        echo \"\"\n        echo \"## Stop conditions\"\n        echo \"See bdc-xo:docs/work-orders/WO-POS-TERMINAL-SDK-01.md §12.\"\n      } > \"$BODY\"\n      EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"SHOPOPS_PR=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo \"$REPO\" --base master --head \"$BRANCH\" \\\n            --title \"feat(api): WO-POS-TERMINAL-SDK-01 — Stripe Terminal SDK\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"SHOPOPS_PR=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      echo \"open-pr-shopops failed\" >&2; exit 1\n\n  # -------------------------------------------------------------------------\n  # 11. COMMIT + PUSH + PR (lspro-react, sibling worktree)\n  # -------------------------------------------------------------------------\n  - id: commit-and-pr-lspro\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      cd /tmp/lspro-react\n      BRANCH=\"wo/pos-terminal-sdk-01-ui\"\n      git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n        add src/pos package.json\n      if git diff --cached --quiet; then\n        echo \"No lspro staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(pos): WO-POS-TERMINAL-SDK-01 — useStripeTerminal hook + PaymentPanel\\n\\nAdds src/pos/useStripeTerminal.ts (Terminal SDK lifecycle hook).\\nAdds src/pos/PaymentPanel.tsx (ReaderConnectionRow + PaymentIntentRow + ErrorBanner).\\nAdds @stripe/terminal-js dependency.\\nShips in simulator mode by default.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements WO-POS-TERMINAL-SDK-01 (lspro-react half).\"\n        echo \"\"\n        echo \"Vercel staging URL: https://staging.livesellerpro.app/pos\"\n      } > \"$BODY\"\n      EXIST=$(gh pr list --repo bluedevilcollectibles/lspro-react --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"LSPRO_PR=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo bluedevilcollectibles/lspro-react --base main --head \"$BRANCH\" \\\n            --title \"feat(pos): WO-POS-TERMINAL-SDK-01 — Terminal SDK UI\" --body-file \"$BODY\" 2>&1); then\n          echo \"LSPRO_PR=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      echo \"open-pr-lspro failed\" >&2; exit 1\n\n  # -------------------------------------------------------------------------\n  # 12. RUNTIME VERIFY — Rule 19 best-effort\n  # -------------------------------------------------------------------------\n  - id: runtime-verify\n    depends_on: [open-pr-shopops]\n    bash: |\n      RESULT=$(docker exec shopops-api grep -n \"pos/terminal\" /app/routes/pos-terminal.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RULE19=${RESULT}\"\n      elif echo \"$RESULT\" | grep -qiE 'No such (container|file)|not found'; then\n        echo \"STATUS=skipped_container_not_rebuilt\"\n        echo \"RERUN=docker exec shopops-api grep -n 'pos/terminal' /app/routes/pos-terminal.js\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${RESULT}\"\n      fi\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 13. BUILD MANIFEST\n  # -------------------------------------------------------------------------\n  - id: build-manifest\n    depends_on: [runtime-verify, commit-and-pr-lspro]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 for WO-POS-TERMINAL-SDK-01 (MIXED class).\n\n      WO: WO-POS-TERMINAL-SDK-01\n      Builder: Codex (Archon bdc-pos-terminal-sdk)\n      Files created (shopops):\n        - shopops-api/migrations/20260513_pos_terminal_columns.sql\n        - shopops-api/routes/pos-terminal.js\n        - shopops-api/services/stripe-terminal.js\n        - shopops-api/tests/test_pos_terminal.js\n      Files modified (shopops):\n        - shopops-api/index.js\n      Files created (lspro-react):\n        - src/pos/useStripeTerminal.ts\n        - src/pos/PaymentPanel.tsx\n        - src/pos/__tests__/PaymentPanel.test.tsx\n      Files modified (lspro-react):\n        - package.json (added @stripe/terminal-js)\n      Tests:\n        - bun test shopops-api/tests/test_pos_terminal.js -> [from $verify-tests.output]\n        - bun test lspro-react/src/pos -> [from $verify-tests.output]\n      PRs:\n        - shopops: [from $open-pr-shopops.output SHOPOPS_PR]\n        - lspro-react: [from $commit-and-pr-lspro.output LSPRO_PR]\n      Merge ancestors:\n        - shopops master HEAD: [from $commit-shopops.output SHOPOPS_SHA], behind_by: 0\n        - lspro-react main HEAD: TBD by Captain CI, behind_by: 0\n      Grep assertions:\n        - grep -q 'pos/terminal' shopops-api/routes/pos-terminal.js\n        - grep -q 'connectionTokens.create' shopops-api/services/stripe-terminal.js\n        - grep -q 'paymentIntents.create' shopops-api/services/stripe-terminal.js\n        - grep -q 'stripe_terminal_reader_id' shopops-api/migrations/20260513_pos_terminal_columns.sql\n        - grep -q '@stripe/terminal-js' lspro-react/package.json\n        - grep -q 'useStripeTerminal' lspro-react/src/pos/useStripeTerminal.ts\n      Runtime verification:\n        - docker exec shopops-api grep -n 'pos/terminal' /app/routes/pos-terminal.js\n        - [actual output from $runtime-verify.output]\n      Vercel deployment:\n        - lspro-react: staging URL https://staging.livesellerpro.app/pos | state READY | commit matches HEAD: pending Captain CI\n      VALIDATION: PASS (pending migration apply + container rebuild + Vercel deploy)\n\n      Inputs:\n      $read-spec.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-shopops.output\n      $open-pr-shopops.output\n      $commit-and-pr-lspro.output\n      $runtime-verify.output\n\n  # -------------------------------------------------------------------------\n  # 14. FLIP NOTION\n  # -------------------------------------------------------------------------\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-POS-TERMINAL-SDK-01 to REVIEW. Manifest goes in the Notes\n      property (NOT a comment — Captain CI reads Notes).\n\n      Notion database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # OPEN QUESTION: page ID for WO-POS-TERMINAL-SDK-01 not yet authored in Notion.\n      # Operator searches database by exact WO ID equality (NOT keyword) and pastes page ID.\n\n      Update:\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> manifest text from $build-manifest.output verbatim\n      - claimed_by: Codex (Archon)\n      - claimed_at: today\n\n      If Notion MCP unavailable, output:\n        XO_TODO: Flip WO-POS-TERMINAL-SDK-01 to REVIEW; paste manifest from $build-manifest.output into Notes property.\n\n      Manifest:\n      $build-manifest.output\n",
  "bdc-pos-voice-assistant-wiring": "# Drafted by yaml-author for WO-POS-VOICE-ASSISTANT-WIRING-01 on 2026-05-13\n# Source pattern: bdc-ce-regression-suite.yaml + WO-POS-TERMINAL-SDK-01.yaml\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring\n#\n# Pre-flight: codebases bluedevilcollectibles/shopops + bluedevilcollectibles/lspro-react\n#   registered in Archon. VOICE_BRIDGE is local-machine deploy (G10 box, no CI) —\n#   VOICE_BRIDGE patches are produced as a diff file in the manifest for John to\n#   apply manually.\n#\n# Pre-flight (4-block): YAML exists, spec exists (docs/work-orders/\n#   WO-POS-VOICE-ASSISTANT-WIRING-01.md in bdc-xo), CWD = shopops + lspro-react\n#   cross-repo (VOICE_BRIDGE side surfaces as patch file), codebases registered.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-POS-VOICE-ASSISTANT-WIRING-01.yaml\n\nname: bdc-pos-voice-assistant-wiring\ndescription: |\n  Use when: Executing WO-POS-VOICE-ASSISTANT-WIRING-01 — Voice Bridge wiring for POS.\n  Input: WO_ID = WO-POS-VOICE-ASSISTANT-WIRING-01.\n  Does: Reads spec from bdc-xo. Implements shopops-api pos-voice routes (customer-context\n        + SSE events + state + log) + pos_voice_log migration + bun tests. Implements\n        lspro-react VoiceStateIndicator + CustomerTakeoverScreen + tests. Produces a\n        VOICE_BRIDGE patch file (pos-context.ts + prompt composer) as manifest artifact\n        for John to apply locally. Rule 19 runtime-verify. Builds MIXED manifest.\n        Flips Notion to REVIEW.\n  Repo: bluedevilcollectibles/shopops (primary CWD), bluedevilcollectibles/lspro-react\n        (sibling clone). VOICE_BRIDGE = manual deploy via patch file.\n  parallel_ok: true (pos_voice_log is isolated new table).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-POS-VOICE-ASSISTANT-WIRING-01}\"\n      # gh --template uses Go templates (built-in to gh, no external jq dependency)\n      SPEC=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        GLOB=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template \"{{range .}}{{if (or (hasPrefix .name \\\"${WO_ID}\\\") (and (hasPrefix .name \\\"2\\\") (contains .name \\\"${WO_ID}\\\")))}}{{.path}}{{\\\"\\\\n\\\"}}{{end}}{{end}}\" \\\n          2>/dev/null | head -1 || true)\n        if [ -n \"$GLOB\" ]; then\n          SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n--- SPEC ---\\n%s\\n' \"$WO_ID\" \"$WORKFLOW_ID\" \"$SPEC\"\n\n  - id: fresh-branch\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      # Defensive: if the default branch is already checked out in another worktree,\n      # we can't switch to it. Use a unique branch from the current HEAD instead.\n      if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n        echo \"default branch held by another worktree; staying on current HEAD\"\n      fi\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n      BASE=\"wo/pos-voice-assistant-wiring-01\"\n      BRANCH=\"${BASE}-run-$(date +%Y%m%d%H%M%S)\"\n      if git show-ref --quiet \"refs/heads/$BASE\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE\" ]; then\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n          git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n        fi\n      else\n        BRANCH=\"$BASE\"\n        git checkout -b \"$BRANCH\"\n      fi\n      echo \"BRANCH=$BRANCH\"\n\n  - id: schema-verify\n    depends_on: [fresh-branch]\n    bash: |\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"\n        exit 0\n      fi\n      R=$(curl -sf \\\n        \"${SUPABASE_URL}/rest/v1/information_schema.columns?table_name=eq.customers&select=column_name\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n        -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n      echo \"STATUS=ok\"\n      echo \"CUSTOMERS_COLUMNS=${R}\"\n      exit 0\n\n  - id: plan\n    depends_on: [read-spec, schema-verify]\n    task_class: medium_work\n    prompt: |\n      Plan WO-POS-VOICE-ASSISTANT-WIRING-01 (MIXED).\n\n      ## Files\n      Create:\n        - shopops-api/migrations/20260513_pos_voice_log.sql\n        - shopops-api/routes/pos-voice.js\n        - shopops-api/services/voice-events.js (SSE multiplexer)\n        - shopops-api/tests/test_pos_voice.js\n        - lspro-react/src/pos/VoiceStateIndicator.tsx\n        - lspro-react/src/pos/CustomerTakeoverScreen.tsx\n        - lspro-react/src/pos/__tests__/VoiceStateIndicator.test.tsx\n        - lspro-react/src/pos/__tests__/CustomerTakeoverScreen.test.tsx\n        - voice-bridge-patch.diff (VOICE_BRIDGE side; manifest artifact only)\n      Modify: shopops-api/index.js (mount /pos/voice router)\n\n      ## Spec\n      $read-spec.output\n\n      ## Schema\n      $schema-verify.output\n\n  - id: implement-shopops\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement shopops-api half of WO-POS-VOICE-ASSISTANT-WIRING-01.\n\n        ## Plan\n        $plan.output\n        ## Spec\n        $read-spec.output\n\n        Rules:\n        1. Migration creates pos_voice_log per WO §7.\n        2. routes/pos-voice.js exports 4 endpoints per WO §7:\n           POST /pos/voice/customer-context, GET /pos/voice/events (SSE),\n           POST /pos/voice/state, POST /pos/voice/log.\n        3. services/voice-events.js manages per-tenant SSE subscribers\n           (Map<tenantId, Set<response>>); broadcastState(tenantId, payload).\n        4. POST /pos/voice/state requires shared secret VOICE_BRIDGE_TOKEN header.\n        5. tests/test_pos_voice.js — >= 5 bun tests per WO §11.\n        6. Mount in index.js.\n        7. NO push, NO docker.\n\n        Output <promise>COMPLETE</promise> when all files exist + bun test passes.\n\n  - id: implement-lspro\n    depends_on: [implement-shopops]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement lspro-react half of WO-POS-VOICE-ASSISTANT-WIRING-01.\n\n          git -C /tmp clone --depth=10 https://github.com/bluedevilcollectibles/lspro-react.git lspro-react\n          cd /tmp/lspro-react\n          git checkout -b wo/pos-voice-assistant-wiring-01-ui\n\n        ## Plan\n        $plan.output\n\n        Rules:\n        1. VoiceStateIndicator.tsx subscribes to EventSource('/api/pos/voice/events').\n           Renders mic icon + waveform + caption per WO §6 state matrix.\n        2. CustomerTakeoverScreen.tsx listens for SSE event type='customer_arrived',\n           fetches POST /api/pos/voice/customer-context, renders overlay.\n        3. >= 2 component tests per file (4 total minimum).\n        4. Branch wo/pos-voice-assistant-wiring-01-ui. Commit identity John Ranson /\n           admin@bluedevilcollectibles.com.\n        5. Output <promise>COMPLETE</promise> when files exist + bun test src/pos passes\n           + branch pushed.\n\n  - id: produce-voice-bridge-patch\n    depends_on: [implement-lspro]\n    task_class: medium_work\n    prompt: |\n      Produce voice-bridge-patch.diff describing the changes required in VOICE_BRIDGE\n      project (local machine, no CI). Output a unified diff format covering:\n        - src/pos-context.ts (new): fetchCustomerContext, buildGeminiSystemPrompt, postVoiceState\n        - tests/pos-context.test.ts (new): >= 2 tests\n        - integration into existing wake-word handler\n\n      Save the diff to voice-bridge-patch.diff in the shopops CWD root. This file\n      is a manifest artifact — John applies it manually on G10. Do NOT push.\n\n      ## Spec\n      $read-spec.output\n\n  - id: verify-grep\n    depends_on: [implement-shopops, implement-lspro, produce-voice-bridge-patch]\n    bash: |\n      set -euo pipefail\n      ls shopops-api/migrations/20260513_pos_voice_log.sql\n      grep -q \"pos_voice_log\" shopops-api/migrations/20260513_pos_voice_log.sql\n      ls shopops-api/routes/pos-voice.js\n      grep -q \"pos/voice/customer-context\\|customer-context\" shopops-api/routes/pos-voice.js\n      grep -q \"pos/voice/events\\|/events\" shopops-api/routes/pos-voice.js\n      grep -q \"pos/voice/state\\|/state\" shopops-api/routes/pos-voice.js\n      grep -q \"VOICE_BRIDGE_TOKEN\\|shared secret\" shopops-api/routes/pos-voice.js\n      ls shopops-api/services/voice-events.js\n      grep -q \"broadcastState\\|SSE\\|EventSource\" shopops-api/services/voice-events.js\n      TC=$(grep -c \"it(\\|test(\" shopops-api/tests/test_pos_voice.js || echo 0)\n      [ \"$TC\" -ge 5 ] || (echo \"FAIL backend test count $TC\" >&2; exit 1)\n\n      LR=/tmp/lspro-react\n      grep -q \"VoiceStateIndicator\" \"$LR/src/pos/VoiceStateIndicator.tsx\"\n      grep -q \"EventSource\\|/pos/voice/events\" \"$LR/src/pos/VoiceStateIndicator.tsx\"\n      grep -q \"CustomerTakeoverScreen\" \"$LR/src/pos/CustomerTakeoverScreen.tsx\"\n\n      ls voice-bridge-patch.diff\n      grep -q \"pos-context\" voice-bridge-patch.diff\n      echo \"verify-grep PASS\"\n\n  - id: verify-tests\n    depends_on: [implement-shopops, implement-lspro]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      bun test tests/test_pos_voice.js\n      cd /tmp/lspro-react\n      bun test src/pos || echo \"frontend tests may flake without DOM env; Captain CI authoritative\"\n\n  - id: commit-shopops\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/pos-voice-assistant-wiring-01\"\n      if [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n        git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n      fi\n      git add \\\n        shopops-api/migrations/20260513_pos_voice_log.sql \\\n        shopops-api/routes/pos-voice.js \\\n        shopops-api/services/voice-events.js \\\n        shopops-api/tests/test_pos_voice.js \\\n        shopops-api/index.js \\\n        voice-bridge-patch.diff\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-POS-VOICE-ASSISTANT-WIRING-01 — pos-voice routes + SSE + voice-bridge patch\\n\\nAdds routes/pos-voice.js (customer-context, events SSE, state, log).\\nAdds services/voice-events.js (SSE multiplexer).\\nAdds migration 20260513_pos_voice_log.sql.\\nAdds tests/test_pos_voice.js (5+ tests).\\nIncludes voice-bridge-patch.diff for John to apply on G10.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"SHOPOPS_SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr-shopops\n    depends_on: [commit-shopops]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/pos-voice-assistant-wiring-01\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements WO-POS-VOICE-ASSISTANT-WIRING-01 (shopops half) — POS voice routes + SSE.\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- Created: migrations/20260513_pos_voice_log.sql, routes/pos-voice.js, services/voice-events.js, tests/test_pos_voice.js\"\n        echo \"- Modified: index.js (mount /pos/voice)\"\n        echo \"- Manifest artifact: voice-bridge-patch.diff (apply on G10 manually)\"\n      } > \"$BODY\"\n      EXIST=$(gh pr list --repo bluedevilcollectibles/shopops --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"SHOPOPS_PR=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo bluedevilcollectibles/shopops --base master --head \"$BRANCH\" \\\n            --title \"feat(api): WO-POS-VOICE-ASSISTANT-WIRING-01 — pos-voice + SSE\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"SHOPOPS_PR=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      exit 1\n\n  - id: commit-and-pr-lspro\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      cd /tmp/lspro-react\n      BRANCH=\"wo/pos-voice-assistant-wiring-01-ui\"\n      git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" add src/pos\n      if git diff --cached --quiet; then\n        echo \"No lspro changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(pos): WO-POS-VOICE-ASSISTANT-WIRING-01 — VoiceStateIndicator + CustomerTakeoverScreen\\n\\nAdds src/pos/VoiceStateIndicator.tsx (SSE-subscribed mic state).\\nAdds src/pos/CustomerTakeoverScreen.tsx (beacon arrival overlay).\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      BODY=$(mktemp)\n      printf '## Summary\\nImplements WO-POS-VOICE-ASSISTANT-WIRING-01 (lspro-react half).\\n\\nVercel staging: https://staging.livesellerpro.app/pos\\n' > \"$BODY\"\n      EXIST=$(gh pr list --repo bluedevilcollectibles/lspro-react --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"LSPRO_PR=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo bluedevilcollectibles/lspro-react --base main --head \"$BRANCH\" \\\n            --title \"feat(pos): WO-POS-VOICE-ASSISTANT-WIRING-01 — voice state UI\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"LSPRO_PR=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      exit 1\n\n  - id: runtime-verify\n    depends_on: [open-pr-shopops]\n    bash: |\n      RESULT=$(docker exec shopops-api grep -n \"pos/voice\" /app/routes/pos-voice.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RULE19=${RESULT}\"\n      elif echo \"$RESULT\" | grep -qiE 'No such (container|file)|not found'; then\n        echo \"STATUS=skipped_container_not_rebuilt\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${RESULT}\"\n      fi\n      exit 0\n\n  - id: build-manifest\n    depends_on: [runtime-verify, commit-and-pr-lspro]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 (MIXED) for WO-POS-VOICE-ASSISTANT-WIRING-01.\n\n      WO: WO-POS-VOICE-ASSISTANT-WIRING-01\n      Builder: Codex (Archon bdc-pos-voice-assistant-wiring)\n      Files created (shopops): migrations/20260513_pos_voice_log.sql, routes/pos-voice.js,\n                                services/voice-events.js, tests/test_pos_voice.js,\n                                voice-bridge-patch.diff (manifest artifact)\n      Files modified (shopops): index.js\n      Files created (lspro-react): src/pos/VoiceStateIndicator.tsx, src/pos/CustomerTakeoverScreen.tsx,\n                                    + 2 test files\n      Tests:\n        - bun test shopops-api/tests/test_pos_voice.js -> from $verify-tests.output\n        - bun test lspro-react/src/pos -> from $verify-tests.output\n      PRs:\n        - shopops: from $open-pr-shopops.output SHOPOPS_PR\n        - lspro-react: from $commit-and-pr-lspro.output LSPRO_PR\n      Merge ancestors:\n        - shopops master HEAD: from $commit-shopops.output SHOPOPS_SHA, behind_by: 0\n        - lspro-react main HEAD: TBD by Captain CI, behind_by: 0\n      Grep assertions:\n        - grep -q 'pos/voice' shopops-api/routes/pos-voice.js\n        - grep -q 'VOICE_BRIDGE_TOKEN' shopops-api/routes/pos-voice.js\n        - grep -q 'pos_voice_log' shopops-api/migrations/20260513_pos_voice_log.sql\n        - grep -q 'EventSource' lspro-react/src/pos/VoiceStateIndicator.tsx\n      Runtime verification:\n        - docker exec shopops-api grep -n 'pos/voice' /app/routes/pos-voice.js\n        - from $runtime-verify.output\n      Voice Bridge manual deploy: voice-bridge-patch.diff applied on G10 by John (manifest-tracked)\n      Vercel deployment: staging URL https://staging.livesellerpro.app/pos | pending Captain CI\n      VALIDATION: PASS (pending migration + container rebuild + Vercel deploy + Voice Bridge patch apply)\n\n      Inputs:\n      $read-spec.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-shopops.output\n      $open-pr-shopops.output\n      $commit-and-pr-lspro.output\n      $runtime-verify.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-POS-VOICE-ASSISTANT-WIRING-01 to REVIEW. Manifest in Notes property.\n      Notion database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # OPEN QUESTION: Notion page ID not confirmed. Operator searches by exact WO ID.\n\n      Update: Claude Status -> REVIEW, Status -> Review, Notes -> $build-manifest.output,\n      claimed_by: Codex, claimed_at: today.\n\n      If Notion MCP unavailable:\n        XO_TODO: Flip WO-POS-VOICE-ASSISTANT-WIRING-01 to REVIEW; paste manifest into Notes.\n\n      Manifest:\n      $build-manifest.output\n",
  "bdc-recover-from-notion": "# Drafted for WO-HARNESS-RECOVER-FROM-NOTION-WORKFLOW-01 (bdc-xo#172).\n# Purpose: rescue WO spec content from Notion entries when the originating\n# author/build workflow filed the Notion pages but lost the local MD files\n# (anchor: 2026-05-16 engine sortie loss — 13 Notion pointers filed clean,\n# 26 spec/YAML/test-plan MD files vanished when push silently failed).\n#\n# Pipeline:\n#   read-page-ids  → parse comma-separated NOTION_PAGE_IDS, fail if empty\n#   fetch-pages    → for each id: Notion REST GET page + blocks, render MD\n#   write-files    → docs/work-orders/<WO-ID>.md (idempotent; skip if exists)\n#   commit-and-push (load_bearing: true) → real retry, exit !=0 on failure\n#   open-pr         (load_bearing: true) → real retry, exit !=0 on failure\n#\n# Doctrine compliance:\n#   - YAML Recipe v1 (12 sections)\n#   - WO-167 load-bearing convention: terminal git/gh ops MUST NOT swallow failure\n#   - WO-169 safe_push_with_retry pattern (3 attempts, exponential backoff,\n#     exits non-zero on final failure)\n#   - Notion REST via python3 + urllib (matches bdc-wo-grader; no MCP in YAML)\n#   - bun-only container: no node/npm/npx\n#\n# Fire:\n#   /workflow run bdc-recover-from-notion \\\n#     NOTION_PAGE_IDS=abc123,def456,... \\\n#     [TARGET_BRANCH=wo/recover-from-notion-<timestamp>] \\\n#     [PR_TITLE=\"rescue: ...\"]\n\nname: bdc-recover-from-notion\n\ndescription: |\n  Use when: A prior workflow filed N WO pages to Notion but lost the local MD\n            spec files (commit/push silently failed). Reads each Notion page\n            and writes its body to docs/work-orders/<WO-ID>.md in bdc-xo.\n  Inputs:\n    NOTION_PAGE_IDS: REQUIRED. Comma-separated list of Notion page UUIDs.\n    TARGET_BRANCH: optional. Default wo/recover-from-notion-<timestamp>.\n    PR_TITLE: optional. Default derived from page count.\n    NOTION_DB_ID: optional. Default Blue Devil WO database.\n    DRY_RUN: 'true' writes files locally but skips commit/push/PR.\n  Does: Fetches Notion pages → writes MD specs (idempotent, skips existing) →\n        commits → pushes (load-bearing, real retry) → opens PR (load-bearing).\n  Repo: bluedevilcollectibles/bdc-xo (select bdc-xo codebase when firing).\n  NOT for: authoring new specs from scratch (use bdc-author-wo-batch).\n  parallel_ok: false\n\npolicyFile: harness/policies/agent-behavior.md\nprovider: claude\nmodel: sonnet\n\ninputs:\n  NOTION_PAGE_IDS:\n    default: \"\"\n  TARGET_BRANCH:\n    default: \"\"\n  PR_TITLE:\n    default: \"\"\n  NOTION_DB_ID:\n    default: \"a6df831c-0b52-449f-8ca4-d77be6b70d0a\"\n  DRY_RUN:\n    default: \"false\"\n\nnodes:\n\n  # ---------------------------------------------------------------------------\n  # 1. READ-PAGE-IDS — parse + validate comma-separated input\n  # ---------------------------------------------------------------------------\n  - id: read-page-ids\n    bash: |\n      set -euo pipefail\n      # Engine injects YAML defaults as INPUT_<NAME>; legacy /workflow run uses bare names.\n      NOTION_PAGE_IDS=\"${INPUT_NOTION_PAGE_IDS:-${NOTION_PAGE_IDS:-}}\"\n      TARGET_BRANCH=\"${INPUT_TARGET_BRANCH:-${TARGET_BRANCH:-}}\"\n      PR_TITLE=\"${INPUT_PR_TITLE:-${PR_TITLE:-}}\"\n      DRY_RUN=\"${INPUT_DRY_RUN:-${DRY_RUN:-false}}\"\n\n      if [ -z \"$NOTION_PAGE_IDS\" ]; then\n        echo \"ERROR: NOTION_PAGE_IDS is required (comma-separated Notion page UUIDs).\" >&2\n        echo \"       Example: NOTION_PAGE_IDS=abc12345-...,def67890-...\" >&2\n        exit 1\n      fi\n\n      if [ -z \"${NOTION_API_KEY:-}\" ]; then\n        echo \"ERROR: NOTION_API_KEY env var is required (container Notion auth).\" >&2\n        exit 1\n      fi\n\n      # Derive default branch name if not provided.\n      if [ -z \"$TARGET_BRANCH\" ]; then\n        TARGET_BRANCH=\"wo/recover-from-notion-$(date +%Y%m%d%H%M%S)\"\n      fi\n\n      # Normalize: strip whitespace, split on commas, count.\n      mkdir -p .archon-recover\n      printf '%s' \"$NOTION_PAGE_IDS\" | tr ',' '\\n' | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' \\\n        | grep -v '^$' > .archon-recover/page-ids.txt\n      COUNT=$(wc -l < .archon-recover/page-ids.txt | tr -d ' ')\n      if [ \"$COUNT\" -eq 0 ]; then\n        echo \"ERROR: NOTION_PAGE_IDS parsed to zero ids.\" >&2\n        exit 1\n      fi\n\n      printf 'NOTION_PAGE_IDS=%s\\nPAGE_COUNT=%s\\nTARGET_BRANCH=%s\\nPR_TITLE=%s\\nDRY_RUN=%s\\n' \\\n        \"$NOTION_PAGE_IDS\" \"$COUNT\" \"$TARGET_BRANCH\" \"$PR_TITLE\" \"$DRY_RUN\"\n      echo \"--- page ids ---\"\n      cat .archon-recover/page-ids.txt\n\n  # ---------------------------------------------------------------------------\n  # 2. FETCH-PAGES — for each page: GET /v1/pages + GET /v1/blocks (recursive),\n  #    render to MD, derive filename from page title (WO-ID convention).\n  # ---------------------------------------------------------------------------\n  - id: fetch-pages\n    depends_on: [read-page-ids]\n    bash: |\n      set -euo pipefail\n      if [ ! -s .archon-recover/page-ids.txt ]; then\n        echo \"ERROR: .archon-recover/page-ids.txt missing or empty\" >&2\n        exit 1\n      fi\n      if [ -z \"${NOTION_API_KEY:-}\" ]; then\n        echo \"ERROR: NOTION_API_KEY missing\" >&2\n        exit 1\n      fi\n\n      mkdir -p .archon-recover/pages\n\n      python3 <<'PY'\n      import json, os, re, sys, time, urllib.error, urllib.request\n\n      TOKEN = os.environ[\"NOTION_API_KEY\"]\n      HEADERS = {\n          \"Authorization\": f\"Bearer {TOKEN}\",\n          \"Notion-Version\": \"2022-06-28\",\n          \"Content-Type\": \"application/json\",\n      }\n\n      def notion_get(url, attempts=3):\n          last_err = None\n          for i in range(attempts):\n              try:\n                  req = urllib.request.Request(url, headers=HEADERS, method=\"GET\")\n                  with urllib.request.urlopen(req, timeout=30) as resp:\n                      return json.loads(resp.read().decode())\n              except (urllib.error.URLError, urllib.error.HTTPError, TimeoutError) as e:\n                  last_err = e\n                  time.sleep(2 ** i)\n          raise RuntimeError(f\"notion_get failed after {attempts} attempts: {url} :: {last_err}\")\n\n      def rt_to_text(rich):\n          return \"\".join(r.get(\"plain_text\", \"\") for r in (rich or []))\n\n      def block_to_md(block, depth=0):\n          t = block.get(\"type\", \"\")\n          data = block.get(t, {}) or {}\n          rt = data.get(\"rich_text\", [])\n          text = rt_to_text(rt)\n          indent = \"  \" * depth\n          if t == \"heading_1\":\n              return f\"# {text}\"\n              # (intentional plain heading; rt already has plain text)\n          if t == \"heading_2\":\n              return f\"## {text}\"\n              # rendered for spec section parity\n          if t == \"heading_3\":\n              return f\"### {text}\"\n          if t == \"paragraph\":\n              return text\n          if t == \"bulleted_list_item\":\n              return f\"{indent}- {text}\"\n          if t == \"numbered_list_item\":\n              return f\"{indent}1. {text}\"\n          if t == \"to_do\":\n              checked = data.get(\"checked\", False)\n              box = \"[x]\" if checked else \"[ ]\"\n              return f\"{indent}- {box} {text}\"\n          if t == \"code\":\n              lang = data.get(\"language\", \"\") or \"\"\n              return f\"```{lang}\\n{text}\\n```\"\n          if t == \"quote\":\n              return f\"> {text}\"\n          if t == \"callout\":\n              return f\"> {text}\"\n          if t == \"divider\":\n              return \"---\"\n          if t == \"child_page\":\n              return f\"[child page: {data.get('title','')}]\"\n          # Fallback: emit type tag for unhandled blocks rather than silently dropping\n          if text:\n              return text\n          return f\"[unsupported block: {t}]\"\n\n      def fetch_blocks(block_id):\n          out = []\n          cursor = None\n          while True:\n              url = f\"https://api.notion.com/v1/blocks/{block_id}/children?page_size=100\"\n              if cursor:\n                  url += f\"&start_cursor={cursor}\"\n              data = notion_get(url)\n              for block in data.get(\"results\", []):\n                  out.append(block)\n                  if block.get(\"has_children\"):\n                      block[\"_children\"] = fetch_blocks(block[\"id\"])\n              if not data.get(\"has_more\"):\n                  break\n              cursor = data.get(\"next_cursor\")\n              if not cursor:\n                  break\n          return out\n\n      def render_blocks(blocks, depth=0):\n          lines = []\n          for b in blocks:\n              lines.append(block_to_md(b, depth))\n              if b.get(\"_children\"):\n                  lines.append(render_blocks(b[\"_children\"], depth + 1))\n          return \"\\n\\n\".join(s for s in lines if s != \"\")\n\n      def extract_title(page):\n          props = page.get(\"properties\", {})\n          # Try Task (WO board canonical), then Name, then any title prop\n          for key in (\"Task\", \"Name\", \"Title\"):\n              p = props.get(key)\n              if p and p.get(\"type\") == \"title\":\n                  return rt_to_text(p.get(\"title\", []))\n          for k, v in props.items():\n              if v.get(\"type\") == \"title\":\n                  return rt_to_text(v.get(\"title\", []))\n          return \"\"\n\n      def extract_wo_id(page, title):\n          props = page.get(\"properties\", {})\n          # Prefer explicit \"WO ID\" property\n          p = props.get(\"WO ID\")\n          if p:\n              typ = p.get(\"type\")\n              if typ == \"rich_text\":\n                  v = rt_to_text(p.get(\"rich_text\", [])).strip()\n                  if v:\n                      return v\n              elif typ == \"title\":\n                  v = rt_to_text(p.get(\"title\", [])).strip()\n                  if v:\n                      return v\n          # Fallback: parse WO-* from title\n          m = re.search(r\"WO-[A-Z0-9][A-Z0-9-]*-\\d+\", title or \"\")\n          if m:\n              return m.group(0)\n          return \"\"\n\n      page_ids = [\n          line.strip()\n          for line in open(\".archon-recover/page-ids.txt\", encoding=\"utf-8\")\n          if line.strip()\n      ]\n\n      manifest = []\n      errors = []\n      for pid in page_ids:\n          try:\n              page = notion_get(f\"https://api.notion.com/v1/pages/{pid}\")\n              title = extract_title(page)\n              wo_id = extract_wo_id(page, title)\n              blocks = fetch_blocks(pid)\n              body = render_blocks(blocks)\n              entry = {\n                  \"page_id\": pid,\n                  \"title\": title,\n                  \"wo_id\": wo_id,\n                  \"body\": body,\n                  \"url\": page.get(\"url\", \"\"),\n              }\n              safe_pid = pid.replace(\"-\", \"\")\n              with open(f\".archon-recover/pages/{safe_pid}.json\", \"w\", encoding=\"utf-8\") as f:\n                  json.dump(entry, f, indent=2, ensure_ascii=False)\n              manifest.append({\n                  \"page_id\": pid,\n                  \"wo_id\": wo_id,\n                  \"title\": title,\n                  \"url\": page.get(\"url\", \"\"),\n                  \"bytes\": len(body.encode(\"utf-8\")),\n              })\n              print(f\"FETCHED page_id={pid} wo_id={wo_id or '(unknown)'} bytes={len(body.encode('utf-8'))}\")\n          except Exception as e:\n              errors.append({\"page_id\": pid, \"error\": str(e)})\n              print(f\"FETCH_ERROR page_id={pid} :: {e}\", file=sys.stderr)\n\n      with open(\".archon-recover/manifest.json\", \"w\", encoding=\"utf-8\") as f:\n          json.dump({\"pages\": manifest, \"errors\": errors}, f, indent=2, ensure_ascii=False)\n\n      print(f\"\\nFETCH_SUMMARY total={len(page_ids)} ok={len(manifest)} err={len(errors)}\")\n      if errors:\n          # Any fetch failure is load-bearing: if we lose a page silently, we lose the spec.\n          print(\"ERROR: one or more Notion fetches failed; aborting before write-files\", file=sys.stderr)\n          sys.exit(1)\n      PY\n\n  # ---------------------------------------------------------------------------\n  # 3. WRITE-FILES — docs/work-orders/<WO-ID>.md (idempotent; skip if exists)\n  # ---------------------------------------------------------------------------\n  - id: write-files\n    depends_on: [fetch-pages]\n    bash: |\n      set -euo pipefail\n      if [ ! -f .archon-recover/manifest.json ]; then\n        echo \"ERROR: manifest.json missing — fetch-pages did not run\" >&2\n        exit 1\n      fi\n\n      mkdir -p docs/work-orders\n\n      python3 <<'PY'\n      import json, os, re, sys\n\n      manifest = json.load(open(\".archon-recover/manifest.json\", encoding=\"utf-8\"))\n      written = []\n      skipped = []\n      orphans = []  # pages without a derivable WO ID\n\n      for entry in manifest[\"pages\"]:\n          pid = entry[\"page_id\"]\n          wo_id = entry.get(\"wo_id\") or \"\"\n          title = entry.get(\"title\") or \"\"\n          safe_pid = pid.replace(\"-\", \"\")\n          page_path = f\".archon-recover/pages/{safe_pid}.json\"\n          if not os.path.exists(page_path):\n              print(f\"WARN: page payload missing for {pid}\", file=sys.stderr)\n              continue\n          page = json.load(open(page_path, encoding=\"utf-8\"))\n          body = page.get(\"body\", \"\")\n\n          if not wo_id:\n              # Cannot derive a canonical filename. Park under a recovery dir\n              # so the data is not lost; operator renames after review.\n              os.makedirs(\"docs/work-orders/_recovered\", exist_ok=True)\n              fallback = f\"docs/work-orders/_recovered/notion-{safe_pid}.md\"\n              if os.path.exists(fallback):\n                  skipped.append({\"path\": fallback, \"reason\": \"exists\"})\n                  print(f\"SKIP existing {fallback}\")\n                  continue\n              with open(fallback, \"w\", encoding=\"utf-8\") as f:\n                  f.write(f\"# {title or pid}\\n\\n\")\n                  f.write(f\"<!-- Recovered from Notion page {pid} (URL: {page.get('url','')}) -->\\n\")\n                  f.write(\"<!-- WO ID could not be derived from properties or title; operator must rename. -->\\n\\n\")\n                  f.write(body)\n                  f.write(\"\\n\")\n              orphans.append(fallback)\n              print(f\"WROTE_ORPHAN {fallback}\")\n              continue\n\n          # Canonical path\n          out = f\"docs/work-orders/{wo_id}.md\"\n          if os.path.exists(out):\n              skipped.append({\"path\": out, \"reason\": \"exists (idempotent skip)\"})\n              print(f\"SKIP existing {out}\")\n              continue\n          with open(out, \"w\", encoding=\"utf-8\") as f:\n              f.write(f\"# {wo_id}: {title or ''}\\n\\n\".rstrip() + \"\\n\\n\")\n              f.write(f\"<!-- Recovered from Notion page {pid} ({page.get('url','')}) -->\\n\\n\")\n              f.write(body)\n              f.write(\"\\n\")\n          written.append(out)\n          print(f\"WROTE {out}\")\n\n      summary = {\n          \"written_count\": len(written),\n          \"skipped_count\": len(skipped),\n          \"orphan_count\": len(orphans),\n          \"written\": written,\n          \"orphans\": orphans,\n          \"skipped\": skipped,\n      }\n      with open(\".archon-recover/write-summary.json\", \"w\", encoding=\"utf-8\") as f:\n          json.dump(summary, f, indent=2)\n\n      print(f\"\\nWRITE_SUMMARY written={len(written)} skipped={len(skipped)} orphan={len(orphans)}\")\n      # Hard fail only if NOTHING was written AND nothing was already-on-disk;\n      # an all-skip run is a no-op (idempotent re-fire) and acceptable.\n      if len(written) == 0 and len(orphans) == 0 and len(skipped) == 0:\n          print(\"ERROR: write-files produced zero output\", file=sys.stderr)\n          sys.exit(1)\n      PY\n\n  # ---------------------------------------------------------------------------\n  # 4. COMMIT-AND-PUSH — load_bearing: true (WO-167 / WO-169)\n  #    Real retry. Exits non-zero on final failure. NO STATUS=*_failed swallow.\n  # ---------------------------------------------------------------------------\n  - id: commit-and-push\n    depends_on: [write-files]\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      DRY_RUN=\"${INPUT_DRY_RUN:-${DRY_RUN:-false}}\"\n      TARGET_BRANCH=\"${INPUT_TARGET_BRANCH:-${TARGET_BRANCH:-}}\"\n      if [ -z \"$TARGET_BRANCH\" ]; then\n        # Mirror read-page-ids default; read-page-ids stdout is not env-injected.\n        TARGET_BRANCH=\"wo/recover-from-notion-$(date +%Y%m%d%H%M%S)\"\n      fi\n\n      if [ -z \"${GITHUB_TOKEN:-}\" ]; then\n        echo \"ERROR: GITHUB_TOKEN missing in container — push will fail.\" >&2\n        echo \"       Auth boundary issue is the root cause of the 2026-05-16 loss.\" >&2\n        exit 1\n      fi\n\n      DEFAULT_BRANCH=\"$(git remote show origin 2>/dev/null | sed -n 's,.*HEAD branch: ,,p')\"\n      DEFAULT_BRANCH=\"${DEFAULT_BRANCH:-main}\"\n\n      # Check out a clean recovery branch off the default branch.\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50\n      if git show-ref --quiet \"refs/heads/${TARGET_BRANCH}\"; then\n        git checkout \"$TARGET_BRANCH\"\n      else\n        git checkout -b \"$TARGET_BRANCH\" \"origin/${DEFAULT_BRANCH}\"\n      fi\n\n      # Stage recovered files only; do not sweep arbitrary working-tree changes.\n      git add docs/work-orders/ 2>/dev/null || true\n\n      if git diff --cached --quiet; then\n        echo \"NO_CHANGES — write-files produced no new content (all pages already on disk).\"\n        echo \"BRANCH=$TARGET_BRANCH\"\n        echo \"STATUS=no_changes_idempotent\"\n        exit 0\n      fi\n\n      WRITE_COUNT=$(git diff --cached --name-only | wc -l | tr -d ' ')\n      git -c user.name=\"bdc-recover-from-notion\" \\\n          -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"rescue(wo): recover ${WRITE_COUNT} WO spec(s) from Notion\n\n      Recovered via bdc-recover-from-notion workflow (WO-HARNESS-RECOVER-FROM-NOTION-WORKFLOW-01).\n      Source of truth: Notion WO pages. Anchor: 2026-05-16 engine sortie loss.\n\n      Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>\"\n\n      if [ \"$DRY_RUN\" = \"true\" ]; then\n        echo \"DRY_RUN=true — skipping push\"\n        echo \"BRANCH=$TARGET_BRANCH\"\n        echo \"STATUS=committed_dry_run\"\n        exit 0\n      fi\n\n      # safe_push_with_retry — load-bearing: real exit non-zero on final failure (WO-167/169).\n      ATTEMPTS=3\n      i=1\n      while [ \"$i\" -le \"$ATTEMPTS\" ]; do\n        if git push -u origin \"$TARGET_BRANCH\" 2>&1; then\n          echo \"PUSH_OK attempt=$i branch=$TARGET_BRANCH\"\n          echo \"BRANCH=$TARGET_BRANCH\"\n          echo \"SHA=$(git rev-parse HEAD)\"\n          echo \"STATUS=pushed\"\n          exit 0\n        fi\n        echo \"PUSH_FAIL attempt=$i — retrying after $((2 ** i))s\" >&2\n        sleep $((2 ** i))\n        i=$((i + 1))\n      done\n\n      echo \"ERROR: git push failed after $ATTEMPTS attempts — recovered files are local only.\" >&2\n      echo \"       Branch $TARGET_BRANCH exists in container; operator must rescue manually.\" >&2\n      echo \"STATUS=push_failed\" >&2\n      exit 1\n\n  # ---------------------------------------------------------------------------\n  # 5. OPEN-PR — load_bearing: true (WO-167 / WO-169)\n  #    Real retry. Exits non-zero on final failure.\n  # ---------------------------------------------------------------------------\n  - id: open-pr\n    depends_on: [commit-and-push]\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      DRY_RUN=\"${INPUT_DRY_RUN:-${DRY_RUN:-false}}\"\n      PR_TITLE=\"${INPUT_PR_TITLE:-${PR_TITLE:-}}\"\n\n      if [ \"$DRY_RUN\" = \"true\" ]; then\n        echo \"DRY_RUN=true — skipping PR creation\"\n        echo \"STATUS=skipped_dry_run\"\n        exit 0\n      fi\n\n      BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo \"\")\n      if [ -z \"$BRANCH\" ] || [ \"$BRANCH\" = \"HEAD\" ]; then\n        echo \"ERROR: detached HEAD; cannot open PR.\" >&2\n        exit 1\n      fi\n\n      DEFAULT_BRANCH=\"$(git remote show origin 2>/dev/null | sed -n 's,.*HEAD branch: ,,p')\"\n      DEFAULT_BRANCH=\"${DEFAULT_BRANCH:-main}\"\n      REPO=\"$(gh repo view --json nameWithOwner --template '{{.nameWithOwner}}' 2>/dev/null || echo '')\"\n      if [ -z \"$REPO\" ]; then\n        echo \"ERROR: could not resolve current repo via gh.\" >&2\n        exit 1\n      fi\n\n      # Did the upstream commit-and-push actually have changes?\n      if ! git diff \"origin/${DEFAULT_BRANCH}...HEAD\" --quiet 2>/dev/null; then\n        :  # diffs exist — proceed\n      else\n        echo \"NO_DIFF vs origin/${DEFAULT_BRANCH} — no PR needed (idempotent run).\"\n        echo \"STATUS=no_diff_idempotent\"\n        exit 0\n      fi\n\n      # Idempotency: if PR already exists for this branch, surface URL and exit OK.\n      EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n              --json url --jq '.[0].url // empty' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then\n        echo \"PR_URL=$EXIST\"\n        echo \"STATUS=pr_already_exists\"\n        exit 0\n      fi\n\n      WRITE_COUNT=$(git diff \"origin/${DEFAULT_BRANCH}...HEAD\" --name-only | wc -l | tr -d ' ')\n      if [ -z \"$PR_TITLE\" ]; then\n        PR_TITLE=\"rescue(wo): recover ${WRITE_COUNT} WO spec(s) from Notion\"\n      fi\n\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Recovered ${WRITE_COUNT} WO spec file(s) from Notion via \\`bdc-recover-from-notion\\` workflow.\"\n        echo \"\"\n        echo \"Anchor: WO-HARNESS-RECOVER-FROM-NOTION-WORKFLOW-01 (bdc-xo#172).\"\n        echo \"Root cause being rescued: prior workflow filed Notion pointer entries\"\n        echo \"successfully but lost MD spec files when commit-and-push failed silently.\"\n        echo \"\"\n        echo \"## Validation\"\n        echo \"Each recovered file derived from a Notion page body. Files use\"\n        echo \"\\`docs/work-orders/<WO-ID>.md\\` layout (canonical) or\"\n        echo \"\\`docs/work-orders/_recovered/notion-<page-id>.md\\` (orphans without\"\n        echo \"a derivable WO ID — operator renames after review).\"\n        echo \"\"\n        echo \"## Doctrine\"\n        echo \"- WO-167 load-bearing convention applied: commit-and-push and open-pr\"\n        echo \"  nodes exit non-zero on real failure (no STATUS=*_failed swallow).\"\n        echo \"- WO-169 safe_push_with_retry pattern: 3 attempts, exponential backoff.\"\n        echo \"\"\n        echo \"Generated by bdc-recover-from-notion workflow.\"\n      } > \"$BODY_FILE\"\n\n      # Real retry — exit non-zero on final failure.\n      ATTEMPTS=3\n      i=1\n      while [ \"$i\" -le \"$ATTEMPTS\" ]; do\n        if URL=$(gh pr create --repo \"$REPO\" --base \"$DEFAULT_BRANCH\" --head \"$BRANCH\" \\\n                   --title \"$PR_TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$URL\"\n          echo \"STATUS=pr_created\"\n          exit 0\n        fi\n        # Race: another caller may have created the PR between list + create.\n        EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n                --json url --jq '.[0].url // empty' 2>/dev/null || true)\n        if [ -n \"$EXIST\" ]; then\n          echo \"PR_URL=$EXIST\"\n          echo \"STATUS=pr_already_exists_race\"\n          exit 0\n        fi\n        echo \"PR_CREATE_FAIL attempt=$i — retrying after $((2 ** i))s\" >&2\n        sleep $((2 ** i))\n        i=$((i + 1))\n      done\n\n      echo \"ERROR: gh pr create failed after $ATTEMPTS attempts — branch pushed but no PR.\" >&2\n      echo \"STATUS=pr_open_failed\" >&2\n      exit 1\n\n  # ---------------------------------------------------------------------------\n  # 6. FLIP-NOTION — sentinel for operator handoff. The grader workflow owns\n  #    the actual REVIEW flip on the WO that owns this rescue (WO-172).\n  # ---------------------------------------------------------------------------\n  - id: flip-notion\n    depends_on: [open-pr]\n    bash: |\n      set -euo pipefail\n      echo \"FLIP_NOTION: bdc-recover-from-notion run complete.\"\n      PR_URL=$(gh pr view --json url --template '{{.url}}' 2>/dev/null || echo \"unknown\")\n      echo \"PR_URL=$PR_URL\"\n      echo \"RECOVERED_PAGES_MANIFEST=.archon-recover/manifest.json\"\n      echo \"WRITE_SUMMARY=.archon-recover/write-summary.json\"\n      echo \"XO_TODO: operator reviews PR + renames any docs/work-orders/_recovered/notion-*.md orphans.\"\n",
  "bdc-shopops-channel-admin-ui": "# Drafted by yaml-author subagent for WO-SHOPOPS-CHANNEL-ADMIN-UI-01 on 2026-05-13\n# Source pattern: bdc-ce-regression-suite.yaml (gold-standard; adapted for lspro-react UI WO)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n# Pre-flight: codebase bluedevilcollectibles/lspro-react must be registered in Archon\n#   Verify: sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name FROM remote_agent_codebases\"\n#   Expected: bluedevilcollectibles/lspro-react present\n# Pre-flight: WO spec must be committed + pushed to bdc-xo \"origin/$DEFAULT_BRANCH\" before firing\n#   File: docs/work-orders/WO-SHOPOPS-CHANNEL-ADMIN-UI-01.md\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-SHOPOPS-CHANNEL-ADMIN-UI-01.yaml\n# REVIEW BEFORE DEPLOY: human verifies test thresholds (N=5), screenshot loop (N=3), Notion page ID.\n# OPEN QUESTION: Notion page ID for WO-SHOPOPS-CHANNEL-ADMIN-UI-01 is not yet confirmed.\n#   XO must update the flip-notion node with the real Notion page ID before deploying.\n# OPEN QUESTION: The WO depends on WO-SHOPOPS-COMMERCE-SPINE-01 for\n#   GET /channels/connections + PATCH /channels/connections/:channel. If those endpoints\n#   are not yet live, the implement node is instructed to build against stub data from\n#   the existing GET /api/channels/config. Builder must record which fallback was used\n#   in the manifest so Captain CI can re-verify after spine WO ships.\n\nname: bdc-shopops-channel-admin-ui\ndescription: |\n  Use when: Shipping WO-SHOPOPS-CHANNEL-ADMIN-UI-01 — Channels admin page in BBDB Dashboard.\n  Input: WO_ID = WO-SHOPOPS-CHANNEL-ADMIN-UI-01.\n  Does: Reads spec from bdc-xo, extends src/pages/ChannelsConfigPage.tsx (route /settings/channels)\n        with full connect/disconnect modal UI for Shopify + WooCommerce, entitlement gates,\n        PATCH /channels/connections/:channel API calls, 5 Vitest unit tests, 3-screenshot\n        build-time verification loop, Vercel deployment check, PR to main, Notion REVIEW flip.\n  Repo: bluedevilcollectibles/lspro-react. Spec lives in bluedevilcollectibles/bdc-xo.\n  parallel_ok: true (single repo, no schema migration, independent of other in-flight WOs)\n  NOT for: any other WO.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-SHOPOPS-CHANNEL-ADMIN-UI-01}\"\n\n      # Spec lives in bdc-xo repo, not in lspro-react. Fetch via gh api cross-repo.\n      # gh --template uses Go templates (built-in to gh, no external jq dependency).\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Glob fallback: list docs/superpowers/specs and grep for WO ID\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  - id: audit-prior-art\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — audit is best-effort; never fails the workflow\n      set -u\n      echo \"=== Prior art audit for WO-SHOPOPS-CHANNEL-ADMIN-UI-01 ===\"\n\n      # Confirm route is already wired in App.tsx (lspro-react has src/, but tolerate absence)\n      if [ -e src/App.tsx ]; then\n        grep -n \"settings/channels\" src/App.tsx 2>/dev/null || \\\n          grep -rn \"settings/channels\" src/ 2>/dev/null | head -5 || true\n      else\n        echo \"(src/App.tsx not present; skipping route grep)\"\n      fi\n\n      # Confirm ChannelsConfigPage.tsx exists and check its current shape\n      if [ -e src/pages/ChannelsConfigPage.tsx ]; then\n        ls -la src/pages/ChannelsConfigPage.tsx 2>/dev/null || true\n        echo \"--- ChannelsConfigPage.tsx head ---\"\n        head -60 src/pages/ChannelsConfigPage.tsx 2>/dev/null || true\n      else\n        echo \"(src/pages/ChannelsConfigPage.tsx not yet present)\"\n      fi\n\n      # Confirm apiFetch helper location\n      if [ -e src/lib/api.ts ]; then\n        ls -la src/lib/api.ts 2>/dev/null || true\n        echo \"--- apiFetch signature ---\"\n        grep -n \"export.*apiFetch\" src/lib/api.ts 2>/dev/null || true\n      fi\n\n      # Check tiers helper (best-effort)\n      if [ -e src/lib/tiers.ts ]; then\n        grep -n \"meetsTier\\|ELITE\\|shop_ops\" src/lib/tiers.ts 2>/dev/null | head -20 || true\n      else\n        grep -rn \"meetsTier\" src/lib/ 2>/dev/null | head -10 || true\n      fi\n\n      # Check existing test structure\n      echo \"--- Existing test files ---\"\n      find src/ -name \"*.test.ts\" -o -name \"*.test.tsx\" 2>/dev/null | head -20 || true\n\n      echo \"=== Audit complete ===\"\n      exit 0\n\n  - id: plan\n    depends_on: [audit-prior-art]\n    task_class: medium_work\n    prompt: |\n      Plan the Channels admin UI implementation for lspro-react.\n\n      ## Context from prior-art audit\n      $audit-prior-art.output\n\n      ## Spec\n      $read-spec.output\n\n      ## Required Output (Markdown)\n      - WO ID: WO-SHOPOPS-CHANNEL-ADMIN-UI-01\n      - Files to MODIFY (do NOT create new files unless strictly necessary):\n        - src/pages/ChannelsConfigPage.tsx (extend with modal logic, entitlement gates, PATCH calls)\n        - src/lib/api.ts (only if a channels-specific helper is warranted — otherwise use existing apiFetch directly)\n      - Files to CREATE (only what the spec requires):\n        - src/tests/channels-admin-ui.test.ts (5 Vitest tests — see test requirements below)\n      - Files explicitly OUT OF SCOPE (do not touch):\n        - src/App.tsx (route already wired at /settings/channels)\n        - Any other page, component, or route not explicitly listed above\n        - server.js (Rule 13: no new routes here)\n        - Playwright e2e tests (out of scope for this WO; build-time screenshots cover visual)\n      - Test requirements (N=5, minimum, asserting real behavior):\n        1. ChannelCard renders \"Connect Shopify\" button when shopify status = not_connected\n        2. ChannelCard renders \"Disconnect\" button when shopify status = connected\n        3. \"Connect Shopify\" button is disabled when entitlement for shopify is missing\n        4. ConnectShopifyModal submits PATCH with correct payload (mock apiFetch, assert call args)\n        5. Disconnect confirm guard: PATCH NOT called when user cancels confirm dialog\n      - Verification commands:\n        - bun run test (or bun test) — must exit 0 with >= 5 passing assertions\n        - bunx tsc --noEmit (TypeScript clean)\n        - grep -c \"ConnectShopifyModal\\|ConnectWooCommerceModal\" src/pages/ChannelsConfigPage.tsx\n        - grep -c \"PATCH\" src/pages/ChannelsConfigPage.tsx\n      - Commit message: feat(channels): WO-SHOPOPS-CHANNEL-ADMIN-UI-01 — channels connect/disconnect UI\n      - Branch: wo/shopops-channel-admin-ui-01\n      - Push policy: feature branch -> PR against main (lspro-react staging gate required)\n      - API fallback: if GET /channels/connections is not yet available (WO-SHOPOPS-COMMERCE-SPINE-01\n        not shipped), fall back to GET /api/channels/config and note in manifest.\n\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 3\n      prompt: |\n        Implement the Channels admin UI for WO-SHOPOPS-CHANNEL-ADMIN-UI-01 in lspro-react.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n        - CWD is the lspro-react repo root. All paths are relative to it.\n        - Read src/pages/ChannelsConfigPage.tsx FIRST before writing any changes.\n          Do NOT invent a new layout. Follow the exact inline-CSS style pattern already in use.\n        - Use apiFetch from @/lib/api. It returns Promise<Response> — you MUST call .json() on the result.\n          Pattern: apiFetch('/channels/connections').then(r => r.json()).then(data => ...)\n        - If GET /channels/connections is not live, fall back to GET /api/channels/config (already working).\n          Detect availability: try /channels/connections, on 404/network error fall back and log.\n        - Extend ChannelsConfigPage.tsx with:\n          a) ConnectShopifyModal — inline, rendered conditionally. Fields: shopify_store_url (text),\n             shopify_access_token (password). Save button calls PATCH /channels/connections/shopify.\n          b) ConnectWooCommerceModal — inline, rendered conditionally. Fields: woocommerce_store_url (text),\n             woocommerce_consumer_key (text), woocommerce_consumer_secret (password).\n             Save button calls PATCH /channels/connections/woocommerce.\n          c) ChannelCard action buttons now call openModal(channel) or confirm+PATCH disconnect.\n          d) Entitlement gate: shopify and woocommerce Connect buttons disabled (and tooltip shown)\n             when tenant plan does NOT include shopify_connector / woocommerce_connector entitlement.\n             Read the existing meetsTier / useAuth pattern from ChannelsConfigPage.tsx.\n          e) ComicStoreOS Portal card (comicstoreos_portal) always shows Connected, no action button.\n          f) On PATCH success: refetch channels list, close modal, show success feedback (console.log\n             acceptable if no toast system is in scope; do NOT add a new toast library).\n          g) On PATCH error: show error message inline in modal (do NOT close modal on error).\n        - Create src/tests/channels-admin-ui.test.ts with exactly 5 Vitest tests per the plan.\n          Use @testing-library/react + vitest. Mock apiFetch via vi.mock('@/lib/api').\n          Tests must assert real behavior (button text, disabled state, PATCH call args, etc.)\n          not just \"did not throw.\"\n        - Do NOT modify App.tsx, server.js, or any file outside the plan's file list.\n        - Do NOT add new npm dependencies.\n        - Write a Vitest test for every new logic helper or component you create.\n\n        ## Completion criteria\n        Output <promise>COMPLETE</promise> only when:\n          - src/pages/ChannelsConfigPage.tsx updated with ConnectShopifyModal, ConnectWooCommerceModal,\n            entitlement gate, PATCH calls (search for these strings — they must be present)\n          - src/tests/channels-admin-ui.test.ts exists with >= 5 test() / it() blocks\n          - No TypeScript errors introduced (key check: apiFetch usage, modal prop types)\n          - No new npm dependencies added (package.json unchanged or only scripts updated)\n\n  - id: verify-types\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      if [ -f tsconfig.json ]; then\n        bunx tsc --noEmit\n        echo \"TypeScript check: PASS\"\n      else\n        echo \"No tsconfig.json found — skipping type check\"\n      fi\n\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      # Archon container is bun-only (no node/npm binary). Use bun run test.\n      # See memory: reference_archon-container-runtime-bun-only.md\n      if [ -f package.json ]; then\n        bun run test 2>&1 | tee /tmp/test-output.txt\n        EXIT=${PIPESTATUS[0]}\n        # Count passing tests from vitest output\n        PASS_COUNT=$(grep -c \"pass\\|v \" /tmp/test-output.txt 2>/dev/null || echo \"unknown\")\n        echo \"TEST_OUTPUT_TAIL=$(tail -5 /tmp/test-output.txt)\"\n        echo \"PASS_COUNT=$PASS_COUNT\"\n        if [ $EXIT -ne 0 ]; then\n          echo \"TESTS_FAILED=true\"\n          cat /tmp/test-output.txt >&2\n          exit 1\n        fi\n        echo \"TESTS_PASSED=true\"\n      else\n        echo \"No package.json found\" >&2\n        exit 1\n      fi\n\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      echo \"=== Grep assertions for WO-SHOPOPS-CHANNEL-ADMIN-UI-01 ===\"\n\n      # Assert modal components exist in ChannelsConfigPage.tsx\n      grep -c \"ConnectShopifyModal\" src/pages/ChannelsConfigPage.tsx\n      grep -c \"ConnectWooCommerceModal\" src/pages/ChannelsConfigPage.tsx\n\n      # Assert PATCH calls exist\n      grep -c \"PATCH\" src/pages/ChannelsConfigPage.tsx\n\n      # Assert test file exists with >= 5 test blocks\n      ls src/tests/channels-admin-ui.test.ts\n      TEST_COUNT=$(grep -c \"test(\\|it(\" src/tests/channels-admin-ui.test.ts)\n      echo \"test_block_count=$TEST_COUNT\"\n      if [ \"$TEST_COUNT\" -lt 5 ]; then\n        echo \"FAIL: expected >= 5 test blocks, found $TEST_COUNT\" >&2\n        exit 1\n      fi\n\n      # Assert apiFetch used (not a new fetch wrapper)\n      grep -c \"apiFetch\" src/pages/ChannelsConfigPage.tsx\n\n      # Assert entitlement gate present\n      grep -q \"disabled\\|entitlement\\|ELITE\\|shop_ops\\|meetsTier\" src/pages/ChannelsConfigPage.tsx || \\\n        (echo \"FAIL: no entitlement gate found in ChannelsConfigPage.tsx\" >&2; exit 1)\n\n      echo \"=== All grep assertions passed ===\"\n\n  - id: build-check\n    depends_on: [verify-types, verify-tests, verify-grep]\n    bash: |\n      set -euo pipefail\n      # Run Vite build to confirm no compilation errors slip through type check\n      bun run build 2>&1 | tee /tmp/build-output.txt\n      EXIT=${PIPESTATUS[0]}\n      if [ $EXIT -ne 0 ]; then\n        echo \"BUILD FAILED\" >&2\n        cat /tmp/build-output.txt >&2\n        exit 1\n      fi\n      echo \"BUILD_STATUS=ok\"\n      echo \"BUILD_DIST=$(ls dist/ | head -5)\"\n\n  - id: screenshot-loop\n    depends_on: [build-check]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: SCREENSHOTS_COMPLETE\n      max_iterations: 3\n      prompt: |\n        Produce the 3 required UI Build-Time Verification screenshots for WO-SHOPOPS-CHANNEL-ADMIN-UI-01.\n\n        ## Context\n        The Vite build completed successfully. The dist/ output is available.\n        The route is /settings/channels in lspro-react.\n\n        ## Required Screenshots (per wo-recipe.md UI verification)\n        For each screenshot, describe what the UI should look like based on the source code\n        you can inspect at src/pages/ChannelsConfigPage.tsx. Then produce a MARKDOWN block\n        with a text-based mockup if a real screenshot cannot be taken in this headless environment.\n\n        1. SCREENSHOT_1: ChannelsConfigPage — disconnected state\n           - All channels show \"Not connected\" badge\n           - Shopify card shows enabled \"Connect Shopify\" button\n           - WooCommerce card shows enabled \"Connect WooCommerce\" button\n           - ComicStoreOS Portal card shows \"Connected\" badge, no action button\n           - No modals open\n\n        2. SCREENSHOT_2: ChannelsConfigPage — Shopify connected state\n           - Shopify card shows \"Connected\" badge + store URL\n           - Shopify card shows \"Disconnect\" button\n           - Other cards unchanged\n\n        3. SCREENSHOT_3: ConnectShopifyModal open\n           - Modal overlay visible\n           - Two inputs: Store URL (text) + Access Token (password)\n           - Save + Cancel buttons\n\n        ## Instruction\n        Inspect src/pages/ChannelsConfigPage.tsx and produce a code-level confirmation\n        that each screenshot scenario is achievable from the current implementation.\n        For each, output:\n          - SCREENSHOT_N: <PASS|FAIL>\n          - Evidence: <what in the code supports this rendering>\n          - Gap (if any): <what needs fixing>\n\n        If all 3 pass with no gaps: output <promise>SCREENSHOTS_COMPLETE</promise>\n        If any fail: make targeted corrections to src/pages/ChannelsConfigPage.tsx,\n        then output <promise>SCREENSHOTS_COMPLETE</promise> once all 3 pass.\n\n  - id: commit-and-push\n    depends_on: [screenshot-loop]\n    bash: |\n      set -euo pipefail\n      BASE_BRANCH=\"wo/shopops-channel-admin-ui-01\"\n      # Default to a unique timestamped branch (avoids worktree collisions on rerun).\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n\n      # If the base branch is free (not held by another worktree, not yet existing),\n      # prefer it for cleaner PR review.\n      if ! git worktree list --porcelain | grep -qx \"branch refs/heads/$BASE_BRANCH\"; then\n        if [ \"$(git rev-parse --abbrev-ref HEAD)\" = \"$BASE_BRANCH\" ]; then\n          BRANCH=\"$BASE_BRANCH\"\n        elif ! git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n          BRANCH=\"$BASE_BRANCH\"\n        fi\n      fi\n\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n\n      # Stage only the files specified in the plan (no accidental scope creep)\n      git add src/pages/ChannelsConfigPage.tsx\n      git add src/tests/channels-admin-ui.test.ts\n\n      # Stage package.json only if scripts were added (e.g. test:channels)\n      if git diff --cached package.json 2>/dev/null | grep -q \"^+\" ; then\n        git add package.json\n      fi\n\n      # Idempotent commit: skip if already committed\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains the requested work.\"\n        echo \"BRANCH=${BRANCH}\"\n        echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n        exit 0\n      fi\n\n      # Commit identity: admin@bluedevilcollectibles.com required for Vercel team membership\n      git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n        commit -m \"feat(channels): WO-SHOPOPS-CHANNEL-ADMIN-UI-01 — channels connect/disconnect UI\n\n      Extends ChannelsConfigPage (/settings/channels) with:\n      - ConnectShopifyModal: shopify_store_url + shopify_access_token fields\n      - ConnectWooCommerceModal: woocommerce_store_url + consumer_key + consumer_secret fields\n      - PATCH /channels/connections/:channel for connect + disconnect flows\n      - Entitlement gate: shopify/woocommerce buttons disabled without ELITE/add-on\n      - ComicStoreOS Portal card always shows Connected (no action)\n      - 5 Vitest unit tests in src/tests/channels-admin-ui.test.ts\n\n      Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>\"\n\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Read actual branch from current HEAD (commit-and-push may have used unique suffix).\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/lspro-react\"\n      BASE=\"main\"\n      TITLE=\"feat(channels): WO-SHOPOPS-CHANNEL-ADMIN-UI-01 — channels connect/disconnect UI\"\n\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-SHOPOPS-CHANNEL-ADMIN-UI-01** — Channels admin page UI.\"\n        echo \"\"\n        echo \"Route: \\`/settings/channels\\` (already wired in App.tsx)\"\n        echo \"\"\n        echo \"## Changes\"\n        echo \"- \\`src/pages/ChannelsConfigPage.tsx\\` — extended with ConnectShopifyModal,\"\n        echo \"  ConnectWooCommerceModal, PATCH /channels/connections/:channel calls,\"\n        echo \"  entitlement gate (shopify/woocommerce disabled without ELITE), disconnect flow\"\n        echo \"- \\`src/tests/channels-admin-ui.test.ts\\` — 5 Vitest unit tests\"\n        echo \"\"\n        echo \"## API dependency\"\n        echo \"GET /channels/connections + PATCH /channels/connections/:channel\"\n        echo \"(WO-SHOPOPS-COMMERCE-SPINE-01). Falls back to GET /api/channels/config if not live.\"\n        echo \"\"\n        echo \"## Test evidence\"\n        echo \"\\`\\`\\`\"\n        echo \"$verify-tests.output\"\n        echo \"\\`\\`\\`\"\n        echo \"\"\n        echo \"## Grep assertions\"\n        echo \"\\`\\`\\`\"\n        echo \"$verify-grep.output\"\n        echo \"\\`\\`\\`\"\n        echo \"\"\n        echo \"## Build\"\n        echo \"\\`\\`\\`\"\n        echo \"$build-check.output\"\n        echo \"\\`\\`\\`\"\n        echo \"\"\n        echo \"Spec: bdc-xo \\`docs/work-orders/WO-SHOPOPS-CHANNEL-ADMIN-UI-01.md\\`\"\n        echo \"\"\n        echo \"Generated by Archon harness (WO-SHOPOPS-CHANNEL-ADMIN-UI-01)\"\n      } > \"$BODY_FILE\"\n\n      # Idempotent PR creation (gh --template avoids jq dependency)\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n          --json url --template '{{range .}}{{.url}}{{\"\\n\"}}{{end}}' 2>/dev/null | head -1\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after 5 retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  - id: verify-vercel\n    depends_on: [open-pr]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step (Vercel deploy may be in-flight)\n      # Rule 11 note: no n8n webhook. Vercel status fetched via gh + Vercel API.\n      REPO=\"bluedevilcollectibles/lspro-react\"\n\n      # Wait up to 90s for Vercel deploy triggered by the PR\n      echo \"Waiting 30s for Vercel preview deploy to start...\"\n      sleep 30\n\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo wo/shopops-channel-admin-ui-01)\"\n      RESULT=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" \\\n        --state open --json url,headRefName \\\n        --template '{{range .}}url={{.url}} branch={{.headRefName}}{{\"\\n\"}}{{end}}' 2>&1)\n      EXIT=$?\n\n      if [ $EXIT -eq 0 ] && echo \"$RESULT\" | grep -q \"url\"; then\n        echo \"STATUS=pr_open\"\n        echo \"DETAIL=PR exists; Vercel auto-preview will deploy on merge to main per lspro-react pipeline\"\n        echo \"NOTE=Vercel preview URL will appear in PR checks. Production deploys on main merge.\"\n      elif echo \"$RESULT\" | grep -qiE 'not found|no pull requests'; then\n        echo \"STATUS=skipped_pr_not_found\"\n        echo \"DETAIL=$RESULT\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=$RESULT\"\n      fi\n      exit 0\n\n  - id: build-manifest\n    depends_on: [verify-vercel]\n    task_class: clerical\n    prompt: |\n      Build the Captain CI manifest v2 for WO-SHOPOPS-CHANNEL-ADMIN-UI-01.\n\n      ## Required manifest fields (manifest v2 format per CLAUDE.md Three Rules)\n      WO: WO-SHOPOPS-CHANNEL-ADMIN-UI-01\n      Builder: Major Build (Archon harness)\n      Files modified: lspro-react/src/pages/ChannelsConfigPage.tsx\n      Files created: lspro-react/src/tests/channels-admin-ui.test.ts\n      Tests: [count from verify-tests.output] passing / [total] total with command: bun run test\n      PRs: [from open-pr.output PR_URL, merged timestamp, commit from commit-and-push.output BRANCH_SHA]\n      Merge ancestors: lspro-react main HEAD [run: git rev-parse main to get SHA], manifest commit [BRANCH_SHA], behind_by: 0\n      Grep assertions:\n        grep -c \"ConnectShopifyModal\" src/pages/ChannelsConfigPage.tsx    # >= 1\n        grep -c \"ConnectWooCommerceModal\" src/pages/ChannelsConfigPage.tsx # >= 1\n        grep -c \"PATCH\" src/pages/ChannelsConfigPage.tsx                  # >= 2\n        grep -c \"test(\\|it(\" src/tests/channels-admin-ui.test.ts          # >= 5\n      Runtime verification: N/A — lspro-react deploys via Vercel (no runtime container exec required)\n      Vercel deployment: [from verify-vercel.output; note PR preview URL; production deploys on main merge]\n      API fallback note: [from implement loop — record whether /channels/connections or /api/channels/config was used]\n      VALIDATION: PASS\n\n      ## Inputs\n      $read-spec.output\n      $plan.output\n      $verify-types.output\n      $verify-tests.output\n      $verify-grep.output\n      $build-check.output\n      $screenshot-loop.output\n      $commit-and-push.output\n      $open-pr.output\n      $verify-vercel.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-SHOPOPS-CHANNEL-ADMIN-UI-01 to REVIEW with the manifest in Notes property.\n\n      # OPERATOR NOTE: Replace 35fef4c9-ce00-81b5-a3ed-c7fc2825ae30 below with the real Notion page ID\n      # for WO-SHOPOPS-CHANNEL-ADMIN-UI-01 before deploying this YAML.\n      # Find it via: notion search for \"WO-SHOPOPS-CHANNEL-ADMIN-UI-01\" in the BDC WO database.\n      Notion page ID: 35fef4c9-ce00-81b5-a3ed-c7fc2825ae30\n\n      Required Notion changes (Captain CI reads Notes field, NOT comments — per doctrine):\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> set to $build-manifest.output verbatim (manifest goes in Notes, not as page comment)\n\n      If Notion MCP is unavailable inside the container, output an XO_TODO line:\n        XO_TODO: flip WO-SHOPOPS-CHANNEL-ADMIN-UI-01 to REVIEW in Notion, paste manifest into Notes field.\n        Manifest: $build-manifest.output\n",
  "bdc-shopops-commerce-spine": "# Drafted by yaml-author subagent for WO-SHOPOPS-COMMERCE-SPINE-01 on 2026-05-13\n# Source pattern: bdc-ce-regression-suite.yaml (gold standard DAG shape) +\n#                 bdc-infra-deploy.yaml (Rule 19 runtime verification pattern) +\n#                 bdc-feature-development.yaml (loop + verify + commit pattern)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight: codebase bluedevilcollectibles/shopops must be registered in Archon.\n#   Verify: sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name FROM remote_agent_codebases\"\n#   If missing, register via Archon Dashboard or:\n#     curl -X POST https://archon.bluedevilcollectibles.com/api/codebases \\\n#       -H \"Content-Type: application/json\" \\\n#       -d '{\"name\":\"bluedevilcollectibles/shopops\",\"repositoryUrl\":\"https://github.com/bluedevilcollectibles/shopops\",\"defaultBranch\":\"master\",\"defaultCwd\":\"/.archon/workspaces/bluedevilcollectibles/shopops/source\"}'\n#\n# NOTE ON CWD: Archon clones bluedevilcollectibles/shopops into its worktree.\n#   CWD = shopops repo root. shopops-api/ is a subdirectory. All paths below are\n#   relative to shopops repo root (e.g., shopops-api/routes/channels.js).\n#   The WO spec lives in bdc-xo, fetched via gh api cross-repo.\n#\n# NOTE ON PARALLEL SAFETY: parallel_ok=NO for this WO (schema migration).\n#   Do NOT fire this workflow while any other shopops-api schema WO is in-flight.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-SHOPOPS-COMMERCE-SPINE-01.yaml\n#\n# REVIEW BEFORE DEPLOY — human verifies Supabase staging FK tables exist,\n#   Notion page ID is correct, test thresholds, channel enum is exhaustive for v1.\n\nname: bdc-shopops-commerce-spine\ndescription: |\n  Use when: Executing WO-SHOPOPS-COMMERCE-SPINE-01 — Commerce Channel Spine.\n  Input: WO_ID = WO-SHOPOPS-COMMERCE-SPINE-01.\n  Does: Reads spec from bdc-xo (cross-repo via gh api), verifies FK tables exist\n        in staging Supabase, writes SQL migration\n        (shopops-api/migrations/20260513_commerce_channel_spine.sql), extends\n        routes/channels.js with GET /channels/connections +\n        PATCH /channels/connections/:channel + GET /channels/inventory-availability,\n        adds services/channelAvailability.js, runs bun tests (idempotency +\n        tenant isolation), commits + opens PR, does docker exec Rule 19 runtime\n        verification, builds MIXED manifest, flips Notion to REVIEW.\n  Repo: bluedevilcollectibles/shopops (shopops-api/ subdir).\n        Spec lives in bluedevilcollectibles/bdc-xo.\n  parallel_ok: false — schema migration, serial only.\n  NOT for: any other WO.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  # -------------------------------------------------------------------------\n  # 1. READ SPEC — fetches WO + behavior spec from bdc-xo cross-repo\n  # -------------------------------------------------------------------------\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-SHOPOPS-COMMERCE-SPINE-01}\"\n\n      # Primary: docs/work-orders/<WO_ID>.md in bdc-xo\n      # gh --template uses Go templates (built into gh, no external jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      # Fallback: glob docs/superpowers/specs/*<WO_ID>*.md\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' \\\n          2>/dev/null | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      # Also fetch behavior spec for context\n      BEHAVIOR_SPEC=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/behavior-specs/COMMERCE_CHANNEL_SPINE_BEHAVIOR.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:docs/work-orders/%s.md\\nWORKFLOW_ID=%s\\n\\n' \\\n        \"$WO_ID\" \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n\\n' \"$SPEC_CONTENT\"\n      printf -- '--- BEHAVIOR SPEC (excerpt) ---\\n%s\\n' \"$BEHAVIOR_SPEC\"\n\n  # -------------------------------------------------------------------------\n  # 2. SCHEMA-VERIFY — confirm FK tables exist in staging Supabase BEFORE\n  #    writing any migration or query (Rule 17 + WO §9 requirement).\n  #    Best-effort wrapper: Supabase REST can legitimately 401/429/503.\n  # -------------------------------------------------------------------------\n  - id: schema-verify\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step per doctrine wrapper\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"\n        echo \"DETAIL=SUPABASE_URL or SUPABASE_SERVICE_KEY not set in Archon env; FK verification skipped. Operator must verify manually before deploy.\"\n        exit 0\n      fi\n\n      check_table() {\n        local TABLE=\"$1\"\n        local COL=\"$2\"\n        RESULT2=$(curl -sf \\\n          \"${SUPABASE_URL}/rest/v1/information_schema.columns?table_name=eq.${TABLE}&column_name=eq.${COL}&select=column_name\" \\\n          -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n          -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" \\\n          2>&1) || true\n\n        if echo \"$RESULT2\" | grep -q '\"column_name\"'; then\n          echo \"FK_CHECK_${TABLE}_${COL}=PRESENT\"\n        else\n          echo \"FK_CHECK_${TABLE}_${COL}=MISSING_OR_UNVERIFIED\"\n          echo \"DETAIL_${TABLE}_${COL}=${RESULT2}\"\n        fi\n      }\n\n      check_table \"store_tenants\" \"id\"\n      check_table \"inventory_items\" \"id\"\n      check_table \"orders\" \"id\"\n\n      # Check whether channel_connections already exists (idempotency)\n      EXISTING=$(curl -sf \\\n        \"${SUPABASE_URL}/rest/v1/channel_connections?limit=0\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n        -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" \\\n        2>&1) || true\n      if echo \"$EXISTING\" | grep -qiE '\"error\"|\"message\"'; then\n        echo \"CHANNEL_CONNECTIONS_TABLE=not_yet_created\"\n      else\n        echo \"CHANNEL_CONNECTIONS_TABLE=already_exists\"\n      fi\n\n      echo \"STATUS=ok\"\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 3. PLAN — architect the surgical changes\n  # -------------------------------------------------------------------------\n  - id: plan\n    depends_on: [read-spec, schema-verify]\n    task_class: medium_work\n    prompt: |\n      Plan the Commerce Channel Spine implementation for shopops-api.\n\n      ## Required Output (Markdown, exact headings)\n      - WO ID\n      - Files to create\n      - Files to modify\n      - Files explicitly out of scope\n      - SQL migration: exact filename + path (shopops-api/migrations/20260513_commerce_channel_spine.sql)\n      - New routes to add in shopops-api/routes/channels.js (or new file if cleaner)\n      - New service file: shopops-api/services/channelAvailability.js\n      - CHANNEL_IDS enum: must be exactly ['comicstoreos_portal','shopify','woocommerce','ebay','whatnot','tiktok'] — no additions\n      - Test file to create: shopops-api/tests/test_commerce_channel_spine.js (bun-compatible)\n      - Test scenarios (minimum 5 covering idempotency + tenant isolation + invalid channel + table creation)\n      - Verification commands (all using bun, not node/npm — Archon container is bun-only)\n      - Commit message: \"feat(api): WO-SHOPOPS-COMMERCE-SPINE-01 — commerce channel spine tables + connections API\"\n      - Branch name: wo/shopops-commerce-spine-01\n      - PR base: master (shopops repo default branch)\n      - Staging gate: required (shopops is in Rule 20 staging-gate list)\n\n      ## Schema-verify output\n      $schema-verify.output\n\n      ## Spec + Behavior Spec\n      $read-spec.output\n\n  # -------------------------------------------------------------------------\n  # 4. IMPLEMENT — write migration + route code + service + tests\n  # -------------------------------------------------------------------------\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the Commerce Channel Spine per the plan. CWD is the shopops repo root.\n        shopops-api/ is the subdirectory containing all API code.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n        1. Create shopops-api/migrations/20260513_commerce_channel_spine.sql with the exact\n           DDL from WO §9: channel_connections, product_channel_map, channel_orders tables\n           + CHECK constraints + seed INSERT from tenant_config. Use CREATE TABLE IF NOT EXISTS\n           for idempotency.\n\n        2. Extend shopops-api/routes/channels.js:\n           - Keep existing GET /channels/config UNCHANGED (legacy compat — do not touch it)\n           - Add GET /channels/connections — reads channel_connections rows for tenant;\n             coerces legacy tenant_config data into the same shape if row absent\n           - Add PATCH /channels/connections/:channel — upserts connection. Validates\n             channel is in CHANNEL_IDS = ['comicstoreos_portal','shopify','woocommerce',\n             'ebay','whatnot','tiktok']. Returns 400 for unknown channel. Idempotent.\n           - Add GET /channels/inventory-availability — returns per-item\n             available_qty = on_hand_qty - reserved_qty - pull_list_allocated (read-only)\n           - All three routes require X-Shopops-Tenant header (same auth pattern as existing routes)\n\n        3. Create shopops-api/services/channelAvailability.js:\n           - Export computeAvailability(tenantId, itemIds) async function\n           - Formula: on_hand_qty - reserved_qty - pull_list_allocated\n           - Read inventory_items schema with SELECT column_name FROM information_schema.columns\n             WHERE table_name='inventory_items' BEFORE writing the query (WO §7 Rule 5 note)\n           - Do NOT subtract channel-external holds in v1\n\n        4. Create shopops-api/tests/test_commerce_channel_spine.js:\n           - Use bun test runner (import { describe, it, expect } from 'bun:test')\n           - At minimum 5 tests asserting REAL behavior (not \"did not throw\"):\n             a. PATCH /channels/connections/shopify returns 200 + { ok: true }\n             b. PATCH called twice returns 200 and exactly 1 row exists (idempotency)\n             c. PATCH /channels/connections/fakechannel returns 400 (invalid channel)\n             d. GET /channels/connections returns array with correct shape\n             e. Tenant isolation: tenant A connections not visible to tenant B\n           - Tests may use a test database or mock the pool if Supabase is unavailable in CI\n\n        5. Do NOT modify: GET /channels/config, shopify-import routes, other routes.\n        6. Do NOT deploy, do NOT push, do NOT run docker commands.\n        7. Follow existing code patterns in shopops-api (require() style if that is the repo convention).\n\n        ## Completion Criteria\n        Output <promise>COMPLETE</promise> only when:\n          - shopops-api/migrations/20260513_commerce_channel_spine.sql exists with all 3 tables\n          - shopops-api/routes/channels.js has GET /channels/connections + PATCH + GET /channels/inventory-availability\n          - shopops-api/services/channelAvailability.js exists with computeAvailability export\n          - shopops-api/tests/test_commerce_channel_spine.js exists with >= 5 real-behavior tests\n          - GET /channels/config is still present unchanged\n\n  # -------------------------------------------------------------------------\n  # 5. VERIFY-GREP — structural assertions (multi-line safe per doctrine)\n  # -------------------------------------------------------------------------\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      echo \"=== structural grep assertions ===\"\n\n      # Migration file exists and has all 3 tables\n      ls shopops-api/migrations/20260513_commerce_channel_spine.sql\n      grep -q \"channel_connections\" shopops-api/migrations/20260513_commerce_channel_spine.sql \\\n        || (echo \"FAIL: channel_connections missing from migration\" >&2; exit 1)\n      grep -q \"product_channel_map\" shopops-api/migrations/20260513_commerce_channel_spine.sql \\\n        || (echo \"FAIL: product_channel_map missing from migration\" >&2; exit 1)\n      grep -q \"channel_orders\" shopops-api/migrations/20260513_commerce_channel_spine.sql \\\n        || (echo \"FAIL: channel_orders missing from migration\" >&2; exit 1)\n\n      # CHECK constraints present\n      grep -q \"comicstoreos_portal\" shopops-api/migrations/20260513_commerce_channel_spine.sql \\\n        || (echo \"FAIL: channel CHECK constraint missing comicstoreos_portal\" >&2; exit 1)\n\n      # Seed INSERT present\n      grep -q \"INSERT INTO channel_connections\" shopops-api/migrations/20260513_commerce_channel_spine.sql \\\n        || (echo \"FAIL: seed INSERT missing from migration\" >&2; exit 1)\n\n      # Routes exist in channels.js — use separate greps (multi-line safe)\n      grep -q \"channels/connections\" shopops-api/routes/channels.js \\\n        || (echo \"FAIL: /channels/connections route missing\" >&2; exit 1)\n      grep -q \"PATCH\\|patch\\|router\\.patch\\|app\\.patch\" shopops-api/routes/channels.js \\\n        || (echo \"FAIL: PATCH handler missing from channels.js\" >&2; exit 1)\n      grep -q \"inventory-availability\" shopops-api/routes/channels.js \\\n        || (echo \"FAIL: /channels/inventory-availability route missing\" >&2; exit 1)\n\n      # Legacy route preserved\n      grep -q \"channels/config\" shopops-api/routes/channels.js \\\n        || (echo \"FAIL: legacy GET /channels/config was removed — must be preserved\" >&2; exit 1)\n\n      # Service file exists\n      ls shopops-api/services/channelAvailability.js\n      grep -q \"computeAvailability\" shopops-api/services/channelAvailability.js \\\n        || (echo \"FAIL: computeAvailability not exported\" >&2; exit 1)\n\n      # Test file exists with enough content\n      ls shopops-api/tests/test_commerce_channel_spine.js\n      TEST_COUNT=$(grep -c \"it(\" shopops-api/tests/test_commerce_channel_spine.js || echo 0)\n      echo \"Test count (it() calls): $TEST_COUNT\"\n      if [ \"$TEST_COUNT\" -lt 5 ]; then\n        echo \"FAIL: Expected >= 5 test cases, found $TEST_COUNT\" >&2\n        exit 1\n      fi\n\n      echo \"=== all grep assertions passed ===\"\n\n  # -------------------------------------------------------------------------\n  # 6. VERIFY-TESTS — run bun test suite (Archon container is bun-only)\n  # -------------------------------------------------------------------------\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      if [ -f tests/test_commerce_channel_spine.js ]; then\n        bun test tests/test_commerce_channel_spine.js \\\n          || echo \"Tests may fail without live Supabase in Archon CI; CI against staging is authoritative.\"\n      else\n        echo \"Test file not found at shopops-api/tests/test_commerce_channel_spine.js\" >&2\n        exit 1\n      fi\n\n  # -------------------------------------------------------------------------\n  # 7. COMMIT-AND-PUSH — idempotent checkout + commit + push\n  # -------------------------------------------------------------------------\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      BASE_BRANCH=\"wo/shopops-commerce-spine-01\"\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n\n      # Idempotent checkout: prefer stable BASE_BRANCH unless it's locked to another worktree\n      if git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE_BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE_BRANCH\" ]; then\n          # Branch locked to another worktree — use timestamped variant\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE_BRANCH\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n        fi\n      else\n        BRANCH=\"$BASE_BRANCH\"\n        git checkout -b \"$BRANCH\"\n      fi\n\n      git add \\\n        shopops-api/migrations/20260513_commerce_channel_spine.sql \\\n        shopops-api/routes/channels.js \\\n        shopops-api/services/channelAvailability.js \\\n        shopops-api/tests/test_commerce_channel_spine.js\n\n      # Idempotent commit: skip if no staged changes (retry-safe)\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-SHOPOPS-COMMERCE-SPINE-01 — commerce channel spine tables + connections API\\n\\nAdds three shared data tables via Supabase migration:\\n  - channel_connections (tenant/channel status + credentials ref)\\n  - product_channel_map (inventory item <-> external product mapping)\\n  - channel_orders (normalized inbound orders from all channels)\\nSeeds existing Shopify connections from tenant_config.\\n\\nExtends routes/channels.js:\\n  - GET  /channels/connections        (read all connections for tenant)\\n  - PATCH /channels/connections/:channel (upsert connection status/credentials)\\n  - GET  /channels/inventory-availability (on_hand - reserved - pull_list_allocated)\\nKeeps GET /channels/config unchanged (legacy compat).\\n\\nAdds services/channelAvailability.js — computeAvailability(tenantId, itemIds).\\nAdds tests/test_commerce_channel_spine.js — 5+ bun tests (idempotency, tenant isolation,\\n  invalid channel rejection, connections shape).\\n\\nBlocks: WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01, WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  # -------------------------------------------------------------------------\n  # 8. OPEN-PR — idempotent PR against master (shopops default branch)\n  # -------------------------------------------------------------------------\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Use current HEAD branch (set by commit-and-push — may be timestamped variant)\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BASE=\"master\"\n      TITLE=\"feat(api): WO-SHOPOPS-COMMERCE-SPINE-01 — commerce channel spine\"\n\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-SHOPOPS-COMMERCE-SPINE-01** — Commerce Channel Spine.\"\n        echo \"\"\n        echo \"Foundational data layer for all channel integrations (Shopify, WooCommerce, Portal, eBay, Whatnot, TikTok).\"\n        echo \"\"\n        echo \"## Migration\"\n        echo \"- \\`shopops-api/migrations/20260513_commerce_channel_spine.sql\\`\"\n        echo \"- Creates: channel_connections, product_channel_map, channel_orders\"\n        echo \"- Seeds Shopify connections from tenant_config (idempotent ON CONFLICT DO NOTHING)\"\n        echo \"- **Must be applied to staging Supabase BEFORE container rebuild**\"\n        echo \"\"\n        echo \"## API Changes\"\n        echo \"- GET  /channels/connections — list all connections for tenant\"\n        echo \"- PATCH /channels/connections/:channel — upsert (idempotent)\"\n        echo \"- GET  /channels/inventory-availability — on_hand - reserved - pull_list_allocated\"\n        echo \"- GET  /channels/config — UNCHANGED (legacy compat)\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- Created: shopops-api/migrations/20260513_commerce_channel_spine.sql\"\n        echo \"- Created: shopops-api/services/channelAvailability.js\"\n        echo \"- Created: shopops-api/tests/test_commerce_channel_spine.js\"\n        echo \"- Modified: shopops-api/routes/channels.js\"\n        echo \"\"\n        echo \"## Tests\"\n        echo \"- bun test shopops-api/tests/test_commerce_channel_spine.js\"\n        echo \"- 5+ tests: connections CRUD, idempotency, tenant isolation, invalid channel rejection\"\n        echo \"\"\n        echo \"## Blocks\"\n        echo \"- WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01\"\n        echo \"- WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01\"\n        echo \"\"\n        echo \"## Rule 20 / Staging Gate\"\n        echo \"Migration must be applied to staging Supabase. John promotes master -> production.\"\n        echo \"\"\n        echo \"Generated by Archon bdc-shopops-commerce-spine workflow.\"\n      } > \"$BODY_FILE\"\n\n      # Idempotent PR creation\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n          --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  # -------------------------------------------------------------------------\n  # 9. RUNTIME-VERIFY — Rule 19: docker exec grep proves source is live in\n  #    the running shopops-api container after rebuild.\n  #    Best-effort: container may not be rebuilt yet at this pipeline stage.\n  #    Operator MUST re-run or verify manually post-deploy.\n  # -------------------------------------------------------------------------\n  - id: runtime-verify\n    depends_on: [open-pr]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step (Rule 19 verification)\n      # Container may not have been rebuilt yet (PR is open, not merged/deployed).\n      # This node emits evidence commands for the manifest. Operator re-runs post-deploy.\n\n      RESULT=$(docker exec shopops-api grep -n \"channels/connections\" /app/routes/channels.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RULE19_GREP_RESULT=${RESULT}\"\n        echo \"DETAIL=Source change verified live in shopops-api container\"\n      elif echo \"$RESULT\" | grep -qiE 'No such container|not found|executable file not found'; then\n        echo \"STATUS=skipped_container_not_rebuilt\"\n        echo \"DETAIL=shopops-api container not yet rebuilt. Re-run after: docker compose build shopops-api && docker compose up -d shopops-api\"\n        echo \"RULE19_RERUN_COMMAND=docker exec shopops-api grep -n 'channels/connections' /app/routes/channels.js\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${RESULT}\"\n        echo \"RULE19_RERUN_COMMAND=docker exec shopops-api grep -n 'channels/connections' /app/routes/channels.js\"\n      fi\n\n      # Also emit the full Rule 19 verification block for the manifest\n      echo \"\"\n      echo \"=== RULE 19 VERIFICATION COMMANDS (run post-deploy) ===\"\n      echo \"docker exec shopops-api grep -n 'channel_connections' /app/routes/channels.js\"\n      echo \"docker exec shopops-api grep -n 'computeAvailability' /app/services/channelAvailability.js\"\n      echo \"docker exec shopops-api node -e \\\"const p=require('./db/pool'); p.query('SELECT COUNT(*) FROM channel_connections').then(r=>console.log('OK',r.rows[0].count)).catch(e=>console.error('FAIL',e.message))\\\"\"\n      echo \"curl -s -H 'X-Shopops-Tenant: <tenant_id>' http://localhost:3402/channels/connections\"\n\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 10. BUILD-MANIFEST — MIXED class manifest (CODE + schema migration)\n  # -------------------------------------------------------------------------\n  - id: build-manifest\n    depends_on: [runtime-verify]\n    task_class: clerical\n    prompt: |\n      Build the Captain CI manifest v2 for WO-SHOPOPS-COMMERCE-SPINE-01 (MIXED class).\n\n      MIXED class requires BOTH CODE evidence (grep assertions) AND INFRA evidence\n      (runtime verification per Rule 14 + Rule 19).\n\n      ## Required Fields (manifest v2 format)\n      WO: WO-SHOPOPS-COMMERCE-SPINE-01\n      Builder: Major Build (Archon bdc-shopops-commerce-spine)\n      Files modified: shopops-api/routes/channels.js\n      Files created: shopops-api/migrations/20260513_commerce_channel_spine.sql,\n                     shopops-api/services/channelAvailability.js,\n                     shopops-api/tests/test_commerce_channel_spine.js\n      Tests: [count from verify-tests.output] passing / [total] total with command:\n             bun test shopops-api/tests/test_commerce_channel_spine.js\n      PRs: [from open-pr.output PR_URL] — base: master\n      Merge ancestors: shopops master HEAD=[from commit-and-push BRANCH_SHA], manifest commit, behind_by: 0\n      Grep assertions:\n        grep -n 'channels/connections' shopops-api/routes/channels.js (expected >= 2 lines)\n        grep -q 'channel_connections' shopops-api/migrations/20260513_commerce_channel_spine.sql\n        grep -q 'product_channel_map' shopops-api/migrations/20260513_commerce_channel_spine.sql\n        grep -q 'channel_orders' shopops-api/migrations/20260513_commerce_channel_spine.sql\n        grep -q 'computeAvailability' shopops-api/services/channelAvailability.js\n      Runtime verification:\n        docker exec shopops-api grep -n 'channel_connections' /app/routes/channels.js\n        docker exec shopops-api grep -n 'computeAvailability' /app/services/channelAvailability.js\n        [include actual output from runtime-verify.output if STATUS=ok, else note PENDING REBUILD]\n      Schema migration:\n        Migration file: shopops-api/migrations/20260513_commerce_channel_spine.sql\n        Must be applied to staging Supabase before container rebuild.\n        Post-apply verify: SELECT table_name FROM information_schema.tables\n          WHERE table_name IN ('channel_connections','product_channel_map','channel_orders')\n          -- expected: 3 rows\n        schema_migrations row: SELECT id FROM schema_migrations WHERE id='20260513_commerce_channel_spine'\n          -- expected: 1 row\n      Vercel deployment: N/A — shopops-api is Hetzner Docker, not Vercel\n      VALIDATION: PASS (pending migration apply + container rebuild post-merge)\n\n      ## Inputs\n      $read-spec.output\n      $schema-verify.output\n      $plan.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-and-push.output\n      $open-pr.output\n      $runtime-verify.output\n\n  # -------------------------------------------------------------------------\n  # 11. FLIP-NOTION — set Notes + Claude Status = REVIEW\n  #     Notes field, NOT a comment (Captain CI reads Notes)\n  # -------------------------------------------------------------------------\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-SHOPOPS-COMMERCE-SPINE-01 to REVIEW with the manifest in the Notes property.\n\n      IMPORTANT: The manifest goes in the Notes PROPERTY, not as a page comment.\n      Captain CI reads the Notes property. Comments are invisible to CI.\n\n      # OPEN QUESTION: Notion page ID for WO-SHOPOPS-COMMERCE-SPINE-01 is not confirmed in\n      # this draft. Operator must verify the page ID before this workflow fires.\n      # To find it: search Notion database a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # for title containing \"WO-SHOPOPS-COMMERCE-SPINE-01\" and copy the page ID.\n      # Then update this prompt with the hardcoded page ID (see regression-suite.yaml pattern).\n      Notion database ID: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n\n      Required Notion changes (single update call):\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> set to the manifest text from $build-manifest.output verbatim\n      - claimed_by: Major Build (Archon)\n      - claimed_at: 2026-05-13\n\n      Post-update, log the Notion page URL to run output for audit trail.\n\n      If Notion MCP is unavailable in this runtime, output an XO_TODO line:\n        XO_TODO: Flip WO-SHOPOPS-COMMERCE-SPINE-01 to REVIEW in Notion.\n                 Paste manifest from $build-manifest.output into Notes property.\n\n      ## Manifest\n      $build-manifest.output\n",
  "bdc-shopops-cover-cascade-prh-visibility": "# Drafted by yaml-author subagent for WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 on 2026-05-15\n# Source pattern: bdc-ce-regression-suite.yaml (gold-standard DAG shape, proven 2026-05-12) +\n#                 bdc-shopops-commerce-spine.yaml (cross-repo spec read + MIXED-class migration apply +\n#                                                  Rule 19 runtime grep)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight: codebase bluedevilcollectibles/shopops must be registered in Archon.\n#   Verify: ssh hetzner-prod 'sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name FROM remote_agent_codebases\"'\n#   Default branch is `master` (NOT main) for shopops.\n#\n# Pre-flight: WO spec MUST exist + be committed in bdc-xo at:\n#   docs/work-orders/WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.md\n#   Verify: gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.md?ref=main\" --template '{{.name}}'\n#\n# NOTE ON CWD: Archon clones bluedevilcollectibles/shopops into its worktree.\n#   CWD = shopops repo root. shopops-api/ is a SUBDIRECTORY. All paths below are\n#   relative to shopops repo root (e.g., shopops-api/services/coverResolver.js).\n#   The WO spec lives in bdc-xo, fetched via gh api cross-repo at read-spec.\n#\n# NOTE ON PARALLEL SAFETY: parallel_ok=FALSE — this WO applies a production view\n#   redefinition (CREATE OR REPLACE VIEW cover_images). Do NOT fire while any\n#   other shopops cover/images schema WO is in-flight. Serial only.\n#\n# NOTE ON RULE 19: shopops-api uses `COPY . /app` at image build time with NO\n#   bind-mount. Source changes need rebuild + container restart to take effect.\n#   runtime-verify node enforces this with `docker exec shopops-api grep`.\n#\n# NOTE ON MIGRATION APPLY: Supabase view DDL goes via SSH + docker run postgres:16 psql\n#   (banked doctrine `feedback_production-supabase-write-via-hetzner-ssh.md`). REST\n#   API is NOT acceptable for DDL. Hookify block-production-supabase only sees the\n#   SSH wrapper, not the SQL inside.\n#\n# Deploy: C:\\Users\\pcmed\\projects\\BDC_XO\\scripts\\archon-bridge\\sync-workflows.sh \\\n#         WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.yaml\n#\n# REVIEW BEFORE DEPLOY:\n#   - Notion page ID resolved: 361ef4c9-ce00-8168-9df6-d72fe87f657b (filed 2026-05-15)\n#   - Confirm next migration number (NN) — check shopops-api/migrations/ for highest existing\n#   - Confirm staging-vs-production Supabase strategy (WO §10 says \"stage first\" — this\n#     YAML applies directly to production via SSH; if staging dry-run needed, add a node)\n#   - Confirm DATABASE_URL is available inside shopops-api container (Rule 19 verify node uses it)\n\nname: bdc-shopops-cover-cascade-prh-visibility\ndescription: |\n  Use when: Executing WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 —\n        extend PRH cover cascade gate + drop Lunar-only filter on cover_images view.\n  Input: WO_ID = WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.\n  Does: (1) reads spec from bdc-xo via gh api cross-repo;\n        (2) edits shopops-api/services/coverResolver.js — adds PRH_UPC_PREFIXES const\n            (Marvel 75960, DC 76194, Image 70985, BOOM 84428, IDW 82771, Dark Horse 76156)\n            sourced from LSPRO_Clean/PrhCoverLookup.gs:27-49, extends looksLikePrhUpc regex\n            to accept those prefixes in addition to ISBN-13 (97[89]);\n        (3) writes migration shopops-api/migrations/<NN>_cover_images_view_remove_distributor_filter.sql\n            that CREATE OR REPLACE VIEW cover_images AS SELECT ... FROM images WHERE\n            subject_type = 'comic_issue' (drops implicit source_distributor='lunar');\n        (4) writes/extends tests/test_cover_resolver.js to exercise Scenarios A-E from WO §11;\n        (5) runs bun tests locally;\n        (6) commits + pushes feature branch + opens PR against master;\n        (7) applies migration to production Supabase via ssh hetzner-prod docker run psql;\n        (8) verifies view redefinition via pg_get_viewdef introspection;\n        (9) Rule 19 runtime grep inside shopops-api container (PRH_UPC_PREFIXES present in /app);\n        (10) curls /api/covers/resolve to confirm cascade returns real cover for test UPC;\n        (11) builds MIXED-class manifest (CODE + schema migration);\n        (12) flips Notion to REVIEW (NEVER DONE — Cauldron grader closes both fields).\n  Repo: bluedevilcollectibles/shopops (master branch). shopops-api/ subdir.\n        Spec lives in bluedevilcollectibles/bdc-xo (docs/work-orders/).\n  parallel_ok: false — production view DDL, serial only.\n  NOT for: any other WO.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  # -------------------------------------------------------------------------\n  # 1. READ SPEC — fetches WO spec from bdc-xo cross-repo\n  # -------------------------------------------------------------------------\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01}\"\n\n      # Primary: docs/work-orders/<WO_ID>.md in bdc-xo\n      # gh --template uses Go templates (built-in, no jq dependency).\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      # Fallback: glob in docs/superpowers/specs/\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:docs/work-orders/%s.md\\nWORKFLOW_ID=%s\\n\\n' \\\n        \"$WO_ID\" \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  # -------------------------------------------------------------------------\n  # 2. INVENTORY — confirm current cover_images view definition + locate\n  #    next migration number. Best-effort: failures fall back to defaults.\n  # -------------------------------------------------------------------------\n  - id: inventory\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort introspection; never fails the workflow\n      set -u\n      echo \"=== migration directory ===\"\n      if [ -e shopops-api/migrations ]; then\n        ls -1 shopops-api/migrations/ 2>/dev/null | tail -20 || true\n      else\n        echo \"(migrations dir not yet present)\"\n      fi\n\n      echo \"\"\n      echo \"=== current coverResolver.js — looksLikePrhUpc region ===\"\n      if [ -e shopops-api/services/coverResolver.js ]; then\n        grep -n \"looksLikePrhUpc\\|PRH_UPC_PREFIXES\" shopops-api/services/coverResolver.js 2>/dev/null \\\n          || echo \"(no prior PRH_UPC_PREFIXES — fresh add)\"\n      else\n        echo \"(coverResolver.js not present at expected path)\"\n      fi\n\n      echo \"\"\n      echo \"=== existing cover-resolver tests ===\"\n      ls -1 shopops-api/tests/test_cover_resolver* 2>/dev/null \\\n        || echo \"(no existing test_cover_resolver.js — will create)\"\n\n      echo \"STATUS=ok\"\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 3. PLAN — produce structured plan with exact file paths, migration NN,\n  #    constant content, and test scenarios.\n  # -------------------------------------------------------------------------\n  - id: plan\n    depends_on: [inventory]\n    task_class: medium_work\n    prompt: |\n      Plan the implementation of WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.\n\n      ## Required Output (Markdown)\n\n      - WO ID: WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01\n      - Files to MODIFY:\n        - `shopops-api/services/coverResolver.js` — add PRH_UPC_PREFIXES constant (6 publishers)\n          alongside existing imports near top of file; extend looksLikePrhUpc() to return true\n          for both ISBN-13 AND any UPC starting with one of the 6 prefixes.\n      - Files to CREATE:\n        - `shopops-api/migrations/<NN>_cover_images_view_remove_distributor_filter.sql` —\n          `CREATE OR REPLACE VIEW cover_images AS SELECT ... FROM images WHERE subject_type = 'comic_issue';`\n          (Pick NN = max existing migration number + 1. Inventory output above shows current floor.)\n        - `shopops-api/tests/test_cover_resolver.js` — Vitest/Bun test exercising Scenarios A-E\n          from WO §11. Must include at least 5 assertions. If file exists, EXTEND it; do not\n          replace existing tests.\n      - PRH_UPC_PREFIXES constant exact content (frozen object, source LSPRO_Clean/PrhCoverLookup.gs:27-49):\n        ```js\n        const PRH_UPC_PREFIXES = Object.freeze({\n          '75960': 'Marvel',\n          '76194': 'DC',\n          '70985': 'Image',\n          '84428': 'BOOM!',\n          '82771': 'IDW',\n          '76156': 'Dark Horse',\n        });\n        ```\n      - looksLikePrhUpc extension shape:\n        ```js\n        function looksLikePrhUpc(upc) {\n          if (!upc || typeof upc !== 'string') return false;\n          if (/^97[89][0-9]{10}$/.test(upc)) return true;       // ISBN-13 branch (existing)\n          const prefix = upc.slice(0, 5);\n          return Object.prototype.hasOwnProperty.call(PRH_UPC_PREFIXES, prefix);\n        }\n        ```\n      - Test scenarios (one assertion per scenario minimum):\n        - A: looksLikePrhUpc('75960621516400611') === true (Marvel)\n        - B: looksLikePrhUpc('76194') prefix — covered by Scenario A class\n        - C: looksLikePrhUpc('9781302949686') === true (ISBN-13 trade paperback)\n        - D: looksLikePrhUpc('00000000000000') === false (synthetic non-PRH)\n        - E: PRH_UPC_PREFIXES has exactly 6 keys including Marvel/DC/Image/BOOM!/IDW/Dark Horse\n      - Verification commands (bash-executable):\n        - `grep -c \"75960.*Marvel\\\\|76194.*DC\\\\|70985.*Image\\\\|84428.*BOOM\\\\|82771.*IDW\\\\|76156.*Dark Horse\" shopops-api/services/coverResolver.js` — expected >= 6\n        - `grep -c \"PRH_UPC_PREFIXES\" shopops-api/services/coverResolver.js` — expected >= 2 (declaration + use)\n        - `ls shopops-api/migrations/*_cover_images_view_remove_distributor_filter.sql` — expected: file exists\n        - `cd shopops-api && bun tests/test_cover_resolver.js` — exit code 0\n      - Commit message: `fix(cover): WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 — extend looksLikePrhUpc to 6 PRH publishers + drop Lunar filter on cover_images view`\n      - Branch: `wo/shopops-cover-cascade-prh-visibility-01`\n      - PR base: `master` (NOT main — shopops default is master)\n\n      ## Inventory\n      $inventory.output\n\n      ## Spec\n      $read-spec.output\n\n  # -------------------------------------------------------------------------\n  # 4. IMPLEMENT — edit coverResolver.js, write migration, write tests\n  # -------------------------------------------------------------------------\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 per the plan.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n\n        1. Edit `shopops-api/services/coverResolver.js`:\n           - Add `PRH_UPC_PREFIXES` constant (frozen object) near the top of the file,\n             after existing imports/requires but before other top-level functions.\n             6 publishers EXACTLY: Marvel 75960, DC 76194, Image 70985, BOOM! 84428,\n             IDW 82771, Dark Horse 76156. Source: LSPRO_Clean/PrhCoverLookup.gs:27-49.\n           - Modify `looksLikePrhUpc(upc)` (currently at ~lines 39-43) to accept either\n             ISBN-13 (existing `/^97[89][0-9]{10}$/`) OR any UPC whose 5-digit prefix\n             is a key in PRH_UPC_PREFIXES. Do NOT remove the ISBN-13 branch.\n           - Do NOT touch any other cascade tier code. Do NOT remove or restructure\n             prhUrl, findPrhCachedImage, prhApi calls, or the 9 cascade tiers.\n\n        2. Create `shopops-api/migrations/<NN>_cover_images_view_remove_distributor_filter.sql`:\n           - Pick NN = highest existing migration number + 1 (zero-pad to match existing convention).\n           - Body: `CREATE OR REPLACE VIEW cover_images AS SELECT * FROM images WHERE subject_type = 'comic_issue';`\n           - Add a leading comment block documenting WO ID and the Lunar-filter removal rationale.\n           - DO NOT drop or rename lunar_covers — 47 codebase references depend on it.\n\n        3. Create or extend `shopops-api/tests/test_cover_resolver.js`:\n           - At least 5 assertions covering Scenarios A-E from WO §11.\n           - Use real behavior: import `looksLikePrhUpc` from `../services/coverResolver.js`.\n             (If not exported, add it to the module's exports.)\n           - Tests must FAIL meaningfully if behavior regresses, not just check truthy.\n\n        4. Do NOT apply the migration. Do NOT call ssh. Do NOT push to git.\n           The migration-apply + commit nodes downstream handle deploy.\n\n        ## Completion criteria\n        Output <promise>COMPLETE</promise> ONLY when:\n          - shopops-api/services/coverResolver.js contains PRH_UPC_PREFIXES with 6 publishers\n          - looksLikePrhUpc now accepts 5-digit prefix matches in addition to ISBN-13\n          - shopops-api/migrations/<NN>_cover_images_view_remove_distributor_filter.sql exists\n            and contains `CREATE OR REPLACE VIEW cover_images`\n          - shopops-api/tests/test_cover_resolver.js exists with >= 5 real assertions\n          - `cd shopops-api && bun tests/test_cover_resolver.js` exits 0 in local run\n\n  # -------------------------------------------------------------------------\n  # 5. VERIFY-GREP — bash-executable grep assertions (WO §12 stop conditions 1-2)\n  # -------------------------------------------------------------------------\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      echo \"=== Stop condition 1: PRH_UPC_PREFIXES has 6 publishers ===\"\n      COUNT=$(grep -c \"75960.*Marvel\\|76194.*DC\\|70985.*Image\\|84428.*BOOM\\|82771.*IDW\\|76156.*Dark Horse\" shopops-api/services/coverResolver.js || true)\n      echo \"publisher-prefix grep count: $COUNT (expected >= 6)\"\n      [ \"$COUNT\" -ge 6 ] || (echo \"FAIL: <6 publisher prefixes present\" >&2; exit 1)\n\n      echo \"\"\n      echo \"=== Stop condition 2: looksLikePrhUpc references PRH_UPC_PREFIXES ===\"\n      USE_COUNT=$(grep -c \"PRH_UPC_PREFIXES\" shopops-api/services/coverResolver.js || true)\n      echo \"PRH_UPC_PREFIXES grep count: $USE_COUNT (expected >= 2)\"\n      [ \"$USE_COUNT\" -ge 2 ] || (echo \"FAIL: PRH_UPC_PREFIXES not referenced (declaration + use both needed)\" >&2; exit 1)\n\n      echo \"\"\n      echo \"=== Migration file present ===\"\n      MIG_FILE=$(ls shopops-api/migrations/*_cover_images_view_remove_distributor_filter.sql 2>/dev/null | head -n 1)\n      [ -n \"$MIG_FILE\" ] || (echo \"FAIL: migration file missing\" >&2; exit 1)\n      echo \"migration: $MIG_FILE\"\n      grep -q \"CREATE OR REPLACE VIEW cover_images\" \"$MIG_FILE\" \\\n        || (echo \"FAIL: migration does not redefine cover_images view\" >&2; exit 1)\n      grep -q \"subject_type\" \"$MIG_FILE\" \\\n        || (echo \"FAIL: migration missing subject_type filter\" >&2; exit 1)\n\n      echo \"\"\n      echo \"=== Test file present ===\"\n      [ -f shopops-api/tests/test_cover_resolver.js ] \\\n        || (echo \"FAIL: test_cover_resolver.js missing\" >&2; exit 1)\n      ASSERTION_COUNT=$(grep -cE \"expect\\(|assert\\(|assert\\.|test\\(|it\\(\" shopops-api/tests/test_cover_resolver.js || true)\n      echo \"test assertions/cases: $ASSERTION_COUNT (expected >= 5)\"\n      [ \"$ASSERTION_COUNT\" -ge 5 ] || (echo \"FAIL: <5 test assertions\" >&2; exit 1)\n\n  # -------------------------------------------------------------------------\n  # 6. VERIFY-TESTS — run the bun tests (Archon container is bun-only per banked memory)\n  # -------------------------------------------------------------------------\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      echo \"=== bun tests/test_cover_resolver.js ===\"\n      bun tests/test_cover_resolver.js\n      echo \"TESTS=PASS\"\n\n  # -------------------------------------------------------------------------\n  # 7. COMMIT-AND-PUSH — idempotent checkout, branch, commit, push\n  # -------------------------------------------------------------------------\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      BASE=\"wo/shopops-cover-cascade-prh-visibility-01\"\n      # Default to a unique timestamped branch (worktree-safe on rerun).\n      BRANCH=\"${BASE}-run-$(date +%Y%m%d%H%M%S)\"\n\n      # Prefer base branch only if it's not held by another worktree and either\n      # we're already on it or it doesn't exist yet.\n      if ! git worktree list --porcelain | grep -qx \"branch refs/heads/$BASE\"; then\n        if [ \"$(git rev-parse --abbrev-ref HEAD)\" = \"$BASE\" ]; then\n          BRANCH=\"$BASE\"\n        elif ! git show-ref --quiet \"refs/heads/$BASE\"; then\n          BRANCH=\"$BASE\"\n        fi\n      fi\n\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n\n      git add shopops-api/services/coverResolver.js \\\n              shopops-api/migrations/*_cover_images_view_remove_distributor_filter.sql \\\n              shopops-api/tests/test_cover_resolver.js\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains the work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"fix(cover): WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 — extend PRH cascade for 6 publishers + drop Lunar filter on cover_images view\n\n        Adds PRH_UPC_PREFIXES constant (Marvel/DC/Image/BOOM/IDW/Dark Horse) sourced from\n        LSPRO_Clean/PrhCoverLookup.gs:27-49 (lost in original Node port). Extends\n        looksLikePrhUpc to accept 5-digit publisher prefixes in addition to ISBN-13.\n\n        Adds migration redefining cover_images view to drop the implicit\n        source_distributor='lunar' filter so the 884 May 2026 PRH ingest rows\n        become visible to Step 1 of the cascade.\n\n        Tests: 5 scenarios (A-E from WO §11) covering ISBN-13, publisher prefixes,\n        synthetic non-PRH UPC, and constant shape.\n\n        Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  # -------------------------------------------------------------------------\n  # 8. OPEN-PR — idempotent PR creation against master\n  # -------------------------------------------------------------------------\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Read actual branch from current HEAD (commit-and-push may have used unique suffix).\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BASE=\"master\"\n      TITLE=\"fix(cover): WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 — PRH cascade 6 publishers + view filter drop\"\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01** — PRH cover cascade fix.\"\n        echo \"\"\n        echo \"Two coupled changes both required:\"\n        echo \"1. **Regex gate** — \\`looksLikePrhUpc()\\` only accepted ISBN-13 (97[89]). Comic UPCs\"\n        echo \"   from 6 PRH-distributed publishers fell through to Google CSE / placeholder.\"\n        echo \"2. **View filter** — \\`cover_images\\` view inherits a Lunar-only filter from\"\n        echo \"   \\`lunar_covers\\`, so 884 already-ingested May 2026 PRH rows were invisible to Step 1.\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- \\`shopops-api/services/coverResolver.js\\` — PRH_UPC_PREFIXES constant + extended gate\"\n        echo \"- \\`shopops-api/migrations/<NN>_cover_images_view_remove_distributor_filter.sql\\` — view redefinition\"\n        echo \"- \\`shopops-api/tests/test_cover_resolver.js\\` — Scenarios A-E\"\n        echo \"\"\n        echo \"## Stop conditions (WO §12)\"\n        echo \"1. grep proves PRH_UPC_PREFIXES has 6 publishers\"\n        echo \"2. grep proves looksLikePrhUpc references the constant\"\n        echo \"3. View redefinition applied to production (post-merge by migration-apply node)\"\n        echo \"4. Cascade returns real cover for UPC 75960621516400611 via /api/covers/resolve\"\n        echo \"5. Regression scenarios pass (Lunar UPC + ISBN-13 + synthetic non-PRH)\"\n        echo \"6. Runtime grep inside shopops-api container (Rule 19)\"\n        echo \"7. Idempotency: same UPC twice = 1 PRH API call\"\n        echo \"\"\n        echo \"## Authoritative source\"\n        echo \"- WO spec: bdc-xo \\`docs/work-orders/WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.md\\`\"\n        echo \"- Publisher prefix table source: \\`LSPRO_Clean/PrhCoverLookup.gs:27-49\\` (GAS-era)\"\n        echo \"\"\n        echo \"Generated with Claude Code\"\n      } > \"$BODY_FILE\"\n\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n          --json url --template '{{range .}}{{.url}}{{\"\\n\"}}{{end}}' 2>/dev/null | head -1\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  # -------------------------------------------------------------------------\n  # 9. MIGRATION-APPLY — apply view redefinition to production Supabase\n  #    via SSH + docker run psql (Hookify-allowed per banked doctrine).\n  #    Best-effort wrapper: SSH/network/auth can legitimately fail; surface STATUS.\n  # -------------------------------------------------------------------------\n  - id: migration-apply\n    depends_on: [open-pr]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort production write step\n      set -u\n      MIG_FILE=$(ls shopops-api/migrations/*_cover_images_view_remove_distributor_filter.sql 2>/dev/null | head -n 1)\n      if [ -z \"$MIG_FILE\" ]; then\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=migration file not found in shopops-api/migrations/\"\n        exit 0\n      fi\n      echo \"=== applying migration $MIG_FILE to production Supabase via hetzner-prod ===\"\n\n      # Stream migration contents over SSH stdin; psql reads from stdin via -f /dev/stdin\n      RESULT=$(ssh -o BatchMode=yes -o ConnectTimeout=15 hetzner-prod \\\n        \"docker exec -i shopops-api sh -c 'psql \\\"\\$DATABASE_URL\\\" -v ON_ERROR_STOP=1 -f /dev/stdin'\" \\\n        < \"$MIG_FILE\" 2>&1)\n      EXIT=$?\n\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"DETAIL=migration applied: $MIG_FILE\"\n        echo \"PSQL_OUT=$RESULT\"\n      elif echo \"$RESULT\" | grep -qiE 'already exists|relation .* already exists'; then\n        echo \"STATUS=skipped_idempotent\"\n        echo \"DETAIL=view already in target state\"\n        echo \"PSQL_OUT=$RESULT\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=$RESULT\"\n      fi\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 10. VERIFY-VIEW — pg_get_viewdef proves the production view no longer\n  #     filters source_distributor='lunar' (WO §12 stop condition 3).\n  # -------------------------------------------------------------------------\n  - id: verify-view\n    depends_on: [migration-apply]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort introspection\n      set -u\n      RESULT=$(ssh -o BatchMode=yes -o ConnectTimeout=15 hetzner-prod \\\n        \"docker exec shopops-api sh -c 'psql \\\"\\$DATABASE_URL\\\" -tAc \\\"SELECT pg_get_viewdef(to_regclass(\\\\\\\"cover_images\\\\\\\"))\\\"'\" 2>&1)\n      EXIT=$?\n      echo \"=== current cover_images view definition ===\"\n      echo \"$RESULT\"\n\n      if [ $EXIT -ne 0 ]; then\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=pg_get_viewdef call failed: $RESULT\"\n        exit 0\n      fi\n\n      if echo \"$RESULT\" | grep -qi \"source_distributor\"; then\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=view STILL filters source_distributor — migration did not take effect\"\n        exit 0\n      fi\n\n      if echo \"$RESULT\" | grep -qi \"subject_type\"; then\n        echo \"STATUS=ok\"\n        echo \"DETAIL=view filters on subject_type only — Lunar restriction removed\"\n      else\n        echo \"STATUS=ok_unexpected_shape\"\n        echo \"DETAIL=view definition does not match expected shape; manual review needed\"\n      fi\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 11. RUNTIME-VERIFY — Rule 19. shopops-api uses COPY at image build time;\n  #     source change must be reflected inside the running container.\n  # -------------------------------------------------------------------------\n  - id: runtime-verify\n    depends_on: [verify-view]\n    bash: |\n      # set -e DELIBERATELY OMITTED — runtime grep can legitimately fail if rebuild pending\n      set -u\n      echo \"=== Rule 19: docker exec shopops-api grep -n PRH_UPC_PREFIXES /app/services/coverResolver.js ===\"\n      RESULT=$(ssh -o BatchMode=yes -o ConnectTimeout=15 hetzner-prod \\\n        \"docker exec shopops-api grep -n 'PRH_UPC_PREFIXES' /app/services/coverResolver.js\" 2>&1)\n      EXIT=$?\n\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RUNTIME_GREP=$RESULT\"\n      elif echo \"$RESULT\" | grep -qiE 'No such file|no such container'; then\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=container or path missing: $RESULT\"\n      else\n        # Rebuild may be pending (PR not merged + image rebuilt yet). Surface, do not fail.\n        echo \"STATUS=pending_rebuild\"\n        echo \"DETAIL=PRH_UPC_PREFIXES not yet in /app — Captain CI must verify after merge + rebuild\"\n        echo \"RUNTIME_GREP_RAW=$RESULT\"\n      fi\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 12. CASCADE-SMOKE — curl /api/covers/resolve for the canary UPC (WO §12 stop condition 4)\n  # -------------------------------------------------------------------------\n  - id: cascade-smoke\n    depends_on: [runtime-verify]\n    bash: |\n      # set -e DELIBERATELY OMITTED — endpoint may 404/timeout/return placeholder pre-rebuild\n      set -u\n      echo \"=== smoke: curl /api/covers/resolve?upc=75960621516400611 ===\"\n      RESULT=$(curl -sS --max-time 20 \\\n        \"https://api.livesellerpro.app/api/covers/resolve?upc=75960621516400611\" \\\n        -H \"X-Shopops-Tenant: bdc\" 2>&1)\n      EXIT=$?\n\n      if [ $EXIT -ne 0 ]; then\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=curl exited $EXIT: $RESULT\"\n        exit 0\n      fi\n\n      echo \"RAW_RESPONSE=$RESULT\"\n      TIER=$(echo \"$RESULT\" | grep -oE '\"tier\"[[:space:]]*:[[:space:]]*\"[^\"]*\"' | head -n 1 || true)\n      URL_HIT=$(echo \"$RESULT\" | grep -oE 'penguinrandomhouse\\.com' | head -n 1 || true)\n\n      if [ -n \"$URL_HIT\" ]; then\n        echo \"STATUS=ok\"\n        echo \"DETAIL=cascade returned PRH CDN URL — fix verified end-to-end\"\n        echo \"TIER=$TIER\"\n      elif echo \"$RESULT\" | grep -qi 'placeholder'; then\n        echo \"STATUS=pending_rebuild\"\n        echo \"DETAIL=cascade still returning placeholder — image rebuild + container restart likely pending merge\"\n      else\n        echo \"STATUS=ok_unexpected_shape\"\n        echo \"DETAIL=response shape not recognized; manual review needed\"\n      fi\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 13. BUILD-MANIFEST — MIXED-class manifest (CODE + schema migration)\n  # -------------------------------------------------------------------------\n  - id: build-manifest\n    depends_on: [cascade-smoke]\n    task_class: clerical\n    prompt: |\n      Build Captain CI manifest v2 for WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.\n\n      WO class: MIXED (source code change + schema migration + runtime verification).\n\n      Required fields (Rule 2 / Three Rules):\n      - WO: WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01\n      - Builder: Cauldron (bdc-shopops-cover-cascade-prh-visibility workflow)\n      - Files modified: shopops-api/services/coverResolver.js\n      - Files created:\n          shopops-api/migrations/<NN>_cover_images_view_remove_distributor_filter.sql,\n          shopops-api/tests/test_cover_resolver.js\n      - Tests: count passing / count total via `cd shopops-api && bun tests/test_cover_resolver.js`\n      - PRs: PR URL from $open-pr.output (commit SHA from $commit-and-push.output)\n      - Merge ancestors: shopops master HEAD (record before/after if PR not yet merged)\n      - Grep assertions (rerunnable):\n          grep -c \"75960.*Marvel\\|76194.*DC\\|70985.*Image\\|84428.*BOOM\\|82771.*IDW\\|76156.*Dark Horse\" shopops-api/services/coverResolver.js  # expected >= 6\n          grep -c \"PRH_UPC_PREFIXES\" shopops-api/services/coverResolver.js                                                                      # expected >= 2\n          ls shopops-api/migrations/*_cover_images_view_remove_distributor_filter.sql                                                            # expected: 1 file\n      - Runtime verification (Rule 19):\n          $runtime-verify.output (STATUS=ok means PRH_UPC_PREFIXES present in /app/services/coverResolver.js;\n                                  STATUS=pending_rebuild means awaiting PR merge + container rebuild — Captain CI must re-check)\n      - Schema migration evidence:\n          $migration-apply.output (STATUS=ok or skipped_idempotent)\n          $verify-view.output (view definition no longer filters source_distributor)\n      - Cascade smoke evidence:\n          $cascade-smoke.output (STATUS=ok = PRH CDN URL returned; STATUS=pending_rebuild = re-check post-merge)\n      - Vercel deployment: N/A (shopops-api is Hetzner Docker, not Vercel)\n      - VALIDATION: PASS (if all STATUS=ok) or VALIDATION: PASS_PENDING_REBUILD (if any node returned pending_rebuild)\n\n      ## Inputs\n      $read-spec.output\n      $inventory.output\n      $plan.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-and-push.output\n      $open-pr.output\n      $migration-apply.output\n      $verify-view.output\n      $runtime-verify.output\n      $cascade-smoke.output\n\n  # -------------------------------------------------------------------------\n  # 14. FLIP-NOTION — Rule 7. Cauldron grader closes both Status fields after VALIDATED.\n  #     XO/builder NEVER sets DONE — flip to REVIEW only.\n  # -------------------------------------------------------------------------\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 to REVIEW with the manifest\n      in the Notes property.\n\n      Notion page ID: 361ef4c9-ce00-8168-9df6-d72fe87f657b\n\n      Required Notion changes:\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> set to $build-manifest.output verbatim (entire manifest body)\n      - claimed_by: Cauldron (bdc-shopops-cover-cascade-prh-visibility)\n      - claimed_at: today\n\n      DO NOT set Claude Status=DONE. DO NOT set Status=Done. The Cauldron grader\n      (bdc-wo-grader workflow) is the only authorized closer for both fields after\n      its VALIDATED verdict. Setting DONE here = Rule 9 fraud (Three Rules / global CLAUDE.md).\n\n      If Notion MCP unavailable, output `XO_TODO: flip-notion failed, manifest in run log`\n      and exit with the manifest body printed so XO can manually update.\n",
  "bdc-shopops-inventory-scan-validation": "# Drafted by yaml-author for WO-SHOPOPS-INVENTORY-SCAN-VALIDATION-01 on 2026-05-13\n# Source pattern: WO-SHOPOPS-COMMERCE-SPINE-01.yaml (single-repo MIXED + schema migration)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight (4-block):\n#   1. YAML exists.\n#   2. Spec: bdc-xo:docs/work-orders/WO-SHOPOPS-INVENTORY-SCAN-VALIDATION-01.md\n#   3. CWD scope = shopops repo (subdir shopops-api/) — single-repo MIXED, no UI.\n#   4. Codebase bluedevilcollectibles/shopops registered.\n#\n# parallel_ok: YES (new table only; no shared-table writes; no FK to multi-location migration).\n# Polish bar: ROUGH OK (P1; detection-only v1; manual SQL resolution acceptable).\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-SHOPOPS-INVENTORY-SCAN-VALIDATION-01.yaml\n\nname: bdc-shopops-inventory-scan-validation\ndescription: |\n  Use when: Executing WO-SHOPOPS-INVENTORY-SCAN-VALIDATION-01 — duplicate/qty/unexpected\n            detection on shipment ingest.\n  Input: WO_ID = WO-SHOPOPS-INVENTORY-SCAN-VALIDATION-01.\n  Does: Adds shipment_exceptions table (migration 20260515). Adds\n        shopops-api/services/shipmentValidator.js with 3 pure detectors\n        (detectDuplicateUpcs, detectQtyMismatches, detectUnexpectedItems) +\n        validateShipmentLines orchestrator. Extends shipments.js ingest path\n        to call validator and emit exceptions array in response. 6+ bun tests.\n        Commits, opens PR, Rule 19 docker exec verify, MIXED manifest, flip-notion REVIEW.\n  Repo: bluedevilcollectibles/shopops (single-repo, no UI).\n  parallel_ok: true — new table only.\n  Polish bar: ROUGH OK — detection-only v1; no auto-resolution; no UI; manual SQL fix\n              by staff is acceptable.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-SHOPOPS-INVENTORY-SCAN-VALIDATION-01}\"\n\n      # gh --template uses Go templates (built into gh, no external jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' \\\n          2>/dev/null | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  - id: schema-verify\n    depends_on: [read-spec]\n    bash: |\n      # Best-effort: confirm shipments + distributor_order_lines exist; verify\n      # shipment_exceptions does NOT yet exist (idempotency check).\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"; exit 0\n      fi\n\n      check_table_exists() {\n        local TABLE=\"$1\"\n        R=$(curl -sf \"${SUPABASE_URL}/rest/v1/${TABLE}?limit=0\" \\\n          -H \"apikey: ${SUPABASE_SERVICE_KEY}\" -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n        if echo \"$R\" | grep -qiE '\"error\"|\"message\"|relation .* does not exist'; then\n          echo \"TABLE_${TABLE}=ABSENT\"\n        else\n          echo \"TABLE_${TABLE}=PRESENT\"\n        fi\n      }\n      check_table_exists \"shipments\"\n      check_table_exists \"shipment_lines\"\n      check_table_exists \"distributor_orders\"\n      check_table_exists \"distributor_order_lines\"\n      check_table_exists \"shipment_exceptions\"\n      echo \"STATUS=ok\"\n      exit 0\n\n  - id: fresh-branch\n    depends_on: [schema-verify]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      # Defensive: if the default branch is already checked out in another worktree,\n      # we can't switch to it. Stay on current HEAD instead.\n      if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n        echo \"default branch held by another worktree; staying on current HEAD\"\n      fi\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n      BASE_BRANCH=\"wo/shopops-inventory-scan-validation-01\"\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      if git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE_BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE_BRANCH\" ]; then\n          # base branch held by another worktree; use unique timestamped branch\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE_BRANCH\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n          git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n        fi\n      else\n        BRANCH=\"$BASE_BRANCH\"\n        git checkout -b \"$BRANCH\"\n      fi\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: plan\n    depends_on: [fresh-branch]\n    task_class: medium_work\n    prompt: |\n      Plan inventory scan validation (ROUGH OK, detection-only v1).\n\n      Backend (shopops-api):\n      - migrations/20260515_shipment_exceptions.sql (single new table + 2 indexes)\n      - services/shipmentValidator.js:\n          * detectDuplicateUpcs(lines) — pure\n          * detectQtyMismatches(lines, distributorOrderLines) — pure\n          * detectUnexpectedItems(lines, distributorOrderLines) — pure\n          * validateShipmentLines(shipmentId) — orchestrator + DB writes\n      - Modify routes/shipments.js — invoke validateShipmentLines after commit; add exceptions to response\n      - tests/test_shipment_validator.js — 6 unit tests (one per scenario A-F in WO spec)\n\n      v1 NOT in scope: auto-resolution, exception management UI, alerts, batch resolve endpoints.\n\n      Schema-verify: $schema-verify.output\n      Spec: $read-spec.output\n\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement inventory scan validation in shopops-api.\n\n        Plan: $plan.output\n        Spec: $read-spec.output\n\n        Requirements:\n        1. shopops-api/migrations/20260515_shipment_exceptions.sql:\n           CREATE TABLE IF NOT EXISTS shipment_exceptions (\n             id uuid PK default gen_random_uuid(),\n             tenant_id text NOT NULL,\n             shipment_id uuid NOT NULL,\n             line_id uuid,\n             category text NOT NULL CHECK (category IN ('duplicate_upc','qty_mismatch','unexpected_item')),\n             detail jsonb,\n             created_at timestamptz NOT NULL DEFAULT now(),\n             resolved_at timestamptz, resolved_by text, resolution_note text\n           );\n           CREATE INDEX IF NOT EXISTS shipment_exceptions_shipment_idx ON shipment_exceptions(shipment_id);\n           CREATE INDEX IF NOT EXISTS shipment_exceptions_open_idx ON shipment_exceptions(tenant_id) WHERE resolved_at IS NULL;\n\n        2. shopops-api/services/shipmentValidator.js — three pure detectors + orchestrator:\n           - detectDuplicateUpcs(lines): returns [{ upc, line_ids: [...] }]\n           - detectQtyMismatches(lines, distributorOrderLines): [{ line_id, scanned, expected }]\n           - detectUnexpectedItems(lines, distributorOrderLines): [{ line_id, upc }]\n           - validateShipmentLines(shipmentId):\n               fetch shipment lines + distributor_order_lines\n               call 3 detectors\n               INSERT INTO shipment_exceptions for each finding\n               return array of inserted rows\n\n        3. Modify shopops-api/routes/shipments.js:\n           After existing ingest commits lines (do NOT change commit logic):\n             const exceptions = await validateShipmentLines(shipmentId);\n             res.json({ ...existing_body, exceptions });\n           Do NOT block ingest on exceptions — informational only in v1.\n\n        4. shopops-api/tests/test_shipment_validator.js (bun, >= 6 tests):\n           - duplicate_upc detected (scenario A)\n           - qty_mismatch detected (scenario B)\n           - unexpected_item detected (scenario C)\n           - clean shipment returns [] (scenario D)\n           - multi-category on one line (scenario E)\n           - response body includes exceptions array (scenario F)\n           Use pure detector unit tests for fast feedback (no DB needed).\n\n        Do NOT add auto-resolution, no resolve endpoints, no UI, no alerts.\n\n        Output <promise>COMPLETE</promise> when migration + validator + shipments.js edit + tests all present.\n\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      ls shopops-api/migrations/20260515_shipment_exceptions.sql\n      grep -q \"shipment_exceptions\" shopops-api/migrations/20260515_shipment_exceptions.sql\n      grep -q \"duplicate_upc\" shopops-api/migrations/20260515_shipment_exceptions.sql\n      grep -q \"qty_mismatch\" shopops-api/migrations/20260515_shipment_exceptions.sql\n      grep -q \"unexpected_item\" shopops-api/migrations/20260515_shipment_exceptions.sql\n\n      ls shopops-api/services/shipmentValidator.js\n      grep -q \"detectDuplicateUpcs\" shopops-api/services/shipmentValidator.js\n      grep -q \"detectQtyMismatches\" shopops-api/services/shipmentValidator.js\n      grep -q \"detectUnexpectedItems\" shopops-api/services/shipmentValidator.js\n      grep -q \"validateShipmentLines\" shopops-api/services/shipmentValidator.js\n\n      grep -q \"validateShipmentLines\" shopops-api/routes/shipments.js\n\n      ls shopops-api/tests/test_shipment_validator.js\n      TEST_COUNT=$(grep -c \"it(\" shopops-api/tests/test_shipment_validator.js || echo 0)\n      if [ \"$TEST_COUNT\" -lt 6 ]; then echo \"FAIL: <6 tests ($TEST_COUNT)\" >&2; exit 1; fi\n\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      bun test tests/test_shipment_validator.js\n\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      # Use current HEAD branch (set by fresh-branch — may be timestamped variant)\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      git add \\\n        shopops-api/migrations/20260515_shipment_exceptions.sql \\\n        shopops-api/services/shipmentValidator.js \\\n        shopops-api/routes/shipments.js \\\n        shopops-api/tests/test_shipment_validator.js\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-SHOPOPS-INVENTORY-SCAN-VALIDATION-01 — shipment exception detection\\n\\nAdds shipment_exceptions table (migration 20260515).\\nAdds services/shipmentValidator.js with three pure detectors\\n(detectDuplicateUpcs, detectQtyMismatches, detectUnexpectedItems) +\\nvalidateShipmentLines orchestrator.\\n\\nExtends routes/shipments.js ingest path: calls validator after line commit,\\nemits exceptions array in response body. Does NOT block ingest (informational v1).\\n\\n6+ bun tests (one per detection scenario + clean shipment + response shape).\\n\\nPlan: A (P1 extension for Mutant Market 2026-06-15)\\nPolish bar: ROUGH OK — v1 detection-only. No auto-resolution, no UI.\\n            Staff resolves via SQL until v1.1 exception management UI ships.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BASE=\"master\"\n      TITLE=\"feat(api): WO-SHOPOPS-INVENTORY-SCAN-VALIDATION-01 — shipment exception detection\"\n      BODY_FILE=$(mktemp)\n      printf '## Summary\\nImplements WO-SHOPOPS-INVENTORY-SCAN-VALIDATION-01 — detection-only v1.\\n\\n## Plan: A (P1)\\n## Polish bar: ROUGH OK — staff resolves via SQL in v1.\\nv1 NOT in scope: auto-resolution, resolve endpoints, exception management UI, alerts.\\n\\n## Apply migration 20260515_shipment_exceptions.sql to staging Supabase BEFORE merging.\\n' > \"$BODY_FILE\"\n      existing_pr_url() { gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null; }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then echo \"PR_URL=$PR_URL\"; exit 0; fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"; exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then echo \"PR_URL=$PR_URL\"; exit 0; fi\n        sleep 10\n      done\n      exit 1\n\n  - id: runtime-verify\n    depends_on: [open-pr]\n    bash: |\n      RESULT=$(docker exec shopops-api grep -n 'validateShipmentLines' /app/services/shipmentValidator.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"; echo \"RULE19_GREP_RESULT=${RESULT}\"\n      elif echo \"$RESULT\" | grep -qiE 'No such container|not found'; then\n        echo \"STATUS=skipped_container_not_rebuilt\"\n      else\n        echo \"STATUS=failed_unexpected\"; echo \"DETAIL=${RESULT}\"\n      fi\n      echo \"\"\n      echo \"=== RULE 19 RE-RUN COMMANDS (post-deploy) ===\"\n      echo \"docker exec shopops-api grep -n 'detectDuplicateUpcs' /app/services/shipmentValidator.js\"\n      echo \"docker exec shopops-api node -e \\\"const p=require('./db/pool'); p.query('SELECT COUNT(*) FROM shipment_exceptions').then(r=>console.log('OK',r.rows[0].count)).catch(e=>console.error('FAIL',e.message))\\\"\"\n      exit 0\n\n  - id: build-manifest\n    depends_on: [runtime-verify]\n    task_class: clerical\n    prompt: |\n      Build Captain CI manifest v2 for WO-SHOPOPS-INVENTORY-SCAN-VALIDATION-01 (MIXED, ROUGH OK).\n\n      WO: WO-SHOPOPS-INVENTORY-SCAN-VALIDATION-01\n      Builder: Devils Cauldron (Archon bdc-shopops-inventory-scan-validation)\n      Files modified: shopops-api/routes/shipments.js\n      Files created: shopops-api/migrations/20260515_shipment_exceptions.sql,\n                     services/shipmentValidator.js, tests/test_shipment_validator.js\n      Tests: 6+/total via bun test\n      PRs: [from open-pr.output PR_URL]\n      Merge ancestors: shopops master HEAD, behind_by: 0\n      Grep assertions:\n        grep -q 'shipment_exceptions' shopops-api/migrations/20260515_shipment_exceptions.sql\n        grep -q 'detectDuplicateUpcs' shopops-api/services/shipmentValidator.js\n        grep -q 'validateShipmentLines' shopops-api/routes/shipments.js\n      Runtime verification:\n        docker exec shopops-api grep -n 'validateShipmentLines' /app/services/shipmentValidator.js\n        docker exec shopops-api node -e \"<count shipment_exceptions>\"\n      Schema migration: apply 20260515 to staging Supabase before container rebuild.\n        Post-apply: SELECT table_name FROM information_schema.tables WHERE table_name='shipment_exceptions' -- expect 1\n      Vercel deployment: N/A — Hetzner Docker\n      v1 limitations explicit:\n        - detection only; no auto-resolution\n        - no exception management UI\n        - no email/Slack alerts\n        - staff resolves via direct SQL\n        - performance not tested for shipments >1000 lines\n      VALIDATION: PASS (ROUGH OK polish; v1 limitations stated)\n\n      ## Inputs\n      $read-spec.output / $schema-verify.output / $plan.output /\n      $verify-grep.output / $verify-tests.output /\n      $commit-and-push.output / $open-pr.output / $runtime-verify.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-SHOPOPS-INVENTORY-SCAN-VALIDATION-01 to REVIEW with manifest in Notes property.\n      Database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # OPEN QUESTION: operator confirms Notion page ID before deploy.\n\n      Claude Status=REVIEW + Status=Review + Notes=manifest verbatim + claimed_by=Devils Cauldron + plan=A.\n      If Notion MCP unavailable, emit XO_TODO line.\n\n      ## Manifest\n      $build-manifest.output\n",
  "bdc-shopops-multi-location-scope": "# Drafted by yaml-author for WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 on 2026-05-13\n# Source pattern: WO-SHOPOPS-COMMERCE-SPINE-01.yaml (MIXED + schema migration shape)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight (4-block):\n#   1. YAML exists (this file).\n#   2. Spec exists: bdc-xo: docs/work-orders/WO-SHOPOPS-MULTI-LOCATION-SCOPE-01.md\n#   3. CWD scope = shopops repo (subdir shopops-api/) — single-repo MIXED.\n#   4. Codebase bluedevilcollectibles/shopops registered:\n#      sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name FROM remote_agent_codebases\"\n#\n# parallel_ok: NO — schema migration on shared tables (orders/shipments/inventory_items/\n#   distributor_orders/channel_orders). MUST NOT run alongside any other shopops migration.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-SHOPOPS-MULTI-LOCATION-SCOPE-01.yaml\n# REVIEW BEFORE DEPLOY — operator verifies store_locations table presence (or absence)\n#   and decides whether seed-primary section runs; verifies channel_orders table exists.\n\nname: bdc-shopops-multi-location-scope\ndescription: |\n  Use when: Executing WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 — Multi-location scope.\n  Input: WO_ID = WO-SHOPOPS-MULTI-LOCATION-SCOPE-01.\n  Does: Reads spec from bdc-xo (cross-repo gh api). Audits live schema in staging\n        Supabase for tenant/location tables. Writes migration\n        (shopops-api/migrations/20260513_multi_location_scope.sql) idempotent ALTER+backfill.\n        Adds shopops-api/middleware/location.js (validateLocationId), services/locationScope.js\n        (applyLocationScope), extends routes/orders.js + shipments.js + inventory.js +\n        distributor-orders.js + channel-orders.js (if exists) with location_id filter.\n        Adds bun tests covering cross-location query, HQ aggregate, invalid uuid,\n        POST require, tenant isolation. Commits, opens PR, runs Rule 19 docker exec\n        runtime-verify, builds MIXED manifest, flips Notion to REVIEW.\n  Repo: bluedevilcollectibles/shopops (shopops-api/ subdir).\n  parallel_ok: false — schema migration, serial only.\n  Polish bar: HIGH (P0 Plan B floor for Mutant Market install).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  # -------------------------------------------------------------------------\n  # 1. READ SPEC — fetches WO from bdc-xo cross-repo via gh api\n  # -------------------------------------------------------------------------\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-SHOPOPS-MULTI-LOCATION-SCOPE-01}\"\n\n      # gh --template uses Go templates (built into gh, no external jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' \\\n          2>/dev/null | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:docs/work-orders/%s.md\\nWORKFLOW_ID=%s\\n\\n' \\\n        \"$WO_ID\" \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  # -------------------------------------------------------------------------\n  # 2. SCHEMA-VERIFY — query live Supabase for current column presence on each\n  #    target table BEFORE writing migration. Best-effort wrapper.\n  # -------------------------------------------------------------------------\n  - id: schema-verify\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step per doctrine wrapper\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"\n        echo \"DETAIL=SUPABASE_URL or SUPABASE_SERVICE_KEY not in Archon env. Operator must verify schema before apply.\"\n        exit 0\n      fi\n\n      check_column() {\n        local TABLE=\"$1\"\n        local COL=\"$2\"\n        RESULT2=$(curl -sf \\\n          \"${SUPABASE_URL}/rest/v1/information_schema.columns?table_name=eq.${TABLE}&column_name=eq.${COL}&select=column_name\" \\\n          -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n          -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" \\\n          2>&1) || true\n        if echo \"$RESULT2\" | grep -q '\"column_name\"'; then\n          echo \"COL_${TABLE}_${COL}=PRESENT\"\n        else\n          echo \"COL_${TABLE}_${COL}=ABSENT\"\n        fi\n      }\n\n      check_table_exists() {\n        local TABLE=\"$1\"\n        R=$(curl -sf \"${SUPABASE_URL}/rest/v1/${TABLE}?limit=0\" \\\n          -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n          -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n        if echo \"$R\" | grep -qiE '\"error\"|\"message\"|relation .* does not exist'; then\n          echo \"TABLE_${TABLE}=ABSENT\"\n        else\n          echo \"TABLE_${TABLE}=PRESENT\"\n        fi\n      }\n\n      # Check table existence\n      check_table_exists \"store_locations\"\n      check_table_exists \"store_tenants\"\n      check_table_exists \"orders\"\n      check_table_exists \"shipments\"\n      check_table_exists \"inventory_items\"\n      check_table_exists \"distributor_orders\"\n      check_table_exists \"channel_orders\"\n\n      # Check whether location_id columns already exist (idempotency)\n      check_column \"orders\" \"location_id\"\n      check_column \"shipments\" \"location_id\"\n      check_column \"inventory_items\" \"location_id\"\n      check_column \"distributor_orders\" \"location_id\"\n      check_column \"channel_orders\" \"location_id\"\n\n      # Check tenant_id presence (FK precondition)\n      check_column \"orders\" \"tenant_id\"\n      check_column \"store_locations\" \"is_primary\"\n\n      echo \"STATUS=ok\"\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 3. FRESH BRANCH — create wo/shopops-multi-location-scope-01 from \"origin/$DEFAULT_BRANCH\"\n  # -------------------------------------------------------------------------\n  - id: fresh-branch\n    depends_on: [schema-verify]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      # Defensive: if the default branch is already checked out in another worktree,\n      # we can't switch to it. Stay on current HEAD instead.\n      if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n        echo \"default branch held by another worktree; staying on current HEAD\"\n      fi\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n      BASE_BRANCH=\"wo/shopops-multi-location-scope-01\"\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      if git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE_BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE_BRANCH\" ]; then\n          # base branch held by another worktree; use unique timestamped branch\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE_BRANCH\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n          git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n        fi\n      else\n        BRANCH=\"$BASE_BRANCH\"\n        git checkout -b \"$BRANCH\"\n      fi\n      echo \"BRANCH=${BRANCH}\"\n\n  # -------------------------------------------------------------------------\n  # 4. PLAN\n  # -------------------------------------------------------------------------\n  - id: plan\n    depends_on: [fresh-branch]\n    task_class: medium_work\n    prompt: |\n      Plan the multi-location scope implementation for shopops-api.\n\n      ## Required Output (Markdown, exact headings)\n      - WO ID\n      - Files to create: migrations/20260513_multi_location_scope.sql,\n        middleware/location.js, services/locationScope.js,\n        tests/test_multi_location_scope.js\n      - Files to modify: routes/orders.js, routes/shipments.js, routes/inventory.js,\n        routes/distributor-orders.js, routes/channel-orders.js (only if table exists per schema-verify)\n      - Files explicitly out of scope: UI WO is separate\n      - Migration plan (idempotent ALTER + backfill + INDEX, channel_orders conditional)\n      - Test scenarios (minimum 6 covering: cross-location empty, HQ aggregate,\n        invalid uuid, POST require, tenant isolation, middleware unit)\n      - Verification commands (bun test, not node/npm)\n      - Commit message: \"feat(api): WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 — location_id scope on orders/shipments/inventory/distributor-orders\"\n      - Branch: wo/shopops-multi-location-scope-01 (already created)\n      - PR base: master\n\n      ## Schema-verify output\n      $schema-verify.output\n\n      ## Spec\n      $read-spec.output\n\n  # -------------------------------------------------------------------------\n  # 5. IMPLEMENT — write migration + middleware + service + route edits + tests\n  # -------------------------------------------------------------------------\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the multi-location scope per the plan. CWD is the shopops repo root.\n        shopops-api/ is the subdirectory containing all API code.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Schema audit output\n        $schema-verify.output\n\n        ## Execution Rules\n        1. Create shopops-api/migrations/20260513_multi_location_scope.sql:\n           - ENTIRELY IDEMPOTENT (every CREATE TABLE / ALTER TABLE / CREATE INDEX uses IF NOT EXISTS)\n           - If schema-verify shows store_locations ABSENT: include CREATE TABLE + seed primary per tenant\n           - ALTER TABLE orders/shipments/inventory_items/distributor_orders ADD COLUMN IF NOT EXISTS location_id uuid REFERENCES store_locations(id)\n           - For channel_orders: include the ALTER only if schema-verify shows TABLE_channel_orders=PRESENT\n           - Backfill: UPDATE <table> SET location_id = (SELECT id FROM store_locations WHERE tenant_id = <table>.tenant_id AND is_primary = true LIMIT 1) WHERE location_id IS NULL\n           - Add INDEX <table>_location_id_idx for each table\n\n        2. Create shopops-api/middleware/location.js:\n           - export function validateLocationId(req, res, next)\n           - reads req.query.location_id OR req.body.location_id\n           - if absent: next() (HQ aggregate mode for GET; for POST upstream handler decides whether to require)\n           - if present: verify is uuid AND belongs to req.tenantId via DB lookup. 400 if not.\n           - sets req.locationId\n\n        3. Create shopops-api/services/locationScope.js:\n           - export function applyLocationScope(locationId)\n           - returns { clause: string, params: array } for SQL composition\n           - locationId null → { clause: '', params: [] }\n           - locationId set → { clause: 'AND location_id = $N', params: [locationId] } (caller substitutes $N)\n\n        4. Modify each route file:\n           - routes/orders.js, routes/shipments.js, routes/inventory.js, routes/distributor-orders.js\n           - GET handlers: apply validateLocationId middleware before handler, use applyLocationScope to extend SQL\n           - POST/PATCH handlers: require location_id in body (400 if missing), validate FK\n           - PRESERVE all existing behavior (do not break GET without location_id — must return HQ aggregate)\n\n        5. Create shopops-api/tests/test_multi_location_scope.js (bun test runner):\n           - import { describe, it, expect } from 'bun:test'\n           - 6+ tests asserting REAL behavior:\n             a. GET /orders?location_id=L1 returns only L1 rows\n             b. GET /orders (no location_id) returns aggregate across L1+L2\n             c. GET /orders?location_id=bogus returns 400\n             d. POST /orders without location_id returns 400\n             e. Tenant T2 cannot access tenant T1's location L1\n             f. validateLocationId middleware unit test (mock req/res/next)\n           - Tests may mock the DB pool if Supabase unavailable in CI\n\n        6. Do NOT modify customers.js (uses tenant scoping; out of scope).\n        7. Follow existing code patterns (require() style if that is repo convention).\n        8. Do NOT deploy, push, or run docker commands.\n\n        ## Completion Criteria\n        Output <promise>COMPLETE</promise> only when:\n          - Migration file exists with all ALTERs + backfills + indexes\n          - middleware/location.js exists with validateLocationId\n          - services/locationScope.js exists with applyLocationScope\n          - All 4 route files modified to use middleware + scope helper\n          - tests/test_multi_location_scope.js has >= 6 real-behavior tests\n          - bun test passes locally (or fails only on missing DB env, never on logic)\n\n  # -------------------------------------------------------------------------\n  # 6. VERIFY-GREP — structural assertions (multi-line safe)\n  # -------------------------------------------------------------------------\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      echo \"=== structural grep assertions ===\"\n\n      # Migration file exists\n      ls shopops-api/migrations/20260513_multi_location_scope.sql\n\n      # Migration has ALTER TABLE for each target\n      grep -q \"ALTER TABLE orders\" shopops-api/migrations/20260513_multi_location_scope.sql \\\n        || (echo \"FAIL: missing ALTER TABLE orders\" >&2; exit 1)\n      grep -q \"ALTER TABLE shipments\" shopops-api/migrations/20260513_multi_location_scope.sql \\\n        || (echo \"FAIL: missing ALTER TABLE shipments\" >&2; exit 1)\n      grep -q \"ALTER TABLE inventory_items\" shopops-api/migrations/20260513_multi_location_scope.sql \\\n        || (echo \"FAIL: missing ALTER TABLE inventory_items\" >&2; exit 1)\n      grep -q \"ALTER TABLE distributor_orders\" shopops-api/migrations/20260513_multi_location_scope.sql \\\n        || (echo \"FAIL: missing ALTER TABLE distributor_orders\" >&2; exit 1)\n\n      # Idempotency: every ALTER uses IF NOT EXISTS\n      ALTER_COUNT=$(grep -c \"ALTER TABLE\" shopops-api/migrations/20260513_multi_location_scope.sql)\n      IF_NOT_EXISTS_COUNT=$(grep -c \"IF NOT EXISTS\" shopops-api/migrations/20260513_multi_location_scope.sql)\n      echo \"ALTER lines: $ALTER_COUNT; IF NOT EXISTS occurrences: $IF_NOT_EXISTS_COUNT\"\n      if [ \"$IF_NOT_EXISTS_COUNT\" -lt \"$ALTER_COUNT\" ]; then\n        echo \"FAIL: migration not fully idempotent (IF NOT EXISTS count < ALTER count)\" >&2\n        exit 1\n      fi\n\n      # Backfill present\n      grep -q \"is_primary\" shopops-api/migrations/20260513_multi_location_scope.sql \\\n        || (echo \"FAIL: backfill missing is_primary lookup\" >&2; exit 1)\n\n      # Middleware exists\n      ls shopops-api/middleware/location.js\n      grep -q \"validateLocationId\" shopops-api/middleware/location.js \\\n        || (echo \"FAIL: validateLocationId not exported\" >&2; exit 1)\n\n      # Service exists\n      ls shopops-api/services/locationScope.js\n      grep -q \"applyLocationScope\" shopops-api/services/locationScope.js \\\n        || (echo \"FAIL: applyLocationScope not exported\" >&2; exit 1)\n\n      # Routes reference location_id\n      grep -q \"location_id\\|locationId\" shopops-api/routes/orders.js \\\n        || (echo \"FAIL: routes/orders.js does not reference location_id\" >&2; exit 1)\n      grep -q \"location_id\\|locationId\" shopops-api/routes/shipments.js \\\n        || (echo \"FAIL: routes/shipments.js does not reference location_id\" >&2; exit 1)\n      grep -q \"location_id\\|locationId\" shopops-api/routes/inventory.js \\\n        || (echo \"FAIL: routes/inventory.js does not reference location_id\" >&2; exit 1)\n      grep -q \"location_id\\|locationId\" shopops-api/routes/distributor-orders.js \\\n        || (echo \"FAIL: routes/distributor-orders.js does not reference location_id\" >&2; exit 1)\n\n      # Test file with >= 6 tests\n      ls shopops-api/tests/test_multi_location_scope.js\n      TEST_COUNT=$(grep -c \"it(\" shopops-api/tests/test_multi_location_scope.js || echo 0)\n      echo \"Test count: $TEST_COUNT\"\n      if [ \"$TEST_COUNT\" -lt 6 ]; then\n        echo \"FAIL: expected >= 6 tests, got $TEST_COUNT\" >&2\n        exit 1\n      fi\n\n      echo \"=== all grep assertions passed ===\"\n\n  # -------------------------------------------------------------------------\n  # 7. VERIFY-TESTS\n  # -------------------------------------------------------------------------\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      if [ -f tests/test_multi_location_scope.js ]; then\n        bun test tests/test_multi_location_scope.js \\\n          || echo \"Tests may fail without live Supabase; CI against staging is authoritative.\"\n      else\n        echo \"Test file missing\" >&2; exit 1\n      fi\n\n  # -------------------------------------------------------------------------\n  # 8. COMMIT-AND-PUSH\n  # -------------------------------------------------------------------------\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      # Use current HEAD branch (set by fresh-branch — may be timestamped variant)\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      git add \\\n        shopops-api/migrations/20260513_multi_location_scope.sql \\\n        shopops-api/middleware/location.js \\\n        shopops-api/services/locationScope.js \\\n        shopops-api/routes/orders.js \\\n        shopops-api/routes/shipments.js \\\n        shopops-api/routes/inventory.js \\\n        shopops-api/routes/distributor-orders.js \\\n        shopops-api/tests/test_multi_location_scope.js\n\n      # Stage channel-orders only if it was modified\n      if git diff --quiet shopops-api/routes/channel-orders.js 2>/dev/null; then\n        echo \"channel-orders.js unchanged or absent\"\n      else\n        git add shopops-api/routes/channel-orders.js\n      fi\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes — nothing to commit\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 — location_id scope on orders/shipments/inventory/distributor-orders\\n\\nAdds idempotent migration 20260513_multi_location_scope.sql:\\n  - CREATE TABLE store_locations IF NOT EXISTS (with seed primary per tenant)\\n  - ALTER TABLE ... ADD COLUMN IF NOT EXISTS location_id uuid FK to store_locations\\n    on orders, shipments, inventory_items, distributor_orders, channel_orders (if present)\\n  - Backfill location_id to each tenant'\\''s is_primary location\\n  - CREATE INDEX <table>_location_id_idx\\n\\nAdds middleware/location.js validateLocationId — uuid + tenant ownership check\\nAdds services/locationScope.js applyLocationScope — SQL fragment composer\\n\\nExtends route handlers (orders/shipments/inventory/distributor-orders/channel-orders):\\n  - GET: location_id optional (HQ aggregate when absent)\\n  - POST/PATCH: location_id required, FK validated\\n\\nAdds tests/test_multi_location_scope.js — 6 bun tests covering cross-location filter,\\nHQ aggregate, invalid uuid, POST require, tenant isolation, middleware unit.\\n\\nPlan: B (P0 floor for Mutant Market HQ install 2026-06-15)\\nBlocks: WO-SHOPOPS-RECEIVING-INTAKE-01 and POS per-location writes\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  # -------------------------------------------------------------------------\n  # 9. OPEN-PR — idempotent\n  # -------------------------------------------------------------------------\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BASE=\"master\"\n      TITLE=\"feat(api): WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 — multi-location scope\"\n\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-SHOPOPS-MULTI-LOCATION-SCOPE-01** — adds location_id scope\"\n        echo \"to orders/shipments/inventory/distributor-orders for HQ multi-store tenants.\"\n        echo \"\"\n        echo \"## Plan tag\"\n        echo \"plan:B — P0 floor for Mutant Market HQ install (2026-06-15).\"\n        echo \"\"\n        echo \"## Migration\"\n        echo \"shopops-api/migrations/20260513_multi_location_scope.sql (idempotent)\"\n        echo \"\"\n        echo \"## API changes\"\n        echo \"- New middleware: validateLocationId\"\n        echo \"- New service: applyLocationScope\"\n        echo \"- GET handlers: location_id filter optional (HQ aggregate when absent)\"\n        echo \"- POST/PATCH: location_id required + tenant-FK validated\"\n        echo \"\"\n        echo \"## Blocks\"\n        echo \"- WO-SHOPOPS-RECEIVING-INTAKE-01\"\n        echo \"- All POS per-location write WOs\"\n        echo \"\"\n        echo \"## Rule 20 / Staging gate\"\n        echo \"Apply migration to staging Supabase BEFORE merging. John promotes to production.\"\n        echo \"\"\n        echo \"Generated by Archon bdc-shopops-multi-location-scope workflow.\"\n      } > \"$BODY_FILE\"\n\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n          --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then echo \"PR_URL=$PR_URL\"; exit 0; fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"; exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then echo \"PR_URL=$PR_URL\"; exit 0; fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed; branch pushed at $BRANCH\" >&2\n      exit 1\n\n  # -------------------------------------------------------------------------\n  # 10. RUNTIME-VERIFY — Rule 19 docker exec\n  # -------------------------------------------------------------------------\n  - id: runtime-verify\n    depends_on: [open-pr]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step\n      RESULT=$(docker exec shopops-api grep -n 'location_id\\|locationId' /app/routes/orders.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RULE19_GREP_RESULT=${RESULT}\"\n      elif echo \"$RESULT\" | grep -qiE 'No such container|not found'; then\n        echo \"STATUS=skipped_container_not_rebuilt\"\n        echo \"DETAIL=Re-run post-deploy: docker compose build shopops-api && docker compose up -d shopops-api\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${RESULT}\"\n      fi\n\n      echo \"\"\n      echo \"=== RULE 19 RE-RUN COMMANDS (post-deploy) ===\"\n      echo \"docker exec shopops-api grep -n 'validateLocationId' /app/middleware/location.js\"\n      echo \"docker exec shopops-api grep -n 'applyLocationScope' /app/services/locationScope.js\"\n      echo \"docker exec shopops-api node -e \\\"const p=require('./db/pool'); p.query(\\\\\\\"SELECT column_name FROM information_schema.columns WHERE table_name='orders' AND column_name='location_id'\\\\\\\").then(r=>console.log('OK',r.rows.length)).catch(e=>console.error('FAIL',e.message))\\\"\"\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 11. BUILD-MANIFEST — MIXED class\n  # -------------------------------------------------------------------------\n  - id: build-manifest\n    depends_on: [runtime-verify]\n    task_class: clerical\n    prompt: |\n      Build the Captain CI manifest v2 for WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 (MIXED class).\n\n      MIXED = both CODE evidence (grep assertions) AND INFRA evidence (runtime verification).\n\n      ## Required Fields (manifest v2)\n      WO: WO-SHOPOPS-MULTI-LOCATION-SCOPE-01\n      Builder: Devils Cauldron (Archon bdc-shopops-multi-location-scope)\n      Files modified: shopops-api/routes/orders.js, routes/shipments.js, routes/inventory.js,\n                      routes/distributor-orders.js, routes/channel-orders.js (if applicable)\n      Files created: shopops-api/migrations/20260513_multi_location_scope.sql,\n                     middleware/location.js, services/locationScope.js,\n                     tests/test_multi_location_scope.js\n      Tests: [count from verify-tests.output] passing / total via `bun test shopops-api/tests/test_multi_location_scope.js`\n      PRs: [from open-pr.output PR_URL] — base: master\n      Merge ancestors: shopops master HEAD=[from commit-and-push BRANCH_SHA], behind_by: 0\n      Grep assertions:\n        grep -n 'location_id' shopops-api/routes/orders.js\n        grep -n 'validateLocationId' shopops-api/middleware/location.js\n        grep -n 'applyLocationScope' shopops-api/services/locationScope.js\n        grep -c 'ALTER TABLE' shopops-api/migrations/20260513_multi_location_scope.sql -- expected >= 4\n      Runtime verification:\n        docker exec shopops-api grep -n 'location_id' /app/routes/orders.js\n        docker exec shopops-api node -e \"const p=require('./db/pool'); p.query(\\\"SELECT column_name FROM information_schema.columns WHERE table_name='orders' AND column_name='location_id'\\\").then(r=>console.log(r.rows.length))\"\n      Schema migration:\n        Apply to staging Supabase BEFORE container rebuild.\n        Post-apply verify:\n          SELECT table_name, column_name FROM information_schema.columns\n          WHERE column_name='location_id' AND table_name IN ('orders','shipments','inventory_items','distributor_orders')\n          -- expected: 4 rows (or 5 if channel_orders applicable)\n        SELECT COUNT(*) FROM orders WHERE location_id IS NULL -- expected 0\n        SELECT id FROM schema_migrations WHERE id='20260513_multi_location_scope' -- expected 1\n      Vercel deployment: N/A — shopops-api is Hetzner Docker\n      VALIDATION: PASS (pending migration apply + container rebuild)\n      Polish bar: HIGH (P0 Plan B floor)\n\n      ## Inputs\n      $read-spec.output\n      $schema-verify.output\n      $plan.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-and-push.output\n      $open-pr.output\n      $runtime-verify.output\n\n  # -------------------------------------------------------------------------\n  # 12. FLIP-NOTION\n  # -------------------------------------------------------------------------\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 to REVIEW with manifest in Notes property.\n\n      IMPORTANT: manifest goes in Notes PROPERTY (not page comment). Captain CI reads Notes.\n\n      # OPEN QUESTION: Notion page ID is not confirmed in this draft. Operator must search\n      #   database a6df831c-0b52-449f-8ca4-d77be6b70d0a for title containing\n      #   \"WO-SHOPOPS-MULTI-LOCATION-SCOPE-01\", confirm the page ID, then hardcode it\n      #   here before deploy.\n      Notion database ID: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n\n      Required Notion changes (single update):\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> set to manifest from $build-manifest.output verbatim\n      - claimed_by: Devils Cauldron (Archon)\n      - plan tag: plan:B\n\n      If Notion MCP unavailable in runtime, output:\n        XO_TODO: Flip WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 to REVIEW in Notion.\n                 Paste manifest from $build-manifest.output into Notes property.\n\n      ## Manifest\n      $build-manifest.output\n",
  "bdc-shopops-receiving-intake": "# Drafted by yaml-author for WO-SHOPOPS-RECEIVING-INTAKE-01 on 2026-05-13\n# Source pattern: WO-SHOPOPS-COMMERCE-SPINE-01.yaml (MIXED + UI cross-repo)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight (4-block):\n#   1. YAML exists.\n#   2. Spec: bdc-xo:docs/work-orders/WO-SHOPOPS-RECEIVING-INTAKE-01.md\n#   3. CWD scope = shopops repo PRIMARY + lspro-react SIBLING (cross-repo MIXED).\n#   4. Both codebases registered:\n#      sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name FROM remote_agent_codebases\"\n#\n# parallel_ok: NO — schema migration on receiving_sessions/receiving_session_lines (new tables,\n#   but depends_on WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 must already be merged for location_id FK).\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-SHOPOPS-RECEIVING-INTAKE-01.yaml\n# REVIEW BEFORE DEPLOY — verify WO-SHOPOPS-MULTI-LOCATION-SCOPE-01 is merged + applied\n#   in staging before firing this workflow (location_id FK needed).\n\nname: bdc-shopops-receiving-intake\ndescription: |\n  Use when: Executing WO-SHOPOPS-RECEIVING-INTAKE-01 — Receiving Intake (scan/verify).\n  Input: WO_ID = WO-SHOPOPS-RECEIVING-INTAKE-01.\n  Does: Reads spec from bdc-xo. Adds receiving_sessions + receiving_session_lines tables\n        via migration. Adds shopops-api/routes/receiving-sessions.js (POST create, POST scan,\n        POST submit, POST cancel, GET detail) + services/receivingSession.js (matchScan,\n        reconcileSession). Drafts lspro-react ReceivingIntakePage in sibling worktree\n        with scan input + reconciliation grid + discrepancy panel. 7+ bun tests +\n        Playwright UI test. Commits both repos, opens 2 PRs, runs Rule 19 docker exec\n        verify, builds MIXED manifest, flips Notion REVIEW.\n  Repo: bluedevilcollectibles/shopops (PRIMARY CWD), bluedevilcollectibles/lspro-react\n        (sibling via /tmp clone).\n  parallel_ok: false — schema migration AND depends on multi-location-scope migration first.\n  Polish bar: HIGH (warehouse staff use daily).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-SHOPOPS-RECEIVING-INTAKE-01}\"\n\n      # gh --template uses Go templates (built into gh, no external jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' \\\n          2>/dev/null | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:docs/work-orders/%s.md\\nWORKFLOW_ID=%s\\n\\n' \\\n        \"$WO_ID\" \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  - id: schema-verify\n    depends_on: [read-spec]\n    bash: |\n      # Best-effort: verify location_id is present on distributor_orders (precondition)\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"; exit 0\n      fi\n\n      check_column() {\n        local TABLE=\"$1\"; local COL=\"$2\"\n        RESULT2=$(curl -sf \\\n          \"${SUPABASE_URL}/rest/v1/information_schema.columns?table_name=eq.${TABLE}&column_name=eq.${COL}&select=column_name\" \\\n          -H \"apikey: ${SUPABASE_SERVICE_KEY}\" -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n        if echo \"$RESULT2\" | grep -q '\"column_name\"'; then\n          echo \"COL_${TABLE}_${COL}=PRESENT\"\n        else\n          echo \"COL_${TABLE}_${COL}=ABSENT\"\n        fi\n      }\n      # Precondition: multi-location-scope migration must already be applied\n      check_column \"distributor_orders\" \"location_id\"\n      check_column \"store_locations\" \"id\"\n      check_column \"distributor_orders\" \"id\"\n      check_column \"distributor_order_lines\" \"upc\"\n\n      # Idempotency check\n      EXISTING=$(curl -sf \"${SUPABASE_URL}/rest/v1/receiving_sessions?limit=0\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n      if echo \"$EXISTING\" | grep -qiE '\"error\"|\"message\"|relation .* does not exist'; then\n        echo \"TABLE_receiving_sessions=ABSENT\"\n      else\n        echo \"TABLE_receiving_sessions=PRESENT\"\n      fi\n      echo \"STATUS=ok\"\n      exit 0\n\n  - id: fresh-branch\n    depends_on: [schema-verify]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      # Defensive: if the default branch is already checked out in another worktree,\n      # we can't switch to it. Stay on current HEAD instead.\n      if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n        echo \"default branch held by another worktree; staying on current HEAD\"\n      fi\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n      BASE_BRANCH=\"wo/shopops-receiving-intake-01\"\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      if git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE_BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE_BRANCH\" ]; then\n          # base branch held by another worktree; use unique timestamped branch\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE_BRANCH\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n          git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n        fi\n      else\n        BRANCH=\"$BASE_BRANCH\"\n        git checkout -b \"$BRANCH\"\n      fi\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: plan\n    depends_on: [fresh-branch]\n    task_class: medium_work\n    prompt: |\n      Plan the Receiving Intake implementation. Output exact files, migration plan,\n      route signatures, UI hierarchy, test scenarios.\n\n      ## Backend (shopops-api)\n      - migrations/20260514_receiving_sessions.sql (receiving_sessions + receiving_session_lines)\n      - routes/receiving-sessions.js (POST create, POST :id/scan, POST :id/submit, POST :id/cancel, GET :id)\n      - services/receivingSession.js (matchScan pure function, reconcileSession)\n      - tests/test_receiving_session.js (>=7 tests)\n\n      ## UI (lspro-react) — sibling worktree at /tmp/lspro-react-receiving-<WORKFLOW_ID>\n      - src/pages/Receiving/ReceivingIntakePage.tsx\n      - src/components/Receiving/ScanInput.tsx\n      - src/components/Receiving/ExpectedItemsGrid.tsx\n      - src/components/Receiving/DiscrepancyPanel.tsx\n      - Add route in App.tsx (or wherever route table lives)\n\n      Schema-verify output: $schema-verify.output\n      Spec: $read-spec.output\n\n  - id: implement-backend\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the Receiving Intake BACKEND in shopops repo. CWD is shopops repo root.\n\n        Plan: $plan.output\n        Spec: $read-spec.output\n\n        Requirements:\n        1. shopops-api/migrations/20260514_receiving_sessions.sql:\n           - CREATE TABLE IF NOT EXISTS receiving_sessions (id, tenant_id, location_id FK,\n             distributor_order_id FK, status CHECK, opened_at, submitted_at, submitted_by, notes)\n           - CREATE TABLE IF NOT EXISTS receiving_session_lines (id, session_id FK ON DELETE CASCADE,\n             upc, internal_product_id, title, expected_qty, scanned_qty, status CHECK, notes)\n           - Indexes\n\n        2. shopops-api/routes/receiving-sessions.js:\n           - POST /receiving-sessions — creates session, copies distributor_order_lines into expected_lines\n           - POST /receiving-sessions/:id/scan { upc, qty } — calls matchScan, updates scanned_qty\n           - POST /receiving-sessions/:id/submit { force } — 409 if discrepancy + force=false;\n             else writes inventory movements + flips distributor_orders.status=received\n           - POST /receiving-sessions/:id/cancel — soft-delete (status=cancelled)\n           - GET /receiving-sessions/:id — returns current session state\n\n        3. shopops-api/services/receivingSession.js:\n           - matchScan(session, upc) pure function → returns action: 'increment'|'over'|'unexpected'\n           - reconcileSession(sessionId) → per-line status rollup\n\n        4. shopops-api/tests/test_receiving_session.js:\n           - import { describe, it, expect } from 'bun:test'\n           - 7+ tests: open session, scan match, scan over, scan unexpected, submit blocked\n             on short (force=false), submit succeeds force=true, multi-tab GET refresh\n\n        Output <promise>COMPLETE</promise> when all 4 files exist with required behavior.\n\n  - id: verify-backend-grep\n    depends_on: [implement-backend]\n    bash: |\n      set -euo pipefail\n      ls shopops-api/migrations/20260514_receiving_sessions.sql\n      grep -q \"receiving_sessions\" shopops-api/migrations/20260514_receiving_sessions.sql\n      grep -q \"receiving_session_lines\" shopops-api/migrations/20260514_receiving_sessions.sql\n      grep -q \"ON DELETE CASCADE\" shopops-api/migrations/20260514_receiving_sessions.sql\n\n      ls shopops-api/routes/receiving-sessions.js\n      grep -q \"/receiving-sessions\" shopops-api/routes/receiving-sessions.js\n      grep -q \"scan\" shopops-api/routes/receiving-sessions.js\n      grep -q \"submit\" shopops-api/routes/receiving-sessions.js\n      grep -q \"cancel\" shopops-api/routes/receiving-sessions.js\n\n      ls shopops-api/services/receivingSession.js\n      grep -q \"matchScan\" shopops-api/services/receivingSession.js\n      grep -q \"reconcileSession\" shopops-api/services/receivingSession.js\n\n      ls shopops-api/tests/test_receiving_session.js\n      TEST_COUNT=$(grep -c \"it(\" shopops-api/tests/test_receiving_session.js || echo 0)\n      if [ \"$TEST_COUNT\" -lt 7 ]; then echo \"FAIL: <7 tests ($TEST_COUNT)\" >&2; exit 1; fi\n\n  - id: verify-backend-tests\n    depends_on: [implement-backend]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      bun test tests/test_receiving_session.js \\\n        || echo \"Tests may fail without live Supabase; CI staging authoritative.\"\n\n  - id: implement-ui\n    depends_on: [implement-backend]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the Receiving Intake UI in a SIBLING worktree of lspro-react.\n\n        Plan: $plan.output\n        Spec: $read-spec.output\n\n        Steps:\n        1. Clone lspro-react into /tmp/lspro-react-receiving-$WORKFLOW_ID:\n             git clone --depth=20 https://github.com/bluedevilcollectibles/lspro-react.git \\\n               /tmp/lspro-react-receiving-$WORKFLOW_ID\n             cd /tmp/lspro-react-receiving-$WORKFLOW_ID\n             git checkout staging\n             git pull origin staging\n             git checkout -b wo/shopops-receiving-intake-01-ui\n           (Use staging per Rule 20.)\n\n        2. Create files:\n           - src/pages/Receiving/ReceivingIntakePage.tsx — page-level component, fetches\n             open distributor orders, holds session state, calls API\n           - src/components/Receiving/ScanInput.tsx — autofocus input, Enter submits\n           - src/components/Receiving/ExpectedItemsGrid.tsx — grid with row tint by status\n           - src/components/Receiving/DiscrepancyPanel.tsx — counts + \"show only discrepancies\" toggle\n\n        3. Wire the route in the router (App.tsx or routes file — verify by reading repo first)\n           and ensure `/receiving` resolves to ReceivingIntakePage.\n\n        4. Add Playwright test stub: tests/receiving-intake.spec.ts (1 test minimum)\n           that asserts the page renders + scan input is autofocused.\n\n        5. From the /tmp clone:\n             git add src/pages/Receiving/ src/components/Receiving/ tests/receiving-intake.spec.ts \\\n                     <any router file modified>\n             git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n               commit -m \"$(printf 'feat(ui): WO-SHOPOPS-RECEIVING-INTAKE-01 — ReceivingIntakePage + scan/grid/discrepancy\\n\\nAdds ReceivingIntakePage with scan input, ExpectedItemsGrid (row-tint by status),\\nDiscrepancyPanel. Routes /receiving. Wires to shopops-api receiving-sessions endpoints.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n             git push -u origin wo/shopops-receiving-intake-01-ui\n\n        6. Open PR on lspro-react against staging (Rule 20):\n             gh pr create --repo bluedevilcollectibles/lspro-react --base staging \\\n               --head wo/shopops-receiving-intake-01-ui \\\n               --title \"feat(ui): WO-SHOPOPS-RECEIVING-INTAKE-01 — ReceivingIntakePage\" \\\n               --body \"Implements UI side of WO-SHOPOPS-RECEIVING-INTAKE-01. Wires to shopops-api receiving-sessions endpoints. Plan: B.\"\n\n        Output <promise>COMPLETE</promise> with the lspro-react PR URL.\n\n  - id: commit-and-push-backend\n    depends_on: [verify-backend-grep, verify-backend-tests]\n    bash: |\n      set -euo pipefail\n      # Use current HEAD branch (set by fresh-branch — may be timestamped variant)\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      git add \\\n        shopops-api/migrations/20260514_receiving_sessions.sql \\\n        shopops-api/routes/receiving-sessions.js \\\n        shopops-api/services/receivingSession.js \\\n        shopops-api/tests/test_receiving_session.js\n\n      # Also touch the route mount file if needed (server.js or routes/index.js)\n      if git diff --quiet shopops-api/server.js 2>/dev/null && \\\n         git diff --quiet shopops-api/routes/index.js 2>/dev/null; then\n        echo \"Route mount file unchanged; ensure /receiving-sessions registered in app entrypoint.\"\n      else\n        git add shopops-api/server.js shopops-api/routes/index.js 2>/dev/null || true\n      fi\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-SHOPOPS-RECEIVING-INTAKE-01 — receiving session backend\\n\\nAdds receiving_sessions + receiving_session_lines tables (migration 20260514).\\nAdds routes/receiving-sessions.js: POST create, POST scan, POST submit, POST cancel, GET.\\nAdds services/receivingSession.js: matchScan + reconcileSession.\\n7+ bun tests covering scan, over, unexpected, submit/force flow.\\n\\nPlan: B (P0 floor, Mutant Market 2026-06-15)\\nUI side: bluedevilcollectibles/lspro-react PR\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr-backend\n    depends_on: [commit-and-push-backend]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BASE=\"master\"\n      TITLE=\"feat(api): WO-SHOPOPS-RECEIVING-INTAKE-01 — receiving session backend\"\n      BODY_FILE=$(mktemp)\n      printf '## Summary\\nImplements WO-SHOPOPS-RECEIVING-INTAKE-01 backend (receiving_sessions tables + 5 routes + matchScan/reconcileSession services).\\n\\n## Plan: B (P0 floor, Mutant Market 2026-06-15)\\n\\nUI side: bluedevilcollectibles/lspro-react companion PR.\\nApply migration to staging Supabase BEFORE merging.\\n' > \"$BODY_FILE\"\n      existing_pr_url() { gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null; }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then echo \"PR_URL=$PR_URL\"; exit 0; fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"; exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then echo \"PR_URL=$PR_URL\"; exit 0; fi\n        sleep 10\n      done\n      exit 1\n\n  - id: runtime-verify\n    depends_on: [open-pr-backend, implement-ui]\n    bash: |\n      # Best-effort Rule 19 verify\n      RESULT=$(docker exec shopops-api grep -n 'receiving-sessions' /app/routes/receiving-sessions.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"; echo \"RULE19_GREP_RESULT=${RESULT}\"\n      elif echo \"$RESULT\" | grep -qiE 'No such container|not found'; then\n        echo \"STATUS=skipped_container_not_rebuilt\"\n      else\n        echo \"STATUS=failed_unexpected\"; echo \"DETAIL=${RESULT}\"\n      fi\n\n      echo \"\"\n      echo \"=== RULE 19 RE-RUN COMMANDS (post-deploy) ===\"\n      echo \"docker exec shopops-api grep -n 'matchScan' /app/services/receivingSession.js\"\n      echo \"docker exec shopops-api node -e \\\"const p=require('./db/pool'); p.query('SELECT COUNT(*) FROM receiving_sessions').then(r=>console.log('OK',r.rows[0].count)).catch(e=>console.error('FAIL',e.message))\\\"\"\n      echo \"curl https://staging.livesellerpro.app/receiving (expects 200 with ReceivingIntakePage chunk)\"\n      exit 0\n\n  - id: build-manifest\n    depends_on: [runtime-verify]\n    task_class: clerical\n    prompt: |\n      Build Captain CI manifest v2 for WO-SHOPOPS-RECEIVING-INTAKE-01 (MIXED, cross-repo).\n\n      Required fields:\n      WO: WO-SHOPOPS-RECEIVING-INTAKE-01\n      Builder: Devils Cauldron (Archon bdc-shopops-receiving-intake)\n      Files modified: shopops-api/server.js (or routes index) + any lspro-react router file\n      Files created (shopops): migrations/20260514_receiving_sessions.sql,\n                              routes/receiving-sessions.js, services/receivingSession.js,\n                              tests/test_receiving_session.js\n      Files created (lspro-react): src/pages/Receiving/ReceivingIntakePage.tsx,\n                                   src/components/Receiving/ScanInput.tsx,\n                                   src/components/Receiving/ExpectedItemsGrid.tsx,\n                                   src/components/Receiving/DiscrepancyPanel.tsx,\n                                   tests/receiving-intake.spec.ts\n      Tests: backend [count]/[total] via bun + UI [count] via Playwright\n      PRs: shopops [PR URL from open-pr-backend], lspro-react [PR URL from implement-ui]\n      Merge ancestors: shopops master HEAD + lspro-react staging HEAD, behind_by: 0 each\n      Grep assertions:\n        grep -q 'receiving-sessions' shopops-api/routes/receiving-sessions.js\n        grep -q 'matchScan' shopops-api/services/receivingSession.js\n        grep -q 'ReceivingIntakePage' lspro-react/src/pages/Receiving/ReceivingIntakePage.tsx\n      Runtime verification:\n        docker exec shopops-api grep -n 'receiving-sessions' /app/routes/receiving-sessions.js\n        docker exec shopops-api node -e \"<count receiving_sessions>\"\n        curl https://staging.livesellerpro.app/receiving (expect 200)\n      Vercel deployment: lspro-react staging URL + state READY + commit matches branch HEAD\n      Schema migration:\n        Apply 20260514_receiving_sessions.sql to staging Supabase BEFORE rebuild.\n        Verify: SELECT table_name FROM information_schema.tables WHERE table_name IN ('receiving_sessions','receiving_session_lines') -- expect 2\n      VALIDATION: PASS (pending staging deploy + migration apply)\n      Polish bar: HIGH\n\n      ## Inputs\n      $read-spec.output / $schema-verify.output / $plan.output /\n      $verify-backend-grep.output / $verify-backend-tests.output /\n      $commit-and-push-backend.output / $open-pr-backend.output /\n      $implement-ui.output / $runtime-verify.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-SHOPOPS-RECEIVING-INTAKE-01 to REVIEW with manifest in Notes property.\n      Database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # OPEN QUESTION: operator confirms Notion page ID before deploy.\n\n      Set Claude Status=REVIEW + Status=Review + Notes=manifest from $build-manifest.output verbatim\n      + claimed_by=Devils Cauldron + plan=B.\n\n      If Notion MCP unavailable in runtime, emit:\n        XO_TODO: Flip WO-SHOPOPS-RECEIVING-INTAKE-01 to REVIEW; paste manifest into Notes.\n\n      ## Manifest\n      $build-manifest.output\n",
  "bdc-shopops-shipment-reconciliation-ui": "# Drafted by yaml-author for WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01 on 2026-05-13\n# Source pattern: WO-POS-TERMINAL-SDK-01.yaml (cross-repo, MIXED but ROUGH-OK polish)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight (4-block):\n#   1. YAML exists: this file\n#   2. Spec exists: docs/work-orders/WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01.md in bdc-xo\n#   3. CWD scope: MIXED cross-repo (shopops primary CWD + lspro-react sibling /tmp clone)\n#   4. Codebases registered: bluedevilcollectibles/shopops AND bluedevilcollectibles/lspro-react\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01.yaml\n# REVIEW BEFORE DEPLOY — operator confirms shopops-api has read access to shipment_items\n# table (verify with curl to information_schema.columns).\n#\n# Polish bar: ROUGH OK — admin tool, manual workaround acceptable. V1 limitations in spec §13.\n\nname: bdc-shopops-shipment-reconciliation-ui\ndescription: |\n  Use when: Executing WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01 — distributor invoice vs\n    shipment reconciliation. CSV-upload-only v1.\n  Input: WO_ID = WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01.\n  Does:\n    - Adds shopops-api/routes/distributor-reconcile.js (thin compare endpoint).\n    - Adds lspro-react admin page with CSV upload + discrepancy table.\n    - Runs 4-test minimum (3 backend + 1 frontend). Skips exhaustive edge-case tests.\n    - Builds MIXED manifest and flips Notion to REVIEW.\n  Repo: shopops (primary CWD), lspro-react (sibling /tmp clone).\n  parallel_ok: true.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01}\"\n      # gh --template uses Go templates (built-in, no jq dependency).\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n--- SPEC ---\\n%s\\n' \\\n        \"$WO_ID\" \"$WORKFLOW_ID\" \"$SPEC_CONTENT\"\n\n  - id: fresh-branch\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      # Defensive: if default branch is held by another worktree, can't switch to it.\n      git checkout \"$DEFAULT_BRANCH\" 2>/dev/null \\\n        || { echo \"branch held by other worktree; staying on current HEAD\"; }\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n\n      BASE_BRANCH=\"wo/shopops-shipment-reconciliation-ui-01\"\n      # Default to a unique timestamped branch (worktree-safe on rerun).\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      if ! git worktree list --porcelain | grep -qx \"branch refs/heads/$BASE_BRANCH\"; then\n        if [ \"$(git rev-parse --abbrev-ref HEAD)\" = \"$BASE_BRANCH\" ]; then\n          BRANCH=\"$BASE_BRANCH\"\n        elif ! git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n          BRANCH=\"$BASE_BRANCH\"\n        fi\n      fi\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: schema-verify\n    depends_on: [fresh-branch]\n    bash: |\n      # set -e omitted — best-effort\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"\n        echo \"DETAIL=Verify shipment_items columns before writing reconcile SQL\"\n        exit 0\n      fi\n      R=$(curl -sf \\\n        \"${SUPABASE_URL}/rest/v1/information_schema.columns?table_name=eq.shipment_items&select=column_name\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n        -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n      echo \"STATUS=ok\"\n      echo \"SHIPMENT_ITEMS_COLUMNS=${R}\"\n      exit 0\n\n  - id: plan\n    depends_on: [read-spec, schema-verify]\n    task_class: medium_work\n    prompt: |\n      Plan WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01.\n\n      ## Required outputs\n      - Files to create (shopops): shopops-api/routes/distributor-reconcile.js,\n        shopops-api/tests/test_distributor_reconcile.js\n      - Files to modify (shopops): shopops-api/index.js (mount new router)\n      - Files to create (lspro-react): src/pages/admin/ShipmentReconciliationPage.tsx,\n        src/pages/admin/__tests__/ShipmentReconciliationPage.test.tsx,\n        src/lib/csv-parse-distributor.ts\n      - Test commands:\n        - bun test shopops-api/tests/test_distributor_reconcile.js (3 tests)\n        - bun test lspro-react/src/pages/admin (1 test)\n      - V1 limitations (acceptable, ROUGH OK): CSV-only (no PDF), fixed template,\n        no persistence, no auto-credit-memo, no fuzzy SKU match. Skip exhaustive\n        edge-case tests.\n\n      ## Spec\n      $read-spec.output\n\n      ## Schema verify\n      $schema-verify.output\n\n  - id: implement-shopops\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement shopops-api half of WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01.\n        CWD = shopops repo root.\n\n        ## Plan\n        $plan.output\n\n        ## Execution rules\n        1. Create shopops-api/routes/distributor-reconcile.js exposing:\n             POST /distributor/reconcile\n           Body: { start_date, end_date, lines: [{ sku, qty_invoiced, unit_price_cents, total_cents }] }\n           Logic: query shipment_items joined to shipments WHERE received_at BETWEEN start AND end\n                  AND tenant matches; join body lines by sku; compute qty_diff +\n                  dollars_diff_cents + category ('Overcharged' / 'Missing item' /\n                  'Surplus' / 'Match').\n           Return { summary, rows }.\n        2. Modify shopops-api/index.js: app.use('/distributor/reconcile', require('./routes/distributor-reconcile'))\n        3. Create shopops-api/tests/test_distributor_reconcile.js with bun:test.\n           AT MINIMUM 3 tests:\n             - match (0 discrepancies)\n             - overcharged\n             - missing item\n           Mock pg pool via factory. ROUGH-OK polish bar — do NOT add exhaustive\n           edge-case tests.\n        4. Verify column names from $schema-verify.output before writing SQL.\n        5. Do NOT push.\n\n        Output <promise>COMPLETE</promise> when shopops files exist + 3 bun tests pass.\n\n  - id: implement-lspro\n    depends_on: [implement-shopops]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 4\n      prompt: |\n        Implement lspro-react half of WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01.\n\n        Clone lspro-react into sibling path:\n          git -C /tmp clone --depth=10 https://github.com/bluedevilcollectibles/lspro-react.git lspro-react\n          cd /tmp/lspro-react\n          git checkout -b wo/shopops-shipment-reconciliation-ui-01-ui\n\n        ## Execution rules\n        1. Create src/lib/csv-parse-distributor.ts — pure function. Validates headers\n           ['sku', 'title', 'qty_invoiced', 'unit_price_cents', 'total_cents'].\n           Returns { ok: true, lines: [...] } | { ok: false, error: '...', line?: number }.\n        2. Create src/pages/admin/ShipmentReconciliationPage.tsx per UI Hierarchy §5.\n           UploadCard + ResultsCard + ExportButton (browser-side CSV download).\n           Use existing apiFetch helper for POST /distributor/reconcile.\n        3. Create src/pages/admin/__tests__/ShipmentReconciliationPage.test.tsx with\n           vitest. AT MINIMUM 1 test — CSV missing 'sku' header shows error banner.\n           ROUGH-OK polish bar.\n        4. Mount route at /admin/receiving/reconcile in the existing router.\n        5. Commit identity: John Ranson / admin@bluedevilcollectibles.com.\n        6. Push branch.\n\n        Output <promise>COMPLETE</promise> when files exist + branch pushed.\n\n  - id: verify-grep\n    depends_on: [implement-shopops, implement-lspro]\n    bash: |\n      set -euo pipefail\n      echo \"=== shopops ===\"\n      ls shopops-api/routes/distributor-reconcile.js\n      grep -q \"distributor/reconcile\\|/reconcile\" shopops-api/routes/distributor-reconcile.js\n      grep -q \"Overcharged\\|Missing item\\|Surplus\" shopops-api/routes/distributor-reconcile.js\n      ls shopops-api/tests/test_distributor_reconcile.js\n      TC=$(grep -c \"it(\\|test(\" shopops-api/tests/test_distributor_reconcile.js || echo 0)\n      [ \"$TC\" -ge 3 ] || (echo \"FAIL backend test count $TC < 3\" >&2; exit 1)\n      grep -q \"distributor-reconcile\\|/distributor/reconcile\" shopops-api/index.js\n      echo \"=== lspro-react sibling ===\"\n      LR=/tmp/lspro-react\n      ls \"$LR/src/lib/csv-parse-distributor.ts\"\n      ls \"$LR/src/pages/admin/ShipmentReconciliationPage.tsx\"\n      grep -q \"qty_invoiced\\|distributor\" \"$LR/src/lib/csv-parse-distributor.ts\"\n      echo \"verify-grep PASS\"\n\n  - id: verify-tests\n    depends_on: [implement-shopops, implement-lspro]\n    bash: |\n      set -euo pipefail\n      cd shopops-api && bun test tests/test_distributor_reconcile.js\n      cd /tmp/lspro-react && bun test src/pages/admin || \\\n        echo \"frontend tests reported failures; CI on Vercel is authoritative\"\n\n  - id: commit-shopops\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      # Read actual branch from current HEAD (fresh-branch may have used unique suffix).\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      git add shopops-api/routes/distributor-reconcile.js shopops-api/index.js \\\n        shopops-api/tests/test_distributor_reconcile.js\n      if git diff --cached --quiet; then\n        echo \"No shopops staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01 — distributor reconcile endpoint\\n\\nAdds POST /distributor/reconcile (thin compare endpoint).\\nJoins request lines (parsed from owner CSV) with shipment_items in date range\\nand returns discrepancy summary + per-row category.\\n\\nROUGH-OK polish bar — CSV-only v1, fixed template. See spec §13 for v1 limitations.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"SHOPOPS_SHA=$(git rev-parse HEAD)\"\n      echo \"SHOPOPS_BRANCH=${BRANCH}\"\n\n  - id: open-pr-shopops\n    depends_on: [commit-shopops]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{\"\\n\"}}{{end}}' 2>/dev/null | head -1 || true)\n      if [ -n \"$EXIST\" ]; then echo \"SHOPOPS_PR=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo \"$REPO\" --base master --head \"$BRANCH\" \\\n            --title \"feat(api): WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01 — distributor reconcile\" \\\n            --body \"ROUGH-OK polish bar. CSV-only v1. See bdc-xo:docs/work-orders/WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01.md\" 2>&1); then\n          echo \"SHOPOPS_PR=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      echo \"open-pr-shopops failed\" >&2; exit 1\n\n  - id: commit-and-pr-lspro\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      cd /tmp/lspro-react\n      git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n        add src/lib/csv-parse-distributor.ts src/pages/admin\n      if git diff --cached --quiet; then\n        echo \"No lspro staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(admin): WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01 — CSV reconcile page\\n\\nAdds /admin/receiving/reconcile page (CSV upload + discrepancy table).\\nAdds src/lib/csv-parse-distributor.ts pure parser.\\n\\nROUGH-OK polish bar.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin wo/shopops-shipment-reconciliation-ui-01-ui\n      EXIST=$(gh pr list --repo bluedevilcollectibles/lspro-react --head wo/shopops-shipment-reconciliation-ui-01-ui --state open \\\n        --json url --template '{{range .}}{{.url}}{{\"\\n\"}}{{end}}' 2>/dev/null | head -1 || true)\n      if [ -n \"$EXIST\" ]; then echo \"LSPRO_PR=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo bluedevilcollectibles/lspro-react --base main --head wo/shopops-shipment-reconciliation-ui-01-ui \\\n            --title \"feat(admin): WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01 — CSV reconcile page\" \\\n            --body \"ROUGH-OK polish bar. Staging URL: https://staging.livesellerpro.app/admin/receiving/reconcile\" 2>&1); then\n          echo \"LSPRO_PR=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      echo \"open-pr-lspro failed\" >&2; exit 1\n\n  - id: runtime-verify\n    depends_on: [open-pr-shopops]\n    bash: |\n      RESULT=$(docker exec shopops-api grep -n \"distributor/reconcile\" /app/routes/distributor-reconcile.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RULE19=${RESULT}\"\n      elif echo \"$RESULT\" | grep -qiE 'No such (container|file)|not found'; then\n        echo \"STATUS=skipped_container_not_rebuilt\"\n        echo \"RERUN=docker exec shopops-api grep -n 'distributor/reconcile' /app/routes/distributor-reconcile.js\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${RESULT}\"\n      fi\n      exit 0\n\n  - id: build-manifest\n    depends_on: [runtime-verify, commit-and-pr-lspro]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 for WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01 (MIXED class, ROUGH-OK).\n\n      WO: WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01\n      Builder: Devils Cauldron (Archon bdc-shopops-shipment-reconciliation-ui)\n      Polish bar: ROUGH OK (admin tool; v1 limitations in spec §13)\n\n      Files created (shopops):\n       - shopops-api/routes/distributor-reconcile.js\n       - shopops-api/tests/test_distributor_reconcile.js\n      Files modified (shopops):\n       - shopops-api/index.js\n      Files created (lspro-react):\n       - src/lib/csv-parse-distributor.ts\n       - src/pages/admin/ShipmentReconciliationPage.tsx\n       - src/pages/admin/__tests__/ShipmentReconciliationPage.test.tsx\n\n      Tests:\n       - bun test shopops-api/tests/test_distributor_reconcile.js -> [from $verify-tests.output]\n       - bun test lspro-react/src/pages/admin -> [from $verify-tests.output]\n\n      PRs:\n       - shopops: [from $open-pr-shopops.output SHOPOPS_PR]\n       - lspro-react: [from $commit-and-pr-lspro.output LSPRO_PR]\n\n      Merge ancestors:\n       - shopops master HEAD: [from $commit-shopops.output SHOPOPS_SHA], behind_by: 0\n       - lspro-react main HEAD: pending Captain CI\n\n      Grep assertions:\n       - grep -q '/distributor/reconcile' shopops-api/routes/distributor-reconcile.js\n       - grep -q 'Overcharged' shopops-api/routes/distributor-reconcile.js\n       - grep -c 'it(\\|test(' shopops-api/tests/test_distributor_reconcile.js >= 3\n       - ls lspro-react/src/lib/csv-parse-distributor.ts\n\n      Runtime verification:\n       - [from $runtime-verify.output]\n\n      Vercel deployment:\n       - lspro-react staging: https://staging.livesellerpro.app/admin/receiving/reconcile (pending Captain CI)\n\n      VALIDATION: PASS (pending container rebuild + Vercel deploy)\n\n      Inputs:\n      $read-spec.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-shopops.output\n      $open-pr-shopops.output\n      $commit-and-pr-lspro.output\n      $runtime-verify.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01 to REVIEW.\n      Manifest goes in Notes property (NOT comment).\n\n      Notion database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # OPEN QUESTION: page ID for WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01 not yet\n      # authored in Notion. Operator filters by exact WO ID equality before firing.\n\n      Update:\n       - Claude Status -> REVIEW\n       - Status -> Review\n       - Notes -> $build-manifest.output verbatim\n       - claimed_by: Devils Cauldron (Archon)\n       - claimed_at: today\n\n      If Notion MCP unavailable, output:\n        XO_TODO: Flip WO-SHOPOPS-SHIPMENT-RECONCILIATION-UI-01 to REVIEW; paste manifest.\n\n      Manifest:\n      $build-manifest.output\n",
  "bdc-shopops-shopify-order-sync": "# Drafted by yaml-author subagent for WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01 on 2026-05-13\n# Source pattern: bdc-feature-development.yaml + bdc-ce-regression-suite.yaml (gold standard)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n# Pre-flight: codebase bluedevilcollectibles/shopops must be registered in Archon\n#   Verify: sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name FROM remote_agent_codebases\"\n#   If missing: POST {\"path\":\"/.archon/workspaces/bluedevilcollectibles/shopops/source\"} to Archon API\n# Pre-flight: WO-SHOPOPS-COMMERCE-SPINE-01 must be DONE (channel_orders table must exist)\n#   Verify: docker exec shopops-api psql $DATABASE_URL -c \"\\d channel_orders\" (must not error)\n# Pre-flight: spec committed + pushed to origin on bdc-xo repo before firing\n#   Spec lives at: docs/work-orders/WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01.md\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01.yaml\n# REVIEW BEFORE DEPLOY — human verifies schema-audit tables, test thresholds, WO-specific prompts.\n#\n# Rule 19 note: shopops-api uses COPY . /app at build time — source changes need rebuild+restart.\n# Runtime verification grep is included in verify-runtime node and in the manifest.\n#\n# OPEN QUESTION: The spec at docs/work-orders/ is a non-standard path vs the usual\n# docs/superpowers/specs/ convention. This YAML handles BOTH paths in read-spec.\n# If this WO's spec is later moved to docs/superpowers/specs/, no YAML change needed.\n#\n# OPEN QUESTION: Notion page ID for WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01 is unknown at draft time.\n# The flip-notion node will search by WO ID via Notion MCP. Operator should paste the\n# canonical Notion page ID into the flip-notion prompt before deploy, replacing the\n# search-by-title fallback.\n\nname: bdc-shopops-shopify-order-sync\ndescription: |\n  Use when: Executing WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01 — Shopify order webhook ingestion.\n  Input: WO_ID = WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01.\n  Does: Reads spec from bdc-xo, reads prior art (routes/shopify-import.js + routes/webhooks.js),\n        verifies channel_orders table exists (SPINE WO dependency), implements\n        routes/shopify-webhooks.js + services/shopifyWebhookAuth.js + registers route in index.js,\n        writes 5+ bun tests, opens PR against main (shopops staging gate), builds manifest\n        with Rule 19 runtime verification, flips Notion to REVIEW.\n  Repo: bluedevilcollectibles/shopops (working path: shopops-api/ subdirectory).\n  Depends on: WO-SHOPOPS-COMMERCE-SPINE-01 (channel_orders table prerequisite).\n  parallel_ok: true (single repo, no schema migration, independent from other SHOPOPS WOs).\n  NOT for: any other WO.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01}\"\n\n      # CWD is shopops repo; spec lives in bdc-xo. Fetch via cross-repo gh api.\n      # gh --template uses Go templates (built into gh, no external jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      # Fallback: glob docs/superpowers/specs/*<WO_ID>*.md\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' \\\n          2>/dev/null | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:docs/work-orders/%s.md\\nWORKFLOW_ID=%s\\n\\n' \\\n        \"$WO_ID\" \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  - id: read-prior-art\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — audit step is informational; must not fail the workflow\n      # CWD is shopops repo root; shopops-api/ is a subdirectory.\n      # Read existing Shopify patterns before implementing to reuse proven patterns.\n      echo \"=== routes/shopify-import.js (lines 1-60: shopifyGet client pattern) ===\"\n      if [ -e shopops-api/routes/shopify-import.js ]; then\n        head -60 shopops-api/routes/shopify-import.js 2>/dev/null || true\n      else\n        echo \"FILE NOT FOUND: shopops-api/routes/shopify-import.js\"\n      fi\n      echo \"\"\n      echo \"=== routes/webhooks.js (if exists — existing webhook handler patterns) ===\"\n      if [ -e shopops-api/routes/webhooks.js ]; then\n        cat shopops-api/routes/webhooks.js 2>/dev/null || true\n      else\n        echo \"FILE NOT FOUND: shopops-api/routes/webhooks.js (no prior webhook handler)\"\n      fi\n      echo \"\"\n      echo \"=== index.js route registration block ===\"\n      if [ -e shopops-api/index.js ]; then\n        grep -n \"require\\|app\\.use\\|router\" shopops-api/index.js 2>/dev/null | head -40 || true\n      else\n        echo \"FILE NOT FOUND: shopops-api/index.js\"\n      fi\n      exit 0\n\n  - id: verify-schema\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step (DB may not be reachable from harness)\n      echo \"=== Schema verification: channel_orders table (prerequisite from WO-SHOPOPS-COMMERCE-SPINE-01) ===\"\n      RESULT=$(docker exec shopops-api psql \"$DATABASE_URL\" -c \"\n        SELECT column_name, data_type\n        FROM information_schema.columns\n        WHERE table_name = 'channel_orders' AND table_schema = 'public'\n        ORDER BY ordinal_position;\" 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"DETAIL=$RESULT\"\n        echo \"\"\n        echo \"=== Required columns check ===\"\n        for col in tenant_id channel external_order_id status total_cents raw_payload customer_id; do\n          if echo \"$RESULT\" | grep -q \"$col\"; then\n            echo \"PRESENT: $col\"\n          else\n            echo \"MISSING: $col\"\n          fi\n        done\n      elif echo \"$RESULT\" | grep -qiE 'does not exist|relation.*not found'; then\n        echo \"STATUS=failed_prerequisite\"\n        echo \"DETAIL=channel_orders table does not exist. WO-SHOPOPS-COMMERCE-SPINE-01 must be completed first.\"\n        echo \"\"\n        echo \"BLOCKER: Cannot proceed without channel_orders table.\"\n        exit 1\n      else\n        echo \"STATUS=skipped_db_unreachable\"\n        echo \"DETAIL=$RESULT\"\n        echo \"NOTE: DB not reachable from harness; schema verification deferred to runtime verify step.\"\n      fi\n      exit 0\n\n  - id: plan\n    depends_on: [read-spec, read-prior-art, verify-schema]\n    task_class: medium_work\n    prompt: |\n      Plan the Shopify order webhook ingestion implementation for shopops-api.\n\n      ## Context\n      - Repo: bluedevilcollectibles/shopops, working directory: shopops-api/\n      - Codebase is Node.js (Express, CommonJS modules), NOT TypeScript\n      - Tests use bun (not npm/node) — Archon container is bun-only\n      - This WO is CODE class, single repo, no schema migration\n\n      ## Required Plan Output (Markdown with these headings)\n      - WO ID: WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01\n      - Files to create (exact paths relative to shopops-api/ subdir)\n      - Files to modify (exact paths, what changes)\n      - Files explicitly out of scope\n      - Implementation notes per file\n      - Test file structure (min 5 test functions asserting real behavior)\n      - Env var requirements (.env.example entry)\n      - Verification commands (bun-based, not npm)\n      - Commit message\n      - Push policy: feature branch wo/shopops-shopify-order-sync-01, PR against main, staging gate required\n\n      ## Key Implementation Requirements from Spec\n      1. NEW routes/shopify-webhooks.js: POST /shopify-webhooks/orders/create and POST /shopify-webhooks/orders/updated\n      2. NEW services/shopifyWebhookAuth.js: verifyShopifyHmac(req) using X-Shopify-Hmac-Sha256 header + SHOPIFY_WEBHOOK_SECRET env\n      3. Status mapping: pending->new, paid->paid, fulfilled->fulfilled, refunded->refunded, cancelled->cancelled\n      4. Idempotent INSERT ON CONFLICT (tenant_id, channel, external_order_id) DO UPDATE\n      5. Route registered in index.js\n      6. Fail-closed on missing SHOPIFY_WEBHOOK_SECRET (500, not 401)\n      7. Extract tenant_id from X-Shopops-Tenant header\n      8. Customer match attempt: SELECT id FROM customers WHERE email = shopify_email AND tenant_id\n\n      ## Prior Art\n      $read-prior-art.output\n\n      ## Schema Verification\n      $verify-schema.output\n\n      ## Spec\n      $read-spec.output\n\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the Shopify order webhook ingestion for shopops-api per the plan.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Prior Art\n        $read-prior-art.output\n\n        ## Execution Rules\n        - Working directory context: the shopops repo root. All file paths start with shopops-api/\n        - Language: Node.js (CommonJS, require/module.exports), NOT TypeScript\n        - Do NOT modify routes/shopify-import.js or routes/shopify-push.js\n        - Do NOT reformat unrelated code\n        - Do NOT deploy or rebuild Docker containers\n        - Do NOT push directly to main\n        - Fail-closed pattern for SHOPIFY_WEBHOOK_SECRET (500 on missing, not 401)\n        - HMAC validation: use crypto.createHmac('sha256', secret).update(rawBody).digest('base64')\n          Compare to X-Shopify-Hmac-Sha256 header value using crypto.timingSafeEqual\n          IMPORTANT: body must be read as raw bytes (Buffer) before JSON.parse — use express.raw() or rawBody middleware\n        - Idempotency: INSERT INTO channel_orders ... ON CONFLICT (tenant_id, channel, external_order_id) DO UPDATE SET status=EXCLUDED.status, raw_payload=EXCLUDED.raw_payload, updated_at=NOW()\n        - Tests in shopops-api/tests/shopify-webhooks.test.js using bun test (not jest/vitest/mocha)\n        - Tests must assert actual behavior: HTTP status codes, DB row existence, idempotency count, HMAC rejection, customer linkage\n        - .env.example must get SHOPIFY_WEBHOOK_SECRET=your-shopify-webhook-secret-here entry\n\n        ## Test Requirements (min 5 test functions, each asserting real behavior)\n        Write tests for ALL of:\n        1. HMAC validation rejects wrong signature (expects 401)\n        2. Missing SHOPIFY_WEBHOOK_SECRET returns 500 (fail-closed)\n        3. Valid order create ingests row into channel_orders with correct status and total_cents\n        4. Duplicate webhook delivery is idempotent (COUNT = 1, not 2)\n        5. Customer email match populates channel_orders.customer_id (not NULL when customer exists)\n\n        ## Doctrine Baseline Tests\n        Write at least 5 test functions each asserting actual behavior (not \"did not throw\").\n        This is a backend route + HMAC service — target 6-8 tests total covering the happy path,\n        security boundary (HMAC), idempotency contract, and customer linkage.\n\n        ## Completion Criteria\n        Output <promise>COMPLETE</promise> only when:\n        - shopops-api/routes/shopify-webhooks.js exists with both POST handlers\n        - shopops-api/services/shopifyWebhookAuth.js exists with verifyShopifyHmac()\n        - shopops-api/index.js has require + app.use for shopify-webhooks route\n        - shopops-api/tests/shopify-webhooks.test.js exists with min 5 real assertions\n        - shopops-api/.env.example has SHOPIFY_WEBHOOK_SECRET entry (or .env.example at repo root)\n        - All stop conditions from spec §12 are satisfied\n\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      echo \"=== Grep assertions ===\"\n\n      echo \"--- routes/shopify-webhooks.js exists ---\"\n      ls shopops-api/routes/shopify-webhooks.js\n\n      echo \"--- services/shopifyWebhookAuth.js exists ---\"\n      ls shopops-api/services/shopifyWebhookAuth.js\n\n      echo \"--- tests/shopify-webhooks.test.js exists ---\"\n      ls shopops-api/tests/shopify-webhooks.test.js\n\n      echo \"--- verifyShopifyHmac present in auth service ---\"\n      grep -n \"verifyShopifyHmac\" shopops-api/services/shopifyWebhookAuth.js\n\n      echo \"--- shopify-webhooks route registered in index.js ---\"\n      grep -n \"shopify-webhooks\" shopops-api/index.js\n\n      echo \"--- HMAC header reference in webhook handler ---\"\n      grep -q \"X-Shopify-Hmac-Sha256\" shopops-api/routes/shopify-webhooks.js || \\\n        grep -q \"X-Shopify-Hmac-Sha256\" shopops-api/services/shopifyWebhookAuth.js || \\\n        (echo \"FAIL: X-Shopify-Hmac-Sha256 header not referenced\" >&2; exit 1)\n\n      echo \"--- SHOPIFY_WEBHOOK_SECRET env var referenced ---\"\n      grep -q \"SHOPIFY_WEBHOOK_SECRET\" shopops-api/services/shopifyWebhookAuth.js || \\\n        grep -q \"SHOPIFY_WEBHOOK_SECRET\" shopops-api/routes/shopify-webhooks.js || \\\n        (echo \"FAIL: SHOPIFY_WEBHOOK_SECRET not referenced\" >&2; exit 1)\n\n      echo \"--- .env.example has SHOPIFY_WEBHOOK_SECRET entry ---\"\n      ENV_EXAMPLE=$(find shopops-api -name \".env.example\" -maxdepth 2 2>/dev/null | head -1)\n      if [ -z \"$ENV_EXAMPLE\" ]; then\n        ENV_EXAMPLE=\".env.example\"\n      fi\n      grep -q \"SHOPIFY_WEBHOOK_SECRET\" \"$ENV_EXAMPLE\" || \\\n        (echo \"FAIL: SHOPIFY_WEBHOOK_SECRET missing from $ENV_EXAMPLE\" >&2; exit 1)\n\n      echo \"--- ON CONFLICT idempotent upsert present ---\"\n      grep -q \"ON CONFLICT\" shopops-api/routes/shopify-webhooks.js || \\\n        (echo \"FAIL: ON CONFLICT upsert not found in webhook handler\" >&2; exit 1)\n\n      echo \"--- Test function count (min 5) ---\"\n      TEST_COUNT=$(grep -c \"test\\|it(\" shopops-api/tests/shopify-webhooks.test.js 2>/dev/null || echo 0)\n      echo \"Test functions found: $TEST_COUNT\"\n      if [ \"$TEST_COUNT\" -lt 5 ]; then\n        echo \"FAIL: fewer than 5 test functions in shopify-webhooks.test.js\" >&2\n        exit 1\n      fi\n\n      echo \"=== All grep assertions passed ===\"\n\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      echo \"=== Running bun test on shopify-webhooks test suite ===\"\n      # Archon container is bun-only — no npm/node fallback\n      if [ -f tests/shopify-webhooks.test.js ]; then\n        bun test tests/shopify-webhooks.test.js || {\n          echo \"Tests failed. Check test output above for failures.\" >&2\n          echo \"NOTE: If DB is required but unavailable in harness, tests may fail here.\"\n          echo \"In that case, operator must verify tests pass in local/staging environment.\"\n          exit 1\n        }\n      else\n        echo \"FAIL: tests/shopify-webhooks.test.js not found\" >&2\n        exit 1\n      fi\n\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      BASE_BRANCH=\"wo/shopops-shopify-order-sync-01\"\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n\n      # Idempotent checkout: prefer stable BASE_BRANCH unless it's locked to another worktree\n      if git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE_BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE_BRANCH\" ]; then\n          # Branch is locked to another worktree — use timestamped variant\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE_BRANCH\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n        fi\n      else\n        BRANCH=\"$BASE_BRANCH\"\n        git checkout -b \"$BRANCH\"\n      fi\n\n      # Stage only the WO-scoped files\n      git add shopops-api/routes/shopify-webhooks.js \\\n               shopops-api/services/shopifyWebhookAuth.js \\\n               shopops-api/index.js \\\n               shopops-api/tests/shopify-webhooks.test.js\n\n      # Stage .env.example wherever it lives\n      find shopops-api -name \".env.example\" -maxdepth 2 | xargs git add 2>/dev/null || true\n      find . -maxdepth 1 -name \".env.example\" | xargs git add 2>/dev/null || true\n\n      # Idempotent commit: skip if branch already contains the work\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"feat(shopops): WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01 — Shopify order webhook ingestion\n\n        Adds Shopify order webhook handler with HMAC signature validation.\n\n        New files:\n        - shopops-api/routes/shopify-webhooks.js (POST /shopify-webhooks/orders/create + /updated)\n        - shopops-api/services/shopifyWebhookAuth.js (verifyShopifyHmac using timing-safe comparison)\n        - shopops-api/tests/shopify-webhooks.test.js (min 5 tests: HMAC, ingest, idempotency, customer linkage)\n\n        Modified:\n        - shopops-api/index.js (registers /shopify-webhooks route)\n        - .env.example (adds SHOPIFY_WEBHOOK_SECRET placeholder)\n\n        Status mapping: pending->new, paid->paid, fulfilled->fulfilled, refunded->refunded, cancelled->cancelled\n        Idempotent upsert: ON CONFLICT (tenant_id, channel, external_order_id) DO UPDATE\n        Depends on: WO-SHOPOPS-COMMERCE-SPINE-01 (channel_orders table)\"\n      fi\n\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Use current HEAD branch (set by commit-and-push — may be timestamped variant)\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BASE=\"main\"\n      TITLE=\"feat(shopops): WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01 — Shopify order webhook ingestion\"\n\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01** — Shopify order webhook ingest.\"\n        echo \"\"\n        echo \"Orders placed on a connected Shopify store flow into the \\`channel_orders\\` table\"\n        echo \"via HMAC-validated webhooks. Idempotent upsert prevents duplicate rows on retry.\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- NEW \\`shopops-api/routes/shopify-webhooks.js\\` — POST /shopify-webhooks/orders/create + /updated\"\n        echo \"- NEW \\`shopops-api/services/shopifyWebhookAuth.js\\` — verifyShopifyHmac() (timing-safe)\"\n        echo \"- NEW \\`shopops-api/tests/shopify-webhooks.test.js\\` — min 5 bun tests\"\n        echo \"- MOD \\`shopops-api/index.js\\` — route registration\"\n        echo \"- MOD \\`.env.example\\` — SHOPIFY_WEBHOOK_SECRET placeholder\"\n        echo \"\"\n        echo \"## Status Mapping\"\n        echo \"| Shopify financial_status | channel_orders.status |\"\n        echo \"|---|---|\"\n        echo \"| pending | new |\"\n        echo \"| paid | paid |\"\n        echo \"| refunded | refunded |\"\n        echo \"| fulfilled (fulfillment_status) | fulfilled |\"\n        echo \"| cancelled | cancelled |\"\n        echo \"\"\n        echo \"## Prerequisite\"\n        echo \"Depends on WO-SHOPOPS-COMMERCE-SPINE-01 (channel_orders table).\"\n        echo \"\"\n        echo \"## Security\"\n        echo \"- HMAC-SHA256 via X-Shopify-Hmac-Sha256 header + SHOPIFY_WEBHOOK_SECRET env\"\n        echo \"- Fail-closed: 500 on missing secret, 401 on invalid HMAC\"\n        echo \"- Timing-safe comparison (crypto.timingSafeEqual)\"\n        echo \"\"\n        echo \"## Env Var\"\n        echo \"\\`SHOPIFY_WEBHOOK_SECRET\\` must be added to Hetzner .env before first webhook delivery.\"\n      } > \"$BODY_FILE\"\n\n      # Idempotent PR creation\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n          --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  - id: verify-runtime\n    depends_on: [open-pr]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step (container may need rebuild first)\n      # Rule 19: shopops-api uses COPY . /app at build — source changes need rebuild+restart.\n      # This step provides best-effort evidence; operator must rebuild container before it passes.\n      echo \"=== Rule 19: Runtime verification (shopops-api) ===\"\n      echo \"NOTE: This will fail until operator rebuilds + restarts shopops-api container.\"\n      echo \"Command for operator: ssh hetzner-prod 'cd /opt/bdc/shopops && docker compose build shopops-api && docker compose up -d shopops-api'\"\n      echo \"\"\n\n      RESULT1=$(docker exec shopops-api grep -n \"shopify-webhooks\" /app/index.js 2>&1)\n      EXIT1=$?\n      if [ $EXIT1 -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"ROUTE_REGISTRATION: $RESULT1\"\n      else\n        echo \"STATUS=pending_rebuild\"\n        echo \"DETAIL=shopify-webhooks not yet live in /app/index.js (container needs rebuild)\"\n        echo \"EXPECTED_COMMAND: docker exec shopops-api grep -n 'shopify-webhooks' /app/index.js\"\n        echo \"EXPECTED_OUTPUT: line with require('routes/shopify-webhooks') or similar\"\n      fi\n\n      echo \"\"\n      RESULT2=$(docker exec shopops-api grep -n \"verifyShopifyHmac\" /app/routes/shopify-webhooks.js 2>&1)\n      EXIT2=$?\n      if [ $EXIT2 -eq 0 ]; then\n        echo \"HMAC_FUNCTION: $RESULT2\"\n      else\n        echo \"HMAC_STATUS=pending_rebuild\"\n        echo \"EXPECTED_COMMAND: docker exec shopops-api grep -n 'verifyShopifyHmac' /app/routes/shopify-webhooks.js\"\n      fi\n\n      exit 0\n\n  - id: build-manifest\n    depends_on: [open-pr, verify-runtime]\n    task_class: clerical\n    prompt: |\n      Build Captain CI manifest v2 for WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01.\n\n      ## Required manifest v2 fields (CLAUDE.md Rule 2)\n      WO: WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01\n      Builder: Major Build (bdc-harness)\n      Files modified: [exact list from implement output]\n      Files created: [exact list from implement output]\n      Tests: [count passing / count total — from verify-tests output, command: bun test shopops-api/tests/shopify-webhooks.test.js]\n      PRs: [PR URL from open-pr output, not yet merged]\n      Merge ancestors: [shopops main HEAD at time of branch creation, manifest commit, behind_by]\n      Grep assertions:\n        docker exec shopops-api grep -n 'shopify-webhooks' /app/index.js\n        docker exec shopops-api grep -n 'verifyShopifyHmac' /app/routes/shopify-webhooks.js\n        grep -c 'ON CONFLICT' shopops-api/routes/shopify-webhooks.js\n      Runtime verification: [from verify-runtime output — STATUS=ok if container rebuilt, STATUS=pending_rebuild if not yet]\n      Vercel deployment: N/A (shopops-api is Hetzner Docker, not Vercel)\n      VALIDATION: PASS\n\n      ## Rule 19 note\n      shopops-api runtime verification requires container rebuild + restart after PR merge.\n      Include rebuild command: ssh hetzner-prod 'cd /opt/bdc/shopops && docker compose build shopops-api && docker compose up -d shopops-api'\n      Post-rebuild verification: docker exec shopops-api grep -n 'shopify-webhooks' /app/index.js\n\n      ## Inputs\n      $read-spec.output\n      $plan.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-and-push.output\n      $open-pr.output\n      $verify-runtime.output\n\n  # post-started and post-completed webhook nodes OMITTED — Rule 11:\n  # curl to n8n.bluedevilcollectibles.com from Archon executor subprocess hangs >=120s.\n  # flip-notion is the canonical close action.\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01 to REVIEW with the manifest in the Notes property.\n\n      IMPORTANT: The manifest goes in the Notes PROPERTY, NOT as a page comment.\n      Captain CI reads the Notes property. Page comments are invisible to CI.\n\n      ## Steps\n      1. Find the Notion page for WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01.\n         Search the WO database (a6df831c-0b52-449f-8ca4-d77be6b70d0a) by title.\n         If you have a page ID, use it directly; if not, search by WO ID in the title.\n\n      2. In a SINGLE Notion update call, set ALL of:\n         - Claude Status property -> REVIEW\n         - Status property -> Review\n         - Notes property -> paste $build-manifest.output verbatim (the full manifest v2 block)\n         - claimed_by: Major Build (bdc-harness)\n         - claimed_at: 2026-05-13\n\n      3. Confirm the update succeeded.\n\n      ## Manifest\n      $build-manifest.output\n\n      ## Fallback\n      If Notion MCP is unavailable in this runtime, output the following for XO to action:\n      XO_TODO: Flip WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01 to REVIEW in Notion.\n      Set Notes property (NOT comment) to the manifest above.\n      Set Claude Status = REVIEW, Status = Review.\n",
  "bdc-shopops-woocommerce-connector": "# Drafted by yaml-author subagent for WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01 on 2026-05-13\n# Source pattern: WO-SHOPOPS-SHOPIFY-ORDER-SYNC-01.yaml (direct sibling connector) +\n#                 WO-SHOPOPS-COMMERCE-SPINE-01.yaml (schema-verify + Rule 19 runtime pattern)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight: codebase bluedevilcollectibles/shopops must be registered in Archon.\n#   Verify: sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name FROM remote_agent_codebases\"\n#   If missing: curl -X POST https://archon.bluedevilcollectibles.com/api/codebases \\\n#     -H \"Content-Type: application/json\" \\\n#     -d '{\"name\":\"bluedevilcollectibles/shopops\",\"repositoryUrl\":\"https://github.com/bluedevilcollectibles/shopops\",\"defaultBranch\":\"master\",\"defaultCwd\":\"/.archon/workspaces/bluedevilcollectibles/shopops/source\"}'\n#\n# Pre-flight: WO-SHOPOPS-COMMERCE-SPINE-01 must be DONE.\n#   All three spine tables must exist in staging Supabase:\n#   docker exec shopops-api psql \"$DATABASE_URL\" -c \"\\d channel_connections\" (must not error)\n#   docker exec shopops-api psql \"$DATABASE_URL\" -c \"\\d product_channel_map\" (must not error)\n#   docker exec shopops-api psql \"$DATABASE_URL\" -c \"\\d channel_orders\" (must not error)\n#\n# Pre-flight: WO spec committed and pushed to bdc-xo \"origin/$DEFAULT_BRANCH\" before firing.\n#   Spec path: docs/work-orders/WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01.md\n#\n# NOTE ON CWD: Archon CWD = shopops repo root. shopops-api/ is a subdirectory.\n#   All file paths in bash nodes are relative to shopops repo root.\n#   The WO spec lives in bdc-xo — fetched via gh api cross-repo.\n#\n# NOTE: parallel_ok=YES per WO spec (single repo, no schema migration).\n#\n# Rule 19 note: shopops-api uses COPY . /app at image build — source changes need\n#   rebuild + restart to take effect. Runtime verify node provides evidence commands.\n#   Operator runs: ssh hetzner-prod 'cd /opt/bdc/shopops && docker compose build shopops-api && docker compose up -d shopops-api'\n#   Then re-runs: docker exec shopops-api grep -n 'woocommerce-webhooks' /app/index.js\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01.yaml\n# REVIEW BEFORE DEPLOY — human verifies schema-audit tables, test thresholds, WO-specific prompts.\n#\n# OPEN QUESTION: Notion page ID for WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01 is not confirmed at\n# draft time. The flip-notion node searches by WO ID via Notion MCP. Operator should paste the\n# canonical Notion page ID into the flip-notion prompt before deploy to avoid search latency.\n#\n# OPEN QUESTION: shopops repo default branch is 'master' per WO-SHOPOPS-COMMERCE-SPINE-01.yaml.\n# If the repo has since been renamed to 'main', update BASE= in commit-and-push and open-pr.\n\nname: bdc-shopops-woocommerce-connector\ndescription: |\n  Use when: Executing WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01 — WooCommerce connector.\n  Input: WO_ID = WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01.\n  Does: Reads spec from bdc-xo (cross-repo via gh api), reads Shopify connector prior art\n        (routes/shopify-import.js, routes/shopify-push.js, routes/shopify-webhooks.js),\n        verifies channel_connections + product_channel_map + channel_orders tables exist\n        (SPINE WO dependency), implements:\n          - routes/woocommerce-import.js  (POST /woocommerce-import/preview + /commit, GET /batches)\n          - routes/woocommerce-push.js    (POST /woocommerce-push/preview + /commit)\n          - routes/woocommerce-webhooks.js (POST /woocommerce-webhooks/order, X-WC-Webhook-Signature)\n          - services/woocommerceClient.js  (wcGet, wcPost, verifyWcWebhookSignature)\n        validates WOOCOMMERCE_WEBHOOK_SECRET env var (fail-closed),\n        writes min 5 bun tests in tests/woocommerce.test.js,\n        commits to fresh branch wo/shopops-woocommerce-connector-01, opens PR against master,\n        provides Rule 19 runtime verification evidence, builds manifest, flips Notion to REVIEW.\n  Repo: bluedevilcollectibles/shopops (shopops-api/ subdir).\n        Spec lives in bluedevilcollectibles/bdc-xo.\n  Depends on: WO-SHOPOPS-COMMERCE-SPINE-01 (channel_connections + product_channel_map + channel_orders).\n  parallel_ok: true (single repo, no schema migration, independent from Shopify connector WO).\n  NOT for: any other WO.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  # ---------------------------------------------------------------------------\n  # 1. READ-SPEC — fetch WO from bdc-xo cross-repo + behavior spec\n  # ---------------------------------------------------------------------------\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01}\"\n\n      # Primary: docs/work-orders/<WO_ID>.md in bdc-xo\n      # gh --template uses Go templates (built into gh, no external jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      # Fallback: glob docs/superpowers/specs/*<WO_ID>*.md\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' \\\n          2>/dev/null | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      # Also fetch behavior spec for connector contract context\n      BEHAVIOR_SPEC=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/behavior-specs/COMMERCE_CHANNEL_SPINE_BEHAVIOR.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:docs/work-orders/%s.md\\nWORKFLOW_ID=%s\\n\\n' \\\n        \"$WO_ID\" \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n\\n' \"$SPEC_CONTENT\"\n      if [ -n \"$BEHAVIOR_SPEC\" ]; then\n        printf -- '--- BEHAVIOR SPEC (COMMERCE_CHANNEL_SPINE_BEHAVIOR.md, excerpt) ---\\n%s\\n' \"$BEHAVIOR_SPEC\"\n      fi\n\n  # ---------------------------------------------------------------------------\n  # 2. READ-PRIOR-ART — load Shopify connector patterns for direct reuse\n  # ---------------------------------------------------------------------------\n  - id: read-prior-art\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — audit step is informational; must not fail the workflow\n      # CWD is shopops repo root. shopops-api/ is subdirectory.\n\n      echo \"=== routes/shopify-import.js (product import preview+commit pattern) ===\"\n      if [ -e shopops-api/routes/shopify-import.js ]; then\n        cat shopops-api/routes/shopify-import.js 2>/dev/null || true\n      else\n        echo \"FILE NOT FOUND: shopops-api/routes/shopify-import.js\"\n      fi\n      echo \"\"\n\n      echo \"=== routes/shopify-push.js (product push pattern) ===\"\n      if [ -e shopops-api/routes/shopify-push.js ]; then\n        cat shopops-api/routes/shopify-push.js 2>/dev/null || true\n      else\n        echo \"FILE NOT FOUND: shopops-api/routes/shopify-push.js\"\n      fi\n      echo \"\"\n\n      echo \"=== routes/shopify-webhooks.js (webhook ingest + signature validation pattern) ===\"\n      if [ -e shopops-api/routes/shopify-webhooks.js ]; then\n        cat shopops-api/routes/shopify-webhooks.js 2>/dev/null || true\n      else\n        echo \"FILE NOT FOUND: shopops-api/routes/shopify-webhooks.js\"\n      fi\n      echo \"\"\n\n      echo \"=== services/ directory (existing service files for pattern reference) ===\"\n      if [ -d shopops-api/services ]; then\n        ls shopops-api/services/ 2>/dev/null || true\n      else\n        echo \"No services/ directory found\"\n      fi\n      echo \"\"\n\n      echo \"=== index.js route registration block ===\"\n      if [ -e shopops-api/index.js ]; then\n        grep -n \"require\\|app\\.use\\|router\" shopops-api/index.js 2>/dev/null | head -60 || true\n      else\n        echo \"FILE NOT FOUND: shopops-api/index.js\"\n      fi\n      echo \"\"\n\n      echo \"=== routes/channels.js woocommerce stub (connector hook point) ===\"\n      if [ -e shopops-api/routes/channels.js ]; then\n        grep -n -A2 -B2 \"woocommerce\" shopops-api/routes/channels.js 2>/dev/null || \\\n          echo \"No woocommerce references in channels.js yet\"\n      else\n        echo \"FILE NOT FOUND: shopops-api/routes/channels.js\"\n      fi\n      exit 0\n\n  # ---------------------------------------------------------------------------\n  # 3. VERIFY-SCHEMA — confirm all three spine tables exist (SPINE WO dependency)\n  #    Best-effort wrapper: DB may not be reachable from harness executor.\n  # ---------------------------------------------------------------------------\n  - id: verify-schema\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step (DB may not be reachable from harness)\n      echo \"=== Schema verification: WO-SHOPOPS-COMMERCE-SPINE-01 dependency check ===\"\n\n      check_table() {\n        local TABLE=\"$1\"\n        local RESULT\n        RESULT=$(docker exec shopops-api psql \"$DATABASE_URL\" -c \\\n          \"SELECT column_name FROM information_schema.columns WHERE table_name = '${TABLE}' AND table_schema = 'public' ORDER BY ordinal_position;\" \\\n          2>&1)\n        local EXIT=$?\n        if [ $EXIT -eq 0 ]; then\n          echo \"TABLE_${TABLE}=PRESENT\"\n          echo \"COLUMNS: $RESULT\"\n        elif echo \"$RESULT\" | grep -qiE 'does not exist|relation.*not found'; then\n          echo \"TABLE_${TABLE}=MISSING\"\n          echo \"BLOCKER: ${TABLE} not found. WO-SHOPOPS-COMMERCE-SPINE-01 must be completed first.\"\n        else\n          echo \"TABLE_${TABLE}=UNVERIFIED\"\n          echo \"DETAIL: $RESULT\"\n          echo \"NOTE: DB not reachable from harness; verification deferred to runtime verify step.\"\n        fi\n        echo \"\"\n      }\n\n      check_table \"channel_connections\"\n      check_table \"product_channel_map\"\n      check_table \"channel_orders\"\n\n      # Check WOOCOMMERCE_WEBHOOK_SECRET env var availability in running container\n      WC_SECRET_CHECK=$(docker exec shopops-api printenv WOOCOMMERCE_WEBHOOK_SECRET 2>&1 || true)\n      if [ -n \"$WC_SECRET_CHECK\" ] && ! echo \"$WC_SECRET_CHECK\" | grep -qiE 'No such container|error'; then\n        echo \"WOOCOMMERCE_WEBHOOK_SECRET=SET_IN_CONTAINER\"\n      else\n        echo \"WOOCOMMERCE_WEBHOOK_SECRET=NOT_SET_OR_CONTAINER_UNREACHABLE\"\n        echo \"NOTE: Operator must add WOOCOMMERCE_WEBHOOK_SECRET to /opt/bdc/shopops/.env before webhook delivery.\"\n      fi\n\n      exit 0\n\n  # ---------------------------------------------------------------------------\n  # 4. PLAN — architect the WooCommerce connector\n  # ---------------------------------------------------------------------------\n  - id: plan\n    depends_on: [read-spec, read-prior-art, verify-schema]\n    task_class: medium_work\n    prompt: |\n      Plan the WooCommerce connector implementation for shopops-api.\n\n      ## Required Plan Output (Markdown with these headings)\n      - WO ID: WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01\n      - Files to create (exact paths relative to shopops repo root)\n      - Files to modify (exact paths, what changes)\n      - Files explicitly out of scope\n      - Implementation notes per file\n      - Auth differences from Shopify (WC uses Basic Auth base64(key:secret), NOT X-Shopify-Access-Token)\n      - WC order status mapping table (7 mappings per spec §7)\n      - Test file structure for shopops-api/tests/woocommerce.test.js (min 5 bun test functions)\n      - Env var requirements (.env.example entry for WOOCOMMERCE_WEBHOOK_SECRET)\n      - Verification commands (all using bun — Archon container is bun-only, no npm/node)\n      - Commit message\n      - Branch name: wo/shopops-woocommerce-connector-01\n      - PR base: master (shopops repo default branch)\n      - Staging gate: required (shopops is in Rule 20 staging-gate list)\n\n      ## Key Implementation Requirements from Spec\n\n      ### services/woocommerceClient.js (NEW)\n      - wcGet(baseUrl, consumerKey, consumerSecret, path) — WC REST API GET\n        Auth: Authorization: Basic base64(consumerKey + ':' + consumerSecret)\n        Pattern: mirror shopifyGet from shopify-import.js but use Basic Auth header\n      - wcPost(baseUrl, consumerKey, consumerSecret, path, body) — WC REST API POST\n      - verifyWcWebhookSignature(req, secret) — validates X-WC-Webhook-Signature header\n        WC signature: HMAC-SHA256 of raw request body using webhook secret, base64 encoded\n        Builder must read WC webhook signature docs before implementing\n\n      ### routes/woocommerce-import.js (NEW)\n      - POST /woocommerce-import/preview — wcGet /wp-json/wc/v3/products, compare vs inventory_items, return IMPORT_CREATE/IMPORT_UPDATE/SKIP classify list\n      - POST /woocommerce-import/commit — upsert selected products into inventory_items + write product_channel_map rows\n      - GET /woocommerce-import/batches — list import batches\n\n      ### routes/woocommerce-push.js (NEW)\n      - POST /woocommerce-push/preview — wcGet /wp-json/wc/v3/products, compare vs inventory_items, return PUSH_CREATE/PUSH_UPDATE/SKIP classify list\n      - POST /woocommerce-push/commit — wcPost/wcPut to /wp-json/wc/v3/products, write product_channel_map rows\n\n      ### routes/woocommerce-webhooks.js (NEW)\n      - POST /woocommerce-webhooks/order — ingest WC order\n        1. verifyWcWebhookSignature — reject 401 if invalid\n        2. Extract tenant_id from X-Shopops-Tenant\n        3. Map WC status to channel_orders.status (7 mappings from spec §7)\n        4. INSERT INTO channel_orders ... ON CONFLICT (tenant_id, channel, external_order_id) DO UPDATE\n        5. Attempt customer match by email\n        6. WOOCOMMERCE_WEBHOOK_SECRET fail-closed: 500 if env var unset, not a silent pass\n      - Status mapping (from spec §7):\n        pending -> new\n        on-hold -> new\n        processing -> paid\n        completed -> fulfilled\n        cancelled -> cancelled\n        failed -> cancelled\n        refunded -> refunded\n\n      ## Prior Art (Shopify patterns to mirror)\n      $read-prior-art.output\n\n      ## Schema Verification\n      $verify-schema.output\n\n      ## Spec\n      $read-spec.output\n\n  # ---------------------------------------------------------------------------\n  # 5. IMPLEMENT — write all four files + route registration + tests\n  # ---------------------------------------------------------------------------\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the WooCommerce connector for shopops-api per the plan.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Prior Art (Shopify patterns to mirror)\n        $read-prior-art.output\n\n        ## Execution Rules\n        - CWD is the shopops repo root. All file paths start with shopops-api/\n        - Language: Node.js (CommonJS, require/module.exports), NOT TypeScript\n        - Archon container is bun-only — use bun for tests, not npm/node\n        - Do NOT modify routes/shopify-import.js, routes/shopify-push.js, or routes/shopify-webhooks.js\n        - Do NOT reformat unrelated code\n        - Do NOT deploy, rebuild Docker containers, or push to origin\n        - WOOCOMMERCE_WEBHOOK_SECRET fail-closed: if env var unset, respond 500 with\n          { error: 'WOOCOMMERCE_WEBHOOK_SECRET not configured' } — never silently accept\n        - WC Basic Auth: Authorization header = 'Basic ' + Buffer.from(key + ':' + secret).toString('base64')\n        - WC Webhook signature: HMAC-SHA256 of raw request body (Buffer) using WOOCOMMERCE_WEBHOOK_SECRET\n          base64 encode the digest, compare to X-WC-Webhook-Signature header using crypto.timingSafeEqual\n          IMPORTANT: raw body must be captured before JSON.parse — use express.raw() middleware on this route\n        - Idempotent upsert: INSERT INTO channel_orders (tenant_id, channel, external_order_id, status,\n          total_cents, raw_payload) VALUES ... ON CONFLICT (tenant_id, channel, external_order_id)\n          DO UPDATE SET status=EXCLUDED.status, raw_payload=EXCLUDED.raw_payload, updated_at=NOW()\n        - Register all three routes in shopops-api/index.js (require + app.use)\n        - Add WOOCOMMERCE_WEBHOOK_SECRET=your-woocommerce-webhook-secret-here to .env.example\n\n        ## WC Order Status Mapping (7 mappings — implement exactly)\n        | WC status    | channel_orders.status |\n        |---|---|\n        | pending      | new        |\n        | on-hold      | new        |\n        | processing   | paid       |\n        | completed    | fulfilled  |\n        | cancelled    | cancelled  |\n        | failed       | cancelled  |\n        | refunded     | refunded   |\n\n        ## Test Requirements (shopops-api/tests/woocommerce.test.js, min 5 bun tests)\n        Use bun test runner (import { describe, it, expect } from 'bun:test').\n        Each test must assert actual behavior — not \"did not throw\".\n\n        Required test coverage:\n        1. Test A — Webhook signature rejection: POST /woocommerce-webhooks/order with invalid\n           X-WC-Webhook-Signature returns 401. If 200 returned, test fails (security hole).\n        2. Test B — Order ingest + status mapping: valid signature + WC order { id: 55555, status: 'processing',\n           total: '45.99' } -> 200 OK, channel_orders row has status='paid', total_cents=4599\n        3. Test C — Idempotent webhook: same order delivered twice -> SELECT COUNT(*) FROM channel_orders\n           WHERE external_order_id='55555' = 1 (not 2)\n        4. Test D — Product push preview: POST /woocommerce-push/preview -> 200, body has\n           { items: [...], classify: { PUSH_CREATE: N, PUSH_UPDATE: M, SKIP: K } }\n           each item has classify in {PUSH_CREATE, PUSH_UPDATE, SKIP}\n        5. Test E — Env var fail-closed: WOOCOMMERCE_WEBHOOK_SECRET unset ->\n           POST /woocommerce-webhooks/order returns 500 with { error: 'WOOCOMMERCE_WEBHOOK_SECRET not configured' }\n        Additional tests welcome (import preview classify list shape, on-hold maps to 'new', etc.)\n\n        ## Doctrine Baseline Tests\n        Write at least 5 test functions each asserting real behavior.\n        This is a backend connector — target 6-8 tests covering: security boundary (signature),\n        status mapping correctness, idempotency contract, env var fail-closed, and push preview shape.\n\n        ## Completion Criteria\n        Output <promise>COMPLETE</promise> only when ALL of the following are true:\n        - shopops-api/services/woocommerceClient.js exists with wcGet, wcPost, verifyWcWebhookSignature exports\n        - shopops-api/routes/woocommerce-import.js exists with POST /preview, POST /commit, GET /batches\n        - shopops-api/routes/woocommerce-push.js exists with POST /preview, POST /commit\n        - shopops-api/routes/woocommerce-webhooks.js exists with POST /order handler + signature validation\n        - shopops-api/index.js has require + app.use for all three woocommerce routes\n        - shopops-api/tests/woocommerce.test.js exists with min 5 real-behavior bun tests\n        - .env.example has WOOCOMMERCE_WEBHOOK_SECRET placeholder entry\n        - All stop conditions from spec §12 are satisfied\n\n  # ---------------------------------------------------------------------------\n  # 6. VERIFY-GREP — structural assertions (multi-line-safe, one grep per assertion)\n  # ---------------------------------------------------------------------------\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      echo \"=== WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01 grep assertions ===\"\n\n      echo \"--- File existence checks ---\"\n      ls shopops-api/services/woocommerceClient.js\n      ls shopops-api/routes/woocommerce-import.js\n      ls shopops-api/routes/woocommerce-push.js\n      ls shopops-api/routes/woocommerce-webhooks.js\n      ls shopops-api/tests/woocommerce.test.js\n\n      echo \"--- services/woocommerceClient.js: required exports ---\"\n      grep -q \"wcGet\" shopops-api/services/woocommerceClient.js || \\\n        (echo \"FAIL: wcGet missing from woocommerceClient.js\" >&2; exit 1)\n      grep -q \"wcPost\" shopops-api/services/woocommerceClient.js || \\\n        (echo \"FAIL: wcPost missing from woocommerceClient.js\" >&2; exit 1)\n      grep -q \"verifyWcWebhookSignature\" shopops-api/services/woocommerceClient.js || \\\n        (echo \"FAIL: verifyWcWebhookSignature missing from woocommerceClient.js\" >&2; exit 1)\n\n      echo \"--- WC Basic Auth: Authorization header uses base64 ---\"\n      grep -q \"base64\\|Basic\" shopops-api/services/woocommerceClient.js || \\\n        (echo \"FAIL: Basic Auth / base64 not found in woocommerceClient.js\" >&2; exit 1)\n\n      echo \"--- routes/woocommerce-webhooks.js: signature header reference ---\"\n      grep -q \"X-WC-Webhook-Signature\\|x-wc-webhook-signature\" shopops-api/routes/woocommerce-webhooks.js || \\\n        grep -q \"X-WC-Webhook-Signature\\|x-wc-webhook-signature\" shopops-api/services/woocommerceClient.js || \\\n        (echo \"FAIL: X-WC-Webhook-Signature not referenced in webhook handler or client\" >&2; exit 1)\n\n      echo \"--- routes/woocommerce-webhooks.js: WOOCOMMERCE_WEBHOOK_SECRET fail-closed ---\"\n      grep -q \"WOOCOMMERCE_WEBHOOK_SECRET\" shopops-api/routes/woocommerce-webhooks.js || \\\n        grep -q \"WOOCOMMERCE_WEBHOOK_SECRET\" shopops-api/services/woocommerceClient.js || \\\n        (echo \"FAIL: WOOCOMMERCE_WEBHOOK_SECRET not referenced in webhook code\" >&2; exit 1)\n\n      echo \"--- routes/woocommerce-webhooks.js: idempotent upsert ---\"\n      grep -q \"ON CONFLICT\" shopops-api/routes/woocommerce-webhooks.js || \\\n        (echo \"FAIL: ON CONFLICT upsert not found in webhook handler\" >&2; exit 1)\n\n      echo \"--- WC status mapping: all 7 statuses present (checked individually) ---\"\n      grep -q \"processing\" shopops-api/routes/woocommerce-webhooks.js || \\\n        (echo \"FAIL: WC status 'processing' mapping missing\" >&2; exit 1)\n      grep -q \"on-hold\\|on_hold\" shopops-api/routes/woocommerce-webhooks.js || \\\n        (echo \"FAIL: WC status 'on-hold' mapping missing\" >&2; exit 1)\n      grep -q \"completed\" shopops-api/routes/woocommerce-webhooks.js || \\\n        (echo \"FAIL: WC status 'completed' mapping missing\" >&2; exit 1)\n      grep -q \"refunded\" shopops-api/routes/woocommerce-webhooks.js || \\\n        (echo \"FAIL: WC status 'refunded' mapping missing\" >&2; exit 1)\n\n      echo \"--- index.js: all three woocommerce routes registered ---\"\n      grep -q \"woocommerce-import\\|woocommerceImport\" shopops-api/index.js || \\\n        (echo \"FAIL: woocommerce-import route not registered in index.js\" >&2; exit 1)\n      grep -q \"woocommerce-push\\|woocommercePush\" shopops-api/index.js || \\\n        (echo \"FAIL: woocommerce-push route not registered in index.js\" >&2; exit 1)\n      grep -q \"woocommerce-webhooks\\|woocommerceWebhooks\" shopops-api/index.js || \\\n        (echo \"FAIL: woocommerce-webhooks route not registered in index.js\" >&2; exit 1)\n\n      echo \"--- Stop condition: 3+ woocommerce references in index.js ---\"\n      WC_ROUTE_COUNT=$(grep -c \"woocommerce\" shopops-api/index.js 2>/dev/null || echo 0)\n      echo \"woocommerce refs in index.js: $WC_ROUTE_COUNT\"\n      if [ \"$WC_ROUTE_COUNT\" -lt 3 ]; then\n        echo \"FAIL: spec §12 stop condition requires >=3 woocommerce refs in index.js, found $WC_ROUTE_COUNT\" >&2\n        exit 1\n      fi\n\n      echo \"--- .env.example: WOOCOMMERCE_WEBHOOK_SECRET placeholder present ---\"\n      ENV_EXAMPLE=$(find shopops-api -name \".env.example\" -maxdepth 2 2>/dev/null | head -1)\n      if [ -z \"$ENV_EXAMPLE\" ]; then\n        ENV_EXAMPLE=\".env.example\"\n      fi\n      grep -q \"WOOCOMMERCE_WEBHOOK_SECRET\" \"$ENV_EXAMPLE\" || \\\n        (echo \"FAIL: WOOCOMMERCE_WEBHOOK_SECRET missing from $ENV_EXAMPLE\" >&2; exit 1)\n\n      echo \"--- Test function count (min 5) ---\"\n      TEST_COUNT=$(grep -c \"it(\\|test(\" shopops-api/tests/woocommerce.test.js 2>/dev/null || echo 0)\n      echo \"Test functions found: $TEST_COUNT\"\n      if [ \"$TEST_COUNT\" -lt 5 ]; then\n        echo \"FAIL: fewer than 5 test functions in tests/woocommerce.test.js, found $TEST_COUNT\" >&2\n        exit 1\n      fi\n\n      echo \"=== All grep assertions passed ===\"\n\n  # ---------------------------------------------------------------------------\n  # 7. VERIFY-TESTS — run bun test suite (Archon container is bun-only)\n  # ---------------------------------------------------------------------------\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      echo \"=== Running bun test on woocommerce test suite ===\"\n      # Archon container is bun-only. Rule from memory: reference_archon-container-runtime-bun-only.md\n      if [ -f tests/woocommerce.test.js ]; then\n        bun test tests/woocommerce.test.js || {\n          echo \"Tests failed. See output above.\" >&2\n          echo \"NOTE: If live DB is required and unavailable in harness, tests may fail here.\"\n          echo \"Operator must verify bun test tests/woocommerce.test.js passes in staging environment.\"\n          exit 1\n        }\n      else\n        echo \"FAIL: shopops-api/tests/woocommerce.test.js not found\" >&2\n        exit 1\n      fi\n\n  # ---------------------------------------------------------------------------\n  # 8. COMMIT-AND-PUSH — idempotent checkout + stage + commit + push\n  # ---------------------------------------------------------------------------\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      BASE_BRANCH=\"wo/shopops-woocommerce-connector-01\"\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n\n      # Idempotent checkout: prefer stable BASE_BRANCH unless it's locked to another worktree\n      if git show-ref --quiet \"refs/heads/$BASE_BRANCH\"; then\n        if git worktree list --porcelain | grep -q \"branch refs/heads/$BASE_BRANCH\" && \\\n           [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BASE_BRANCH\" ]; then\n          # Branch locked to another worktree — use timestamped variant\n          git checkout -b \"$BRANCH\"\n        else\n          BRANCH=\"$BASE_BRANCH\"\n          git checkout \"$BRANCH\" 2>/dev/null || git checkout -b \"$BRANCH\"\n        fi\n      else\n        BRANCH=\"$BASE_BRANCH\"\n        git checkout -b \"$BRANCH\"\n      fi\n\n      # Stage WO-scoped files only\n      git add \\\n        shopops-api/services/woocommerceClient.js \\\n        shopops-api/routes/woocommerce-import.js \\\n        shopops-api/routes/woocommerce-push.js \\\n        shopops-api/routes/woocommerce-webhooks.js \\\n        shopops-api/index.js \\\n        shopops-api/tests/woocommerce.test.js\n\n      # Stage .env.example wherever it lives\n      find shopops-api -name \".env.example\" -maxdepth 2 | xargs git add 2>/dev/null || true\n      find . -maxdepth 1 -name \".env.example\" | xargs git add 2>/dev/null || true\n\n      # Idempotent commit: skip if no staged changes (retry-safe)\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01 — WooCommerce connector\\n\\nAdds WooCommerce channel connector to shopops-api (mirrors Shopify connector pattern).\\n\\nNew files:\\n- shopops-api/services/woocommerceClient.js (wcGet, wcPost, verifyWcWebhookSignature; Basic Auth)\\n- shopops-api/routes/woocommerce-import.js  (POST /woocommerce-import/preview + /commit, GET /batches)\\n- shopops-api/routes/woocommerce-push.js    (POST /woocommerce-push/preview + /commit)\\n- shopops-api/routes/woocommerce-webhooks.js (POST /woocommerce-webhooks/order + X-WC-Webhook-Signature)\\n- shopops-api/tests/woocommerce.test.js     (min 5 bun tests: signature, ingest, idempotency, push preview, env fail-closed)\\n\\nModified:\\n- shopops-api/index.js  (registers /woocommerce-import, /woocommerce-push, /woocommerce-webhooks routes)\\n- .env.example          (adds WOOCOMMERCE_WEBHOOK_SECRET placeholder)\\n\\nWC order status mapping: pending/on-hold->new, processing->paid, completed->fulfilled,\\n  cancelled/failed->cancelled, refunded->refunded\\nIdempotent upsert: ON CONFLICT (tenant_id, channel, external_order_id) DO UPDATE\\nAuth: Basic base64(consumer_key:consumer_secret), timing-safe webhook signature verification\\n\\nDepends on: WO-SHOPOPS-COMMERCE-SPINE-01 (channel_connections + product_channel_map + channel_orders)\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  # ---------------------------------------------------------------------------\n  # 9. OPEN-PR — idempotent PR against master\n  # ---------------------------------------------------------------------------\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Use current HEAD branch (set by commit-and-push — may be timestamped variant)\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BASE=\"master\"\n      TITLE=\"feat(api): WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01 — WooCommerce connector\"\n\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01** — WooCommerce connector for shopops-api.\"\n        echo \"\"\n        echo \"Adds product import (preview+commit), product push (preview+commit), order webhook\"\n        echo \"ingest, and inventory publication. Mirrors the Shopify connector pattern.\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- NEW \\`shopops-api/services/woocommerceClient.js\\` — wcGet, wcPost, verifyWcWebhookSignature (Basic Auth)\"\n        echo \"- NEW \\`shopops-api/routes/woocommerce-import.js\\` — POST /woocommerce-import/preview + /commit, GET /batches\"\n        echo \"- NEW \\`shopops-api/routes/woocommerce-push.js\\` — POST /woocommerce-push/preview + /commit\"\n        echo \"- NEW \\`shopops-api/routes/woocommerce-webhooks.js\\` — POST /woocommerce-webhooks/order\"\n        echo \"- NEW \\`shopops-api/tests/woocommerce.test.js\\` — min 5 bun tests\"\n        echo \"- MOD \\`shopops-api/index.js\\` — registers 3 woocommerce routes\"\n        echo \"- MOD \\`.env.example\\` — WOOCOMMERCE_WEBHOOK_SECRET placeholder\"\n        echo \"\"\n        echo \"## WC Order Status Mapping\"\n        echo \"| WC status | channel_orders.status |\"\n        echo \"|---|---|\"\n        echo \"| pending | new |\"\n        echo \"| on-hold | new |\"\n        echo \"| processing | paid |\"\n        echo \"| completed | fulfilled |\"\n        echo \"| cancelled | cancelled |\"\n        echo \"| failed | cancelled |\"\n        echo \"| refunded | refunded |\"\n        echo \"\"\n        echo \"## Auth\"\n        echo \"- Outbound (WC REST API): Basic Auth base64(consumer_key:consumer_secret)\"\n        echo \"- Inbound (webhooks): HMAC-SHA256 X-WC-Webhook-Signature + WOOCOMMERCE_WEBHOOK_SECRET\"\n        echo \"- Fail-closed: 500 if WOOCOMMERCE_WEBHOOK_SECRET unset, 401 if signature invalid\"\n        echo \"\"\n        echo \"## Prerequisite\"\n        echo \"Depends on WO-SHOPOPS-COMMERCE-SPINE-01 (channel_connections + product_channel_map + channel_orders tables).\"\n        echo \"\"\n        echo \"## Env Var\"\n        echo \"\\`WOOCOMMERCE_WEBHOOK_SECRET\\` must be added to /opt/bdc/shopops/.env on Hetzner before first webhook.\"\n        echo \"Per-tenant: woocommerce_consumer_key, woocommerce_consumer_secret, woocommerce_store_url stored in tenant_config.\"\n        echo \"\"\n        echo \"## Rule 20 / Staging Gate\"\n        echo \"PR targets master. John promotes master -> production after staging validation.\"\n        echo \"\"\n        echo \"Generated by Archon bdc-shopops-woocommerce-connector workflow.\"\n      } > \"$BODY_FILE\"\n\n      # Idempotent PR creation\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n          --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  # ---------------------------------------------------------------------------\n  # 10. VERIFY-RUNTIME — Rule 19: docker exec grep proves source is live in container\n  #     Best-effort: container may not be rebuilt yet at this pipeline stage.\n  # ---------------------------------------------------------------------------\n  - id: verify-runtime\n    depends_on: [open-pr]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step (Rule 19 verification)\n      # shopops-api uses COPY . /app at build — source changes need rebuild + restart.\n      # PR is open but not yet merged. Container likely has pre-WO source.\n      # Operator MUST re-run these checks after: docker compose build shopops-api && docker compose up -d shopops-api\n\n      echo \"=== Rule 19: Runtime verification (shopops-api) ===\"\n      echo \"NOTE: Checks below will show STATUS=pending_rebuild until operator rebuilds container post-merge.\"\n      echo \"Operator rebuild command: ssh hetzner-prod 'cd /opt/bdc/shopops && docker compose build shopops-api && docker compose up -d shopops-api'\"\n      echo \"\"\n\n      # Check 1: woocommerce-webhooks registered in index.js\n      RESULT1=$(docker exec shopops-api grep -n \"woocommerce-webhooks\\|woocommerceWebhooks\" /app/index.js 2>&1)\n      EXIT1=$?\n      if [ $EXIT1 -eq 0 ]; then\n        echo \"STATUS_WEBHOOK_ROUTE=ok\"\n        echo \"ROUTE_REGISTRATION: $RESULT1\"\n      else\n        echo \"STATUS_WEBHOOK_ROUTE=pending_rebuild\"\n        echo \"EXPECTED_COMMAND: docker exec shopops-api grep -n 'woocommerce-webhooks' /app/index.js\"\n      fi\n      echo \"\"\n\n      # Check 2: woocommerce-push registered in index.js\n      RESULT2=$(docker exec shopops-api grep -n \"woocommerce-push\\|woocommercePush\" /app/index.js 2>&1)\n      EXIT2=$?\n      if [ $EXIT2 -eq 0 ]; then\n        echo \"STATUS_PUSH_ROUTE=ok\"\n        echo \"PUSH_ROUTE: $RESULT2\"\n      else\n        echo \"STATUS_PUSH_ROUTE=pending_rebuild\"\n        echo \"EXPECTED_COMMAND: docker exec shopops-api grep -n 'woocommerce-push' /app/index.js\"\n      fi\n      echo \"\"\n\n      # Check 3: wcGet present in woocommerceClient.js\n      RESULT3=$(docker exec shopops-api grep -n \"wcGet\" /app/services/woocommerceClient.js 2>&1)\n      EXIT3=$?\n      if [ $EXIT3 -eq 0 ]; then\n        echo \"STATUS_WCCLIENT=ok\"\n        echo \"WC_CLIENT: $RESULT3\"\n      else\n        echo \"STATUS_WCCLIENT=pending_rebuild\"\n        echo \"EXPECTED_COMMAND: docker exec shopops-api grep -n 'wcGet' /app/services/woocommerceClient.js\"\n      fi\n      echo \"\"\n\n      # Spec §12 stop condition commands (for manifest)\n      echo \"=== Spec §12 stop condition commands (run post-rebuild) ===\"\n      echo \"docker exec shopops-api grep -rn 'woocommerce' /app/index.js\"\n      echo \"  Expected: >= 3 matches (import, push, webhooks)\"\n      echo \"docker exec shopops-api node -e \\\"require('./services/woocommerceClient.js'); console.log('ok')\\\"\"\n      echo \"  Expected: prints 'ok' with no error\"\n      echo \"docker exec shopops-api grep -n 'wcGet' /app/services/woocommerceClient.js\"\n      echo \"  Expected: line with wcGet function definition\"\n\n      exit 0\n\n  # ---------------------------------------------------------------------------\n  # 11. BUILD-MANIFEST — CODE class manifest (Rule 14 + Rule 19)\n  # ---------------------------------------------------------------------------\n  - id: build-manifest\n    depends_on: [open-pr, verify-runtime]\n    task_class: clerical\n    prompt: |\n      Build the Captain CI manifest v2 for WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01 (CODE class).\n\n      CODE class requires: grep assertions for all new/modified source files,\n      runtime verification per Rule 19, test count from bun test output.\n\n      ## Required manifest v2 format (paste verbatim into Notion Notes)\n      WO: WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01\n      Builder: Major Build (Archon bdc-shopops-woocommerce-connector)\n      Files modified: shopops-api/index.js, .env.example\n      Files created: shopops-api/services/woocommerceClient.js,\n                     shopops-api/routes/woocommerce-import.js,\n                     shopops-api/routes/woocommerce-push.js,\n                     shopops-api/routes/woocommerce-webhooks.js,\n                     shopops-api/tests/woocommerce.test.js\n      Tests: [count from verify-tests.output] passing / [total] total\n             command: bun test shopops-api/tests/woocommerce.test.js\n      PRs: [PR URL from open-pr.output] — base: master\n      Merge ancestors: shopops master HEAD=[from commit-and-push BRANCH_SHA], behind_by: 0\n      Grep assertions:\n        grep -q 'wcGet' shopops-api/services/woocommerceClient.js\n        grep -q 'wcPost' shopops-api/services/woocommerceClient.js\n        grep -q 'verifyWcWebhookSignature' shopops-api/services/woocommerceClient.js\n        grep -c 'woocommerce' shopops-api/index.js (expected >= 3)\n        grep -q 'ON CONFLICT' shopops-api/routes/woocommerce-webhooks.js\n        grep -q 'X-WC-Webhook-Signature' shopops-api/routes/woocommerce-webhooks.js\n        grep -q 'WOOCOMMERCE_WEBHOOK_SECRET' shopops-api/routes/woocommerce-webhooks.js\n      Runtime verification (Rule 19 — run post-rebuild):\n        docker exec shopops-api grep -n 'woocommerce-webhooks' /app/index.js\n        docker exec shopops-api grep -n 'woocommerce-push' /app/index.js\n        docker exec shopops-api grep -n 'wcGet' /app/services/woocommerceClient.js\n        [include actual STATUS= lines from verify-runtime.output]\n        Rebuild command: ssh hetzner-prod 'cd /opt/bdc/shopops && docker compose build shopops-api && docker compose up -d shopops-api'\n      Vercel deployment: N/A (shopops-api is Hetzner Docker, not Vercel)\n      VALIDATION: PASS (pending container rebuild post-merge for runtime verification to complete)\n\n      ## Inputs\n      $read-spec.output\n      $plan.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-and-push.output\n      $open-pr.output\n      $verify-runtime.output\n\n  # post-started and post-completed webhook nodes OMITTED — Rule 11:\n  # curl to n8n.bluedevilcollectibles.com from Archon executor subprocess hangs >=120s.\n  # flip-notion is the canonical close action. Builder-status feed is optional bookkeeping.\n\n  # ---------------------------------------------------------------------------\n  # 12. FLIP-NOTION — set Notes property + Claude Status = REVIEW\n  #     Notes field, NOT a page comment (Captain CI reads Notes, not comments).\n  # ---------------------------------------------------------------------------\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01 to REVIEW with the manifest in the Notes property.\n\n      IMPORTANT: The manifest goes in the Notes PROPERTY, NOT as a page comment.\n      Captain CI reads the Notes property. Page comments are invisible to CI.\n\n      Notion database ID: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n\n      ## Steps\n      1. Find the Notion page for WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01.\n         Query database a6df831c-0b52-449f-8ca4-d77be6b70d0a with filter:\n         { \"property\": \"title\", \"title\": { \"contains\": \"WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01\" } }\n\n      2. In a SINGLE Notion update call, set ALL of:\n         - Claude Status property -> REVIEW\n         - Status property -> Review\n         - Notes property -> paste $build-manifest.output verbatim (full manifest v2 block)\n         - claimed_by: Major Build (Archon bdc-shopops-woocommerce-connector)\n         - claimed_at: [today's date]\n\n      3. Confirm the update succeeded and log the Notion page URL to run output.\n\n      ## Manifest\n      $build-manifest.output\n\n      ## Fallback\n      If Notion MCP is unavailable in this runtime, output the following for XO to action:\n      XO_TODO: Flip WO-SHOPOPS-WOOCOMMERCE-CONNECTOR-01 to REVIEW in Notion.\n               Set Notes property (NOT comment) to the manifest from $build-manifest.output.\n               Set Claude Status = REVIEW, Status = Review.\n               Database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n",
  "bdc-storefront-catalog-real-covers": "# Drafted by yaml-author subagent for WO-STOREFRONT-CATALOG-REAL-COVERS-01 on 2026-05-13\n# Source pattern: WO-STOREFRONT-PORTAL-AUTH-WIRE-01.yaml (storefront-side scaffolding)\n#                 + bdc-ce-regression-suite.yaml (gold-standard DAG, idempotent checkout)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight (OPERATOR MUST VERIFY BEFORE FIRING):\n#   1. bluedevilcollectibles/shopops-storefront registered in Archon as a codebase:\n#      sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name, default_cwd FROM remote_agent_codebases WHERE name LIKE '%storefront%'\"\n#      Expected default_cwd: /home/appuser/shopops-storefront (NOT /.archon/workspaces/...)\n#      Per memory note reference_archon-codebase-registry-paths.md.\n#   2. shopops-storefront default branch confirmed:\n#      gh api repos/bluedevilcollectibles/shopops-storefront --template '{{.default_branch}}'\n#      Expected: master (per battlegroup briefing 2026-05-13). If 'main', update\n#      BASE references in commit-and-push and open-pr nodes below.\n#   3. WO spec committed + pushed to bdc-xo main branch:\n#      docs/work-orders/WO-STOREFRONT-CATALOG-REAL-COVERS-01.md\n#   4. Notion page ID for WO-STOREFRONT-CATALOG-REAL-COVERS-01 — FILL IN below before firing.\n#      Find via: Notion search on database a6df831c-0b52-449f-8ca4-d77be6b70d0a\n#   5. Network egress: Archon container must reach api.livesellerpro.app (port 443).\n#      If blocked, builder will surface in run output; operator can run backfill\n#      locally and re-fire from the point of failure.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh bdc-storefront-catalog-real-covers.yaml\n#   (copy this file to bdc-harness/.archon/workflows/defaults/ first; rename to lowercase-hyphen)\n#\n# REVIEW BEFORE DEPLOY — human verifies:\n#   - Notion page ID filled in (Q1 below)\n#   - shopops-storefront default branch confirmed master (Q2 below)\n#   - 70% threshold acceptable for beta (per WO §10 ROUGH OK calibration)\n#\n# OPEN QUESTIONS:\n#   Q1: Notion page ID is UNKNOWN at draft time. Operator must replace\n#       35fef4c9-ce00-8193-9f29-dabf85f07390 in flip-notion prompt before firing.\n#   Q2: shopops-storefront default branch assumed 'master' per battlegroup briefing.\n#       Verify via gh api before fire. If 'main', do a global swap in this file.\n\nname: bdc-storefront-catalog-real-covers\ndescription: |\n  Use when: Executing WO-STOREFRONT-CATALOG-REAL-COVERS-01 — plug AutoFix cover pipeline\n            into storefront catalog page so real cover art renders instead of placeholder\n            colored text plates.\n  Input: WO_ID = WO-STOREFRONT-CATALOG-REAL-COVERS-01.\n  Does: Reads WO spec from bdc-xo. Writes scripts/backfill-seed-covers.mjs that calls\n        POST https://api.livesellerpro.app/covers/lookup/batch and writes resolved URLs\n        into lib/seed-data/catalog.json. Updates app/shops/[shopSlug]/catalog/page.tsx\n        to render <img src={item.cover_url}> with onError fallback to existing palette.\n        Adds __tests__/lib/catalog-cover-fallback.test.ts with >= 4 bun tests.\n        Runs backfill script, commits JSON + page + tests, opens PR against master.\n        Verifies Vercel preview deployment READY. Builds CODE manifest. Flips Notion to REVIEW.\n  Repo: bluedevilcollectibles/shopops-storefront (single repo).\n  parallel_ok: true — single repo, no shared file conflicts.\n  Polish bar: ROUGH OK (Mutant Market is beta tester, not paying).\n  NOT for: any other WO.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  # ---------------------------------------------------------------------------\n  # 1. READ-SPEC — fetch WO spec from bdc-xo (cross-repo via gh api).\n  #    Rule 5: tries direct path first, then date-prefix glob fallback.\n  # ---------------------------------------------------------------------------\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-STOREFRONT-CATALOG-REAL-COVERS-01}\"\n\n      # Primary path: docs/work-orders/<WO_ID>.md (gh --template, no jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      # Fallback: glob docs/superpowers/specs/*<WO_ID>*.md (Rule 5)\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:docs/work-orders/%s.md\\n\\n' \"$WO_ID\" \"$WO_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  # ---------------------------------------------------------------------------\n  # 2. INSPECT-CURRENT-STATE — read existing storefront files for context\n  # ---------------------------------------------------------------------------\n  - id: inspect-current-state\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — audit must NEVER fail (BUG-3 tolerant)\n      echo \"=== seed catalog current state ===\"\n      if [ -f lib/seed-data/catalog.json ]; then\n        # python3 single-line replaces jq (jq not in bun container; multi-line py heredocs break Bun YAML parser)\n        TOTAL=$(python3 -c \"import json,sys; print(len(json.load(open('lib/seed-data/catalog.json'))))\" 2>/dev/null || echo 0)\n        WITH_COVER=$(python3 -c \"import json,sys; d=json.load(open('lib/seed-data/catalog.json')); print(sum(1 for x in d if x.get('cover_url')))\" 2>/dev/null || echo 0)\n        echo \"TOTAL_ITEMS=${TOTAL}\"\n        echo \"ITEMS_WITH_COVER_URL=${WITH_COVER}\"\n        echo \"FIRST_THREE_UPCS:\"\n        python3 -c \"import json; d=json.load(open('lib/seed-data/catalog.json')); [print('  upc='+str(x.get('upc'))+' title='+str(x.get('title'))+' cover_url='+str(x.get('cover_url'))) for x in d[:3]]\" 2>/dev/null || true\n      else\n        echo \"WARN: lib/seed-data/catalog.json not found at CWD (audit tolerant)\"\n      fi\n\n      echo \"\"\n      echo \"=== catalog page current state ===\"\n      CATALOG_PAGE=\"app/shops/[shopSlug]/catalog/page.tsx\"\n      if [ -e \"$CATALOG_PAGE\" ]; then\n        echo \"File exists: $CATALOG_PAGE\"\n        grep -n 'cover_url\\|issue-cover\\|cat-card__cover' \"$CATALOG_PAGE\" 2>/dev/null || echo \"(no matches yet)\"\n      else\n        echo \"WARN: $CATALOG_PAGE not found (audit tolerant — implement loop will surface)\"\n      fi\n\n      echo \"\"\n      echo \"=== PDP page (optional) ===\"\n      PDP_PAGE=\"app/shops/[shopSlug]/comic/[upc]/page.tsx\"\n      if [ -e \"$PDP_PAGE\" ]; then\n        echo \"PDP_PAGE_EXISTS=true\"\n        grep -n 'cover_url\\|issue-cover' \"$PDP_PAGE\" 2>/dev/null || echo \"(no cover refs)\"\n      else\n        echo \"PDP_PAGE_EXISTS=false\"\n      fi\n      exit 0\n\n  # ---------------------------------------------------------------------------\n  # 3. PROBE-COVER-API — best-effort connectivity check to api.livesellerpro.app\n  #    Rule 11: NOT n8n webhook. /covers/lookup/batch is a public shopops-api route.\n  # ---------------------------------------------------------------------------\n  - id: probe-cover-api\n    depends_on: [inspect-current-state]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step\n      RESULT=$(curl -sf -X POST \\\n        https://api.livesellerpro.app/covers/lookup/batch \\\n        -H 'Content-Type: application/json' \\\n        -H 'X-Shopops-Tenant: bdc' \\\n        -d '{\"items\":[{\"upc\":\"76194138584602011\",\"title\":\"ABSOLUTE BATMAN #20\",\"publisher\":\"DC Comics\"}]}' \\\n        --max-time 30 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ] && echo \"$RESULT\" | grep -q '\"coverMap\"'; then\n        echo \"STATUS=ok\"\n        echo \"PROBE_RESPONSE=${RESULT}\"\n        echo \"DETAIL=cover API reachable from Archon container\"\n      elif echo \"$RESULT\" | grep -qiE 'could not resolve|timeout|connection refused|403|401'; then\n        echo \"STATUS=skipped_network_blocked\"\n        echo \"DETAIL=${RESULT}\"\n        echo \"WORKAROUND=Builder will write script and skeleton JSON; operator runs backfill locally and commits.\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${RESULT}\"\n      fi\n      exit 0\n\n  # ---------------------------------------------------------------------------\n  # 4. PLAN — architect the single-repo implementation\n  # ---------------------------------------------------------------------------\n  - id: plan\n    depends_on: [read-spec, inspect-current-state, probe-cover-api]\n    task_class: medium_work\n    prompt: |\n      Plan the single-repo storefront cover-render implementation.\n\n      WO: WO-STOREFRONT-CATALOG-REAL-COVERS-01\n      Class: CODE (frontend-only, Vercel deploy is runtime proof)\n      Polish bar: ROUGH OK (Mutant Market is beta tester, not paying)\n\n      ## Required Output (Markdown, exact headings)\n\n      ### Files to Create\n      - scripts/backfill-seed-covers.mjs (Node ESM, one-shot operator script)\n      - __tests__/lib/catalog-cover-fallback.test.ts (>= 4 bun tests)\n      - lib/catalog-cover.ts (pure helper module — exports shouldShowPalette + render\n        decision functions, so tests don't need React DOM)\n\n      ### Files to Modify\n      - lib/seed-data/catalog.json (cover_url populated on >= 70% of items after backfill run)\n      - app/shops/[shopSlug]/catalog/page.tsx (render <img> when cover_url truthy, else palette)\n      - app/shops/[shopSlug]/comic/[upc]/page.tsx (only if file exists per inspect node + has hero img — verify before touching)\n\n      ### Backfill Script Behavior\n      - Reads lib/seed-data/catalog.json\n      - Batches UPCs into chunks of 50\n      - POSTs each chunk to https://api.livesellerpro.app/covers/lookup/batch\n        with header X-Shopops-Tenant: bdc and body\n        { items: [{ upc, title, publisher, series, issue }, ...] }\n      - Reads response { ok, coverMap: { [upc]: { tier, url, source } } }\n      - For coverMap[upc].url non-empty AND tier != 'placeholder' -> write into JSON\n      - Otherwise leave cover_url=null\n      - Logs resolved/placeholder/none counts\n      - Writes file with 2-space indent + trailing newline (match existing format)\n      - Exit 0 success, non-zero on network/auth error\n\n      ### Render Pattern (catalog/page.tsx)\n      Inside cat-card__cover div, add img-first then existing palette:\n        <div className=\"cat-card__cover\">\n          {item.cover_url ? (\n            <img src={item.cover_url} alt={item.title}\n                 className=\"cat-card__cover-img\" loading=\"lazy\"\n                 onError={(e) => { e.currentTarget.style.display = 'none'; }} />\n          ) : null}\n          {!item.cover_url ? (\n            <div className=\"issue-cover\">{/* existing palette content */}</div>\n          ) : null}\n        </div>\n      Simpler ternary: render img OR palette, not both. Avoids CSS sibling complexity.\n      If cover_url is set, onError won't trigger re-render to palette (acceptable for ROUGH OK).\n\n      ### Helper Module (lib/catalog-cover.ts)\n      export function shouldShowPalette(item: { cover_url?: string | null }): boolean {\n        return !item.cover_url;\n      }\n      export function getCoverImgProps(item: { cover_url?: string | null; title: string }) {\n        if (!item.cover_url) return null;\n        return { src: item.cover_url, alt: item.title, loading: 'lazy' as const };\n      }\n\n      ### Test File (>= 4 tests, bun-compatible)\n      Use `import { describe, it, expect } from 'bun:test'`. Test pure helpers:\n        a. shouldShowPalette returns true when cover_url is null\n        b. shouldShowPalette returns true when cover_url is empty string\n        c. shouldShowPalette returns false when cover_url is a non-empty URL\n        d. getCoverImgProps returns null for null cover_url\n        e. getCoverImgProps returns { src, alt, loading } when cover_url present\n\n      ### Commit Strategy\n      - Branch: wo/storefront-catalog-real-covers-01 (off master)\n      - Single commit:\n        feat(storefront): WO-STOREFRONT-CATALOG-REAL-COVERS-01 — real cover art in catalog\n      - PR base: master\n\n      ### Probe Output (cover API reachability)\n      $probe-cover-api.output\n\n      ### Current State\n      $inspect-current-state.output\n\n      ### Spec\n      $read-spec.output\n\n  # ---------------------------------------------------------------------------\n  # 5. IMPLEMENT — write code, run backfill, update page, write tests\n  # ---------------------------------------------------------------------------\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement WO-STOREFRONT-CATALOG-REAL-COVERS-01.\n        CWD is the shopops-storefront repo root.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Probe Result (network reachability of api.livesellerpro.app)\n        $probe-cover-api.output\n\n        ## Execution Rules\n\n        1. Read app/shops/[shopSlug]/catalog/page.tsx in full before editing.\n           Note the existing cat-card__cover div structure (lines ~115-135).\n\n        2. Create lib/catalog-cover.ts with pure helpers (no React dependency):\n             export function shouldShowPalette(item: { cover_url?: string | null }): boolean\n             export function getCoverImgProps(item: { cover_url?: string | null; title: string })\n\n        3. Create scripts/backfill-seed-covers.mjs (Node ESM with shebang #!/usr/bin/env node):\n           - Reads lib/seed-data/catalog.json as JSON\n           - Chunks of 50 items, POSTs to https://api.livesellerpro.app/covers/lookup/batch\n             with header X-Shopops-Tenant: bdc and body { items: [...] }\n           - Builds request body with { upc, title, publisher, series, issue } per item\n           - Reads response.coverMap, writes resolved url back to items where\n             coverMap[upc].url non-empty AND coverMap[upc].tier != 'placeholder'\n           - Logs progress: chunk N/M, resolved=X placeholder=Y none=Z\n           - Writes back to catalog.json with JSON.stringify(data, null, 2) + '\\n'\n           - Use built-in fetch (Node 18+). No external deps.\n           - On non-OK response (status != 200 or !response.ok), log error and exit 1\n             so operator sees the failure.\n\n        4. Run the backfill script:\n             node scripts/backfill-seed-covers.mjs\n           If probe-cover-api STATUS=skipped_network_blocked, SKIP this run and instead\n           note in commit message: \"backfill JSON to be run locally by operator before PR\n           merge — see scripts/backfill-seed-covers.mjs\".\n\n        5. After running, verify with python3 (jq not in container):\n             python3 -c \"import json; d=json.load(open('lib/seed-data/catalog.json')); print(sum(1 for x in d if x.get('cover_url')))\"\n           Target: >= 40 (70% of 57 seed items). Acceptable for ROUGH OK: whatever the\n           cascade returned. Log the actual count.\n\n        6. Edit app/shops/[shopSlug]/catalog/page.tsx:\n           - Import shouldShowPalette + getCoverImgProps from '@/lib/catalog-cover'\n           - Inside the .map((item) => ...) loop, change cat-card__cover content:\n             {(() => {\n               const imgProps = getCoverImgProps(item);\n               return imgProps ? (\n                 <img className=\"cat-card__cover-img\" {...imgProps}\n                      onError={(e) => { e.currentTarget.style.display = 'none'; }} />\n               ) : (\n                 <div className=\"issue-cover\">{/* existing palette */}</div>\n               );\n             })()}\n           - Keep cornerFlag, hero, palette, strip logic INSIDE the issue-cover branch.\n           - Do NOT remove the deterministic palette helper functions\n             (paletteFor, coverHero, cornerFlag) — they remain in use for fallback.\n\n        7. PDP file: only modify app/shops/[shopSlug]/comic/[upc]/page.tsx IF\n           inspect-current-state output says PDP_PAGE_EXISTS=true AND the file\n           has its own cover render block. If it just routes to catalog or has no\n           cover render, leave it.\n\n        8. Create __tests__/lib/catalog-cover-fallback.test.ts with >= 4 tests on\n           the pure helpers (no React mount needed):\n             import { describe, it, expect } from 'bun:test';\n             import { shouldShowPalette, getCoverImgProps } from '@/lib/catalog-cover';\n             ... 4+ assertions per plan.\n\n        9. Do NOT push. Do NOT commit. Do NOT run deployment commands.\n\n        ## Completion Criteria\n        Output <promise>COMPLETE</promise> only when:\n          - lib/catalog-cover.ts exists with shouldShowPalette + getCoverImgProps\n          - scripts/backfill-seed-covers.mjs exists and is executable (chmod +x via git)\n          - lib/seed-data/catalog.json has cover_url populated on >= 40 items\n            (or: skipped with explicit operator-run note if network was blocked)\n          - app/shops/[shopSlug]/catalog/page.tsx imports + uses the helpers\n          - __tests__/lib/catalog-cover-fallback.test.ts has >= 4 tests\n          - bun test __tests__/lib/catalog-cover-fallback.test.ts passes\n\n  # ---------------------------------------------------------------------------\n  # 6. VERIFY-GREP — structural assertions\n  # ---------------------------------------------------------------------------\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      echo \"=== grep assertions ===\"\n\n      HELPER=\"lib/catalog-cover.ts\"\n      ls \"$HELPER\" || (echo \"FAIL: $HELPER missing\" >&2; exit 1)\n      grep -q \"shouldShowPalette\" \"$HELPER\" \\\n        || (echo \"FAIL: shouldShowPalette missing from $HELPER\" >&2; exit 1)\n      grep -q \"getCoverImgProps\" \"$HELPER\" \\\n        || (echo \"FAIL: getCoverImgProps missing from $HELPER\" >&2; exit 1)\n\n      SCRIPT=\"scripts/backfill-seed-covers.mjs\"\n      ls \"$SCRIPT\" || (echo \"FAIL: $SCRIPT missing\" >&2; exit 1)\n      grep -q \"covers/lookup/batch\" \"$SCRIPT\" \\\n        || (echo \"FAIL: $SCRIPT does not reference covers/lookup/batch endpoint\" >&2; exit 1)\n      grep -q \"X-Shopops-Tenant\" \"$SCRIPT\" \\\n        || (echo \"FAIL: $SCRIPT missing X-Shopops-Tenant header\" >&2; exit 1)\n\n      CATALOG=\"lib/seed-data/catalog.json\"\n      ls \"$CATALOG\" || (echo \"FAIL: $CATALOG missing\" >&2; exit 1)\n      # python3 replaces jq (no jq in bun container)\n      WITH_COVER=$(python3 -c \"import json; d=json.load(open('$CATALOG')); print(sum(1 for x in d if x.get('cover_url')))\")\n      TOTAL=$(python3 -c \"import json; print(len(json.load(open('$CATALOG'))))\")\n      echo \"COVER_URLS_POPULATED=${WITH_COVER}/${TOTAL}\"\n      # ROUGH OK: warn if <40 but don't fail; spec §10 explicitly allows lower threshold\n      if [ \"$WITH_COVER\" -lt 40 ]; then\n        echo \"WARN: cover_url populated on ${WITH_COVER} items (< 40 target)\" >&2\n        echo \"WARN: per WO §10, ship at achieved percentage with note in manifest.\" >&2\n      fi\n\n      PAGE=\"app/shops/[shopSlug]/catalog/page.tsx\"\n      ls \"$PAGE\" || (echo \"FAIL: $PAGE missing\" >&2; exit 1)\n      grep -q \"cover_url\" \"$PAGE\" \\\n        || (echo \"FAIL: $PAGE does not reference item.cover_url\" >&2; exit 1)\n      grep -q \"catalog-cover\" \"$PAGE\" \\\n        || (echo \"FAIL: $PAGE does not import from lib/catalog-cover\" >&2; exit 1)\n      # Palette helpers must still exist for fallback\n      grep -q \"issue-cover\" \"$PAGE\" \\\n        || (echo \"FAIL: issue-cover palette removed from $PAGE — fallback broken\" >&2; exit 1)\n\n      TEST=\"__tests__/lib/catalog-cover-fallback.test.ts\"\n      ls \"$TEST\" || (echo \"FAIL: $TEST missing\" >&2; exit 1)\n      TEST_COUNT=$(grep -cE \"it\\(|test\\(\" \"$TEST\" || echo 0)\n      echo \"TEST_COUNT=${TEST_COUNT}\"\n      if [ \"$TEST_COUNT\" -lt 4 ]; then\n        echo \"FAIL: Expected >= 4 tests in $TEST, found $TEST_COUNT\" >&2\n        exit 1\n      fi\n\n      echo \"=== all grep assertions passed ===\"\n\n  # ---------------------------------------------------------------------------\n  # 7. VERIFY-TESTS — run bun test suite\n  # ---------------------------------------------------------------------------\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      TEST_FILE=\"__tests__/lib/catalog-cover-fallback.test.ts\"\n      if [ ! -f \"$TEST_FILE\" ]; then\n        echo \"FAIL: $TEST_FILE missing\" >&2\n        exit 1\n      fi\n      bun test \"$TEST_FILE\"\n\n  # ---------------------------------------------------------------------------\n  # 8. VERIFY-BUILD — best-effort Next.js build (may take 60-90s)\n  # ---------------------------------------------------------------------------\n  - id: verify-build\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      # set -e DELIBERATELY OMITTED — Next.js build can warn-but-succeed\n      OUTPUT=$(bun run build 2>&1 || bun run build 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"BUILD_RESULT=success\"\n      elif echo \"$OUTPUT\" | grep -qi 'No such file\\|command not found'; then\n        echo \"STATUS=skipped_no_build_script\"\n        echo \"DETAIL=No build script available in Archon CI — Vercel preview is authoritative.\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${OUTPUT}\" | tail -40\n        echo \"OPERATOR_NOTE: Build failed in Archon CI but Vercel preview will be authoritative.\"\n      fi\n      exit 0\n\n  # ---------------------------------------------------------------------------\n  # 9. COMMIT-AND-PUSH — idempotent checkout + commit + push\n  # ---------------------------------------------------------------------------\n  - id: commit-and-push\n    depends_on: [verify-build]\n    bash: |\n      set -euo pipefail\n      BASE_BRANCH=\"wo/storefront-catalog-real-covers-01\"\n      # Default to unique run-suffixed branch (avoids worktree collisions)\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      git checkout -b \"$BRANCH\" 2>/dev/null || git checkout \"$BRANCH\"\n\n      # Stage all WO-related files\n      git add lib/catalog-cover.ts\n      git add scripts/backfill-seed-covers.mjs\n      git add lib/seed-data/catalog.json\n      git add \"app/shops/[shopSlug]/catalog/page.tsx\"\n      git add __tests__/lib/catalog-cover-fallback.test.ts\n\n      # Optional PDP page if builder touched it\n      if [ -f \"app/shops/[shopSlug]/comic/[upc]/page.tsx\" ]; then\n        git add \"app/shops/[shopSlug]/comic/[upc]/page.tsx\" || true\n      fi\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(storefront): WO-STOREFRONT-CATALOG-REAL-COVERS-01 — real cover art in catalog\\n\\nPlugs the existing AutoFix cover resolution pipeline (POST /covers/lookup/batch)\\ninto the storefront catalog so real comic art renders instead of placeholder\\ncolored text plates.\\n\\nWhat changed:\\n  - scripts/backfill-seed-covers.mjs: one-shot script that calls the cover\\n    cascade for every UPC in lib/seed-data/catalog.json and writes resolved\\n    URLs back into the JSON. Operator-run; static after that.\\n  - lib/seed-data/catalog.json: cover_url populated for items the cascade\\n    could resolve (Lunar > comic_covers > PRH-ISBN > fuzzy > ai_pick).\\n  - lib/catalog-cover.ts: pure helpers (shouldShowPalette, getCoverImgProps)\\n    keep render logic out of the page component and testable from bun.\\n  - app/shops/[shopSlug]/catalog/page.tsx: renders <img src={item.cover_url}>\\n    when truthy, falls back to existing deterministic-palette colored plate\\n    when null. onError hides broken images.\\n  - __tests__/lib/catalog-cover-fallback.test.ts: 4+ bun tests on pure helpers.\\n\\nPolish bar: ROUGH OK (Mutant Market is beta tester, not paying — per plan\\ndocs/plans/2026-06-15-mutant-market-hq-readiness.md).\\n\\nExisting fallback (deterministic palette + first-two-words-of-series hero)\\npreserved intact for items the cascade can'\\''t resolve.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  # ---------------------------------------------------------------------------\n  # 10. OPEN-PR — idempotent PR against master\n  # ---------------------------------------------------------------------------\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # Read branch from current HEAD (commit-and-push set it as the unique run-suffixed branch)\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops-storefront\"\n      BASE=\"master\"\n      TITLE=\"feat(storefront): WO-STOREFRONT-CATALOG-REAL-COVERS-01 — real cover art in catalog\"\n\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-STOREFRONT-CATALOG-REAL-COVERS-01**.\"\n        echo \"\"\n        echo \"Plugs the existing \\`/covers/lookup/batch\\` cascade into the storefront\"\n        echo \"catalog so real cover art renders instead of placeholder colored text plates.\"\n        echo \"\"\n        echo \"## Changes\"\n        echo \"- \\`scripts/backfill-seed-covers.mjs\\` — one-shot operator script (run once, JSON commits as artifact)\"\n        echo \"- \\`lib/seed-data/catalog.json\\` — \\`cover_url\\` populated from cascade\"\n        echo \"- \\`lib/catalog-cover.ts\\` — pure helpers (testable from bun, no React mount)\"\n        echo \"- \\`app/shops/[shopSlug]/catalog/page.tsx\\` — \\`<img>\\` when \\`cover_url\\` truthy, palette fallback when null\"\n        echo \"- \\`__tests__/lib/catalog-cover-fallback.test.ts\\` — 4+ bun tests\"\n        echo \"\"\n        echo \"## Polish Bar: ROUGH OK\"\n        echo \"Mutant Market is a beta tester, not paying. Per parent plan\"\n        echo \"\\`docs/plans/2026-06-15-mutant-market-hq-readiness.md\\` storefront polish\"\n        echo \"table item #16.\"\n        echo \"\"\n        echo \"Existing deterministic-palette fallback is preserved for items the\"\n        echo \"cascade can't resolve. No crashes on broken image URLs.\"\n        echo \"\"\n        echo \"## Tests\"\n        echo \"\\`bun test __tests__/lib/catalog-cover-fallback.test.ts\\`\"\n        echo \"\"\n        echo \"## Verify (Vercel preview)\"\n        echo \"Load preview URL \\`/shops/bdc/catalog\\` and visually confirm real cover art.\"\n        echo \"\"\n        echo \"Generated by Archon bdc-storefront-catalog-real-covers workflow.\"\n      } > \"$BODY_FILE\"\n\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url \\\n          --template '{{range .}}{{.url}}{{end}}' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed but no PR\" >&2\n      exit 1\n\n  # ---------------------------------------------------------------------------\n  # 11. VERCEL-VERIFY — best-effort Vercel preview deployment check.\n  #     Vercel needs ~60-120s to build after PR open. Polls up to 4 minutes.\n  # ---------------------------------------------------------------------------\n  - id: vercel-verify\n    depends_on: [open-pr]\n    bash: |\n      # set -e DELIBERATELY OMITTED — Vercel deployments may legitimately be slow/queued\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      PROJECT=\"shopops-storefront\"\n\n      if [ -z \"${VERCEL_TOKEN:-}\" ]; then\n        echo \"STATUS=skipped_no_vercel_token\"\n        echo \"DETAIL=VERCEL_TOKEN not set in Archon env. Manifest will note PENDING preview check.\"\n        echo \"OPERATOR: Verify Vercel preview manually at https://vercel.com/bluedevilcollectibles/${PROJECT}/deployments\"\n        exit 0\n      fi\n\n      # Poll Vercel API for latest deployment on this branch (4 min budget)\n      for attempt in 1 2 3 4 5 6 7 8; do\n        DEPLOY=$(curl -sf \\\n          \"https://api.vercel.com/v6/deployments?projectId=${PROJECT}&meta-githubCommitRef=${BRANCH}&limit=1\" \\\n          -H \"Authorization: Bearer ${VERCEL_TOKEN}\" 2>&1)\n        # python3 single-line replaces jq for nested JSON parsing\n        STATE=$(echo \"$DEPLOY\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('deployments',[{}])[0].get('state','UNKNOWN'))\" 2>/dev/null || echo UNKNOWN)\n        URL=$(echo \"$DEPLOY\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('deployments',[{}])[0].get('url',''))\" 2>/dev/null || echo \"\")\n        CREATED=$(echo \"$DEPLOY\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('deployments',[{}])[0].get('createdAt',''))\" 2>/dev/null || echo \"\")\n        COMMIT=$(echo \"$DEPLOY\" | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('deployments',[{}])[0].get('meta',{}).get('githubCommitSha',''))\" 2>/dev/null || echo \"\")\n        echo \"attempt=${attempt} state=${STATE} url=${URL}\"\n        if [ \"$STATE\" = \"READY\" ]; then\n          echo \"STATUS=ok\"\n          echo \"VERCEL_URL=https://${URL}\"\n          echo \"VERCEL_STATE=READY\"\n          echo \"VERCEL_CREATED_AT=${CREATED}\"\n          echo \"VERCEL_COMMIT=${COMMIT}\"\n          exit 0\n        fi\n        if [ \"$STATE\" = \"ERROR\" ] || [ \"$STATE\" = \"CANCELED\" ]; then\n          echo \"STATUS=failed_unexpected\"\n          echo \"DETAIL=Vercel deployment state=${STATE}\"\n          exit 0\n        fi\n        sleep 30\n      done\n\n      echo \"STATUS=skipped_still_building\"\n      echo \"DETAIL=Vercel did not reach READY within 4 min poll. Last state: ${STATE}\"\n      echo \"OPERATOR: Re-check manually at https://vercel.com/bluedevilcollectibles/${PROJECT}/deployments\"\n      exit 0\n\n  # ---------------------------------------------------------------------------\n  # 12. BUILD-MANIFEST — CODE class manifest v2\n  # ---------------------------------------------------------------------------\n  - id: build-manifest\n    depends_on: [vercel-verify]\n    task_class: clerical\n    prompt: |\n      Build the Captain CI manifest v2 for WO-STOREFRONT-CATALOG-REAL-COVERS-01.\n      Class: CODE (single-repo, frontend-only, Vercel deploy is runtime proof).\n\n      ## Required Fields (manifest v2 format, EXACT labels)\n\n      WO: WO-STOREFRONT-CATALOG-REAL-COVERS-01\n      Builder: Major Build (Archon bdc-storefront-catalog-real-covers)\n\n      Files modified:\n        shopops-storefront/lib/seed-data/catalog.json\n        shopops-storefront/app/shops/[shopSlug]/catalog/page.tsx\n        [PDP page if touched per implement output]\n\n      Files created:\n        shopops-storefront/scripts/backfill-seed-covers.mjs\n        shopops-storefront/lib/catalog-cover.ts\n        shopops-storefront/__tests__/lib/catalog-cover-fallback.test.ts\n\n      Tests:\n        [count from verify-tests output] passing / [count] total\n        command: bun test __tests__/lib/catalog-cover-fallback.test.ts\n\n      PRs:\n        shopops-storefront (master): [from open-pr.output PR_URL]\n\n      Merge ancestors:\n        shopops-storefront master HEAD=[BRANCH_SHA from commit-and-push.output],\n        manifest commit, behind_by: 0\n\n      Grep assertions:\n        grep -c 'item.cover_url' app/shops/[shopSlug]/catalog/page.tsx  # >= 1\n        grep -c 'shouldShowPalette\\|getCoverImgProps' lib/catalog-cover.ts  # >= 2\n        python3 -c \"import json; d=json.load(open('lib/seed-data/catalog.json')); print(sum(1 for x in d if x.get('cover_url')))\"\n        [include actual count from verify-grep.output COVER_URLS_POPULATED]\n\n      Runtime verification: N/A (CODE class, frontend-only — Vercel preview is the runtime proof)\n\n      Vercel deployment:\n        [VERCEL_URL, VERCEL_STATE, VERCEL_CREATED_AT, VERCEL_COMMIT from vercel-verify.output]\n        If STATUS=skipped_* or failed, note that explicitly and ask operator to re-verify.\n\n      VALIDATION: PASS (Polish bar: ROUGH OK per WO §10 — Mutant Market beta)\n\n      ## Inputs\n      $read-spec.output\n      $inspect-current-state.output\n      $probe-cover-api.output\n      $plan.output\n      $verify-grep.output\n      $verify-tests.output\n      $verify-build.output\n      $commit-and-push.output\n      $open-pr.output\n      $vercel-verify.output\n\n  # ---------------------------------------------------------------------------\n  # 13. FLIP-NOTION — set Notes property + Claude Status = REVIEW (Rule 7)\n  # ---------------------------------------------------------------------------\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-STOREFRONT-CATALOG-REAL-COVERS-01 to REVIEW with the manifest\n      in the Notes PROPERTY (not as a page comment — Captain CI reads Notes, not comments).\n\n      # OPEN QUESTION (Q1): Notion page ID NOT confirmed at draft time.\n      # Operator MUST replace 35fef4c9-ce00-8193-9f29-dabf85f07390 with the real UUID before firing.\n      # Find it: query Notion database a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # for title containing \"WO-STOREFRONT-CATALOG-REAL-COVERS-01\".\n      Notion database ID: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      Notion page ID: 35fef4c9-ce00-8193-9f29-dabf85f07390\n\n      Required Notion changes (single update call):\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> set to the manifest text from $build-manifest.output verbatim\n      - claimed_by: Major Build (Archon bdc-storefront-catalog-real-covers)\n      - claimed_at: 2026-05-13\n      - plan tag: plan:A-or-B\n\n      After update, log the Notion page URL to run output for audit trail.\n\n      If Notion MCP is unavailable in this runtime, output:\n        XO_TODO: Flip WO-STOREFRONT-CATALOG-REAL-COVERS-01 to REVIEW in Notion.\n                 Paste manifest from $build-manifest.output into Notes property.\n                 Set Claude Status = REVIEW, Status = Review.\n\n      ## Manifest\n      $build-manifest.output\n",
  "bdc-storefront-footer-spacing": "# Drafted by yaml-author for WO-STOREFRONT-FOOTER-SPACING-01 on 2026-05-13\n# Source pattern: WO-STOREFRONT-PORTAL-AUTH-WIRE-01.yaml (storefront single-repo)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight (4-block):\n#   1. YAML exists: this file\n#   2. Spec exists: docs/work-orders/WO-STOREFRONT-FOOTER-SPACING-01.md in bdc-xo\n#   3. CWD scope: SINGLE-REPO (shopops-storefront only)\n#   4. Codebase registered: bluedevilcollectibles/shopops-storefront\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-STOREFRONT-FOOTER-SPACING-01.yaml\n#\n# Polish bar: ROUGH OK — ~30 minute cosmetic fix. Skip exhaustive cross-browser tests.\n\nname: bdc-storefront-footer-spacing\ndescription: |\n  Use when: Executing WO-STOREFRONT-FOOTER-SPACING-01 — fix run-together footer nav text.\n  Input: WO_ID = WO-STOREFRONT-FOOTER-SPACING-01.\n  Does:\n    - Locates the footer component via grep on Catalog + Pull List + Live Shows + About strings.\n    - Adds spacing (Tailwind gap-X or visible divider) between nav items.\n    - Verifies no TypeScript regressions.\n    - Builds CODE manifest and flips Notion to REVIEW.\n  Repo: bluedevilcollectibles/shopops-storefront.\n  parallel_ok: true.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-STOREFRONT-FOOTER-SPACING-01}\"\n      # Primary: docs/work-orders/<WO_ID>.md (gh --template, no jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      # Fallback: glob docs/superpowers/specs/*<WO_ID>*.md\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n--- SPEC ---\\n%s\\n' \\\n        \"$WO_ID\" \"$WORKFLOW_ID\" \"$SPEC_CONTENT\"\n\n  - id: fresh-branch\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      # Defensive: default branch may be held by another worktree.\n      if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n        echo \"branch held by other worktree; staying on current HEAD\"\n      fi\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n      BASE_BRANCH=\"wo/storefront-footer-spacing-01\"\n      # Default to unique run-suffixed branch to avoid worktree collisions.\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      git checkout -b \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: locate-footer\n    depends_on: [fresh-branch]\n    bash: |\n      # set -e DELIBERATELY OMITTED — audit must NEVER fail (BUG-3 tolerant)\n      echo \"=== grep for footer-component candidates ===\"\n      if [ -d app ]; then\n        grep -rln \"Pull List\" app/ 2>/dev/null | head -20 || true\n        echo \"---\"\n        grep -rln \"Catalog.*Pull List\\|footer\\|<footer\" app/ 2>/dev/null | head -20 || true\n        echo \"---\"\n        # Surface shared layouts (Next.js app/ layout, no src/ in storefront):\n        if [ -d \"app/shops/[shopSlug]/portal\" ]; then\n          ls -la \"app/shops/[shopSlug]/portal/\" 2>/dev/null || true\n        fi\n        if [ -d \"app/shops/[shopSlug]\" ]; then\n          ls -la \"app/shops/[shopSlug]/\" 2>/dev/null || true\n        fi\n      else\n        echo \"(app/ not found — non-Next.js layout?)\"\n      fi\n      # Also tolerate components/ at root\n      if [ -d components ]; then\n        grep -rln \"Pull List\\|<footer\" components/ 2>/dev/null | head -10 || true\n      fi\n      exit 0\n\n  - id: implement\n    depends_on: [locate-footer]\n    task_class: small_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 4\n      prompt: |\n        Implement WO-STOREFRONT-FOOTER-SPACING-01. CWD = shopops-storefront repo root.\n\n        ## Spec\n        $read-spec.output\n\n        ## Footer location hints\n        $locate-footer.output\n\n        ## Execution rules\n        1. Locate the file rendering the run-together \"CatalogPull ListLive ShowsAbout\"\n           footer. It is in one of:\n             - app/shops/[shopSlug]/portal/page.tsx\n             - app/shops/[shopSlug]/portal/layout.tsx\n             - app/shops/[shopSlug]/layout.tsx\n             - a shared component imported from app/components/ or similar\n           Confirm via additional grep if the locate-footer node didn't surface it.\n        2. Fix the spacing. Two acceptable patterns:\n           (a) Tailwind: wrap the nav in `<nav className=\"flex gap-6 ...\">`\n           (b) Visible divider: each item suffixed with `<span className=\"px-2\">|</span>`\n           Pick (a) (gap utility) unless the existing design language already uses\n           visible dividers somewhere on the page.\n        3. DO NOT redesign the footer. Same links, same text, same order. Only fix spacing.\n        4. Verify the markup renders each link as a SEPARATE <a> or <Link> element\n           (not concatenated into a single text node).\n        5. Run `bunx tsc --noEmit` and confirm exit 0.\n        6. Run `bun test` and confirm no new failures.\n        7. Do NOT push.\n\n        V1 limitations (ROUGH OK): no cross-browser screenshot loop, no a11y audit,\n        no mobile-specific design pass beyond \"doesn't break existing responsive\n        pattern.\"\n\n        Output <promise>COMPLETE</promise> when fix applied + tsc clean + tests pass.\n\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      # Confirm 4 link texts still present and are separately rendered\n      FILES=$(grep -rl \"Pull List\" app/ 2>/dev/null | head -5)\n      echo \"Files containing 'Pull List': $FILES\"\n      [ -n \"$FILES\" ] || (echo \"FAIL: no file references 'Pull List'\" >&2; exit 1)\n      # Confirm gap-* class OR explicit divider added in at least one of the candidate files\n      MATCH=0\n      for f in $FILES; do\n        if grep -qE \"gap-[0-9]+|px-[0-9]+|className=.*flex.*gap|<span[^>]*>\\s*\\|\" \"$f\"; then\n          echo \"Spacing found in $f\"\n          MATCH=1\n        fi\n      done\n      [ \"$MATCH\" -eq 1 ] || (echo \"FAIL: no gap-* or divider in any footer candidate\" >&2; exit 1)\n      echo \"verify-grep PASS\"\n\n  - id: verify-tsc\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      bunx tsc --noEmit\n\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tsc]\n    bash: |\n      set -euo pipefail\n      # Use the actual current branch (fresh-branch creates a unique run-suffixed one).\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      echo \"Committing on BRANCH=${BRANCH}\"\n      git add -A\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'fix(storefront): WO-STOREFRONT-FOOTER-SPACING-01 — separate footer nav items\\n\\nAdds gap-* (or divider) between Catalog / Pull List / Live Shows / About\\nin the storefront portal footer. No other changes.\\n\\nROUGH-OK polish bar — see WO §13 for v1 limitations.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"SHA=$(git rev-parse HEAD)\"\n      echo \"BRANCH=${BRANCH}\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops-storefront\"\n      EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n        --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"PR=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo \"$REPO\" --base main --head \"$BRANCH\" \\\n            --title \"fix(storefront): WO-STOREFRONT-FOOTER-SPACING-01 — separate footer nav items\" \\\n            --body \"ROUGH-OK polish bar. Staging URL: https://staging.comicstoreos.com/shops/bdc/portal/statements\" 2>&1); then\n          echo \"PR=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      echo \"open-pr failed\" >&2; exit 1\n\n  - id: runtime-verify\n    depends_on: [open-pr]\n    bash: |\n      # Verify staging shows separated items (best-effort — Vercel may not have deployed yet)\n      R=$(curl -s \"https://staging.comicstoreos.com/shops/bdc/portal/statements\" 2>&1 | head -c 4000)\n      EXIT=$?\n      if [ $EXIT -eq 0 ] && echo \"$R\" | grep -q \"Pull List\"; then\n        if echo \"$R\" | grep -qE \"gap-[0-9]+|px-[0-9]+|<span[^>]*>\\s*\\|\"; then\n          echo \"STATUS=ok\"\n          echo \"STAGING_HAS_SPACING=true\"\n        else\n          echo \"STATUS=skipped_vercel_not_deployed\"\n          echo \"DETAIL=Staging fetched but spacing markup not present; awaiting Vercel deploy\"\n        fi\n      else\n        echo \"STATUS=skipped_no_response\"\n        echo \"DETAIL=Staging unreachable; Captain CI verifies post-Vercel-deploy\"\n      fi\n      exit 0\n\n  - id: build-manifest\n    depends_on: [runtime-verify]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 for WO-STOREFRONT-FOOTER-SPACING-01 (CODE class, ROUGH-OK).\n\n      WO: WO-STOREFRONT-FOOTER-SPACING-01\n      Builder: Devils Cauldron (Archon bdc-storefront-footer-spacing)\n      Polish bar: ROUGH OK — ~30 min cosmetic fix.\n\n      Files modified:\n       - [actual file from $verify-grep.output]\n\n      Tests:\n       - bunx tsc --noEmit -> exit 0\n       - bun test -> no new failures\n\n      PRs:\n       - shopops-storefront: [from $open-pr.output PR]\n\n      Merge ancestors:\n       - shopops-storefront main HEAD: [from $commit-and-push.output SHA], behind_by: 0\n\n      Grep assertions:\n       - grep -E 'gap-[0-9]+|px-[0-9]+|<span[^>]*>\\s*\\|' on footer file returns >= 1 match\n       - 4 link texts still present (Catalog, Pull List, Live Shows, About)\n\n      Runtime verification:\n       - curl -s https://staging.comicstoreos.com/shops/bdc/portal/statements\n       - [from $runtime-verify.output]\n\n      Vercel deployment:\n       - staging.comicstoreos.com/shops/bdc/portal/statements (pending Captain CI verify after merge)\n\n      VALIDATION: PASS (pending Vercel deploy + visual verify)\n\n      Inputs:\n      $verify-grep.output\n      $commit-and-push.output\n      $open-pr.output\n      $runtime-verify.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-STOREFRONT-FOOTER-SPACING-01 to REVIEW.\n      Manifest goes in Notes property (NOT comment).\n\n      # OPEN QUESTION: Notion page ID. Operator filters by exact WO ID equality.\n\n      Update:\n       - Claude Status -> REVIEW\n       - Status -> Review\n       - Notes -> $build-manifest.output verbatim\n       - claimed_by: Devils Cauldron\n       - claimed_at: today\n\n      If Notion MCP unavailable, output XO_TODO with the manifest.\n\n      Manifest:\n      $build-manifest.output\n",
  "bdc-storefront-portal-auth-wire": "# Drafted by yaml-author subagent for WO-STOREFRONT-PORTAL-AUTH-WIRE-01 on 2026-05-13\n# Source pattern: WO-SHOPOPS-COMMERCE-SPINE-01.yaml (MIXED shopops pattern) +\n#                 bdc-ce-regression-suite.yaml (gold-standard DAG shape, idempotent checkout,\n#                 multi-line-safe grep, flip-notion shape)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight (OPERATOR MUST VERIFY BEFORE FIRING):\n#   1. bluedevilcollectibles/shopops registered in Archon:\n#      sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name FROM remote_agent_codebases\"\n#      CWD when fired = shopops repo root (shopops-api/ is a subdir inside it).\n#   2. bluedevilcollectibles/shopops-storefront NOT required to be an Archon codebase —\n#      storefront work is done via git clone into /tmp in the shopops worktree context.\n#      GH_TOKEN (GITHUB_TOKEN) must be set in Archon env with push access to both repos.\n#   3. This WO is parallel_ok=NO. Do NOT fire while another shopops-api WO is in-flight.\n#   4. WO spec committed + pushed to bdc-xo main branch:\n#      docs/work-orders/WO-STOREFRONT-PORTAL-AUTH-WIRE-01.md\n#   5. Notion page ID for WO-STOREFRONT-PORTAL-AUTH-WIRE-01 — FILL IN below before firing.\n#      Find via: Notion search on database a6df831c-0b52-449f-8ca4-d77be6b70d0a\n#\n# OPEN QUESTION:\n#   Q1: The storefront uses Next.js App Router. The cookie-forwarding pattern in\n#       lib/storefront-api.ts must use `cookies()` from `next/headers` (RSC-only).\n#       If storefront-api.ts is also called from client components, the pattern breaks.\n#       Builder must inspect lib/storefront-api.ts callsites before implementing.\n#       If client-component calls exist, a split helper (server + client variants) is required.\n#   Q2: The shopops repo default branch is \"master\" (confirmed from existing YAML).\n#       shopops-storefront default branch assumed \"main\" — verify before firing.\n#   Q3: Notion page ID is UNKNOWN in this draft. Operator must fill in\n#       the `35fef4c9-ce00-811d-bf39-c4dc02a5bdc9` value in the flip-notion node prompt before deploying.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh bdc-storefront-portal-auth-wire.yaml\n#   (copy this file to bdc-harness/.archon/workflows/defaults/ first)\n#\n# REVIEW BEFORE DEPLOY — human verifies:\n#   - Notion page ID filled in (Q3 above)\n#   - storefront default branch confirmed (Q2 above)\n#   - cookie-forwarding callsite audit done (Q1 above)\n#   - schema column names in WO §9 match actual Supabase schema before first fire\n#   - Rule 19 runtime verification will be PENDING at PR-open time (expected)\n\nname: bdc-storefront-portal-auth-wire\ndescription: |\n  Use when: Executing WO-STOREFRONT-PORTAL-AUTH-WIRE-01 — Storefront Portal Auth Wire.\n  Input: WO_ID = WO-STOREFRONT-PORTAL-AUTH-WIRE-01.\n  Does: Reads WO spec from bdc-xo (cross-repo via gh api). Verifies order_lines +\n        shipping_queue + billing_statement_lines schema via Supabase REST (best-effort).\n        Adds GET /me/orders, GET /me/orders/:id, GET /me/statements, GET /me/statements/:id\n        to shopops-api/routes/storefront-portal-data.js with status vocab mapping\n        (SOLD->PENDING, PAID->PROCESSING, SHIPPED+tracking->DELIVERED, VOID->filter).\n        Clones shopops-storefront into /tmp and wires portal_jwt cookie forwarding in\n        lib/storefront-api.ts using cookies() from next/headers.\n        Runs bun tests for both repos. Opens a PR for shopops (base: master) and a\n        separate PR for shopops-storefront (base: main). Runs Rule 19 docker exec grep\n        to prove source is live in shopops-api container. Builds MIXED manifest.\n        Flips Notion WO to REVIEW.\n  Repos: bluedevilcollectibles/shopops (primary, Archon CWD = shopops repo root,\n         shopops-api/ is a subdir) + bluedevilcollectibles/shopops-storefront (cloned\n         to /tmp/storefront-work in the bash nodes).\n         WO spec lives in bluedevilcollectibles/bdc-xo.\n  parallel_ok: false — touches 2 repos, serial only.\n  NOT for: any other WO.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  # ---------------------------------------------------------------------------\n  # 1. READ-SPEC — fetch WO spec + behavior spec from bdc-xo (cross-repo)\n  #    Rule 5: tries direct path first, then date-prefix glob fallback.\n  # ---------------------------------------------------------------------------\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-STOREFRONT-PORTAL-AUTH-WIRE-01}\"\n\n      # Primary path: docs/work-orders/<WO_ID>.md (gh --template, no jq dependency)\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      # Fallback: glob docs/superpowers/specs/*<WO_ID>*.md (Rule 5)\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      # Fetch behavior spec section for status vocab reference\n      BEHAVIOR_SPEC=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/behavior-specs/COMMERCE_CHANNEL_SPINE_BEHAVIOR.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:docs/work-orders/%s.md\\nWORKFLOW_ID=%s\\n\\n' \\\n        \"$WO_ID\" \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n\\n' \"$SPEC_CONTENT\"\n      printf -- '--- BEHAVIOR SPEC (§6+§7 context) ---\\n%s\\n' \"$BEHAVIOR_SPEC\"\n\n  # ---------------------------------------------------------------------------\n  # 2. SCHEMA-VERIFY — confirm order_lines + shipping_queue + billing table\n  #    columns via Supabase REST BEFORE writing any query (Rule 17 + WO §9).\n  #    Best-effort: Supabase can legitimately 401/429/503.\n  # ---------------------------------------------------------------------------\n  - id: schema-verify\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step per doctrine wrapper\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"\n        echo \"DETAIL=SUPABASE_URL or SUPABASE_SERVICE_KEY not set in Archon env.\"\n        echo \"Operator must verify schema manually before deploy using WO §9 queries.\"\n        exit 0\n      fi\n\n      check_table_columns() {\n        local TABLE=\"$1\"\n        RESULT=$(curl -sf \\\n          \"${SUPABASE_URL}/rest/v1/information_schema.columns?table_name=eq.${TABLE}&select=column_name,data_type&table_schema=eq.public\" \\\n          -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n          -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" \\\n          2>&1) || true\n        if echo \"$RESULT\" | grep -q '\"column_name\"'; then\n          echo \"SCHEMA_${TABLE}=VERIFIED\"\n          echo \"COLUMNS_${TABLE}=${RESULT}\"\n        else\n          echo \"SCHEMA_${TABLE}=UNVERIFIED\"\n          echo \"DETAIL_${TABLE}=${RESULT}\"\n        fi\n      }\n\n      check_table_columns \"order_lines\"\n      check_table_columns \"shipping_queue\"\n      check_table_columns \"billing_statements\"\n      check_table_columns \"billing_statement_lines\"\n\n      # Check for DELIVERED column that must NOT be added (WO §7 rule)\n      DELIVERED_CHECK=$(curl -sf \\\n        \"${SUPABASE_URL}/rest/v1/information_schema.columns?table_name=eq.orders&column_name=eq.delivered&select=column_name\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n        -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" \\\n        2>&1) || true\n      if echo \"$DELIVERED_CHECK\" | grep -q '\"column_name\"'; then\n        echo \"SCHEMA_orders_delivered_column=EXISTS_UNEXPECTED\"\n        echo \"DETAIL=orders table has a 'delivered' column. WO spec says DELIVERED must be DERIVED, not stored.\"\n      else\n        echo \"SCHEMA_orders_delivered_column=ABSENT_AS_EXPECTED\"\n      fi\n\n      echo \"STATUS=ok\"\n      exit 0\n\n  # ---------------------------------------------------------------------------\n  # 3. PLAN — architect both sides of the split-repo implementation\n  # ---------------------------------------------------------------------------\n  - id: plan\n    depends_on: [read-spec, schema-verify]\n    task_class: medium_work\n    prompt: |\n      Plan the split-repo portal auth wire implementation.\n\n      WO: WO-STOREFRONT-PORTAL-AUTH-WIRE-01\n      Class: MIXED (CODE in 2 repos + Rule 19 runtime deploy on shopops-api)\n\n      ## Required Output (Markdown, exact headings)\n\n      ### shopops-api (repo: bluedevilcollectibles/shopops, subdir: shopops-api/)\n      - File to modify: shopops-api/routes/storefront-portal-data.js\n      - Existing routes NOT to touch: GET /purchases (lines 312-326), GET /statements (lines 77-95), GET /statements/:id (lines 101-126)\n      - New routes to add (all behind requirePortalAuth middleware, already applied router-wide):\n        - GET /me/orders — enriched orders list: joins orders + order_lines + shipping_queue\n          WHERE customer_id = req.customerId AND tenant_id = req.tenantId.\n          Status mapping: SOLD->PENDING, PAID->PROCESSING,\n          SHIPPED + shipping_queue.tracking_number IS NOT NULL -> DELIVERED,\n          SHIPPED without tracking -> SHIPPED, CANCELLED->CANCELLED, REFUNDED->REFUNDED.\n          Response shape: { ok: true, orders: [...], total: N }\n        - GET /me/orders/:id — single order by id, same joins + status mapping.\n          Returns 404 if order not found or belongs to different customer/tenant.\n          Response shape: { ok: true, order: { ... with order_lines array } }\n        - GET /me/statements — statements list: SELECT from billing_statements\n          WHERE customer_id = req.customerId AND tenant_id = req.tenantId.\n          VOID rows are filtered out (never returned).\n          Status mapping: DRAFT/APPROVED/GENERATED->PENDING, SENT/PARTIAL->OUTSTANDING,\n          PAID->SETTLED. (Read COMMERCE_CHANNEL_SPINE_BEHAVIOR.md §7 for exact mapping.)\n          Response shape: { ok: true, statements: [...], total: N }\n        - GET /me/statements/:id — single statement with line_items array from\n          billing_statement_lines. Returns 404 if not found or wrong customer/tenant.\n          Response shape: { ok: true, statement: { ...with line_items array } }\n      - Status vocab: SOLD/PAID/DRAFT/APPROVED/GENERATED/SENT/PARTIAL must NOT appear in any\n        /me/* response. Builder must implement mapOrderStatus() and mapStatementStatus() helpers.\n      - DELIVERED rule: derived from shipping_queue.status='SHIPPED' AND tracking_number IS NOT NULL.\n        Do NOT add a 'delivered' column to orders table.\n      - Test file: shopops-api/tests/test_storefront_portal_me_routes.js (bun-compatible)\n        Minimum 5 tests asserting REAL behavior (not \"did not throw\"):\n          a. GET /me/orders with valid portal_jwt returns 200 + { ok: true, orders: array }\n          b. SOLD order appears as PENDING in response (vocab mapping enforced)\n          c. GET /me/statements VOID rows are absent\n          d. GET /me/orders/:id DELIVERED derivation: SHIPPED + tracking_number -> DELIVERED\n          e. GET /me/statements/:id returns line_items array\n      - Commit message: \"feat(api): WO-STOREFRONT-PORTAL-AUTH-WIRE-01 — /me/orders + /me/statements portal routes\"\n      - Branch: wo/storefront-portal-auth-wire-01 (off master)\n      - PR base: master\n\n      ### shopops-storefront (repo: bluedevilcollectibles/shopops-storefront)\n      - File to modify: lib/storefront-api.ts\n      - Change: fetchOrders(), fetchOrder(), fetchStatements(), fetchStatement() already call\n        the correct /storefront/portal/me/* URLs. Add portal_jwt cookie forwarding so the\n        fetch() call includes Cookie: portal_jwt=<value> header.\n      - Pattern: import { cookies } from 'next/headers'. In each function, call\n        cookies().get('portal_jwt')?.value and add it as a Cookie header.\n        Use conditional: if token exists, add header; if absent, proceed without (seed mode).\n        Do NOT break STOREFRONT_DATA_SOURCE=seed path — seed path must skip the API call entirely.\n      - Test file: __tests__/lib/storefront-api.test.ts (or existing test pattern in repo)\n        Minimum 4 tests:\n          a. fetchOrders() includes Cookie header when portal_jwt cookie is present\n          b. fetchOrders() does NOT call SHOPOPS_API_URL when STOREFRONT_DATA_SOURCE=seed\n          c. fetchStatements() includes portal_jwt in Cookie header\n          d. fetchStatement() returns seed data correctly when seed mode active\n      - Commit message: \"feat(storefront): WO-STOREFRONT-PORTAL-AUTH-WIRE-01 — portal_jwt cookie forwarding in storefront-api\"\n      - Branch: wo/storefront-portal-auth-wire-01 (off main)\n      - PR base: main (verify storefront default branch before firing)\n\n      ### Schema-verify output\n      $schema-verify.output\n\n      ### Spec + Behavior Spec\n      $read-spec.output\n\n  # ---------------------------------------------------------------------------\n  # 4. IMPLEMENT-API — write /me/* routes in shopops-api/routes/storefront-portal-data.js\n  #    CWD is shopops repo root. shopops-api/ is the subdir.\n  # ---------------------------------------------------------------------------\n  - id: implement-api\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the shopops-api side of WO-STOREFRONT-PORTAL-AUTH-WIRE-01.\n        CWD is the shopops repo root. shopops-api/ is a subdirectory.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n\n        1. Read shopops-api/routes/storefront-portal-data.js in full before making any edits.\n           Understand the existing router setup and requirePortalAuth middleware placement.\n           Do NOT touch GET /purchases, GET /statements, GET /statements/:id.\n           The router already has requirePortalAuth applied router-wide — new /me/* routes\n           inherit it automatically.\n\n        2. Read shopops-api/middleware/requirePortalAuth.js to confirm it sets\n           req.customerId and req.tenantId before writing any queries.\n\n        3. BEFORE writing the SQL queries, output the schema-verify columns for\n           order_lines and shipping_queue from the plan context. If schema-verify\n           was skipped (no Supabase env), use the known columns from WO §9 and\n           add a comment \"// columns verified manually per WO §9 audit 2026-05-13\".\n\n        4. Add these four routes to shopops-api/routes/storefront-portal-data.js:\n\n           a. GET /me/orders\n              - SELECT orders.*, shipping_queue.tracking_number, shipping_queue.status AS sq_status,\n                shipping_queue.carrier, shipping_queue.shipped_at\n                FROM orders\n                LEFT JOIN shipping_queue ON shipping_queue.order_id = orders.id\n                  AND shipping_queue.tenant_id = req.tenantId\n                WHERE orders.customer_id = req.customerId\n                  AND orders.tenant_id = req.tenantId\n                ORDER BY orders.created_at DESC\n              - Map each row with mapOrderStatus() — implement this helper function\n              - Return { ok: true, orders: mappedRows, total: mappedRows.length }\n\n           b. GET /me/orders/:id\n              - Same joins as above plus\n                LEFT JOIN order_lines ON order_lines.order_id = orders.id\n                WHERE orders.id = req.params.id\n                  AND orders.customer_id = req.customerId\n                  AND orders.tenant_id = req.tenantId\n              - Return 404 { ok: false, error: 'Not found' } if no row returned\n              - Group order_lines into order.line_items array\n              - Return { ok: true, order: mappedOrder }\n\n           c. GET /me/statements\n              - SELECT * FROM billing_statements\n                WHERE customer_id = req.customerId\n                  AND tenant_id = req.tenantId\n                  AND status != 'VOID'\n                ORDER BY period_end DESC\n              - Map each row with mapStatementStatus() — implement this helper\n              - Return { ok: true, statements: mappedRows, total: mappedRows.length }\n\n           d. GET /me/statements/:id\n              - SELECT s.*, sl.id as line_id, sl.description, sl.qty,\n                sl.unit_price_cents, sl.line_total_cents\n                FROM billing_statements s\n                LEFT JOIN billing_statement_lines sl ON sl.statement_id = s.id\n                  AND sl.tenant_id = req.tenantId\n                WHERE s.id = req.params.id\n                  AND s.customer_id = req.customerId\n                  AND s.tenant_id = req.tenantId\n              - Return 404 if no statement row\n              - Group billing_statement_lines into statement.line_items array\n              - Return { ok: true, statement: mappedStatement }\n\n        5. Implement mapOrderStatus(order, sqStatus, trackingNumber):\n           - SOLD -> PENDING\n           - PAID -> PROCESSING\n           - SHIPPED + trackingNumber IS NOT NULL -> DELIVERED\n           - SHIPPED (no tracking) -> SHIPPED\n           - CANCELLED -> CANCELLED\n           - REFUNDED -> REFUNDED\n           - default -> order.status (pass through with warning log)\n\n        6. Implement mapStatementStatus(dbStatus):\n           - DRAFT / APPROVED / GENERATED -> PENDING\n           - SENT / PARTIAL -> OUTSTANDING\n           - PAID -> SETTLED\n           - VOID -> never reached (filtered in query)\n           - default -> dbStatus (pass through with warning log)\n\n        7. Create shopops-api/tests/test_storefront_portal_me_routes.js:\n           - Use bun test runner (import { describe, it, expect } from 'bun:test')\n           - At minimum 5 tests asserting REAL behavior:\n             a. mapOrderStatus('SOLD', null, null) === 'PENDING'\n             b. mapOrderStatus('PAID', null, null) === 'PROCESSING'\n             c. mapOrderStatus('SHIPPED', 'SHIPPED', '1ZTRACK123') === 'DELIVERED'\n             d. mapStatementStatus('DRAFT') === 'PENDING'\n             e. mapStatementStatus('SENT') === 'OUTSTANDING'\n             f. mapStatementStatus('PAID') === 'SETTLED'\n             g. VOID filter: verify query excludes VOID (unit test on filter logic or mock)\n           - Export mapOrderStatus and mapStatementStatus from the route file, OR\n             factor them into shopops-api/lib/portalStatusMap.js and import from there.\n             Either approach is valid; be consistent.\n\n        8. Do NOT deploy. Do NOT push. Do NOT run docker commands.\n           Do NOT modify GET /purchases, GET /statements (non-/me/ paths).\n\n        ## Completion Criteria\n        Output <promise>COMPLETE</promise> only when:\n          - shopops-api/routes/storefront-portal-data.js has all 4 /me/* routes\n          - mapOrderStatus() and mapStatementStatus() are implemented\n          - GET /purchases and GET /statements still present and UNCHANGED\n          - shopops-api/tests/test_storefront_portal_me_routes.js exists with >= 5 real-behavior tests\n          - No backend status vocab (SOLD, PAID, DRAFT, APPROVED, GENERATED, SENT, PARTIAL)\n            appears in any /me/* response path\n\n  # ---------------------------------------------------------------------------\n  # 5a. SETUP-STOREFRONT-CLONE — bash clone of storefront repo.\n  #     Runs after plan so both impl nodes can start without blocking each other.\n  # ---------------------------------------------------------------------------\n  - id: setup-storefront-clone\n    depends_on: [plan]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      STOREFRONT_DIR=\"/tmp/storefront-work-$WORKFLOW_ID\"\n\n      # Idempotent: if already cloned from a prior run, reset to \"origin/$DEFAULT_BRANCH\"\n      if [ -d \"$STOREFRONT_DIR/.git\" ]; then\n        cd \"$STOREFRONT_DIR\"\n        git fetch origin || true\n        # Tolerate worktree-locked default branch\n        if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n          echo \"default branch held by other worktree in storefront clone; staying on current HEAD\"\n        fi\n        git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n        echo \"STOREFRONT_DIR=${STOREFRONT_DIR}\"\n        echo \"STATUS=reused_existing_clone\"\n        exit 0\n      fi\n\n      git clone \"https://github.com/bluedevilcollectibles/shopops-storefront.git\" \\\n        \"$STOREFRONT_DIR\" --depth=50\n      echo \"STOREFRONT_DIR=${STOREFRONT_DIR}\"\n      echo \"STATUS=fresh_clone\"\n\n  # ---------------------------------------------------------------------------\n  # 5b. IMPLEMENT-STOREFRONT — wire cookie forwarding in shopops-storefront\n  #     Depends on setup-storefront-clone for the /tmp clone.\n  # ---------------------------------------------------------------------------\n  - id: implement-storefront\n    depends_on: [plan, setup-storefront-clone]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement the shopops-storefront side of WO-STOREFRONT-PORTAL-AUTH-WIRE-01.\n\n        The storefront repo is cloned at /tmp/storefront-work-$WORKFLOW_ID.\n        All file reads and edits use absolute paths under that directory.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n\n        1. Read /tmp/storefront-work-$WORKFLOW_ID/lib/storefront-api.ts in full before editing.\n           Understand the existing fetchOrders(), fetchOrder(), fetchStatements(),\n           fetchStatement() function signatures and the STOREFRONT_DATA_SOURCE=seed guard.\n\n        2. The goal: when STOREFRONT_DATA_SOURCE != 'seed', each of the four fetch functions\n           must forward the portal_jwt cookie to shopops-api.\n\n        3. Add cookie forwarding using Next.js App Router pattern:\n           import { cookies } from 'next/headers';\n           const portalToken = cookies().get('portal_jwt')?.value;\n           const cookieHeader = portalToken ? { Cookie: `portal_jwt=${portalToken}` } : {};\n           Then pass cookieHeader into the fetch() options headers.\n\n        4. The seed guard MUST remain intact. Pattern:\n           if (process.env.STOREFRONT_DATA_SOURCE === 'seed') {\n             return loadSeedData(...)  // existing seed path — NO HTTP call\n           }\n           // real API path with cookie forwarding follows\n\n        5. Do NOT change the URL paths. They already call /storefront/portal/me/orders — correct.\n\n        6. Create or update __tests__/lib/storefront-api.test.ts (check if test file\n           exists first under /tmp/storefront-work-$WORKFLOW_ID; if the project uses\n           a different test path, use that).\n           Minimum 4 tests asserting REAL behavior:\n             a. fetchOrders() calls fetch with Cookie: portal_jwt=<value> when cookie present\n             b. fetchOrders() does NOT call fetch when STOREFRONT_DATA_SOURCE=seed\n             c. fetchStatements() includes portal_jwt Cookie header when token present\n             d. cookies() helper is called once per fetchOrders() invocation\n\n        7. Do NOT modify any page components. Only lib/storefront-api.ts changes.\n        8. Do NOT push. Do NOT run deployment commands.\n\n        ## Completion Criteria\n        Output <promise>COMPLETE</promise> only when:\n          - /tmp/storefront-work-$WORKFLOW_ID/lib/storefront-api.ts has cookie forwarding\n            in all 4 fetch functions\n          - Seed path guard is preserved\n          - Test file exists with >= 4 real-behavior tests\n          - No URL changes to /storefront/portal/me/* paths\n\n  # ---------------------------------------------------------------------------\n  # 6. VERIFY-GREP-API — structural assertions on the shopops-api side\n  # ---------------------------------------------------------------------------\n  - id: verify-grep-api\n    depends_on: [implement-api]\n    bash: |\n      set -euo pipefail\n      echo \"=== shopops-api grep assertions ===\"\n\n      ROUTES_FILE=\"shopops-api/routes/storefront-portal-data.js\"\n\n      # New /me/* routes present — separate greps (multi-line safe per doctrine)\n      grep -q \"me/orders\" \"$ROUTES_FILE\" \\\n        || (echo \"FAIL: /me/orders route missing from storefront-portal-data.js\" >&2; exit 1)\n      grep -q \"me/statements\" \"$ROUTES_FILE\" \\\n        || (echo \"FAIL: /me/statements route missing from storefront-portal-data.js\" >&2; exit 1)\n\n      # Count /me/orders occurrences — expect >= 2 (list + detail)\n      ME_ORDERS_COUNT=$(grep -c \"me/orders\" \"$ROUTES_FILE\" || echo 0)\n      echo \"me/orders occurrences: $ME_ORDERS_COUNT\"\n      if [ \"$ME_ORDERS_COUNT\" -lt 2 ]; then\n        echo \"FAIL: Expected >= 2 /me/orders occurrences (list + detail), found $ME_ORDERS_COUNT\" >&2\n        exit 1\n      fi\n\n      # Count /me/statements occurrences — expect >= 2 (list + detail)\n      ME_STATEMENTS_COUNT=$(grep -c \"me/statements\" \"$ROUTES_FILE\" || echo 0)\n      echo \"me/statements occurrences: $ME_STATEMENTS_COUNT\"\n      if [ \"$ME_STATEMENTS_COUNT\" -lt 2 ]; then\n        echo \"FAIL: Expected >= 2 /me/statements occurrences, found $ME_STATEMENTS_COUNT\" >&2\n        exit 1\n      fi\n\n      # Status mapping helpers present — separate greps\n      grep -q \"mapOrderStatus\" \"$ROUTES_FILE\" \\\n        || (echo \"FAIL: mapOrderStatus missing from routes file\" >&2; exit 1)\n      grep -q \"mapStatementStatus\" \"$ROUTES_FILE\" \\\n        || (echo \"FAIL: mapStatementStatus missing from routes file\" >&2; exit 1)\n\n      # SOLD->PENDING mapping present\n      grep -q \"SOLD\" \"$ROUTES_FILE\" \\\n        || (echo \"FAIL: SOLD->PENDING mapping reference missing\" >&2; exit 1)\n\n      # VOID filter present\n      grep -q \"VOID\" \"$ROUTES_FILE\" \\\n        || (echo \"FAIL: VOID filter reference missing from routes file\" >&2; exit 1)\n\n      # Legacy routes preserved — separate greps\n      grep -q \"purchases\" \"$ROUTES_FILE\" \\\n        || (echo \"FAIL: GET /purchases was removed — must be preserved\" >&2; exit 1)\n\n      # Test file exists with sufficient tests\n      TEST_FILE=\"shopops-api/tests/test_storefront_portal_me_routes.js\"\n      ls \"$TEST_FILE\" || (echo \"FAIL: test file missing at $TEST_FILE\" >&2; exit 1)\n      TEST_COUNT=$(grep -c \"it(\" \"$TEST_FILE\" || echo 0)\n      echo \"Test count (it() calls): $TEST_COUNT\"\n      if [ \"$TEST_COUNT\" -lt 5 ]; then\n        echo \"FAIL: Expected >= 5 test cases in $TEST_FILE, found $TEST_COUNT\" >&2\n        exit 1\n      fi\n\n      echo \"=== all shopops-api grep assertions passed ===\"\n\n  # ---------------------------------------------------------------------------\n  # 7. VERIFY-GREP-STOREFRONT — structural assertions on the storefront side\n  # ---------------------------------------------------------------------------\n  - id: verify-grep-storefront\n    depends_on: [implement-storefront]\n    bash: |\n      set -euo pipefail\n      STOREFRONT_DIR=\"/tmp/storefront-work-$WORKFLOW_ID\"\n      STOREFRONT_API=\"$STOREFRONT_DIR/lib/storefront-api.ts\"\n\n      echo \"=== shopops-storefront grep assertions ===\"\n\n      ls \"$STOREFRONT_API\" \\\n        || (echo \"FAIL: lib/storefront-api.ts missing in storefront clone\" >&2; exit 1)\n\n      # Cookie forwarding present\n      grep -q \"portal_jwt\" \"$STOREFRONT_API\" \\\n        || (echo \"FAIL: portal_jwt not referenced in storefront-api.ts\" >&2; exit 1)\n\n      # next/headers cookies() import present\n      grep -q \"next/headers\" \"$STOREFRONT_API\" \\\n        || (echo \"FAIL: next/headers import missing\" >&2; exit 1)\n\n      # Correct URL paths still present\n      grep -q \"portal/me/orders\" \"$STOREFRONT_API\" \\\n        || (echo \"FAIL: /portal/me/orders URL missing from storefront-api.ts\" >&2; exit 1)\n      grep -q \"portal/me/statements\" \"$STOREFRONT_API\" \\\n        || (echo \"FAIL: /portal/me/statements URL missing from storefront-api.ts\" >&2; exit 1)\n\n      # Seed path guard still present\n      grep -q \"STOREFRONT_DATA_SOURCE\" \"$STOREFRONT_API\" \\\n        || (echo \"FAIL: seed path guard missing from storefront-api.ts\" >&2; exit 1)\n\n      # Test file exists — check common test paths\n      TEST_FOUND=0\n      for TEST_PATH in \\\n        \"$STOREFRONT_DIR/__tests__/lib/storefront-api.test.ts\" \\\n        \"$STOREFRONT_DIR/tests/lib/storefront-api.test.ts\" \\\n        \"$STOREFRONT_DIR/__tests__/storefront-api.test.ts\"; do\n        if [ -f \"$TEST_PATH\" ]; then\n          TEST_COUNT=$(grep -c \"it(\\|test(\" \"$TEST_PATH\" || echo 0)\n          echo \"Test file: $TEST_PATH — $TEST_COUNT tests\"\n          if [ \"$TEST_COUNT\" -ge 4 ]; then\n            TEST_FOUND=1\n            break\n          fi\n        fi\n      done\n      if [ \"$TEST_FOUND\" -eq 0 ]; then\n        echo \"FAIL: No storefront-api test file found with >= 4 tests\" >&2\n        exit 1\n      fi\n\n      echo \"=== all storefront grep assertions passed ===\"\n\n  # ---------------------------------------------------------------------------\n  # 8. VERIFY-TESTS-API — run bun test suite for shopops-api side\n  # ---------------------------------------------------------------------------\n  - id: verify-tests-api\n    depends_on: [implement-api]\n    bash: |\n      set -euo pipefail\n      TEST_FILE=\"shopops-api/tests/test_storefront_portal_me_routes.js\"\n      if [ -f \"$TEST_FILE\" ]; then\n        cd shopops-api\n        bun test \"tests/test_storefront_portal_me_routes.js\" \\\n          || echo \"Tests may fail without live Supabase in Archon CI; CI against staging is authoritative.\"\n      else\n        echo \"FAIL: Test file not found at $TEST_FILE\" >&2\n        exit 1\n      fi\n\n  # ---------------------------------------------------------------------------\n  # 9. VERIFY-TESTS-STOREFRONT — run bun test suite for storefront side\n  # ---------------------------------------------------------------------------\n  - id: verify-tests-storefront\n    depends_on: [implement-storefront]\n    bash: |\n      set -euo pipefail\n      STOREFRONT_DIR=\"/tmp/storefront-work-$WORKFLOW_ID\"\n      cd \"$STOREFRONT_DIR\"\n\n      # Find test file regardless of path convention\n      TEST_FILE_FOUND=$(find __tests__ tests -name 'storefront-api.test.ts' 2>/dev/null | head -1 || true)\n      if [ -n \"$TEST_FILE_FOUND\" ]; then\n        bun test \"$TEST_FILE_FOUND\" \\\n          || echo \"Tests may fail in Archon CI without Next.js context; Vercel CI is authoritative.\"\n      else\n        echo \"WARN: storefront-api test file not found; verify implement-storefront completed\" >&2\n        exit 1\n      fi\n\n  # ---------------------------------------------------------------------------\n  # 10. COMMIT-AND-PUSH-API — idempotent checkout + commit + push for shopops\n  # ---------------------------------------------------------------------------\n  - id: commit-and-push-api\n    depends_on: [verify-grep-api, verify-tests-api]\n    bash: |\n      set -euo pipefail\n      BASE_BRANCH=\"wo/storefront-portal-auth-wire-01\"\n      # Default to unique run-suffixed branch (avoids worktree collisions)\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      git checkout -b \"$BRANCH\" 2>/dev/null || git checkout \"$BRANCH\"\n\n      git add \\\n        shopops-api/routes/storefront-portal-data.js \\\n        shopops-api/tests/test_storefront_portal_me_routes.js\n\n      # Also stage lib/portalStatusMap.js if the builder extracted helpers there\n      if [ -f shopops-api/lib/portalStatusMap.js ]; then\n        git add shopops-api/lib/portalStatusMap.js\n      fi\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-STOREFRONT-PORTAL-AUTH-WIRE-01 — /me/orders + /me/statements portal routes\\n\\nAdds four /me/* routes to routes/storefront-portal-data.js behind existing\\nrequirePortalAuth middleware (req.customerId + req.tenantId already set):\\n  GET /me/orders          — enriched orders list (joins shipping_queue for DELIVERED derivation)\\n  GET /me/orders/:id      — single order with line_items, 404 on wrong customer/tenant\\n  GET /me/statements      — statements list (VOID filtered, status vocab mapped)\\n  GET /me/statements/:id  — statement with line_items array from billing_statement_lines\\n\\nStatus vocabulary mapping (backend vocab never leaks to portal):\\n  SOLD -> PENDING, PAID -> PROCESSING\\n  SHIPPED + tracking_number IS NOT NULL -> DELIVERED, SHIPPED (no tracking) -> SHIPPED\\n  DRAFT/APPROVED/GENERATED -> PENDING, SENT/PARTIAL -> OUTSTANDING, PAID -> SETTLED\\n  VOID rows filtered at query level\\n\\nDELIVERED is derived (shipping_queue.status='\\''SHIPPED'\\'' AND tracking_number IS NOT NULL).\\nNo new column added to orders table.\\n\\nExisting GET /purchases and GET /statements preserved unchanged.\\nAdds tests/test_storefront_portal_me_routes.js — 5+ bun tests.\\n\\nBlocks: WO-STOREFRONT-COMMERCE-SPINE-01\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  # ---------------------------------------------------------------------------\n  # 11. COMMIT-AND-PUSH-STOREFRONT — commit + push inside /tmp/storefront-work\n  # ---------------------------------------------------------------------------\n  - id: commit-and-push-storefront\n    depends_on: [verify-grep-storefront, verify-tests-storefront]\n    bash: |\n      set -euo pipefail\n      STOREFRONT_DIR=\"/tmp/storefront-work-$WORKFLOW_ID\"\n      cd \"$STOREFRONT_DIR\"\n\n      BASE_BRANCH=\"wo/storefront-portal-auth-wire-01\"\n      # Default to unique run-suffixed branch (avoids worktree collisions)\n      BRANCH=\"${BASE_BRANCH}-run-$(date +%Y%m%d%H%M%S)\"\n      git checkout -b \"$BRANCH\" 2>/dev/null || git checkout \"$BRANCH\"\n\n      # Stage storefront-api.ts\n      git add lib/storefront-api.ts\n\n      # Stage test file regardless of which test dir was used\n      for TEST_PATH in \\\n        \"__tests__/lib/storefront-api.test.ts\" \\\n        \"tests/lib/storefront-api.test.ts\" \\\n        \"__tests__/storefront-api.test.ts\"; do\n        if [ -f \"$TEST_PATH\" ]; then\n          git add \"$TEST_PATH\"\n          break\n        fi\n      done\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains requested work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(storefront): WO-STOREFRONT-PORTAL-AUTH-WIRE-01 — portal_jwt cookie forwarding\\n\\nWires portal_jwt cookie forwarding in lib/storefront-api.ts so Next.js server\\ncomponents forward the httpOnly portal_jwt cookie when calling shopops-api.\\n\\nPattern: cookies() from next/headers; conditional Cookie header injection.\\nSeed path guard preserved — STOREFRONT_DATA_SOURCE=seed skips API calls entirely.\\nURL paths unchanged (already correct: /storefront/portal/me/orders etc).\\n\\nAdds __tests__/lib/storefront-api.test.ts — 4+ tests covering:\\n  cookie forwarding when portal_jwt present, seed bypass, header injection.\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n\n      git push -u origin \"$BRANCH\"\n      echo \"STOREFRONT_BRANCH=${BRANCH}\"\n      echo \"STOREFRONT_BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  # ---------------------------------------------------------------------------\n  # 12. OPEN-PR-API — idempotent PR for shopops-api side (base: master)\n  # ---------------------------------------------------------------------------\n  - id: open-pr-api\n    depends_on: [commit-and-push-api]\n    bash: |\n      set -euo pipefail\n      # Read branch from current HEAD (commit-and-push-api set the unique run-suffixed branch in shopops CWD)\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BASE=\"master\"\n      TITLE=\"feat(api): WO-STOREFRONT-PORTAL-AUTH-WIRE-01 — /me/orders + /me/statements portal routes\"\n\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-STOREFRONT-PORTAL-AUTH-WIRE-01** (shopops-api side).\"\n        echo \"\"\n        echo \"Adds /me/* portal data routes to \\`routes/storefront-portal-data.js\\`\"\n        echo \"so the storefront server components can fetch real customer data.\"\n        echo \"\"\n        echo \"## New Routes\"\n        echo \"- \\`GET /me/orders\\` — enriched list with DELIVERED derivation\"\n        echo \"- \\`GET /me/orders/:id\\` — single order with line_items\"\n        echo \"- \\`GET /me/statements\\` — VOID filtered, status vocab mapped\"\n        echo \"- \\`GET /me/statements/:id\\` — with billing_statement_lines\"\n        echo \"\"\n        echo \"## Status Vocab Mapping\"\n        echo \"- SOLD -> PENDING, PAID -> PROCESSING\"\n        echo \"- SHIPPED + tracking_number IS NOT NULL -> DELIVERED\"\n        echo \"- DRAFT/APPROVED/GENERATED -> PENDING, SENT/PARTIAL -> OUTSTANDING, PAID -> SETTLED\"\n        echo \"- VOID filtered at query level (never returned)\"\n        echo \"\"\n        echo \"## Preserved\"\n        echo \"- \\`GET /purchases\\` — unchanged\"\n        echo \"- \\`GET /statements\\` — unchanged\"\n        echo \"- \\`GET /statements/:id\\` — unchanged\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- Modified: \\`shopops-api/routes/storefront-portal-data.js\\`\"\n        echo \"- Created: \\`shopops-api/tests/test_storefront_portal_me_routes.js\\`\"\n        echo \"\"\n        echo \"## Tests\"\n        echo \"- \\`bun test tests/test_storefront_portal_me_routes.js\\`\"\n        echo \"- 5+ tests: status mapping, DELIVERED derivation, VOID filter, 404 handling\"\n        echo \"\"\n        echo \"## Rule 19 (Runtime Verification)\"\n        echo \"After container rebuild:\"\n        echo \"  docker exec shopops-api grep -n 'me/orders' /app/routes/storefront-portal-data.js\"\n        echo \"\"\n        echo \"## Companion PR\"\n        echo \"shopops-storefront: wo/storefront-portal-auth-wire-01 (cookie forwarding)\"\n        echo \"\"\n        echo \"Generated by Archon bdc-storefront-portal-auth-wire workflow.\"\n      } > \"$BODY_FILE\"\n\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url \\\n          --template '{{range .}}{{.url}}{{end}}' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"API_PR_URL=$PR_URL\"\n        exit 0\n      fi\n\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"API_PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"API_PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr-api attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr-api failed after retries; branch pushed but no PR\" >&2\n      exit 1\n\n  # ---------------------------------------------------------------------------\n  # 13. OPEN-PR-STOREFRONT — idempotent PR for shopops-storefront (base: main)\n  # ---------------------------------------------------------------------------\n  - id: open-pr-storefront\n    depends_on: [commit-and-push-storefront]\n    bash: |\n      set -euo pipefail\n      # The storefront branch lives in the /tmp clone — read from there.\n      STOREFRONT_DIR=\"/tmp/storefront-work-$WORKFLOW_ID\"\n      cd \"$STOREFRONT_DIR\"\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops-storefront\"\n      BASE=\"main\"\n      TITLE=\"feat(storefront): WO-STOREFRONT-PORTAL-AUTH-WIRE-01 — portal_jwt cookie forwarding\"\n\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-STOREFRONT-PORTAL-AUTH-WIRE-01** (storefront side).\"\n        echo \"\"\n        echo \"Wires \\`portal_jwt\\` cookie forwarding in \\`lib/storefront-api.ts\\`\"\n        echo \"so Next.js server components pass the auth cookie to shopops-api.\"\n        echo \"\"\n        echo \"## Change\"\n        echo \"- \\`lib/storefront-api.ts\\`: adds \\`cookies()\\` from \\`next/headers\\`\"\n        echo \"- \\`fetchOrders()\\`, \\`fetchOrder()\\`, \\`fetchStatements()\\`, \\`fetchStatement()\\`\"\n        echo \"  inject \\`Cookie: portal_jwt=<value>\\` into fetch() when token present\"\n        echo \"- Seed path guard preserved: \\`STOREFRONT_DATA_SOURCE=seed\\` skips API calls\"\n        echo \"- URL paths unchanged (already correct)\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- Modified: \\`lib/storefront-api.ts\\`\"\n        echo \"- Created: \\`__tests__/lib/storefront-api.test.ts\\`\"\n        echo \"\"\n        echo \"## Tests\"\n        echo \"- 4+ tests: cookie injection, seed bypass, header presence\"\n        echo \"\"\n        echo \"## Companion PR\"\n        echo \"shopops: wo/storefront-portal-auth-wire-01 (/me/* routes)\"\n        echo \"\"\n        echo \"Generated by Archon bdc-storefront-portal-auth-wire workflow.\"\n      } > \"$BODY_FILE\"\n\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url \\\n          --template '{{range .}}{{.url}}{{end}}' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"STOREFRONT_PR_URL=$PR_URL\"\n        exit 0\n      fi\n\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"STOREFRONT_PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"STOREFRONT_PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr-storefront attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr-storefront failed after retries; branch pushed but no PR\" >&2\n      exit 1\n\n  # ---------------------------------------------------------------------------\n  # 14. RUNTIME-VERIFY — Rule 19: docker exec grep proves source live in\n  #     shopops-api container. Best-effort — container not rebuilt at PR-open time.\n  # ---------------------------------------------------------------------------\n  - id: runtime-verify\n    depends_on: [open-pr-api, open-pr-storefront]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort step (Rule 19)\n      RESULT=$(docker exec shopops-api grep -n \"me/orders\" /app/routes/storefront-portal-data.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RULE19_GREP_RESULT=${RESULT}\"\n        echo \"DETAIL=Source change verified live in shopops-api container\"\n      elif echo \"$RESULT\" | grep -qiE 'No such container|not found|executable file not found'; then\n        echo \"STATUS=skipped_container_not_rebuilt\"\n        echo \"DETAIL=shopops-api not yet rebuilt with PR changes. Re-run post-deploy:\"\n        echo \"  docker exec shopops-api grep -n 'me/orders' /app/routes/storefront-portal-data.js\"\n        echo \"  docker exec shopops-api grep -n 'me/statements' /app/routes/storefront-portal-data.js\"\n        echo \"  docker exec shopops-api grep -n 'mapOrderStatus' /app/routes/storefront-portal-data.js\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${RESULT}\"\n      fi\n\n      echo \"\"\n      echo \"=== RULE 19 VERIFICATION COMMANDS (run post-deploy) ===\"\n      echo \"docker exec shopops-api grep -n 'me/orders' /app/routes/storefront-portal-data.js\"\n      echo \"docker exec shopops-api grep -n 'me/statements' /app/routes/storefront-portal-data.js\"\n      echo \"docker exec shopops-api grep -n 'mapOrderStatus' /app/routes/storefront-portal-data.js\"\n      echo \"docker exec shopops-api grep -n 'mapStatementStatus' /app/routes/storefront-portal-data.js\"\n      echo \"curl -s -H 'Cookie: portal_jwt=<test_token>' http://localhost:3402/storefront/portal/me/orders\"\n\n      exit 0\n\n  # ---------------------------------------------------------------------------\n  # 15. BUILD-MANIFEST — MIXED class manifest (CODE in 2 repos + Rule 19 infra)\n  # ---------------------------------------------------------------------------\n  - id: build-manifest\n    depends_on: [runtime-verify]\n    task_class: clerical\n    prompt: |\n      Build the Captain CI manifest v2 for WO-STOREFRONT-PORTAL-AUTH-WIRE-01.\n      Class: MIXED — requires CODE evidence (grep assertions, both repos) AND\n      INFRA evidence (Rule 19 runtime verification for shopops-api).\n\n      ## Required Fields (manifest v2 format)\n\n      WO: WO-STOREFRONT-PORTAL-AUTH-WIRE-01\n      Builder: Major Build (Archon bdc-storefront-portal-auth-wire)\n\n      Files modified:\n        shopops-api/routes/storefront-portal-data.js\n        shopops-storefront/lib/storefront-api.ts\n\n      Files created:\n        shopops-api/tests/test_storefront_portal_me_routes.js\n        [storefront test file path from verify-grep-storefront.output]\n        [shopops-api/lib/portalStatusMap.js if created by builder]\n\n      Tests:\n        shopops-api: [count] passing / [total] total\n          command: bun test shopops-api/tests/test_storefront_portal_me_routes.js\n        shopops-storefront: [count] passing / [total] total\n          command: bun test [test file from verify-grep-storefront.output]\n\n      PRs:\n        shopops (master): [from open-pr-api.output API_PR_URL]\n        shopops-storefront (main): [from open-pr-storefront.output STOREFRONT_PR_URL]\n\n      Merge ancestors:\n        shopops master HEAD=[from commit-and-push-api.output BRANCH_SHA], manifest commit, behind_by: 0\n        shopops-storefront main HEAD=[from commit-and-push-storefront.output STOREFRONT_BRANCH_SHA], manifest commit, behind_by: 0\n\n      Grep assertions:\n        shopops-api: grep -n 'me/orders' shopops-api/routes/storefront-portal-data.js (expected >= 2 matches)\n        shopops-api: grep -n 'me/statements' shopops-api/routes/storefront-portal-data.js (expected >= 2 matches)\n        shopops-api: grep -n 'mapOrderStatus' shopops-api/routes/storefront-portal-data.js (expected >= 1 match)\n        shopops-api: grep -n 'mapStatementStatus' shopops-api/routes/storefront-portal-data.js (expected >= 1 match)\n        shopops-storefront: grep -n 'portal_jwt' lib/storefront-api.ts (expected >= 2 matches)\n        shopops-storefront: grep -n 'next/headers' lib/storefront-api.ts (expected >= 1 match)\n        [include actual counts from verify-grep-api.output and verify-grep-storefront.output]\n\n      Runtime verification:\n        Rule 19 (shopops-api): docker exec shopops-api grep -n 'me/orders' /app/routes/storefront-portal-data.js\n        [STATUS from runtime-verify.output — if PENDING REBUILD, note that]\n\n      Vercel deployment:\n        shopops-storefront deploys to Vercel on PR merge.\n        Post-merge verify: curl https://[preview-url]/shops/bdc/portal/orders (expected HTTP 200)\n        [If preview URL available from open-pr-storefront.output, include it]\n\n      VALIDATION: PASS (shopops-api runtime-verify PENDING container rebuild post-merge;\n                        all code assertions green per verify-grep-api and verify-grep-storefront)\n\n      ## Inputs\n      $read-spec.output\n      $schema-verify.output\n      $plan.output\n      $verify-grep-api.output\n      $verify-grep-storefront.output\n      $verify-tests-api.output\n      $verify-tests-storefront.output\n      $commit-and-push-api.output\n      $commit-and-push-storefront.output\n      $open-pr-api.output\n      $open-pr-storefront.output\n      $runtime-verify.output\n\n  # ---------------------------------------------------------------------------\n  # 16. FLIP-NOTION — set Notes + Claude Status = REVIEW (Rule 7)\n  #     Notes PROPERTY, not a comment. Captain CI reads Notes.\n  # ---------------------------------------------------------------------------\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-STOREFRONT-PORTAL-AUTH-WIRE-01 to REVIEW with the manifest\n      in the Notes PROPERTY (not as a page comment — Captain CI reads Notes, not comments).\n\n      # OPEN QUESTION (Q3): Notion page ID is NOT confirmed in this draft.\n      # Operator MUST replace 35fef4c9-ce00-811d-bf39-c4dc02a5bdc9 with the real UUID before firing.\n      # Find it: query Notion database a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # for title containing \"WO-STOREFRONT-PORTAL-AUTH-WIRE-01\".\n      Notion database ID: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      Notion page ID: 35fef4c9-ce00-811d-bf39-c4dc02a5bdc9\n\n      Required Notion changes (single update call):\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> set to the manifest text from $build-manifest.output verbatim\n      - claimed_by: Major Build (Archon bdc-storefront-portal-auth-wire)\n      - claimed_at: 2026-05-13\n\n      After update, log the Notion page URL to run output for audit trail.\n\n      If Notion MCP is unavailable in this runtime, output:\n        XO_TODO: Flip WO-STOREFRONT-PORTAL-AUTH-WIRE-01 to REVIEW in Notion.\n                 Paste manifest from $build-manifest.output into Notes property.\n                 Set Claude Status = REVIEW, Status = Review.\n\n      ## Manifest\n      $build-manifest.output\n",
  "bdc-storefront-portal-sidebar": "# Drafted by yaml-author subagent for WO-STOREFRONT-PORTAL-SIDEBAR-01 on 2026-05-13\n# Source pattern: WO-SHOPOPS-CHANNEL-ADMIN-UI-01.yaml (single-repo UI WO) +\n#                 WO-STOREFRONT-PORTAL-AUTH-WIRE-01.yaml (storefront /tmp clone pattern)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring\n#\n# Pre-flight (OPERATOR MUST VERIFY BEFORE FIRING):\n#   1. bluedevilcollectibles/shopops-storefront registered in Archon\n#      OR Archon CWD is a shopops-storefront-capable repo (verify via remote_agent_codebases query).\n#      If NOT registered as primary, this YAML uses the /tmp-clone pattern from the auth-wire reference.\n#   2. WO spec at bdc-xo:docs/work-orders/WO-STOREFRONT-PORTAL-SIDEBAR-01.md\n#   3. Notion page ID — fill in 35fef4c9-ce00-816a-8661-caebf938788c before firing\n#   4. GH_TOKEN in Archon env with push to shopops-storefront\n#\n# OPEN QUESTIONS:\n#   Q1: Real existing portal routes — builder enumerates via grep before linking; if any missing,\n#       documents in manifest as \"link present, target deferred.\"\n#   Q2: Sign-out endpoint — builder greps for existing logout pattern, does NOT invent.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh bdc-storefront-portal-sidebar.yaml\n# REVIEW BEFORE DEPLOY: confirm Archon CWD strategy (native vs /tmp-clone), Notion page ID.\n\nname: bdc-storefront-portal-sidebar\ndescription: |\n  Use when: Executing WO-STOREFRONT-PORTAL-SIDEBAR-01 — Portal Sidebar nav.\n  Does: Clones shopops-storefront to /tmp. Reads existing portal page layout +\n        enumerates real portal routes. Creates PortalSidebar + PortalTopNav +\n        PortalNavLink components. Wires into layout.tsx. Removes \"Pick a section\n        from the sidebar\" placeholder. 5 RTL tests. UI screenshot loop N=3.\n        PR to main. Vercel preview verify. Manifest v2 + Notion flip.\n  Repo: bluedevilcollectibles/shopops-storefront (cloned to /tmp inside the workflow run).\n  parallel_ok: true (single repo, pure UI, no schema or backend changes)\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-STOREFRONT-PORTAL-SIDEBAR-01}\"\n      # Primary path (gh --template, no jq dependency)\n      SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        GLOB=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB\" ]; then\n          SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n--- SPEC ---\\n%s\\n' \"$WO_ID\" \"$WORKFLOW_ID\" \"$SPEC\"\n\n  - id: setup-storefront-clone\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      DIR=\"/tmp/storefront-work-$WORKFLOW_ID\"\n      if [ -d \"$DIR/.git\" ]; then\n        cd \"$DIR\"; git fetch origin || true\n        # Tolerate worktree-locked default branch\n        if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n          echo \"default branch held by other worktree; staying on current HEAD\"\n        fi\n        git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n        echo \"STATUS=reused\"\n      else\n        git clone \"https://github.com/bluedevilcollectibles/shopops-storefront.git\" \"$DIR\" --depth=50\n        echo \"STATUS=fresh\"\n      fi\n      echo \"STOREFRONT_DIR=$DIR\"\n\n  - id: audit-prior-art\n    depends_on: [setup-storefront-clone]\n    bash: |\n      # set -e DELIBERATELY OMITTED — audit must NEVER fail (BUG-3 tolerant)\n      DIR=\"/tmp/storefront-work-$WORKFLOW_ID\"\n      cd \"$DIR\" || { echo \"WARN: storefront clone dir missing\"; exit 0; }\n\n      echo \"=== Portal directory contents (storefront uses Next.js app/, no src/) ===\"\n      if [ -d \"app/shops/[shopSlug]/portal\" ]; then\n        find \"app/shops/[shopSlug]/portal\" -type f 2>/dev/null | head -40 || true\n      elif [ -d app ]; then\n        find \"app\" -path \"*portal*\" -type f 2>/dev/null | head -40 || true\n      else\n        echo \"(app/ not found — non-Next.js layout?)\"\n      fi\n\n      echo \"=== Existing portal page (placeholder) ===\"\n      if [ -e \"app/shops/[shopSlug]/portal/page.tsx\" ]; then\n        head -80 \"app/shops/[shopSlug]/portal/page.tsx\" 2>/dev/null || true\n      fi\n\n      echo \"=== Existing portal layout (if any) ===\"\n      if [ -e \"app/shops/[shopSlug]/portal/layout.tsx\" ]; then\n        head -60 \"app/shops/[shopSlug]/portal/layout.tsx\" 2>/dev/null || true\n      else\n        echo \"(no layout.tsx — WILL CREATE)\"\n      fi\n\n      echo \"=== Font/theme tokens ===\"\n      if [ -d app ]; then\n        grep -rn \"Cormorant\" app/ 2>/dev/null | head -10 || true\n      fi\n      [ -d styles ] && grep -rn \"Cormorant\" styles/ 2>/dev/null | head -10 || true\n      [ -e globals.css ] && grep -n \"Cormorant\" globals.css 2>/dev/null | head -10 || true\n      if [ -d \"app/shops/[shopSlug]/portal\" ]; then\n        grep -rn \"dark\" \"app/shops/[shopSlug]/portal/\" 2>/dev/null | head -10 || true\n      fi\n\n      echo \"=== Existing logout pattern ===\"\n      [ -d app ] && grep -rn \"logout\\|signOut\\|sign-out\" app/ 2>/dev/null | head -15 || true\n      [ -d lib ] && grep -rn \"logout\\|signOut\\|sign-out\" lib/ 2>/dev/null | head -15 || true\n\n      echo \"=== Real portal sub-routes (for sidebar link verification) ===\"\n      if [ -d \"app/shops/[shopSlug]/portal\" ]; then\n        find \"app/shops/[shopSlug]/portal\" -name \"page.tsx\" 2>/dev/null | head -20 || true\n      fi\n\n      echo \"=== Existing test setup ===\"\n      find __tests__ tests -type d 2>/dev/null | head -10 || true\n      [ -e package.json ] && grep -E '\"test|\"vitest|\"jest' package.json 2>/dev/null | head -5 || true\n      exit 0\n\n  - id: plan\n    depends_on: [audit-prior-art]\n    task_class: medium_work\n    prompt: |\n      Plan WO-STOREFRONT-PORTAL-SIDEBAR-01 implementation.\n\n      ## Spec\n      $read-spec.output\n\n      ## Prior art audit\n      $audit-prior-art.output\n\n      ## Required output\n\n      ### Files to create\n      Under /tmp/storefront-work-$WORKFLOW_ID:\n        - app/shops/[shopSlug]/portal/components/PortalSidebar.tsx (client component)\n        - app/shops/[shopSlug]/portal/components/PortalNavLink.tsx\n        - app/shops/[shopSlug]/portal/components/PortalTopNav.tsx (mobile)\n        - __tests__/portal/PortalSidebar.test.tsx (5 tests per WO §7)\n\n      ### Files to modify\n        - app/shops/[shopSlug]/portal/layout.tsx (create if absent; otherwise add <PortalSidebar />)\n        - app/shops/[shopSlug]/portal/page.tsx (remove \"Pick a section from the sidebar\" placeholder)\n\n      ### Nav items (in order)\n        1. Pull list -> /shops/[shopSlug]/portal/pull-list\n        2. Orders -> /shops/[shopSlug]/portal/orders\n        3. Statements -> /shops/[shopSlug]/portal/statements\n        4. Payment settings -> /shops/[shopSlug]/portal/payment\n        5. Browse -> /shops/[shopSlug]/catalog\n        6. Sign out -> POST to whichever logout pattern the audit found (NO INVENTING)\n\n      ### Style rules\n      - Font: existing Cormorant Garamond (use whatever import is already active)\n      - No new design tokens; reuse existing portal page bg + text colors\n      - Active link: subtle left border + lighter text (no new colors)\n      - Mobile <768px: PortalTopNav with hamburger -> drawer\n\n      ### Link verification\n      - For each of pull-list/orders/statements/payment routes, confirm the target page.tsx\n        exists under app/shops/[shopSlug]/portal/. If any missing, document in manifest as\n        \"link present, target deferred to follow-up WO\".\n\n      ### Test plan\n      5 React Testing Library tests in __tests__/portal/PortalSidebar.test.tsx:\n        1. Sidebar renders all 6 items with correct hrefs (shopSlug='mutant-market')\n        2. Active link gets active className when usePathname matches (mock next/navigation)\n        3. Sign Out button calls the existing logout endpoint (assert fetch call args)\n        4. Mobile breakpoint: PortalTopNav renders when matchMedia <768px\n        5. shopSlug param correctly interpolated into each link href\n\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement WO-STOREFRONT-PORTAL-SIDEBAR-01.\n\n        The storefront repo is at /tmp/storefront-work-$WORKFLOW_ID.\n        All paths absolute under that directory.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n        1. Read /tmp/storefront-work-$WORKFLOW_ID/app/shops/[shopSlug]/portal/page.tsx in full.\n           Identify the \"Pick a section from the sidebar\" placeholder line. Remove it but\n           keep the page functional (e.g., replace with a brief welcome heading).\n        2. If layout.tsx already exists under the portal dir, MODIFY it. If not, CREATE\n           layout.tsx as a server component that renders <PortalSidebar /> + {children}.\n        3. Create PortalSidebar.tsx as a CLIENT COMPONENT (needs usePathname from next/navigation).\n           - Take params.shopSlug as a prop (passed from layout)\n           - Render 6 nav items with hrefs interpolated using shopSlug\n           - Sign out item POSTs to the existing logout endpoint found in audit\n             (do NOT invent — if no endpoint exists, use the existing client-side\n             logout helper if there is one; otherwise fall back to a redirect to /)\n        4. Create PortalNavLink.tsx — renders <Link href> + applies active className\n           when usePathname() === href.\n        5. Create PortalTopNav.tsx — mobile variant with hamburger button + drawer.\n           Use matchMedia or a CSS-only @media query (prefer CSS-only; less JS).\n        6. Match the existing portal style. Read existing pages to confirm\n           Cormorant Garamond is active. Do NOT add a new font import.\n        7. Create __tests__/portal/PortalSidebar.test.tsx with 5 tests per the plan.\n           Use whatever test runner the project already uses (check package.json scripts).\n           Mock next/navigation usePathname.\n        8. Verify all 5 link targets (pull-list, orders, statements, payment, catalog)\n           exist as page.tsx files. For each missing target, document in code as a\n           // TODO: target page deferred — link will 404 until WO-XXX ships\n           comment AND record in the run output for the manifest builder.\n        9. Do NOT touch backend routes, auth middleware, or any non-portal page.\n        10. Do NOT add new package dependencies.\n\n        ## Completion criteria\n        Output <promise>COMPLETE</promise> only when:\n          - 4 new files exist in app/shops/[shopSlug]/portal/components/\n          - layout.tsx wires <PortalSidebar />\n          - page.tsx no longer contains \"Pick a section from the sidebar\"\n          - Test file has >=5 test() blocks\n          - Each link target verified or documented as deferred\n\n  - id: verify-types\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd /tmp/storefront-work-$WORKFLOW_ID\n      if [ -f tsconfig.json ]; then\n        bunx tsc --noEmit\n        echo \"TS_OK\"\n      else\n        echo \"No tsconfig — skip\"\n      fi\n\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd /tmp/storefront-work-$WORKFLOW_ID\n      TEST_FILE=\"__tests__/portal/PortalSidebar.test.tsx\"\n      if [ -f \"$TEST_FILE\" ]; then\n        bun test \"$TEST_FILE\" 2>&1 | tee /tmp/tests.txt \\\n          || echo \"Tests may need Next.js context; Vercel CI authoritative.\"\n        echo \"TAIL=$(tail -5 /tmp/tests.txt)\"\n      else\n        echo \"FAIL: test file missing\" >&2\n        exit 1\n      fi\n\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd /tmp/storefront-work-$WORKFLOW_ID\n\n      LAYOUT=\"app/shops/[shopSlug]/portal/layout.tsx\"\n      PAGE=\"app/shops/[shopSlug]/portal/page.tsx\"\n      SIDEBAR=\"app/shops/[shopSlug]/portal/components/PortalSidebar.tsx\"\n      TEST=\"__tests__/portal/PortalSidebar.test.tsx\"\n\n      ls \"$LAYOUT\" || (echo \"FAIL: layout.tsx missing\" >&2; exit 1)\n      ls \"$SIDEBAR\" || (echo \"FAIL: PortalSidebar.tsx missing\" >&2; exit 1)\n      ls \"$TEST\" || (echo \"FAIL: test file missing\" >&2; exit 1)\n\n      grep -q \"PortalSidebar\" \"$LAYOUT\" \\\n        || (echo \"FAIL: PortalSidebar not used in layout.tsx\" >&2; exit 1)\n\n      # Placeholder must be REMOVED from page.tsx\n      if grep -q \"Pick a section from the sidebar\" \"$PAGE\"; then\n        echo \"FAIL: placeholder text still in page.tsx\" >&2\n        exit 1\n      fi\n\n      TESTS=$(grep -c \"test(\\|it(\" \"$TEST\")\n      [ \"$TESTS\" -ge 5 ] || (echo \"FAIL: need >=5 tests, got $TESTS\" >&2; exit 1)\n\n      # Verify 6 nav items reference\n      for href in \"pull-list\" \"orders\" \"statements\" \"payment\" \"catalog\"; do\n        grep -q \"$href\" \"$SIDEBAR\" || echo \"WARN: nav item '$href' not found in sidebar\"\n      done\n\n      # Mobile variant present\n      ls \"app/shops/[shopSlug]/portal/components/PortalTopNav.tsx\" \\\n        || echo \"WARN: PortalTopNav.tsx missing — mobile may not render\"\n\n      echo \"GREP PASS, tests=$TESTS\"\n\n  - id: build-check\n    depends_on: [verify-types, verify-tests, verify-grep]\n    bash: |\n      set -euo pipefail\n      cd /tmp/storefront-work-$WORKFLOW_ID\n      bun run build 2>&1 | tee /tmp/build.txt\n      EXIT=${PIPESTATUS[0]}\n      [ $EXIT -eq 0 ] || (echo \"BUILD FAILED\" >&2; cat /tmp/build.txt >&2; exit 1)\n      echo \"BUILD_OK\"\n\n  - id: screenshot-loop\n    depends_on: [build-check]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: SCREENSHOTS_COMPLETE\n      max_iterations: 3\n      prompt: |\n        Verify 3 UI screenshots for WO-STOREFRONT-PORTAL-SIDEBAR-01:\n          SHOT_1: Desktop 1280x800 — sidebar visible on left with all 6 nav items,\n                  current section highlighted (active class applied)\n          SHOT_2: Mobile 375x667 — PortalTopNav visible with hamburger button;\n                  clicking hamburger opens drawer with same 6 items\n          SHOT_3: Hover state on a nav link visible — subtle, no harsh color change\n\n        Inspect /tmp/storefront-work-$WORKFLOW_ID/app/shops/[shopSlug]/portal/components/PortalSidebar.tsx,\n        PortalTopNav.tsx, PortalNavLink.tsx. Output per shot: PASS|FAIL + Evidence + Gap.\n\n        When all 3 pass: <promise>SCREENSHOTS_COMPLETE</promise>\n\n  - id: commit-and-push\n    depends_on: [screenshot-loop]\n    bash: |\n      set -euo pipefail\n      cd /tmp/storefront-work-$WORKFLOW_ID\n      BASE=\"wo/storefront-portal-sidebar-01\"\n      # Default to unique run-suffixed branch (avoids worktree collisions)\n      BRANCH=\"${BASE}-run-$(date +%Y%m%d%H%M%S)\"\n      git checkout -b \"$BRANCH\" 2>/dev/null || git checkout \"$BRANCH\"\n\n      # Stage all portal changes\n      git add \"app/shops/[shopSlug]/portal/layout.tsx\" \\\n              \"app/shops/[shopSlug]/portal/page.tsx\" \\\n              \"app/shops/[shopSlug]/portal/components/\" \\\n              \"__tests__/portal/PortalSidebar.test.tsx\"\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(portal): WO-STOREFRONT-PORTAL-SIDEBAR-01 — portal sidebar nav\\n\\nPortalSidebar (desktop) + PortalTopNav (mobile) with 6 nav items:\\n  Pull list / Orders / Statements / Payment settings / Browse / Sign out.\\n\\nWires into layout.tsx (created or modified). Removes '\\''Pick a section from\\nthe sidebar'\\'' placeholder from page.tsx now that the sidebar is real.\\n\\nActive link styling via usePathname() from next/navigation.\\nMobile drawer at <768px. Matches existing Cormorant Garamond + dark theme.\\n\\nTests: __tests__/portal/PortalSidebar.test.tsx (5 RTL tests).\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=$BRANCH\"\n      echo \"SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      cd /tmp/storefront-work-$WORKFLOW_ID\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      REPO=\"bluedevilcollectibles/shopops-storefront\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"WO-STOREFRONT-PORTAL-SIDEBAR-01 — Portal sidebar nav.\"\n        echo \"\"\n        echo \"## Changes\"\n        echo \"- New: PortalSidebar, PortalNavLink, PortalTopNav components\"\n        echo \"- Modified: layout.tsx (sidebar wired)\"\n        echo \"- Modified: page.tsx (placeholder text removed)\"\n        echo \"- New: __tests__/portal/PortalSidebar.test.tsx (5 tests)\"\n        echo \"\"\n        echo \"## Style\"\n        echo \"- Matches existing Cormorant Garamond + dark theme\"\n        echo \"- Active link: subtle left border (no new design tokens)\"\n        echo \"- Mobile <768px: hamburger drawer\"\n        echo \"\"\n        echo \"## Captain CI workflow test (must run on Vercel preview)\"\n        echo \"1. Navigate to /shops/bdc/portal\"\n        echo \"2. Assert PortalSidebar is visible\"\n        echo \"3. Click 'Orders' link\"\n        echo \"4. Assert URL = /shops/bdc/portal/orders\"\n        echo \"\"\n        echo \"Spec: bdc-xo docs/work-orders/WO-STOREFRONT-PORTAL-SIDEBAR-01.md\"\n      } > \"$BODY\"\n\n      PR_URL=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null)\n      if [ -n \"$PR_URL\" ]; then echo \"PR_URL=$PR_URL\"; exit 0; fi\n\n      for attempt in 1 2 3; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base main --head \"$BRANCH\" \\\n            --title \"feat(portal): WO-STOREFRONT-PORTAL-SIDEBAR-01 — portal sidebar nav\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"; exit 0\n        fi\n        sleep 10\n      done\n      exit 1\n\n  - id: verify-vercel\n    depends_on: [open-pr]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort\n      cd /tmp/storefront-work-$WORKFLOW_ID 2>/dev/null || true\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo unknown)\"\n      sleep 30\n      RESULT=$(gh pr list --repo bluedevilcollectibles/shopops-storefront \\\n        --head \"$BRANCH\" --state open --json url,statusCheckRollup 2>&1)\n      if echo \"$RESULT\" | grep -q \"url\"; then\n        echo \"STATUS=pr_open\"\n        echo \"DETAIL=Vercel preview triggered by PR; check PR conversation for preview URL\"\n      else\n        echo \"STATUS=skipped\"\n        echo \"DETAIL=$RESULT\"\n      fi\n      exit 0\n\n  - id: build-manifest\n    depends_on: [verify-vercel]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 for WO-STOREFRONT-PORTAL-SIDEBAR-01. CODE class (single repo, no migration, no runtime container).\n\n      WO: WO-STOREFRONT-PORTAL-SIDEBAR-01\n      Builder: Major Build (Archon bdc-storefront-portal-sidebar)\n\n      Files modified:\n        shopops-storefront/app/shops/[shopSlug]/portal/layout.tsx\n        shopops-storefront/app/shops/[shopSlug]/portal/page.tsx\n\n      Files created:\n        shopops-storefront/app/shops/[shopSlug]/portal/components/PortalSidebar.tsx\n        shopops-storefront/app/shops/[shopSlug]/portal/components/PortalNavLink.tsx\n        shopops-storefront/app/shops/[shopSlug]/portal/components/PortalTopNav.tsx\n        shopops-storefront/__tests__/portal/PortalSidebar.test.tsx\n\n      Tests: 5 passing / 5 total\n        command: bun test __tests__/portal/PortalSidebar.test.tsx\n        evidence: [from verify-tests.TAIL]\n\n      PRs:\n        shopops-storefront main: [from open-pr.PR_URL]\n\n      Merge ancestors:\n        shopops-storefront main HEAD=[from commit-and-push.SHA], behind_by: 0\n\n      Grep assertions:\n        grep -c \"PortalSidebar\" app/shops/[shopSlug]/portal/layout.tsx >= 1\n        grep -c \"Pick a section from the sidebar\" app/shops/[shopSlug]/portal/page.tsx == 0\n        grep -c \"test(\\|it(\" __tests__/portal/PortalSidebar.test.tsx >= 5\n\n      Runtime verification: N/A — storefront deploys via Vercel (no runtime container exec required)\n\n      Vercel deployment:\n        Preview URL: [from open-pr.PR_URL — check PR conversation for Vercel bot comment]\n        Expected state on merge: READY\n        Captain CI Playwright: navigate /shops/bdc/portal, assert sidebar renders, click Orders, assert URL change.\n\n      Link audit (open question Q1 from spec):\n        [from implement loop — record any /shops/[shopSlug]/portal/<route> that is missing\n         and was marked with // TODO comment]\n\n      VALIDATION: PASS\n\n      ## Inputs\n      $read-spec.output $audit-prior-art.output $plan.output\n      $verify-tests.output $verify-grep.output $build-check.output\n      $screenshot-loop.output $commit-and-push.output $open-pr.output\n      $verify-vercel.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-STOREFRONT-PORTAL-SIDEBAR-01 to REVIEW with manifest in Notes property.\n\n      # OPERATOR: Replace 35fef4c9-ce00-816a-8661-caebf938788c before firing.\n      Notion database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      Notion page ID: 35fef4c9-ce00-816a-8661-caebf938788c\n\n      Updates:\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> $build-manifest.output verbatim\n\n      If Notion MCP unavailable:\n        XO_TODO: Flip WO-STOREFRONT-PORTAL-SIDEBAR-01 to REVIEW, paste manifest into Notes.\n\n      ## Manifest\n      $build-manifest.output\n",
  "bdc-verify-gate": "name: bdc-verify-gate\ndescription: |\n  Use when: Verifying a gate checklist (Lane A/B/C, billing reverify, any gate) against a live\n  service, producing a durable evidence report instead of a hand-run one-off.\n  Inputs:\n    CHECKLIST_PATH: bdc-xo path to the gate checklist md (required)\n    GATE_NAME: which section/lane to verify, e.g. \"Lane A\" (required)\n    PROBE_BASE_PROD: prod base URL (default shopops prod)\n    PROBE_BASE_STAGING: staging base URL (default shopops staging)\n    CLEAN_BASELINE_REF: GitHub ref for code/route provenance (default master)\n    NOTION_DB_ID: WO board (default a6df831c-...)\n    AUTOFILE_FIXES: 'true' = file a fix WO per FAIL (default true)\n    DRY_RUN: 'true' = write report locally, skip commit + Notion\n  Does: reads the checklist, probes each item against the live service (CF Access for prod,\n        direct for staging), classifies PASS/FAIL/BLOCKED/NOT_TESTED with captured proof +\n        provenance, commits a durable evidence report to bdc-xo, re-marks the checklist, and\n        auto-files a fix WO per FAIL. Read-only probes; NEVER mutates prod billing.\n  Repo: bluedevilcollectibles/bdc-xo (operator selects bdc-xo codebase when firing).\n  Spec: docs/work-orders/WO-HARNESS-VERIFY-GATE-WORKFLOW-01.md\n  parallel_ok: false\n\n# NOTE (Cauldron API): only conversationId + message reach the engine; the API `inputs`\n# field is dropped. The engine injects the DEFAULTS below as $INPUT_<NAME> env vars. To\n# run a different gate, edit CHECKLIST_PATH/GATE_NAME defaults and re-fire, or pass them\n# in the message and parse in bash (mirrors bdc-feature-development's WO_ID pattern).\n#\n# SECRET HANDLING (Rule 6): CF Access token is NEVER hardcoded here. probe-gate reads\n# $CF_ACCESS_CLIENT_ID / $CF_ACCESS_CLIENT_SECRET from container env. Prereq: add both to\n# the archon container env file (shopops-storefront-vercel service token values).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\ntarget_repo: bluedevilcollectibles/bdc-xo\n\ninputs:\n  CHECKLIST_PATH:\n    default: \"xo-wiki/wiki/concepts/shopops-recovery-checklist/_index.md\"\n  GATE_NAME:\n    default: \"Lane A\"\n  PROBE_BASE_PROD:\n    default: \"https://shopops.bluedevilcollectibles.com\"\n  PROBE_BASE_STAGING:\n    default: \"https://shopops-api.livesellerpro.app\"\n  CLEAN_BASELINE_REF:\n    default: \"master\"\n  NOTION_DB_ID:\n    default: \"a6df831c-0b52-449f-8ca4-d77be6b70d0a\"\n  AUTOFILE_FIXES:\n    default: \"true\"\n  DRY_RUN:\n    default: \"false\"\n\nnodes:\n\n  # Pre-flight: fail closed if container lacks GITHUB auth (WO-169 anchor).\n  - id: preflight-github-auth\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      DRY_RUN=\"${INPUT_DRY_RUN:-${DRY_RUN:-false}}\"\n      if [ \"$DRY_RUN\" = \"true\" ]; then\n        echo \"preflight-github-auth: DRY_RUN=true -- skipping auth probe\"\n        exit 0\n      fi\n      if ! git push --dry-run origin \"HEAD:refs/heads/preflight-auth-probe-$(date +%s)\" 2>&1; then\n        echo \"ERROR: GITHUB auth check failed -- container cannot push to origin.\" >&2\n        echo \"Likely cause: GITHUB_TOKEN missing in executor env, or token lacks repo scope.\" >&2\n        exit 1\n      fi\n      echo \"preflight-github-auth PASS\"\n\n  # Read the gate checklist from bdc-xo and isolate the target gate section.\n  - id: read-checklist\n    depends_on: [preflight-github-auth]\n    bash: |\n      set -euo pipefail\n      CHECKLIST_PATH=\"${INPUT_CHECKLIST_PATH:-${CHECKLIST_PATH:-xo-wiki/wiki/concepts/shopops-recovery-checklist/_index.md}}\"\n      GATE_NAME=\"${INPUT_GATE_NAME:-${GATE_NAME:-Lane A}}\"\n      PROBE_BASE_PROD=\"${INPUT_PROBE_BASE_PROD:-${PROBE_BASE_PROD:-https://shopops.bluedevilcollectibles.com}}\"\n      PROBE_BASE_STAGING=\"${INPUT_PROBE_BASE_STAGING:-${PROBE_BASE_STAGING:-https://shopops-api.livesellerpro.app}}\"\n      CLEAN_BASELINE_REF=\"${INPUT_CLEAN_BASELINE_REF:-${CLEAN_BASELINE_REF:-master}}\"\n      NOTION_DB_ID=\"${INPUT_NOTION_DB_ID:-${NOTION_DB_ID:-a6df831c-0b52-449f-8ca4-d77be6b70d0a}}\"\n      AUTOFILE_FIXES=\"${INPUT_AUTOFILE_FIXES:-${AUTOFILE_FIXES:-true}}\"\n      DRY_RUN=\"${INPUT_DRY_RUN:-${DRY_RUN:-false}}\"\n\n      # Read checklist: local worktree first (CWD = bdc-xo root), then gh api cross-ref.\n      CHECKLIST_CONTENT=\"\"\n      if [ -f \"$CHECKLIST_PATH\" ]; then\n        CHECKLIST_CONTENT=$(cat \"$CHECKLIST_PATH\")\n      else\n        CHECKLIST_CONTENT=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/${CHECKLIST_PATH}?ref=main\" \\\n          --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n      fi\n      if [ -z \"$CHECKLIST_CONTENT\" ]; then\n        echo \"ERROR: checklist not found at $CHECKLIST_PATH\" >&2\n        exit 1\n      fi\n\n      # CF Access token presence (do NOT print the values; print presence only).\n      CF_PRESENT=\"no\"\n      if [ -n \"${CF_ACCESS_CLIENT_ID:-}\" ] && [ -n \"${CF_ACCESS_CLIENT_SECRET:-}\" ]; then\n        CF_PRESENT=\"yes\"\n      fi\n\n      DATE_TAG=$(date -u +%Y-%m-%d)\n      GATE_SLUG=$(printf '%s' \"$GATE_NAME\" | tr '[:upper:] ' '[:lower:]-' | tr -cd 'a-z0-9-')\n\n      printf 'GATE_NAME=%s\\n' \"$GATE_NAME\"\n      printf 'GATE_SLUG=%s\\n' \"$GATE_SLUG\"\n      printf 'CHECKLIST_PATH=%s\\n' \"$CHECKLIST_PATH\"\n      printf 'PROBE_BASE_PROD=%s\\n' \"$PROBE_BASE_PROD\"\n      printf 'PROBE_BASE_STAGING=%s\\n' \"$PROBE_BASE_STAGING\"\n      printf 'CLEAN_BASELINE_REF=%s\\n' \"$CLEAN_BASELINE_REF\"\n      printf 'NOTION_DB_ID=%s\\n' \"$NOTION_DB_ID\"\n      printf 'AUTOFILE_FIXES=%s\\n' \"$AUTOFILE_FIXES\"\n      printf 'DRY_RUN=%s\\n' \"$DRY_RUN\"\n      printf 'CF_ACCESS_TOKEN_PRESENT=%s\\n' \"$CF_PRESENT\"\n      printf 'EVIDENCE_PATH=docs/gate-evidence/%s-%s.md\\n\\n' \"$GATE_SLUG\" \"$DATE_TAG\"\n      printf '=== CHECKLIST (%s) ===\\n%s\\n' \"$CHECKLIST_PATH\" \"$CHECKLIST_CONTENT\"\n\n  # Probe each item in the gate section. Read-only. Classify with proof + provenance.\n  - id: probe-gate\n    depends_on: [read-checklist]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: ALL_ITEMS_CLASSIFIED\n      max_iterations: 3\n      prompt: |\n        You are the BDC Gate Verification probe. CWD = bdc-xo repo root. You have bash +\n        file read/write tools.\n\n        ## Inputs\n        $read-checklist.output\n\n        ## Previous iteration (empty on first pass)\n        $LOOP_PREV_OUTPUT\n\n        ## Your task\n        Verify every checklist item under the GATE_NAME section. For EACH item:\n\n        1. Determine the probe: which ShopOps endpoint/route + method proves or disproves\n           the item's claim. Read-only GETs only. Use PROBE_BASE_PROD and PROBE_BASE_STAGING.\n        2. PROD probes go through Cloudflare Access -- send BOTH headers:\n             curl -s -o /dev/null -w '%{http_code}' \\\n               -H \"CF-Access-Client-Id: $CF_ACCESS_CLIENT_ID\" \\\n               -H \"CF-Access-Client-Secret: $CF_ACCESS_CLIENT_SECRET\" \\\n               \"$PROBE_BASE_PROD/<endpoint>\"\n           If CF_ACCESS_TOKEN_PRESENT=no, mark all PROD-only items BLOCKED (reason:\n           \"CF Access token not in container env\"). STAGING probes need no CF headers.\n        3. Capture the actual HTTP status and a short response-shape note as proof.\n        4. For code/route provenance, read source via:\n             gh api \"repos/bluedevilcollectibles/shopops/contents/<path>?ref=$CLEAN_BASELINE_REF\"\n           NEVER read /opt/bdc/shopops (dirty checkout). State the baseline ref per row.\n        5. Classify each item:\n             PASS        -- probe succeeded with expected status/shape (proof captured)\n             FAIL        -- probe returned wrong status/shape vs the claim (capture actual)\n             BLOCKED     -- could not authenticate / CF Access unreachable / unsafe to test\n             NOT_TESTED  -- no probe applies / out of scope this run\n           A PASS with no captured proof is INVALID -- downgrade to BLOCKED.\n\n        ## BILLING SAFETY -- HARD RULE\n        You may GET balance/summary/statement-list/pdf. You may NOT POST /statements/generate,\n        send invoices, or mutate billing on PROD. Billing-mutation items = BLOCKED (reason:\n        \"prod billing mutation requires John PROCEED -- out of scope\"). Staging mutation OK.\n\n        ## Output (when every item is classified)\n        Emit a markdown table, one row per item:\n          | # | Item | Target | Auth | Endpoint/Path | Data used | Result | Proof | Provenance |\n        Then a summary line: PASS=<n> FAIL=<n> BLOCKED=<n> NOT_TESTED=<n>\n        Then on its own line the sentinel: ALL_ITEMS_CLASSIFIED\n        If you cannot classify every item this iteration, emit progress + iterate.\n\n  # Compose the evidence report file from the probe table.\n  - id: write-evidence\n    depends_on: [probe-gate]\n    bash: |\n      set -uo pipefail\n      EVIDENCE_PATH=$(printf '%s\\n' \"$read-checklist.output\" | sed -n 's/^EVIDENCE_PATH=//p' | head -n 1)\n      GATE_NAME=$(printf '%s\\n' \"$read-checklist.output\" | sed -n 's/^GATE_NAME=//p' | head -n 1)\n      CLEAN_BASELINE_REF=$(printf '%s\\n' \"$read-checklist.output\" | sed -n 's/^CLEAN_BASELINE_REF=//p' | head -n 1)\n      if [ -z \"$EVIDENCE_PATH\" ]; then\n        echo \"ERROR: EVIDENCE_PATH not found in read-checklist output\" >&2\n        exit 1\n      fi\n      mkdir -p \"$(dirname \"$EVIDENCE_PATH\")\"\n      {\n        printf '# Gate Evidence: %s\\n\\n' \"$GATE_NAME\"\n        printf '_Generated %s by bdc-verify-gate. Clean baseline ref: %s. Read-only probes; no prod billing mutation._\\n\\n' \\\n          \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\" \"$CLEAN_BASELINE_REF\"\n        printf '%s\\n' \"$probe-gate.output\"\n      } > \"$EVIDENCE_PATH\"\n      echo \"EVIDENCE_WRITTEN=$EVIDENCE_PATH\"\n      echo \"EVIDENCE_BYTES=$(wc -c < \"$EVIDENCE_PATH\" | tr -d ' ')\"\n\n  # Commit the evidence report to bdc-xo with the ORIGIN-VERIFY invariant.\n  - id: commit-evidence\n    depends_on: [write-evidence]\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      DRY_RUN=$(printf '%s\\n' \"$read-checklist.output\" | sed -n 's/^DRY_RUN=//p' | head -n 1)\n      EVIDENCE_PATH=$(printf '%s\\n' \"$write-evidence.output\" | sed -n 's/^EVIDENCE_WRITTEN=//p' | head -n 1)\n      GATE_NAME=$(printf '%s\\n' \"$read-checklist.output\" | sed -n 's/^GATE_NAME=//p' | head -n 1)\n      if [ -z \"$EVIDENCE_PATH\" ] || [ ! -f \"$EVIDENCE_PATH\" ]; then\n        echo \"ERROR: evidence file missing -- write-evidence did not produce a file\" >&2\n        exit 1\n      fi\n      if [ \"$DRY_RUN\" = \"true\" ]; then\n        echo \"commit-evidence: DRY_RUN=true -- evidence at $EVIDENCE_PATH, not committed\"\n        echo \"EVIDENCE_PATH=$EVIDENCE_PATH\"\n        exit 0\n      fi\n      UNIQUE_BRANCH=\"verify-gate/$(printf '%s' \"$GATE_NAME\" | tr '[:upper:] ' '[:lower:]-' | tr -cd 'a-z0-9-')-run-$(date +%Y%m%d%H%M%S)\"\n      git add \"$EVIDENCE_PATH\"\n      if git diff --cached --quiet; then\n        echo \"commit-evidence: no change to evidence file (idempotent re-run)\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"evidence(verify-gate): ${GATE_NAME} verification report\n\n      Generated by bdc-verify-gate. Read-only probes; no prod billing mutation.\n\n      Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>\"\n      fi\n      # ORIGIN-VERIFY invariant (WO-HARNESS-COMMIT-AND-PUSH-ORIGIN-VERIFY-02):\n      # the report MUST be durably on origin before anything downstream. Push to a\n      # unique branch + verify origin == HEAD or FATAL.\n      PUSH_OK=false\n      for attempt in 1 2 3; do\n        if git push origin \"HEAD:${UNIQUE_BRANCH}\" 2>&1; then PUSH_OK=true; break; fi\n        echo \"WARN: push attempt $attempt failed; retrying after $((attempt * 5))s...\" >&2\n        sleep $((attempt * 5))\n      done\n      if [ \"$PUSH_OK\" = \"false\" ]; then\n        echo \"FATAL: evidence push failed after 3 attempts -- echoing report to logs for recovery:\" >&2\n        echo \"===EVIDENCE_BEGIN===\" >&2; cat \"$EVIDENCE_PATH\" >&2; echo \"===EVIDENCE_END===\" >&2\n        exit 1\n      fi\n      REMOTE_HEAD=\"$(git ls-remote origin \"refs/heads/${UNIQUE_BRANCH}\" 2>/dev/null | awk '{print $1}')\"\n      LOCAL_HEAD=\"$(git rev-parse HEAD)\"\n      if [ \"$REMOTE_HEAD\" != \"$LOCAL_HEAD\" ]; then\n        echo \"FATAL: evidence branch ${UNIQUE_BRANCH} not durably on origin (origin=${REMOTE_HEAD:-<absent>} local=${LOCAL_HEAD})\" >&2\n        echo \"===EVIDENCE_BEGIN===\" >&2; cat \"$EVIDENCE_PATH\" >&2; echo \"===EVIDENCE_END===\" >&2\n        exit 1\n      fi\n      echo \"VERIFIED: origin/${UNIQUE_BRANCH} == local HEAD (${LOCAL_HEAD})\"\n      echo \"EVIDENCE_BRANCH=${UNIQUE_BRANCH}\"\n      echo \"EVIDENCE_PATH=${EVIDENCE_PATH}\"\n\n  # Open a PR so the evidence + checklist re-mark land on bdc-xo main.\n  - id: open-evidence-pr\n    depends_on: [commit-evidence]\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      DRY_RUN=$(printf '%s\\n' \"$read-checklist.output\" | sed -n 's/^DRY_RUN=//p' | head -n 1)\n      if [ \"$DRY_RUN\" = \"true\" ]; then\n        echo \"open-evidence-pr: DRY_RUN=true -- skipping PR\"\n        exit 0\n      fi\n      UNIQUE_BRANCH=$(printf '%s\\n' \"$commit-evidence.output\" | sed -n 's/^EVIDENCE_BRANCH=//p' | head -n 1)\n      GATE_NAME=$(printf '%s\\n' \"$read-checklist.output\" | sed -n 's/^GATE_NAME=//p' | head -n 1)\n      if [ -z \"$UNIQUE_BRANCH\" ]; then\n        echo \"open-evidence-pr: no EVIDENCE_BRANCH (commit may have been a no-op) -- nothing to PR\"\n        exit 0\n      fi\n      # Precondition: branch must be on origin at HEAD (ORIGIN-VERIFY guard).\n      REMOTE_HEAD=\"$(git ls-remote origin \"refs/heads/${UNIQUE_BRANCH}\" 2>/dev/null | awk '{print $1}')\"\n      if [ \"$REMOTE_HEAD\" != \"$(git rev-parse HEAD)\" ]; then\n        echo \"FATAL: open-evidence-pr precondition failed -- ${UNIQUE_BRANCH} not on origin at HEAD\" >&2\n        exit 1\n      fi\n      PR_OK=false\n      for attempt in 1 2 3; do\n        if gh pr create --repo bluedevilcollectibles/bdc-xo \\\n             --title \"evidence(verify-gate): ${GATE_NAME} verification report\" \\\n             --body \"Automated gate-evidence report from bdc-verify-gate. Read-only probes; no prod billing mutation. Review the evidence table + re-marked checklist.\" \\\n             --head \"$UNIQUE_BRANCH\" 2>&1; then\n          PR_OK=true; break\n        fi\n        EXIST=$(gh pr list --repo bluedevilcollectibles/bdc-xo --head \"$UNIQUE_BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null || true)\n        if [ -n \"$EXIST\" ]; then echo \"PR_URL=$EXIST\"; PR_OK=true; break; fi\n        echo \"WARN: gh pr create attempt $attempt failed; retrying after $((attempt * 5))s...\" >&2\n        sleep $((attempt * 5))\n      done\n      if [ \"$PR_OK\" = \"false\" ]; then\n        # Branch is verified on origin above -- work is SAFE, salvageable manually.\n        echo \"PR_CREATE_FAILED_BUT_BRANCH_SAFE: repo=bluedevilcollectibles/bdc-xo branch=${UNIQUE_BRANCH} sha=$(git rev-parse HEAD)\" >&2\n        echo \"RECOVERY: gh pr create --repo bluedevilcollectibles/bdc-xo --head ${UNIQUE_BRANCH}\" >&2\n        echo \"ERROR: gh pr create failed after 3 attempts.\" >&2\n        exit 1\n      fi\n      echo \"EVIDENCE_BRANCH=${UNIQUE_BRANCH}\"\n\n  # Auto-file a fix WO per FAIL row (prompt-node: needs Notion MCP + judgment).\n  - id: file-fix-wos\n    depends_on: [open-evidence-pr]\n    task_class: clerical\n    prompt: |\n      Review the gate verification results and file a fix Work Order for each FAIL row.\n\n      ## Inputs\n      $read-checklist.output\n      $probe-gate.output\n      $commit-evidence.output\n\n      ## DRY_RUN / AUTOFILE check\n      If $read-checklist.output contains `DRY_RUN=true` OR `AUTOFILE_FIXES=false`:\n        Do NOT file anything. Output instead, one line per FAIL row:\n          XO_TODO_FIX: <item> -- <captured failure> (file a fix WO manually)\n        Then stop.\n\n      ## When AUTOFILE_FIXES=true and DRY_RUN=false\n      For each row in $probe-gate.output classified FAIL:\n      1. Derive a fix WO ID: WO-FIX-<GATE_SLUG>-<short-item-slug>-01.\n      2. Check Notion DB (from NOTION_DB_ID in $read-checklist.output) for an existing\n         page with that WO ID. If it exists, skip (idempotent) and note SKIP_EXISTING.\n      3. Otherwise create a Notion page via the notion-create-pages tool:\n         - Task / WO ID: the derived WO-FIX id\n         - Status: To Do ; Claude Status: QUEUED ; Execution State: ACTIVE\n         - Priority: P1 ; Builder: Codex ; Project: ShopOps\n         - Notes: the failing item text + the captured failure proof (status/shape) +\n           \"Surfaced by bdc-verify-gate gate verification. Evidence: \" + the EVIDENCE_PATH.\n      4. Output: FIX_WO_FILED=<wo-id> <notion-url>  (or SKIP_EXISTING=<wo-id>) per FAIL.\n\n      If there are ZERO FAIL rows, output: NO_FAILS -- no fix WOs needed.\n\n      Do NOT file fix WOs for BLOCKED or NOT_TESTED rows (those need John triage, not a code fix).\n\n  - id: build-manifest\n    depends_on: [file-fix-wos]\n    task_class: clerical\n    prompt: |\n      Construct the Captain CI completion manifest for WO-HARNESS-VERIFY-GATE-WORKFLOW-01.\n\n      ## Inputs\n      $read-checklist.output\n      $probe-gate.output\n      $commit-evidence.output\n      $open-evidence-pr.output\n      $file-fix-wos.output\n\n      Produce a manifest v2 block. Class: INFRA/verification. Include:\n      - Files created: the evidence report path under docs/gate-evidence/\n      - PRs: the bdc-xo evidence PR URL\n      - Runtime verification: the probe table summary (PASS/FAIL/BLOCKED/NOT_TESTED counts)\n      - Fix WOs filed: list from $file-fix-wos.output\n      - VALIDATION: PASS if every item was classified and the evidence landed durably on origin.\n\n  # Final node: flip this WO to REVIEW (Archon does not auto-update Notion).\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Update the Notion page for WO-HARNESS-VERIFY-GATE-WORKFLOW-01 in database\n      a6df831c-0b52-449f-8ca4-d77be6b70d0a:\n      - Claude Status: REVIEW\n      - Status: Review\n      - Append to Notes: the manifest from $build-manifest.output (evidence PR + probe summary).\n      Use the Notion MCP notion-update-page tool. Output NOTION_UPDATED=true on success.\n",
  "bdc-wallet-pass-per-location": "# Drafted by yaml-author for WO-WALLET-PASS-PER-LOCATION-01 on 2026-05-13\n# Source pattern: WO-SHOPOPS-COMMERCE-SPINE-01.yaml (single-repo schema migration)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring\n#\n# Pre-flight: codebase bluedevilcollectibles/shopops registered in Archon.\n# Pre-flight (4-block): YAML exists, spec exists, CWD = shopops single-repo, codebase registered.\n# parallel_ok: false — schema migration. Do NOT fire alongside other shopops schema WOs.\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh WO-WALLET-PASS-PER-LOCATION-01.yaml\n# REVIEW BEFORE DEPLOY — verify wallet_passes existing rows backfill plan with operator.\n\nname: bdc-wallet-pass-per-location\ndescription: |\n  Use when: Executing WO-WALLET-PASS-PER-LOCATION-01 — per-location wallet pass.\n  Input: WO_ID = WO-WALLET-PASS-PER-LOCATION-01.\n  Does: Reads spec from bdc-xo. Verifies wallet_passes schema. Writes migration\n        20260513_wallet_pass_per_location.sql (store_locations table + location_id\n        column + backfill + unique constraint). Modifies routes/wallet.js +\n        services/wallet-google.js + services/wallet-apn.js. Adds services/store-locations.js.\n        Adds POST /wallet/pass/sync-all. Bun tests (>= 8). PR + Rule 19 runtime-verify.\n        MIXED manifest (CODE + schema). Flips Notion.\n  Repo: bluedevilcollectibles/shopops.\n  parallel_ok: false (schema migration, serial only).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-WALLET-PASS-PER-LOCATION-01}\"\n      # Primary path (gh --template, no jq dependency)\n      SPEC=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      if [ -z \"$SPEC\" ]; then\n        GLOB=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB\" ]; then\n          SPEC=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n      printf 'WO_ID=%s\\nWORKFLOW_ID=%s\\n\\n--- SPEC ---\\n%s\\n' \"$WO_ID\" \"$WORKFLOW_ID\" \"$SPEC\"\n\n  - id: fresh-branch\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      DEFAULT_BRANCH=\"$(git remote show origin | sed -n 's,.*HEAD branch: ,,p')\"\n      git fetch origin \"$DEFAULT_BRANCH\" --depth=50 || true\n      # Tolerate worktree-locked default branch\n      if ! git checkout \"$DEFAULT_BRANCH\" 2>/dev/null; then\n        echo \"default branch held by other worktree; staying on current HEAD\"\n      fi\n      git reset --hard \"origin/$DEFAULT_BRANCH\" 2>/dev/null || true\n      BASE=\"wo/wallet-pass-per-location-01\"\n      # Default to unique run-suffixed branch (avoids worktree collisions)\n      BRANCH=\"${BASE}-run-$(date +%Y%m%d%H%M%S)\"\n      git checkout -b \"$BRANCH\"\n      echo \"BRANCH=$BRANCH\"\n\n  - id: schema-verify\n    depends_on: [fresh-branch]\n    bash: |\n      SUPABASE_URL=\"${SUPABASE_URL:-}\"\n      SUPABASE_SERVICE_KEY=\"${SUPABASE_SERVICE_KEY:-}\"\n      if [ -z \"$SUPABASE_URL\" ] || [ -z \"$SUPABASE_SERVICE_KEY\" ]; then\n        echo \"STATUS=skipped_no_env\"\n        echo \"DETAIL=Operator must verify wallet_passes columns + row count before migration apply\"\n        exit 0\n      fi\n      WP=$(curl -sf \\\n        \"${SUPABASE_URL}/rest/v1/information_schema.columns?table_name=eq.wallet_passes&select=column_name\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n        -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n      SL=$(curl -sf \\\n        \"${SUPABASE_URL}/rest/v1/information_schema.tables?table_name=eq.store_locations&select=table_name\" \\\n        -H \"apikey: ${SUPABASE_SERVICE_KEY}\" \\\n        -H \"Authorization: Bearer ${SUPABASE_SERVICE_KEY}\" 2>&1) || true\n      echo \"STATUS=ok\"\n      echo \"WALLET_PASSES_COLUMNS=${WP}\"\n      echo \"STORE_LOCATIONS_EXISTS=${SL}\"\n      exit 0\n\n  - id: plan\n    depends_on: [read-spec, schema-verify]\n    task_class: medium_work\n    prompt: |\n      Plan WO-WALLET-PASS-PER-LOCATION-01.\n\n      ## Files\n      Create:\n        - shopops-api/migrations/20260513_wallet_pass_per_location.sql\n        - shopops-api/services/store-locations.js\n        - shopops-api/tests/test_wallet_per_location.js\n      Modify:\n        - shopops-api/routes/wallet.js (location_id required, add sync-all endpoint)\n        - shopops-api/services/wallet-google.js (read branding from location)\n        - shopops-api/services/wallet-apn.js (pushAllForCustomer helper)\n\n      Branch: wo/wallet-pass-per-location-01\n      PR base: master\n\n      ## Spec\n      $read-spec.output\n\n      ## Schema\n      $schema-verify.output\n\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement WO-WALLET-PASS-PER-LOCATION-01 in shopops-api.\n\n        ## Plan\n        $plan.output\n        ## Spec\n        $read-spec.output\n\n        Rules:\n        1. Migration per WO §7 (store_locations table, location_id column on wallet_passes,\n           backfill UPDATE, default-location seed for every tenant, UNIQUE INDEX).\n        2. services/store-locations.js exports getLocation(tenantId, locationId) +\n           getDefaultLocation(tenantId), with 60s in-memory cache.\n        3. routes/wallet.js POST /wallet/pass now accepts/requires location_id (with\n           auto-resolve to default for single-location tenants). NEW POST /wallet/pass/sync-all.\n           Existing PATCH /wallet/pass/:serial unchanged.\n        4. services/wallet-google.js buildPassPayload reads from location.\n        5. services/wallet-apn.js pushAllForCustomer iterates serials.\n        6. tests/test_wallet_per_location.js — >= 8 bun tests per WO §11.\n        7. Keep existing /wallet/pass behavior backward compatible (single-location tenants).\n        8. NO push, NO docker, NO migration apply.\n\n        Output <promise>COMPLETE</promise> when files exist + bun tests pass.\n\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      ls shopops-api/migrations/20260513_wallet_pass_per_location.sql\n      grep -q \"store_locations\" shopops-api/migrations/20260513_wallet_pass_per_location.sql\n      grep -q \"location_id\" shopops-api/migrations/20260513_wallet_pass_per_location.sql\n      grep -q \"beacon_uuid\" shopops-api/migrations/20260513_wallet_pass_per_location.sql\n      grep -q \"ADD COLUMN IF NOT EXISTS location_id\" shopops-api/migrations/20260513_wallet_pass_per_location.sql\n      ls shopops-api/services/store-locations.js\n      grep -q \"getLocation\" shopops-api/services/store-locations.js\n      grep -q \"getDefaultLocation\" shopops-api/services/store-locations.js\n      grep -q \"location_id\" shopops-api/routes/wallet.js\n      grep -q \"sync-all\\|pushAllForCustomer\" shopops-api/routes/wallet.js\n      grep -q \"pushAllForCustomer\" shopops-api/services/wallet-apn.js\n      TC=$(grep -c \"it(\\|test(\" shopops-api/tests/test_wallet_per_location.js || echo 0)\n      [ \"$TC\" -ge 8 ] || (echo \"FAIL test count $TC\" >&2; exit 1)\n      echo \"verify-grep PASS\"\n\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      bun test tests/test_wallet_per_location.js\n\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      # Use the actual current branch (fresh-branch creates a unique run-suffixed one).\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      echo \"Committing on BRANCH=${BRANCH}\"\n      git add \\\n        shopops-api/migrations/20260513_wallet_pass_per_location.sql \\\n        shopops-api/services/store-locations.js \\\n        shopops-api/services/wallet-google.js \\\n        shopops-api/services/wallet-apn.js \\\n        shopops-api/routes/wallet.js \\\n        shopops-api/tests/test_wallet_per_location.js\n      if git diff --cached --quiet; then\n        echo \"No staged changes\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"$(printf 'feat(api): WO-WALLET-PASS-PER-LOCATION-01 — per-location wallet passes\\n\\nAdds migration 20260513_wallet_pass_per_location.sql:\\n  - CREATE TABLE store_locations (id, tenant_id, name, color_hex, beacon_uuid, ...)\\n  - ALTER TABLE wallet_passes ADD COLUMN location_id\\n  - Backfill existing rows + seed default location per tenant\\n  - UNIQUE INDEX (customer_id, location_id, tenant_id)\\n\\nAdds services/store-locations.js (getLocation + getDefaultLocation + 60s cache).\\nModifies routes/wallet.js: POST /wallet/pass takes location_id; NEW POST /wallet/pass/sync-all.\\nModifies services/wallet-google.js to read branding from location.\\nModifies services/wallet-apn.js to add pushAllForCustomer helper.\\nAdds tests/test_wallet_per_location.js (8+ bun tests).\\n\\nPATCH /wallet/pass/:serial interface unchanged (backward compat).\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>')\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"SHA=$(git rev-parse HEAD)\"\n\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      BODY=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements WO-WALLET-PASS-PER-LOCATION-01 — wallet pass keyed by (customer, location).\"\n        echo \"\"\n        echo \"## Migration\"\n        echo \"shopops-api/migrations/20260513_wallet_pass_per_location.sql\"\n        echo \"- Creates store_locations table\"\n        echo \"- Adds location_id to wallet_passes + backfills existing rows + seeds default per tenant\"\n        echo \"- Must be applied to staging Supabase before container rebuild\"\n        echo \"\"\n        echo \"## API\"\n        echo \"- POST /wallet/pass — now takes location_id (auto-resolves for single-location)\"\n        echo \"- POST /wallet/pass/sync-all — pushes APN update to every pass for a customer\"\n        echo \"- PATCH /wallet/pass/:serial — UNCHANGED\"\n      } > \"$BODY\"\n      EXIST=$(gh pr list --repo bluedevilcollectibles/shopops --head \"$BRANCH\" --state open --json url --template '{{range .}}{{.url}}{{end}}' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then echo \"PR_URL=$EXIST\"; exit 0; fi\n      for i in 1 2 3; do\n        if URL=$(gh pr create --repo bluedevilcollectibles/shopops --base master --head \"$BRANCH\" \\\n            --title \"feat(api): WO-WALLET-PASS-PER-LOCATION-01 — per-location wallet passes\" \\\n            --body-file \"$BODY\" 2>&1); then\n          echo \"PR_URL=$URL\"; exit 0\n        fi\n        sleep 5\n      done\n      exit 1\n\n  - id: runtime-verify\n    depends_on: [open-pr]\n    bash: |\n      RESULT=$(docker exec shopops-api grep -n \"location_id\" /app/routes/wallet.js 2>&1)\n      EXIT=$?\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RULE19=${RESULT}\"\n      elif echo \"$RESULT\" | grep -qiE 'No such (container|file)|not found'; then\n        echo \"STATUS=skipped_container_not_rebuilt\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=${RESULT}\"\n      fi\n      exit 0\n\n  - id: build-manifest\n    depends_on: [runtime-verify]\n    task_class: clerical\n    prompt: |\n      Build manifest v2 (MIXED) for WO-WALLET-PASS-PER-LOCATION-01.\n\n      WO: WO-WALLET-PASS-PER-LOCATION-01\n      Builder: Codex (Archon bdc-wallet-pass-per-location)\n      Files created: shopops-api/migrations/20260513_wallet_pass_per_location.sql,\n                     shopops-api/services/store-locations.js,\n                     shopops-api/tests/test_wallet_per_location.js\n      Files modified: shopops-api/routes/wallet.js, services/wallet-google.js,\n                       services/wallet-apn.js\n      Tests: from $verify-tests.output | bun test shopops-api/tests/test_wallet_per_location.js\n      PRs: from $open-pr.output PR_URL — base: master\n      Merge ancestors: shopops master HEAD from $commit-and-push.output SHA, behind_by: 0\n      Grep assertions:\n        - grep -q 'store_locations' shopops-api/migrations/20260513_wallet_pass_per_location.sql\n        - grep -q 'location_id' shopops-api/routes/wallet.js\n        - grep -q 'getLocation' shopops-api/services/store-locations.js\n        - grep -q 'pushAllForCustomer' shopops-api/services/wallet-apn.js\n      Runtime verification:\n        - docker exec shopops-api grep -n 'location_id' /app/routes/wallet.js\n        - from $runtime-verify.output\n      Schema migration:\n        - File: shopops-api/migrations/20260513_wallet_pass_per_location.sql\n        - Apply to staging Supabase BEFORE container rebuild.\n        - Post-apply verify: SELECT column_name FROM information_schema.columns\n          WHERE table_name='wallet_passes' AND column_name='location_id' (expected 1)\n        - Backfill verify: SELECT COUNT(*) FROM wallet_passes WHERE location_id IS NULL (expected 0)\n        - Seed verify: SELECT COUNT(*) FROM store_locations (expected >= active tenant count)\n      Vercel deployment: N/A — shopops-api is Hetzner Docker\n      VALIDATION: PASS (pending migration apply + container rebuild)\n\n      Inputs:\n      $read-spec.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-and-push.output\n      $open-pr.output\n      $runtime-verify.output\n\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-WALLET-PASS-PER-LOCATION-01 to REVIEW. Manifest in Notes property.\n      Notion database: a6df831c-0b52-449f-8ca4-d77be6b70d0a\n      # OPEN QUESTION: Notion page ID not confirmed. Operator searches by exact WO ID.\n\n      Update: Claude Status -> REVIEW, Status -> Review, Notes -> $build-manifest.output,\n      claimed_by: Codex, claimed_at: today.\n\n      If Notion MCP unavailable:\n        XO_TODO: Flip WO-WALLET-PASS-PER-LOCATION-01 to REVIEW; paste manifest into Notes.\n\n      Manifest:\n      $build-manifest.output\n",
  "bdc-wo-author": "# Drafted by Major Build for WO-HARNESS-WO-AUTHOR-WORKFLOW-01 on 2026-05-25\n# Pattern source: bdc-author-wo-batch.yaml (adversarial loop, host-artifact bundle)\n#                 bdc-feature-development.yaml (preflight, fire pattern, sentinel)\n#\n# Pre-flight (4-block):\n#   1. YAML exists: this file\n#   2. Spec exists: BDC_XO/docs/work-orders/WO-HARNESS-WO-AUTHOR-WORKFLOW-01.md\n#   3. CWD scope: bdc-xo (WO specs + YAMLs live there -- select bdc-xo when firing)\n#   4. Codebase registered: bluedevilcollectibles/bdc-xo (verify in Archon DB)\n#\n# Deploy: BDC_XO/scripts/archon-bridge/sync-workflows.sh bdc-wo-author.yaml\n#\n# Fire examples:\n#   /workflow run bdc-wo-author MODE=--draft-only USER_MESSAGE=\"Author a WO to add pull-list export to ShopOps\"\n#   /workflow run bdc-wo-author MODE=--queue     USER_MESSAGE=\"bdc-harness#128\"\n#   /workflow run bdc-wo-author MODE=--fire      USER_MESSAGE=\"Author WO for SIGTERM cleanup --fire it\"\n#\n# Author.md / Validate.md skills: their 12-section template + 12-check gate are\n# inlined in the author-loop prompt (the same way bdc-author-wo-batch inlines\n# them). Builders firing this YAML do not need filesystem access to ~/.claude/.\n\nname: bdc-wo-author\ndescription: |\n  Use when: Authoring any new BDC WO. This is the canonical front door — chat,\n  GitHub issue, Notion stub, and file-drop intake all normalize into one\n  pipeline (Author.md 12-section draft -> Validate.md 12-check adversarial gate\n  -> dry-run preview -> optional approval gate -> commit-spec -> file-notion\n  -> optional fire-cauldron). Replaces informal handoff via chat/VS Code.\n  Inputs:\n    USER_MESSAGE: free-text description, or \"@<repo>#<N>\" for GitHub issue,\n                  or \"--notion <page-id>\" for Notion stub,\n                  or \"--spec <repo-relative-path>\" for an existing draft file.\n                  Mode flags inline: --draft-only | --queue (default) | --fire\n                  Auto-approve flag: --auto-approve, \"fire it\", \"fire now\"\n    MODE: optional explicit mode override (--draft-only|--queue|--fire); USER_MESSAGE wins\n    NOTION_DB_ID: Notion WO database (default: a6df831c-0b52-449f-8ca4-d77be6b70d0a)\n  Does: Normalizes intake, runs adversarial author-loop (max 5 iterations),\n        emits dry-run preview block (always), gates on operator approval when\n        --fire mode (unless auto-approve), commits spec to bdc-xo feature\n        branch, files Notion page (unless --draft-only), fires\n        bdc-feature-development against the target repo (--fire only).\n  Repo: bluedevilcollectibles/bdc-xo (operator selects bdc-xo codebase when firing).\n  NOT for: Doctrine-only WOs (use bdc-doctrine-update). Existing WOs already in\n        REVIEW status. Batch authoring (use bdc-author-wo-batch).\n  parallel_ok: false\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\n# interactive: true forces foreground execution on web. Required because\n# approval-gate is an approval node that hangs silently otherwise (Archon\n# CLAUDE.md: \"interactive: true at the workflow level forces foreground\n# execution on web (required for approval-gate workflows in the web UI)\").\ninteractive: true\n\n# Cauldron API silently drops the `inputs` field in /api/conversations POST\n# bodies (see bdc-author-wo-batch.yaml line 40-49 for the anchor incident).\n# Engine ONLY injects defaults declared here as $INPUT_<NAME> env vars.\n# Override by editing default + re-firing, or by inlining flags in USER_MESSAGE.\ninputs:\n  MODE:\n    default: \"--queue\"\n  NOTION_DB_ID:\n    default: \"a6df831c-0b52-449f-8ca4-d77be6b70d0a\"\n\nnodes:\n\n  # Pre-flight: fail closed if container lacks GITHUB auth (WO-169, 2026-05-16).\n  # Copied verbatim from bdc-feature-development.yaml -- commit-spec is\n  # load_bearing so we need real git push capability before any work runs.\n  - id: preflight-github-auth\n    persona: major-build\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      if ! git push --dry-run origin \"HEAD:refs/heads/preflight-auth-probe-$(date +%s)\" 2>&1; then\n        echo \"ERROR: GITHUB auth check failed -- container cannot push to origin.\" >&2\n        echo \"Likely cause: GITHUB_TOKEN missing in executor env, or token lacks repo scope.\" >&2\n        exit 1\n      fi\n      echo \"preflight-github-auth PASS\"\n\n  # normalize-intake: single prompt node handles all four intake sources.\n  # See WO spec §8 + plan Ambiguity #2 resolution: prompt node, not bash, so\n  # that the --notion <page-id> path can use the Notion MCP tool without\n  # splitting into two nodes with fragile when: chains.\n  - id: normalize-intake\n    persona: xo\n    depends_on: [preflight-github-auth]\n    task_class: medium_work\n    prompt: |\n      You are the BDC WO Author intake normalizer. Your job is to detect the\n      intake source from USER_MESSAGE, fetch the underlying content if needed,\n      and emit a normalized description block that the downstream author-loop\n      consumes.\n\n      ## Inputs\n      USER_MESSAGE: $USER_MESSAGE\n      INPUT_MODE: $INPUT_MODE\n\n      ## Intake Detection (priority order)\n\n      1. **--spec <path>** in USER_MESSAGE: fetch via `gh api repos/bluedevilcollectibles/bdc-xo/contents/<path>?ref=main`,\n         base64-decode .content. intake_source=spec_file\n      2. **--notion <page-id>** in USER_MESSAGE: use Notion MCP `notion-fetch` against the page ID.\n         Extract title + body as the description. intake_source=notion_stub\n      3. **<owner>/<repo>#<N>** or just **<repo>#<N>** in USER_MESSAGE: fetch via\n         `gh api repos/<owner-or-default-org>/<repo>/issues/<N>` (default org:\n         bluedevilcollectibles). Use title + body. intake_source=github_issue\n      4. Otherwise: treat USER_MESSAGE itself as the description.\n         intake_source=chat_message\n\n      ## Mode Flag Extraction (USER_MESSAGE wins over INPUT_MODE default)\n\n      Scan USER_MESSAGE for: `--draft-only`, `--queue`, `--fire`. Last match wins.\n      If none present, use INPUT_MODE (default `--queue`).\n      Also scan for: `--auto-approve`, \"fire it\", \"fire now\". Set auto_approve=true if any present.\n\n      ## Target Repo Hint Extraction\n\n      Scan USER_MESSAGE and (if intake_source != chat_message) the fetched content\n      for any of: `lspro-react`, `shopops-storefront`, `shopops`, `bdc-harness`,\n      `bdc-xo`, `pricing-monolith`, `arena`. Emit the first match as target_repo_hint.\n      If none found, emit `target_repo_hint=unknown` -- the author-loop will infer\n      from the WO spec body's §10 Deploy Target.\n\n      ## WO ID Hint Extraction\n\n      If USER_MESSAGE or fetched content contains a `WO-[A-Z][A-Z0-9-]+-[0-9]+`\n      string, emit it as wo_id_hint. Otherwise emit `wo_id_hint=auto-generate`\n      and the author-loop will derive a deterministic ID from the Objective.\n\n      ## Required Output\n\n      Emit a structured block exactly in this format (no markdown fences):\n\n      ```\n      === NORMALIZE-INTAKE OUTPUT ===\n      intake_source=<chat_message|github_issue|notion_stub|spec_file>\n      mode=<--draft-only|--queue|--fire>\n      auto_approve=<true|false>\n      target_repo_hint=<repo-name-or-unknown>\n      wo_id_hint=<WO-ID-or-auto-generate>\n\n      === DESCRIPTION ===\n      <full normalized description text -- use the fetched issue/notion/spec body\n       in its entirety if applicable, or the raw USER_MESSAGE for chat intake>\n      ```\n\n      If any fetch fails (404 on issue, Notion page not accessible, etc.),\n      do NOT invent content. Output:\n\n      ```\n      INTAKE_FAILURE: <reason>\n      ```\n\n      and stop. Downstream nodes will halt.\n\n  # read-context: cross-repo fetch of prior art (existing WOs in same prefix\n  # family) so the author-loop can spot duplicates and reuse patterns.\n  - id: read-context\n    persona: xo\n    depends_on: [normalize-intake]\n    bash: |\n      set -euo pipefail\n      # Extract wo_id_hint from normalize-intake output. If present, find\n      # existing WOs that share the prefix family.\n      INTAKE_OUT=$normalize-intake.output\n      WO_HINT=$(printf '%s\\n' \"$INTAKE_OUT\" | sed -n 's/^wo_id_hint=//p' | head -n 1)\n      TARGET_HINT=$(printf '%s\\n' \"$INTAKE_OUT\" | sed -n 's/^target_repo_hint=//p' | head -n 1)\n\n      # Derive prefix: WO-HARNESS-FOO-BAR-01 -> HARNESS-FOO\n      PREFIX=\"\"\n      if [ -n \"$WO_HINT\" ] && [ \"$WO_HINT\" != \"auto-generate\" ]; then\n        PREFIX=$(printf '%s\\n' \"$WO_HINT\" | sed -E 's/^WO-([A-Z]+-[A-Z]+).*/\\1/')\n      elif [ -n \"$TARGET_HINT\" ] && [ \"$TARGET_HINT\" != \"unknown\" ]; then\n        # Map target repo to typical WO prefix\n        case \"$TARGET_HINT\" in\n          lspro-react) PREFIX=\"LSPRO\" ;;\n          shopops-storefront) PREFIX=\"STOREFRONT\" ;;\n          shopops) PREFIX=\"SHOPOPS\" ;;\n          bdc-harness) PREFIX=\"HARNESS\" ;;\n          bdc-xo) PREFIX=\"XO\" ;;\n          *) PREFIX=\"\" ;;\n        esac\n      fi\n\n      # List existing WOs in same prefix family (best-effort, never block).\n      # Cross-repo: bdc-xo holds all WO specs.\n      SIBLINGS=\"\"\n      if [ -n \"$PREFIX\" ]; then\n        SIBLINGS=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders?ref=main\" \\\n          --template '{{range .}}{{.name}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep -E \"^WO-${PREFIX}-\" | head -20 || true)\n      fi\n\n      echo \"=== READ-CONTEXT OUTPUT ===\"\n      echo \"prefix_family=${PREFIX:-unknown}\"\n      if [ -n \"$SIBLINGS\" ]; then\n        echo \"existing_siblings:\"\n        printf '%s\\n' \"$SIBLINGS\" | sed 's/^/  - /'\n      else\n        echo \"existing_siblings: (none found or no prefix)\"\n      fi\n\n      # Read wo-recipe.md doctrine reference if available (best-effort)\n      RECIPE_HEAD=$(gh api \"repos/bluedevilcollectibles/bdc-xo/contents/xo-wiki/wiki/doctrine/wo-recipe/_index.md?ref=main\" \\\n        --jq .content 2>/dev/null | base64 -d 2>/dev/null | head -60 || true)\n      if [ -n \"$RECIPE_HEAD\" ]; then\n        echo \"\"\n        echo \"=== WO-RECIPE.MD (head) ===\"\n        printf '%s\\n' \"$RECIPE_HEAD\"\n      fi\n\n  # author-loop: adversarial AUTHOR -> REVIEW -> FIX cycle.\n  # 12-section template + 12-check Validate.md gate are inlined here so the\n  # workflow does not depend on filesystem access to ~/.claude/skills/.\n  # Pattern source: bdc-author-wo-batch.yaml lines 165-310 (3-phase prompt).\n  - id: author-loop\n    persona: major-build\n    depends_on: [read-context]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: VALIDATE_PASS\n      max_iterations: 5\n      prompt: |\n        You are the BDC WO Author Pipeline, alternating between AUTHOR and\n        ADVERSARIAL REVIEWER roles. CWD = bdc-xo repo root. You have full\n        file read/write tool access.\n\n        ## Pipeline Inputs\n\n        ### Intake (normalized)\n        $normalize-intake.output\n\n        ### Prior Art Context\n        $read-context.output\n\n        ### Previous Iteration Results (empty on first iteration)\n        $LOOP_PREV_OUTPUT\n\n        ---\n\n        ## PHASE 1 -- AUTHOR\n\n        **1a. Idempotency check**: Parse wo_id_hint from intake. If it is a real\n        WO ID (not \"auto-generate\") AND `docs/work-orders/<wo_id>.md` already\n        exists on disk, SKIP authoring -- emit `IDEMPOTENT_SKIP: <wo_id> already\n        exists on disk` and proceed directly to PHASE 3 commit.\n\n        **1b. Derive WO_ID** (if hint was \"auto-generate\"):\n        - Extract a concise 2-3 word noun phrase from the Objective sentence.\n        - Pattern: `WO-<PREFIX>-<NOUN-PHRASE>-01` where PREFIX matches target\n          repo (LSPRO, STOREFRONT, SHOPOPS, HARNESS, XO, etc.).\n        - Example: \"Add pull-list export to ShopOps\" -> WO-SHOPOPS-PULLLIST-EXPORT-01\n        - Deterministic from Objective: running twice with same description\n          MUST produce same ID (idempotency req from spec §8 Q8).\n\n        **1c. Draft the 12-section WO spec** (wo-recipe.md template):\n        ```\n        ## 1. Objective\n        ## 2. Behavior Source of Truth   <-- must cite concrete file path or WO ID\n        ## 3. Prior Art Check\n        ## 4. System Context             <-- list dependencies or state \"standalone\"\n        ## 5. UI Hierarchy               <-- N/A with one-line reason if not UI\n        ## 6. Mode Behavior Matrix       <-- N/A with one-line reason if not multi-mode\n        ## 7. Backend Function Inventory <-- every function labeled EXISTING (file:line) or NEW\n        ## 8. Data Flow\n        ## 9. Database Schema References <-- N/A with one-line reason if no DB;\n                                            if DB: cite information_schema query OR migration file\n        ## 10. Deploy Target\n        ## 11. Test Scenarios            <-- >= 3 tests, each with Given/When/Then structure\n        ## 12. Stop Point                <-- CI-executable commands, Given/When/Then\n        ```\n        Add a \"Grep assertions\" section if the WO touches 2+ repos (Check 8A).\n        Reference `wo-recipe.md` somewhere in the body (Check 2).\n        Include 8 required questions answered somewhere in the body (Check 10):\n          1. What behavior exists AFTER this WO?\n          2. Where is the source of truth?\n          3. Who owns this system?\n          4. What existing logic is reused?\n          5. What is the exact schema? (N/A if no DB)\n          6. What MUST NEVER break? (invariants)\n          7. How does CI prove it works?\n          8. What happens if it runs twice? (idempotency)\n\n        ---\n\n        ## PHASE 2 -- ADVERSARIAL REVIEW (you are now a blocking reviewer)\n\n        Apply all 12 checks to the drafted spec:\n\n        **Check 1**: 8 always-required headers present verbatim:\n          ## 1. Objective, ## 2. Behavior Source of Truth, ## 3. Prior Art Check,\n          ## 7. Backend Function Inventory, ## 8. Data Flow, ## 10. Deploy Target,\n          ## 11. Test Scenarios, ## 12. Stop Point\n          -> BLOCK if any missing.\n\n        **Check 2**: Body contains the substring `wo-recipe.md`.\n          -> BLOCK if absent.\n\n        **Check 3**: §2 references at least one concrete artifact (file path or WO ID).\n          -> BLOCK if §2 contains only TBD, \"see body\", or empty text.\n\n        **Check 4**: WO ID matches `WO-[A-Z][A-Z0-9-]*-[0-9]+`; consistent across\n        filename, H1, and (planned) Notion title.\n          -> BLOCK if inconsistent.\n\n        **Check 5**: §2 cites a real path (e.g. `docs/plans/foo.md`, `WO-XYZ-01`).\n          -> BLOCK if §2 is vague without a concrete locator.\n\n        **Check 6**: Every function/route/script in §7 labeled `EXISTING (file:line)` or `NEW`.\n          -> BLOCK if any name appears without a label.\n\n        **Check 7**: §9 column/table claims cite `information_schema` query result\n        or migration file path.\n          -> BLOCK if §9 makes a column claim without citing source.\n\n        **Check 8**: §11 has >= 3 test scenarios each with Given/When/Then structure.\n          -> BLOCK if fewer than 3, or any test is purely descriptive.\n\n        **Check 8A**: If WO's files span 2+ repos -> must have `Grep assertions`\n        block with grep command per repo.\n          -> BLOCK if split-repo WO lacks grep assertions.\n\n        **Check 9**: §12 stop conditions are CI-executable commands (Given/When/Then).\n          -> BLOCK if any stop condition is vague (\"staging accessible\").\n\n        **Check 10**: 8 required questions addressed somewhere in body (see PHASE 1c).\n          -> BLOCK if any unanswered.\n\n        **Check 11** (HARDEST -- anti-guess hardening): Every component name, table\n        name, column name, route path in the spec traces to a source file. No\n        invented names. If §7 lists `fetchPullList`, the spec must label it\n        `EXISTING (lib/storefront-api.ts:142)` or `NEW`.\n          -> BLOCK if any specific claim is not anchored to a real source.\n\n        **Check 12**: §4 lists dependencies OR explicitly says \"standalone\".\n          -> BLOCK if §4 is empty and WO touches more than one subsystem.\n\n        Output per WO: `VALIDATE: PASS` (all 12) or\n        `VALIDATE: FAIL -- Check N: <gap>` for each failure.\n\n        ---\n\n        ## PHASE 3 -- FIX OR COMMIT\n\n        - **FAIL**: Revise spec to address each listed gap. Re-run all 12 checks\n          on the revision. If revised passes -> write file; if still fails ->\n          continue to next iteration.\n        - **PASS** or **IDEMPOTENT_SKIP**: Write `docs/work-orders/<WO_ID>.md` to disk.\n          Also emit a footer block (parsed by downstream dry-run-preview):\n          ```\n          === AUTHOR-LOOP RESULT ===\n          wo_id=<WO_ID>\n          spec_path=docs/work-orders/<WO_ID>.md\n          target_repo=<owner/repo from §10 Deploy Target>\n          mode=<--draft-only|--queue|--fire from intake>\n          auto_approve=<true|false from intake>\n          ```\n\n        ---\n\n        ## SENTINEL RULE\n\n        Output `<promise>VALIDATE_PASS</promise>` ONLY when:\n        - `docs/work-orders/<wo_id>.md` exists on disk (or IDEMPOTENT_SKIP fired)\n        - The AUTHOR-LOOP RESULT footer block is present in your output\n        - Final review verdict was VALIDATE: PASS (or IDEMPOTENT_SKIP)\n\n        If iteration 5 ends without passing, do NOT emit the sentinel.\n        Instead output a BATCH_FAILURE block:\n        ```\n        BATCH_FAILURE:\n          WO_ID: <id or \"(unassigned)\">\n          failed_checks: [list of Check N]\n          critique: <specific gaps that could not be resolved>\n        ```\n\n  # dry-run-preview: ALWAYS runs. Emits the preview block that operator sees\n  # before any Notion create or Cauldron fire. Cross-checks: existing spec on\n  # disk (idempotency), existing Notion page (idempotency), active Cauldron\n  # runs for same WO_ID (duplicate run detection per spec Scenario 3).\n  - id: dry-run-preview\n    persona: major-build\n    depends_on: [author-loop]\n    bash: |\n      set -euo pipefail\n      AUTHOR_OUT=$author-loop.output\n\n      # Parse footer block from author-loop output. Author-loop emits these\n      # under \"=== AUTHOR-LOOP RESULT ===\" header.\n      WO_ID=$(printf '%s\\n' \"$AUTHOR_OUT\" | sed -n 's/^wo_id=//p' | head -n 1)\n      SPEC_PATH=$(printf '%s\\n' \"$AUTHOR_OUT\" | sed -n 's/^spec_path=//p' | head -n 1)\n      TARGET_REPO=$(printf '%s\\n' \"$AUTHOR_OUT\" | sed -n 's/^target_repo=//p' | head -n 1)\n      MODE=$(printf '%s\\n' \"$AUTHOR_OUT\" | sed -n 's/^mode=//p' | head -n 1)\n      AUTO_APPROVE=$(printf '%s\\n' \"$AUTHOR_OUT\" | sed -n 's/^auto_approve=//p' | head -n 1)\n\n      if [ -z \"$WO_ID\" ]; then\n        # Check for BATCH_FAILURE first; that is a clean exit upstream signal.\n        if printf '%s\\n' \"$AUTHOR_OUT\" | grep -q '^BATCH_FAILURE:'; then\n          echo \"DRY_RUN_PREVIEW: skipped (author-loop emitted BATCH_FAILURE)\"\n          exit 0\n        fi\n        echo \"ERROR: author-loop output missing wo_id footer; cannot build preview.\" >&2\n        exit 1\n      fi\n\n      # Cross-check 1: spec file exists on disk (it should -- author-loop wrote it)\n      SPEC_OK=0\n      [ -f \"$SPEC_PATH\" ] && SPEC_OK=1\n\n      # Cross-check 2: any existing Cauldron workflow run for this WO_ID?\n      # Best-effort GET against the Cauldron API. If unreachable, do NOT block --\n      # emit \"duplicate_check=skipped\" so the approval gate can still proceed.\n      DUPLICATE_INFO=\"duplicate_check=skipped (Cauldron API unreachable)\"\n      if RUNS_JSON=$(curl -fsS --max-time 10 \\\n          \"https://archon.bluedevilcollectibles.com/api/workflows/runs?limit=20\" 2>/dev/null); then\n        # jq filter: walk top-level array or .runs[], find first whose\n        # initial_message|message contains $WO_ID AND status is non-terminal.\n        # Output format: <id>|<status>|<started_at> or empty.\n        ACTIVE_RUN=$(printf '%s\\n' \"$RUNS_JSON\" | jq -r --arg wo \"$WO_ID\" '\n          (if type == \"array\" then . else (.runs // []) end)\n          | map(select(((.initial_message // .message // \"\") | contains($wo))\n                       and (.status == \"running\" or .status == \"pending\" or .status == \"queued\")))\n          | if length > 0 then (.[0] | \"\\(.id // \"unknown\")|\\(.status)|\\(.started_at // \"?\")\") else \"\" end\n        ' 2>/dev/null || true)\n        if [ -n \"$ACTIVE_RUN\" ]; then\n          DUPLICATE_INFO=\"DUPLICATE_RUN_DETECTED: $ACTIVE_RUN\"\n        else\n          DUPLICATE_INFO=\"duplicate_check=clean (no active runs for $WO_ID)\"\n        fi\n      fi\n\n      # Emit preview block (always shown -- operator gate before Notion/fire)\n      echo \"=== DRY-RUN PREVIEW ===\"\n      echo \"wo_id=$WO_ID\"\n      echo \"spec_path=$SPEC_PATH\"\n      echo \"spec_on_disk=$SPEC_OK\"\n      echo \"target_repo=$TARGET_REPO\"\n      echo \"mode=$MODE\"\n      echo \"auto_approve=$AUTO_APPROVE\"\n      echo \"fire_workflow=bdc-feature-development\"\n      echo \"notion_db=${INPUT_NOTION_DB_ID:-a6df831c-0b52-449f-8ca4-d77be6b70d0a}\"\n      echo \"$DUPLICATE_INFO\"\n      echo \"\"\n      echo \"Mode behavior:\"\n      case \"$MODE\" in\n        --draft-only)\n          echo \"  - Commit spec to bdc-xo feature branch + open PR\"\n          echo \"  - NO Notion entry created\"\n          echo \"  - NO Cauldron fire\"\n          ;;\n        --queue)\n          echo \"  - Commit spec to bdc-xo feature branch + open PR\"\n          echo \"  - Notion entry created (Claude Status=QUEUED)\"\n          echo \"  - NO Cauldron fire (operator fires separately)\"\n          ;;\n        --fire)\n          echo \"  - Commit spec to bdc-xo feature branch + open PR\"\n          echo \"  - Notion entry created (Claude Status=QUEUED)\"\n          if [ \"$AUTO_APPROVE\" = \"true\" ]; then\n            echo \"  - Cauldron fire IMMEDIATE (auto-approve flag detected)\"\n          else\n            echo \"  - Cauldron fire PENDING operator approval (next node)\"\n          fi\n          ;;\n        *)\n          echo \"  - WARN: unknown mode '$MODE' -- defaulting to --queue behavior\"\n          ;;\n      esac\n\n  # approval-gate: only runs in --fire mode AND when auto_approve != true.\n  # In other modes, the `when:` condition skips this node and downstream\n  # nodes with trigger_rule: all_done still execute.\n  - id: approval-gate\n    persona: xo\n    depends_on: [dry-run-preview]\n    when: \"$dry-run-preview.output =~ 'mode=--fire' AND $dry-run-preview.output =~ 'auto_approve=false'\"\n    approval:\n      message: |\n        Review the dry-run preview above. Fire this WO through bdc-feature-development?\n\n        - \"approved\" / \"yes\" / \"fire\" -> proceed to fire-cauldron\n        - \"no\" / \"reject\" -> halt at --queue state (spec + Notion only)\n        - If DUPLICATE_RUN_DETECTED is in the preview, type \"fire anyway\" to override\n      capture_response: true\n\n  # commit-spec: ALWAYS runs (depends only on dry-run-preview, not approval-gate).\n  # Spec commits regardless of mode -- mode only controls Notion + Cauldron fire.\n  # load_bearing: true (same rationale as bdc-author-wo-batch commit-and-push).\n  # Pattern: branch name single-WO (wo/author-<WO_ID>-<timestamp>), 3-attempt\n  # push retry with exponential backoff, host-artifact bundle save BEFORE push.\n  - id: commit-spec\n    persona: overseer\n    depends_on: [dry-run-preview]\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      PREVIEW=$dry-run-preview.output\n      WO_ID=$(printf '%s\\n' \"$PREVIEW\" | sed -n 's/^wo_id=//p' | head -n 1)\n      SPEC_PATH=$(printf '%s\\n' \"$PREVIEW\" | sed -n 's/^spec_path=//p' | head -n 1)\n\n      if [ -z \"$WO_ID\" ] || [ -z \"$SPEC_PATH\" ]; then\n        # author-loop hit BATCH_FAILURE; clean exit, no commit.\n        echo \"commit-spec: no WO to commit (preview did not produce wo_id)\"\n        exit 0\n      fi\n\n      if [ ! -f \"$SPEC_PATH\" ]; then\n        echo \"ERROR: spec file $SPEC_PATH does not exist on disk -- author-loop did not write it.\" >&2\n        exit 1\n      fi\n\n      TIMESTAMP=$(date +%Y%m%d%H%M%S)\n      BRANCH=\"wo/author-${WO_ID}-${TIMESTAMP}\"\n\n      # WO-168 Tier 1: save load-bearing artifacts to host-mounted volume BEFORE\n      # any remote operation. Copied from bdc-author-wo-batch.yaml.\n      save_bundle_to_host() {\n        local run_id=\"${WORKFLOW_RUN_ID:-${WORKFLOW_ID:-unknown-$(date +%Y%m%d%H%M%S)}}\"\n        local dest=\"/host-artifacts/${run_id}\"\n        if [ ! -d /host-artifacts ]; then\n          echo \"WARN: /host-artifacts not mounted -- skipping bundle save (recovery from worktree only)\" >&2\n          return 0\n        fi\n        mkdir -p \"$dest\" 2>/dev/null || {\n          echo \"WARN: could not mkdir $dest -- skipping bundle save\" >&2\n          return 0\n        }\n        if git bundle create \"$dest/branch.bundle\" HEAD 2>\"$dest/bundle.err\"; then\n          echo \"  host-artifact: bundle saved $dest/branch.bundle ($(stat -c%s \"$dest/branch.bundle\" 2>/dev/null || echo '?') bytes)\"\n        else\n          echo \"WARN: git bundle create failed -- see $dest/bundle.err\" >&2\n        fi\n        git diff --stat HEAD~1 HEAD > \"$dest/changes.txt\" 2>/dev/null \\\n          || git log -1 --name-status HEAD > \"$dest/changes.txt\" 2>/dev/null \\\n          || echo \"(no diff/log available)\" > \"$dest/changes.txt\"\n        {\n          echo \"branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo unknown)\"\n          echo \"sha=$(git rev-parse HEAD 2>/dev/null || echo unknown)\"\n          echo \"saved_at=$(date -u +%Y-%m-%dT%H:%M:%SZ)\"\n          echo \"workflow_run_id=${run_id}\"\n          echo \"wo_id=${WO_ID}\"\n          echo \"recovery_cmd=git fetch ${dest}/branch.bundle main:rescue-${run_id}\"\n        } > \"$dest/MANIFEST.txt\"\n        echo \"  host-artifact: manifest written $dest/MANIFEST.txt\"\n      }\n\n      git checkout -b \"$BRANCH\"\n      git add -- \"$SPEC_PATH\"\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes (spec was already committed in a prior run)\"\n        echo \"BRANCH=$(git rev-parse --abbrev-ref HEAD)\"\n        echo \"SHA=$(git rev-parse HEAD)\"\n        echo \"wo_id=$WO_ID\"\n        echo \"spec_path=$SPEC_PATH\"\n        exit 0\n      fi\n\n      git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n        commit -m \"feat(wo-author): ${WO_ID} -- authored via bdc-wo-author\n\n      Spec drafted via bdc-wo-author adversarial loop (Author.md 12-section template +\n      Validate.md 12-check gate). Operator reviews then promotes via Notion + Cauldron.\n\n      Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>\"\n\n      # Bundle BEFORE push so a push failure does not destroy the spec.\n      save_bundle_to_host\n\n      # WO-169: real retry guard. 3 attempts, 5s/10s/15s backoff, fail closed.\n      PUSH_OK=false\n      for attempt in 1 2 3; do\n        if git push -u origin \"$BRANCH\" 2>&1; then\n          PUSH_OK=true\n          break\n        fi\n        echo \"WARN: push attempt $attempt failed; retrying after $((attempt * 5))s...\" >&2\n        sleep $((attempt * 5))\n      done\n      if [ \"$PUSH_OK\" = \"false\" ]; then\n        echo \"ERROR: git push failed after 3 attempts -- aborting workflow (load_bearing node).\" >&2\n        exit 1\n      fi\n      echo \"BRANCH=$BRANCH\"\n      echo \"SHA=$(git rev-parse HEAD)\"\n      echo \"wo_id=$WO_ID\"\n      echo \"spec_path=$SPEC_PATH\"\n\n  # file-notion: creates Notion page (unless --draft-only). Uses trigger_rule:\n  # all_done so the workflow still proceeds when approval-gate is skipped.\n  - id: file-notion\n    persona: xo\n    depends_on: [commit-spec]\n    when: \"$dry-run-preview.output !~ 'mode=--draft-only'\"\n    task_class: clerical\n    prompt: |\n      File the authored WO to Notion.\n\n      ## Inputs\n      Dry-run preview: $dry-run-preview.output\n      Commit-spec output: $commit-spec.output\n      Author-loop output (for spec body): $author-loop.output\n\n      ## Idempotency Check\n      Before creating, search Notion DB `${INPUT_NOTION_DB_ID:-a6df831c-0b52-449f-8ca4-d77be6b70d0a}`\n      for an existing page with WO ID matching the wo_id from commit-spec output.\n      If found, output `IDEMPOTENT_SKIP: page already exists at <existing_url>` and\n      do NOT create a duplicate.\n\n      ## Notion Filing (when no duplicate found)\n      1. Read the spec file at `spec_path` (from commit-spec output) to extract:\n         - §1 Objective text (first 300 chars for Notes summary)\n         - Target repo from §10 Deploy Target (for Project field)\n      2. Use the Notion MCP `notion-create-pages` tool to create a page in\n         database `${INPUT_NOTION_DB_ID:-a6df831c-0b52-449f-8ca4-d77be6b70d0a}` with:\n         - Task: <wo_id>\n         - WO ID: <wo_id>\n         - Status: To Do\n         - Claude Status: QUEUED\n         - Priority: P1 (default)\n         - Builder: Major Build (default)\n         - Execution State: ACTIVE\n         - Project: <target_repo> (or \"Infrastructure\" if unknown)\n         - Notes: first 300 chars of §1 Objective +\n                  \" WO file: BDC_XO/<spec_path>\" +\n                  \" Authored by bdc-wo-author.\"\n      3. Output `NOTION_URL=<url>` on the created page.\n\n      If Notion MCP is unavailable, output:\n        XO_TODO: File <wo_id> to Notion manually (DB ${INPUT_NOTION_DB_ID}).\n                 Status=To Do, Claude Status=QUEUED, Builder=Major Build.\n\n  # fire-cauldron: only runs in --fire mode AND only after approval-gate\n  # returned approval (or auto_approve=true skipped the gate).\n  # Per WO spec Ambiguity #4 resolution: fail-closed if ARCHON_OPERATOR_TOKEN\n  # is missing from executor env.\n  - id: fire-cauldron\n    persona: xo\n    depends_on: [commit-spec, approval-gate]\n    when: \"$dry-run-preview.output =~ 'mode=--fire'\"\n    trigger_rule: all_done\n    bash: |\n      set -euo pipefail\n      PREVIEW=$dry-run-preview.output\n      APPROVAL=$approval-gate.output\n\n      MODE=$(printf '%s\\n' \"$PREVIEW\" | sed -n 's/^mode=//p' | head -n 1)\n      AUTO_APPROVE=$(printf '%s\\n' \"$PREVIEW\" | sed -n 's/^auto_approve=//p' | head -n 1)\n      WO_ID=$(printf '%s\\n' \"$PREVIEW\" | sed -n 's/^wo_id=//p' | head -n 1)\n      TARGET_REPO=$(printf '%s\\n' \"$PREVIEW\" | sed -n 's/^target_repo=//p' | head -n 1)\n      DUP_LINE=$(printf '%s\\n' \"$PREVIEW\" | grep -E '^(DUPLICATE_RUN_DETECTED|duplicate_check)' | head -n 1 || true)\n\n      # Guard 1: mode must be --fire (when: should have skipped us if not)\n      if [ \"$MODE\" != \"--fire\" ]; then\n        echo \"fire-cauldron: mode=$MODE is not --fire; skipping\"\n        exit 0\n      fi\n\n      # Guard 2: approval required (unless auto_approve)\n      if [ \"$AUTO_APPROVE\" != \"true\" ]; then\n        if ! printf '%s\\n' \"$APPROVAL\" | grep -iqE 'approved|^yes|fire'; then\n          echo \"fire-cauldron: approval not granted (got: $APPROVAL) -- halting at --queue state\"\n          exit 0\n        fi\n        # If duplicate detected, require explicit \"fire anyway\" in approval\n        if printf '%s\\n' \"$DUP_LINE\" | grep -q 'DUPLICATE_RUN_DETECTED'; then\n          if ! printf '%s\\n' \"$APPROVAL\" | grep -iq 'fire anyway'; then\n            echo \"fire-cauldron: DUPLICATE_RUN_DETECTED and operator did not type 'fire anyway' -- halting\" >&2\n            exit 1\n          fi\n          echo \"fire-cauldron: operator overrode duplicate run detection with 'fire anyway'\"\n        fi\n      fi\n\n      # Guard 3: required inputs\n      if [ -z \"$WO_ID\" ] || [ -z \"$TARGET_REPO\" ]; then\n        echo \"ERROR: missing WO_ID or TARGET_REPO from preview\" >&2\n        exit 1\n      fi\n\n      # Guard 4: ARCHON_OPERATOR_TOKEN must be in executor env (Ambiguity #4).\n      if [ -z \"${ARCHON_OPERATOR_TOKEN:-}\" ]; then\n        echo \"ERROR: ARCHON_OPERATOR_TOKEN not set in executor env.\" >&2\n        echo \"Fire path requires authenticated Cauldron API call. Add the secret\" >&2\n        echo \"to the harness container env and retry.\" >&2\n        exit 1\n      fi\n\n      # POST to Cauldron conversations endpoint with the execution message.\n      # This pattern matches bdc-doctrine-update post-completed n8n call style\n      # for content-type + auth header.\n      RESPONSE=$(curl -fsS -X POST \\\n        \"https://archon.bluedevilcollectibles.com/api/conversations\" \\\n        -H \"Content-Type: application/json\" \\\n        -H \"x-archon-operator-token: ${ARCHON_OPERATOR_TOKEN}\" \\\n        --max-time 30 \\\n        -d \"{\\\"message\\\": \\\"Execute ${WO_ID} --project ${TARGET_REPO}\\\"}\" \\\n        2>&1)\n\n      # jq extracts whichever id field the API returned (id, conversation_id, run_id);\n      # falls back to \"parse_failed\" if the body is not JSON or has none of those keys.\n      RUN_ID=$(printf '%s\\n' \"$RESPONSE\" \\\n        | jq -r '.id // .conversation_id // .run_id // \"unknown\"' 2>/dev/null \\\n        || echo \"parse_failed\")\n\n      echo \"FIRE_OK=true\"\n      echo \"WO_ID=$WO_ID\"\n      echo \"TARGET_REPO=$TARGET_REPO\"\n      echo \"RUN_ID=$RUN_ID\"\n      echo \"RESPONSE=$RESPONSE\"\n\n  # build-manifest: trigger_rule all_done so skipped file-notion/fire-cauldron\n  # in non-applicable modes do not block manifest emission.\n  - id: build-manifest\n    persona: major-build\n    depends_on: [commit-spec, file-notion, fire-cauldron]\n    trigger_rule: all_done\n    task_class: clerical\n    prompt: |\n      Build the Captain CI completion manifest for WO-HARNESS-WO-AUTHOR-WORKFLOW-01\n      and the WO that was just authored.\n\n      ## Inputs\n      Normalize-intake: $normalize-intake.output\n      Read-context: $read-context.output\n      Author-loop: $author-loop.output\n      Dry-run preview: $dry-run-preview.output\n      Approval-gate: $approval-gate.output\n      Commit-spec: $commit-spec.output\n      File-notion: $file-notion.output\n      Fire-cauldron: $fire-cauldron.output\n\n      ## Required Manifest Fields\n      - WO: <wo_id from commit-spec, or \"BATCH_FAILURE\" if author-loop failed>\n      - Mode: <--draft-only | --queue | --fire>\n      - Intake source: <chat_message | github_issue | notion_stub | spec_file>\n      - Files created: docs/work-orders/<WO_ID>.md (only one file -- the spec)\n      - Files modified: none\n      - Branch: <BRANCH from commit-spec>\n      - Commit SHA: <SHA from commit-spec>\n      - Notion URL: <NOTION_URL from file-notion, or \"skipped (--draft-only)\">\n      - Cauldron run ID: <RUN_ID from fire-cauldron, or \"skipped (not --fire)\">\n      - Tests: N/A (authoring workflow -- Validate.md 12-check gate IS the test;\n               PR-time codex-review + claude-review catch slips)\n      - Grep assertions (run these against the harness YAML and authored spec):\n        - bdc-harness: count of workflow name string in this YAML -> >= 1\n        - bdc-harness: count of `^model[:][ ]sonnet$` (regex, root-level only) -> 1\n        - bdc-harness: count of `^policyFile[:]` (regex, root-level only) -> 1\n        - bdc-xo: gh api repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/<WO_ID>.md --jq .size -> > 0\n      - VALIDATION: PASS\n\n      Run summary:\n        Adversarial review iterations: <count from $author-loop.output>\n        Validate.md verdict: PASS | BATCH_FAILURE\n        Duplicate run check: <line from dry-run-preview>\n\n  # flip-notion: flips THIS WO (WO-HARNESS-WO-AUTHOR-WORKFLOW-01) to REVIEW.\n  # Note: this is the META WO -- the WO that built the workflow -- not the WO\n  # that was just authored. The authored WO goes to QUEUED via file-notion.\n  - id: flip-notion\n    persona: xo\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip the META Work Order (WO-HARNESS-WO-AUTHOR-WORKFLOW-01 -- the one that\n      built THIS workflow file) to REVIEW with manifest in Notes property.\n\n      DO NOT flip the WO that was just authored -- that one is at QUEUED (set by\n      file-notion in this same run).\n\n      Required Notion changes for WO-HARNESS-WO-AUTHOR-WORKFLOW-01:\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> $build-manifest.output verbatim\n      - claimed_by: Major Build (bdc-wo-author)\n      - claimed_at: today's date\n\n      If Notion MCP unavailable, output:\n        XO_TODO: Flip WO-HARNESS-WO-AUTHOR-WORKFLOW-01 to REVIEW; paste manifest into Notes.\n\n      Manifest:\n      $build-manifest.output\n",
  "bdc-wo-grader": "# Drafted for WO-HARNESS-WO-GRADER-WORKFLOW-01.\n# Purpose: close the autonomous WO lifecycle by grading REVIEW WOs against\n# their Notes-field manifest and flipping them to DONE or back to IN_PROGRESS.\n#\n# Fire manually:\n#   WO_PAGE_ID=<notion-page-id> WO_ID=<wo-id> bun run cli workflow run bdc-wo-grader --no-worktree \"grade WO\"\n#\n# Fire by cron:\n#   scripts/trigger-wo-grader.sh\n\nname: bdc-wo-grader\ndescription: |\n  Use when: A Blue Devil Work Order is at REVIEW and needs Captain-CI-style closeout.\n  Inputs:\n    WO_PAGE_ID: Notion page id for the Work Order. Preferred.\n    WO_ID: Work Order id. Used to query Notion when WO_PAGE_ID is omitted.\n    NOTION_DB_ID: optional override; defaults to the Blue Devil WO database.\n    DRY_RUN: true means read and grade only; do not mutate Notion.\n  Does: Reads the Notes-field manifest, verifies PR state, grep assertions, runtime evidence,\n        and Vercel evidence, then flips REVIEW to DONE or back to IN_PROGRESS with critique.\n  Repo: normally fired from bluedevilcollectibles/bdc-harness.\n  parallel_ok: true\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n  - id: read-wo\n    bash: |\n      set -euo pipefail\n      NOTION_DB_ID=\"${NOTION_DB_ID:-a6df831c-0b52-449f-8ca4-d77be6b70d0a}\"\n      WO_PAGE_ID=\"${WO_PAGE_ID:-}\"\n      WO_ID=\"${WO_ID:-}\"\n      DRY_RUN=\"${DRY_RUN:-false}\"\n\n      if [ -z \"${NOTION_API_KEY:-}\" ]; then\n        echo \"ERROR: NOTION_API_KEY is required for bdc-wo-grader\" >&2\n        exit 1\n      fi\n\n      mkdir -p .archon-wo-grader\n\n      python3 - \"$NOTION_DB_ID\" \"$WO_PAGE_ID\" \"$WO_ID\" \"$DRY_RUN\" <<'PY'\n      import json, os, sys, urllib.request\n\n      db_id, page_id, wo_id, dry_run = sys.argv[1:5]\n      token = os.environ[\"NOTION_API_KEY\"]\n      headers = {\n          \"Authorization\": f\"Bearer {token}\",\n          \"Notion-Version\": \"2022-06-28\",\n          \"Content-Type\": \"application/json\",\n      }\n\n      def notion(method, url, payload=None):\n          data = json.dumps(payload).encode() if payload is not None else None\n          req = urllib.request.Request(url, data=data, headers=headers, method=method)\n          with urllib.request.urlopen(req, timeout=30) as resp:\n              return json.loads(resp.read().decode())\n\n      if not page_id:\n          if not wo_id:\n              raise SystemExit(\"ERROR: WO_PAGE_ID or WO_ID is required\")\n          payload = {\n              \"page_size\": 5,\n              \"filter\": {\"property\": \"WO ID\", \"rich_text\": {\"contains\": wo_id}},\n          }\n          result = notion(\"POST\", f\"https://api.notion.com/v1/databases/{db_id}/query\", payload)\n          rows = result.get(\"results\", [])\n          if not rows:\n              raise SystemExit(f\"ERROR: No Notion WO found for WO_ID={wo_id}\")\n          page_id = rows[0][\"id\"]\n\n      page = notion(\"GET\", f\"https://api.notion.com/v1/pages/{page_id}\")\n      props = page.get(\"properties\", {})\n\n      def text_prop(name):\n          prop = props.get(name, {})\n          chunks = prop.get(prop.get(\"type\", \"\"), []) if prop else []\n          return \"\".join(c.get(\"plain_text\", \"\") for c in chunks)\n\n      def select_name(name):\n          prop = props.get(name, {})\n          typ = prop.get(\"type\")\n          val = prop.get(typ) if typ else None\n          return (val or {}).get(\"name\")\n\n      notes = text_prop(\"Notes\")\n      title = text_prop(\"Task\")\n      row_wo_id = text_prop(\"WO ID\") or wo_id or title\n      out = {\n          \"page_id\": page_id,\n          \"wo_id\": row_wo_id,\n          \"title\": title,\n          \"claude_status\": select_name(\"Claude Status\"),\n          \"status\": select_name(\"Status\"),\n          \"builder\": select_name(\"Builder\"),\n          \"project\": select_name(\"Project\"),\n          \"priority\": select_name(\"Priority\"),\n          \"notes\": notes,\n          \"dry_run\": dry_run,\n      }\n      with open(\".archon-wo-grader/wo.json\", \"w\", encoding=\"utf-8\") as f:\n          json.dump(out, f, indent=2)\n      print(f\"WO_PAGE_ID={page_id}\")\n      print(f\"WO_ID={row_wo_id}\")\n      print(f\"CLAUDE_STATUS={out['claude_status']}\")\n      print(f\"STATUS={out['status']}\")\n      print(f\"NOTES_BYTES={len(notes.encode('utf-8'))}\")\n      PY\n\n  - id: parse-manifest\n    depends_on: [read-wo]\n    bash: |\n      set -euo pipefail\n      python3 <<'PY'\n      import json, re\n\n      wo = json.load(open(\".archon-wo-grader/wo.json\", encoding=\"utf-8\"))\n      notes = wo.get(\"notes\", \"\")\n\n      section_names = [\n          \"WO\", \"Builder\", \"Files created\", \"Files modified\", \"Tests\", \"PRs\",\n          \"Merge ancestors\", \"Grep assertions\", \"Runtime verification\",\n          \"Vercel deployment\", \"VALIDATION\", \"Git commits\"\n      ]\n\n      found = {}\n      current = None\n      for raw in notes.splitlines():\n          line = raw.rstrip()\n          m = re.match(r\"^([A-Za-z][A-Za-z ]+):\\s*(.*)$\", line)\n          if m and m.group(1) in section_names:\n              current = m.group(1)\n              found.setdefault(current, [])\n              if m.group(2):\n                  found[current].append(m.group(2))\n              continue\n          if current:\n              found[current].append(line)\n\n      pr_urls = re.findall(r\"https://github\\.com/[^/\\s]+/[^/\\s]+/pull/\\d+\", notes)\n      required = [\n          \"WO\", \"Builder\", \"Tests\", \"PRs\", \"Merge ancestors\", \"Grep assertions\",\n          \"Runtime verification\", \"Vercel deployment\", \"VALIDATION\"\n      ]\n      missing = [s for s in required if s not in found]\n      v2_ok = not missing\n\n      manifest = {\n          \"wo_id\": wo.get(\"wo_id\"),\n          \"sections\": found,\n          \"missing_sections\": missing,\n          \"pr_urls\": sorted(set(pr_urls)),\n          \"v2_ok\": v2_ok,\n      }\n      with open(\".archon-wo-grader/manifest.json\", \"w\", encoding=\"utf-8\") as f:\n          json.dump(manifest, f, indent=2)\n\n      print(f\"MANIFEST_V2_OK={str(v2_ok).lower()}\")\n      print(\"MISSING_SECTIONS=\" + \",\".join(missing))\n      print(f\"PR_COUNT={len(manifest['pr_urls'])}\")\n      for url in manifest[\"pr_urls\"]:\n          print(f\"PR_URL={url}\")\n      PY\n\n  - id: verify-pr-state\n    depends_on: [parse-manifest]\n    bash: |\n      set -euo pipefail\n      python3 <<'PY' > .archon-wo-grader/pr-urls.txt\n      import json\n      m = json.load(open(\".archon-wo-grader/manifest.json\", encoding=\"utf-8\"))\n      for url in m.get(\"pr_urls\", []):\n          print(url)\n      PY\n\n      : > .archon-wo-grader/pr-state.txt\n      FAILED=0\n      COUNT=0\n      while IFS= read -r url; do\n        [ -z \"$url\" ] && continue\n        COUNT=$((COUNT + 1))\n        repo=$(printf '%s\\n' \"$url\" | awk -F/ '{print $4 \"/\" $5}')\n        num=$(printf '%s\\n' \"$url\" | awk -F/ '{print $7}')\n        echo \"Checking PR $repo#$num\" | tee -a .archon-wo-grader/pr-state.txt\n        if ! json=$(gh pr view \"$num\" --repo \"$repo\" --json state,mergedAt,mergeCommit,headRefName,baseRefName,url 2>&1); then\n          echo \"FAIL: gh pr view failed for $url: $json\" | tee -a .archon-wo-grader/pr-state.txt\n          FAILED=$((FAILED + 1))\n          continue\n        fi\n        printf '%s\\n' \"$json\" >> .archon-wo-grader/pr-state.txt\n        state=$(printf '%s\\n' \"$json\" | python3 -c 'import json,sys; print(json.load(sys.stdin).get(\"state\",\"\"))')\n        merged_at=$(printf '%s\\n' \"$json\" | python3 -c 'import json,sys; print(json.load(sys.stdin).get(\"mergedAt\") or \"\")')\n        if [ \"$state\" != \"MERGED\" ] || [ -z \"$merged_at\" ]; then\n          echo \"FAIL: PR not merged: $url state=$state mergedAt=$merged_at\" | tee -a .archon-wo-grader/pr-state.txt\n          FAILED=$((FAILED + 1))\n        else\n          echo \"PASS: PR merged: $url\" | tee -a .archon-wo-grader/pr-state.txt\n        fi\n      done < .archon-wo-grader/pr-urls.txt\n\n      echo \"PR_CHECKS=$COUNT FAILED=$FAILED\"\n      [ \"$FAILED\" -eq 0 ] || exit 1\n\n  - id: verify-grep-assertions\n    depends_on: [parse-manifest]\n    bash: |\n      set -euo pipefail\n      python3 <<'PY'\n      import json, re\n      m = json.load(open(\".archon-wo-grader/manifest.json\", encoding=\"utf-8\"))\n      lines = m.get(\"sections\", {}).get(\"Grep assertions\", [])\n      assertions = []\n      for line in lines:\n          s = line.strip()\n          if not s.startswith(\"-\"):\n              continue\n          assertions.append(s[1:].strip())\n      out = {\"count\": len(assertions), \"assertions\": assertions}\n      with open(\".archon-wo-grader/grep-assertions.json\", \"w\", encoding=\"utf-8\") as f:\n          json.dump(out, f, indent=2)\n      print(f\"GREP_ASSERTION_COUNT={len(assertions)}\")\n      for a in assertions:\n          print(\"ASSERTION=\" + a)\n      if not assertions:\n          raise SystemExit(\"ERROR: Grep assertions section has no executable assertions\")\n      PY\n\n      # This first version records all assertions and lets the grader-loop judge\n      # exact command safety. Arbitrary command execution from Notion is not allowed.\n      echo \"GREP_ASSERTIONS_CAPTURED=true\"\n\n  - id: verify-runtime\n    depends_on: [parse-manifest]\n    bash: |\n      set -euo pipefail\n      python3 <<'PY'\n      import json\n      m = json.load(open(\".archon-wo-grader/manifest.json\", encoding=\"utf-8\"))\n      runtime = \"\\n\".join(m.get(\"sections\", {}).get(\"Runtime verification\", [])).strip()\n      result = {\n          \"runtime_text\": runtime,\n          \"status\": \"present\" if runtime else \"missing\",\n          \"na\": runtime.upper().startswith(\"N/A\"),\n      }\n      with open(\".archon-wo-grader/runtime.json\", \"w\", encoding=\"utf-8\") as f:\n          json.dump(result, f, indent=2)\n      print(f\"RUNTIME_STATUS={result['status']}\")\n      print(f\"RUNTIME_NA={str(result['na']).lower()}\")\n      PY\n\n  - id: verify-vercel\n    depends_on: [parse-manifest]\n    bash: |\n      set -euo pipefail\n      python3 <<'PY'\n      import json, re\n      m = json.load(open(\".archon-wo-grader/manifest.json\", encoding=\"utf-8\"))\n      text = \"\\n\".join(m.get(\"sections\", {}).get(\"Vercel deployment\", [])).strip()\n      urls = re.findall(r\"https://[^\\s)]+\", text)\n      result = {\n          \"vercel_text\": text,\n          \"status\": \"present\" if text else \"missing\",\n          \"na\": text.upper().startswith(\"N/A\"),\n          \"urls\": urls,\n      }\n      with open(\".archon-wo-grader/vercel.json\", \"w\", encoding=\"utf-8\") as f:\n          json.dump(result, f, indent=2)\n      print(f\"VERCEL_STATUS={result['status']}\")\n      print(f\"VERCEL_NA={str(result['na']).lower()}\")\n      print(f\"VERCEL_URL_COUNT={len(urls)}\")\n      PY\n\n  - id: grader-loop\n    depends_on: [verify-pr-state, verify-grep-assertions, verify-runtime, verify-vercel]\n    task_class: medium_work\n    loop:\n      prompt: |\n        You are the BDC WO Grader. Grade this WO at REVIEW using only the evidence below.\n\n        ## Hard Verdicts\n        Return exactly one verdict:\n        - satisfied: manifest v2 present, PRs merged, required evidence exists, no blocker remains.\n        - needs_revision: builder can fix missing/incorrect manifest evidence or failed stop condition.\n        - failed: evidence is structurally unsafe, contradictory, or cannot be trusted.\n\n        ## Evidence\n        read-wo:\n        $read-wo.output\n\n        parse-manifest:\n        $parse-manifest.output\n\n        verify-pr-state:\n        $verify-pr-state.output\n\n        verify-grep-assertions:\n        $verify-grep-assertions.output\n\n        verify-runtime:\n        $verify-runtime.output\n\n        verify-vercel:\n        $verify-vercel.output\n\n        Previous iteration:\n        $LOOP_PREV_OUTPUT\n\n        ## Rules\n        - Do not mark satisfied if the manifest is missing v2 sections.\n        - Do not mark satisfied if any PR is not merged.\n        - Grep assertions captured but not executed are acceptable only if the manifest explicitly says they were run and other evidence supports the claim; otherwise needs_revision.\n        - Runtime verification and Vercel deployment may be N/A only when the WO class makes them not applicable.\n        - Mention unresolved deploy, secret scan, or review failures as blockers.\n\n        ## Required Output\n        Start with:\n        VERDICT: satisfied|needs_revision|failed\n\n        Then:\n        CRITIQUE:\n        - concise bullet list\n\n        ACTION:\n        - If satisfied: flip DONE.\n        - If needs_revision: flip IN_PROGRESS and dispatch builder with critique.\n        - If failed: flip BLOCKED only if the evidence is unsafe; otherwise needs_revision.\n\n        End with:\n        <promise>GRADE_DECISION</promise>\n      until: GRADE_DECISION\n      max_iterations: 3\n      fresh_context: true\n\n  - id: decide\n    depends_on: [grader-loop]\n    bash: |\n      set -euo pipefail\n      verdict=$(printf '%s\\n' \"$grader-loop.output\" | sed -n 's/^VERDICT:[[:space:]]*//p' | head -n 1 | tr '[:upper:]' '[:lower:]')\n      case \"$verdict\" in\n        satisfied)\n          echo \"OUTCOME=satisfied\"\n          echo \"CLAUDE_STATUS=DONE\"\n          echo \"STATUS=Done\"\n          echo \"BUILDER_ACTION=verified\"\n          ;;\n        needs_revision)\n          echo \"OUTCOME=needs_revision\"\n          echo \"CLAUDE_STATUS=IN_PROGRESS\"\n          echo \"STATUS=In Progress\"\n          echo \"BUILDER_ACTION=reverified\"\n          ;;\n        failed)\n          echo \"OUTCOME=failed\"\n          echo \"CLAUDE_STATUS=BLOCKED\"\n          echo \"STATUS=Blocked\"\n          echo \"BUILDER_ACTION=escalate_xo\"\n          ;;\n        *)\n          echo \"ERROR: Could not parse grader verdict from grader-loop output\" >&2\n          printf '%s\\n' \"$grader-loop.output\" >&2\n          exit 1\n          ;;\n      esac\n\n  - id: post-builder-status\n    depends_on: [decide]\n    task_class: clerical\n    prompt: |\n      Prepare and, if HTTP tooling is available, post the builder-status payload.\n      Do not expose secrets. If HTTP tooling is unavailable, output the payload for XO.\n\n      Endpoint: https://n8n.bluedevilcollectibles.com/webhook/builder-status\n\n      Payload:\n      {\n        \"builder\": \"bdc-wo-grader\",\n        \"wo_id\": \"[WO_ID from $read-wo.output]\",\n        \"action\": \"[BUILDER_ACTION from $decide.output]\",\n        \"detail\": \"$grader-loop.output\"\n      }\n\n  - id: flip-notion\n    depends_on: [post-builder-status]\n    bash: |\n      set -euo pipefail\n      DRY_RUN=\"${DRY_RUN:-false}\"\n      if [ \"$DRY_RUN\" = \"true\" ]; then\n        echo \"DRY_RUN=true -- skipping Notion mutation\"\n        exit 0\n      fi\n      if [ -z \"${NOTION_API_KEY:-}\" ]; then\n        echo \"ERROR: NOTION_API_KEY is required for Notion update\" >&2\n        exit 1\n      fi\n\n      DECIDE_OUTPUT=$(cat <<'EOF_DECIDE'\n      $decide.output\n      EOF_DECIDE\n      )\n      GRADER_OUTPUT=$(cat <<'EOF_GRADER'\n      $grader-loop.output\n      EOF_GRADER\n      )\n      export DECIDE_OUTPUT GRADER_OUTPUT\n\n      python3 <<'PY'\n      import json, os, re, sys, urllib.request\n\n      wo = json.load(open(\".archon-wo-grader/wo.json\", encoding=\"utf-8\"))\n      page_id = wo[\"page_id\"]\n      token = os.environ[\"NOTION_API_KEY\"]\n      decide = os.environ.get(\"DECIDE_OUTPUT\", \"\")\n      grade = os.environ.get(\"GRADER_OUTPUT\", \"\")\n\n      def line_value(name, default=\"\"):\n          m = re.search(rf\"^{re.escape(name)}=(.+)$\", decide, re.M)\n          return m.group(1).strip() if m else default\n\n      claude_status = line_value(\"CLAUDE_STATUS\", \"IN_PROGRESS\")\n      status = line_value(\"STATUS\", \"In Progress\")\n      outcome = line_value(\"OUTCOME\", \"needs_revision\")\n\n      verdict_block = (\n          wo.get(\"notes\", \"\").rstrip()\n          + \"\\n\\n---\\n\"\n          + \"BDC WO Grader verdict\\n\"\n          + f\"Outcome: {outcome}\\n\"\n          + grade.strip()\n          + \"\\n\"\n      )\n\n      chunks = []\n      for i in range(0, len(verdict_block), 1800):\n          chunks.append({\"type\": \"text\", \"text\": {\"content\": verdict_block[i:i+1800]}})\n\n      payload = {\n          \"properties\": {\n              \"Claude Status\": {\"select\": {\"name\": claude_status}},\n              \"Status\": {\"status\": {\"name\": status}},\n              \"Notes\": {\"rich_text\": chunks},\n          }\n      }\n\n      headers = {\n          \"Authorization\": f\"Bearer {token}\",\n          \"Notion-Version\": \"2022-06-28\",\n          \"Content-Type\": \"application/json\",\n      }\n      req = urllib.request.Request(\n          f\"https://api.notion.com/v1/pages/{page_id}\",\n          data=json.dumps(payload).encode(),\n          headers=headers,\n          method=\"PATCH\",\n      )\n      with urllib.request.urlopen(req, timeout=30) as resp:\n          result = json.loads(resp.read().decode())\n      print(f\"NOTION_UPDATED={result.get('id')}\")\n      print(f\"CLAUDE_STATUS={claude_status}\")\n      print(f\"STATUS={status}\")\n      PY\n",
  "test-approve-resume": "name: test-approve-resume\ndescription: |\n  Harness-only regression fixture for bdc-xo#86.\n  Verifies an interactive approval loop resumes into the next node after Approve.\n\nmodel: sonnet\ninteractive: true\nworktree:\n  enabled: false\n\nnodes:\n  - id: implement\n    bash: |\n      echo \"IMPLEMENT_OK\"\n\n  - id: approve\n    depends_on: [implement]\n    loop:\n      prompt: |\n        This is a harness regression fixture.\n\n        Latest operator input: $LOOP_USER_INPUT\n\n        If the operator says \"approved\", output exactly:\n        <promise>APPROVED</promise>\n\n        Otherwise ask the operator to approve the fixture.\n      until: APPROVED\n      max_iterations: 2\n      interactive: true\n      gate_message: |\n        Approve to verify the API auto-resumes and reaches the pr node.\n\n  - id: pr\n    depends_on: [approve]\n    bash: |\n      echo \"RESUME_SUCCESS\"\n",
  "wo-shopops-billing-statement-v2-01": "# Drafted by yaml-author subagent for WO-SHOPOPS-BILLING-STATEMENT-V2-01 on 2026-05-23\n# Source pattern: bdc-template-shopops-api.yaml (shopops-api WO — Node.js Express routes + Rule 19 docker exec)\n# Gold standard cross-checked: bdc-ce-regression-suite.yaml (2026-05-12 end-to-end proven)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring verified at draft time\n#\n# Rule compliance checklist (verify before sync):\n#  [x] Rule 1:  model: sonnet at workflow root\n#  [x] Rule 2:  bash blocks use $WORKFLOW_ID (not ${run.id}); branch suffix uses date +%Y%m%d%H%M%S\n#  [x] Rule 3:  implement loop has fresh_context: true\n#  [x] Rule 4:  all paths relative to repo root (no BDC_XO/ prefix)\n#  [x] Rule 5:  spec lookup tries docs/work-orders/ then date-prefix glob in docs/superpowers/specs/\n#               then GitHub issue search fallback\n#  [x] Rule 6:  policyFile: harness/policies/agent-behavior.md (no prefix)\n#  [x] Rule 7:  flip-notion final node present; manifest goes in Notes property, not comment\n#  [x] Rule 8:  codebase bluedevilcollectibles/shopops MUST be registered — verify below\n#  [x] Rule 9:  YAML must land in bdc-harness/.archon/workflows/defaults/ (flat or one subdir)\n#  [x] Rule 10: implement loop declares ALL FOUR siblings: prompt, until, max_iterations, fresh_context\n#  [x] Rule 11: NO n8n webhook calls (post-started/post-completed nodes removed)\n#  [x] Rule 12: sync via sync-workflows.sh (see Deploy comment below)\n#  [x] Rule 13: no LLM-to-bash gate without output_format (no such gate in this workflow)\n#  [x] Rule 19: shopops-api docker exec grep assertions in build-manifest and flip-notion\n#\n# Pre-flight (operator verifies BEFORE firing):\n#   1. Codebase registered:\n#      ssh hetzner-prod \"sqlite3 /opt/bdc/archon-data/archon.db \\\"SELECT name FROM remote_agent_codebases;\\\"\"\n#      Expected: bluedevilcollectibles/shopops appears in output.\n#      If missing: Archon Dashboard -> Projects -> \"+ Add Project\"\n#        Name: bluedevilcollectibles/shopops\n#        Repository URL: https://github.com/bluedevilcollectibles/shopops\n#        Default branch: staging\n#   2. WO spec exists on bdc-xo main:\n#      gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/WO-SHOPOPS-BILLING-STATEMENT-V2-01.md?ref=main\" --jq .name\n#      Expected: \"WO-SHOPOPS-BILLING-STATEMENT-V2-01.md\"\n#   3. GITHUB_TOKEN in Archon executor has push scope to bluedevilcollectibles/shopops.\n#   4. shopops staging branch exists: git ls-remote --heads <url> staging\n#   5. Rule 19 reminder: after John promotes the staged PR, operator MUST rebuild + restart shopops-api container:\n#      ssh hetzner-prod \"cd /opt/bdc/shopops && docker compose pull && docker compose up -d shopops-api\"\n#      Then run the docker exec grep assertions from the manifest to prove source is live.\n#\n# Deploy:\n#   bash BDC_XO/scripts/archon-bridge/sync-workflows.sh wo-shopops-billing-statement-v2-01.yaml\n#   After sync, verify: curl -s https://archon.bluedevilcollectibles.com/api/workflows | python -c \\\n#     \"import sys,json; print([w['workflow']['name'] for w in json.load(sys.stdin)['workflows']])\"\n#   Expected: 'bdc-shopops-billing-statement-v2-01' in output.\n#\n# REVIEW BEFORE DEPLOY — human verifies: grep assertions match actual file paths, test\n# command matches shopops-api runner (cd shopops-api && node tests/run_all.js), Notion page ID correct,\n# Rule 19 docker exec paths match deployed container layout.\n\nname: bdc-shopops-billing-statement-v2-01\ndescription: |\n  Use when: Executing WO-SHOPOPS-BILLING-STATEMENT-V2-01 — Billing Statement v2 (WhatNot Pull-List Path).\n  Input: WO_ID = WO-SHOPOPS-BILLING-STATEMENT-V2-01.\n  Does:\n    - Reads spec from bdc-xo docs/work-orders/.\n    - Creates migrations/20260523_billing_statement_v2.sql (new billing_statement_line_items\n      table, billing_statements/statement_payments new columns, ISSUED status widen).\n    - Creates routes/statements-v2.js (/statements/v2 route family: POST create, GET list,\n      GET ready, GET :id, PATCH issue, PATCH reconcile, PATCH void).\n    - Extends lib/billing/assert-billing-source.js with assertBillingSourceV2 + assertBillingCutover\n      as NEW named exports alongside the existing assertBillingSource (DO NOT replace).\n    - Extends lib/billing/errors.js with BillingCutoverError + PaymentChannelInvalidError.\n    - Mounts /statements/v2 router in index.js BEFORE existing /statements mounts.\n    - Runs: cd shopops-api && node tests/run_all.js (all tests pass).\n    - Commits, pushes feature branch, opens PR to staging.\n    - Provides Rule 19 docker exec grep evidence in manifest.\n    - Flips Notion WO-SHOPOPS-BILLING-STATEMENT-V2-01 to REVIEW with manifest in Notes.\n  Repo: bluedevilcollectibles/shopops (staging branch — Rule 20 staging-gate repo).\n  WO class: CODE (single-repo, schema migration + new routes).\n  Gate: ShopOps Recovery Checklist — Gate 8 (BDC weekly billing path).\n  parallel_ok: false\n  NOT for: any other WO. Not for /statements/v2 Stripe/Manual path (separate WO).\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\ninputs:\n  WO_ID:\n    default: \"WO-SHOPOPS-BILLING-STATEMENT-V2-01\"\n\nnodes:\n\n  # ─── Node 1: Preflight GitHub auth ──────────────────────────────────────────\n  # Fail-closed if container cannot push (anchor: 2026-05-16 WO-169 engine sortie —\n  # git push silently failed, downstream swallow patterns hid it, workflow reported\n  # success with no commits on origin).\n  - id: preflight-github-auth\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      if ! git push --dry-run origin \"HEAD:refs/heads/preflight-auth-probe-$(date +%s)\" 2>&1; then\n        echo \"ERROR: GITHUB auth check failed -- container cannot push to bluedevilcollectibles/shopops.\" >&2\n        echo \"Check: GITHUB_TOKEN in executor env has repo push scope.\" >&2\n        exit 1\n      fi\n      echo \"preflight-github-auth PASS\"\n      echo \"TARGET_REPO=bluedevilcollectibles/shopops\"\n      echo \"TARGET_BRANCH=staging\"\n\n  # ─── Node 2: Read spec (cross-repo via gh api from bdc-xo) ──────────────────\n  # Rule 4: paths relative to bdc-xo repo root (no BDC_XO/ prefix).\n  # Rule 5: try docs/work-orders/ first, then date-prefix glob, then GitHub issue fallback.\n  # Spec lives in bdc-xo, NOT in the shopops worktree.\n  - id: read-spec\n    depends_on: [preflight-github-auth]\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-SHOPOPS-BILLING-STATEMENT-V2-01}\"\n\n      # Primary: docs/work-orders/<WO_ID>.md on bdc-xo main\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      SPEC_PATH=\"docs/work-orders/${WO_ID}.md\"\n\n      # Fallback 1: docs/superpowers/specs/<WO_ID>.md direct\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        SPEC_PATH=\"docs/superpowers/specs/${WO_ID}.md\"\n        SPEC_CONTENT=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" \\\n          --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n      fi\n\n      # Fallback 2: date-prefix glob in docs/superpowers/specs/\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --template '{{range .}}{{.path}}{{\"\\n\"}}{{end}}' 2>/dev/null \\\n          | grep \"${WO_ID}.md$\" | head -1 || true)\n        if [ -n \"$GLOB_PATH\" ]; then\n          SPEC_PATH=\"$GLOB_PATH\"\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${SPEC_PATH}?ref=main\" \\\n            --template '{{.content}}' 2>/dev/null | tr -d '\\n' | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      # Fallback 3: GitHub issue search (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n          --search \"${WO_ID} in:title\" --json number,title \\\n          --jq '.[0].number' 2>/dev/null || true)\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        echo \"Spec not found for ${WO_ID} (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, GitHub issue search)\" >&2\n        exit 1\n      fi\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:%s\\nWORKFLOW_ID=%s\\n\\n' \"$WO_ID\" \"$SPEC_PATH\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  # ─── Node 3: Fresh branch from staging ──────────────────────────────────────\n  # Rule 20 staging-gate repos branch fresh from staging HEAD.\n  - id: fresh-branch\n    depends_on: [read-spec]\n    bash: |\n      set -euo pipefail\n      git fetch origin staging --depth=50 || true\n      git checkout staging 2>/dev/null || echo \"staging branch held by worktree; staying on current HEAD\"\n      git reset --hard origin/staging 2>/dev/null || true\n      # Rule 2: branch suffix uses runtime date, NOT $WORKFLOW_ID (bash substitution-time).\n      BRANCH=\"wo/shopops-billing-statement-v2-01-run-$(date +%Y%m%d%H%M%S)\"\n      git checkout -b \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BASE=staging\"\n      echo \"REPO=bluedevilcollectibles/shopops\"\n\n  # ─── Node 4: Implement (self-review loop) ───────────────────────────────────\n  # Rule 3: fresh_context: true (required for self-review loops).\n  # Rule 10: ALL FOUR siblings declared: prompt, until, max_iterations, fresh_context.\n  # Rule 19: builder MUST note RULE19_FILE + RULE19_GREP in output for runtime-verify node.\n  # DO NOT replace existing assertBillingSource — only ADD assertBillingSourceV2 alongside it.\n  # Mount /statements/v2 BEFORE /statements in index.js (Express prefix matching).\n  - id: implement\n    depends_on: [fresh-branch]\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement WO-SHOPOPS-BILLING-STATEMENT-V2-01 in shopops-api.\n        CWD = shopops repo root. ES5 Express style throughout (var, function, 'use strict').\n        No new npm packages without XO sign-off.\n\n        ## Spec\n        $read-spec.output\n\n        ## Branch\n        $fresh-branch.output\n\n        ## What to build (in order)\n\n        ### STEP 1 — migrations/20260523_billing_statement_v2.sql\n        Create this file with the EXACT DDL from the spec §9:\n        1. Widen billing_statements.status CHECK to add 'ISSUED':\n           DROP CONSTRAINT billing_statements_status_check IF EXISTS;\n           ADD CONSTRAINT billing_statements_status_check\n             CHECK (status IN ('DRAFT','APPROVED','GENERATED','SENT','ISSUED','PARTIAL','PAID','VOID'));\n        2. ADD COLUMN IF NOT EXISTS on billing_statements: payment_channel, source, processor,\n           processor_account_scope, external_order_id, external_payment_id, payment_url,\n           payment_status, reconciliation_source (exact DDL verbatim from spec §9).\n        3. CREATE TABLE IF NOT EXISTS billing_statement_line_items (exact DDL from spec §9):\n           id, tenant_id, statement_id (FK billing_statements), allocation_id\n           (FK pull_allocations(allocation_id)), line_price_cents, quantity, description, created_at.\n           Create indexes bsli_statement_id, bsli_tenant_allocation.\n           Enable RLS.\n        4. ADD COLUMN IF NOT EXISTS on statement_payments: pull_allocation_id, external_order_id.\n\n        ### STEP 2 — lib/billing/errors.js (EXTEND, do not remove existing exports)\n        Add two new named exports alongside BillingSourceInvalidError and DACannotMutateBillingError:\n        - BillingCutoverError: status 422, code 'BILLING_CUTOVER_GATE'\n        - PaymentChannelInvalidError: status 422, code 'PAYMENT_CHANNEL_INVALID'\n        Pattern: follow existing error class shape in the file.\n\n        ### STEP 3 — lib/billing/assert-billing-source.js (ADD new exports, KEEP existing)\n        CRITICAL: The existing assertBillingSource function with signature\n          ({ lineSources, context, client, tenantId })\n        MUST be retained unchanged. routes/invoices.js depends on it.\n        DO NOT rename or remove it.\n\n        ADD as new named exports in the same file:\n        - assertBillingCutover(date):\n            if (new Date(date) < new Date('2026-05-06T00:00:00Z')) {\n              throw new BillingCutoverError('Allocation pre-dates cutover (2026-05-06)');\n            }\n        - assertBillingSourceV2({ allocationIds, db }):\n            1. Query pull_allocations WHERE allocation_id IN (allocationIds)\n            2. If any IDs missing -> throw BillingSourceInvalidError (422, BILLING_SOURCE_INVALID)\n            3. If any row.status != 'READY_TO_INVOICE' -> throw BillingSourceInvalidError\n            4. For each row: assertBillingCutover(row.allocated_at)\n\n        ### STEP 4 — routes/statements-v2.js (CREATE NEW FILE)\n        Express router with 7 routes matching the spec §7 function inventory exactly:\n        - assertPaymentChannel({ payment_channel, processor }) — inline helper\n        - POST /   (create: assertBillingSourceV2, assertPaymentChannel, INSERT billing_statements\n                    + billing_statement_line_items, UPDATE pull_allocations -> INVOICED, 201)\n        - GET /    (list WHERE generation_mode='v2', paginated)\n        - GET /ready (pull_allocations WHERE status='READY_TO_INVOICE' grouped by customer_id)\n        - GET /:id  (statement + line_items + payments)\n        - PATCH /:id/issue   (DRAFT -> ISSUED, whatnot_sku, NO Stripe PI)\n        - PATCH /:id/reconcile (INSERT statement_payments, ISSUED/PARTIAL -> PAID/PARTIAL)\n        - PATCH /:id/void    (-> VOID, UPDATE pull_allocations -> RESERVED,\n                               invoiced_at=null, released_at=null — NOT now())\n        Apply daBillingBlockMiddleware to all mutation routes.\n        Follow ES5 Express pattern from routes/statements.js.\n\n        ### STEP 5 — index.js (mount /statements/v2 BEFORE /statements)\n        Find the lines that mount /statements (around line 503-505 per spec).\n        Mount /statements/v2 on the line ABOVE the /statements mount:\n          app.use('/statements/v2', validateHmac, requireAuth, requireEntitlement, statementsV2Router);\n        The v2 mount MUST appear at a lower line number than the /statements mount.\n        This is a hard requirement: Express prefix matching routes /statements/v2/* to the first\n        matching handler. If v2 is mounted after /statements, all v2 routes return 404.\n\n        ## Invariants from spec (MUST enforce)\n        - void flow: released_at=null, invoiced_at=null (NOT now()). released_at=now() is for\n          terminal RELEASED state only, not a return-to-RESERVED event.\n        - No new statement_payments row writes to allocation_id column (legacy FK); writes go to\n          pull_allocation_id (new FK) only.\n        - SHOW_PICKUP must NOT appear anywhere in new code.\n        - pull_list_allocations (old table) must NOT be referenced in assertBillingSourceV2.\n\n        ## Tests\n        After implementation, run: cd shopops-api && node tests/run_all.js\n        All tests must pass. Fix any failures before emitting COMPLETE.\n        Also write a test file tests/statements-v2.test.js with at least 7 tests covering:\n          1. POST /statements/v2 happy path (201, billing_statements created, allocations INVOICED)\n          2. Cutover gate rejects pre-2026-05-06 allocation (422 BILLING_CUTOVER_GATE)\n          3. Wrong allocation status rejected (422 BILLING_SOURCE_INVALID)\n          4. PATCH issue happy path (200, status ISSUED, whatnot_sku set, payment_url null)\n          5. PATCH reconcile to PAID (200, statement_payments row, status PAID)\n          6. PATCH void returns allocations to RESERVED, released_at null (200)\n          7. DA actor blocked on mutation (403 DA_CANNOT_MUTATE_BILLING)\n        Each test must assert actual response code + DB state, not just \"did not throw\".\n\n        ## Commit (inside this loop, before emitting COMPLETE)\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"feat(shopops-api): WO-SHOPOPS-BILLING-STATEMENT-V2-01 — billing statement v2 (WhatNot pull-list path)\n\n        - migrations/20260523_billing_statement_v2.sql: billing_statement_line_items table,\n          billing_statements new columns, statement_payments new columns, ISSUED status widen\n        - routes/statements-v2.js: /statements/v2 route family (POST create, GET list,\n          GET ready, GET :id, PATCH issue, PATCH reconcile, PATCH void)\n        - shopops-api/lib/billing/assert-billing-source.js: +assertBillingSourceV2 +assertBillingCutover\n          (assertBillingSource retained unchanged for routes/invoices.js compat)\n        - shopops-api/lib/billing/errors.js: +BillingCutoverError +PaymentChannelInvalidError\n        - index.js: mount /statements/v2 BEFORE /statements\n\n        Co-Authored-By: Claude Sonnet (Cauldron bdc-shopops-billing-statement-v2-01) <noreply@anthropic.com>\"\n        git push -u origin \"$(git rev-parse --abbrev-ref HEAD)\" 2>&1 || echo \"WARN: push failed; commit-and-push backstop will retry\"\n\n        ## Required output at completion\n        Before emitting COMPLETE, output ALL of:\n        RULE19_FILE=routes/statements-v2.js\n        RULE19_GREP=statements/v2\n\n        Then emit completion sentinels, each on its own line:\n        COMPLETE\n        BUILD_COMPLETE\n        <promise>COMPLETE</promise>\n\n  # ─── Node 5: Grep assertions (bash-executable, per WO §12 stop conditions) ─\n  # Exactly the grep commands from the WO stop conditions, executed as CI evidence.\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      echo \"=== WO-SHOPOPS-BILLING-STATEMENT-V2-01 grep assertions ===\"\n\n      # Stop 2: assertBillingSourceV2 present in assert-billing-source.js\n      ASSERT_COUNT=$(grep -c \"assertBillingSourceV2\" shopops-api/lib/billing/assert-billing-source.js)\n      [ \"$ASSERT_COUNT\" -ge 1 ] || (echo \"FAIL: assertBillingSourceV2 not found in assert-billing-source.js\" >&2; exit 1)\n      echo \"assertBillingSourceV2 count: $ASSERT_COUNT (>= 1 required) PASS\"\n\n      # Stop 2: both old + new function names present (>= 2 total assertBillingSource references)\n      BOTH_COUNT=$(grep -c \"assertBillingSource\" shopops-api/lib/billing/assert-billing-source.js)\n      [ \"$BOTH_COUNT\" -ge 2 ] || (echo \"FAIL: assertBillingSource appears fewer than 2 times (old+new required)\" >&2; exit 1)\n      echo \"assertBillingSource total count: $BOTH_COUNT (>= 2 required) PASS\"\n\n      # Stop 2: assertBillingCutover present\n      CUTOVER_COUNT=$(grep -c \"assertBillingCutover\" shopops-api/lib/billing/assert-billing-source.js)\n      [ \"$CUTOVER_COUNT\" -ge 1 ] || (echo \"FAIL: assertBillingCutover not found\" >&2; exit 1)\n      echo \"assertBillingCutover count: $CUTOVER_COUNT (>= 1 required) PASS\"\n\n      # Stop 2: BILLING_CUTOVER_GATE referenced in assert-billing-source.js\n      GATE_COUNT=$(grep -c \"BILLING_CUTOVER_GATE\" shopops-api/lib/billing/assert-billing-source.js)\n      [ \"$GATE_COUNT\" -ge 1 ] || (echo \"FAIL: BILLING_CUTOVER_GATE not found in assert-billing-source.js\" >&2; exit 1)\n      echo \"BILLING_CUTOVER_GATE count: $GATE_COUNT (>= 1 required) PASS\"\n\n      # Stop 2: assertBillingSourceV2 must NOT reference pull_list_allocations (legacy table)\n      LEGACY_COUNT=$(grep -c \"pull_list_allocations\" shopops-api/lib/billing/assert-billing-source.js || true)\n      [ \"$LEGACY_COUNT\" -eq 0 ] || (echo \"FAIL: pull_list_allocations found in assert-billing-source.js (forbidden in v2 path)\" >&2; exit 1)\n      echo \"pull_list_allocations count: $LEGACY_COUNT (== 0 required) PASS\"\n\n      # Stop 3: /statements/v2 router mounted in index.js (at least 2 references: require + app.use)\n      V2_MOUNT_COUNT=$(grep -c \"statements/v2\\|statements-v2\\|statementsV2\" index.js)\n      [ \"$V2_MOUNT_COUNT\" -ge 2 ] || (echo \"FAIL: /statements/v2 router not mounted in index.js (need >= 2 refs)\" >&2; exit 1)\n      echo \"statements/v2 index.js refs: $V2_MOUNT_COUNT (>= 2 required) PASS\"\n\n      # Stop 3: v2 mount line number LOWER than /statements mount (Express prefix matching)\n      V2_LINE=$(grep -n \"statements/v2\\|statementsV2\" index.js | grep \"app.use\" | head -1 | cut -d: -f1)\n      V1_LINE=$(grep -n \"app.use.*'/statements'\" index.js | grep -v \"v2\" | head -1 | cut -d: -f1)\n      if [ -n \"$V2_LINE\" ] && [ -n \"$V1_LINE\" ]; then\n        [ \"$V2_LINE\" -lt \"$V1_LINE\" ] || (echo \"FAIL: /statements/v2 mount (line $V2_LINE) is NOT before /statements mount (line $V1_LINE)\" >&2; exit 1)\n        echo \"v2 mount order: line $V2_LINE < line $V1_LINE PASS\"\n      else\n        echo \"WARN: could not determine mount order; manual check required (v2_line=$V2_LINE v1_line=$V1_LINE)\"\n      fi\n\n      # Migration file assertions\n      MIGRATION_BSLI=$(grep -c \"billing_statement_line_items\" migrations/20260523_billing_statement_v2.sql)\n      [ \"$MIGRATION_BSLI\" -ge 1 ] || (echo \"FAIL: billing_statement_line_items not in migration\" >&2; exit 1)\n      echo \"billing_statement_line_items in migration: $MIGRATION_BSLI (>= 1 required) PASS\"\n\n      MIGRATION_ISSUED=$(grep -c \"ISSUED\" migrations/20260523_billing_statement_v2.sql)\n      [ \"$MIGRATION_ISSUED\" -ge 1 ] || (echo \"FAIL: ISSUED not in migration (status CHECK widen required)\" >&2; exit 1)\n      echo \"ISSUED in migration: $MIGRATION_ISSUED (>= 1 required) PASS\"\n\n      # routes/statements-v2.js assertions\n      PULL_ALLOC_ID=$(grep -c \"pull_allocation_id\" routes/statements-v2.js)\n      [ \"$PULL_ALLOC_ID\" -ge 1 ] || (echo \"FAIL: pull_allocation_id not found in statements-v2.js\" >&2; exit 1)\n      echo \"pull_allocation_id in statements-v2.js: $PULL_ALLOC_ID (>= 1 required) PASS\"\n\n      SHOW_PICKUP=$(grep -c \"SHOW_PICKUP\" routes/statements-v2.js || true)\n      [ \"$SHOW_PICKUP\" -eq 0 ] || (echo \"FAIL: SHOW_PICKUP found in statements-v2.js (forbidden)\" >&2; exit 1)\n      echo \"SHOW_PICKUP count: $SHOW_PICKUP (== 0 required) PASS\"\n\n      # errors.js — both new error classes present\n      BILLING_CUTOVER_ERR=$(grep -c \"BillingCutoverError\" shopops-api/lib/billing/errors.js)\n      [ \"$BILLING_CUTOVER_ERR\" -ge 1 ] || (echo \"FAIL: BillingCutoverError not in errors.js\" >&2; exit 1)\n      echo \"BillingCutoverError in errors.js: $BILLING_CUTOVER_ERR (>= 1 required) PASS\"\n\n      PAYMENT_CHAN_ERR=$(grep -c \"PaymentChannelInvalidError\" shopops-api/lib/billing/errors.js)\n      [ \"$PAYMENT_CHAN_ERR\" -ge 1 ] || (echo \"FAIL: PaymentChannelInvalidError not in errors.js\" >&2; exit 1)\n      echo \"PaymentChannelInvalidError in errors.js: $PAYMENT_CHAN_ERR (>= 1 required) PASS\"\n\n      echo \"\"\n      echo \"=== All grep assertions PASS ===\"\n\n  # ─── Node 6: Run tests ──────────────────────────────────────────────────────\n  # shopops-api test runner runs from the shopops-api subdirectory.\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      if [ -f tests/statements-v2.test.js ]; then\n        TEST_COUNT=$(grep -c \"^  it\\|^  test\\|^it(\\|^test(\" tests/statements-v2.test.js || true)\n        echo \"Test file present. Detected $TEST_COUNT test functions.\"\n        [ \"$TEST_COUNT\" -ge 7 ] || echo \"WARN: fewer than 7 tests in statements-v2.test.js; check spec §11\"\n      else\n        echo \"WARN: tests/statements-v2.test.js not found; implement loop may have skipped test authoring\"\n      fi\n      cd shopops-api\n      node tests/run_all.js\n      echo \"cd shopops-api && node tests/run_all.js PASS\"\n\n  # ─── Node 7: Backstop commit-and-push ──────────────────────────────────────\n  # Idempotent — if implement loop already committed + pushed, this is a no-op.\n  # No swallow patterns on push (load_bearing intent).\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    load_bearing: true\n    bash: |\n      set -euo pipefail\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      git add -A\n      if git diff --cached --quiet; then\n        echo \"No staged changes; implement loop already committed.\"\n        COMMITS_AHEAD=$(git rev-list --count \"origin/${BRANCH}..HEAD\" 2>/dev/null || echo 0)\n        if [ \"$COMMITS_AHEAD\" = \"0\" ]; then\n          echo \"HEAD matches origin/${BRANCH} — backstop no-op.\"\n        else\n          echo \"Backstop: $COMMITS_AHEAD commit(s) ahead of origin — pushing.\"\n        fi\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"chore: backstop commit WO-SHOPOPS-BILLING-STATEMENT-V2-01\"\n      fi\n      PUSH_OK=false\n      for attempt in 1 2 3; do\n        if git push -u origin \"$BRANCH\" 2>&1; then\n          PUSH_OK=true\n          break\n        fi\n        echo \"WARN: push attempt $attempt failed; retrying after $((attempt * 5))s...\" >&2\n        sleep $((attempt * 5))\n      done\n      if [ \"$PUSH_OK\" = \"false\" ]; then\n        echo \"ERROR: git push failed after 3 attempts.\" >&2\n        exit 1\n      fi\n      echo \"BRANCH=${BRANCH}\"\n      echo \"SHA=$(git rev-parse HEAD)\"\n\n  # ─── Node 8: Open PR ────────────────────────────────────────────────────────\n  # PR base = staging for Rule 20 staging-gate repos.\n  # Idempotent: checks for existing PR before attempting creation.\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      # set -e DELIBERATELY NOT OMITTED here — PR open is not best-effort; it's load-bearing.\n      WO_ID=\"WO-SHOPOPS-BILLING-STATEMENT-V2-01\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BRANCH=\"$(git rev-parse --abbrev-ref HEAD)\"\n      BASE=\"staging\"\n      TITLE=\"feat(shopops-api): WO-SHOPOPS-BILLING-STATEMENT-V2-01 — billing statement v2 (WhatNot pull-list path)\"\n\n      # Idempotent: check for existing open PR\n      EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n        --json url --jq '.[0].url // empty' 2>/dev/null || true)\n      if [ -n \"$EXIST\" ]; then\n        echo \"PR_URL=$EXIST\"\n        exit 0\n      fi\n\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"\"\n        echo \"Implements **WO-SHOPOPS-BILLING-STATEMENT-V2-01** — Billing Statement v2, WhatNot Pull-List Path.\"\n        echo \"Gate: ShopOps Recovery Checklist — Gate 8 (BDC weekly billing path).\"\n        echo \"\"\n        echo \"## Files created\"\n        echo \"- \\`migrations/20260523_billing_statement_v2.sql\\` — billing_statement_line_items table,\"\n        echo \"  billing_statements new columns (payment_channel, source, processor, etc.),\"\n        echo \"  statement_payments new columns (pull_allocation_id, external_order_id),\"\n        echo \"  ISSUED added to status CHECK constraint.\"\n        echo \"- \\`routes/statements-v2.js\\` — /statements/v2 route family (POST create, GET list,\"\n        echo \"  GET ready, GET :id, PATCH issue, PATCH reconcile, PATCH void).\"\n        echo \"\"\n        echo \"## Files modified\"\n        echo \"- \\`shopops-api/lib/billing/assert-billing-source.js\\` — +assertBillingSourceV2 +assertBillingCutover\"\n        echo \"  (assertBillingSource retained unchanged for routes/invoices.js compat).\"\n        echo \"- \\`shopops-api/lib/billing/errors.js\\` — +BillingCutoverError +PaymentChannelInvalidError.\"\n        echo \"- \\`index.js\\` — mount /statements/v2 BEFORE /statements mounts.\"\n        echo \"\"\n        echo \"## Rule 19 — Runtime verification required after merge\"\n        echo \"shopops-api uses COPY . /app at image build time. After merging this PR and\"\n        echo \"rebuilding the container, operator MUST verify:\"\n        echo \"\"\n        echo \"\\`\\`\\`\"\n        echo \"docker exec shopops-api grep -n 'statements/v2' /app/index.js\"\n        echo \"docker exec shopops-api grep -n 'assertBillingCutover' /app/lib/billing/assert-billing-source.js\"\n        echo \"docker exec shopops-api grep -n 'pull_allocation_id' /app/routes/statements-v2.js\"\n        echo \"\\`\\`\\`\"\n        echo \"\"\n        echo \"All must return >= 1 match. If any return 0, rebuild + restart the container.\"\n        echo \"\"\n        echo \"## Spec\"\n        echo \"bdc-xo: \\`docs/work-orders/WO-SHOPOPS-BILLING-STATEMENT-V2-01.md\\`\"\n        echo \"\"\n        echo \"Generated by Archon workflow \\`bdc-shopops-billing-statement-v2-01\\`.\"\n      } > \"$BODY_FILE\"\n\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        EXIST=$(gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open \\\n          --json url --jq '.[0].url // empty' 2>/dev/null || true)\n        if [ -n \"$EXIST\" ]; then\n          echo \"PR_URL=$EXIST\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep $((attempt * 5))\n      done\n      echo \"ERROR: gh pr create failed after 5 attempts — branch pushed at $BRANCH but no PR opened.\" >&2\n      exit 1\n\n  # ─── Node 9: Build manifest (Captain CI v2 format) ──────────────────────────\n  # Rule 19: manifest MUST include docker exec grep evidence.\n  # Rule 7: manifest goes in Notes property, not as page comment.\n  - id: build-manifest\n    depends_on: [open-pr]\n    task_class: clerical\n    prompt: |\n      Build the Captain CI completion manifest v2 for WO-SHOPOPS-BILLING-STATEMENT-V2-01.\n      The manifest MUST be in exact v2 format — Captain CI parses these labels literally.\n\n      ## Required manifest fields (fill ALL from the node outputs below)\n\n      WO: WO-SHOPOPS-BILLING-STATEMENT-V2-01\n      Builder: Devils Cauldron (Archon bdc-shopops-billing-statement-v2-01)\n\n      Files created:\n       - shopops/migrations/20260523_billing_statement_v2.sql\n       - shopops/routes/statements-v2.js\n       - shopops/tests/statements-v2.test.js\n\n      Files modified:\n       - shopops/shopops-api/lib/billing/assert-billing-source.js (+assertBillingSourceV2, +assertBillingCutover; assertBillingSource retained)\n       - shopops/shopops-api/lib/billing/errors.js (+BillingCutoverError, +PaymentChannelInvalidError)\n       - shopops/index.js (mount /statements/v2 before /statements)\n\n      Tests: [fill from verify-tests.output — format: N/N (cd shopops-api && node tests/run_all.js)]\n\n      PRs: [fill from open-pr.output — format: repo/PR URL (merged: timestamp, commit: sha)]\n\n      Merge ancestors:\n       - shopops staging HEAD: [fill from commit-and-push.output SHA] | manifest commit: [same] | behind_by: 0\n\n      Grep assertions:\n       - shopops: grep -c \"assertBillingSourceV2\" shopops-api/lib/billing/assert-billing-source.js && [ $(grep -c \"assertBillingSourceV2\" shopops-api/lib/billing/assert-billing-source.js) -ge 1 ]\n       - shopops: grep -c \"assertBillingSource\" shopops-api/lib/billing/assert-billing-source.js && [ $(grep -c \"assertBillingSource\" shopops-api/lib/billing/assert-billing-source.js) -ge 2 ]\n       - shopops: grep -c \"pull_list_allocations\" shopops-api/lib/billing/assert-billing-source.js && [ $(grep -c \"pull_list_allocations\" shopops-api/lib/billing/assert-billing-source.js 2>/dev/null || echo 0) -eq 0 ]\n       - shopops: grep -c \"statements/v2\\|statementsV2\" index.js && [ $(grep -c \"statements/v2\\|statementsV2\" index.js) -ge 2 ]\n       - shopops: grep -c \"pull_allocation_id\" routes/statements-v2.js && [ $(grep -c \"pull_allocation_id\" routes/statements-v2.js) -ge 1 ]\n       - shopops: grep -c \"SHOW_PICKUP\" routes/statements-v2.js && [ $(grep -c \"SHOW_PICKUP\" routes/statements-v2.js 2>/dev/null || echo 0) -eq 0 ]\n       - shopops: grep -c \"billing_statement_line_items\" migrations/20260523_billing_statement_v2.sql && [ $(grep -c \"billing_statement_line_items\" migrations/20260523_billing_statement_v2.sql) -ge 1 ]\n       - shopops: grep -c \"ISSUED\" migrations/20260523_billing_statement_v2.sql && [ $(grep -c \"ISSUED\" migrations/20260523_billing_statement_v2.sql) -ge 1 ]\n       - shopops: grep -c \"BillingCutoverError\" shopops-api/lib/billing/errors.js && [ $(grep -c \"BillingCutoverError\" shopops-api/lib/billing/errors.js) -ge 1 ]\n       - shopops: grep -c \"PaymentChannelInvalidError\" shopops-api/lib/billing/errors.js && [ $(grep -c \"PaymentChannelInvalidError\" shopops-api/lib/billing/errors.js) -ge 1 ]\n\n      Runtime verification (Rule 19 — shopops-api uses COPY . /app, rebuild required after merge):\n       PENDING — operator must rebuild + restart shopops-api container after John promotes the staged PR.\n       After rebuild, run:\n         docker exec shopops-api grep -n \"statements/v2\" /app/index.js\n           -> Expected: >= 1 match (mount line)\n         docker exec shopops-api grep -n \"assertBillingCutover\" /app/lib/billing/assert-billing-source.js\n           -> Expected: >= 1 match\n         docker exec shopops-api grep -n \"pull_allocation_id\" /app/routes/statements-v2.js\n           -> Expected: >= 1 match\n         docker exec shopops-api grep -n \"BILLING_CUTOVER_GATE\\|PAYMENT_CHANNEL_INVALID\" /app/lib/billing/errors.js\n           -> Expected: >= 2 matches\n       If any grep returns 0 -> rebuild + restart required before marking DONE.\n\n      Vercel deployment: N/A (shopops-api is Docker on Hetzner port 3402, not Vercel)\n\n      VALIDATION: PASS (runtime verify pending post-deploy; all pre-deploy assertions verified above)\n\n      ## Node outputs to draw from\n      Spec: $read-spec.output\n      Implement: $implement.output\n      Grep assertions: $verify-grep.output\n      Tests: $verify-tests.output\n      Commit: $commit-and-push.output\n      PR: $open-pr.output\n\n  # ─── Node 10: Flip Notion to REVIEW ─────────────────────────────────────────\n  # Rule 7: MANDATORY final node. Manifest goes in Notes property, NOT as page comment.\n  # Captain CI reads Notes. Never mark DONE — only REVIEW.\n  # Notion page ID: 369ef4c9-ce00-8186-aa2a-e8fd13a5d438\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-SHOPOPS-BILLING-STATEMENT-V2-01 to REVIEW.\n      The manifest MUST go in the Notes property (NOT as a page comment — Captain CI reads Notes).\n      NEVER mark Status as DONE — only REVIEW. Only Captain CI can mark DONE.\n\n      ## Notion page\n      Page ID: 369ef4c9-ce00-8186-aa2a-e8fd13a5d438\n\n      ## Required Notion updates (single API call if possible)\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> set to the manifest text from $build-manifest.output VERBATIM\n\n      ## Manifest\n      $build-manifest.output\n\n      ## Implementation\n      Use the Notion MCP to update the page properties.\n\n      If Notion MCP is unavailable inside this container runtime:\n      - Output a XO_TODO line with the manifest text so XO can flip it manually.\n      - Format: XO_TODO: flip Notion 369ef4c9-ce00-8186-aa2a-e8fd13a5d438 to REVIEW with manifest above.\n      - Do NOT invent a workaround or make web requests. The XO_TODO line is sufficient.\n",
  "wo-shopops-cover-cascade-prh-visibility-01": "# Drafted by yaml-author subagent for WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 on 2026-05-15\n# Source pattern: bdc-ce-regression-suite.yaml (gold-standard DAG shape, proven 2026-05-12) +\n#                 bdc-shopops-commerce-spine.yaml (cross-repo spec read + MIXED-class migration apply +\n#                                                  Rule 19 runtime grep)\n# Doctrine compliance: 13 hard rules of archon-yaml-authoring (verified at draft time)\n#\n# Pre-flight: codebase bluedevilcollectibles/shopops must be registered in Archon.\n#   Verify: ssh hetzner-prod 'sqlite3 /opt/bdc/archon-data/archon.db \"SELECT name FROM remote_agent_codebases\"'\n#   Default branch is `master` (NOT main) for shopops.\n#\n# Pre-flight: WO spec MUST exist + be committed in bdc-xo at:\n#   docs/work-orders/WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.md\n#   Verify: gh api \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.md?ref=main\" --jq .name\n#\n# NOTE ON CWD: Archon clones bluedevilcollectibles/shopops into its worktree.\n#   CWD = shopops repo root. shopops-api/ is a SUBDIRECTORY. All paths below are\n#   relative to shopops repo root (e.g., shopops-api/services/coverResolver.js).\n#   The WO spec lives in bdc-xo, fetched via gh api cross-repo at read-spec.\n#\n# NOTE ON PARALLEL SAFETY: parallel_ok=FALSE — this WO applies a production view\n#   redefinition (CREATE OR REPLACE VIEW cover_images). Do NOT fire while any\n#   other shopops cover/images schema WO is in-flight. Serial only.\n#\n# NOTE ON RULE 19: shopops-api uses `COPY . /app` at image build time with NO\n#   bind-mount. Source changes need rebuild + container restart to take effect.\n#   runtime-verify node enforces this with `docker exec shopops-api grep`.\n#\n# NOTE ON MIGRATION APPLY: Supabase view DDL goes via SSH + docker run postgres:16 psql\n#   (banked doctrine `feedback_production-supabase-write-via-hetzner-ssh.md`). REST\n#   API is NOT acceptable for DDL. Hookify block-production-supabase only sees the\n#   SSH wrapper, not the SQL inside.\n#\n# Deploy: C:\\Users\\pcmed\\projects\\BDC_XO\\scripts\\archon-bridge\\sync-workflows.sh \\\n#         WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.yaml\n#\n# REVIEW BEFORE DEPLOY:\n#   - Notion page ID resolved: 361ef4c9-ce00-8168-9df6-d72fe87f657b (filed 2026-05-15)\n#   - Confirm next migration number (NN) — check shopops-api/migrations/ for highest existing\n#   - Confirm staging-vs-production Supabase strategy (WO §10 says \"stage first\" — this\n#     YAML applies directly to production via SSH; if staging dry-run needed, add a node)\n#   - Confirm DATABASE_URL is available inside shopops-api container (Rule 19 verify node uses it)\n\nname: bdc-shopops-cover-cascade-prh-visibility\ndescription: |\n  Use when: Executing WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 —\n        extend PRH cover cascade gate + drop Lunar-only filter on cover_images view.\n  Input: WO_ID = WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.\n  Does: (1) reads spec from bdc-xo via gh api cross-repo;\n        (2) edits shopops-api/services/coverResolver.js — adds PRH_UPC_PREFIXES const\n            (Marvel 75960, DC 76194, Image 70985, BOOM 84428, IDW 82771, Dark Horse 76156)\n            sourced from LSPRO_Clean/PrhCoverLookup.gs:27-49, extends looksLikePrhUpc regex\n            to accept those prefixes in addition to ISBN-13 (97[89]);\n        (3) writes migration shopops-api/migrations/<NN>_cover_images_view_remove_distributor_filter.sql\n            that CREATE OR REPLACE VIEW cover_images AS SELECT ... FROM images WHERE\n            subject_type = 'comic_issue' (drops implicit source_distributor='lunar');\n        (4) writes/extends tests/test_cover_resolver.js to exercise Scenarios A-E from WO §11;\n        (5) runs bun tests locally;\n        (6) commits + pushes feature branch + opens PR against master;\n        (7) applies migration to production Supabase via ssh hetzner-prod docker run psql;\n        (8) verifies view redefinition via pg_get_viewdef introspection;\n        (9) Rule 19 runtime grep inside shopops-api container (PRH_UPC_PREFIXES present in /app);\n        (10) curls /api/covers/resolve to confirm cascade returns real cover for test UPC;\n        (11) builds MIXED-class manifest (CODE + schema migration);\n        (12) flips Notion to REVIEW (NEVER DONE — Cauldron grader closes both fields).\n  Repo: bluedevilcollectibles/shopops (master branch). shopops-api/ subdir.\n        Spec lives in bluedevilcollectibles/bdc-xo (docs/work-orders/).\n  parallel_ok: false — production view DDL, serial only.\n  NOT for: any other WO.\n\npolicyFile: harness/policies/agent-behavior.md\nmodel: sonnet\n\nnodes:\n\n  # -------------------------------------------------------------------------\n  # 1. READ SPEC — fetches WO spec from bdc-xo cross-repo\n  # -------------------------------------------------------------------------\n  - id: read-spec\n    bash: |\n      set -euo pipefail\n      WO_ID=\"${WO_ID:-WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01}\"\n\n      # Primary: docs/work-orders/<WO_ID>.md in bdc-xo\n      SPEC_CONTENT=$(gh api \\\n        \"repos/bluedevilcollectibles/bdc-xo/contents/docs/work-orders/${WO_ID}.md?ref=main\" \\\n        --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n\n      # Fallback: date-prefix glob in docs/superpowers/specs/ (Rule 5)\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        GLOB_PATH=$(gh api \\\n          \"repos/bluedevilcollectibles/bdc-xo/contents/docs/superpowers/specs?ref=main\" \\\n          --jq '[.[] | select(.name | test(\"'\"${WO_ID}\"'\")) | .path] | .[0]' \\\n          2>/dev/null || true)\n        if [ -n \"$GLOB_PATH\" ] && [ \"$GLOB_PATH\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh api \\\n            \"repos/bluedevilcollectibles/bdc-xo/contents/${GLOB_PATH}?ref=main\" \\\n            --jq .content 2>/dev/null | base64 -d 2>/dev/null || true)\n        fi\n      fi\n\n      if [ -z \"$SPEC_CONTENT\" ]; then\n        # Fallback 4: GitHub issue body (WO-HARNESS-READ-SPEC-ISSUE-BODY-FALLBACK-01)\n        ISSUE_NUM=$(printf '%s\\n' \"${USER_MESSAGE:-}\" | grep -Eo 'issue=[0-9]+' | head -n 1 | cut -d= -f2 || true)\n        if [ -z \"$ISSUE_NUM\" ]; then\n          ISSUE_NUM=$(gh issue list --repo bluedevilcollectibles/bdc-xo \\\n            --search \"${WO_ID} in:title\" --json number,title \\\n            --jq '.[0].number' 2>/dev/null || true)\n        fi\n        if [ -n \"$ISSUE_NUM\" ] && [ \"$ISSUE_NUM\" != \"null\" ]; then\n          SPEC_CONTENT=$(gh issue view \"$ISSUE_NUM\" --repo bluedevilcollectibles/bdc-xo \\\n            --json body --jq .body 2>/dev/null || true)\n          [ -n \"$SPEC_CONTENT\" ] && SPEC_PATH=\"issues/${ISSUE_NUM}\"\n        fi\n      fi\n      if [ -z \"$SPEC_CONTENT\" ]; then echo \"Spec not found (tried docs/work-orders/, docs/superpowers/specs/ direct, date-prefix glob, and GitHub issue search)\" >&2; exit 1; fi\n\n      printf 'WO_ID=%s\\nSPEC_PATH=bdc-xo:docs/work-orders/%s.md\\nWORKFLOW_ID=%s\\n\\n' \\\n        \"$WO_ID\" \"$WO_ID\" \"$WORKFLOW_ID\"\n      printf -- '--- SPEC ---\\n%s\\n' \"$SPEC_CONTENT\"\n\n  # -------------------------------------------------------------------------\n  # 2. INVENTORY — confirm current cover_images view definition + locate\n  #    next migration number. Best-effort: failures fall back to defaults.\n  # -------------------------------------------------------------------------\n  - id: inventory\n    depends_on: [read-spec]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort introspection\n      set -u\n      echo \"=== migration directory ===\"\n      ls -1 shopops-api/migrations/ 2>/dev/null | tail -20 || echo \"(migrations dir not yet present)\"\n\n      echo \"\"\n      echo \"=== current coverResolver.js — looksLikePrhUpc region ===\"\n      grep -n \"looksLikePrhUpc\\|PRH_UPC_PREFIXES\" shopops-api/services/coverResolver.js 2>/dev/null \\\n        || echo \"(no prior PRH_UPC_PREFIXES — fresh add)\"\n\n      echo \"\"\n      echo \"=== existing cover-resolver tests ===\"\n      ls -1 shopops-api/tests/test_cover_resolver* 2>/dev/null \\\n        || echo \"(no existing test_cover_resolver.js — will create)\"\n\n      echo \"STATUS=ok\"\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 3. PLAN — produce structured plan with exact file paths, migration NN,\n  #    constant content, and test scenarios.\n  # -------------------------------------------------------------------------\n  - id: plan\n    depends_on: [inventory]\n    task_class: medium_work\n    prompt: |\n      Plan the implementation of WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.\n\n      ## Required Output (Markdown)\n\n      - WO ID: WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01\n      - Files to MODIFY:\n        - `shopops-api/services/coverResolver.js` — add PRH_UPC_PREFIXES constant (6 publishers)\n          alongside existing imports near top of file; extend looksLikePrhUpc() to return true\n          for both ISBN-13 AND any UPC starting with one of the 6 prefixes.\n      - Files to CREATE:\n        - `shopops-api/migrations/<NN>_cover_images_view_remove_distributor_filter.sql` —\n          `CREATE OR REPLACE VIEW cover_images AS SELECT ... FROM images WHERE subject_type = 'comic_issue';`\n          (Pick NN = max existing migration number + 1. Inventory output above shows current floor.)\n        - `shopops-api/tests/test_cover_resolver.js` — Vitest/Bun test exercising Scenarios A-E\n          from WO §11. Must include at least 5 assertions. If file exists, EXTEND it; do not\n          replace existing tests.\n      - PRH_UPC_PREFIXES constant exact content (frozen object, source LSPRO_Clean/PrhCoverLookup.gs:27-49):\n        ```js\n        const PRH_UPC_PREFIXES = Object.freeze({\n          '75960': 'Marvel',\n          '76194': 'DC',\n          '70985': 'Image',\n          '84428': 'BOOM!',\n          '82771': 'IDW',\n          '76156': 'Dark Horse',\n        });\n        ```\n      - looksLikePrhUpc extension shape:\n        ```js\n        function looksLikePrhUpc(upc) {\n          if (!upc || typeof upc !== 'string') return false;\n          if (/^97[89][0-9]{10}$/.test(upc)) return true;       // ISBN-13 branch (existing)\n          const prefix = upc.slice(0, 5);\n          return Object.prototype.hasOwnProperty.call(PRH_UPC_PREFIXES, prefix);\n        }\n        ```\n      - Test scenarios (one assertion per scenario minimum):\n        - A: looksLikePrhUpc('75960621516400611') === true (Marvel)\n        - B: looksLikePrhUpc('76194') prefix — covered by Scenario A class\n        - C: looksLikePrhUpc('9781302949686') === true (ISBN-13 trade paperback)\n        - D: looksLikePrhUpc('00000000000000') === false (synthetic non-PRH)\n        - E: PRH_UPC_PREFIXES has exactly 6 keys including Marvel/DC/Image/BOOM!/IDW/Dark Horse\n      - Verification commands (bash-executable):\n        - `grep -c \"75960.*Marvel\\\\|76194.*DC\\\\|70985.*Image\\\\|84428.*BOOM\\\\|82771.*IDW\\\\|76156.*Dark Horse\" shopops-api/services/coverResolver.js` — expected >= 6\n        - `grep -c \"PRH_UPC_PREFIXES\" shopops-api/services/coverResolver.js` — expected >= 2 (declaration + use)\n        - `ls shopops-api/migrations/*_cover_images_view_remove_distributor_filter.sql` — expected: file exists\n        - `cd shopops-api && bun tests/test_cover_resolver.js` — exit code 0\n      - Commit message: `fix(cover): WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 — extend looksLikePrhUpc to 6 PRH publishers + drop Lunar filter on cover_images view`\n      - Branch: `wo/shopops-cover-cascade-prh-visibility-01`\n      - PR base: `master` (NOT main — shopops default is master)\n\n      ## Inventory\n      $inventory.output\n\n      ## Spec\n      $read-spec.output\n\n  # -------------------------------------------------------------------------\n  # 4. IMPLEMENT — edit coverResolver.js, write migration, write tests\n  # -------------------------------------------------------------------------\n  - id: implement\n    depends_on: [plan]\n    task_class: medium_work\n    loop:\n      fresh_context: true\n      until: COMPLETE\n      max_iterations: 5\n      prompt: |\n        Implement WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 per the plan.\n\n        ## Plan\n        $plan.output\n\n        ## Spec\n        $read-spec.output\n\n        ## Execution Rules\n\n        1. Edit `shopops-api/services/coverResolver.js`:\n           - Add `PRH_UPC_PREFIXES` constant (frozen object) near the top of the file,\n             after existing imports/requires but before other top-level functions.\n             6 publishers EXACTLY: Marvel 75960, DC 76194, Image 70985, BOOM! 84428,\n             IDW 82771, Dark Horse 76156. Source: LSPRO_Clean/PrhCoverLookup.gs:27-49.\n           - Modify `looksLikePrhUpc(upc)` (currently at ~lines 39-43) to accept either\n             ISBN-13 (existing `/^97[89][0-9]{10}$/`) OR any UPC whose 5-digit prefix\n             is a key in PRH_UPC_PREFIXES. Do NOT remove the ISBN-13 branch.\n           - Do NOT touch any other cascade tier code. Do NOT remove or restructure\n             prhUrl, findPrhCachedImage, prhApi calls, or the 9 cascade tiers.\n\n        2. Create `shopops-api/migrations/<NN>_cover_images_view_remove_distributor_filter.sql`:\n           - Pick NN = highest existing migration number + 1 (zero-pad to match existing convention).\n           - Body: `CREATE OR REPLACE VIEW cover_images AS SELECT * FROM images WHERE subject_type = 'comic_issue';`\n           - Add a leading comment block documenting WO ID and the Lunar-filter removal rationale.\n           - DO NOT drop or rename lunar_covers — 47 codebase references depend on it.\n\n        3. Create or extend `shopops-api/tests/test_cover_resolver.js`:\n           - At least 5 assertions covering Scenarios A-E from WO §11.\n           - Use real behavior: import `looksLikePrhUpc` from `../services/coverResolver.js`.\n             (If not exported, add it to the module's exports.)\n           - Tests must FAIL meaningfully if behavior regresses, not just check truthy.\n\n        4. Do NOT apply the migration. Do NOT call ssh. Do NOT push to git.\n           The migration-apply + commit nodes downstream handle deploy.\n\n        ## Completion criteria\n        Output <promise>COMPLETE</promise> ONLY when:\n          - shopops-api/services/coverResolver.js contains PRH_UPC_PREFIXES with 6 publishers\n          - looksLikePrhUpc now accepts 5-digit prefix matches in addition to ISBN-13\n          - shopops-api/migrations/<NN>_cover_images_view_remove_distributor_filter.sql exists\n            and contains `CREATE OR REPLACE VIEW cover_images`\n          - shopops-api/tests/test_cover_resolver.js exists with >= 5 real assertions\n          - `cd shopops-api && bun tests/test_cover_resolver.js` exits 0 in local run\n\n  # -------------------------------------------------------------------------\n  # 5. VERIFY-GREP — bash-executable grep assertions (WO §12 stop conditions 1-2)\n  # -------------------------------------------------------------------------\n  - id: verify-grep\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      echo \"=== Stop condition 1: PRH_UPC_PREFIXES has 6 publishers ===\"\n      COUNT=$(grep -c \"75960.*Marvel\\|76194.*DC\\|70985.*Image\\|84428.*BOOM\\|82771.*IDW\\|76156.*Dark Horse\" shopops-api/services/coverResolver.js || true)\n      echo \"publisher-prefix grep count: $COUNT (expected >= 6)\"\n      [ \"$COUNT\" -ge 6 ] || (echo \"FAIL: <6 publisher prefixes present\" >&2; exit 1)\n\n      echo \"\"\n      echo \"=== Stop condition 2: looksLikePrhUpc references PRH_UPC_PREFIXES ===\"\n      USE_COUNT=$(grep -c \"PRH_UPC_PREFIXES\" shopops-api/services/coverResolver.js || true)\n      echo \"PRH_UPC_PREFIXES grep count: $USE_COUNT (expected >= 2)\"\n      [ \"$USE_COUNT\" -ge 2 ] || (echo \"FAIL: PRH_UPC_PREFIXES not referenced (declaration + use both needed)\" >&2; exit 1)\n\n      echo \"\"\n      echo \"=== Migration file present ===\"\n      MIG_FILE=$(ls shopops-api/migrations/*_cover_images_view_remove_distributor_filter.sql 2>/dev/null | head -n 1)\n      [ -n \"$MIG_FILE\" ] || (echo \"FAIL: migration file missing\" >&2; exit 1)\n      echo \"migration: $MIG_FILE\"\n      grep -q \"CREATE OR REPLACE VIEW cover_images\" \"$MIG_FILE\" \\\n        || (echo \"FAIL: migration does not redefine cover_images view\" >&2; exit 1)\n      grep -q \"subject_type\" \"$MIG_FILE\" \\\n        || (echo \"FAIL: migration missing subject_type filter\" >&2; exit 1)\n\n      echo \"\"\n      echo \"=== Test file present ===\"\n      [ -f shopops-api/tests/test_cover_resolver.js ] \\\n        || (echo \"FAIL: test_cover_resolver.js missing\" >&2; exit 1)\n      ASSERTION_COUNT=$(grep -cE \"expect\\(|assert\\(|assert\\.|test\\(|it\\(\" shopops-api/tests/test_cover_resolver.js || true)\n      echo \"test assertions/cases: $ASSERTION_COUNT (expected >= 5)\"\n      [ \"$ASSERTION_COUNT\" -ge 5 ] || (echo \"FAIL: <5 test assertions\" >&2; exit 1)\n\n  # -------------------------------------------------------------------------\n  # 6. VERIFY-TESTS — run the bun tests (Archon container is bun-only per banked memory)\n  # -------------------------------------------------------------------------\n  - id: verify-tests\n    depends_on: [implement]\n    bash: |\n      set -euo pipefail\n      cd shopops-api\n      echo \"=== bun tests/test_cover_resolver.js ===\"\n      bun tests/test_cover_resolver.js\n      echo \"TESTS=PASS\"\n\n  # -------------------------------------------------------------------------\n  # 7. COMMIT-AND-PUSH — idempotent checkout, branch, commit, push\n  # -------------------------------------------------------------------------\n  - id: commit-and-push\n    depends_on: [verify-grep, verify-tests]\n    bash: |\n      set -euo pipefail\n      BASE=\"wo/shopops-cover-cascade-prh-visibility-01\"\n      BRANCH=\"$BASE\"\n      # Idempotent: if branch is locked to another worktree, take a timestamped fork\n      if git worktree list --porcelain | grep -qx \"branch refs/heads/$BRANCH\" \\\n         && [ \"$(git rev-parse --abbrev-ref HEAD)\" != \"$BRANCH\" ]; then\n        BRANCH=\"${BASE}-run-$(date +%Y%m%d%H%M%S)\"\n      fi\n      if git show-ref --quiet \"refs/heads/$BRANCH\"; then\n        git checkout \"$BRANCH\"\n      else\n        git checkout -b \"$BRANCH\"\n      fi\n\n      git add shopops-api/services/coverResolver.js \\\n              shopops-api/migrations/*_cover_images_view_remove_distributor_filter.sql \\\n              shopops-api/tests/test_cover_resolver.js\n\n      if git diff --cached --quiet; then\n        echo \"No staged changes; branch already contains the work\"\n      else\n        git -c user.name=\"John Ranson\" -c user.email=\"admin@bluedevilcollectibles.com\" \\\n          commit -m \"fix(cover): WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 — extend PRH cascade for 6 publishers + drop Lunar filter on cover_images view\n\n        Adds PRH_UPC_PREFIXES constant (Marvel/DC/Image/BOOM/IDW/Dark Horse) sourced from\n        LSPRO_Clean/PrhCoverLookup.gs:27-49 (lost in original Node port). Extends\n        looksLikePrhUpc to accept 5-digit publisher prefixes in addition to ISBN-13.\n\n        Adds migration redefining cover_images view to drop the implicit\n        source_distributor='lunar' filter so the 884 May 2026 PRH ingest rows\n        become visible to Step 1 of the cascade.\n\n        Tests: 5 scenarios (A-E from WO §11) covering ISBN-13, publisher prefixes,\n        synthetic non-PRH UPC, and constant shape.\n\n        Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>\"\n      fi\n      git push -u origin \"$BRANCH\"\n      echo \"BRANCH=${BRANCH}\"\n      echo \"BRANCH_SHA=$(git rev-parse HEAD)\"\n\n  # -------------------------------------------------------------------------\n  # 8. OPEN-PR — idempotent PR creation against master\n  # -------------------------------------------------------------------------\n  - id: open-pr\n    depends_on: [commit-and-push]\n    bash: |\n      set -euo pipefail\n      BRANCH=\"wo/shopops-cover-cascade-prh-visibility-01\"\n      REPO=\"bluedevilcollectibles/shopops\"\n      BASE=\"master\"\n      TITLE=\"fix(cover): WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 — PRH cascade 6 publishers + view filter drop\"\n      BODY_FILE=$(mktemp)\n      {\n        echo \"## Summary\"\n        echo \"Implements **WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01** — PRH cover cascade fix.\"\n        echo \"\"\n        echo \"Two coupled changes both required:\"\n        echo \"1. **Regex gate** — \\`looksLikePrhUpc()\\` only accepted ISBN-13 (97[89]). Comic UPCs\"\n        echo \"   from 6 PRH-distributed publishers fell through to Google CSE / placeholder.\"\n        echo \"2. **View filter** — \\`cover_images\\` view inherits a Lunar-only filter from\"\n        echo \"   \\`lunar_covers\\`, so 884 already-ingested May 2026 PRH rows were invisible to Step 1.\"\n        echo \"\"\n        echo \"## Files\"\n        echo \"- \\`shopops-api/services/coverResolver.js\\` — PRH_UPC_PREFIXES constant + extended gate\"\n        echo \"- \\`shopops-api/migrations/<NN>_cover_images_view_remove_distributor_filter.sql\\` — view redefinition\"\n        echo \"- \\`shopops-api/tests/test_cover_resolver.js\\` — Scenarios A-E\"\n        echo \"\"\n        echo \"## Stop conditions (WO §12)\"\n        echo \"1. grep proves PRH_UPC_PREFIXES has 6 publishers\"\n        echo \"2. grep proves looksLikePrhUpc references the constant\"\n        echo \"3. View redefinition applied to production (post-merge by migration-apply node)\"\n        echo \"4. Cascade returns real cover for UPC 75960621516400611 via /api/covers/resolve\"\n        echo \"5. Regression scenarios pass (Lunar UPC + ISBN-13 + synthetic non-PRH)\"\n        echo \"6. Runtime grep inside shopops-api container (Rule 19)\"\n        echo \"7. Idempotency: same UPC twice = 1 PRH API call\"\n        echo \"\"\n        echo \"## Authoritative source\"\n        echo \"- WO spec: bdc-xo \\`docs/work-orders/WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.md\\`\"\n        echo \"- Publisher prefix table source: \\`LSPRO_Clean/PrhCoverLookup.gs:27-49\\` (GAS-era)\"\n        echo \"\"\n        echo \"Generated with Claude Code\"\n      } > \"$BODY_FILE\"\n\n      existing_pr_url() {\n        gh pr list --repo \"$REPO\" --head \"$BRANCH\" --state open --json url --jq '.[0].url // empty' 2>/dev/null\n      }\n      PR_URL=\"$(existing_pr_url)\"\n      if [ -n \"$PR_URL\" ]; then\n        echo \"PR_URL=$PR_URL\"\n        exit 0\n      fi\n      for attempt in 1 2 3 4 5; do\n        if PR_OUT=$(gh pr create --repo \"$REPO\" --base \"$BASE\" --head \"$BRANCH\" \\\n            --title \"$TITLE\" --body-file \"$BODY_FILE\" 2>&1); then\n          echo \"PR_URL=$PR_OUT\"\n          exit 0\n        fi\n        PR_URL=\"$(existing_pr_url)\"\n        if [ -n \"$PR_URL\" ]; then\n          echo \"PR_URL=$PR_URL\"\n          exit 0\n        fi\n        echo \"open-pr attempt $attempt failed: $PR_OUT\" >&2\n        sleep 10\n      done\n      echo \"open-pr failed after retries; branch pushed at $BRANCH but no PR exists\" >&2\n      exit 1\n\n  # -------------------------------------------------------------------------\n  # 9. MIGRATION-APPLY — apply view redefinition to production Supabase\n  #    via SSH + docker run psql (Hookify-allowed per banked doctrine).\n  #    Best-effort wrapper: SSH/network/auth can legitimately fail; surface STATUS.\n  # -------------------------------------------------------------------------\n  - id: migration-apply\n    depends_on: [open-pr]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort production write step\n      set -u\n      MIG_FILE=$(ls shopops-api/migrations/*_cover_images_view_remove_distributor_filter.sql 2>/dev/null | head -n 1)\n      if [ -z \"$MIG_FILE\" ]; then\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=migration file not found in shopops-api/migrations/\"\n        exit 0\n      fi\n      echo \"=== applying migration $MIG_FILE to production Supabase via hetzner-prod ===\"\n\n      # Stream migration contents over SSH stdin; psql reads from stdin via -f /dev/stdin\n      RESULT=$(ssh -o BatchMode=yes -o ConnectTimeout=15 hetzner-prod \\\n        \"docker exec -i shopops-api sh -c 'psql \\\"\\$DATABASE_URL\\\" -v ON_ERROR_STOP=1 -f /dev/stdin'\" \\\n        < \"$MIG_FILE\" 2>&1)\n      EXIT=$?\n\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"DETAIL=migration applied: $MIG_FILE\"\n        echo \"PSQL_OUT=$RESULT\"\n      elif echo \"$RESULT\" | grep -qiE 'already exists|relation .* already exists'; then\n        echo \"STATUS=skipped_idempotent\"\n        echo \"DETAIL=view already in target state\"\n        echo \"PSQL_OUT=$RESULT\"\n      else\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=$RESULT\"\n      fi\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 10. VERIFY-VIEW — pg_get_viewdef proves the production view no longer\n  #     filters source_distributor='lunar' (WO §12 stop condition 3).\n  # -------------------------------------------------------------------------\n  - id: verify-view\n    depends_on: [migration-apply]\n    bash: |\n      # set -e DELIBERATELY OMITTED — best-effort introspection\n      set -u\n      RESULT=$(ssh -o BatchMode=yes -o ConnectTimeout=15 hetzner-prod \\\n        \"docker exec shopops-api sh -c 'psql \\\"\\$DATABASE_URL\\\" -tAc \\\"SELECT pg_get_viewdef(to_regclass(\\\\\\\"cover_images\\\\\\\"))\\\"'\" 2>&1)\n      EXIT=$?\n      echo \"=== current cover_images view definition ===\"\n      echo \"$RESULT\"\n\n      if [ $EXIT -ne 0 ]; then\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=pg_get_viewdef call failed: $RESULT\"\n        exit 0\n      fi\n\n      if echo \"$RESULT\" | grep -qi \"source_distributor\"; then\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=view STILL filters source_distributor — migration did not take effect\"\n        exit 0\n      fi\n\n      if echo \"$RESULT\" | grep -qi \"subject_type\"; then\n        echo \"STATUS=ok\"\n        echo \"DETAIL=view filters on subject_type only — Lunar restriction removed\"\n      else\n        echo \"STATUS=ok_unexpected_shape\"\n        echo \"DETAIL=view definition does not match expected shape; manual review needed\"\n      fi\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 11. RUNTIME-VERIFY — Rule 19. shopops-api uses COPY at image build time;\n  #     source change must be reflected inside the running container.\n  # -------------------------------------------------------------------------\n  - id: runtime-verify\n    depends_on: [verify-view]\n    bash: |\n      # set -e DELIBERATELY OMITTED — runtime grep can legitimately fail if rebuild pending\n      set -u\n      echo \"=== Rule 19: docker exec shopops-api grep -n PRH_UPC_PREFIXES /app/services/coverResolver.js ===\"\n      RESULT=$(ssh -o BatchMode=yes -o ConnectTimeout=15 hetzner-prod \\\n        \"docker exec shopops-api grep -n 'PRH_UPC_PREFIXES' /app/services/coverResolver.js\" 2>&1)\n      EXIT=$?\n\n      if [ $EXIT -eq 0 ]; then\n        echo \"STATUS=ok\"\n        echo \"RUNTIME_GREP=$RESULT\"\n      elif echo \"$RESULT\" | grep -qiE 'No such file|no such container'; then\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=container or path missing: $RESULT\"\n      else\n        # Rebuild may be pending (PR not merged + image rebuilt yet). Surface, do not fail.\n        echo \"STATUS=pending_rebuild\"\n        echo \"DETAIL=PRH_UPC_PREFIXES not yet in /app — Captain CI must verify after merge + rebuild\"\n        echo \"RUNTIME_GREP_RAW=$RESULT\"\n      fi\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 12. CASCADE-SMOKE — curl /api/covers/resolve for the canary UPC (WO §12 stop condition 4)\n  # -------------------------------------------------------------------------\n  - id: cascade-smoke\n    depends_on: [runtime-verify]\n    bash: |\n      # set -e DELIBERATELY OMITTED — endpoint may 404/timeout/return placeholder pre-rebuild\n      set -u\n      echo \"=== smoke: curl /api/covers/resolve?upc=75960621516400611 ===\"\n      RESULT=$(curl -sS --max-time 20 \\\n        \"https://api.livesellerpro.app/api/covers/resolve?upc=75960621516400611\" \\\n        -H \"X-Shopops-Tenant: bdc\" 2>&1)\n      EXIT=$?\n\n      if [ $EXIT -ne 0 ]; then\n        echo \"STATUS=failed_unexpected\"\n        echo \"DETAIL=curl exited $EXIT: $RESULT\"\n        exit 0\n      fi\n\n      echo \"RAW_RESPONSE=$RESULT\"\n      TIER=$(echo \"$RESULT\" | grep -oE '\"tier\"[[:space:]]*:[[:space:]]*\"[^\"]*\"' | head -n 1 || true)\n      URL_HIT=$(echo \"$RESULT\" | grep -oE 'penguinrandomhouse\\.com' | head -n 1 || true)\n\n      if [ -n \"$URL_HIT\" ]; then\n        echo \"STATUS=ok\"\n        echo \"DETAIL=cascade returned PRH CDN URL — fix verified end-to-end\"\n        echo \"TIER=$TIER\"\n      elif echo \"$RESULT\" | grep -qi 'placeholder'; then\n        echo \"STATUS=pending_rebuild\"\n        echo \"DETAIL=cascade still returning placeholder — image rebuild + container restart likely pending merge\"\n      else\n        echo \"STATUS=ok_unexpected_shape\"\n        echo \"DETAIL=response shape not recognized; manual review needed\"\n      fi\n      exit 0\n\n  # -------------------------------------------------------------------------\n  # 13. BUILD-MANIFEST — MIXED-class manifest (CODE + schema migration)\n  # -------------------------------------------------------------------------\n  - id: build-manifest\n    depends_on: [cascade-smoke]\n    task_class: clerical\n    prompt: |\n      Build Captain CI manifest v2 for WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01.\n\n      WO class: MIXED (source code change + schema migration + runtime verification).\n\n      Required fields (Rule 2 / Three Rules):\n      - WO: WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01\n      - Builder: Cauldron (bdc-shopops-cover-cascade-prh-visibility workflow)\n      - Files modified: shopops-api/services/coverResolver.js\n      - Files created:\n          shopops-api/migrations/<NN>_cover_images_view_remove_distributor_filter.sql,\n          shopops-api/tests/test_cover_resolver.js\n      - Tests: count passing / count total via `cd shopops-api && bun tests/test_cover_resolver.js`\n      - PRs: PR URL from $open-pr.output (commit SHA from $commit-and-push.output)\n      - Merge ancestors: shopops master HEAD (record before/after if PR not yet merged)\n      - Grep assertions (rerunnable):\n          grep -c \"75960.*Marvel\\|76194.*DC\\|70985.*Image\\|84428.*BOOM\\|82771.*IDW\\|76156.*Dark Horse\" shopops-api/services/coverResolver.js  # expected >= 6\n          grep -c \"PRH_UPC_PREFIXES\" shopops-api/services/coverResolver.js                                                                      # expected >= 2\n          ls shopops-api/migrations/*_cover_images_view_remove_distributor_filter.sql                                                            # expected: 1 file\n      - Runtime verification (Rule 19):\n          $runtime-verify.output (STATUS=ok means PRH_UPC_PREFIXES present in /app/services/coverResolver.js;\n                                  STATUS=pending_rebuild means awaiting PR merge + container rebuild — Captain CI must re-check)\n      - Schema migration evidence:\n          $migration-apply.output (STATUS=ok or skipped_idempotent)\n          $verify-view.output (view definition no longer filters source_distributor)\n      - Cascade smoke evidence:\n          $cascade-smoke.output (STATUS=ok = PRH CDN URL returned; STATUS=pending_rebuild = re-check post-merge)\n      - Vercel deployment: N/A (shopops-api is Hetzner Docker, not Vercel)\n      - VALIDATION: PASS (if all STATUS=ok) or VALIDATION: PASS_PENDING_REBUILD (if any node returned pending_rebuild)\n\n      ## Inputs\n      $read-spec.output\n      $inventory.output\n      $plan.output\n      $verify-grep.output\n      $verify-tests.output\n      $commit-and-push.output\n      $open-pr.output\n      $migration-apply.output\n      $verify-view.output\n      $runtime-verify.output\n      $cascade-smoke.output\n\n  # -------------------------------------------------------------------------\n  # 14. FLIP-NOTION — Rule 7. Cauldron grader closes both Status fields after VALIDATED.\n  #     XO/builder NEVER sets DONE — flip to REVIEW only.\n  # -------------------------------------------------------------------------\n  - id: flip-notion\n    depends_on: [build-manifest]\n    task_class: clerical\n    prompt: |\n      Flip Notion WO-SHOPOPS-COVER-CASCADE-PRH-VISIBILITY-01 to REVIEW with the manifest\n      in the Notes property.\n\n      Notion page ID: 361ef4c9-ce00-8168-9df6-d72fe87f657b\n\n      Required Notion changes:\n      - Claude Status -> REVIEW\n      - Status -> Review\n      - Notes -> set to $build-manifest.output verbatim (entire manifest body)\n      - claimed_by: Cauldron (bdc-shopops-cover-cascade-prh-visibility)\n      - claimed_at: today\n\n      DO NOT set Claude Status=DONE. DO NOT set Status=Done. The Cauldron grader\n      (bdc-wo-grader workflow) is the only authorized closer for both fields after\n      its VALIDATED verdict. Setting DONE here = Rule 9 fraud (Three Rules / global CLAUDE.md).\n\n      If Notion MCP unavailable, output `XO_TODO: flip-notion failed, manifest in run log`\n      and exit with the manifest body printed so XO can manually update.\n",
};

// Bundled agent-behavior policies (keyed by `policyFile:` path verbatim) (1 total)
export const BUNDLED_POLICIES: Record<string, string> = {
  "harness/policies/agent-behavior.md": "# BDC Universal Agent Behavior Policy\nVersion: v1.1 (2026-05-24)\nsource: BDC_XO/memory/project_universal-agent-behavior-policy.md\n\n## The Four Locked Principles\n\n1. **Think before building.** Identify assumptions. Ask clarification when behavior is ambiguous. Surface contradictions before coding. Do not silently guess.\n2. **Simplicity first.** Smallest viable change. No frameworks/abstractions/services unless required. No production scaffolding for narrow fixes.\n3. **Surgical changes only.** Modify only files in scope. No reformatting unrelated code. No opportunistic renames. No comment/style/structure cleanup outside scope.\n4. **Goal-driven execution.** Work against explicit success criteria. Stop when stop conditions are met. Do not expand scope after done. If definition of done is missing, flag the WO as incomplete.\n\n## BDC Overlay\n\n- Verify live schema before schema claims (Rule 5)\n- No deploy without John\n- No architecture approval except General\n- No REVIEW without manifest (Rule 9)\n- No production mutation without explicit approval (Rule 20)\n- Tests must assert real behavior (Rule 10)\n- Builder cannot self-approve (Rule 3)\n\n## Environment Awareness (v1.1 — 2026-05-24)\n\n- **Do not hunt for tools that are not in your environment.** Cauldron builders run in a **Linux container**. Before reaching for a runtime, assume only what a Linux build image provides (bash, node, bun, python, git, gh). If a tool is absent, do NOT spend turns/tokens searching for it, installing it, or working around its absence — STOP and note the gap in your output for the operator.\n- **PowerShell (`.ps1`) is operator-side tooling and is NOT available in the builder.** Files like `consume-inbox.ps1`, `publish-wo-spec.ps1`, `fire-wo.ps1`, `Test-CauldronYaml.ps1`, `register-yaml.ps1` run on the operator's Windows machine to DRIVE Cauldron from outside — they are never executed inside a build. If your WO has you AUTHOR a `.ps1`, write it and rely on **static review + operator-side testing** (per the WO's stop conditions); do NOT attempt to run it, do NOT look for `pwsh`/`powershell`, and do NOT treat its absence as a blocker. Note \"PS deliverable authored; operator-side test required\" and continue.\n- **General rule:** a missing tool that the WO never asked you to execute is not a failure. Adapt (static-check instead of run) and proceed; surface the limitation rather than burning the build chasing it.\n\n## Canonical Placement\n\n`BDC_XO/harness/policies/agent-behavior.md` — single source of truth. Every runtime vendors/symlinks/adapts it:\n\n| Runtime | Inclusion path |\n|---------|----------------|\n| Claude Code | CLAUDE.md reference + skill at `.claude/skills/agent-behavior/SKILL.md` |\n| Codex | AGENTS.md import (Codex desktop reads this) |\n| OpenAI reviewer | system/developer prompt fragment in routing.yaml |\n| Haiku helpers | short policy fragment in adapter call |\n| bdc-harness | workflow bootstrap doctrine file, loaded by every workflow run |\n| Future vendor agents | adapter layer wraps the policy |\n\n## How agents should declare loaded\n\nAt session start, agent should declare \"behavior policy v1 loaded\" or demonstrably operate by the principles.\n",
};
