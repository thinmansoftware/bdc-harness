import{beforeEach,expect,test}from'bun:test';import{isTickStale,recordTickHeartbeat,resetTickHeartbeat}from'./deadman';beforeEach(resetTickHeartbeat);test('2 missed intervals healthy, 3 degraded, recovery re-arms',()=>{recordTickHeartbeat(0);expect(isTickStale(1000,2000)).toBe(false);expect(isTickStale(1000,3000)).toBe(true);recordTickHeartbeat(3500);expect(isTickStale(1000,3500)).toBe(false);});
